<!-- cj-doc kind="api-member" level="6" id="stdx.compress.zlib.enum.compresslevel.value-bestspeed" parent="stdx.compress.zlib.enum.compresslevel" -->
# CompressLevel.BestSpeed

[← CompressLevel](index.md)

## 签名

```cangjie role=signature
BestSpeed
```

构造一个压缩速度优先的压缩等级枚举实例，表示压缩速度最快，压缩率相对较低。
