<!-- cj-doc kind="api-member" level="6" id="std.unittest.common.class.configuration.tostring" parent="std.unittest.common.class.configuration" -->
# Configuration.toString

[← Configuration](index.md)

## 签名

```cangjie role=signature
public func toString(): String
```

该对象的字符化对象，当内部对象未实现 ToString 接口时，输出 '<not printable>' 。

## 契约

返回值：

- String - 字符串。
