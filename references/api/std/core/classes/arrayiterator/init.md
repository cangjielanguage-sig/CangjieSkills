<!-- cj-doc kind="api-member" level="6" id="std.core.class.arrayiterator.init" parent="std.core.class.arrayiterator" -->
# ArrayIterator<T>.init

[← ArrayIterator<T>](index.md)

## 签名

```cangjie role=signature
public init(data: Array<T>)
```

给定一个 Array 数组实例，创建其对应的迭代器，用来迭代遍历该数组实例中全部对象。

## 契约

参数：

- data: Array\<T> - 数组实例。
