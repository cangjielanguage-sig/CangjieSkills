# 共享元素转场一镜到底

This directory focuses on implementing shared element transitions, often referred to as "one-shot to the end" animations, within the ArkUI framework. It covers techniques for creating fluid UI interactions where elements seamlessly move between states or pages. Key topics include direct component manipulation without creating new instances and utilizing the `geometryTransition` API for system-managed adjustments. The content includes practical code examples in ArkTS and Cangjie, addressing performance optimization and complex layout transitions. It is suitable for HarmonyOS and ArkUI developers looking to build sophisticated, high-fidelity navigation effects and responsive user interfaces.

## Quick Navigation

**What do you want to learn or implement?**

*   Understand the definition, visual examples, and comparison of transition methods → 共享元素转场一镜到底.md
*   Modify existing components directly without creating new instances for better performance → 不新建组件并直接变化原组件/
*   Use the `geometryTransition` API for system-managed position and size adjustments → 使用geometryTransition共享元素转场/

## Detailed Description

### 共享元素转场一镜到底.md 共享元素转场一镜到底.md
This document defines and explains the implementation of shared element transitions, also known as "one shot to the end" animations, for creating fluid UI interactions. The content covers the definition of the transition effect, visual examples comparing standard and animated transitions, and a comparison table of implementation methods. Key information includes two primary approaches: modifying the original container directly for simple scenarios without route jumps, and using the geometryTransition API within an animateTo closure for system-managed position and size adjustments. Targeted at UI developers, this guide helps select the appropriate transition method based on complexity and performance requirements.

### 不新建组件并直接变化原组件/
This directory focuses on advanced UI animation techniques within the ArkUI framework, specifically targeting scenarios where developers need to modify existing components dynamically without the performance overhead of creating new instances. It covers the implementation of seamless "one-shot" animations, expandable lists, and complex layout transitions using state variables, `animateTo`, and `TransitionEffect`. The content includes practical code examples in both Cangjie and ArkTS, demonstrating how to achieve smooth property transitions like opacity, translation, and height changes directly on the original component. This resource is suitable for HarmonyOS and ArkUI developers looking to optimize rendering performance and create highly fluid, responsive user interfaces through direct component manipulation.

### 使用geometryTransition共享元素转场/
This directory serves as a comprehensive technical resource for implementing shared element transitions within the ArkUI framework, specifically utilizing the `geometryTransition` interface. It is designed to guide developers through the process of creating seamless, fluid UI animations that significantly enhance user experience. The content covers the fundamental definition and internal mechanisms of geometryTransition, clearly distinguishing it from other transition methods, while providing practical examples of binding components with the same ID to enable "one-shot" animations. Additionally, it addresses advanced scenarios by combining these transitions with modal pages, utilizing state variables, `animateTo` for precise timing, and `TransitionEffect` for asymmetric animations. This collection is specifically tailored for HarmonyOS and ArkUI developers—particularly those proficient in the Cangjie language—who possess a solid grasp of state management and are looking to build sophisticated, high-fidelity navigation effects in their applications.