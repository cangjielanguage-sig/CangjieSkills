# cj-huks-key-derivation-overview

This directory serves as the definitive reference for understanding and implementing key derivation functionality within the OpenHarmony HUKS (Harmony Universal KeyStore) framework. It provides an in-depth look at the Key Derivation Functions (KDF) supported by the system, offering developers the necessary guidelines to utilize these cryptographic primitives effectively. The documentation outlines the specific algorithms supported, namely HKDF and PBKDF2, and details their integration with secure hashing algorithms like SHA256, SHA384, and SHA512. Crucially, it defines the operational boundaries of these functions, such as the restriction that derivation can only be performed on keys already managed by HUKS (excluding direct password inputs) and the lack of support for lightweight device profiles. Furthermore, it specifies the resulting key types—AES, HMAC, and SM4—that can be generated for applications running on API level 15 and above. This guide is indispensable for developers and security architects focused on building secure applications, as it clarifies the algorithm compatibility and key management prerequisites essential for maintaining robust security standards within the HarmonyOS ecosystem. It is specifically suitable for backend developers and system integrators who require a deep understanding of cryptographic constraints to ensure their applications function correctly across different device types and security levels.

## Quick Navigation

Use the decision tree below to quickly locate the specific technical details regarding HUKS key derivation capabilities, supported algorithms, and system constraints.

**What do you want to learn?**
*   **Understand the core concepts:** Get an introduction to Key Derivation Functions (KDF) and their specific role within the HUKS architecture → cj-huks-key-derivation-overview.md
*   **Verify algorithm support:** Check the detailed specification table for supported derivation algorithms (HKDF, PBKDF2) and associated digest algorithms (SHA256/384/512) → cj-huks-key-derivation-overview.md
*   **Review system constraints:** Learn about critical usage limitations, including the exclusion of direct passwords and unavailability on lightweight devices → cj-huks-key-derivation-overview.md
*   **Determine key generation outputs:** Identify which key types (AES, HMAC, SM4) can be generated and the minimum API level (15+) required for these operations → cj-huks-key-derivation-overview.md

## Detailed Description

### cj-huks-key-derivation-overview.md cj-huks-key-derivation-overview.md
This reference document outlines the key derivation functionality within the HUKS framework, defining supported algorithms and usage constraints. It covers an introduction to Key Derivation Functions (KDF) and a detailed specification table of supported algorithms. Key takeaways include that derivation is restricted to HUKS-managed keys (excluding direct passwords) and is unavailable on lightweight devices. The document details support for HKDF and PBKDF2 with SHA256/384/512, generating AES, HMAC, or SM4 keys for API level 15+. This guide targets developers implementing cryptographic security who need to understand algorithm compatibility and key management prerequisites.