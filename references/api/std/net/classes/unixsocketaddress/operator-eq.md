<!-- cj-doc kind="api-member" level="6" id="std.net.class.unixsocketaddress.operator-eq" parent="std.net.class.unixsocketaddress" -->
# UnixSocketAddress.==

[← UnixSocketAddress](index.md)

## 签名

```cangjie role=signature
public operator func ==(rhs: UnixSocketAddress): Bool
```

判断两个 UnixSocketAddress 对象是否相等。

## 契约

参数：

- rhs: UnixSocketAddress - 参与比较的 UnixSocketAddress 对象。

返回值：

- Bool - 如果两个 UnixSocketAddress 对象相等，则返回 `true`；否则，返回 `false`。
