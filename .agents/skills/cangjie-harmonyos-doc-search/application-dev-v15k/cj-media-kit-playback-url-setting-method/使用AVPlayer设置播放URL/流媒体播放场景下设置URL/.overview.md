# 流媒体播放场景下设置URL

This directory provides a technical overview and implementation guide for setting media URLs in streaming scenarios using the Cangjie language. It focuses on the AVPlayer component and MediaKit, covering essential tasks such as playing standard HTTP/HTTPS resources, managing HLS (m3u8) streams for live or VOD, and customizing HTTP headers for authentication. The content also explores advanced use cases like using local RAW file descriptors and parsing local m3u8 files within the application sandbox to trigger online playback. These resources are tailored for Cangjie developers and multimedia engineers seeking to build robust streaming applications with flexible resource handling capabilities.

## Quick Navigation

**What do you want to learn or implement?**

*   Learn how to configure AVPlayer for standard HTTP/HTTPS resources, handle HLS (m3u8) streams, customize HTTP request headers for authentication, and utilize local RAW file descriptors → 流媒体播放场景下设置URL_1.md
*   Discover how to play online streaming media by parsing a local m3u8 file located within the application sandbox using file handles → 流媒体播放场景下设置URL_2.md

## Detailed Description

The following files provide specific technical guidance and code examples for implementing these URL configurations in Cangjie.

### 流媒体播放场景下设置URL_1.md 流媒体播放场景下设置URL_1.md
This technical guide demonstrates how to configure media URLs for the AVPlayer component in Cangjie across various streaming scenarios. It details four specific use cases: playing standard HTTP/HTTPS resources, handling HLS (m3u8) streams for live or VOD, customizing HTTP request headers for authentication, and utilizing local RAW file descriptors to trigger online stream playback. Key takeaways include using createMediaSourceWithUrl for advanced configurations like headers and PlaybackStrategy for buffering control, as well as constructing fd:// URLs via resourceManager.getRawFd. This guide targets Cangjie developers implementing multimedia features who need to handle diverse network and local resource configurations.

### 流媒体播放场景下设置URL_2.md 流媒体播放场景下设置URL_2.md
This technical guide explains how to play online streaming media by parsing a local m3u8 file located within the application sandbox. The document details the process of obtaining a file handle using fs.open, constructing a file descriptor URL (fdUrl), and configuring HTTP headers for the request. Key steps include creating an AVPlayer instance, initializing a MediaSource with the constructed URL and headers, and setting a PlaybackStrategy for buffering. Intended for developers using the Cangjie language and MediaKit, this tutorial provides code examples for handling complex streaming scenarios involving local manifest files and network resources.