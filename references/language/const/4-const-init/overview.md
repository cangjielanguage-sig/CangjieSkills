<!-- cj-doc kind="guide-leaf" level="5" id="language.const.4-const-init.overview" parent="language.const.4-const-init" -->
# 概述与共同规则

[← 4. const init](index.md)

如果一个 `struct` 或 `class` 定义了 `const` 构造器，那么这个 `struct`/`class` 实例可以用在 const 表达式中。
