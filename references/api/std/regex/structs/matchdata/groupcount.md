<!-- cj-doc kind="api-member" level="6" id="std.regex.struct.matchdata.groupcount" parent="std.regex.struct.matchdata" -->
# MatchData.groupCount

[← MatchData](index.md)

## 签名

```cangjie role=signature
public func groupCount(): Int64
```

获取捕获组的个数。

## 契约

返回值：

- Int64 - 捕获组的个数。
