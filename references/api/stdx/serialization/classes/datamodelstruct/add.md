<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.datamodelstruct.add" parent="stdx.serialization.serialization.class.datamodelstruct" -->
# DataModelStruct.add

[← DataModelStruct](index.md)

## 签名

```cangjie role=signature
public func add(fie: Field): DataModelStruct
```

添加数据 `fie` 到 DataModelStruct 中。

## 契约

参数：

- fie: Field - 传入的 Field 类型的数据。

返回值：

- DataModelStruct - 得到新的 DataModelStruct。
