# argopt_package_overview

This directory contains the comprehensive API overview documentation for the `std.argopt` package, a specialized library designed for parsing command-line argument strings into structured names and values. It thoroughly covers the functional definitions of various argument types, distinguishing between short options, long options, short-prefixed long options, and non-options, while also explaining special delimiter rules. The documentation details the primary `parseArguments` function, the deprecated `ArgOpt` class, enums for `ArgumentMode` and `ArgumentSpec`, the `ParsedArguments` struct, and exception handling mechanisms. This resource is suitable for developers implementing command-line interfaces who need a deep understanding of argument specifications and parsing logic to build robust applications. By providing a clear breakdown of the API, it ensures that developers can effectively integrate argument parsing into their software projects. It serves as the foundational reference for understanding the package's architecture and operational behavior.

## Quick Navigation

What do you want to learn?

*   Understand the complete API overview and parsing capabilities for command-line arguments → argopt_package_overview.md
*   Review specific argument types, delimiter rules, and exception handling strategies → argopt_package_overview.md
*   Access references for core functions like `parseArguments`, enums, and data structures → argopt_package_overview.md

## Detailed Description

### argopt_package_overview.md argopt_package_overview.md
This document serves as the API overview for the std.argopt package, detailing its capabilities for parsing command-line argument strings into names and values. It covers functional definitions of argument types, distinguishing between short options, long options, short-prefixed long options, and non-options, while explaining special delimiter rules. The content provides a comprehensive API list including the primary parseArguments function, the deprecated ArgOpt class, enums for ArgumentMode and ArgumentSpec, the ParsedArguments struct, and exception handling. This reference guide targets developers implementing command-line interfaces who need to understand argument specifications and parsing logic. It acts as the essential manual for navigating the package's components, ensuring users can correctly implement and manage command-line arguments within their applications.