# class_ConfigureMock

This directory contains comprehensive API reference documentation for the Cangjie `ConfigureMock` class, a fundamental utility designed to facilitate the configuration of mock objects within modern testing frameworks. It provides detailed technical specifications for static methods and functions essential for intercepting and stubbing various types of class members, including property getters, setters, and standard member functions. The documentation covers critical implementation details such as argument matchers, stub call expressions, reference tokens, and the specific selector objects returned for configuration. This resource is specifically tailored for Cangjie developers and QA engineers who require precise control over object behavior during unit testing, enabling the simulation of complex scenarios and the isolation of dependencies to ensure code reliability and correctness. By mastering these tools, developers can effectively control execution flows and verify interactions within their test suites, ultimately leading to more robust and maintainable software.

## Quick Navigation

**What do you want to do?**

*   **Configure property getters or stub standard member methods** → class_ConfigureMock_3more.md
    *   Access API reference for `stubGetter` and `stubFunction` to manage getters and methods using selectors.
*   **Intercept and stub property setter calls** → static_func_stubSetterT_fae19e1e.md
    *   Access API reference for `stubSetter` to manage property setters and `SetterActionSelector` configuration.

## Detailed Description

The following files provide the specific API details for the components mentioned above, including function signatures, parameters, and return types.

### class_ConfigureMock_3more.md class_ConfigureMock_3more.md
This document serves as API reference documentation for the Cangjie class `ConfigureMock`, designed to facilitate the configuration of mock objects within a testing framework. The content details two primary static methods: `stubGetter` for intercepting property getter calls and `stubFunction` for stubbing standard member methods. Key information includes the parameters required for these methods, such as stub call expressions, argument matchers, reference tokens, and line numbers, which are used to return specific selector objects like `GetterActionSelector` and `MethodActionSelector`. This documentation is intended for Cangjie developers implementing unit tests who need to simulate object behavior and control method execution flows.

### static_func_stubSetterT_fae19e1e.md static_func_stubSetterT_fae19e1e.md
This document provides API reference details for the static function `stubSetter`, designed to create operator objects for inserting stub code into property Setter methods within a testing or mocking framework. The content covers the function signature, a description of its functionality, a detailed list of parameters including type capture and argument matching, and the return value type. Key information includes the requirement for a stub call expression, an argument matcher, and the target property name, ultimately returning a `SetterActionSelector` to configure the stub behavior. This technical reference is intended for developers writing unit tests or using mocking libraries who need to simulate or intercept property setter calls.