<!-- cj-doc kind="api-member" level="7" id="std.core.interface.hasher.write.write-f06662bdc6" parent="std.core.interface.hasher.write" -->
# Hasher.func write(UInt64)

[← Hasher.write](index.md)

## 签名

```cangjie role=signature
mut func write(value: UInt64): Unit
```

通过该函数把想要哈希运算的 UInt64 值传入，然后进行哈希组合运算。

## 契约

参数：

- value: UInt64 - 待运算的值。
