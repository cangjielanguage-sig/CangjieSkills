# 常见问题

This directory provides technical guides and best practices for Cangjie developers, focusing on resolving common issues related to state management and UI responsiveness. It covers critical topics such as avoiding infinite re-render loops caused by modifying state variables in the build method, preventing memory leaks through proper callback lifecycle management, and troubleshooting arrow functions that fail to update the UI. Key concepts include `@State`, `@Publish`, `@Observed`, and the `aboutToDisappear` lifecycle. This resource is suitable for application developers using the Cangjie language who want to optimize performance and ensure stable UI behavior.

## Quick Navigation

**What do you want to learn?**

*   Prevent infinite loops and memory leaks in state management? → 不允许在build里改状态变量_2more.md 不允许在build里改状态变量
*   Resolve issues where arrow functions fail to update UI state? → 使用箭头函数改变状态变量未生效_2more.md 使用箭头函数改变状态变量未生效

## Detailed Description

### 不允许在build里改状态变量_2more.md 不允许在build里改状态变量_2more.md
This guide provides essential state management best practices for Cangjie developers to prevent runtime errors and memory leaks. It covers the prohibition of modifying state variables within the build method and the proper lifecycle management of callbacks. Key takeaways include that changing state variables in build triggers infinite re-render loops, leading to app freezes. Additionally, developers must unregister callbacks in aboutToDisappear to release component instances and avoid memory leaks caused by captured this references. This document targets application developers using the Cangjie language and state management frameworks.

### 使用箭头函数改变状态变量未生效_2more.md 使用箭头函数改变状态变量未生效_2more.md
This technical guide addresses common state management pitfalls in Cangjie UI development, specifically regarding arrow functions and UI refresh mechanisms. The document is divided into two main sections: resolving issues with arrow functions not updating state variables by explicitly passing the view model, and explaining that state variables only trigger refreshes in directly bound UI components. Key takeaways include understanding `this` context in arrow functions and distinguishing between value copying in simple types versus reference behavior in nested objects. This tutorial targets Cangjie developers working with `@State`, `@Publish`, and `@Observed` decorators to ensure proper UI responsiveness.