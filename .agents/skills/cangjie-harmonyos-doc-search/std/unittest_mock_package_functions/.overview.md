# unittest_mock_package_functions

This directory serves as the definitive API reference for the `unittest_mock_package_functions` module, specifically designed for the Cangjie programming language. It provides comprehensive documentation on the core functions required to implement sophisticated unit testing strategies through the use of mock and spy objects. The content thoroughly explains the mechanics behind the basic mock constructor, which generates objects devoid of default logic, as well as the advanced mock constructor that supports stub modes for pre-defined behaviors. Additionally, it details the spy constructor, which allows developers to wrap real instances to maintain original functionality while enabling selective overrides. By leveraging these tools, developers can isolate components, simulate complex dependencies, and ensure code reliability. This resource is indispensable for software developers, QA engineers, and testers who are focused on building robust, maintainable, and high-quality Cangjie applications through rigorous unit testing and behavior verification.

## Quick Navigation

What is your goal with the unittest mock package? Select the option below that best matches your current testing requirements.

*   **Generate Mocks**: You want to create mock objects from scratch that have no default behavior and require you to specify actions using `@On` → unittest_mock_package_functions.md
*   **Configure Stubs**: You need to use mock constructors that accept stub modes to pre-configure behaviors or return values for specific test cases → unittest_mock_package_functions.md
*   **Monitor Real Objects**: You want to create spy objects that wrap real instances, preserving their default logic but allowing you to override or track specific interactions → unittest_mock_package_functions.md
*   **General API Review**: You are looking for a complete overview of the available functions for creating test doubles in Cangjie → unittest_mock_package_functions.md

## Detailed Description

### unittest_mock_package_functions.md unittest_mock_package_functions.md
This document serves as an API reference for core functions within the unittest mock package, detailing how to create mock and spy objects in Cangjie. It covers three primary functions: the basic mock constructor, the mock constructor accepting stub modes, and the spy constructor. Key information includes that mock functions generate objects with no default implementation, allowing behavior specification via @On, while spy wraps real instances to preserve default implementations but permit overrides. This guide targets developers implementing unit tests who need to simulate or monitor object behavior using stub modes and spies.