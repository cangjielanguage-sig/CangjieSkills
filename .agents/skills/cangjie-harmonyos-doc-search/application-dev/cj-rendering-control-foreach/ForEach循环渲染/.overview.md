# ForEach循环渲染

This directory provides comprehensive documentation and tutorials on the ForEach component within the ArkUI framework, specifically using the Cangjie language. It covers the core mechanisms of loop rendering, including key generation rules, initial and non-initial rendering logic, and dynamic data source management. Key topics include component lifecycle, state management with decorators like @State, and performance optimization strategies. It also highlights common anti-patterns to avoid. This resource is suitable for ArkUI developers looking to implement efficient, dynamic lists and optimize UI rendering performance.

## Quick Navigation

**What do you want to learn?**

*   Understand the basic interface and initial rendering logic → ForEach循环渲染_3more.md, 首次渲染.md
*   Handle dynamic data changes and non-initial updates → 数据源数组项发生变化.md, 非首次渲染_3more.md
*   Optimize performance and follow best practices → 使用建议.md
*   Review common anti-patterns and warnings → [不推荐案例]

## Detailed Description

### ForEach循环渲染_3more.md ForEach循环渲染_3more.md
This document outlines the ForEach interface for loop rendering array data within the ArkUI framework. It covers key generation rules, explaining how the keyGenerator function creates unique identifiers to track component changes, and component creation rules for initial and subsequent renders. Key takeaways include the necessity of unique keys to prevent UI errors and the requirement for parent container components like List. Intended for developers, this API documentation ensures efficient dynamic UI updates and highlights the consequences of duplicate keys.

### 使用建议.md 使用建议.md
This document provides usage recommendations for the ForEach component to optimize rendering performance and ensure stability. It covers key generation strategies, handling basic versus object data types, and usage within container components like List and Grid. Key takeaways include using unique IDs as keys instead of indices to prevent performance loss and unexpected rendering, and converting basic data types to objects with IDs for dynamic sources. The guide strictly advises against mixing ForEach with LazyForEach in scrollable containers and clarifies that the index parameter is primarily for modifying immutable data sources. Intended for UI developers, this guide offers best practices for efficient list rendering and component lifecycle management. Keywords: ForEach, key generation, rendering performance, LazyForEach, UI optimization, ArkUI.

### 数据源数组项发生变化.md 数据源数组项发生变化.md
This tutorial demonstrates how to handle dynamic data source changes in ArkUI lists, specifically when adding or removing items. It covers the implementation of a paginated list using Cangjie code, focusing on the ForEach component, ObservedArrayList, and state management decorators like @State and @Prop. The document emphasizes using unique object IDs as keys in ForEach to ensure correct rendering during array modifications. It illustrates triggering data updates via scroll events (onReachEnd) and how the framework detects changes to re-render the UI. This guide targets ArkUI developers needing to implement dynamic lists with variable data lengths, such as news feeds or infinite scrolling interfaces.

### 非首次渲染_3more.md 非首次渲染_3more.md
This guide explains the rendering mechanism of the ForEach component during non-initial updates in ArkUI development. It covers the logic of key generation for component reuse, data source mutation handling, and specific usage scenarios like skeleton screens. The document details that ForEach reuses existing components if keys match and creates new ones if they do not, illustrated by modifying array items. It also demonstrates rendering static lists for loading states. This tutorial targets ArkUI developers using Cangjie who need to optimize list rendering and manage state changes efficiently.

### 首次渲染.md 首次渲染.md
This document explains the initial rendering mechanism of the ForEach component in ArkUI, specifically focusing on key generation and component creation. It covers the standard rendering process with unique keys and the undefined behavior when duplicate keys occur. The document demonstrates that ForEach uses a keyGeneratorFunc to create unique keys for array items. Crucially, it warns that if keys are identical, the framework does not create new components for subsequent items, resulting in missing UI elements. This guide targets ArkUI developers using the Cangjie language who need to understand list rendering logic and state management to ensure correct UI display.

### 不推荐案例
This directory serves as a repository of anti-patterns and technical warnings specifically designed for ArkUI developers working with the Cangjie language. It focuses on common mistakes made when implementing the `ForEach` component, particularly regarding the `keyGenerator` parameter. The content covers critical issues such as unexpected rendering results caused by incorrect key usage and performance degradation resulting from omitted key generation rules. By analyzing these negative cases, developers can gain a deeper understanding of component lifecycle, state management, and rendering efficiency. This resource is essential for any developer looking to write robust, high-performance ArkUI code and avoid subtle functional bugs.