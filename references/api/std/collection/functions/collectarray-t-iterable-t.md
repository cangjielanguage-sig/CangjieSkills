<!-- cj-doc kind="api-member" level="5" id="std.collection.func.collectarray-t-iterable-t" parent="std.collection" -->
# collectArray<T>(Iterable<T>)

[← std.collection](../index.md)

## 签名

```cangjie role=signature
public func collectArray<T>(it: Iterable<T>): Array<T>
```

将一个迭代器转换成 Array 类型。

## 契约

参数：

- it: Iterable\<T> - 给定的迭代器。

返回值：

- Array\<T> - 返回一个数组。
