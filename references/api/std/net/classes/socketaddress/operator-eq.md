<!-- cj-doc kind="api-member" level="6" id="std.net.class.socketaddress.operator-eq" parent="std.net.class.socketaddress" -->
# SocketAddress.==

[← SocketAddress](index.md)

## 签名

```cangjie role=signature
public operator func ==(rhs: SocketAddress): Bool
```

判断两个 SocketAddress 对象是否相等。

## 契约

参数：

- rhs: SocketAddress - 参与比较的 SocketAddress 对象。

返回值：

- Bool - 如果两个 SocketAddress 对象相等，则返回 `true`；否则，返回 `false`。
