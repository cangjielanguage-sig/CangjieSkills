# class_LazySeqT

This directory contains comprehensive API reference documentation for the `LazySeq` class within the Cangjie programming language. It focuses on an immutable, lazily evaluated sequence type designed for high-performance functional programming. The documentation covers core concepts such as lazy evaluation, memoization, and O(1) complexity for concatenation operations. It details instance methods for sequence manipulation (e.g., map, append, prepend) and static utility functions for constructing sequences from various sources like Iterables and Arrays. This resource is essential for Cangjie developers seeking to implement efficient, immutable data structures and leverage functional programming patterns in their software projects.

## Quick Navigation

**What do you want to learn?**

*   Understand the core LazySeq class definition, its inheritance from Iterable, and instance methods for sequence manipulation → class_LazySeqT_12more.md
*   Learn how to use static utility functions to construct LazySeq instances from collections or interleave multiple sequences → static_func_mixLazySeqTLaz_3more.md

## Detailed Description

The following files provide in-depth technical specifications for the LazySeq class and its associated utilities.

### class_LazySeqT_12more.md class_LazySeqT_12more.md
This document serves as API reference documentation for the LazySeq class in the Cangjie language, detailing an immutable, lazily evaluated sequence type. It covers class definition, constructors, instance methods for sequence manipulation like append, concat, map, and prepend, as well as static methods for mixing multiple sequences. Key concepts include lazy evaluation and memoization, where values are computed during iteration. The class inherits from Iterable and supports functional operations like mapping and interleaving sequences with O(1) complexity for concatenation. Targeted at Cangjie developers, this reference guide provides the necessary syntax and parameters for implementing efficient, immutable data structures in their applications.

### static_func_mixLazySeqTLaz_3more.md static_func_mixLazySeqTLaz_3more.md
This document serves as API reference documentation for static utility functions within the LazySeq class, specifically focusing on sequence construction and manipulation. It details three specific functions: the `mix` function for interleaving five separate LazySeq instances, and two overloaded `of` functions for creating LazySeq objects from Iterable collections or Arrays. Key information includes the function signatures, parameters, and return types, highlighting how `mix` combines sequences and how `of` acts as a factory constructor. This reference is intended for developers working with the Cangjie programming language who need to generate or combine lazy sequences efficiently.