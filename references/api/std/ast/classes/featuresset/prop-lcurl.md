<!-- cj-doc kind="api-member" level="6" id="std.ast.class.featuresset.prop-lcurl" parent="std.ast.class.featuresset" -->
# FeaturesSet.lCurl

[← FeaturesSet](index.md)

## 签名

```cangjie role=signature
public mut prop lCurl: Token
```

获取或设置在 FeaturesSet 节点里的 `{` 。

类型：Token

## 异常

- ASTException - 当配置的不是 `{` 时抛出异常。

