<!-- cj-doc kind="api-member" level="6" id="std.unittest.prop_test.class.lazyseq.append" parent="std.unittest.prop_test.class.lazyseq" -->
# LazySeq<T>.append

[← LazySeq<T>](index.md)

## 签名

```cangjie role=signature
public func append(element: T): LazySeq<T>
```

增加一个元素。

## 契约

参数：

- element: T - 被增加的元素。

返回值：

- LazySeq\<T> - 增加元素后的序列。
