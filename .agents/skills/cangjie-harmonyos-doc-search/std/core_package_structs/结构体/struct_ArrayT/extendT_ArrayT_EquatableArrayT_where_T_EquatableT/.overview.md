# extendT_ArrayT_EquatableArrayT_where_T_EquatableT

This directory provides API documentation for a specific extension of the generic Array type within the Cangjie programming language. It applies to arrays where the element type `T` satisfies the Equatable constraint, enabling advanced comparison and manipulation capabilities. The documentation covers a comprehensive set of tools, including search functions like `indexOf` and `lastIndexOf`, boundary modification methods such as `removePrefix` and `trim`, and operator overloads for equality checks. It is designed for Cangjie developers seeking to perform precise element lookups, structural modifications, and equality comparisons on arrays of equatable items.

## Quick Navigation

**What do you want to do?**

*   Check for element existence or find the index of elements/sub-arrays → extendT_ArrayT_EquatableAr_5more.md
*   Locate elements starting from a specific index or search from the end (`lastIndexOf`) → func_indexOfT_Int64_5more.md
*   Remove or trim array boundaries (prefix/suffix) and check inequality → func_removePrefixArrayT_7more.md
*   Compare two arrays for equality using the `==` operator → operator_func_ArrayT.md

## Detailed Description

### extendT_ArrayT_EquatableAr_5more.md extendT_ArrayT_EquatableAr_5more.md
This API documentation details the extension of the generic Array type to implement the Equatable interface in the Cangjie language, enabling equality operations. The document covers four primary functions: `contains` for checking element existence, and three `indexOf` overloads for locating single elements or sub-arrays, including a variant that starts searching from a specific index. Key information includes the use of `Option<Int64>` for return values to handle cases where elements are not found, and a specific note regarding potential ambiguity with Int64 types in variadic syntax. This reference guide targets Cangjie developers who need to perform search and comparison operations on arrays containing equatable elements.

### func_indexOfT_Int64_5more.md func_indexOfT_Int64_5more.md
This API reference document details array search functions in the Cangjie programming language, specifically focusing on locating elements and sub-arrays. The main sections cover the `indexOf` function with a start index and four overloads of `lastIndexOf` for both single elements and sub-arrays, including variations with start indices. Key information includes that all functions return `Option<Int64>`, yielding `None` if the target is not found. The documentation explicitly defines boundary handling for the `fromIndex` parameter, where negative values default to zero and out-of-bounds values result in `None`. Intended for Cangjie developers, this guide provides the necessary syntax and logic for implementing search operations within array data structures.

### func_removePrefixArrayT_7more.md func_removePrefixArrayT_7more.md
This document provides API reference documentation for array manipulation methods in the Cangjie programming language. It covers functions for removing specific sequences from the start or end of an array (`removePrefix`, `removeSuffix`) and trimming elements based on predicates or a set of values (`trimStart`, `trimEnd`). Additionally, it documents the inequality operator (`!=`) for array comparison. Key information includes function signatures, parameter descriptions, return values, and executable code examples illustrating how these methods return modified array slices. This guide is intended for Cangjie developers looking to perform precise array modifications and comparisons.

### operator_func_ArrayT.md operator_func_ArrayT.md
This document provides API reference details for the equality operator overload for the Array data structure in the Cangjie programming language. It covers the function signature, functionality description, input parameters, and return values for the `==` operator. The key information is that the operator determines equality by verifying that every element within two Array instances matches. It accepts a single parameter of type Array and returns a Boolean value indicating the result. This reference material is intended for developers using the Cangjie language who need to compare Array instances for equality in their code.