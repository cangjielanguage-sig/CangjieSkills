<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.struct.httpstatuscode.field-status_gateway_timeout" parent="stdx.net.http.struct.httpstatuscode" -->
# HttpStatusCode.STATUS_GATEWAY_TIMEOUT

[← HttpStatusCode](index.md)

## 签名

```cangjie role=signature
public static const STATUS_GATEWAY_TIMEOUT: UInt16 = 504
```

从上游服务器（URI 标识出的服务器，例如 HTTP、FTP、LDAP）或者辅助服务器（例如 DNS）收到响应超时。

## 契约

类型：UInt16
