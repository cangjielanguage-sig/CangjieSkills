<!-- cj-doc kind="api-member" level="6" id="std.ast.class.qualifiedtype.prop-identifier" parent="std.ast.class.qualifiedtype" -->
# QualifiedType.identifier

[← QualifiedType](index.md)

## 签名

```cangjie role=signature
public mut prop identifier: Token
```

获取或设置 QualifiedType 节点成员的标识符，如 `var a : T.a` 中的 `a`。

## 契约

类型：Token
