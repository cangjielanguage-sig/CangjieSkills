<!-- cj-doc kind="api-member" level="6" id="std.ast.class.tokens.init" parent="std.ast.class.tokens" -->
# Tokens.init

[← Tokens](index.md)

本页汇总 3 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 Tokens 对象。

## init(Array<Token>)

### 签名

```cangjie role=signature
public init(tokArray: Array<Token>)
```

构造一个 Tokens 对象。

### 契约

参数：

- tokArray: Array\<Token> - 一组包含 Token 的 Array 类型。

## init(ArrayList<Token>)

### 签名

```cangjie role=signature
public init(tokArrayList: ArrayList<Token>)
```

构造一个 Tokens 对象。

### 契约

参数：

- tokArrayList: ArrayList\<Token> - 一组包含 Token 的 ArrayList 类型。
