# extendT_IteratorT

This directory contains comprehensive API reference documentation for extending the Iterator type in the Cangjie programming language. It covers a wide range of functional-style operations for data sequence manipulation, including intermediate operations like `map`, `filter`, and `flatMap`, as well as terminal operations like `reduce`, `count`, and `fold`. The documentation details method signatures, parameters, return values, and includes verified code examples. It is suitable for Cangjie developers looking to perform complex data transformations, control iteration flow, and manage collection traversals efficiently.

## Quick Navigation

What do you want to do?

-   **Check element conditions or existence?**
    -   Verify if all or any elements meet criteria → extendT_IteratorT_5more.md
    -   Check if the iterator is empty or if no elements match → func_isEmpty_5more.md
    -   Retrieve the first element → func_count_5more.md
-   **Transform or filter data sequences?**
    -   Filter elements or combine filtering with mapping → func_count_5more.md
    -   Map elements or flatten nested structures → func_flatMapRT_-_IteratorR_5more.md, func_isEmpty_5more.md
-   **Aggregate or accumulate values?**
    -   Count elements or find the last one → func_count_5more.md, func_isEmpty_5more.md
    -   Reduce or fold sequences into a single value → func_flatMapRT_-_IteratorR_5more.md, func_isEmpty_5more.md
-   **Control iteration flow or slicing?**
    -   Skip, step, or take specific numbers of elements → func_skipInt64_3more.md
-   **Combine multiple iterators?**
    -   Concatenate sequences or zip them into tuples → extendT_IteratorT_5more.md, func_zipRIteratorR.md
-   **Perform side effects or advanced traversal?**
    -   Execute actions per element or insert separators → func_flatMapRT_-_IteratorR_5more.md

## Detailed Description

### extendT_IteratorT_5more.md extendT_IteratorT_5more.md
This document serves as API reference documentation for extending the Iterator type in the Cangjie programming language. It details specific iterator methods including all, any, at, and concat, distinguishing between intermediate operations that create new iterators and terminal operations that consume elements. Each method entry provides the function signature, description, parameters, return values, and verified Cangjie code examples demonstrating usage and output. The target audience is Cangjie developers seeking to manipulate data sequences and perform functional-style operations on collections.

### func_count_5more.md func_count_5more.md
This document provides API reference documentation for specific iterator utility methods in the Cangjie programming language, detailing their functionality, parameters, and return values. It covers five key functions: count for element tallying, enumerate for indexed iteration, filter for conditional selection, filterMap for combined filtering and mapping, and first for retrieving the head element. Key takeaways include the consumption behavior of count and first, which empty the iterator, and the lazy evaluation nature of filter and filterMap which return new iterators. The document includes verified Cangjie code examples demonstrating usage and expected output. Targeted at Cangjie developers using iterators, this reference requires understanding of generic types, Option enums, and lambda expressions.

### func_flatMapRT_-_IteratorR_5more.md func_flatMapRT_-_IteratorR_5more.md
This document provides API reference documentation for five advanced methods within the Cangjie Iterator class. It covers the flatMap, fold, forEach, inspect, and intersperse functions, detailing their syntax, parameters, and return values. Key concepts include mapping and flattening sequences, accumulating values with an initial state, executing side effects, and inserting separators between elements. Each function is accompanied by verified code examples demonstrating practical usage and output. This technical reference is intended for Cangjie developers who need to perform complex data transformations and traversals on iterator objects.

### func_isEmpty_5more.md func_isEmpty_5more.md
This document serves as API reference documentation for specific methods of the Cangjie Iterator class. It details the functionality, parameters, and return values for five key methods: isEmpty, last, map, none, and reduce. Important concepts include the consumption behavior of iterators, where methods like isEmpty and last consume elements to determine results, while map transforms elements and reduce aggregates them from left to right. The documentation is intended for Cangjie developers who need to manipulate collections and requires familiarity with functional programming patterns and the Option type.

### func_skipInt64_3more.md func_skipInt64_3more.md
This document serves as API reference documentation for three specific manipulation methods of the Iterator class in the Cangjie programming language. It details the skip, step, and take functions, each accepting an Int64 parameter to control element traversal. Key information includes that skip bypasses initial elements, step skips elements between iterations, and take extracts a specific number of elements. All methods return a new Iterator and throw IllegalArgumentException for invalid negative counts, with verified code examples provided. This technical reference is intended for Cangjie developers seeking to control iteration flow and data processing logic.

### func_zipRIteratorR.md func_zipRIteratorR.md
This document serves as API reference documentation for the Cangjie zip function, which merges two iterators into a single iterator of tuples. The content includes the function signature, a description of its behavior, parameter definitions, and return value specifications. Key information highlights that the resulting iterator's length depends on the shorter input iterator, producing elements as tuples of type (T, R). A practical code example demonstrates merging two arrays and iterating through the result. This guide is intended for Cangjie developers looking to combine data streams or synchronize iteration over multiple collections.