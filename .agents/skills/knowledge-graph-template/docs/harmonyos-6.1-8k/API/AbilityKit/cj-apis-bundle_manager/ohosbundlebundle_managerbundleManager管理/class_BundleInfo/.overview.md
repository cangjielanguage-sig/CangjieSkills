# class_BundleInfo

本目录包含仓颉语言中 `BundleInfo` 类的 API 参考文档，专为鸿蒙应用开发设计。它涵盖了用于封装应用包元数据的核心结构，包括应用名称、版本号、权限状态、安装时间及模块配置等。文档详细介绍了通过 `getBundleInfoForSelf` 接口访问的只读属性，支持分布式版本兼容性检查。适用于需要通过编程方式查询和管理应用包信息、签名详情及运行时配置的鸿蒙开发者。

## 快速导航

您想了解什么？

- 了解类的定义、用途及核心只读属性（如应用名称、版本、模块信息） → class_BundleInfo_8more_71ca9886.md
- 查看具体的属性详情，包括权限、路由表、签名信息及供应商元数据 → let_reqPermission_8more_4f25269f.md

## 详细描述

### class_BundleInfo_8more_71ca9886.md class_BundleInfo_8more_71ca9886.md
这是一份Cangjie语言的API参考文档，定义了`BundleInfo`类，用于封装应用包的详细信息。文档涵盖了应用名称、版本号、权限状态、安装时间、应用配置（`appInfo`）及模块信息（`hapModulesInfo`）等核心只读属性。该类通过`getBundleInfoForSelf`接口获取，支持分布式版本兼容性检查。主要面向鸿蒙开发者，用于读取和管理应用包元数据，系统能力属于BundleManager.BundleFramework.Core。

### let_reqPermission_8more_4f25269f.md let_reqPermission_8more_4f25269f.md
本文档是仓颉语言的API参考文档，详细列出了应用包信息的八个核心只读属性。主要内容包括权限详情、路由表配置、签名信息以及版本和供应商等元数据。这些属性通过调用getBundleInfoForSelf接口并配合特定标志位获取，直接映射app.json5配置或系统运行时状态。文档旨在为开发者提供查询应用基础信息和运行时配置的标准接口，适用于鸿蒙系统应用开发场景。