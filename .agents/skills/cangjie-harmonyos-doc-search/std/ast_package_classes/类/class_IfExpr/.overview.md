# class_IfExpr

This directory provides comprehensive API reference documentation for the `IfExpr` class and related utility functions within the Cangjie programming language's Abstract Syntax Tree (AST) framework. It focuses extensively on the structural definition and manipulation of conditional expressions, specifically if-else statements. Core topics covered include the class hierarchy, properties for managing conditions and execution blocks, and the handling of lexical tokens like keywords and parentheses. Additionally, the documentation addresses specific exception handling for invalid tokens and missing else branches. Beyond the class definition, it includes specifications for AST node conversion to tokens and tree traversal mechanisms using the Visitor pattern. This documentation is essential for developers, language engineers, and compiler designers who need to programmatically parse, modify, or traverse conditional logic structures within the Cangjie language environment. It serves as a critical resource for understanding how conditional logic is represented and processed internally by the language compiler or interpreter.

## Quick Navigation

**What do you want to learn about the IfExpr class?**
- Understand the class structure, inheritance from Expr, and property management → class_IfExpr_10more.md
- Learn about constructors and exception handling for invalid tokens or missing else branches → class_IfExpr_10more.md

**What do you want to do with AST nodes?**
- Convert the current syntax tree node into a Tokens type → func_toTokens_2more.md
- Iterate over the current node and its children using a Visitor instance → func_toTokens_2more.md

## Detailed Description

The following files provide in-depth technical specifications and usage guidelines for the components located in this directory. These documents collectively provide the necessary interface definitions for interacting with conditional expressions in the AST.

### class_IfExpr_10more.md
This document serves as API reference documentation for the IfExpr class, which represents conditional expressions within an Abstract Syntax Tree (AST). It covers the class structure, its inheritance from the Expr parent type, and detailed specifications for properties managing the condition, execution blocks, and lexical tokens like keywords and parentheses. Key information includes two constructors for object instantiation and specific exception handling for invalid token types or missing else branches. Intended for developers working on language parsing or AST manipulation, this guide explains how to programmatically access and modify if-else statement structures using the Cangjie language.

### func_toTokens_2more.md
This document provides API reference documentation for functions within an Abstract Syntax Tree (AST) package in the Cangjie programming language. It covers two main functionalities: the toTokens function, which converts the current syntax tree node into a Tokens type, and the traverse function, which iterates over the current node and its children using a Visitor instance. Key takeaways include the specific return types for toTokens and the ability to prematurely stop the traversal process by overriding the visit function and calling breakTraverse. This technical guide is intended for developers working with AST manipulation who require precise definitions of node conversion and tree traversal mechanisms.