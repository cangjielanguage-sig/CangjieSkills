# unittest_common_package_structs

This directory contains the essential API reference documentation for the data structures utilized within the `unittest_common` package of the Cangjie testing framework. It is designed to assist developers in understanding the core components that facilitate configuration management and command-line interface generation. The documentation specifically details the `KeyTags` struct, which is pivotal for defining configuration keys and managing tag names, and the `OptionInfo` struct, which is responsible for storing metadata for command-line options that appear in help pages. Key aspects covered include the properties of `KeyTags` and the various fields of `OptionInfo`, such as descriptions, names, type mappings, and user-defined statuses. This resource is highly suitable for software developers and QA engineers who are actively working with the Cangjie framework, providing them with the necessary definitions to handle complex configuration logic and automate the generation of help text effectively.

## Quick Navigation

What do you want to learn?

*   Access the API reference for common package data structures → unittest_common_package_structs.md
*   Understand the `KeyTags` struct for configuration key definitions → unittest_common_package_structs.md
*   Review the `OptionInfo` struct for command-line option metadata → unittest_common_package_structs.md
*   Learn about type mappings and user-defined status fields → unittest_common_package_structs.md

## Detailed Description

### unittest_common_package_structs.md unittest_common_package_structs.md

This document serves as API reference documentation for data structures within the unittest_common package. It details the `KeyTags` struct, used for defining configuration keys, and the `OptionInfo` struct, which stores metadata for command-line options displayed in help pages. Key information includes the properties of `KeyTags` for managing tag names and the fields of `OptionInfo` such as description, name, type mappings, and user-defined status. Intended for developers working with the Cangjie testing framework, this reference provides the necessary definitions to handle configuration and help text generation.