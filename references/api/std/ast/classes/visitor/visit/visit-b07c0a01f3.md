<!-- cj-doc kind="api-member" level="7" id="std.ast.class.visitor.visit.visit-b07c0a01f3" parent="std.ast.class.visitor.visit" -->
# Visitor.func visit(TypePattern)

[← Visitor.visit](index.md)

## 签名

```cangjie role=signature
protected open func visit(_: TypePattern): Unit
```

定义访问节点时的操作，需要重写。

## 契约

参数：

- _: TypePattern - TypePattern 类型的被遍历节点。
