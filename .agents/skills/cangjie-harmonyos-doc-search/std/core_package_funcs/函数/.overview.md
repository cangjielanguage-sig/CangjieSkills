# 函数

This directory contains API reference documentation for standard utility functions in the Cangjie programming language. It covers a wide range of functionalities including console input/output operations (print, println, eprint), memory management (raw data acquisition, size calculation), type comparisons (min, max), and Option type handling (ifSome, ifNone). The documentation provides syntax details, parameters, return values, and code examples for developers working with system-level programming, C interoperability, and general application logic. It is suitable for Cangjie developers seeking precise implementation details for standard library operations involving data types, streams, and concurrency control.

## Quick Navigation

**What do you want to do?**

*   **Handle Console Output (Print/Println)**
    *   Generic or specific numeric types (Float, Int) → func_printFloat32_Bool_6more.md, func_printTT_Bool_where_T__7more.md
    *   Unsigned integers, Rune, or String → func_printRune_Bool_6more.md, func_printlnInt32_8more.md
    *   Standard Error (stderr) output → func_acquireArrayRawDataTA_3more.md, func_eprintTT_Bool_where_T_4more.md
*   **Manage Memory and System Resources**
    *   Low-level memory pointers and alignment → func_acquireArrayRawDataTA_3more.md, func_printlnUInt8_5more.md, func_sizeOfT_where_T_CType_3more.md
    *   Concurrency (sleep) or type size → func_sizeOfT_where_T_CType_3more.md
*   **Process Data and Logic**
    *   Handle Option types (ifSome, ifNone) → func_eprintTT_Bool_where_T_4more.md, func_ifSomeTOptionT_T_-_Un_2more.md
    *   Compare values (min, max) → func_ifSomeTOptionT_T_-_Un_2more.md, func_minTT_T_ArrayT_where__3more.md
    *   Read console input or check reference equality → func_printlnUInt8_5more.md

## Detailed Description

### func_acquireArrayRawDataTA_3more.md
This document serves as API reference documentation for three specific utility functions in the Cangjie programming language, focusing on low-level memory management and output operations. It details `acquireArrayRawData` for obtaining raw pointers from arrays, `alignOf` for querying memory alignment requirements, and `eprint` for writing to the standard error stream. Key takeaways include the strict requirement to pair `acquireArrayRawData` with `releaseArrayRawData` to prevent memory issues and the constraint that generic types must implement `CType`. The documentation provides syntax, parameters, return values, and verified code examples for each function. The target audience is Cangjie developers working on system-level programming or C interoperability who need to handle raw memory pointers and error streams.

### func_eprintTT_Bool_where_T_4more.md
This API reference document details standard error output functions and Option handling utilities in the Cangjie programming language. It covers the eprint and eprintln functions for both generic types and strings, alongside the ifNone utility for Option types. Key information includes that these print functions direct output to standard error (stderr) instead of standard output, with generic variants requiring the ToString interface. The ifNone function executes a specific action only when an Option value is None. This documentation serves Cangjie developers seeking to implement error logging and conditional logic for optional values.

### func_ifSomeTOptionT_T_-_Un_2more.md
This document serves as API reference documentation for utility functions within the Cangjie standard library. It details the `ifSome` function, which executes a specific action only when an `Option` type contains a value, and the `max` function, which returns the largest element from a set of comparable data types. Key information includes parameter definitions, return types, and constraints such as the `Comparable` interface requirement. The documentation also provides a warning regarding floating-point `NaN` comparisons and includes verified code examples demonstrating usage with primitive types and custom classes. This resource is designed for developers seeking to implement optional value handling or comparison logic in Cangjie applications.

### func_minTT_T_ArrayT_where__3more.md
This document serves as API reference material for specific utility functions in the Cangjie programming language. It details the generic `min` function for finding the smallest value among comparable types, alongside `print` function overloads for `Bool` and `Float16` types. Key takeaways include the requirement for types to implement the `Comparable` interface, a warning regarding `NaN` handling in floating-point comparisons, and the specific six-decimal formatting for `Float16` output. Intended for Cangjie developers, this reference provides syntax, parameter descriptions, and practical code examples to facilitate correct implementation of comparison and console output operations.

### func_printFloat32_Bool_6more.md
This document serves as API reference documentation for overloaded print functions in the Cangjie programming language, specifically handling numeric data types. It details function signatures for Float32, Float64, Int16, Int32, Int64, and Int8, explaining their parameters and usage. Key takeaways include that floating-point types are formatted to six decimal places, while integer types output standard string representations. All functions accept a flush boolean parameter to control buffer clearing. This reference is intended for Cangjie developers seeking to output numeric data to the console.

### func_printRune_Bool_6more.md
This document serves as API reference documentation for overloaded print functions in the Cangjie programming language, specifically handling Rune, String, and unsigned integer types. It details six function signatures: print(Rune, Bool), print(String, Bool), and print(UInt8/16/32/64, Bool). Each function outputs the string representation of the input data to the console and accepts a flush parameter (defaulting to false) to control buffer clearing. The document provides syntax definitions, parameter descriptions, and verified code examples for each data type. This reference is intended for Cangjie developers seeking to output specific data types to the console.

### func_printTT_Bool_where_T__7more.md
This document serves as API reference documentation for standard output functions in the Cangjie programming language. It details the generic print function, which accepts types implementing the ToString interface, and multiple overloaded println functions for specific data types including Bool, Float16, Float32, Float64, and Int16. Key information includes function signatures, parameter descriptions such as the optional flush argument for buffer control, and verified code examples demonstrating output formatting. The target audience is Cangjie developers seeking to handle console output and string representation of various data types.

### func_printlnInt32_8more.md
This document serves as an API reference for overloaded println functions in the Cangjie programming language, detailing how to output various data types to the console. It covers specific implementations for integer types including Int8, Int32, Int64, UInt16, UInt32, and UInt64, as well as the Rune and String types. Each function prints the string representation of its argument followed by a newline. The documentation provides function signatures, parameter descriptions, and verified code examples demonstrating usage and expected output. This reference is intended for Cangjie developers seeking standard output functionality for basic data types.

### func_printlnUInt8_5more.md
This document serves as API reference documentation for five specific utility functions in the Cangjie programming language, covering input/output, comparison, and memory management. The main sections describe the println overloads for UInt8 and generic ToString types, the readln console input function, the refEq reference equality checker, and the unsafe releaseArrayRawData function. Key information includes function signatures, parameter definitions, return values, and verified code examples demonstrating expected behavior. This reference is intended for Cangjie developers who need precise syntax and usage details for standard library operations, particularly those involving console interaction and unsafe pointer handling.

### func_sizeOfT_where_T_CType_3more.md
This API reference document outlines three utility functions in the Cangjie language for memory management and concurrency control. The main sections describe sizeOf<T>() for retrieving the memory footprint of types, sleep(Duration) for suspending thread execution, and zeroValue<T>() for creating zero-initialized instances. Key takeaways include the specific return types like UIntNative, the behavior of thread yielding, and the critical safety warning that zeroValue is unsafe and requires valid assignment before use to avoid runtime crashes. This documentation targets Cangjie developers implementing low-level system operations or multi-threaded logic.