# class_BufferedInputStreamT_where_T_InputStream

This directory contains API documentation for the `BufferedInputStream<T>` class in the Cangjie standard library, designed to optimize data reading by wrapping input streams with buffering capabilities. It covers the class definition, initialization methods, and extensions for resource management and seekable operations. Key topics include buffer configuration, automatic cleanup via the `Resource` interface, random access via the `Seekable` interface, and core reading functions. This documentation is suitable for Cangjie developers looking to implement efficient I/O operations, manage stream lifecycles, and handle buffered data streams with generic types.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition and initialization methods → class_BufferedInputStreamT_4more.md
*   Manage resource lifecycle and automatic cleanup → extendT_BufferedInputStreamT_Resource_whe_aed56789.md
*   Implement random access and cursor manipulation → extendT_BufferedInputStreamT_Seekable_whe_d6560437.md
*   Perform read operations and reset streams → func_readArrayByte_3more.md

## Detailed Description

### class_BufferedInputStreamT_4more.md
This document serves as API reference for the BufferedInputStream class in the Cangjie standard library, detailing how to wrap input streams with buffering capabilities to optimize data reading. It covers the class definition, its inheritance from InputStream, and three initialization methods: using a default buffer size, a custom byte array for performance, or a specific capacity. Key takeaways include the default buffer size of 4096, the ability to reuse buffers to reduce memory allocation, and exceptions thrown for invalid buffer sizes. The target audience is Cangjie developers seeking to optimize IO operations, requiring prerequisite knowledge of generic types and stream handling.

### extendT_BufferedInputStreamT_Resource_whe_aed56789.md
This API documentation details the extension of the BufferedInputStream class to implement the Resource interface in the Cangjie language, enabling automatic resource management. The document covers the extension declaration syntax, the close() function for stream termination, and the isClosed() function for status checking. Key information includes the ability to use BufferedInputStream within try-with-resource syntax for automatic cleanup, a warning against using the stream after closing, and a practical code example demonstrating these features. This reference guide targets Cangjie developers working with I/O streams who require efficient resource lifecycle management.

### extendT_BufferedInputStreamT_Seekable_whe_d6560437.md
This API documentation defines a Cangjie language extension that implements the Seekable interface for BufferedInputStream, conditional on the underlying stream type being seekable. It covers properties for querying stream metrics, including length, position, and remainLength, alongside the seek function for cursor manipulation. Key information includes that seek clears the buffer before moving, allows seeking past the stream end, and throws an IOException for invalid negative positions. The document includes a practical code example demonstrating usage and error handling. This reference is designed for developers utilizing the standard I/O library who require random access to buffered data streams. Keywords: Cangjie, BufferedInputStream, Seekable, IO, API, extension, stream, cursor.

### func_readArrayByte_3more.md
This document provides API reference documentation for input stream operations within the Cangjie standard library. It covers three primary functions: read(Array<Byte>) for reading data blocks into a buffer, readByte() for retrieving individual bytes, and reset(T) for binding new input streams while preserving buffer capacity. Key information includes parameter definitions, return types such as Int64 and nullable Byte, and exception handling for invalid arguments. The content is intended for Cangjie developers seeking to implement buffered data reading and stream management, featuring code examples to demonstrate practical usage and error handling. Keywords: Cangjie, BufferedInputStream, IO operations, API reference.