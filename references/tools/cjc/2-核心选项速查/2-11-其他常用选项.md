<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjc.2-核心选项速查.2-11-其他常用选项" parent="tools.cjc.2-核心选项速查" -->
# 2.11 其他常用选项

[← 2. 核心选项速查](index.md)

速查`--trimpath <prefix>`：移除调试信息中的路径前缀；`-s`, `--strip-all`：删除输出文件中的符号表；`--set-runtime-rpath`：写入运行时库路径到 RPATH；另含更多表项。

| 选项 | 说明 |
|------|------|
| `--trimpath <prefix>` | 移除调试信息中的路径前缀 |
| `-s`, `--strip-all` | 删除输出文件中的符号表 |
| `--set-runtime-rpath` | 写入运行时库路径到 RPATH |
| `--disable-reflection` | 关闭反射信息生成 |
| `--no-sub-pkg` | 声明无子包以缩减 code size |
| `--stack-trace-format=[default\|simple\|all]` | 异常调用栈格式 |
