<!-- cj-doc kind="api-member" level="5" id="std.deriving.macro.deriveinclude" parent="std.deriving" -->
# @DeriveInclude

[← std.deriving](../index.md)

## 签名

```cangjie role=signature
@DeriveInclude
```

`DeriveInclude` 可为已被 @Derive 宏修饰的声明增加需要处理的属性，属性默认情况不会被 Deriving 处理。
