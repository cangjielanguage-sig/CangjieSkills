# 组件可见区域变化事件

This directory focuses on the `onVisibleAreaChange` event mechanism within the HarmonyOS ArkUI framework, specifically tailored for developers using the Cangjie language. It provides comprehensive resources for tracking when UI components enter or leave the screen's visible area, a critical requirement for performance optimization and user interaction analysis. The content covers practical implementation details, including setting visibility thresholds, handling callback events, and managing state updates in scrollable views. It is particularly suitable for developers working on features such as advertisement exposure tracking, dynamic content loading, or custom scroll behaviors who need precise control over component visibility states. By understanding these events, developers can create more responsive and efficient applications that react intelligently to user scrolling and layout changes.

## Quick Navigation

**What do you want to learn?**

*   **Learn by example:** View a complete Cangjie code sample demonstrating how to monitor visibility changes in a list, including state management and scroll edge detection → 示例代码.md
*   **Understand the API:** Read the detailed technical documentation regarding function signatures, parameters, ratio thresholds, and specific usage constraints like occlusion rules → 组件可见区域变化事件_2more.md

## Detailed Description

### 示例代码.md 示例代码.md
This document provides a Cangjie code example demonstrating how to monitor component visibility changes within a scrollable view. The primary focus is on the `onVisibleAreaChange` callback, state management using `@State`, and scroll event handling. Key information includes setting visibility ratios to `[0.0, 1.0]` to trigger callbacks when a specific `Text` component is fully visible or invisible, thereby updating the UI state dynamically. The example also illustrates list rendering via `ForEach` and scroll edge detection. This tutorial is intended for HarmonyOS developers using the Cangjie language who need to implement visibility-based logic in their UI applications.

### 组件可见区域变化事件_2more.md 组件可见区域变化事件_2more.md
This API documentation defines the onVisibleAreaChange function for tracking changes in a component's visible screen area, primarily for scenarios like ad exposure tracking. The document details the function signature, system capabilities, version requirements, parameter definitions for thresholds and callbacks, and specific usage constraints regarding occlusion and node types. Key takeaways include that the function accepts an array of area ratio thresholds (0.0 to 1.0) and a callback indicating the change direction and current ratio, while ignoring sibling overlaps and non-tree nodes like overlays. Targeted at HarmonyOS developers using the Cangjie language and ArkUI framework, this reference guide requires knowledge of UI layout and event handling.