<!-- cj-doc kind="guide-leaf" level="6" id="language.collections.hashmap.6-删除.6-3-条件删除" parent="language.collections.hashmap.6-删除" -->
# 6.3 条件删除

[← 6. 删除](index.md)

```cangjie cjtest=syntax id=syntax-aa8de87c70-1 form=unit
func removeIf(predicate: (K, V) -> Bool): Unit
```

```cangjie cjtest=syntax id=syntax-aa8de87c70-2 form=stmt
let map = HashMap<String, Int64>([("a", 1), ("b", 20), ("c", 3)])
map.removeIf { k, v => v > 10 }  // 删除 v > 10 的条目 → {"a": 1, "c": 3}
```

- 在 `predicate` 中修改 HashMap 会抛 `ConcurrentModificationException`
