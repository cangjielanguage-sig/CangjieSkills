# 安全区域

This directory provides comprehensive documentation for managing Safe Areas within the Cangjie ArkUI framework. It covers the fundamental concepts of safe areas, API references for extending component layouts into system regions (like status bars and notches), and handling virtual keyboard interactions. Key topics include the `expandSafeArea` function, `setKeyboardAvoidMode`, and basic type definitions such as `SafeAreaType` and `SafeAreaEdge`. The content also includes practical code examples demonstrating immersive layouts. This resource is suitable for HarmonyOS developers using the Cangjie language who need to optimize UI layouts for various screen configurations and system obstructions.

## Quick Navigation

**What do you want to learn?**
*   Understand the core concept of Safe Areas and default layout behavior → 安全区域.md
*   Review basic type definitions (SafeAreaType, SafeAreaEdge, KeyboardAvoidMode) → 基础类型定义.md

**What do you want to do?**
*   Extend component boundaries into system-defined safe areas (status bars, notches) → func_expandSafeAreaArraySafeAreaType_Arra_9de2e8b9.md
*   Manage page layout adjustments when the virtual keyboard appears → func_setKeyboardAvoidMo_1b6b9bdd.md
*   View a practical Cangjie code example for immersive layouts → 示例代码.md

## Detailed Description

### func_expandSafeAreaArraySafeAreaType_Arra_9de2e8b9.md
This document provides API reference documentation for the expandSafeArea function in the Cangjie ArkUI framework, which allows components to extend their layout into system-defined safe areas like status bars or keyboard regions. The content covers the function signature, system capabilities, version requirements, and detailed parameter definitions for types (SYSTEM, CUTOUT, KEYBOARD) and edges (TOP, BOTTOM, START, END). It includes an extensive Description section explaining usage constraints and behavioral rules. Key takeaways include default parameter values, the requirement for component boundaries to overlap with safe areas for expansion, and specific limitations such as incompatibility with fixed dimensions or scroll containers without proper nesting. It also notes that events in expanded areas may be intercepted by the system. This reference guide targets developers using the ArkUI framework in Cangjie who need to manage UI layout around system obstructions like notches or keyboards.

### func_setKeyboardAvoidMo_1b6b9bdd.md
This document provides API reference details for the Cangjie function setKeyboardAvoidMode, which manages how a page layout adjusts when the virtual keyboard appears. It covers the function signature, system capabilities, version requirements, and a parameter table defining the KeyboardAvoidMode enum. Key information includes the default mode being OFFSET, and specific behaviors for RESIZE mode, which compresses the page size and disables certain safe area expansions, versus NONE mode, where the keyboard covers the page. This reference is intended for ArkUI developers using the Cangjie language who need to control UI behavior during keyboard interactions.

### 基础类型定义.md
This document serves as an API reference for basic type definitions within the ArkUI framework using the Cangjie language. It details three primary enumerations: KeyboardAvoidMode, SafeAreaType, and SafeAreaEdge. Key takeaways include specific modes for keyboard interaction, such as OFFSET and RESIZE, and configurations for extending safe areas to cover system bars, cutouts, and keyboards across different edges. Targeted at developers implementing UI layouts, this reference helps manage screen real estate and input interactions effectively.

### 安全区域.md
This document defines the concept of Safe Areas in UI layout, explaining how content avoids system elements like status bars by default. It covers methods to extend drawing boundaries using the expandSafeArea attribute and keyboard avoidance configuration via setKeyboardAvoidMode. Key information includes achieving immersive effects by expanding safe areas or using full-screen window settings, and noting that camera cutouts are not avoided by default unless specific metadata is configured. This technical reference is intended for UI developers needing to manage layout constraints and immersive display modes.

### 示例代码.md
This document provides a Cangjie code example demonstrating how to implement a UI layout with safe area expansion and text input control within a HarmonyOS application. The content focuses on the EntryView class, utilizing Stack and Column components to structure the interface. It specifically highlights the use of the expandSafeArea modifier to extend background images into system and keyboard safe areas, and demonstrates programmatic text manipulation via TextInputController to set the caret position. Key takeaways include understanding declarative UI syntax, handling safe area insets, and managing text input state. This reference material is intended for Cangjie developers building HarmonyOS apps who need to optimize layouts for different screen configurations and implement interactive text fields.