<!-- cj-doc kind="api-member" level="6" id="std.unittest.mock.class.unorderedverifier.checkthat" parent="std.unittest.mock.class.unorderedverifier" -->
# UnorderedVerifier.checkThat

[← UnorderedVerifier](index.md)

## 签名

```cangjie role=signature
public func checkThat(statement: VerifyStatement):UnorderedVerifier
```

添加一条 “验证语句”。

## 契约

参数：

- statement: VerifyStatement - 待被添加的“验证语句”。

返回值：

- UnorderedVerifier - 返回对象自身。
