# interface_SaturatingOpT

This directory contains the core interface definition and type-specific implementations for saturating arithmetic operations in the Cangjie programming language. It covers the generic `SaturatingOp` interface and its extensions for various integer types (Int8, Int16, Int32, Int64, IntNative, and their unsigned counterparts). The primary focus is on preventing integer overflow and underflow by clamping results to maximum or minimum values, ensuring numerical stability. This resource is suitable for Cangjie developers and system engineers who need safe, predictable arithmetic logic in critical applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the generic interface definition → interface_SaturatingOpT_11more.md
*   Implement saturating operations for signed integers:
    *   8-bit integers → extend_Int8_SaturatingOpInt8/
    *   16-bit integers → extend_Int16_SaturatingOpInt16/
    *   32-bit integers → extend_Int32_SaturatingOpInt32/
    *   64-bit integers (with power functions) → extend_Int64_SaturatingOpInt64_SaturatingPow/
    *   Native platform integers → extend_IntNative_SaturatingOpIntNative/
*   Implement saturating operations for unsigned integers:
    *   8-bit integers → extend_UInt8_SaturatingOpUInt8/
    *   16-bit integers → extend_UInt16_SaturatingOpUInt16/
    *   32-bit integers → extend_UInt32_SaturatingOpUInt32/
    *   64-bit integers → extend_UInt64_SaturatingOpUInt64/
    *   Native platform integers → extend_UIntNative_SaturatingOpUIntNative/

## Detailed Description

### interface_SaturatingOpT_11more.md interface_SaturatingOpT_11more.md
This API reference document defines the SaturatingOp interface in the Cangjie programming language, which provides saturating arithmetic operations for generic type T to handle integer overflows gracefully. The document details the interface definition and provides specifications for eleven functions including addition, subtraction, multiplication, division, increment, decrement, negation, modulo, and bitwise shifts. Key information includes the saturation logic where overflow returns the maximum value and underflow returns the minimum value, preventing wrap-around errors. Shift operations specifically clamp the shift amount to the bit width minus one. This technical reference is intended for Cangjie developers implementing safe integer arithmetic logic.

### extend_Int8_SaturatingOpInt8/
This directory contains essential API documentation for the Cangjie programming language, specifically targeting the implementation of the `SaturatingOp` interface for the `Int8` data type. It provides a detailed overview of saturating arithmetic functions, covering operations such as addition, subtraction, multiplication, division, negation, increment, decrement, modulo, and left shift. The documentation emphasizes a saturation strategy that clamps results to the type's maximum or minimum values during overflow or underflow events, ensuring data integrity and preventing unexpected wrapping.

### extend_Int16_SaturatingOpInt16/
This directory provides comprehensive API documentation for implementing saturating arithmetic operations within the Cangjie standard library, specifically focusing on the `Int16` type. It details safe mathematical functions designed to handle overflow and underflow scenarios by clamping results to the type's minimum or maximum values, rather than wrapping around or throwing runtime errors. Key topics covered include addition, subtraction, multiplication, division, increment, decrement, negation, modulo, and bit shifting operations.

### extend_Int32_SaturatingOpInt32/
This directory provides comprehensive API documentation for implementing saturating arithmetic operations specifically for the Int32 type within the Cangjie programming language. It focuses on robust mathematical functions designed to prevent integer wrapping by clamping results to the type's maximum or minimum values during overflow or underflow scenarios. The documentation covers a wide suite of operations, including basic arithmetic, bit shifting, and specific utility functions. This resource is essential for Cangjie developers who require safe, predictable arithmetic behavior in boundary conditions.

### extend_Int64_SaturatingOpInt64_SaturatingPow/
This directory provides a comprehensive collection of API reference documentation designed for extending the Int64 type within the Cangjie programming language to support advanced saturating arithmetic capabilities. It thoroughly covers the implementation of SaturatingOp and SaturatingPow interfaces, detailing specific functions such as saturatingAdd, saturatingMul, saturatingDiv, and saturatingPow. The documentation highlights the critical saturation strategy where operations return the type's maximum value on overflow and minimum value on underflow, effectively preventing wrapping behavior and ensuring numerical stability.

### extend_IntNative_SaturatingOpIntNative/
This directory serves as a technical reference for the implementation of the `SaturatingOp` interface extension for the `IntNative` type within the Cangjie core package. It details a suite of saturating arithmetic functions that ensure numerical safety by clamping results to maximum or minimum limits upon overflow or underflow, rather than allowing values to wrap around. The documentation covers a wide range of operations, including basic arithmetic like addition and multiplication, as well as bit manipulation and negation.

### extend_UInt8_SaturatingOpUInt8/
This directory provides comprehensive API documentation for the Cangjie programming language, focusing on saturating arithmetic operations for unsigned integer types, with a primary emphasis on UInt8. It details the implementation of the `SaturatingOp` interface and specific utility functions designed to handle arithmetic edge cases safely. The documentation covers a wide range of operations, including addition, subtraction, multiplication, division, and bit shifting, explaining how they prevent wrapping errors by clamping results to type boundaries.

### extend_UInt16_SaturatingOpUInt16/
This directory serves as a comprehensive technical resource for the Cangjie programming language, specifically targeting the implementation of saturating arithmetic operations for the `UInt16` data type. It includes detailed API documentation that covers the `SaturatingOp` interface, which defines a standard set of arithmetic and bitwise operations designed to handle boundary conditions gracefully. The documentation explains how operations like addition, subtraction, multiplication, and shifting clamp results to maximum or minimum values instead of wrapping around, which is crucial for preventing logic errors in critical systems.

### extend_UInt32_SaturatingOpUInt32/
This directory provides API reference documentation for the Cangjie programming language, specifically focusing on saturating arithmetic operations for unsigned integer types. It details the implementation of the `SaturatingOp` interface for `UInt32` and documents specific utility functions for `UInt64` and `UInt32`. The content covers essential arithmetic and bitwise operations designed to prevent unexpected wrapping behavior by handling overflow and underflow through saturation strategies.

### extend_UInt64_SaturatingOpUInt64/
This directory provides comprehensive API reference documentation for extending the `UInt64` type with saturating operations within the Cangjie programming language. It focuses on the implementation of the `SaturatingOp` interface and specific utility functions designed to handle arithmetic and bitwise manipulations safely. The documentation covers a wide range of operations, including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and bit shifting. A core concept explored here is saturation behavior, where results are clamped to the type's maximum or minimum limits upon overflow or underflow.

### extend_UIntNative_SaturatingOpUIntNative/
This directory contains API reference documentation for extending the `UIntNative` type in the Cangjie programming language with saturating arithmetic operations. It focuses on the `SaturatingOp` interface implementation, providing a comprehensive suite of safe mathematical functions. These include standard operations like addition, subtraction, multiplication, and division, as well as bit manipulation and negation. The core functionality ensures that results clamp to the type's maximum or minimum values during overflow or underflow, preventing the undefined behavior associated with wrapping arithmetic.