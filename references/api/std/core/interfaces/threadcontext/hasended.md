<!-- cj-doc kind="api-member" level="6" id="std.core.interface.threadcontext.hasended" parent="std.core.interface.threadcontext" -->
# ThreadContext.hasEnded

[← ThreadContext](index.md)

## 签名

```cangjie role=signature
func hasEnded(): Bool
```

检查方法，用于检查当前 context 是否已结束。

## 契约

返回值：

- Bool - 如果结束返回 true，否则返回 false。
