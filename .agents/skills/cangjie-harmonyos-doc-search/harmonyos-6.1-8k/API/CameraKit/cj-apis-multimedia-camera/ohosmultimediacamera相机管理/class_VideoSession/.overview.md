# class_VideoSession

本目录包含仓颉语言中 `VideoSession` 类的 API 参考文档，属于 HarmonyOS CameraKit 的一部分。它涵盖了视频录制会话的核心功能，包括会话配置、能力查询及事件管理。主要特性涉及闪光灯、对焦、变焦及防抖等。文档详细说明了预配置会话参数（分辨率、画幅比例）的方法，以及管理事件监听器（错误、对焦状态、平滑变焦）的接口。适用于使用仓颉语言开发 HarmonyOS 多媒体相机应用的开发者，提供了实现稳健视频录制功能所需的必要接口。

## Quick Navigation

**您想了解什么？**

*   查看 VideoSession 类定义及能力查询 → class_VideoSessio_2more_959079d8.md
*   对会话进行预配置（分辨率、画幅） → func_preconfigPreconfig_30a8588b.md
*   注册事件监听回调 (`on`)
    *   监听普通录像会话错误事件 → func_onCameraEvents_Callback0Argument.md
    *   监听相机聚焦状态变化 → func_onCameraEvents_Callback1ArgumentFocusState.md
    *   监听平滑变焦状态变化 → func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md
*   注销事件监听回调 (`off`)
    *   取消监听错误事件或聚焦状态 → func_offCameraEve_2more_4be4c4ed.md
    *   取消监听平滑变焦状态或错误事件 → func_offCameraEve_2more_d8087603.md

## Detailed Description

### class_VideoSessio_2more_959079d8.md class_VideoSessio_2more_959079d8.md
这是一份仓颉语言的 API 参考文档，主要定义了用于普通录像模式的 `VideoSession` 类。该类继承自 `Session`、`Flash`、`Focus` 等多个父接口，集成了闪光灯、曝光、对焦、变焦及防抖等相机核心功能，支持多种分辨率和帧率。文档详细说明了 `canPreconfig` 方法，用于查询当前会话是否支持指定的预配置类型和画幅比例，包含参数定义、返回值、异常处理及代码示例。该文档面向使用 CameraKit 进行多媒体相机应用开发的开发者，提供了系统能力要求和版本信息，帮助开发者实现视频录制功能的预配置检测。

### func_offCameraEve_2more_4be4c4ed.md func_offCameraEve_2more_4be4c4ed.md
这份文档是 CameraKit 中 `off` 函数的 API 参考，旨在指导开发者注销相机事件的监听回调。文档详细介绍了两个重载函数：分别用于取消监听普通录像会话的错误事件和相机聚焦状态变化。关键信息涵盖参数类型（如 `CameraEvents`、`Callback0Argument`）、异常处理（错误码 7400201）及仓颉代码示例。该文档面向具备相机开发基础的开发人员，用于管理会话事件监听的生命周期。

### func_offCameraEve_2more_d8087603.md func_offCameraEve_2more_d8087603.md
本文档是仓颉语言的 API 参考文档，主要介绍了用于注销相机事件监听器的两个重载 `off` 函数。文档涵盖了函数定义、参数详情（特别是 `CameraEvents` 和回调类型）、系统能力、异常处理（错误码 7400201）及使用示例。关键点在于区分了注销平滑变焦状态变化监听与注销普通录像会话错误事件的不同用法，并强调了会话创建成功后的操作时机。目标受众是使用 HarmonyOS CameraKit 进行多媒体开发的程序员，旨在指导其正确管理视频会话中的事件监听生命周期。

### func_onCameraEvents_Callback0Argument.md func_onCameraEvents_Callback0Argument.md
本文档描述了用于注册回调函数以监听普通录像会话错误事件的 API 函数 `on`。文档涵盖了函数签名、参数定义、系统能力要求、异常处理及代码示例。关键信息指出 `eventType` 必须为 `CameraError`，在会话配置或输入操作出错时触发，且特别说明不支持在回调内部调用 `off` 方法。该函数属于多媒体相机核心能力，适用于需要处理相机服务错误的仓颉语言开发者。

### func_onCameraEvents_Callback1ArgumentFocusState.md func_onCameraEvents_Callback1ArgumentFocusState.md
这是一份 API 参考文档，详细说明了用于监听相机聚焦状态变化的 `on` 函数及其用法。文档涵盖了功能描述、参数定义（`eventType` 和 `callback`）、异常处理及代码示例。关键信息指出该接口仅支持 `FocusStateChange` 事件，且必须在自动对焦模式下触发，同时禁止在回调内部调用 `off` 注销。该文档面向使用仓颉语言开发 HarmonyOS 相机功能的开发者，需具备相机会话创建及回调处理的基础知识。

### func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md
这是一份仓颉语言的 API 参考文档，详细说明了用于监听相机平滑变焦状态变化的 `on` 函数。文档主要包含函数定义、功能描述、参数列表（`eventType` 和 `callback`）、异常处理及代码示例。关键信息指出，监听事件必须为 `SmoothZoomInfoAvailable`，且不支持在回调方法中调用 `off` 注销。文档还列出了相关的系统能力、起始版本及错误码。该文档面向使用仓颉进行多媒体相机应用开发的开发者，旨在帮助他们通过注册回调函数实时获取平滑变焦的详细信息。

### func_preconfigPreconfig_30a8588b.md func_preconfigPreconfig_30a8588b.md
本文档是仓颉语言中 `preconfig` 函数的 API 参考文档，旨在对当前相机会话进行预配置。文档详细介绍了函数签名、系统能力、参数定义（包括必填的预期分辨率 `PreconfigType` 和可选的画幅比例 `PreconfigRatio`，默认为 16:9）以及可能抛出的业务异常。此外，还提供了包含上下文获取和会话创建的完整代码示例。该文档面向使用 CameraKit 进行多媒体开发的开发者，帮助他们理解如何在版本 22 及以上的系统中设置相机预配置参数。