# class_JsonReader

This directory contains comprehensive API reference documentation for the `JsonReader` class within the Cangjie programming language ecosystem. It focuses specifically on the mechanisms required for deserializing JSON data streams into native Cangjie objects. The documentation covers essential aspects of stream processing, including initialization, token inspection, and structural navigation. It highlights error handling protocols, such as throwing `IllegalStateException` for invalid formats, and details specific behaviors like whitespace skipping and raw byte retrieval. This resource is primarily suitable for Cangjie developers and system architects who are implementing custom JSON parsing logic and require granular control over data stream consumption, validation, and extraction.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class structure, constructor initialization, and basic stream processing methods like `peek()`, `readName()`, and structural end calls → class_JsonReader_6more.md
*   Explore specific functions for generic value deserialization, raw byte retrieval, skipping data structures, and consuming structural start tokens → func_readValueT_where_T_Js_5more.md

## Detailed Description

The following files provide in-depth technical specifications for the `JsonReader` class and its associated functions.

### class_JsonReader_6more.md class_JsonReader_6more.md
This document provides API reference documentation for the JsonReader class in the Cangjie language, which enables deserialization of JSON data streams into Cangjie objects. The main sections detail the class constructor init(InputStream) and essential stream processing methods such as peek(), readName(), endArray(), and endObject(). Key information includes that the reader automatically skips whitespace, peek() inspects the next token type without consuming it, and structural methods require matching start calls. The document specifies that invalid JSON formats trigger IllegalStateException. This reference is designed for developers implementing custom JSON parsing logic who require precise control over data stream consumption and validation.

### func_readValueT_where_T_Js_5more.md func_readValueT_where_T_Js_5more.md
This API reference document details specific functions for reading and navigating JSON data streams in the Cangjie programming language. It covers five primary functions: readValue for generic deserialization, readValueBytes for raw byte retrieval, skip for ignoring data structures, and startArray and startObject for consuming structural tokens. Key takeaways include the conditional behavior of readValue when handling Strings based on the next JsonToken, the raw data rules for readValueBytes, and the fact that all functions throw IllegalStateException for malformed JSON. This guide targets Cangjie developers implementing JSON parsing who need precise control over stream consumption and data extraction.