# class_FuzzDataProvider

This directory contains documentation for the `FuzzDataProvider` class in the Cangjie programming language, a utility designed to facilitate fuzzing operations by converting raw byte streams into structured data types. It covers methods for consuming integers, strings, and booleans, as well as properties for tracking data state and offset. The documentation includes details on interoperability with native C code and exception handling. This resource is suitable for developers and security engineers working on fuzzing frameworks, binary data parsing, or serialization who need precise control over data type conversion from byte arrays.

## Quick Navigation

**What do you want to learn?**

*   Convert raw data into specific integer types, arrays, or strings → func_consumeInt8_13more.md
*   Access data state properties and general consumption methods → let_data_19more.md
*   Create instances from native C pointers for interoperability → static_func_withNativeD_a2c34b3d.md
*   Review the comprehensive class structure and error handling → [class_FuzzDataProvider/]

## Detailed Description

### func_consumeInt8_13more.md func_consumeInt8_13more.md
This document provides API reference documentation for the FuzzDataProvider class in the Cangjie programming language, focusing on methods that convert raw data into specific type instances. The main sections detail functions for consuming single values and arrays of various integer types (Int8, UInt8, UInt16, UInt32, UInt64), as well as Rune and UTF-8 String types. Key information includes the static factory method withCangjieData for creating instances and the exception handling for negative count parameters in array consumption methods. This guide is intended for developers working on fuzzing or data serialization who require precise control over data type conversion from byte arrays.

### let_data_19more.md let_data_19more.md
This document serves as API reference documentation for a Cangjie data structure designed to parse and consume byte arrays. It details properties for tracking data state, such as data, offset, and remainingBytes, alongside a comprehensive suite of methods for converting raw bytes into specific types. Key functions include consumeAll for full conversion and typed consumers like consumeInt32, consumeFloat64, and consumeBool that automatically advance the internal offset. This reference is intended for developers working with binary data parsing, serialization, or network protocols in the Cangjie programming language.

### static_func_withNativeD_a2c34b3d.md static_func_withNativeD_a2c34b3d.md
This document serves as API reference documentation for a specific static function named withNativeData in the Cangjie programming language, designed to create a FuzzDataProvider instance from native C pointers. The content covers the function signature, its functionality description, a detailed list of input parameters including data type and length, and the return value specification. It links to the main class definition for FuzzDataProvider. The function is marked as unsafe and static. It takes a CPointer<UInt8> representing external data and an Int64 length to construct a FuzzDataProvider object. This is essential for interoperability between Cangjie and C code in fuzzing contexts. Targeted at developers using the fuzzing package who need to interface with native C memory. Prerequisites include understanding Cangjie syntax, unsafe pointers, and fuzzing concepts.

### class_FuzzDataProvider/
This directory contains comprehensive documentation for the `FuzzDataProvider` class within the Cangjie programming language. It serves as a utility class designed to facilitate fuzzing operations by converting raw mutation data byte streams into standard Cangjie basic data types. The documentation covers the class structure, public properties, and a wide array of methods for consuming data types such as integers, booleans, and strings. Additionally, it details error handling mechanisms, specifically the `ExhaustedException`. This resource is essential for developers and security engineers building fuzzing frameworks who require a mechanism to interpret byte sequences as structured data programmatically.