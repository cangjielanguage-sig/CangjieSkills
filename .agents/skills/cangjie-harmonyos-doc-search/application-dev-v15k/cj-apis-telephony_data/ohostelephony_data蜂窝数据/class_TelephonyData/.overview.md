# class_TelephonyData

This directory provides API reference documentation for the `TelephonyData` class in the Cangjie programming language. It is designed to assist developers in querying cellular network status and SIM card information. The content covers static methods for retrieving data flow types, connection states, default SIM IDs, and slot IDs, as well as checking cellular data and roaming status. Key aspects include handling return types like `DataFlowType` and `DataConnectState`, managing slot indexing, and understanding required permissions such as `ohos.permission.GET_NETWORK_INFO`. This documentation is intended for application developers implementing telephony features who need to access system cellular data capabilities and handle potential business exceptions.

## Quick Navigation

What do you want to learn?

*   Understand the TelephonyData class structure and query general cellular data status → class_TelephonyData_6more.md
*   Check if cellular data roaming is enabled for a specific SIM slot → static_func_isCellularD_57d18a4e.md

## Detailed Description

### class_TelephonyData_6more.md
This document serves as API reference for the TelephonyData class, detailing static methods to query cellular network status and SIM card information in the Cangjie programming language. It covers functions to retrieve data flow types, connection states, default SIM IDs, and slot IDs, alongside a method to check if cellular data is enabled. Key takeaways include specific return types like DataFlowType and DataConnectState, slot indexing (0 or 1), and the requirement of the ohos.permission.GET_NETWORK_INFO permission for status checks, which may throw business exceptions. This targets application developers building telephony-related features who need to access system cellular data capabilities.

### static_func_isCellularD_57d18a4e.md
This document provides API reference documentation for the Cangjie function isCellularDataRoamingEnabled, which checks if cellular data roaming is enabled for a specific SIM slot. It details the function signature, required permissions, and system capabilities. Key sections include input parameters (slotId), return values (Bool), and a comprehensive list of potential exceptions with specific error codes. The document also provides a practical code example demonstrating how to call the function and handle errors. Developers must ensure a SIM card is inserted before invocation. This guide is intended for Cangjie developers implementing telephony features who need to query network roaming status and manage related permissions and error scenarios.