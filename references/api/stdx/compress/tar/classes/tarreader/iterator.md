<!-- cj-doc kind="api-member" level="6" id="stdx.compress.tar.class.tarreader.iterator" parent="stdx.compress.tar.class.tarreader" -->
# TarReader.iterator

[← TarReader](index.md)

## 签名

```cangjie role=signature
public func iterator(): Iterator<TarEntry>
```

返回一个迭代器，迭代 tar 文件中的条目。

## 返回值

- Iterator<TarEntry> - 一个 TarEntry 的迭代器。

