# stdcore

The `stdcore` directory serves as the primary documentation hub for the core package of the Cangjie language standard library. It acts as the definitive guide for the language's foundational infrastructure, covering essential programming capabilities that are available to developers immediately upon starting a project. Key topics include built-in data types (such as integers and floating-point numbers), frequently used utility functions (like `print` and `println`), and critical standard interfaces (including `ToString` and `Hashable`). Furthermore, it documents core classes, structs, and the exception handling hierarchy. Because the core package is automatically imported by default, understanding its contents is mandatory for all Cangjie developers. This documentation is specifically tailored for programmers and system architects who need a deep understanding of the basic building blocks and structural components required to construct robust, efficient applications in the Cangjie environment. It ensures users can leverage the full power of the standard library from the very first line of code.

## Quick Navigation

**What do you want to learn?**

*   **Understand the basics:** Learn about the core package's fundamental role, its automatic import behavior, and its main components like types and functions → 功能介绍.md 功能介绍.md
*   **Find specific APIs:** Search for detailed definitions, structural skeletons, and usage contexts for interfaces, classes, and concurrency primitives → API_列表/

## Detailed Description

### 功能介绍.md 功能介绍.md
This document serves as an introduction to the core package within the Cangjie language standard library, outlining its fundamental role in providing essential programming capabilities. The text details the package's components, including built-in types like integers and floats, common functions such as print and println, standard interfaces like ToString and Hashable, core classes and structs including Array and String, and various exception classes. A key takeaway is that the core package is automatically imported by default, eliminating the need for explicit import statements. This reference material targets Cangjie language developers who need to understand the foundational building blocks available in the standard library.

### API_列表/
This directory serves as a comprehensive API reference index for the Cangjie programming language's standard library. It organizes core language components, including interfaces, classes, structs, enumerations, type aliases, and exception handling mechanisms. The documentation covers essential functionalities ranging from data structures and concurrency primitives to system-level interoperability and memory management. Designed for developers and system architects, this resource provides quick access to definitions, usage contexts, and structural skeletons for building robust applications in the Cangjie environment.