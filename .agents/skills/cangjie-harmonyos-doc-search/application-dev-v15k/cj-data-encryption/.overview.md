# cj-data-encryption

This directory serves as a specialized technical resource focused on data encryption strategies within the Cangjie programming language ecosystem and the ArkData framework. It is designed to provide developers with the necessary knowledge to implement robust security measures that ensure the integrity and confidentiality of stored data. The content specifically addresses encryption mechanisms for both Relational and Key-Value (KV) databases, offering detailed insights into configuration processes such as retrieving the `UIAbilityContext`, defining `StoreConfig`, and utilizing `KVOptions`. Key topics include the default state of encryption (disabled), the requirement for explicit activation during initialization, and the immutable nature of encryption settings post-creation. This documentation is highly suitable for application developers and system architects who are building secure applications and need to adhere to high security standards through practical implementation guidelines and code examples. By following these guidelines, developers can effectively prevent unauthorized access and ensure that their data storage layers comply with modern security expectations, thereby safeguarding user information against potential threats.

## Quick Navigation

- What do you want to learn?
    - Overview of database encryption in Cangjie/ArkData → `数据库加密/`
    - Configuration steps for Relational database encryption → `数据库加密/`
    - Configuration steps for Key-Value database encryption → `数据库加密/`
    - Understanding `UIAbilityContext` and `StoreConfig` usage → `数据库加密/`
- What do you want to do?
    - Enable encryption for a new database instance → `数据库加密/`
    - Verify security constraints and immutability rules → `数据库加密/`
    - Review code examples for data protection → `数据库加密/`
    - Secure sensitive application data effectively → `数据库加密/`

## Detailed Description

### 数据库加密/
This directory provides a comprehensive technical overview of database encryption mechanisms within the Cangjie development environment and the ArkData framework. It is designed to assist developers in implementing robust data security measures to protect the integrity of stored content. The documentation covers encryption strategies for both Relational and Key-Value databases, detailing the necessary configuration steps such as retrieving the UIAbilityContext, setting up `StoreConfig`, and utilizing `KVOptions`. Key learning points include the fact that encryption is disabled by default and must be explicitly enabled during initialization, as well as the critical constraint that encryption settings are immutable once the database is created. This resource is suitable for application developers seeking to secure sensitive data through practical code examples and detailed configuration guidelines, ensuring that applications meet high security standards.