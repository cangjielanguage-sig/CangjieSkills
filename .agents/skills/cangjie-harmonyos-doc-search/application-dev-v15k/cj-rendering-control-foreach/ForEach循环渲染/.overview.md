# ForEach循环渲染

This directory provides a comprehensive technical guide for the ForEach component within the ArkUI framework, specifically using the Cangjie language. It covers essential mechanisms such as initial and non-initial rendering, key generation rules, and component lifecycle management. Key topics include handling dynamic data source changes, performance optimization strategies, and avoiding common pitfalls like duplicate keys. The documentation is designed for developers building dynamic UIs who need to ensure efficient rendering and stable state management in HarmonyOS applications.

## Quick Navigation

- **Understand Rendering Mechanisms**
  - How does the initial render work? → 首次渲染.md
  - How does re-rendering and component reuse work? → 非首次渲染_3more.md
- **Manage Data and Keys**
  - What are the key generation rules and component creation logic? → ForEach循环渲染_3more.md
  - How to handle dynamic data changes (insert/delete)? → 数据源数组项发生变化.md
- **Optimize and Avoid Pitfalls**
  - What are the best practices for performance? → 使用建议.md
  - What are the anti-patterns to avoid? → 不推荐案例/

## Detailed Description

### ForEach循环渲染_3more.md ForEach循环渲染_3more.md
This document serves as a technical guide for using the ForEach interface to perform loop rendering based on array data in the ArkUI framework. It covers two main topics: key generation rules and component creation rules. Key information includes the use of the keyGeneratorFunc parameter to create unique identifiers for array elements, where key changes trigger component recreation. The text also highlights the necessity of compatible parent containers like List and warns against duplicate keys to ensure proper UI updates. This guide is intended for developers utilizing the Cangjie language and ArkUI framework to manage dynamic UI components efficiently.

### 使用建议.md 使用建议.md
This document serves as a usage guide for the ForEach component, focusing on best practices for key generation and performance optimization. It covers topics such as selecting unique keys for object data, the risks of using array indices, and handling basic data types. Key takeaways include using unique object IDs instead of indices to avoid rendering issues and performance loss, and converting basic data types to objects with IDs for dynamic sources. It also warns against mixing ForEach with LazyForEach in containers like List and Grid. The target audience is developers seeking to optimize UI rendering and component lifecycle management in their applications. Keywords: ForEach, key generation, performance, rendering, LazyForEach, UI components.

### 数据源数组项发生变化.md 数据源数组项发生变化.md
This tutorial demonstrates how to handle dynamic data source changes in ArkUI using Cangjie, specifically when array items are inserted or deleted. The document presents a code example implementing a "load more" feature where new items are appended to a list upon a gesture trigger. Key sections include the scenario setup, the implementation using ObservedArrayList and ForEach, and an explanation of the rendering logic. The primary takeaway is that data sources should be object arrays using unique IDs as keys to ensure stable rendering when the list length changes. This guide targets developers building dynamic UIs who need to understand how @State and ForEach interact to update the interface efficiently. Keywords: ArkUI, Cangjie, ForEach, ObservedArrayList, dynamic list, unique ID, @State.

### 非首次渲染_3more.md 非首次渲染_3more.md
This technical guide explains the rendering mechanism of the ForEach component during non-initial updates, focusing on key-based component reuse and creation. It covers the logic of key generation during re-rendering, demonstrates data modification scenarios with code examples, and outlines usage cases like static data source rendering for skeleton screens. Key takeaways include that ForEach reuses existing components for matching keys but creates new ones for unique keys, illustrated by modifying an array item. The document also provides a skeleton screen implementation example. Targeted at HarmonyOS developers using the Cangjie language and ArkUI framework, this guide helps optimize list rendering performance and understand state management.

### 首次渲染.md 首次渲染.md
This technical guide explains the initial rendering mechanism of the ForEach component in ArkUI development, focusing on key generation and component creation. It covers the implementation of keyGeneratorFunc and compares rendering outcomes between data sources with unique values versus duplicate values. Key takeaways include the rule that unique keys generate corresponding components, whereas duplicate keys lead to undefined behavior where only one component is created instead of multiple. This document targets application developers using the ArkUI framework and Cangjie language who need to understand list rendering logic and avoid key conflicts.

### 不推荐案例/
This directory serves as a comprehensive repository of technical documentation designed to highlight anti-patterns and common pitfalls encountered during ArkUI development using the Cangjie language. It specifically addresses critical issues related to the `ForEach` component, focusing on scenarios that lead to incorrect UI updates and significant performance degradation caused by improper key generation strategies. Key topics covered include component lifecycle management, state management nuances, rendering efficiency optimization, and the implementation of stable key generators to ensure smooth UI behavior. These resources are essential for developers seeking to optimize application performance and avoid complex bugs associated with list rendering and dynamic data manipulation. By understanding these "not recommended" cases, developers can write more robust and efficient code.