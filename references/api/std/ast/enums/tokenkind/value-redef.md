<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-redef" parent="std.ast.enum.tokenkind" -->
# TokenKind.REDEF

[← TokenKind](index.md)

## 签名

```cangjie role=signature
REDEF
```

构造一个表示 `redef` 的枚举实例。
