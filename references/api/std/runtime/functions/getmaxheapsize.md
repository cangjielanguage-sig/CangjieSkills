<!-- cj-doc kind="api-member" level="5" id="std.runtime.func.getmaxheapsize" parent="std.runtime" -->
# getMaxHeapSize()

[← std.runtime](../index.md)

## 签名

```cangjie role=signature
public func getMaxHeapSize(): Int64
```

获取仓颉堆可以使用的最大值，单位为 byte。

## 契约

返回值：

- Int64 - 仓颉堆可以使用的最大值，单位为 byte。
