# 边框设置

This directory covers border styling and configuration for UI components in the Cangjie ArkUI framework. It includes API references for setting border properties like width, color, style, and radius, as well as practical code examples. It also defines fundamental data types for edge styling, including support for Right-to-Left (RTL) layouts. This documentation is suitable for UI developers and engineers working on HarmonyOS or OpenHarmony applications who need to customize the visual appearance and geometry of component boundaries.

## Quick Navigation

**What do you want to learn?**

*   See practical code examples for border styling → 示例代码.md
*   Set rounded corners (borderRadius) for components → func_borderRadiusLength_600a69f8.md
*   Configure general border properties (width, color, style) → 边框设置_7more.md
*   Understand fundamental data types for edges (EdgeColors, EdgeWidths) → 基础类型定义/

## Detailed Description

### func_borderRadiusLength_600a69f8.md func_borderRadiusLength_600a69f8.md
This document serves as API reference documentation for the borderRadius function in the Cangjie language, designed to set rounded corners for UI components. The content details the function signature, system capabilities, version requirements, and a parameter table defining the radius for each corner. Key information includes that all four parameters are optional named arguments defaulting to 0.vp, the radius is constrained by the component's dimensions (maximum half of width or height), and the function returns the component instance for chaining. This reference guide targets UI developers using the ArkUI framework who need to customize component styling with specific border radii.

### 示例代码.md 示例代码.md
This document provides a Cangjie code example demonstrating how to apply different border styles to UI components within a HarmonyOS application. The content focuses on the implementation of the EntryView class, utilizing layout structures like Column and Flex to arrange Text elements. Key information includes the specific syntax for setting border properties such as borderStyle (Dashed and Dotted), borderWidth, borderColor, and borderRadius. The example visually distinguishes between two text boxes to show the effect of these styles. This snippet serves as a practical reference for developers working with the Cangjie UIKit, specifically those looking to customize component appearance and layout in OpenHarmony projects.

### 边框设置_7more.md 边框设置_7more.md
This document serves as an API reference for configuring component border styles within the ArkUI framework using the Cangjie language. It details functions for setting comprehensive border properties via the border method, as well as individual attributes like borderStyle, borderWidth, borderColor, and borderRadius. Key information includes support for distinct edge widths using EdgeWidths, percentage-based radii, and default values like Color.BLACK. All methods return the current instance to facilitate fluent interface chaining. This documentation is intended for UI developers seeking to customize the visual appearance and geometry of component boundaries in ArkUI applications.

### 基础类型定义/
This directory contains API reference documents for fundamental styling classes within the Cangjie ArkUI framework. It focuses on defining visual properties for UI component borders, including colors, widths, and corner radii. Key classes covered include EdgeColors, EdgeWidths, and their localized variants that support Right-to-Left (RTL) layouts. These definitions are essential for creating responsive and internationalized user interfaces. The documentation is suitable for ArkUI developers using the Cangjie language who need to customize the appearance and layout behavior of UI elements.