# class_Matcher_supdeprecatedsup

This directory contains API reference documentation for the deprecated Cangjie Matcher class and its associated regular expression functions. It covers legacy pattern matching capabilities, including scanning input sequences, finding matches, replacing text, and splitting strings. Key topics include the Matcher constructor, methods like `findAll`, `replace`, and `split`, as well as state management functions. The documentation highlights that this class is deprecated in favor of the Regex class. It is suitable for Cangjie developers maintaining legacy code or migrating existing applications to modern regex implementations.

## Quick Navigation

**What do you want to learn?**

*   Understand the deprecated Matcher class structure and basic matching methods → class_Matcher_supdeprecate_5more.md
*   Find all matches, check full matches, or retrieve matched strings → func_findAll_6more.md
*   Perform string replacements, manage matcher regions, or split sequences → func_replaceString_Int64_7more.md
*   Split strings using a specific limit parameter → func_splitInt64.md

## Detailed Description

### class_Matcher_supdeprecate_5more.md class_Matcher_supdeprecate_5more.md
This document provides API reference documentation for the deprecated Cangjie Matcher class, which is designed for scanning input sequences and performing regular expression matching. The content details the class constructor, which accepts a Regex object and a String, alongside key methods such as allCount for retrieving total matches and find for locating specific subsequences either sequentially or from a set index. Crucially, the documentation notes that this class is deprecated in favor of the Regex class and specifies return types like Option<MatchData> and potential exceptions like RegexException. This reference guide targets Cangjie developers implementing pattern matching who need to understand legacy functionality or migrate existing code.

### func_findAll_6more.md func_findAll_6more.md
This document serves as API reference documentation for regex matching and manipulation functions in the Cangjie programming language. It details six specific functions: findAll, fullMatch, getString, matchStart, region, and replace. Key information includes functionality for finding all matches, matching entire sequences or starts, retrieving matched strings, and replacing text. Most functions return Option types or Strings, with RegexException handling for extraction failures. The target audience is Cangjie developers implementing regular expression logic who require precise syntax and behavior definitions.

### func_replaceString_Int64_7more.md func_replaceString_Int64_7more.md
This document serves as API reference documentation for regex manipulation functions in the Cangjie programming language, specifically within a Matcher context. It details methods for string replacement, including replace for single occurrences and replaceAll for multiple or limited replacements. Additionally, it covers matcher state management functions like resetRegion, resetString, and setRegion to control search boundaries, alongside a split function for dividing sequences. Key information includes parameter definitions, return types, and exception handling for invalid indices. This guide is intended for developers implementing advanced regular expression operations and string processing in Cangjie applications.

### func_splitInt64.md func_splitInt64.md
This document serves as API reference documentation for the split function in the Cangjie programming language, specifically detailing the overload that accepts an Int64 limit parameter. The main sections include the function signature, a functional description, parameter definitions, and return value specifications. Key information highlights that the function splits an input sequence into substrings based on a regular expression. The behavior depends on the limit parameter: if positive, it returns at most that many substrings; if zero or negative, it returns the maximum possible number of splits. The return type is an array of strings. This technical reference is intended for Cangjie developers seeking to perform string manipulation, requiring knowledge of the language's core types and regular expressions.