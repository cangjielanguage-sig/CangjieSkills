<!-- cj-doc kind="api-member" level="6" id="std.ast.class.decl.field-identifier_" parent="std.ast.class.decl" -->
# Decl.identifier_

[← Decl](index.md)

## 签名

```cangjie role=signature
protected var identifier_: Token
```

获取或设置声明节点的标识符，如 `class foo {}` 中的 `foo`。

## 契约

类型：Token
