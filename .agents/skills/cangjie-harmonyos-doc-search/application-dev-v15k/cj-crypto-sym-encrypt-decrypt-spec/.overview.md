# cj-crypto-sym-encrypt-decrypt-spec

This directory serves as a comprehensive repository for technical specifications regarding symmetric key encryption and decryption algorithms. It primarily focuses on the implementation details of widely used cryptographic standards, specifically 3DES, SM4, and AES. The content covers essential operational aspects such as supported modes of operation (including ECB, CBC, and GCM), standard padding schemes (PKCS5 and PKCS7), and the precise syntax for parameter configuration. Furthermore, the documents offer critical guidance on technical constraints like key lengths, block sizes, and specific API requirements, notably for API version 12 and above. This resource is specifically tailored for developers and system architects who are integrating cryptographic functions using the CryptoArchitectureKit or similar libraries, providing them with the necessary knowledge to understand algorithm configurations and string parameter formats for securely instantiating cryptographic instances.

## Quick Navigation

- **What do you want to learn?**
    - Understand symmetric encryption specs (3DES, SM4, AES) → 对称密钥加解密算法规格/
    - Learn about supported modes (ECB, CBC, GCM) and padding schemes → 对称密钥加解密算法规格/
    - Check API requirements (v12+) and parameter configuration syntax → 对称密钥加解密算法规格/

- **What do you want to do?**
    - Configure algorithm parameters for CryptoArchitectureKit → 对称密钥加解密算法规格/
    - Determine correct key lengths and block sizes → 对称密钥加解密算法规格/

## Detailed Description

### 对称密钥加解密算法规格/
This directory contains technical specifications for symmetric key encryption and decryption algorithms. It covers implementation details for 3DES, SM4, and AES, including supported modes (ECB, CBC, GCM, etc.), padding schemes (PKCS5, PKCS7), and parameter configuration syntax. The documents provide guidance on key lengths, block sizes, and API requirements (specifically API version 12+). It is suitable for developers integrating cryptographic functions using the CryptoArchitectureKit or similar libraries who need to understand algorithm configurations and string parameter formats for instantiating cryptographic instances.