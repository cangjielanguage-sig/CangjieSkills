<!-- cj-doc kind="api-member" level="6" id="std.collection.class.treemap.values" parent="std.collection.class.treemap" -->
# TreeMap<K, V> where K <: Comparable<K>.values

[← TreeMap<K, V> where K <: Comparable<K>](index.md)

## 签名

```cangjie role=signature
public func values(): Collection<V>
```

返回 TreeMap 中包含的值，并将所有的 value 存储在一个容器中。

## 契约

返回值：

- Collection\<V> - 包含所有值的集合。
