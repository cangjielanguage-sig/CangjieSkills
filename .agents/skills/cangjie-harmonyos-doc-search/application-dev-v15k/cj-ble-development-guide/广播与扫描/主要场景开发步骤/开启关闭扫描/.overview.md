# 开启关闭扫描

This directory focuses on the implementation of Bluetooth Low Energy (BLE) scanning functionality, specifically the processes of starting and stopping device discovery. It covers the complete lifecycle of BLE scanning, including system prerequisites, scan management, and the critical parsing of raw advertising data packets. Key topics include the BleScanManager class, scan filters, and extracting specific data like service UUIDs and manufacturer information. The content is tailored for application developers working on HarmonyOS, OpenHarmony, or ArkTS environments who need to integrate robust Bluetooth connectivity and data interpretation into their applications.

## Quick Navigation

**What do you want to learn?**

*   Implement the full BLE scanning lifecycle using Cangjie → 开启关闭扫描_1.md
*   Understand the logic for parsing raw advertising data packets → 开启关闭扫描_2.md
*   Extract specific data types (UUIDs, manufacturer data) using ArkTS → 开启关闭扫描_3.md

## Detailed Description

### 开启关闭扫描_1.md 开启关闭扫描_1.md
This document provides a technical tutorial for implementing Bluetooth Low Energy (BLE) scanning functionality using the Cangjie programming language. It covers essential prerequisites such as system capabilities and device states, followed by a detailed code example demonstrating the BleScanManager class. Key takeaways include methods for subscribing to scan results, configuring scan filters and options, and parsing raw advertising data for specific information like service UUIDs and manufacturer data. Intended for application developers working on HarmonyOS or OpenHarmony systems, this guide explains the complete lifecycle of initiating, managing, and stopping BLE device discovery.

### 开启关闭扫描_2.md 开启关闭扫描_2.md
This document provides a technical code reference for parsing Bluetooth Low Energy (BLE) advertising data packets. It details the logic for iterating through raw byte arrays to extract specific information such as device flags, local names, TX power levels, and service UUIDs. Key sections include the main parsing loop that matches data types against constants like BLE_ADV_TYPE_LOCAL_NAME_COMPLETE and helper functions designed to process 16-bit, 32-bit, and 128-bit service UUIDs and manufacturer data. The code demonstrates how to handle variable-length data structures and convert them into usable string formats. This reference is intended for software developers working on Bluetooth connectivity, specifically those needing to interpret scan results within a mobile or embedded application context. Keywords: BLE, advertising data parsing, service UUIDs, manufacturer data, Bluetooth scanning, byte array processing.

### 开启关闭扫描_3.md 开启关闭扫描_3.md
This document provides code examples for parsing Bluetooth Low Energy (BLE) scan results, specifically focusing on handling raw data like UUIDs, service data, and manufacturer data. It details helper functions such as `getUuidFromArray` for formatting 16, 32, and 128-bit UUIDs, and `parseManufactureData` for extracting manufacturer IDs. The content includes a `ScanResultCb` callback class implementation to process incoming scan results and log device IDs. Additionally, it references the Bluetooth service subsystem error codes. This technical reference is designed for developers using ArkTS to implement BLE scanning logic, offering practical snippets for data extraction and result handling within a broader scanning application workflow.