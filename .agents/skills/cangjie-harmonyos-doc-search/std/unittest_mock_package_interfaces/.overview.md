# unittest_mock_package_interfaces

This directory serves as a specialized repository for the `unittest_mock_package_interfaces`, providing in-depth API reference documentation for the `ValueListener` interface within the Cangjie unittest mock package. Its primary purpose is to enable developers to monitor, capture, and process arguments passed to mock stubs with high precision. The content covers a wide array of functionalities, including key member functions like `addCallback` for defining custom operations, `allValues` and `lastValue` for retrieving captured data, and static methods `new` and `onEach` for interface instantiation. It also clarifies the interface's critical role in parameter matching when used in conjunction with `argThat` or `capture`. Illustrated with practical code examples demonstrating runtime assertions on captured objects, this resource is indispensable for developers writing unit tests who need to verify specific argument values and behaviors within mocked method calls to ensure application stability.

## Quick Navigation

What do you want to learn?

*   Access the complete API reference for the ValueListener interface → unittest_mock_package_interfaces.md
*   Learn how to effectively monitor and process arguments in mock stubs → unittest_mock_package_interfaces.md
*   Master the usage of member functions like addCallback, allValues, and lastValue → unittest_mock_package_interfaces.md
*   Understand static methods for instantiation: new and onEach → unittest_mock_package_interfaces.md
*   Explore the interface's role in parameter matching with argThat or capture → unittest_mock_package_interfaces.md
*   Study code examples showing runtime assertions on captured objects → unittest_mock_package_interfaces.md

## Detailed Description

### unittest_mock_package_interfaces.md unittest_mock_package_interfaces.md
This document provides API reference documentation for the ValueListener interface in the Cangjie unittest mock package, enabling developers to monitor and process arguments passed to mock stubs. It details key member functions such as addCallback for defining operations, allValues and lastValue for retrieving captured data, and static methods new and onEach for instantiation. The text explains the interface's role in parameter matching alongside argThat or capture, illustrated by a code example demonstrating runtime assertions on captured objects. This resource is intended for developers writing unit tests who need to verify specific argument values and behaviors within mocked method calls.