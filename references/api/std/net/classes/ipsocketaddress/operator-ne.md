<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipsocketaddress.operator-ne" parent="std.net.class.ipsocketaddress" -->
# IPSocketAddress.!=

[← IPSocketAddress](index.md)

## 签名

```cangjie role=signature
public operator func !=(rhs: IPSocketAddress): Bool
```

判断两个 IPSocketAddress 对象是否不等。

## 契约

参数：

- rhs: IPSocketAddress - 参与比较的 IPSocketAddress 对象。

返回值：

- Bool - 如果两个 IPSocketAddress 对象不等，则返回 `true`；否则，返回 `false`。
