# extend_Int64_WrappingOpInt64_WrappingPow

This directory provides API reference documentation for extending the `Int64` type in the Cangjie programming language with wrapping arithmetic and bitwise operations. It specifically details the implementation of the `WrappingOp` and `WrappingPow` interfaces. The content covers a comprehensive range of operations, including addition, subtraction, multiplication, division, modulo, negation, exponentiation, and bit shifting. A key focus is the high-bit truncation strategy used to handle overflow, ensuring that operations wrap around rather than throwing errors. This documentation is essential for Cangjie developers who require precise control over integer overflow behavior and perform low-level data processing.

## Quick Navigation

**What do you want to learn?**

*   Understand the general implementation of WrappingOp and WrappingPow interfaces for Int64, including arithmetic and left shift operations → extend_Int64_WrappingOpIn_10more.md
*   Learn about specific wrapping functions like right shift (`wrappingShr`) and subtraction (`wrappingSub`) for UInt64 and Int64 → func_wrappingShrUInt64_2more.md

## Detailed Description

### extend_Int64_WrappingOpIn_10more.md extend_Int64_WrappingOpIn_10more.md
This API reference document details the implementation of the WrappingOp and WrappingPow interfaces for the Int64 type in the Cangjie language. It covers a comprehensive list of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, exponentiation, and left shift. Key information highlights that all functions utilize a high-bit truncation strategy to handle overflow, ensuring operations wrap around rather than erroring. Specifically, the wrappingShl function uses the low 6 bits of the shift amount if it exceeds the bit width. This documentation targets Cangjie developers requiring precise control over integer overflow behavior in their applications.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md
This document serves as API reference documentation for wrapping arithmetic and bit manipulation functions within the Cangjie programming language. It details two specific functions: `wrappingShr`, which performs a right shift using the low 6 bits of the shift amount if it exceeds the bit width, and `wrappingSub`, which executes subtraction while truncating high bits upon overflow. Both functions accept a parameter of type `UInt64` or `Int64` and return an `Int64` result. This guide is intended for Cangjie developers requiring precise control over overflow behavior and low-level data processing.