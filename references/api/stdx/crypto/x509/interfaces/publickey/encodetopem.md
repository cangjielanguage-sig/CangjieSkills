<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.interface.publickey.encodetopem" parent="stdx.crypto.x509.interface.publickey" -->
# PublicKey.encodeToPem

[← PublicKey](index.md)

## 签名

```cangjie role=signature
override func encodeToPem(): PemEntry
```

将公钥编码为 PEM 格式。

## 契约

返回值：

- PemEntry - 公钥数据 PEM 格式编码生成的对象。
