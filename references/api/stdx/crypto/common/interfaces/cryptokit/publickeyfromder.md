<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.common.interface.cryptokit.publickeyfromder" parent="stdx.crypto.common.interface.cryptokit" -->
# CryptoKit.publicKeyFromDer

[← CryptoKit](index.md)

## 签名

```cangjie role=signature
func publicKeyFromDer(encoded: DerBlob): PublicKey
```

将公钥从 DER 格式解码。

## 参数

- encoded: DerBlob - 待解码的 DerBlob 对象。

## 返回值

- PublicKey - 解码得到的公钥。

