# 结构体

This directory contains API reference documentation for core data structures in the Cangjie programming language. It covers fundamental types such as Arrays, Strings, Ranges, Durations, and Hashers, alongside specialized structures for C interoperability and low-level memory management. The documentation details struct definitions, properties, methods, and operator overloads, providing essential guidance for both high-level application development and system-level programming. It is suitable for Cangjie developers seeking to understand type behaviors, implement custom algorithms, or perform Foreign Function Interface (FFI) operations with C code.

## Quick Navigation

**What do you want to learn?**

*   **Manage C interoperability and raw memory?**
    *   Handle raw pointers for Arrays → struct_CPointerHandleT_where_T_CType.md
    *   Manage resource lifecycles and cleanup → struct_CPointerResourceT_w_2more.md
    *   Perform manual memory allocation (malloc/free) → struct_LibC.md
*   **Work with fundamental data types?**
    *   Manipulate generic Arrays and sequences → `struct_ArrayT/`
    *   Process Strings and UTF-8 data → `struct_String/`
    *   Define and iterate over Ranges → `struct_RangeT_where_T_CountableT_Comparab_0ec58319/`
*   **Use utility structures?**
    *   Calculate time intervals with high precision → `struct_Duration/`
    *   Implement hashing algorithms → `struct_DefaultHasher/`

## Detailed Description

### struct_CPointerHandleT_where_T_CType.md struct_CPointerHandleT_where_T_CType.md
This API reference document details the CPointerHandle struct in the Cangjie language, designed to represent the raw pointer of an Array for C interoperability. The content covers the struct's definition, its properties (array and pointer), and available initialization methods. Key information includes the constraint that the generic type T must implement the CType interface, and the critical note that both constructors are deprecated in favor of the acquireArrayRawData function. This documentation is intended for Cangjie developers performing low-level memory management and C language integration. Keywords: Cangjie, CPointerHandle, struct, API, C interoperability, raw pointer, Array, deprecated, CType.

### struct_CPointerResourceT_w_2more.md struct_CPointerResourceT_w_2more.md
This API reference document details resource management structures for C interoperability in the Cangjie language. It covers the generic CPointerResource struct and the CStringResource struct, both inheriting from the Resource interface. Key components include the value property holding the underlying pointer or string, alongside methods close() to free memory and isClosed() to check release status. Instances are typically obtained via asResource() member functions. This documentation targets developers working with low-level C types who need explicit memory lifecycle management and safe resource cleanup.

### struct_LibC.md struct_LibC.md
This document serves as API reference documentation for the Cangjie programming language's LibC struct, facilitating high-frequency C interoperability operations. It details static methods for manual memory management, specifically allocating and releasing heap memory for CType instances and C-style strings. Key sections include malloc for allocating memory blocks, mallocCString for converting Cangjie strings to null-terminated C strings, and corresponding free functions for deallocation. The documentation highlights unsafe operations, parameter requirements like count, and potential exceptions such as IllegalArgumentException or IllegalMemoryException. This reference is intended for developers working on Foreign Function Interface (FFI) tasks who need to manage C memory resources directly within Cangjie code.

### struct_ArrayT/
This directory contains comprehensive API reference documentation for the `struct_ArrayT` in the Cangjie programming language. It covers the core definition of the generic Array type, including its constructors, properties, and essential manipulation methods. The documentation details various operator overloads for element access and slicing, as well as extensions for specific interfaces like `ToString` and `Equatable`. It is suitable for Cangjie developers seeking detailed syntax, behavioral descriptions, and usage examples for array operations, ranging from basic initialization to advanced functional transformations and comparisons.

### struct_DefaultHasher/
This directory contains comprehensive API reference documentation for the `DefaultHasher` struct within the Cangjie programming language's standard library. It focuses on the default implementation of the Hasher interface, providing essential details on struct definition, initialization with optional seeds, and state management. The content covers a wide array of methods for finalizing, resetting, and updating hash states, including overloaded `write` functions for numerous primitive data types such as integers, floats, booleans, and strings. This documentation is specifically designed for Cangjie developers who are implementing custom hashing algorithms, building data structures, or working with hash-based collections and require precise control over hash combination operations.

### struct_Duration/
This directory contains API reference documentation for the `Duration` struct in the Cangjie programming language. It covers the comprehensive functionality required to represent and manipulate time intervals with nanosecond precision. The documentation details arithmetic and comparison operators, unit conversion methods, static constants for time units, and core struct properties including boundary values. It is suitable for Cangjie developers who need to perform precise time calculations, formatting, and comparisons within their applications.

### struct_RangeT_where_T_CountableT_Comparab_0ec58319/
This directory provides comprehensive API documentation for the generic `Range` struct within the Cangjie programming language. It focuses on the implementation of sequences defined by a fixed range and step, where the generic type `T` is constrained to be both Countable and Comparable. The documentation covers the core struct definition, including initialization logic, properties like start and end, and iteration capabilities. Additionally, it details interface extensions for `Equatable` and `Hashable` to support equality comparisons and hashing. This resource is essential for Cangjie developers working with range-based sequences, generic constraints, and collection operations. It includes specific references for struct definitions and interface extensions to guide developers in effective implementation and usage of the Range type.

### struct_String/
This directory contains comprehensive API reference documentation for the `String` struct in the Cangjie programming language. It covers a wide range of string manipulation capabilities, including basic comparisons, searching, splitting, trimming, and case conversion. The documentation details operator overloads for indexing and concatenation, as well as static methods for high-performance UTF-8 handling. Key topics include UTF-8 byte arrays, rune iteration, memory constraints, and exception handling. This resource is suitable for Cangjie developers seeking detailed syntax, parameter definitions, and usage examples for implementing efficient string processing logic in their applications.