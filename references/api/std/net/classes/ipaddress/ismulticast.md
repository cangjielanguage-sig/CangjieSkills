<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.ismulticast" parent="std.net.class.ipaddress" -->
# IPAddress.isMulticast

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public open func isMulticast(): Bool
```

判断此 IPAddress 对象是不是多播地址。

## 契约

返回值：

- Bool - 返回 true 表示是多播地址，否则返回 false。
