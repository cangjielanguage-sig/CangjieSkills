# cj-appendix-thread

This directory provides a comprehensive technical guide dedicated to thread control management within the Cangjie programming language, specifically tailored for HarmonyOS application development. It addresses the architectural necessity of separating UI and non-UI logic to ensure application responsiveness and stability. The content covers the development paradigm for custom thread scheduling, thread creation using the `spawn` keyword, and the `launch` function API. It explains how to bind Cangjie user threads to the OS UI thread using the `Main` object to prevent performance issues. This resource is essential for Cangjie developers looking to master concurrency, manage UI state updates, and optimize application performance through proper thread handling. It assumes the reader has a foundational understanding of concurrency concepts and UI state management.

## Quick Navigation

**What do you want to learn?**

*   Understand the background of UI versus non-UI logic separation → cj-appendix-thread.md
*   Learn the development paradigm for custom thread scheduling → cj-appendix-thread.md
*   Master thread creation using the `spawn` keyword → cj-appendix-thread.md
*   Understand the `launch` function API for task submission → cj-appendix-thread.md
*   Learn how to bind Cangjie user threads to the OS UI thread using the `Main` object → cj-appendix-thread.md
*   View practical code examples demonstrating state updates within a UI thread context → cj-appendix-thread.md

## Detailed Description

### cj-appendix-thread.md cj-appendix-thread.md

This document serves as a technical guide for managing thread control in Cangjie when developing HarmonyOS applications, specifically addressing the execution of UI-related logic. It covers the background of UI versus non-UI logic separation, the development paradigm for custom thread scheduling, thread creation using the spawn keyword, and the launch function API. Key information includes binding Cangjie user threads to the OS UI thread using the Main object to prevent performance issues. The document details the launch function for submitting tasks to the main thread and provides a practical code example demonstrating state updates within a UI thread context. Targeted at Cangjie developers, this guide requires understanding of concurrency and UI state management.