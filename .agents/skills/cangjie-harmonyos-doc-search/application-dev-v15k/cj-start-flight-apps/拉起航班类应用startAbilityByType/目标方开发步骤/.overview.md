# 目标方开发步骤

This directory serves as a comprehensive technical resource for developers tasked with building the target side of a flight query application within the HarmonyOS ecosystem. It meticulously covers the necessary steps to configure `module.json5` for feature declaration using `linkFeature` and URI attributes, as well as parsing incoming parameters inside `UIAbility` lifecycle methods. The documentation provides detailed parameter definitions for two primary scenarios: querying by flight number and by location, specifying required fields such as `flightNo` and `originLocation`. Furthermore, it includes code examples for page routing logic utilizing `localStorage` and `windowStage`. These guides are specifically designed for developers utilizing the Cangjie language and ArkUI frameworks to implement feature-based application invocation and efficient data handling.

## Quick Navigation

What do you want to learn?

- Configure module features and parse incoming parameters in UIAbility? → 目标方开发步骤_1.md
- Implement page routing logic using URIs and localStorage? → 目标方开发步骤_2.md

## Detailed Description

### 目标方开发步骤_1.md
This guide provides step-by-step instructions for developing the target side of a flight query application. It covers configuring module.json5 to declare supported features using linkFeature and URI attributes, and parsing incoming parameters within UIAbility lifecycle methods. Key information includes specific parameter definitions for two scenarios: querying by flight number and by location, detailing required fields like flightNo and originLocation. The document provides a complete Cangjie code example demonstrating how to handle want.uri and want.parameters. Intended for developers using the Cangjie language and HarmonyOS frameworks, this tutorial facilitates the implementation of feature-based application invocation and data handling.

### 目标方开发步骤_2.md
This document provides a code example demonstrating page routing logic based on specific URIs within a development workflow. It details the `loadPage` function, handling scenarios for flight number queries and location-based queries, alongside a default fallback. The code illustrates how to use `localStorage` to pass parameters like flight numbers and dates before invoking `windowStage.loadContent` to display the appropriate view. Intended for developers implementing target-side integration logic, likely within a HarmonyOS ArkUI environment, requiring knowledge of URI handling and local storage management. Keywords: routing, URI, localStorage, windowStage, parameters.