# class_GetterActionSelectorTRet

This directory provides comprehensive API documentation for the `GetterActionSelector` class and associated exception handling functions within a unit testing mock framework. It focuses on enabling developers to configure specific behaviors for property getter functions found within mock or spy objects. The content covers essential methods for stubbing getters, allowing users to return specific values, access synthetic fields, invoke original logic, or throw exceptions. Furthermore, it details how to manage void returns and utilize exception factories. The documentation emphasizes the use of return types like `CardinalitySelector` and `Continuation` to facilitate method chaining and set execution expectations. This reference is primarily suitable for developers writing unit tests using the Cangjie programming language who need to implement precise mocking strategies and robust error handling in their test cases.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition and methods for configuring property getter behaviors in mock objects → class_GetterActionSelector_8more.md
*   Learn how to configure stub behaviors specifically for throwing exceptions or handling void returns → func_throws_-_Exception_2more.md

## Detailed Description

### class_GetterActionSelector_8more.md class_GetterActionSelector_8more.md

This API documentation defines the GetterActionSelector class, which enables developers to configure behavior for property getter functions within mock or spy objects. The document details the class definition and its primary methods, including getsField, getsOriginal, returns, returnsConsecutively, and throws. Key information includes how these methods allow users to stub getters by returning specific values, accessing synthetic fields, invoking original logic, or throwing exceptions. Most methods return CardinalitySelector or Continuation objects to support method chaining and setting execution expectations. This reference is intended for developers using the unit testing mock framework to write test cases with stubbed properties.

### func_throws_-_Exception_2more.md func_throws_-_Exception_2more.md

This document serves as API reference material for a testing framework, detailing methods to configure stub behaviors regarding exceptions and void returns. The primary sections include the `throws` function, which allows developers to specify that a stub should throw a generated exception, and an extension for `MethodActionSelector<Unit>` that introduces a `returns` function. Key information highlights that `throws` accepts an exception factory closure, while `returns` indicates a no-action behavior; both return a `CardinalitySelector` for defining expected execution counts. Intended for developers writing unit tests, this documentation requires knowledge of mocking concepts and the Cangjie programming language syntax.