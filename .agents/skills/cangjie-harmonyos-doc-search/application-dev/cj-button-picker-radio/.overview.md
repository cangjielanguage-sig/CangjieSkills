# cj-button-picker-radio

This directory contains the essential documentation for the `cj-button-picker-radio` component, a fundamental UI element designed for the ArkUI framework using the Cangjie language. It serves as a comprehensive guide for HarmonyOS developers who need to implement single-selection interfaces where users are required to choose one option from a mutually exclusive list. The documentation covers the complete lifecycle of the Radio component, starting with initialization which requires `value` and `group` parameters to effectively manage mutual exclusion. It details the `checked` attribute for state management and the `onChange` event for handling user inputs dynamically. By combining API references with practical code examples, this guide helps developers build robust, standard-compliant interfaces efficiently, ensuring a seamless user experience in HarmonyOS applications.

## Quick Navigation

**What do you want to learn?**
*   Understand the fundamental API and initialization of the Radio component → cj-button-picker-radio.md
*   Learn how to configure `value` and `group` parameters to ensure mutual exclusion → cj-button-picker-radio.md
*   Manage selection states effectively using the `checked` attribute → cj-button-picker-radio.md
*   Implement dynamic interaction logic with the `onChange` event → cj-button-picker-radio.md
*   Review a practical code example for creating a row of radio buttons → cj-button-picker-radio.md
*   Explore support for standard ArkUI attributes and events in Cangjie → cj-button-picker-radio.md

## Detailed Description

### cj-button-picker-radio.md cj-button-picker-radio.md
This document provides comprehensive API reference documentation for the Radio component within the ArkUI framework, specifically written for the Cangjie language. It covers the component's initialization in detail, which requires the configuration of `value` and `group` parameters to effectively manage mutual exclusion among various options. Key sections of the document include an explanation of the `checked` attribute for setting the selection state and the `onChange` event for handling user interactions. The document also provides a practical code example demonstrating how to implement a row of radio buttons within a shared group. Intended for HarmonyOS developers, this guide explains how to create single-selection interfaces, supporting standard ArkUI attributes and events to ensure full compatibility and functionality within the application ecosystem.