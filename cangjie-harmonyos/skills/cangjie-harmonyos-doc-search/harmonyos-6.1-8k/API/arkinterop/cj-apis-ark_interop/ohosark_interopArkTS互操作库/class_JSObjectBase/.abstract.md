本目录包含 Cangjie 语言中 `JSObjectBase` 类的 API 参考文档，专注于与 JavaScript 和 ArkTS 的互操作。内容涵盖了绑定仓颉对象、调用方法、定义访问器的方法，以及用于获取、设置和检查 JS 对象属性的函数。核心主题包括 `JSValue`、`JSContext`、属性描述符和跨语言对象绑定。本文档适用于需要与 ArkTS 运行时或 JavaScript 环境进行深度交互的 Cangjie 应用开发者，提供了安全高效操作对象所需的工具。