<!-- cj-doc kind="api-member" level="7" id="std.math.numeric.struct.bigint.issigned" parent="std.math.numeric.struct.bigint.extension.extend-bigint-integer-bigint" -->
# BigInt.isSigned

[← extend BigInt <: Integer<BigInt>](extensions/extend-bigint-integer-bigint.md)

## 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 BigInt 类型是否是有符号类型。

## 契约

返回值：

- Bool - 总是返回 `true`。
