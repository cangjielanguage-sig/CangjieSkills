# cj-huks-refined-user-identity-authentication

This directory serves as a specialized repository for technical documentation and code examples related to the implementation of fine-grained user identity authentication and access control within the HarmonyOS ecosystem. It specifically targets developers utilizing the Cangjie programming language to build secure applications. The core focus is on the integration of the Universal Keystore Kit (HUKS) to enforce robust secondary authentication mechanisms—such as biometric verification or lock screen passwords—during critical cryptographic operations. By detailing the use of specific tags like `HUKS_TAG_KEY_AUTH_PURPOSE` and supporting modes such as AES/CBC, AES/GCM, and SM4/CBC, this resource provides the necessary guidance for ensuring that sensitive data remains protected and is only accessed following explicit user verification. It is an essential reference for software developers aiming to elevate the security posture of their applications through precise identity management.

## Quick Navigation

**What do you want to learn?**
*   Do you want to understand the architecture of fine-grained user identity authentication in HarmonyOS? → `细粒度用户身份认证访问控制开发指导/`
*   Are you looking for methods to enforce secondary authentication, such as biometric verification or lock screen passwords, for sensitive operations? → `细粒度用户身份认证访问控制开发指导/`
*   Do you need to know how to configure the `HUKS_TAG_KEY_AUTH_PURPOSE` tag correctly? → `细粒度用户身份认证访问控制开发指导/`
*   Are you searching for supported cryptographic modes like AES/CBC, AES/GCM, or SM4/CBC? → `细粒度用户身份认证访问控制开发指导/`
*   Do you require practical implementation workflows and code examples written in the Cangjie language? → `细粒度用户身份认证访问控制开发指导/`

## Detailed Description

This section provides an in-depth look at the resources available within the project directory. As the project focuses on high-security identity management, the documentation is structured to guide developers through the complexities of the Universal Keystore Kit (HUKS). The subdirectory listed below contains the primary technical guide necessary for implementing these security features effectively, ensuring that developers have access to the specific protocols and coding standards required for secure application development.

### 细粒度用户身份认证访问控制开发指导/
This directory provides a comprehensive technical guide for implementing fine-grained user identity authentication and access control within HarmonyOS applications. It focuses on leveraging the Universal Keystore Kit (HUKS) to enforce secondary authentication mechanisms—such as biometric verification or lock screen passwords—specifically for sensitive key operations like encryption and decryption. The documentation covers essential concepts, including the use of the `HUKS_TAG_KEY_AUTH_PURPOSE` tag, supported cryptographic modes (AES/CBC, AES/GCM, SM4/CBC), and practical implementation workflows using the Cangjie language. It is intended for software developers seeking to enhance application security by ensuring that sensitive data remains protected and is only accessed following explicit user verification.