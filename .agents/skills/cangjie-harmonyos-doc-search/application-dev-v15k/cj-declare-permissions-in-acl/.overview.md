# cj-declare-permissions-in-acl

This directory serves as a comprehensive technical resource for HarmonyOS developers focusing on the management and implementation of restricted permissions through the Access Control List (ACL). It details the end-to-end workflow required to legally and technically integrate sensitive capabilities into applications that go beyond standard privilege levels. Key topics include validating usage scenarios against strict platform policies, navigating the AppGallery Connect (AGC) interface to request a Profile file, and the precise code-side configurations needed to activate these permissions. The guide highlights the critical dependency on obtaining a permission certificate from AGC before any configuration attempts, ensuring that applications do not fail during installation due to missing authorization. Furthermore, it clarifies the procedural differences between debugging environments using auto-signing and production builds requiring manual release signing. This documentation is essential for developers whose applications require access to sensitive user data or system capabilities, ensuring compliance with strict auditing standards and the necessity for precise scenario documentation.

## Quick Navigation

What do you want to learn?

*   Understand the complete workflow for applying and declaring ACL permissions → cj-declare-permissions-in-acl.md
*   Check prerequisites and validate specific usage scenarios before applying → cj-declare-permissions-in-acl.md
*   Navigate the application process for a Profile file on AppGallery Connect (AGC) → cj-declare-permissions-in-acl.md
*   Configure code-side declarations and install permission certificates correctly → cj-declare-permissions-in-acl.md
*   Troubleshoot installation failures related to missing or incorrect permissions → cj-declare-permissions-in-acl.md
*   Differentiate between the processes for debugging auto-signing and manual release signing → cj-declare-permissions-in-acl.md

## Detailed Description

### cj-declare-permissions-in-acl.md

This guide provides comprehensive instructions for HarmonyOS developers on the process of applying for and declaring restricted permissions (ACL) required for special application scenarios. It covers prerequisites like checking usage scenarios, the application process via AppGallery Connect (AGC) for a Profile file, and the subsequent code-side declaration steps. Key takeaways include the necessity of obtaining a permission certificate from AGC before configuration to prevent installation failure, and the distinction between debugging auto-signing and manual release applications. Intended for developers needing sensitive permissions, it emphasizes strict auditing and the requirement for accurate scenario descriptions.