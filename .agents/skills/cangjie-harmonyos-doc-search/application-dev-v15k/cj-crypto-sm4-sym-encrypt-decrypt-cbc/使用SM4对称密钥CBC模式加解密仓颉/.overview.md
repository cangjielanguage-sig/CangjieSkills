# 使用SM4对称密钥CBC模式加解密仓颉

This directory focuses on implementing SM4 symmetric encryption and decryption using CBC mode within the Cangjie programming language. It covers the essential cryptographic workflows, including key generation, initialization vector (IV) management, and data processing using the CryptoArchitectureKit. Key topics include SM4_128, CBC mode, PKCS7 padding, and the `Cipher` lifecycle methods such as `init`, `update`, and `doFinal`. The content is designed for Cangjie developers who need to secure application data through robust cryptographic standards, providing both theoretical guidance and practical code examples to ensure data integrity and security.

## Quick Navigation

- **What do you want to learn?**
    - Understand the step-by-step implementation guide for SM4 encryption/decryption workflows and lifecycle management → 使用SM4对称密钥CBC模式加解密仓颉_3more.md
    - Review a complete synchronous code example demonstrating the full test cycle and data integrity verification → 示例.md

## Detailed Description

The following files provide the necessary documentation and code samples to master these cryptographic techniques in Cangjie.

### 使用SM4对称密钥CBC模式加解密仓颉_3more.md 使用SM4对称密钥CBC模式加解密仓颉_3more.md
This technical guide provides comprehensive, step-by-step instructions for implementing SM4 symmetric encryption and decryption using CBC mode in the Cangjie programming language. The document thoroughly covers two primary workflows: the encryption process and the decryption process. Key information includes generating a 128-bit symmetric key using `createSymKeyGenerator`, creating a `Cipher` instance with the specific transformation string `SM4_128|CBC|PKCS7`, and managing the cryptographic lifecycle via `init`, `update`, and `doFinal` methods. It also highlights the necessity of `IvParamsSpec` for CBC mode and handling data segmentation. This guide is intended for Cangjie developers utilizing the CryptoArchitectureKit to secure application data.

### 示例.md 示例.md
This document provides a synchronous code example demonstrating cryptographic operations using the Cangjie CryptoArchitectureKit. It covers random number generation, initialization vector (IV) creation, symmetric key conversion from raw data, and SM4 encryption and decryption processes. Key takeaways include the implementation of the `SM4_128_CBC_PKCS7` algorithm and a complete test workflow verifying data integrity through encryption and decryption cycles. Intended for developers implementing security features, this example assumes familiarity with Cangjie syntax and basic cryptographic concepts.