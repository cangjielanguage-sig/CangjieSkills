<!-- cj-doc kind="api-member" level="5" id="std.collection.func.count-t-iterable-t" parent="std.collection" -->
# count<T>(Iterable<T>)

[← std.collection](../index.md)

## 签名

```cangjie role=signature
public func count<T>(it: Iterable<T>): Int64
```

统计迭代器包含元素数量。

## 契约

参数：

- it: Iterable\<T> - 给定的迭代器。

返回值：

- Int64 - 返回迭代器包含元素数量。
