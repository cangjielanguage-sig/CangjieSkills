# 订阅进程内Ability生命周期变化

This directory provides comprehensive technical guidance on managing UIAbility lifecycle events within the same application process on the HarmonyOS platform. It details the mechanisms for subscribing to and unsubscribing from state changes—such as creation, window stage events, and foreground or background transitions—using the ApplicationContext. These capabilities are essential for gathering DFX statistics, including page dwell time, and for maintaining application health. The documentation includes practical code examples in both Cangjie and ArkTS, illustrating the implementation of `AbilityLifecycleCallback`, listener ID management, and proper resource cleanup. This resource is specifically designed for application developers utilizing the AbilityKit framework who require precise monitoring and control over their application's lifecycle behavior to ensure performance and stability.

## Quick Navigation

- What do you want to do?
    - Register and monitor lifecycle events for DFX statistics → 订阅进程内Ability生命周期变化_1.md
    - Unregister lifecycle callbacks to prevent resource leaks → 订阅进程内Ability生命周期变化_2.md

## Detailed Description

### 订阅进程内Ability生命周期变化_1.md 订阅进程内Ability生命周期变化_1.md
This technical guide explains how to subscribe to intra-process UIAbility lifecycle changes for DFX statistics like page dwell time. It covers the ApplicationContext subscription mechanism, lifecycle event types including creation, window stages, and foreground/background transitions, and listener ID management. Key information includes a Cangjie code example demonstrating the implementation of AbilityLifecycleCallback and registration via applicationContext.on(), noting that listener IDs auto-increment and return -1 upon exceeding limits. Intended for application developers using the AbilityKit framework to monitor application behavior. Keywords: UIAbility, ApplicationContext, lifecycle, DFX, Cangjie, AbilityKit, callback.

### 订阅进程内Ability生命周期变化_2.md 订阅进程内Ability生命周期变化_2.md
This technical guide demonstrates how to properly unregister lifecycle callbacks for an Ability within an application process to prevent resource leaks. The document focuses on the implementation of the `onDestroy` method, specifically detailing the use of the `ApplicationContext.off` method to remove listeners for `ABILITY_LIFE_CYCLE` events. Key information includes retrieving the application context, utilizing a specific `lifecycleId` for cancellation, and implementing error handling with try-catch blocks to manage `BusinessException` during the unregistration process. Intended for HarmonyOS developers using ArkTS, this snippet ensures proper cleanup of event subscriptions when a component is destroyed.