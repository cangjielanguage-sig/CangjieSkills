# UIAbility组件基本用法

This directory serves as a technical resource for mastering the basic usage of the UIAbility component in Cangjie application development. It covers essential procedures for initializing UI components, managing lifecycle callbacks, and preventing common launch issues like white screen errors. The documentation highlights the importance of the `onWindowStageCreate` method and demonstrates how to set initial views using `loadContent`. Furthermore, it explores the retrieval and application of UIAbilityContext for system-level operations and configuration management. Designed for developers utilizing DevEco Studio and the AbilityKit framework, this guide ensures proper setup of application entry points and effective context handling across different modules.

## Quick Navigation

**What do you want to learn or configure?**

-   **Configure the startup page and lifecycle initialization** → UIAbility组件基本用法_2more.md
-   **Access and utilize UIAbility context information** → 获取UIAbility的上下文信息.md

## Detailed Description

### UIAbility组件基本用法_2more.md UIAbility组件基本用法_2more.md
This technical guide outlines the basic usage of the UIAbility component, focusing on specifying the startup page to prevent white screen errors during application launch. The document details the onWindowStageCreate() lifecycle callback and demonstrates using the WindowStage object's loadContent() method to set the initial view. It includes a Cangjie code example showing how to replace the default "Index" page with a custom page like "EntryView". Intended for developers using DevEco Studio, this guide provides essential configuration steps for UIAbility initialization and context management.

### 获取UIAbility的上下文信息.md 获取UIAbility的上下文信息.md
This document provides a technical guide for developers on accessing and utilizing UIAbilityContext in Cangjie applications. It covers retrieving context within the UIAbility class using `this.context`, sharing it with UI pages via global variables, and invoking lifecycle methods like `startAbility` and `terminateSelf`. Key takeaways include the pattern of storing context globally for component access and proper error handling during termination. Intended for application developers using the AbilityKit framework, this tutorial explains how to manage configuration information and control ability instances effectively.