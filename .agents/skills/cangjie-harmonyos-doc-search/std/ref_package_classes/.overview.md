# ref_package_classes

This directory contains the essential API reference documentation for the class definitions within the `ref` package of the Cangjie programming language. It focuses specifically on weak reference mechanisms, which are critical tools for advanced memory management in modern software development. The content covers the generic `WeakRef` class and its sealed abstract parent, `WeakRefBase`, providing developers with the necessary knowledge to implement sophisticated garbage collection strategies. By utilizing a specified `CleanupPolicy`, developers can allow the garbage collector to handle reachable objects efficiently without manual intervention. The documentation also details key members such as the `value` property, which returns an `Option` type for safe access, and the `clear` method for forced cleanup. This resource is highly suitable for systems programmers and application developers who require precise control over object lifecycles, need to prevent memory leaks, or wish to optimize resource usage in Cangjie applications.

## Quick Navigation

**What do you want to learn?**
- Understand the core architecture of the `ref` package, including `WeakRef` and `WeakRefBase` → ref_package_classes.md
- Explore how `CleanupPolicy` influences garbage collection behavior → ref_package_classes.md

**What do you want to do?**
- Retrieve object values safely using the `value` property that returns `Option` → ref_package_classes.md
- Configure or inspect the current cleanup strategy via `cleanupPolicy` → ref_package_classes.md
- Manually trigger cleanup operations using the `clear` method → ref_package_classes.md

## Detailed Description

### ref_package_classes.md
This API reference document details the class definitions within the ref package of the Cangjie programming language, specifically focusing on weak reference mechanisms for memory management. The main sections cover the generic class WeakRef and its sealed abstract parent WeakRefBase. Key information includes the functionality of WeakRef to allow garbage collection of reachable objects based on a specified CleanupPolicy. It highlights essential members such as the value property returning Option, the cleanupPolicy property, and the clear method for forced cleanup. This documentation is intended for developers requiring precise control over object lifecycles and garbage collection behavior in Cangjie applications.