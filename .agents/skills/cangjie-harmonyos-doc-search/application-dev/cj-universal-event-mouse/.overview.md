# cj-universal-event-mouse

This directory serves as the definitive technical guide for the `cj-universal-event-mouse` module, a key component of the Cangjie ArkUI framework. It is specifically curated for developers who need to incorporate mouse interaction capabilities into their application interfaces. The directory focuses on the `onMouse` function, which is essential for detecting and responding to user actions performed with a mouse, such as clicking or hovering over UI elements. The provided documentation offers a deep dive into the API, covering all necessary aspects including module imports, function signatures, and the detailed parameters of the `MouseEvent` callback. It also explains the return values that facilitate event handling logic. Furthermore, the directory provides critical context regarding the feature's current state, highlighting that it is in Beta, supports only external mice, and utilizes a default event pass-through mechanism. This resource is indispensable for developers targeting ArkUI version 22 or higher, ensuring they have the knowledge required to build interactive, mouse-responsive applications while navigating the framework's specific technical constraints.

## Quick Navigation

What do you want to learn?

*   Access the full API reference for the mouse event module → cj-universal-event-mouse.md
*   Learn how to implement the `onMouse` function in your components → cj-universal-event-mouse.md
*   Understand the specific parameters and data types in `MouseEvent` → cj-universal-event-mouse.md
*   Find out what return values are provided by the event callbacks → cj-universal-event-mouse.md
*   Check the system requirements and version compatibility (ArkUI 22+) → cj-universal-event-mouse.md
*   Review the Beta feature limitations and external mouse support → cj-universal-event-mouse.md
*   Understand the default event pass-through behavior and its implications → cj-universal-event-mouse.md

## Detailed Description

### cj-universal-event-mouse.md cj-universal-event-mouse.md

This document provides API reference documentation for handling mouse events in the Cangjie ArkUI framework. It focuses on the `onMouse` function, which triggers callbacks when a component is clicked or the mouse moves over it. Key sections include the module import statement, the function signature, parameter descriptions for the `MouseEvent` callback, and return values. Important notes indicate that the feature is currently in Beta, supports only external mice, and events pass through by default. The documentation targets developers using ArkUI version 22 or higher who need to implement mouse interaction logic in their applications.