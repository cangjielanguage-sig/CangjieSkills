<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509name.tostring" parent="stdx.crypto.x509.class.x509name" -->
# X509Name.toString

[← X509Name](index.md)

## 签名

```cangjie role=signature
public override func toString(): String
```

生成证书实体名称字符串。

## 契约

返回值：

- String - 证书实体名称字符串，包含实体名称中存在的字段信息。
