# extend_UInt32_CarryingOpUInt32

This directory provides comprehensive API reference documentation for the Cangjie programming language, specifically detailing the implementation of the `CarryingOp` interface for the `UInt32` type. It focuses on a suite of arithmetic and bitwise operations designed to handle calculations safely using a wrapping strategy. The core functionality of these APIs involves returning a tuple containing the operation result alongside a Boolean flag that explicitly indicates if truncation or overflow occurred during the process. This documentation is essential for Cangjie developers who need to perform explicit overflow detection and handling, particularly in low-level programming contexts where precise integer control and safety are critical. Key topics include Cangjie, UInt32, overflow detection, wrapping strategy, and the `CarryingOp` interface.

## Quick Navigation

**What do you want to learn?**

*   Understand basic arithmetic operations with overflow checks (Add, Dec, Div, Inc, Mod) → extend_UInt32_CarryingOpUI_6more.md
*   Explore advanced operations including multiplication, negation, and bitwise shifts (Mul, Neg, Shl, Shr, Sub) → func_carryingMulUInt32_5more.md

## Detailed Description

### extend_UInt32_CarryingOpUI_6more.md extend_UInt32_CarryingOpUI_6more.md
This API reference document details the implementation of the `CarryingOp` interface for the `UInt32` type in the Cangjie programming language. It outlines specific arithmetic functions including `carryingAdd`, `carryingDec`, `carryingDiv`, `carryingInc`, and `carryingMod`. Key information includes that each function utilizes a wrapping strategy for calculations and returns a tuple containing a Boolean indicating if truncation occurred alongside the operation result. This documentation targets Cangjie developers who need to perform arithmetic with explicit overflow detection and handling. By utilizing these functions, developers can ensure that their applications handle edge cases in arithmetic without unexpected behavior.

### func_carryingMulUInt32_5more.md func_carryingMulUInt32_5more.md
This document serves as API reference documentation for Cangjie language functions that perform arithmetic and bitwise operations with overflow detection. It details five specific functions: `carryingMul`, `carryingNeg`, `carryingShl`, `carryingShr`, and `carryingSub`, all utilizing the wrapping strategy. Key information includes that each function returns a tuple containing a Boolean indicating if truncation occurred and the resulting `UInt32` value. The target audience is Cangjie developers requiring precise control over integer overflow behavior in low-level programming contexts. These functions are particularly useful when implementing algorithms that require strict adherence to bit-width limits. Keywords: Cangjie, API, overflow detection, wrapping strategy, UInt32, arithmetic operations.