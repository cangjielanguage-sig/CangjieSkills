<!-- cj-doc kind="guide-leaf" level="5" id="language.class.3-继承.3-4-重写与重定义" parent="language.class.3-继承" -->
# 3.4 重写与重定义

[← 3. 继承](index.md)

- **重写（实例函数）**：父类函数须为 `open`；子类使用 `override`（可选）。基于运行时类型的动态分派
- **重定义（静态函数）**：子类使用 `redef`（可选）。基于类型名的静态分派
- 命名参数须在父类与重写之间匹配
- 泛型重写时子类约束须**等于或宽松于**父类

---
