<!-- cj-doc kind="api-member" level="5" id="std.core.func.refeq-object-object" parent="std.core" -->
# refEq(Object, Object)

[← std.core](../index.md)

## 签名

```cangjie role=signature
public func refEq(a: Object, b: Object): Bool
```

判断两个 Object 实例的内存地址是否相同。

## 契约

参数：

- a: Object - 一个 Object 实例。
- b: Object - 另一个 Object 实例。

返回值：

- Bool - 如果两个 Object 实例的内存地址相同，返回 true，否则返回 false。
