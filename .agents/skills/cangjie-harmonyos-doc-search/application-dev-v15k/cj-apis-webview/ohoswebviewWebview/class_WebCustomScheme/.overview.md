# class_WebCustomScheme

This directory provides comprehensive API reference documentation for the `WebCustomScheme` class in the Cangjie language, specifically designed to facilitate the configuration and management of custom URL protocols within webview environments. It thoroughly covers the class initialization process and details a wide array of configurable properties that dictate protocol behavior. Key topics include security settings such as `isSecure` and `isLocal`, network capabilities including `isSupportCORS` and `isSupportFetch`, and content policy controls like `isCspBypassing`. Furthermore, the documentation specifies the strict rules for defining `schemeName` and explains how to toggle features like code caching and display isolation. This resource is suitable for developers integrating webviews who require granular control over security and network features when defining custom schemes, applicable from system API version 20.

## Quick Navigation

What do you want to learn?

*   How to configure security, network capabilities, and content policies for the scheme? → class_WebCustomScheme_10more.md
*   How to initialize the class and understand the strict naming rules for the scheme? → initString.md

## Detailed Description

### class_WebCustomScheme_10more.md
This document serves as API reference documentation for the WebCustomScheme class in the Cangjie language, designed to configure custom URL protocols within a webview environment. It details the class initialization and provides specifications for configurable properties that control protocol behavior, including security settings like isSecure and isLocal, network capabilities such as isSupportCORS and isSupportFetch, and content policy controls like isCspBypassing. Key information includes the specific rules for defining the schemeName and the ability to toggle code caching and display isolation. This documentation targets developers integrating webviews who need to define custom schemes with granular control over security and network features, applicable from system API version 20.

### initString.md
This document serves as API reference documentation for the constructor of the WebCustomScheme class in the Cangjie language, detailing how to initialize a custom web protocol scheme. The content covers the function signature, system capabilities, version requirements, and a detailed parameter table defining the constraints for the custom scheme name. The key takeaway is the strict validation rules for the schemeName parameter: it must be a string starting with a letter, containing only lowercase letters, digits, or specific symbols, with a maximum length of 32 characters. This reference guide targets developers using the Webview core capabilities who need to define and register custom URL protocols for web applications.