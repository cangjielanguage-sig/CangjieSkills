# cj-share-app-file

This directory contains a technical guide focused on the mechanisms for sharing files between applications using Uniform Resource Identifiers (URIs). It covers the `startAbility` method for invoking file processing applications, details shareable sandbox directories like el1 and el2, and defines the specific file URI format specification. The content explains how to share single files via a file selector, enabling recipient apps to perform read and write operations using `ohos.file_fs.open`. This resource is intended for application developers who need to implement cross-app data exchange effectively and requires an understanding of the application sandbox and file system APIs. It serves as a comprehensive reference for ensuring secure and efficient file interoperability within the development environment, specifically addressing how to identify resource owners and locations through the URI structure to facilitate seamless data access.

## Quick Navigation

**What do you want to learn?**

*   Understand the core mechanism for sharing files between applications using URIs → cj-share-app-file.md
*   Learn how to use the `startAbility` method to invoke file processing apps → cj-share-app-file.md
*   Identify shareable sandbox directories, including el1 and el2 base paths → cj-share-app-file.md
*   Define and implement the file URI format specification (`file://<bundleName>/<path>`) → cj-share-app-file.md
*   Enable recipient apps to perform read and write operations via `ohos.file_fs.open` → cj-share-app-file.md
*   Understand the requirements for application sandbox and file system APIs → cj-share-app-file.md

## Detailed Description

### cj-share-app-file.md

This technical guide explains the mechanism for sharing files between applications using Uniform Resource Identifiers (URIs). It covers the `startAbility` method for invoking file processing apps, lists shareable sandbox directories such as el1 and el2 base paths, and defines the file URI format specification. Key takeaways include the ability to share single files via a file selector, allowing recipient apps to perform read and write operations using `ohos.file_fs.open`. The document specifies the URI structure as `file://<bundleName>/<path>`, identifying the resource owner and location. Intended for application developers, this guide requires an understanding of the application sandbox and file system APIs to implement cross-app data exchange effectively. It provides the necessary specifications to ensure that applications can correctly interpret and access file resources shared across different security contexts.