<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonbool.kind" parent="stdx.encoding.json.class.jsonbool" -->
# JsonBool.kind

[← JsonBool](index.md)

## 签名

```cangjie role=signature
public func kind(): JsonKind
```

返回当前 JsonBool 所属的 JsonKind 类型（JsBool）。

## 契约

返回值：

- JsonKind - 当前 JsonBool 所属的 JsonKind 类型（JsBool）。
