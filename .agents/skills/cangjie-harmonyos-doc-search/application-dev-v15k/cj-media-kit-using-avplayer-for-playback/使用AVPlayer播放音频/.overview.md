# 使用AVPlayer播放音频

This directory provides a comprehensive guide to implementing audio playback using the AVPlayer component within the HarmonyOS ecosystem. It covers the entire development lifecycle, from player creation and resource configuration to playback control, state monitoring, and memory management. Key topics include handling state transitions, managing audio focus, and dealing with interruptions or device changes. The documentation is tailored for application developers and multimedia engineers seeking to integrate robust audio capabilities into their software using the Cangjie language, with a focus on both local and network media resources.

## Quick Navigation

**What do you want to learn?**

- Understand the end-to-end workflow, best practices, and resource management → 使用AVPlayer播放音频_2more.md
- Learn specific development steps, state transitions, and event listeners → 开发步骤及注意事项.md
- Review comprehensive Cangjie code examples and lifecycle implementation → 完整示例/

## Detailed Description

### 使用AVPlayer播放音频_2more.md 使用AVPlayer播放音频_2more.md
This development guide provides a comprehensive tutorial on using the AVPlayer component to implement end-to-end audio playback for media resources. The document details the full playback workflow, including player creation, resource setting, parameter configuration, playback control, and resource management. It also covers state monitoring and development best practices. Key takeaways include the importance of managing player states to avoid errors, the need to call reset() or release() to free memory, and handling audio interruptions or device changes. It notes that background playback is currently unsupported and requires internet permissions for online resources. This guide targets application developers working with media playback who need to integrate audio functionality into their software.

### 开发步骤及注意事项.md 开发步骤及注意事项.md
This document serves as a technical tutorial outlining the standard workflow for implementing media playback using the AVPlayer API. It details the lifecycle from creating an instance and setting essential event listeners like stateChange and bufferingUpdate to resource configuration, playback control, and instance release. Key information includes specific state transitions (idle, initialized, prepared), requirements for local sandbox paths versus network permissions, and the configuration of AudioRendererInfo. The guide targets application developers integrating multimedia features, emphasizing the importance of handling errors and managing audio focus during playback.

### 完整示例/
This directory contains comprehensive Cangjie code examples for implementing audio and media playback using the AVPlayer framework in HarmonyOS. It covers essential topics such as initializing the player, managing state transitions, handling callbacks, and configuring various data sources including local files, file descriptors, and live streams. Key concepts include AVPlayer lifecycle, error handling, and UIAbility integration. These examples are suitable for HarmonyOS developers and multimedia application engineers looking to master media playback capabilities and event management in Cangjie.