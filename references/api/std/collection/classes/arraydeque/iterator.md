<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arraydeque.iterator" parent="std.collection.class.arraydeque" -->
# ArrayDeque<T>.iterator

[← ArrayDeque<T>](index.md)

## 签名

```cangjie role=signature
public func iterator(): Iterator<T>
```

获取此双端队列中元素的迭代器，其顺序为从前到后的顺序。

## 契约

返回值：

- Iterator\<T> - 元素的迭代器。
