# Observed宏和Publish宏嵌套类对象属性变化

This directory provides a comprehensive technical guide on using the `@Observed` and `@Publish` macros in the Cangjie language for ArkUI development. It focuses on managing state observation and property changes within nested custom classes and arrays. The content covers deep observation mechanisms, variable initialization, UI update triggers, and specific constraints like inheritance and constructor restrictions. It is suitable for Cangjie developers who need to implement reactive user interfaces and handle complex data models beyond basic state management.

## Quick Navigation

**What do you want to learn?**

*   Understand the basic usage, specifications, and initialization rules for @Observed and @Publish → Observed宏和Publish宏嵌套类对象属性变_4more.md
*   Troubleshoot common errors regarding constructors and UI update prerequisites → 常见问题.md
*   Learn about change observation behavior, rendering mechanisms, and complex types → 观察变化和行为表现.md
*   Review specific constraints, inheritance rules, and usage limitations → 限制条件_2more.md

## Detailed Description

### Observed宏和Publish宏嵌套类对象属性变_4more.md Observed宏和Publish宏嵌套类对象属性变_4more.md
This technical guide explains the usage of @Observed and @Publish macros in the Cangjie language to enable observation of property changes within nested custom classes and arrays. The document covers the overview of deep observation, detailed specifications for the @Observed class macro and @Publish variable macro, and rules for variable initialization and passing. Key takeaways include that @Observed marks classes for internal change tracking, while @Publish decorates variables in child components to establish two-way binding with parent components. Specific restrictions apply, such as forbidding inheritance in @Observed classes and requiring parent initialization for @Publish variables. Targeted at Cangjie developers familiar with basic state management macros like @State, this reference helps manage complex data models.

### 常见问题.md 常见问题.md
This troubleshooting guide addresses common errors in Cangjie ArkUI state management regarding the @Observed and @Publish decorators. It covers three main topics: constructor restrictions in @Observed classes, prerequisites for triggering UI updates with @Publish, and managing nested custom types. Key takeaways include that @Observed classes cannot define custom constructors, @Publish variables only trigger UI refreshes if their class is @Observed, and nested objects require both decorators on the child class to propagate changes. This document targets Cangjie developers seeking to resolve state synchronization and UI rendering issues.

### 观察变化和行为表现.md 观察变化和行为表现.md
This technical guide explains how to implement and manage state observation in Cangjie using the @Observed and @Publish decorators within the ArkUI framework. It covers observing changes in complex types like nested classes and arrays, handling DateTime updates, and understanding the framework's rendering and update mechanisms. Key takeaways include that nested classes must be decorated with @Observed to track property changes, while arrays should use ObservedArrayList. The framework automatically generates getters and setters to trigger UI re-renders when @Publish properties change. This document is intended for Cangjie developers building reactive user interfaces who need to understand state management and component re-rendering logic. Keywords: Cangjie, ArkUI, @Observed, @Publish, State Management, Reactive UI.

### 限制条件_2more.md 限制条件_2more.md
This document outlines the specific constraints and usage scenarios for the @Observed and @Publish decorators in Cangjie ArkUI development. It details limitations regarding class inheritance, constructor definitions, variable types, and initialization requirements. Key takeaways include that @Observed classes cannot inherit or define constructors, and @Publish only works on var members. The example demonstrates that nested custom types must also be decorated with @Observed to trigger UI updates upon property changes. This reference guide targets Cangjie developers implementing reactive state management in ArkUI applications.