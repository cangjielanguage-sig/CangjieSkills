<!-- cj-doc kind="api-member" level="6" id="std.unittest.class.assertexception.init" parent="std.unittest.class.assertexception" -->
# AssertException.init

[← AssertException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造函数。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

构造函数。

### 契约

参数：

- message: String - 指定的异常信息。
