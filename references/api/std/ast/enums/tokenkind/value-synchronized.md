<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-synchronized" parent="std.ast.enum.tokenkind" -->
# TokenKind.SYNCHRONIZED

[← TokenKind](index.md)

## 签名

```cangjie role=signature
SYNCHRONIZED
```

构造一个表示 `synchronized` 的枚举实例。
