<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.class.tlssocket.hashcode" parent="stdx.net.tls.class.tlssocket" -->
# TlsSocket.hashCode

[← TlsSocket](index.md)

## 签名

```cangjie role=signature
public override func hashCode(): Int64
```

返回 TLS 套接字对象的哈希值。

## 契约

返回值：

- Int64 - 对 TLS 套接字对象进行哈希计算后得到的结果。
