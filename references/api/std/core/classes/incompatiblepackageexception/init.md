<!-- cj-doc kind="api-member" level="6" id="std.core.class.incompatiblepackageexception.init" parent="std.core.class.incompatiblepackageexception" -->
# IncompatiblePackageException.init

[← IncompatiblePackageException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 IncompatiblePackageException 实例，默认异常信息为空。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据异常信息构造一个 IncompatiblePackageException实例。

### 契约

参数：

- message: String - 异常提示信息。
