<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.class.websocket.prop-subprotocol" parent="stdx.net.http.class.websocket" -->
# WebSocket.subProtocol

[← WebSocket](index.md)

## 签名

```cangjie role=signature
public prop subProtocol: String
```

获取与对端协商到的 subProtocol，协商时，客户端提供一个按偏好排名的 subProtocols 列表，服务器从中选取一个或零个子协议。

## 契约

类型：String
