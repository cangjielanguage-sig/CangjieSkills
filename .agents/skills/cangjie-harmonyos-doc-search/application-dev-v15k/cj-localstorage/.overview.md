# cj-localstorage

The `cj-localstorage` directory is a specialized resource dedicated to the implementation and management of page-level UI state using LocalStorage within the Cangjie framework. It provides an in-depth look at the architecture of in-memory storage, detailing the lifecycle management and state synchronization mechanisms essential for modern application development. The content focuses heavily on practical implementation, specifically the use of `@LocalStorageLink` and `@LocalStorageProp` decorators for effective data binding. Furthermore, it addresses usage constraints and offers practical scenarios for sharing state across different components and UIAbility instances. This documentation is specifically designed for developers and software architects building HarmonyOS applications who need to master reactive state management, ensure reliable data persistence, and efficiently handle complex data types within their projects.

## Quick Navigation

**What do you want to learn?**

- Understand the core concepts of in-memory storage and lifecycle management → `LocalStorage页面级UI状态存储/`
- Learn how to use `@LocalStorageLink` and `@LocalStorageProp` for data binding → `LocalStorage页面级UI状态存储/`
- Explore state sharing across components and UIAbility instances → `LocalStorage页面级UI状态存储/`

## Detailed Description

### LocalStorage页面级UI状态存储/
This directory provides comprehensive documentation and guides for managing page-level UI state using LocalStorage within the Cangjie framework. It covers core concepts like in-memory storage, lifecycle management, and state synchronization mechanisms. Key topics include the `@LocalStorageLink` and `@LocalStorageProp` decorators for data binding, usage constraints, and practical scenarios for state sharing across components and UIAbility instances. This resource is suitable for developers and architects working with HarmonyOS applications who need to implement reactive state management, ensure data persistence, and handle complex data types efficiently.