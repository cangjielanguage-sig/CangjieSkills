<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509certificaterequest.encodetoder" parent="stdx.crypto.x509.class.x509certificaterequest" -->
# X509CertificateRequest.encodeToDer

[← X509CertificateRequest](index.md)

## 签名

```cangjie role=signature
public func encodeToDer(): DerBlob
```

将数字证书签名请求编码成 Der 格式。

## 契约

返回值：

- DerBlob - 编码后的 Der 格式的数字证书签名请求。
