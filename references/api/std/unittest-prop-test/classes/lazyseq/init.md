<!-- cj-doc kind="api-member" level="6" id="std.unittest.prop_test.class.lazyseq.init" parent="std.unittest.prop_test.class.lazyseq" -->
# LazySeq<T>.init

[← LazySeq<T>](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造器。

## init(T)

### 签名

```cangjie role=signature
public init(element: T)
```

构造器。

### 契约

参数：

- element: T - 初始元素。
