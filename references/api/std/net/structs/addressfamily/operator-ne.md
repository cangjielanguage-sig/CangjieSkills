<!-- cj-doc kind="api-member" level="6" id="std.net.struct.addressfamily.operator-ne" parent="std.net.struct.addressfamily" -->
# AddressFamily.!=

[← AddressFamily](index.md)

## 签名

```cangjie role=signature
public operator func !=(rhs: AddressFamily): Bool
```

比较地址族值是否不等。

## 契约

参数：

- rhs: AddressFamily - 参与比较的 AddressFamily 对象。

返回值：

- Bool - 如果两个 AddressFamily 对象不等，则返回 `true`；否则，返回 `false`。
