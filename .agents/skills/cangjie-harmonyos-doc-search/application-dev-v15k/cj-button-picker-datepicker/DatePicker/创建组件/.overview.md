# 创建组件

This directory provides comprehensive API reference documentation for creating sliding date picker components using the Cangjie language within the HarmonyOS ArkUI framework. It focuses on the `initDateTime` constructors, detailing how to initialize date selection interfaces with specific parameters for start dates, end dates, and selected values. The documentation covers critical system capabilities, including date range validation (hard limits of 1900-2100), automatic edge case correction, and support for two-way data binding. These resources are essential for ArkUI developers (version 12 or higher) who need to implement robust, user-friendly date selection controls while adhering to strict system constraints and logic handling. By following these guides, developers can ensure their applications handle date inputs correctly across various scenarios.

## Quick Navigation

**What do you want to do?**

*   **Initialize a standard date picker** → initDateTime_DateTime_DateTime.md
    *   Use this constructor to set up a picker with defined start, end, and selected dates, including automatic correction for invalid ranges.
*   **Initialize a picker with callback and binding** → initDateTime_DateTime_DateTimeDateTime_-_Unit.md
    *   Use this constructor to implement a picker that supports selection callbacks and two-way binding for the selected value.

## Detailed Description

The following files contain the specific API references and technical details for implementing these components.

### initDateTime_DateTime_DateTime.md initDateTime_DateTime_DateTime.md
This API reference document details the constructor for a sliding date picker component in the Cangjie language, enabling the creation of date selection interfaces within specified ranges. The documentation outlines the function signature, system capabilities, and a comprehensive parameter table for start, end, and selected dates. Key information includes the default values (1970 to 2100) and complex logic handling edge cases, such as automatic correction when the start date exceeds the end date or when the selected date falls outside valid bounds. It also defines the system's hard limit of 1900-2100. Intended for ArkUI developers using version 12 or higher, this guide ensures proper implementation of date validation and picker initialization.

### initDateTime_DateTime_DateTimeDateTime_-_Unit.md initDateTime_DateTime_DateTimeDateTime_-_Unit.md
This document serves as API reference documentation for a Cangjie language constructor that initializes a sliding date picker component. It details the function signature, system capabilities, and parameter definitions for start date, end date, and selection callback. Key information includes extensive logic for handling edge cases, such as date range validation and automatic adjustments when inputs fall outside the system's valid range of 1900-01-31 to 2100-12-31. The document highlights that the selected parameter supports two-way binding. This technical reference is intended for HarmonyOS ArkUI developers implementing date selection interfaces using the Cangjie language.