<!-- cj-doc kind="api-member" level="6" id="std.collection.class.hashset.prop-capacity" parent="std.collection.class.hashset" -->
# HashSet<T> where T <: Hashable & Equatable<T>.capacity

[← HashSet<T> where T <: Hashable & Equatable<T>](index.md)

## 签名

```cangjie role=signature
public prop capacity: Int64
```

返回此 HashSet 的内部数组容量大小。

## 契约

> **注意：**
>
> 容量大小不一定等于 HashSet 的 size。

类型：Int64
