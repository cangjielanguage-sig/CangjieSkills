# 使用_import_语法加载仓颉三方库_A且三方库_A_依赖三方库_B

This directory provides a collection of technical guides focused on integrating Cangjie third-party libraries into ArkTS projects using the `import` syntax, specifically addressing complex scenarios where the primary library (Library A) has a dependency on another Cangjie library (Library B). It details multiple implementation strategies, ranging from nested sub-packages to independent HAR module compilation. Key topics include managing cross-language dependencies, configuring build environments like `cjpm.toml` and `oh-package.json5`, and utilizing `JSModule` for interoperability. These resources are tailored for HarmonyOS developers who need to ensure type safety and seamless function exposure when bridging Cangjie and ArkTS code.

## Quick Navigation

**How do you want to structure the dependency between Library A and Library B?**

*   Learn the standard approach for loading Library A (which depends on Library B) in ArkTS → 使用_import_语法加载仓颉三方库_A且三方库__2more.md
*   Treat Library B as a sub-package within Library A → 方案二三方库_B_作为三方库_A_的子包.md
*   Compile Library B as a standalone HAR module linked by Library A → 方案三三方库_B_作为一个模块编译成_har_包.md

## Detailed Description

### 使用_import_语法加载仓颉三方库_A且三方库__2more.md 使用_import_语法加载仓颉三方库_A且三方库__2more.md
This tutorial explains how to load a Cangjie third-party library in ArkTS using the import syntax, specifically addressing scenarios where the target library depends on another Cangjie library. The document covers creating the library structure, configuring dependencies in cjpm.toml, implementing interop interfaces using JSModule, and setting up ArkTS type definitions. Key takeaways include the necessity of declaring dependencies in both Cangjie and ArkTS configuration files and using .d.ts files for type safety. This guide targets HarmonyOS developers integrating Cangjie code into ArkTS applications.

### 方案三三方库_B_作为一个模块编译成_har_包.md 方案三三方库_B_作为一个模块编译成_har_包.md
This tutorial guides developers on integrating a Cangjie static library (package_b) into an ArkTS project by compiling it as a HAR module and linking it via a Cangjie dynamic library (package_a). The document details creating the static library, configuring dependencies in cjpm.toml and oh-package.json5, implementing ArkTS-Cangjie interop using JSModule, and defining TypeScript declarations. Key steps involve setting up the module structure, importing the static library within the dynamic library, and exposing the combined functionality to the ArkTS layer using specific interop APIs. Intended for developers building hybrid applications using ArkTS and Cangjie, requiring knowledge of module configuration and cross-language interoperability.

### 方案二三方库_B_作为三方库_A_的子包.md 方案二三方库_B_作为三方库_A_的子包.md
This technical tutorial outlines the implementation of a nested Cangjie library structure where library B serves as a sub-package of library A within an ArkTS project. Key sections cover creating the specific directory hierarchy and source files for both packages, configuring ArkTS type definitions, and setting up build dependencies in oh-package.json5 and cjpm.toml. The document highlights the Cangjie import syntax for sub-packages and the JavaScript interop mechanism using JSModule to expose functions. It is intended for developers integrating complex Cangjie modules into HarmonyOS applications, focusing on dependency management and cross-language invocation.