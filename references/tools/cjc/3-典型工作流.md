<!-- cj-doc kind="guide-leaf" level="4" id="tools.cjc.3-典型工作流" parent="tools.cjc" -->
# 3. 典型工作流

[← cjc 编译器](index.md)

```bash
# 单文件编译运行
cjc hello.cj -o hello && ./hello

# 多包项目手动编译
cjc -p lib/utils --output-type=staticlib
cjc main.cj libutils.a -o app

# 带调试信息编译
cjc main.cj -g -O0 -o app_debug

# 发布优化构建
cjc main.cj -O2 --lto=full -s -o app_release
```

> **提示**：多文件项目推荐使用 [`cjpm`](../cjpm/index.md)，无需手动管理编译依赖。
