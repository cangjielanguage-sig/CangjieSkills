# class_HashMapK_V_where_K_Hashable_EquatableK

This directory contains comprehensive API reference documentation for the `HashMap` class in the Cangjie programming language. It details the implementation of a hash table-based map structure where keys must be `Hashable` and `Equatable`. The documentation covers class definitions, generic constraints, initialization methods, core operations like lookups and modifications, and extensions for equality and string conversion. It is suitable for Cangjie developers seeking to implement efficient key-value storage, manage collections, and understand the specific behaviors and exceptions associated with hash maps in the standard library.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition, properties, and basic constructors? → class_HashMapK_V_where_K_H_6more.md
*   Learn how to initialize maps with specific capacities or generator functions? → initInt64_5more.md
*   Access or modify data using subscript operators? → operator_func_K_2more.md
*   Perform common operations like cloning, checking existence, or iterating? → func_clone_9more.md
*   Manage data removal, capacity reservation, or conversion to arrays? → func_removeK_5more.md
*   Compare two HashMap instances for equality? → extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md
*   Convert a HashMap to a string representation? → extendK_V_HashMapK_V_To_d1bae6de.md

## Detailed Description

### class_HashMapK_V_where_K_H_6more.md class_HashMapK_V_where_K_H_6more.md
This document provides API reference documentation for the `HashMap` class in the Cangjie programming language, detailing its implementation as a hash table for the Map interface. It covers the class definition with generic constraints requiring keys to be `Hashable` and `Equatable`, properties like `capacity` and `size`, and multiple initialization methods including default, capacity-based, and collection-based constructors. Key takeaways include that `HashMap` offers O(1) time complexity for lookups and modifications, does not guarantee element order, and handles duplicate keys by overwriting previous values. This reference guide targets developers using the Cangjie standard library who need to implement key-value storage with fast access.

### extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md
This API reference document details the extension of the Cangjie HashMap type to implement the Equatable interface, enabling equality comparisons. It covers the extension declaration requiring values to be Equatable and defines two operator overloads: the inequality operator (!=) and the equality operator (==). The key information specifies that two HashMap instances are considered equal only if they contain identical key-value pairs. This documentation targets Cangjie developers working with collections who need to perform equality checks on HashMap instances.

### extendK_V_HashMapK_V_To_d1bae6de.md extendK_V_HashMapK_V_To_d1bae6de.md
This API documentation details a Cangjie language extension that implements the ToString interface for the HashMap collection class. The content covers the extension declaration, which requires both keys and values to satisfy the ToString constraint, and defines the public toString() function. Key takeaways include the specific string output format, which represents the map as a list of tuples, such as "[(k1, v1), (k2, v2)]". This reference guide is intended for developers working with the Cangjie standard library who need to convert HashMap instances into string representations for logging or debugging purposes.

### func_clone_9more.md func_clone_9more.md
This document serves as API reference documentation for nine specific member functions of the HashMap class in the Cangjie standard library, detailing their usage, parameters, and return values. The file covers essential operations including cloning the map, checking for key existence via single keys or collections, retrieving values or entry views, and managing map contents through iteration, key extraction, and bulk removal. Key functions include `clone` for copying, `get` which returns an Option type for safe access, `contains` for validation, and `remove` for bulk deletion. The documentation highlights return types like `HashMapIterator`, `EquatableCollection`, and `MapEntryView`. Targeted at Cangjie developers, this reference guide requires understanding of generic types and collection interfaces to effectively implement hash map manipulations in their applications.

### func_removeK_5more.md func_removeK_5more.md
This document provides API reference documentation for specific manipulation and query functions within the Cangjie HashMap class. It details five key functions: remove, removeIf, reserve, toArray, and values. Key information includes that remove deletes a key and returns the value as an Option, while removeIf conditionally deletes entries but throws a ConcurrentModificationException if the map is modified during iteration. The reserve function manages capacity expansion logic, whereas toArray and values facilitate data conversion and extraction. This reference is intended for Cangjie developers seeking to manage HashMap data structures, requiring knowledge of generics, lambda expressions, and exception handling.

### initInt64_5more.md initInt64_5more.md
This document provides API reference documentation for initialization and modification methods of the HashMap class in the Cangjie programming language. It covers two constructors for creating maps with specific capacities or via generator functions, alongside methods for adding elements and clearing data. Key information includes handling duplicate keys by overwriting values, returning Option<V> for replaced values, and throwing IllegalArgumentException for invalid negative parameters. This technical reference is intended for Cangjie developers who need precise syntax and behavioral details for managing key-value collections effectively.

### operator_func_K_2more.md operator_func_K_2more.md
This document serves as API reference documentation for operator overloading functions, specifically the subscript operator, within a Cangjie collection context. It covers two overloaded operator functions: a getter method that retrieves values by key and a setter method that inserts or updates key-value pairs. The getter returns type V for a given key K but throws a NoneValueException if the key is absent. The setter accepts a key and a value, returning Unit, and handles both adding new entries and overwriting existing ones in a HashMap. This reference is intended for Cangjie developers implementing or interacting with hash-based collections, requiring knowledge of operator overloading and exception handling.