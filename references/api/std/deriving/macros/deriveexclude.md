<!-- cj-doc kind="api-member" level="5" id="std.deriving.macro.deriveexclude" parent="std.deriving" -->
# @DeriveExclude

[← std.deriving](../index.md)

## 签名

```cangjie role=signature
@DeriveExclude
```

`DeriveExclude` 可为已被 @Derive 宏修饰的声明排除不需要处理的字段，字段默认被 Deriving 处理。
