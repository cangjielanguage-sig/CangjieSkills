# class_UnixSocketAddress

This directory contains comprehensive API reference documentation for the `UnixSocketAddress` class within the Cangjie standard library. It focuses on the technical implementation of encapsulating Unix Domain Socket file system paths. The documentation covers class inheritance hierarchies, various constructors accepting `String` or `Array<Byte>`, and essential properties like `family` and `size`. Key topics include handling named, unnamed, and abstract addresses, managing path length limitations, and retrieving raw byte arrays compatible with the `sockaddr_un` structure. This resource is specifically suitable for Cangjie developers implementing low-level network communication who require precise specifications for managing socket addresses, converting them to strings, or comparing instances.

## Quick Navigation

- **What do you want to learn?**
    - Understand the class structure, constructors, and properties → class_UnixSocketAddress_7more.md
    - Convert a socket address to a readable string representation → func_toString.md
    - Compare two socket addresses for equality or inequality → operator_func_UnixSocketAd_2more.md

## Detailed Description

### class_UnixSocketAddress_7more.md
This document provides API reference documentation for the `UnixSocketAddress` class in the Cangjie standard library, designed to encapsulate Unix Domain Socket file system paths. It covers class inheritance from `SocketAddress` and `Equatable`, constructors accepting `String` or `Array<Byte>`, and properties like `family` and `size`. Key information includes support for `unnamed` and `abstract` addresses, a maximum path length of 108, and methods to retrieve raw byte arrays compatible with the `sockaddr_un` structure. Intended for Cangjie developers, this guide assists in implementing network communication using Unix Domain Sockets.

### func_toString.md
This document provides API reference documentation for the toString() method belonging to the UnixSocketAddress class in the Cangjie standard library. It details the function's signature, which returns a String representation of the socket address, such as /tmp/socket1. The text includes a detailed example section demonstrating how the method handles various input scenarios, including standard strings and byte arrays with specific null character handling for abstract namespaces. Key information highlights that the function converts the internal address structure into a readable path string. This resource is intended for Cangjie developers implementing network communication using Unix domain sockets who require a way to inspect or log address objects.

### operator_func_UnixSocketAd_2more.md
This document provides API reference details for the equality and inequality operator functions of the UnixSocketAddress class in the Cangjie language. It specifically documents the != and == operator functions, explaining how to compare two UnixSocketAddress objects. Key information includes the function signatures, the rhs parameter representing the right-hand side object, and the Bool return value indicating the comparison result. This documentation is intended for developers working with network programming in Cangjie who need to validate or compare socket addresses.