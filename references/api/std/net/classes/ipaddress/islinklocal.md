<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.islinklocal" parent="std.net.class.ipaddress" -->
# IPAddress.isLinkLocal

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public open func isLinkLocal(): Bool
```

判断此 IPAddress 对象是不是链路本地地址。

## 契约

返回值：

- Bool - 返回 true 表示是链路本地地址，否则返回 false。
