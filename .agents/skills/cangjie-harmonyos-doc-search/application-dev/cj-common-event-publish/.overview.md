# cj-common-event-publish

This directory serves as a comprehensive technical resource for Cangjie developers, focusing on the implementation of custom common event publishing using the BasicServicesKit framework. The content provides an in-depth look at the `publish()` interface, guiding users through various scenarios such as publishing events without data (restricted to unordered types) and publishing events with data (supporting unordered, ordered, and sticky configurations). It emphasizes the practical configuration of `CommonEventPublishData`, specifically detailing how to utilize properties like `isOrdered` for sequencing and `isSticky` to enable late subscribers to receive previously broadcasted data. This guide is essential for developers building complex inter-component communication systems who need to manage event flow and state retention effectively. It assumes the reader has a basic understanding of Cangjie module imports and general event handling concepts, aiming to bridge the gap to advanced application development.

## Quick Navigation

**What do you want to learn?**

*   **Target Audience:** Who is this guide intended for and what are the prerequisites? → cj-common-event-publish.md
*   **Basic Publishing:** How to utilize the `publish()` interface to send simple events without information? → cj-common-event-publish.md
*   **Data-Driven Events:** How to publish events that carry specific data payloads and configure their behavior? → cj-common-event-publish.md
*   **Event Ordering:** How to set the `isOrdered` property to ensure events are processed in a specific sequence? → cj-common-event-publish.md
*   **Sticky Event Mechanics:** How to configure `isSticky` to allow late subscribers to receive data sent before they subscribed? → cj-common-event-publish.md
*   **Data Configuration:** How to structure `CommonEventPublishData` to define event properties accurately? → cj-common-event-publish.md
*   **Inter-Component Communication:** How to apply these publishing techniques to facilitate communication between components in Cangjie? → cj-common-event-publish.md

## Detailed Description

The following file provides the complete technical guide for implementing event publishing features in your Cangjie project.

### cj-common-event-publish.md cj-common-event-publish.md

This technical tutorial guides developers on publishing custom common events using the Cangjie language's BasicServicesKit. It details the `publish()` interface, covering scenarios for publishing events without information (unordered only) and with data (supporting unordered, ordered, and sticky types). Key takeaways include the configuration of `CommonEventPublishData` to set event properties like `isOrdered` and `isSticky`, and the distinction that sticky events allow late subscribers to receive previously sent data. Intended for Cangjie developers implementing inter-component communication, this guide requires basic knowledge of module imports and event handling.