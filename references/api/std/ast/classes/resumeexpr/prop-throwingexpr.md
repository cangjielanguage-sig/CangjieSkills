<!-- cj-doc kind="api-member" level="6" id="std.ast.class.resumeexpr.prop-throwingexpr" parent="std.ast.class.resumeexpr" -->
# ResumeExpr.throwingExpr

[← ResumeExpr](index.md)

## 签名

```cangjie role=signature
public mut prop throwingExpr: Option<Expr>
```

获取或设置 `throwing` 关键字之后的表达式。

类型：Option<Expr>

