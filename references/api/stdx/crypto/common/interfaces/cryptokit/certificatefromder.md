<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.common.interface.cryptokit.certificatefromder" parent="stdx.crypto.common.interface.cryptokit" -->
# CryptoKit.certificateFromDer

[← CryptoKit](index.md)

## 签名

```cangjie role=signature
func certificateFromDer(encoded: DerBlob): Certificate
```

将证书从 DER 格式解码。

## 参数

- encoded: DerBlob - 待解码的 DerBlob 对象。

## 返回值

- Certificate - 解码得到的证书。

