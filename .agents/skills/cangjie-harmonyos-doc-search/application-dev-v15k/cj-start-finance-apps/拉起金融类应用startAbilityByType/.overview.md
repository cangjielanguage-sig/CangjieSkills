# 拉起金融类应用startAbilityByType

This directory provides comprehensive technical documentation for launching financial applications using the `startAbilityByType` interface on the HarmonyOS platform. It details the end-to-end process, covering how a client application invokes financial features and how a target application receives and processes these requests. Key technical concepts include JSON parameter construction, `module.json5` configuration, URI parsing, and the use of specific schemes like Transfer or CreditCardRepayment. The guides are specifically designed for developers utilizing the Cangjie language and AbilityKit framework to implement secure and efficient financial transaction workflows, such as bank transfers and credit card repayments. By following these instructions, developers can ensure seamless interoperability between different financial apps and service providers.

## Quick Navigation

What do you want to do?

*   **Initiate a financial request**: Learn how to construct parameters and call the API to launch a financial app extension panel → 拉起金融类应用startAbilityByType_3more.md (Parameter specs & Cangjie code)
*   **Handle incoming requests**: Learn how to configure your app to receive external calls and parse parameters → 目标方开发步骤.md (module.json5 & URI parsing)

## Detailed Description

### 拉起金融类应用startAbilityByType_3more.md 拉起金融类应用startAbilityByType_3more.md
This technical guide explains how to launch a financial application extension panel using the startAbilityByType interface. It covers parameter specifications for the finance type, specifically sceneType and bankCardNo, and provides step-by-step development instructions. Key takeaways include a Cangjie code example demonstrating how to construct JSON parameters and handle the API callback within a UI component. Targeted at developers using the Cangjie language and AbilityKit, this document facilitates the integration of financial transaction capabilities like transfers or credit card repayments.

### 目标方开发步骤.md 目标方开发步骤.md
This guide outlines the development steps for configuring a HarmonyOS application as a target for external feature invocations. It covers configuring module.json5 with uris and linkFeature attributes, and parsing incoming parameters within UIAbility lifecycle methods. Developers must define specific schemes and features like Transfer or CreditCardRepayment to match external Want objects. The document provides a Cangjie code example demonstrating how to extract want.uri and want.parameters to dynamically load pages like TransferPage. This tutorial targets HarmonyOS developers using the Cangjie language who need to implement deep linking or feature-specific routing for financial applications.