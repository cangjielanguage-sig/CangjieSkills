# 交叉轴对齐方式

This directory provides a comprehensive technical guide on configuring cross-axis alignment within layout systems, specifically focusing on the ArkUI framework and the Cangjie programming language. It covers the fundamental rules governing parent containers and child elements, detailing how properties like `alignItems`, `alignSelf`, and `alignContent` control positioning. The content explains the priority hierarchy where child settings override container defaults and explores various alignment modes such as Start, Center, Stretch, and Baseline. It is suitable for HarmonyOS developers and UI designers seeking granular control over Flex layouts to build responsive interfaces.

## Quick Navigation

**What do you want to learn?**

*   Understand the basic rules and priority hierarchy of cross-axis alignment → 交叉轴对齐方式.md
*   Configure alignment for the entire Flex container (alignItems) → 容器组件设置交叉轴对齐.md
*   Override alignment for individual child elements (alignSelf) → 子元素设置交叉轴对齐.md
*   Manage alignment in multi-line wrapping layouts (alignContent) → 内容对齐/

## Detailed Description

### 交叉轴对齐方式.md 交叉轴对齐方式.md
This document serves as a technical reference explaining the configuration of cross-axis alignment within a layout system. It covers the interaction between parent containers and child elements regarding alignment properties. The key information provided is the priority hierarchy, explicitly stating that alignment settings applied to child elements override those set on the container. This brief guide is intended for developers or designers utilizing layout frameworks, helping them understand how to control element positioning effectively. Keywords include cross-axis, alignment, container, child elements, priority, and layout rules.

### 子元素设置交叉轴对齐.md 子元素设置交叉轴对齐.md
This document demonstrates how to override the cross-axis alignment of individual child elements within a Flex layout container using the alignSelf attribute in the Cangjie programming language. The content covers the relationship between the parent container's alignItems property and the child element's alignSelf property, specifically how the latter takes precedence. The provided code example illustrates a Flex container set to center alignment, where specific child components use alignSelf to align to the start or baseline, effectively overriding the parent's default configuration. This tutorial targets developers working with the ArkUI framework and Cangjie language who need granular control over UI component alignment.

### 容器组件设置交叉轴对齐.md 容器组件设置交叉轴对齐.md
This technical guide explains how to configure cross-axis alignment for child elements within a Flex container using the Cangjie programming language. It details six specific alignment options available via the alignItems parameter: Auto, Start, Center, End, Stretch, and Baseline. Each option is demonstrated with executable Cangjie code snippets and corresponding visual references, illustrating how elements behave under different layout rules, such as stretching to fill the container or aligning along the text baseline. The content targets HarmonyOS developers using Cangjie who need to manage UI layout and element positioning effectively.

### 内容对齐/
This directory serves as a specialized technical resource for mastering content alignment within multi-line Flex layouts using the Cangjie language, primarily targeting the ArkUI development environment. It comprehensively covers the `alignContent` parameter, a critical property for determining how rows of child elements are positioned along the cross-axis in wrapping containers. The content explores a full spectrum of alignment modes, ranging from fundamental placements like `FlexAlign.Start`, `Center`, and `End` to sophisticated spacing distributions such as `SpaceBetween`, `SpaceAround`, and `SpaceEvenly`. Through the use of executable Cangjie code snippets and visual demonstrations, this guide clarifies the distinct behaviors of each alignment strategy. It is designed for UI developers and software engineers who require precise control over layout mechanics to build responsive and visually consistent applications.