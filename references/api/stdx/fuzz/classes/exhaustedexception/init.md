<!-- cj-doc kind="api-member" level="6" id="stdx.fuzz.fuzz.class.exhaustedexception.init" parent="stdx.fuzz.fuzz.class.exhaustedexception" -->
# ExhaustedException.init

[← ExhaustedException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建 ExhaustedException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

创建 ExhaustedException 实例。

### 契约

参数：

- message: String - 异常提示信息。
