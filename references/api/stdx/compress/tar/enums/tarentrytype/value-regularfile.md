<!-- cj-doc kind="api-member" level="6" id="stdx.compress.tar.enum.tarentrytype.value-regularfile" parent="stdx.compress.tar.enum.tarentrytype" -->
# TarEntryType.RegularFile

[← TarEntryType](index.md)

## 签名

```cangjie role=signature
RegularFile
```

构造一个标准普通文件类型枚举实例，对应 POSIX/USTAR 格式中的普通文件（typeflag `'0'`）。

