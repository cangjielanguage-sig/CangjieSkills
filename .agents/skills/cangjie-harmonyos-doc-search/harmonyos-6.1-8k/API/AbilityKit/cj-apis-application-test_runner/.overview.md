# cj-apis-application-test_runner

本目录包含仓颉语言中 `ohos.application.test_runner` 模块的 API 参考文档，专注于指导 HarmonyOS 应用开发者实现自定义单元测试框架。文档详细介绍了模块的导入方式、所需权限声明以及核心 `TestRunner` 类的定义与使用方法。通过继承 `TestRunner` 类并覆盖 `onPrepare` 和 `onRun` 方法，开发者可以灵活地管理测试环境准备和测试用例执行流程。该资源特别适合具备仓颉语言开发能力的工程师，用于构建健壮的应用层单元测试逻辑，确保应用质量。核心关键词包括：仓颉、HarmonyOS、单元测试、TestRunner、API参考。

## 快速导航

您想了解什么？

*   查看模块的 API 参考文档及核心类定义 → cj-apis-application-test_runner.md
*   学习如何通过继承 `TestRunner` 实现自定义测试框架 → cj-apis-application-test_runner.md
*   了解 `onPrepare` 和 `onRun` 方法的具体实现细节 → cj-apis-application-test_runner.md

## 详细说明

### cj-apis-application-test_runner.md cj-apis-application-test_runner.md
本文档是仓颉语言中 `ohos.application.test_runner` 模块的 API 参考文档，旨在指导开发者实现自定义单元测试框架。文档内容全面，涵盖了模块导入、所需权限、使用说明及 `TestRunner` 类的核心定义。关键信息指出，开发者需继承 `TestRunner` 类并覆盖 `onPrepare` 和 `onRun` 方法，分别用于准备测试环境和运行测试用例。该文档适用于具备仓颉开发能力的 HarmonyOS 应用开发者，用于构建和执行应用层的单元测试逻辑，是开发测试相关功能时的必备指南。