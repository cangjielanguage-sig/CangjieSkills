<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.interface.key.encodetopem" parent="stdx.crypto.x509.interface.key" -->
# Key.encodeToPem

[← Key](index.md)

## 签名

```cangjie role=signature
func encodeToPem(): PemEntry
```

将密钥编码为 PEM 格式。

## 契约

返回值：

- PemEntry - 密钥数据 PEM 格式编码生成的对象。
