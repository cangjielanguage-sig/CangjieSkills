<!-- cj-doc kind="api-member" level="6" id="std.ast.class.body.init" parent="std.ast.class.body" -->
# Body.init

[← Body](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 Body 对象。

## init(ArrayList<Decl>)

### 签名

```cangjie role=signature
public init(decls: ArrayList<Decl>)
```

构造一个 Body 对象。

### 契约

参数：

- decls: ArrayList\<Decl> - 将要构造 Body 类型的声明列表。
