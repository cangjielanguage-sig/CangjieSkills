<!-- cj-doc kind="api-member" level="6" id="std.unittest.struct.measurementinfo.prop-name" parent="std.unittest.struct.measurementinfo" -->
# MeasurementInfo.name

[← MeasurementInfo](index.md)

## 签名

```cangjie role=signature
prop name: String
```

当前 `Measurement` 类型的唯一显示名称。

## 契约

功能：当前 `Measurement` 类型的唯一显示名称。
有助于区分报告表中的不同测量类型。
默认值为 `Measurement`。

类型：String。
