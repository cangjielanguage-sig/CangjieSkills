<!-- cj-doc kind="api-member" level="6" id="std.unittest.struct.timenow.prop-name" parent="std.unittest.struct.timenow" -->
# TimeNow.name

[← TimeNow](index.md)

## 签名

```cangjie role=signature
prop name: String
```

提供当前时间单位唯一的显示名称，例如：`Duration(ns)` 或 `Duration(s)`。

## 契约

类型：String。
