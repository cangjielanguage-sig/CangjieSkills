# interface_DatagramSocket

This directory provides comprehensive API reference documentation for the `DatagramSocket` interface in the Cangjie programming language. It focuses on packet-based network communication, specifically handling unreliable, unordered data transmission typical of protocols like UDP. The content covers the interface definition, inheritance hierarchy, and detailed specifications for properties such as localAddress, remoteAddress, and timeout settings. Additionally, it documents core functions for data transfer, including syntax, parameters, and exception handling. This resource is suitable for Cangjie developers and software engineers implementing network protocols or applications requiring datagram communication, offering the necessary technical details to manage socket data transfer and errors effectively.

## Quick Navigation

- **What do you want to learn?**
    - **Understand the interface structure:** View the main `DatagramSocket` definition, its inheritance from `Resource` and `ToString`, and properties like `localAddress` and `receiveTimeout` → interface_DatagramSocket_5more.md
    - **Implement data transfer:** Learn about the specific functions `receiveFrom` and `sendTo`, including their blocking behavior, buffer management using `Array<Byte>`, and error handling → func_receiveFromArrayByte_2more.md

## Detailed Description

### func_receiveFromArrayByte_2more.md func_receiveFromArrayByte_2more.md
This document provides API reference documentation for two network communication functions: receiveFrom and sendTo. The content details the syntax, parameters, return values, and exceptions for blocking datagram reception and transmission. Key takeaways include that receiveFrom stores data in a user-provided buffer and returns the sender's address, while sendTo transmits data to a specific remote address, potentially blocking if buffers are full. Both functions utilize Array<Byte> for data handling. This reference is intended for developers implementing network protocols who need to manage socket data transfer and error handling.

### interface_DatagramSocket_5more.md interface_DatagramSocket_5more.md
This document provides API reference documentation for the DatagramSocket interface in the Cangjie programming language, detailing its properties and methods for packet-based network communication. The content covers the interface definition, inheritance hierarchy from Resource and ToString, and detailed specifications for properties like localAddress, remoteAddress, receiveTimeout, and sendTimeout, alongside core functions receiveFrom and sendTo. Key takeaways include that DatagramSocket handles unreliable, unordered packet transmission (e.g., UDP) with size limits, supports configurable timeouts, and manages local and remote socket addresses, throwing specific exceptions like SocketException on errors. This reference is intended for Cangjie developers implementing network protocols or applications requiring datagram communication, assuming knowledge of socket programming concepts.