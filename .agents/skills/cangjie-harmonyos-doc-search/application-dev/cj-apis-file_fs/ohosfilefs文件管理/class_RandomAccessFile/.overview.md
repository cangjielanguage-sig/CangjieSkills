# class_RandomAccessFile

This directory provides comprehensive API reference documentation for the `RandomAccessFile` class in the Cangjie programming language, specifically designed for random read and write file operations. It covers core functionalities such as managing file descriptors, manipulating offset pointers, and handling data buffers. The documentation details methods for reading data, setting file pointers, and writing strings or byte arrays, including parameters for length, offset, and encoding. It also addresses error handling through `BusinessException` codes. This content is intended for Cangjie developers who possess knowledge of the FileIO kit and system file management, offering the technical details necessary to implement complex file I/O logic effectively.

## Quick Navigation

What do you want to learn or do?

*   Understand the class structure, properties (fd, filePointer), and read operations → class_RandomAccessFile_5more.md
*   Adjust the file pointer offset or write string data with specific options → func_setFilePointerInt64_2more.md
*   Write byte array data to a file using WriteOptions and error handling → func_writeArrayByte_WriteOptions.md

## Detailed Description

### class_RandomAccessFile_5more.md
This document serves as API reference documentation for the Cangjie RandomAccessFile class, designed for random read and write file operations. It covers key properties such as the file descriptor fd and offset pointer filePointer, alongside essential methods like close and read. The read function is highlighted, detailing parameters for byte buffers and ReadOptions (length, offset), return values, and potential BusinessException errors. Intended for Cangjie developers, this guide requires familiarity with the FileIo kit and system file management capabilities.

### func_setFilePointerInt64_2more.md
This document provides API reference documentation for file manipulation functions in the Cangjie programming language, specifically focusing on setting file pointers and writing data. The content details the `setFilePointer` function for adjusting file offsets and the `write` function for writing strings with configurable options like length, offset, and encoding. Key takeaways include the specific parameter requirements, return values indicating bytes written, and a comprehensive list of potential `BusinessException` error codes for handling I/O failures. Targeted at Cangjie developers, this guide requires knowledge of file I/O concepts and exception handling to effectively manage random access file operations.

### func_writeArrayByte_WriteOptions.md
This document serves as API reference material for the Cangjie function write, designed to write byte array data to a file. It covers the function signature, parameters including the data buffer and WriteOptions for length and offset control, and the Int64 return value representing bytes written. Key takeaways include the handling of specific file system error codes via BusinessException and the default UTF-8 encoding support. The content targets Cangjie developers working with the FileIO kit, providing a usage example for implementing file writing operations with error handling.