# interface_WrappingOpT

This directory contains documentation for the `WrappingOp` interface in the Cangjie programming language, which defines integer arithmetic operations that handle overflow by truncating high-order bits. It covers the core interface definition and its implementations across various integer types, including signed and unsigned variants (Int8, Int16, Int32, Int64, IntNative, and their unsigned counterparts). The documentation details operations like addition, subtraction, multiplication, division, and bit shifting, ensuring deterministic behavior where results wrap around instead of causing errors. This resource is suitable for Cangjie developers working on systems programming, low-level algorithms, or performance-critical code requiring precise control over numeric overflow.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and method specifications → interface_WrappingOpT_11more.md
*   Explore implementations for specific integer types:
    *   16-bit integers (Int16, UInt16) → [2], [7]
    *   32-bit integers (Int32, UInt32) → [3], [8]
    *   64-bit integers (Int64, UInt64) → [4], [9]
    *   8-bit integers (Int8, UInt8) → [5], [10]
    *   Native integers (IntNative, UIntNative) → [6], [11]

## Detailed Description

### interface_WrappingOpT_11more.md interface_WrappingOpT_11more.md
This document serves as the API reference for the Cangjie `WrappingOp` interface, defining integer arithmetic operations that handle overflow by truncating high-order bits. It covers the interface definition and specifications for methods like addition, subtraction, multiplication, and shifting. It targets developers implementing low-level numeric algorithms requiring wrapping behavior.

### [2] extend_Int16_WrappingOpInt16
Provides API reference for the `WrappingOp` implementation for the `Int16` type. It covers arithmetic and bitwise operations using high-order truncation to manage overflow safely. This is crucial for developers needing deterministic integer behavior in systems programming without runtime errors.

### [3] extend_Int32_WrappingOpInt32
Offers API documentation for the `WrappingOp` implementation for `Int32`. It details arithmetic and bitwise operations using a high-bit truncation strategy, ensuring results wrap around on overflow. It specifies that shift operations use the low 5 bits of the count, essential for deterministic low-level programming.

### [4] extend_Int64_WrappingOpInt64_WrappingPow
Details the `WrappingOp` and `WrappingPow` interface implementation for `Int64`. It covers arithmetic, bitwise, and exponentiation operations using high-bit truncation to handle overflow. This documentation is vital for developers requiring precise control over integer overflow in low-level data processing.

### [5] extend_Int8_WrappingOpInt8
Focuses on the `WrappingOp` implementation for the `Int8` data type. It details arithmetic and bitwise operations with high-bit truncation to manage overflow seamlessly. It is intended for developers working on performance-critical algorithms requiring precise control over 8-bit signed integer arithmetic.

### [6] extend_IntNative_WrappingOpIntNative
Provides API documentation for the `WrappingOp` implementation for the `IntNative` type. It focuses on arithmetic and bitwise operations designed to handle overflow through high-bit truncation, ensuring predictable results. This resource is essential for low-level systems programming where deterministic overflow behavior is necessary.

### [7] extend_UInt16_WrappingOpUInt16
Contains API reference for the `WrappingOp` implementation specific to `UInt16`. It covers arithmetic and bitwise operations designed to handle overflow through high-bit truncation, ensuring results wrap around. It is crucial for system-level programming requiring precise control over unsigned 16-bit integer arithmetic.

### [8] extend_UInt32_WrappingOpUInt32
Contains API documentation for the `WrappingOp` implementation for `UInt32`. It focuses on arithmetic and bitwise operations with high-bit truncation to handle overflow safely. It highlights how shift operations manage excess bits, suitable for developers needing predictable behavior in unsigned 32-bit arithmetic.

### [9] extend_UInt64_WrappingOpUInt64
Focuses on the `WrappingOp` implementation for the `UInt64` type. It covers arithmetic and bitwise operations using high-order truncation to handle overflow. It details specific behaviors for shift operations, such as using the low 6 bits, making it suitable for performance-critical scenarios.

### [10] extend_UInt8_WrappingOpUInt8
Provides API documentation for the `WrappingOp` implementation for `UInt8`. It covers arithmetic and bitwise operations designed to handle overflow through high-order truncation. It is suitable for developers working on low-level systems or embedded programming where precise control over numeric overflow is essential.

### [11] extend_UIntNative_WrappingOpUIntNative
Offers API documentation for the `WrappingOp` implementation for `UIntNative`. It details arithmetic and bitwise functions managing overflow through high-bit truncation. This resource is ideal for developers requiring low-level control over integer behavior and consistent overflow handling in systems-level applications.