# 指定密钥参数生成非对称密钥对仓颉

This directory focuses on the technical implementation of generating asymmetric key pairs using specified parameters within the Cangjie programming language and the CryptoArchitectureKit framework. It covers essential cryptographic algorithms including RSA, ECC, and SM2, providing detailed guides on constructing key specifications, handling BigInt inputs in big-endian mode, and managing key attributes. The content is designed for software developers and security engineers who need to integrate custom key generation workflows into their applications, ensuring secure encryption, decryption, and key storage capabilities.

## Quick Navigation

**What do you want to learn?**

*   Learn the general process for generating asymmetric keys in Cangjie → 指定密钥参数生成非对称密钥对仓颉.md
*   Generate ECC key pairs with specific parameters → 指定密钥参数生成ECC密钥对.md
*   Generate SM2 key pairs based on curve names → 根据椭圆曲线名生成SM2密钥对.md
*   Explore practical ECC code examples and parameter inspection → 以根据密钥参数生成ECC密钥为例/
*   Generate RSA public keys using predefined parameters → 指定密钥参数生成RSA公钥/

## Detailed Description

### 指定密钥参数生成ECC密钥对.md 指定密钥参数生成ECC密钥对.md
This technical tutorial guides developers on generating Elliptic Curve Cryptography (ECC) key pairs using specified parameters within the CryptoArchitectureKit framework. The document outlines a four-step process: constructing an ECCCommonParamsSpec object, creating an asymmetric key generator via createAsyKeyGeneratorBySpec, generating the key pair using generateKeyPair, and retrieving key parameters with getAsyKeySpec. Key information includes the requirement to set the algorithm name to 'ECC' and the type to COMMON_PARAMS_SPEC. It also specifies that bigint inputs must be positive and in big-endian mode. This guide targets software developers implementing cryptographic functions, requiring prior knowledge of the API reference and ECC specifications.

### 指定密钥参数生成非对称密钥对仓颉.md 指定密钥参数生成非对称密钥对仓颉.md
This technical guide demonstrates how to generate asymmetric key pairs using specified parameters within the Cangjie programming language. It covers implementation details for three major cryptographic algorithms: RSA, ECC, and SM2. The document explains the process of creating KeyPair objects and retrieving their parameter attributes, which are essential for subsequent cryptographic operations like encryption and decryption, as well as for secure storage and transport. Targeted at Cangjie developers, this guide provides the necessary code patterns to integrate these security features into applications. Keywords: Cangjie, Asymmetric Key Pair, RSA, ECC, SM2, Cryptography, Key Generation.

### 根据椭圆曲线名生成SM2密钥对.md 根据椭圆曲线名生成SM2密钥对.md
This technical guide details the procedure for generating SM2 asymmetric key pairs based on elliptic curve names using the CryptoArchitectureKit. It covers constructing public key parameters via ECCCommonParamsSpec, creating ECCKeyPairSpec objects, initializing the generator with createAsyKeyGeneratorBySpec, generating keys, and retrieving curve parameters. The document emphasizes that BigInt inputs must be in big-endian mode and positive. It provides a complete Cangjie code example demonstrating the workflow from parameter specification to key generation. Targeted at developers implementing cryptography in Cangjie, this guide requires knowledge of SM2 algorithms and API specifications. Keywords: SM2, Key Pair Generation, Elliptic Curve, CryptoArchitectureKit, Cangjie, API.

### 以根据密钥参数生成ECC密钥为例/
This directory provides comprehensive Cangjie code examples focused on generating Elliptic Curve Cryptography (ECC) keys based on specific parameters. It details the process of defining common ECC specifications, constructing `ECCCommonParamsSpec` structures with field types and curve coefficients, and utilizing the `CryptoArchitectureKit`. The tutorials cover essential operations such as logging BigInt values, retrieving detailed key attributes, and extracting specific parameters like generator points and private key values. This resource is ideal for developers implementing custom cryptographic operations who need a practical guide to key generation workflows and parameter inspection within the Cangjie environment.

### 指定密钥参数生成RSA公钥/
This directory provides technical resources and code examples focused on the specific cryptographic task of generating RSA public keys using predefined parameters rather than relying on random generation. It details the necessary steps for constructing key specifications, such as defining the modulus and public exponent, and initializing cryptographic generators to produce valid RSA public key objects. The content covers essential programming concepts including BigInt handling in big-endian mode, API usage for key generation, and the critical practice of validating generated keys against their original specifications. These tutorials are specifically designed for software developers and security engineers working with cryptographic frameworks like CryptoArchitectureKit in languages such as Cangjie and Kotlin, aiming to implement secure, custom key management solutions.