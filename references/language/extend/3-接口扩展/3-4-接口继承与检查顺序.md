<!-- cj-doc kind="guide-leaf" level="5" id="language.extend.3-接口扩展.3-4-接口继承与检查顺序" parent="language.extend.3-接口扩展" -->
# 3.4 接口继承与检查顺序

[← 3. 接口扩展](index.md)

同一类型的接口扩展存在父子接口关系时先检查父接口、再检查子接口；子接口默认实现可覆盖先继承的同签名实现，顺序无法确定时编译报错。

- 父接口扩展先检查，然后子接口
- 子接口的默认实现**覆盖**父接口的
- 两个无关接口提供同一成员的冲突默认实现 → **编译错误**
```cangjie cjtest=syntax id=syntax-e0f581a399-1 form=unit
interface I1 {
    func foo(): Unit { println("I1 foo") }
}
interface I2 <: I1 {
    func foo(): Unit { println("I2 foo") }
}
class A {}

extend A <: I1 {}  // 先检查
extend A <: I2 {}  // 后检查，I2.foo 覆盖 I1.foo

main() {
    A().foo()  // 输出：I2 foo
}
```
