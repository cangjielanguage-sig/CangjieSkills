<!-- cj-doc kind="api-member" level="6" id="std.math.interface.floatingpoint.isnan" parent="std.math.interface.floatingpoint" -->
# FloatingPoint<T>.isNaN

[← FloatingPoint<T>](index.md)

## 签名

```cangjie role=signature
func isNaN(): Bool
```

判断浮点数是否为非数值。

## 契约

返回值：

- Bool - 如果浮点数的值为非数值，则返回 `true`；否则，返回 `false`。
