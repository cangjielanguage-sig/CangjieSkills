<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.getaddressbytes" parent="std.net.class.ipaddress" -->
# IPAddress.getAddressBytes

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public func getAddressBytes(): Array<Byte>
```

返回此 IPAddress 对象的原始 IP 地址。

## 契约

返回值：

- Array\<Byte> - 原始 IP 地址的 Array\<Byte> 表示。
