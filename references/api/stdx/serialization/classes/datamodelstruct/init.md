<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.datamodelstruct.init" parent="stdx.serialization.serialization.class.datamodelstruct" -->
# DataModelStruct.init

[← DataModelStruct](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个空参的 `DataModelStructfields` 默认为空的 ArrayList<Field>。

## init(ArrayList<Field>)

### 签名

```cangjie role=signature
public init(list: ArrayList<Field>)
```

构造一个具有初始数据的 DataModelStruct。

### 契约

参数：

- list: ArrayList\<Field> - 传入的 ArrayList\<Field> 类型的数据。
