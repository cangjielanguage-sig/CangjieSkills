<!-- cj-doc kind="api-member" level="6" id="std.overflow.interface.wrappingop.wrappingdec" parent="std.overflow.interface.wrappingop" -->
# WrappingOp<T>.wrappingDec

[← WrappingOp<T>](index.md)

本页汇总 11 个同名重载；先按签名选择，再读取对应契约。

## func wrappingDec()

### 签名

```cangjie role=signature
func wrappingDec(): T
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- T - 自减运算结果。

## func wrappingDec()

适用扩展：[extend Int16 <: WrappingOp<Int16>](extensions/extend-int16-wrappingop-int16.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): Int16
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- Int16 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend Int32 <: WrappingOp<Int32>](extensions/extend-int32-wrappingop-int32.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): Int32
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- Int32 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend Int64 <: WrappingOp<Int64> & WrappingPow](extensions/extend-int64-wrappingop-int64-wrappingpow.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): Int64
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- Int64 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend Int8 <: WrappingOp<Int8>](extensions/extend-int8-wrappingop-int8.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): Int8
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- Int8 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend IntNative <: WrappingOp<IntNative>](extensions/extend-intnative-wrappingop-intnative.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): IntNative
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- IntNative - 自减运算结果。

## func wrappingDec()

适用扩展：[extend UInt16 <: WrappingOp<UInt16>](extensions/extend-uint16-wrappingop-uint16.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): UInt16
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- UInt16 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend UInt32 <: WrappingOp<UInt32>](extensions/extend-uint32-wrappingop-uint32.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): UInt32
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- UInt32 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend UInt64 <: WrappingOp<UInt64>](extensions/extend-uint64-wrappingop-uint64.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): UInt64
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- UInt64 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend UInt8 <: WrappingOp<UInt8>](extensions/extend-uint8-wrappingop-uint8.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): UInt8
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- UInt8 - 自减运算结果。

## func wrappingDec()

适用扩展：[extend UIntNative <: WrappingOp<UIntNative>](extensions/extend-uintnative-wrappingop-uintnative.md)。

### 签名

```cangjie role=signature
public func wrappingDec(): UIntNative
```

使用高位截断策略的自减运算。

### 契约

当运算出现溢出时，高位截断，否则返回运算结果。

返回值：

- UIntNative - 自减运算结果。
