<!-- cj-doc kind="api-member" level="6" id="std.convert.interface.radixconvertible.tostring" parent="std.convert.interface.radixconvertible" -->
# RadixConvertible<T>.toString

[← RadixConvertible<T>](index.md)

本页汇总 9 个同名重载；先按签名选择，再读取对应契约。

## func toString(Int64)

### 签名

```cangjie role=signature
func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

## func toString(Int64)

适用扩展：[extend Int16 <: RadixConvertible<Int16>](extensions/extend-int16-radixconvertible-int16.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend Int32 <: RadixConvertible<Int32>](extensions/extend-int32-radixconvertible-int32.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend Int64 <: RadixConvertible<Int64>](extensions/extend-int64-radixconvertible-int64.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend Int8 <: RadixConvertible<Int8>](extensions/extend-int8-radixconvertible-int8.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend UInt16 <: RadixConvertible<UInt16>](extensions/extend-uint16-radixconvertible-uint16.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend UInt32 <: RadixConvertible<UInt32>](extensions/extend-uint32-radixconvertible-uint32.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend UInt64 <: RadixConvertible<UInt64>](extensions/extend-uint64-radixconvertible-uint64.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。

## func toString(Int64)

适用扩展：[extend UInt8 <: RadixConvertible<UInt8>](extensions/extend-uint8-radixconvertible-uint8.md)。

### 签名

```cangjie role=signature
public func toString(radix!: Int64): String
```

返回指定进制形式字符串。

### 契约

参数：

- radix!: Int64 - 指定的进制。

返回值：

- String - 指定进制形式字符串。

异常：

- IllegalArgumentException - 当进制不合规时，抛出异常。
