<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509certificate.tostring" parent="stdx.crypto.x509.class.x509certificate" -->
# X509Certificate.toString

[← X509Certificate](index.md)

## 签名

```cangjie role=signature
public override func toString(): String
```

生成证书名称字符串，包含证书的使用者信息、有效期以及颁发者信息。

## 契约

返回值：

- String - 证书名称字符串。
