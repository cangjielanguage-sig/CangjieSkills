<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.datamodelfloat.init" parent="stdx.serialization.serialization.class.datamodelfloat" -->
# DataModelFloat.init

[← DataModelFloat](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init(Float64)

### 签名

```cangjie role=signature
public init(fv: Float64)
```

构造一个具有初始数据的 DataModelFloat 实例。

### 契约

参数：

- fv: Float64 - 传入的 Float64 类型的数据。

## init(Int64)

### 签名

```cangjie role=signature
public init(v: Int64)
```

构造一个具有初始数据的 DataModelFloat 实例。

### 契约

参数：

- v: Int64 - 传入的 Int64 类型的数据。
