<!-- cj-doc kind="api-member" level="6" id="std.core.struct.array.filter" parent="std.core.struct.array" -->
# Array<T>.filter

[← Array<T>](index.md)

## 签名

```cangjie role=signature
public func filter(predicate: (T) -> Bool): Array<T>
```

返回一个满足筛选条件的元素的新数组。

## 注意
>
不支持平台：OpenHarmony。

## 参数

- predicate: (T) -> Bool - 给定的条件。

## 返回值

- Array<T> - 返回一个满足筛选条件的元素的新数组。

