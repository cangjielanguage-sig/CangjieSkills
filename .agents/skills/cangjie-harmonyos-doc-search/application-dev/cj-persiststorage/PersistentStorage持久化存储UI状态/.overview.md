# PersistentStorage持久化存储UI状态

This directory provides a comprehensive technical overview of the PersistentStorage module within the ArkUI framework, specifically designed to manage the persistence of UI state across application restarts. It thoroughly explains the synchronization mechanisms with AppStorage, the lifecycle of data persistence, and the correct implementation patterns required to avoid common errors. Key topics include supported data types, the critical order of operations for initialization, performance constraints such as the 2kb data limit, and strategies to prevent data loss during app updates. This resource is essential for application developers using ArkUI or Cangjie who need to build robust applications with reliable state management and consistent user experiences.

## Quick Navigation

What do you want to learn?

- Understand the core mechanism, data types, and performance limitations? → PersistentStorage持久化存储UI状态_3more.md
- Learn correct usage patterns, initialization order, and data flow? → 使用场景.md

## Detailed Description

### PersistentStorage持久化存储UI状态_3more.md PersistentStorage持久化存储UI状态_3more.md
This technical guide explains how to use PersistentStorage to persist UI state across application restarts by synchronizing with AppStorage. It covers the overview of the persistence mechanism, the relationship between PersistentStorage and AppStorage, and specific usage limitations. Key information includes support for simple types and JSON-serializable objects, while nested objects are unsupported due to change detection issues. The document warns against persisting large datasets or frequently changing variables, recommending data under 2kb to avoid synchronous UI thread blocking. This guide targets application developers using the ArkUI framework who need to manage state persistence, assuming prior knowledge of AppStorage.

### 使用场景.md 使用场景.md
This technical guide explains the correct usage patterns for integrating PersistentStorage with AppStorage in ArkUI/Cangjie applications to manage data persistence. It covers initializing properties via persistProp, accessing them using @StorageLink, and the critical order of operations between storage mechanisms. Key information includes the data flow during first launch, updates, and subsequent launches, emphasizing that accessing AppStorage before PersistentStorage causes data loss. The document details how to correctly load persisted values and conditionally override them. This guide targets developers building ArkUI applications who need to understand state management and data persistence lifecycles.