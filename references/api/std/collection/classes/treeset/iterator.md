<!-- cj-doc kind="api-member" level="6" id="std.collection.class.treeset.iterator" parent="std.collection.class.treeset" -->
# TreeSet<T> where T <: Comparable<T>.iterator

[← TreeSet<T> where T <: Comparable<T>](index.md)

## 签名

```cangjie role=signature
public func iterator(): Iterator<T>
```

返回 TreeSet 的迭代器，迭代器按元素值从小到大的顺序迭代。

## 契约

返回值：

- Iterator\<T> - TreeSet 的迭代器。
