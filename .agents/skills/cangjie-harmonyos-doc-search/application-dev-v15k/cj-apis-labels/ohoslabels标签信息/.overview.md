# ohoslabels标签信息

This directory contains API reference documentation for the `ohos.labels` package in the Cangjie language, focusing on metadata annotations and permission management systems. It covers classes and structs used to define API attributes, such as API levels, deprecation versions, system capabilities, and permission logic. Key components include the `APILevel` class, permission combination classes (`PermissionAnd`, `PermissionOr`), and extensions to the `String` struct for permission handling. This documentation is intended for system developers, framework engineers, and software developers working on HarmonyOS API definitions who need to implement access control and versioning logic.

## Quick Navigation

**What do you want to learn?**

*   Define API metadata and versioning attributes → class_APILevel.md
*   Implement logical permission combinations (AND/OR) → class_PermissionAnd_2more.md
*   Understand the package overview and PermissionValue interface → ohoslabels标签信息_4more.md
*   Use strings to represent and manipulate permissions → struct_String.md

## Detailed Description

### class_APILevel.md class_APILevel.md
This document serves as API reference documentation for the Cangjie language's APILevel class, which defines metadata tags used to annotate APIs. The content details the class structure, listing read-only properties such as API level, deprecation version, required permissions, and system capabilities. It also documents the constructor, specifying parameters for atomic service support, cross-platform compatibility, and Stage model restrictions. Key information includes the default values for these attributes and the read-only nature of the properties. This guide is intended for developers using Cangjie to define or consume APIs, requiring an understanding of API versioning and system architecture concepts like the Stage model.

### class_PermissionAnd_2more.md class_PermissionAnd_2more.md
This API reference document defines the PermissionAnd and PermissionOr classes in the Cangjie language, designed to represent logical combinations of permission sets. The documentation covers the constructors for both classes, which accept two PermissionValue parameters, and their overridden operator functions. Key information includes the implementation of bitwise & (AND) and | (OR) operators that allow these permission sets to interact and combine with other PermissionValue objects. Intended for software developers, this reference requires an understanding of the Cangjie syntax and permission management systems to effectively implement access control logic.

### ohoslabels标签信息_4more.md ohoslabels标签信息_4more.md
This document serves as API reference documentation for the ohos.labels package, detailing annotations used to describe API attributes like permissions, system capabilities, and support models. It primarily covers the PermissionValue interface, which defines operator overloads for performing bitwise AND and OR operations on permission sets. Key information includes the specific parameters and return values for these operators, alongside a note that the package is intended for internal API annotation and is not recommended for general users. The content targets system developers or framework engineers working with HarmonyOS API definitions in the Cangjie language.

### struct_String.md struct_String.md
This API reference documents the extension of the String struct in the Cangjie language to implement the PermissionValue interface, enabling strings to represent individual permissions. The content covers the inheritance hierarchy and defines two overloaded operator functions: the bitwise AND (&) and OR (|) operators for combining permission sets. Key information includes that both operators accept a PermissionValue parameter and return a new PermissionValue, facilitating complex permission logic starting from version 19. This technical documentation targets Cangjie developers implementing permission management systems who need to understand how to manipulate string-based permissions using set operations. Keywords: Cangjie, String, PermissionValue, API reference, bitwise operators, permission management.