<!-- cj-doc kind="guide-leaf" level="4" id="language.for.5-自定义可迭代类型" parent="language.for" -->
# 5. 自定义可迭代类型

[← for-in 与迭代](index.md)

实现 `Iterable<T>` 接口即可获得 for-in 支持：

```cangjie cjtest=syntax id=syntax-eedf22a7cb-1 form=unit
class Countdown <: Iterable<Int64> {
    let start: Int64
    init(start: Int64) { this.start = start }

    public func iterator(): Iterator<Int64> {
        CountdownIterator(start)
    }
}

class CountdownIterator <: Iterator<Int64> {
    var current: Int64
    init(start: Int64) { current = start }

    public func next(): Option<Int64> {
        if (current > 0) {
            let v = current
            current--
            return Some(v)
        }
        return None
    }
}

main() {
    for (i in Countdown(5)) {
        print("${i} ")  // 输出：5 4 3 2 1
    }
}
```

---
