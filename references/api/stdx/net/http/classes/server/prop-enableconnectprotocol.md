<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.class.server.prop-enableconnectprotocol" parent="stdx.net.http.class.server" -->
# Server.enableConnectProtocol

[← Server](index.md)

## 签名

```cangjie role=signature
public prop enableConnectProtocol: Bool
```

HTTP/2 专用，用来限制对端发送的报文是否支持通过 connect 方法升级协议，true 表示支持。

## 契约

类型：Bool
