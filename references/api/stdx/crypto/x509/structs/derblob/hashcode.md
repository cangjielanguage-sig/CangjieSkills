<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.derblob.hashcode" parent="stdx.crypto.x509.struct.derblob" -->
# DerBlob.hashCode

[← DerBlob](index.md)

## 签名

```cangjie role=signature
public override func hashCode(): Int64
```

返回 DerBlob 对象哈希值。

## 契约

返回值：

- Int64 - 对 DerBlob 对象进行哈希计算后得到的结果。
