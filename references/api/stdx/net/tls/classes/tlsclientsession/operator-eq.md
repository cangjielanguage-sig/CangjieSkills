<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.class.tlsclientsession.operator-eq" parent="stdx.net.tls.class.tlsclientsession" -->
# TlsClientSession.==

[← TlsClientSession](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: TlsClientSession): Bool
```

判断会话 id 是否相同。

## 参数

- other: TlsClientSession - 被比较的会话对象。

## 返回值

- Bool - 若会话对象相同，返回 `true`；否则，返回 `false`。

