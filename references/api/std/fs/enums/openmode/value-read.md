<!-- cj-doc kind="api-member" level="6" id="std.fs.enum.openmode.value-read" parent="std.fs.enum.openmode" -->
# OpenMode.Read

[← OpenMode](index.md)

## 签名

```cangjie role=signature
Read
```

构造一个 OpenMode 实例，指定以只读的方式打开文件。

## 契约

功能：构造一个 OpenMode 实例，指定以只读的方式打开文件。如果文件不存在，则将引发 FSException 异常。
