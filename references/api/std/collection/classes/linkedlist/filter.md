<!-- cj-doc kind="api-member" level="6" id="std.collection.class.linkedlist.filter" parent="std.collection.class.linkedlist" -->
# LinkedList<T>.filter

[← LinkedList<T>](index.md)

## 签名

```cangjie role=signature
public func filter(predicate: (T) -> Bool): LinkedList<T>
```

返回一个满足筛选条件的元素的新链表。

## 注意
>
不支持平台：OpenHarmony。

## 参数

- predicate: (T) -> Bool - 给定的条件。

## 返回值

- LinkedList<T> - 返回一个满足筛选条件的元素的新链表。

