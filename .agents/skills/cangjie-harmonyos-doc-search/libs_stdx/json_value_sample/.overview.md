# json_value_sample

This directory provides a comprehensive code tutorial focused on the practical implementation of JSON data handling within the Cangjie programming language. It specifically addresses the critical need for converting between raw string data and structured `JsonValue` objects, a common requirement for data interchange and configuration management. The tutorial covers the complete lifecycle of JSON processing, including parsing raw strings into objects using `JsonValue.fromStr` and serializing them back to string formats. It distinguishes between compact output via `toString()` and human-readable, pretty-printed results via `toJsonString()`. This resource is suitable for Cangjie developers looking to master the `stdx.encoding.json` library and handle complex nested structures efficiently. By following this guide, users will gain the necessary skills to integrate robust JSON parsing and generation capabilities into their software projects.

## Quick Navigation

**What do you want to learn?**
*   **Parsing JSON Strings:** Learn how to convert raw JSON text into a structured `JsonValue` object using the `JsonValue.fromStr` method → json_value_sample.md
*   **Serializing Objects:** Understand how to transform `JsonValue` objects back into string format for data storage or network transmission → json_value_sample.md
*   **Output Formatting:** Explore the differences between generating a compact string with `toString()` and a formatted, pretty-printed result with `toJsonString()` → json_value_sample.md
*   **Handling Nested Data:** See practical examples of how to manage and process complex nested JSON structures within your Cangjie applications → json_value_sample.md
*   **Library Usage:** Gain insights into the `stdx.encoding.json` library and its role in Cangjie data processing workflows → json_value_sample.md

**Who is this for?**
*   Cangjie developers seeking to implement JSON parsing and serialization → json_value_sample.md

## Detailed Description

The directory contains a single file that provides the complete tutorial and code examples.

### json_value_sample.md json_value_sample.md
This document serves as a code tutorial demonstrating how to perform mutual conversions between JsonValue objects and String types in the Cangjie programming language. It covers the parsing of a raw JSON string into a JsonValue object using JsonValue.fromStr and the subsequent serialization of the object back into string formats. The example highlights two output methods: toString() for a compact representation and toJsonString() for a formatted, pretty-printed result, illustrating the handling of complex nested JSON structures. Intended for Cangjie developers, this sample requires basic knowledge of the language syntax and the stdx.encoding.json library to implement JSON data processing. Keywords: Cangjie, JSON, JsonValue, parsing, serialization, pretty-printing.