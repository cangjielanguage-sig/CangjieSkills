<!-- cj-doc kind="guide-leaf" level="5" id="language.pattern_match.2-模式类型.2-7-模式嵌套" parent="language.pattern_match.2-模式类型" -->
# 2.7 模式嵌套

[← 2. 模式类型](index.md)

元组和枚举模式可**任意深度嵌套**：

```cangjie cjtest=syntax id=syntax-92692182a8-1 form=unit
enum TimeUnit { | Year(UInt64) | Month(UInt64) }
enum Command  { | SetTimeUnit(TimeUnit) | Quit }

main() {
    match ((SetTimeUnit(Year(2024)), true)) {
        case (SetTimeUnit(Year(y)), true) => println("year ${y}")  // 匹配
        case _ => ()
    }
}
```

---
