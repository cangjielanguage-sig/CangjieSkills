<!-- cj-doc kind="api-member" level="6" id="std.ast.class.annotation.prop-identifier" parent="std.ast.class.annotation" -->
# Annotation.identifier

[← Annotation](index.md)

## 签名

```cangjie role=signature
public mut prop identifier: Token
```

获取或设置 Annotation 节点的标识符，如 `@CallingConv[xxx]` 中的 `CallingConv`。

## 契约

类型：Token
