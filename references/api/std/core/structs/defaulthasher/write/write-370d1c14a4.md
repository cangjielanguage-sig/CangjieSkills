<!-- cj-doc kind="api-member" level="7" id="std.core.struct.defaulthasher.write.write-370d1c14a4" parent="std.core.struct.defaulthasher.write" -->
# DefaultHasher.func write(UInt64)

[← DefaultHasher.write](index.md)

## 签名

```cangjie role=signature
public mut func write(value: UInt64): Unit
```

通过该函数把想要哈希运算的 UInt64 值传入，然后进行哈希组合运算。

## 契约

参数：

- value: UInt64 - 待运算的值。
