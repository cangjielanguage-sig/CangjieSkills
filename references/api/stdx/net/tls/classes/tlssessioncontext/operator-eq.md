<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.class.tlssessioncontext.operator-eq" parent="stdx.net.tls.class.tlssessioncontext" -->
# TlsSessionContext.==

[← TlsSessionContext](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: TlsSessionContext): Bool
```

判断两 TlsSessionContext 实例名称是否相同。

## 契约

参数：

- other: TlsSessionContext - 被比较的会话上下文对象。

返回值：

- Bool - 若 TlsSessionContext 对象相同，返回 `true`；否则，返回 `false`。
