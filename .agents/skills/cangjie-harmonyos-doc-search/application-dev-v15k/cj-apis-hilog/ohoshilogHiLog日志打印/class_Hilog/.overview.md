# class_Hilog

This directory provides comprehensive API reference documentation for the Hilog class within the Cangjie programming language, designed as part of the PerformanceAnalysisKit. It is dedicated to enabling applications and system services to output categorized and structured logs. The content covers a wide range of static methods used for printing logs at various severity levels, including DEBUG, INFO, WARN, ERROR, and FATAL. It details specific system capabilities, version requirements, and mandatory parameters such as domain (a 16-bit identifier) and tag (used for class or behavior identification). This documentation is highly suitable for Cangjie developers who need to implement robust logging mechanisms, control log output based on specific domains and tags, and ensure their applications adhere to system logging standards.

## Quick Navigation

**What do you want to learn?**

*   Understand the core Hilog class API for printing logs at different levels (DEBUG, INFO, ERROR, FATAL) and learn about mandatory parameters like domain and tag → class_Hilog_5more.md
*   Learn how to check if logs are printable using `isLoggable` or output warning-level logs using `warn` → static_func_isLoggableUInt_2more.md

## Detailed Description

### class_Hilog_5more.md class_Hilog_5more.md
This document provides API reference documentation for the Hilog class in the Cangjie programming language, enabling applications and services to output categorized logs. It covers static methods for printing logs at DEBUG, INFO, ERROR, and FATAL levels, detailing their specific system capabilities and version requirements. Key information includes the mandatory parameters for all methods: domain (a 16-bit identifier), tag (for class or behavior identification), and format string. The document highlights that DEBUG logs are typically disabled in release versions. Intended for developers using the PerformanceAnalysisKit, it includes practical code examples demonstrating how to implement these logging functions effectively.

### static_func_isLoggableUInt_2more.md static_func_isLoggableUInt_2more.md
This document serves as API reference documentation for the HiLog module in the Cangjie language, detailing static functions for logging management. It covers the `isLoggable` function, which checks if logs can be printed based on domain, tag, and level, and the `warn` function, which outputs warning-level logs. Key information includes parameter definitions for `domain` (0x0-0xFFFF) and `tag`, return values, and system capabilities. The document targets Cangjie developers seeking to implement or control logging behavior within their applications, providing code examples for practical usage.