<!-- cj-doc kind="api-member" level="7" id="std.ast.class.visitor.visit.visit-6ff305e042" parent="std.ast.class.visitor.visit" -->
# Visitor.func visit(PackageHeader)

[← Visitor.visit](index.md)

## 签名

```cangjie role=signature
protected open func visit(_: PackageHeader): Unit
```

定义访问节点时的操作，需要重写。

## 契约

参数：

- _: PackageHeader - PackageHeader 类型的被遍历节点。
