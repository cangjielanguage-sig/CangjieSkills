# struct_PemEntry

This directory provides comprehensive API reference documentation for the `PemEntry` struct within the Cangjie language's X.509 package. It is dedicated to the handling of PEM-encoded data, a standard format used for storing cryptographic keys and certificates. The documentation covers the struct's core definition, initialization methods, and various utility functions for data manipulation. Key topics include parsing PEM text, managing headers, encoding binary data to strings, and identifying specific cryptographic standards such as X.509, PKCS#8, and CRLs. This resource is essential for developers and security engineers implementing cryptographic parsing, generation, or validation routines who require a deep understanding of RFC 7468 and X.509 standards.

## Quick Navigation

**What do you want to learn or do?**

*   Understand the core struct definition, PEM format components, and the specific static string constants used for identifying cryptographic types like X.509 certificates and private keys (RSA, EC, SM2) → struct_PemEntry_12more.md
*   Learn about the practical implementation details, including constructors, public properties for accessing label and body, and instance methods for encoding to string or retrieving headers → PemEntryString_ArrayString_8more.md

## Detailed Description

### PemEntryString_ArrayString_8more.md PemEntryString_ArrayString_8more.md
This document serves as API reference documentation for the PemEntry struct within an X.509 package, detailing its constructors, properties, and methods for handling PEM-encoded data in the Cangjie language. It covers the primary constructor accepting a label, headers, and optional body, alongside a simplified initialization method. The documentation also defines public properties for accessing the label, headers, and binary body, as well as instance methods for encoding to string, retrieving specific headers, and generating string representations. Key takeaways include the ability to construct PemEntry objects with optional headers and DerBlob content, encode them into OS-specific PEM formatted strings, and iterate over header values. Targeted at developers working with cryptographic operations, specifically those needing to parse, construct, or manipulate PEM entries programmatically.

### struct_PemEntry_12more.md struct_PemEntry_12more.md
This API documentation defines the PemEntry struct in the Cangjie language, designed to represent a single PEM encoded text structure for storing certificates and keys. The document covers the struct definition, its properties including label, headers, and body, and available constructors. Key information includes an explanation of the PEM format components and a comprehensive list of static string constants used to identify various cryptographic types, such as X.509 certificates, CRLs, and private keys (RSA, EC, SM2, PKCS#8). This reference is intended for developers implementing cryptographic parsing or generation who require knowledge of X.509 standards and RFC 7468.