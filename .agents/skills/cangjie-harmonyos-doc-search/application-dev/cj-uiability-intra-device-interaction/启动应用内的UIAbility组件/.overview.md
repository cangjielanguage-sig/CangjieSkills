# 启动应用内的UIAbility组件

This directory provides a comprehensive technical guide for developers working with OpenHarmony and the Cangjie language, focusing on launching UIAbility components within the same application. It covers the fundamental concepts of UIAbility as the system's smallest scheduling unit, detailing lifecycle management, navigation between modules, and parameter passing using Want objects. The content addresses both cold and hot starts, configuration in `module.json5`, and practical implementation examples. It is suitable for developers building multi-page or multi-ability applications who need to master the Stage model, component transitions, and state recovery mechanisms.

## Quick Navigation

**What do you want to learn?**

*   Understand the basics of UIAbility and internal launching → 启动应用内的UIAbility组件.md
*   View practical code samples and repository links → 示例代码.md
*   Launch a specific page within a UIAbility (Cold/Hot start) → 启动UIAbility的指定页面/
*   Manage lifecycle and start abilities using Want objects → 启动应用内的UIAbility/

## Detailed Description

### 启动应用内的UIAbility组件.md 启动应用内的UIAbility组件.md
This document serves as a technical guide explaining the methods for launching UIAbility components within the same application. It covers the definition of UIAbility as the smallest system scheduling unit and its role in navigating between functional modules on a device. The content distinguishes between starting internal application abilities and external ones, focusing specifically on intra-application implementation. Targeted at developers needing to manage navigation and module interaction within their applications, this guide provides the foundational knowledge for handling component lifecycles and transitions. Keywords: UIAbility, system scheduling, application navigation, component startup, AbilityKit.

### 示例代码.md 示例代码.md
This document functions as a reference index providing access to specific OpenHarmony development samples. The main content consists of a section titled "Example Code" which links to a GitCode repository project focused on launching UIAbility components within an application. Key information includes the specific path to the LaunchUIAbilityComponentsApp sample, illustrating how to implement component startup logic. This resource is intended for OpenHarmony developers, particularly those using the Cangjie language, who need practical examples to understand application architecture and component interaction. The document serves as a navigational aid for locating educational code.

### 启动UIAbility的指定页面/
This directory covers the technical implementation of launching specific pages within a UIAbility component in HarmonyOS development. It distinguishes between cold starts (initialization) and hot starts (state recovery), detailing how to use the Want object to pass parameters for navigation. Key topics include lifecycle callbacks like `onCreate`, `onWindowStageCreate`, and `onNewWant`, as well as routing logic using the Cangjie language. This guide is suitable for application developers using the Stage model who need to implement precise inter-page navigation, deep linking, or cross-app interactions.

### 启动应用内的UIAbility/
This directory serves as a technical resource for HarmonyOS developers focusing on the lifecycle management of UIAbility components within a single application using the Cangjie language. It details the procedures for initiating specific abilities via the `startAbility` method, parameter transmission using `Want` objects, and the proper termination of instances. The content explores the integration of the AbilityKit framework within the Stage model, offering insights into handling lifecycle callbacks like `onCreate` and managing business exceptions during termination. Additionally, it covers configuration settings in `module.json5` to control task visibility. This guide is essential for developers building multi-page or multi-ability applications requiring robust navigation and state management.