<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-mul" parent="std.math.numeric.struct.bigint" -->
# BigInt.*

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func *(that: BigInt): BigInt
```

BigInt 乘法。

## 契约

参数：

- that: BigInt - 乘数。

返回值：

- BigInt - 一个新 BigInt，它是此 BigInt 与另外一个 BigInt 相乘后的结果。
