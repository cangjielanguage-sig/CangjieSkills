# class_FuzzDataProvider

This directory contains comprehensive documentation for the `FuzzDataProvider` class within the Cangjie programming language. It serves as a utility class designed to facilitate fuzzing operations by converting raw mutation data byte streams into standard Cangjie basic data types. The documentation covers the class structure, public properties, and a wide array of methods for consuming data types such as integers, booleans, and strings. Additionally, it details error handling mechanisms, specifically the `ExhaustedException`. This resource is essential for developers and security engineers building fuzzing frameworks who require a mechanism to interpret byte sequences as structured data programmatically.

## Quick Navigation

What do you want to learn?

*   Understand the class structure and public properties → class_FuzzDataProvider_1.md
*   Access the API reference table for basic data types → class_FuzzDataProvider_2.md
*   Review comprehensive methods for integers, strings, and Runes → class_FuzzDataProvider_3.md
*   Handle data insufficiency and ExhaustedException → class_FuzzDataProvider_4.md

## Detailed Description

### class_FuzzDataProvider_1.md class_FuzzDataProvider_1.md
This document provides API reference documentation for the FuzzDataProvider class within the Cangjie programming language. It details the class structure, highlighting public properties such as the data array, remaining bytes, and offset. The primary function of this utility class is to facilitate fuzzing operations by converting raw mutation data byte streams into standard Cangjie basic data types. This information is essential for developers and security engineers building fuzzing frameworks who require a mechanism to interpret byte sequences as structured data.

### class_FuzzDataProvider_2.md class_FuzzDataProvider_2.md
This document serves as an API reference for the FuzzDataProvider class, detailing methods for consuming various data types during fuzzing operations. The content is organized into a table mapping target types, such as Bool, Byte, Int, UInt, and Float, to their corresponding API functions, including both single value and array consumption methods. Key takeaways include that all methods retrieve data from a mutation source and will throw an ExhaustedException if the available data length is insufficient. This reference is intended for developers implementing fuzzing tests who need to understand how to programmatically access and consume test data.

### class_FuzzDataProvider_3.md class_FuzzDataProvider_3.md
This document serves as a reference guide for the data consumption methods available in the FuzzDataProvider class. It details a comprehensive list of functions for retrieving various data types, including integers (Int8 to Int64), unsigned integers, booleans, Runes, and ASCII or UTF-8 strings. Key takeaways include the behavior of methods when mutation data is insufficient, specifically the throwing of an ExhaustedException, and the availability of functions to consume all remaining bytes or convert them to strings. This technical reference is intended for developers implementing fuzzing tests who need to programmatically generate and consume test data.

### class_FuzzDataProvider_4.md class_FuzzDataProvider_4.md
This documentation section explains the behavior of the FuzzDataProvider class when input data is insufficient. It covers the handling of the ExhaustedException, the interaction with the libfuzzer framework, and the impact of the max_len parameter. Key information includes that when data is insufficient, an ExhaustedException is thrown but automatically caught by the framework to signal an invalid run, allowing data to grow over time. If the maximum length is reached without satisfying requirements, the process exits, prompting users to modify test cases or increase max_len. This guide targets developers writing fuzz tests who need to understand data consumption and error handling within the fuzzing framework.