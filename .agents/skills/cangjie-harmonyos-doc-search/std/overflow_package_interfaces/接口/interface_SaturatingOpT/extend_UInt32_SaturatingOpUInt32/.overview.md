# extend_UInt32_SaturatingOpUInt32

This directory provides API reference documentation for the Cangjie programming language, specifically focusing on saturating arithmetic operations for unsigned integer types. It details the implementation of the `SaturatingOp` interface for `UInt32` and documents specific utility functions for `UInt64` and `UInt32`. The content covers essential arithmetic and bitwise operations designed to prevent unexpected wrapping behavior by handling overflow and underflow through saturation strategies. This resource is ideal for Cangjie developers seeking safe, boundary-checked integer operations to ensure numerical stability in their software projects.

## Quick Navigation

**What do you want to learn?**

*   Understand the comprehensive SaturatingOp interface for UInt32, including addition, subtraction, and bitwise logic → extend_UInt32_SaturatingOp_9more.md
*   Learn about specific saturating functions like `saturatingShr` (UInt64) and `saturatingSub` (UInt32) → func_saturatingShrUInt64_2more.md

## Detailed Description

### extend_UInt32_SaturatingOp_9more.md extend_UInt32_SaturatingOp_9more.md
This API reference document details the implementation of the SaturatingOp interface for the UInt32 type in the Cangjie programming language. It covers a comprehensive list of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and left shift. Key information includes the saturation strategy where overflow returns the maximum value and underflow returns the minimum value, preventing unexpected wrapping behavior. The left shift operation specifically clamps the shift amount to valid ranges. This documentation targets Cangjie developers requiring safe, boundary-checked arithmetic operations for unsigned 32-bit integers.

### func_saturatingShrUInt64_2more.md func_saturatingShrUInt64_2more.md
This document serves as API reference documentation for saturating arithmetic functions within the Cangjie programming language. It covers two specific functions: `saturatingShr` for UInt64 and `saturatingSub` for UInt32. Key information includes the function signatures, parameter descriptions, and the specific saturation logic applied. For `saturatingShr`, the shift amount is clamped to prevent shifting beyond the operand size, whereas `saturatingSub` returns the type's maximum or minimum value to handle overflow and underflow conditions. This guide is intended for developers requiring safe integer operations that prevent standard arithmetic errors.