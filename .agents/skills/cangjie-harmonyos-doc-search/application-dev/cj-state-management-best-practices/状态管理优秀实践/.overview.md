# 状态管理优秀实践

This directory focuses on best practices for state management in ArkUI application development using the Cangjie language. It covers performance optimization techniques designed to minimize unnecessary component re-renders and reduce execution overhead. Key topics include controlling component associations for complex state objects, optimizing loop logic by minimizing state variable reads, and using temporary variables for data calculations. Additionally, it addresses correct UI update mechanisms by avoiding dummy triggers. This guide is suitable for ArkUI developers seeking to enhance application quality and rendering efficiency through efficient state variable usage.

## Quick Navigation

**What do you want to learn?**

*   Optimize performance by controlling component associations for complex objects → 合理控制对象类型状态变量关联的组件数量_2more.md
*   Reduce rendering overhead by using temporary variables for calculations → 建议使用临时变量替换状态变量.md
*   Implement correct UI update logic without dummy triggers → 状态管理优秀实践_2more.md

## Detailed Description

### 合理控制对象类型状态变量关联的组件数量_2more.md 合理控制对象类型状态变量关联的组件数量_2more.md
This document provides performance optimization guidelines for ArkUI application development using Cangjie, focusing on state management best practices. It covers two main topics: controlling component associations for complex state objects to avoid redundant refreshes, and optimizing loop logic by minimizing state variable reads. Key takeaways include splitting complex objects to limit refresh scope and reading state variables outside loops rather than repeatedly inside iterations, illustrated with comparative code examples. This guide targets ArkUI developers seeking to enhance application performance through efficient state variable usage and avoiding unnecessary rendering overhead.

### 建议使用临时变量替换状态变量.md 建议使用临时变量替换状态变量.md
This guide advises ArkUI developers to use temporary variables for data calculations instead of directly modifying state variables to enhance application performance. It explains the mechanism where state changes trigger component re-renders and contrasts a negative example of direct manipulation with a positive example using temporary variables. Key takeaways include that direct assignments cause unnecessary rendering behaviors, whereas consolidating changes into a single assignment via a temporary variable reduces execution time. The document includes a performance comparison table demonstrating the efficiency gains. This tutorial targets application developers using the Cangjie language and ArkUI framework who seek to optimize rendering efficiency and reduce overhead.

### 状态管理优秀实践_2more.md 状态管理优秀实践_2more.md
This guide provides ArkUI application developers with best practices for efficient state management to improve application quality. It covers a specific inefficient scenario: forcing UI updates for non-state variables using a dummy trigger. The document contrasts a "Counter-example" with a "Recommended Example," demonstrating that UI updates should be driven by the framework detecting state changes. The key takeaway is to use the @State decorator on actual data variables, such as ObservedArrayList or Color, rather than relying on a separate variable to trigger refreshes. This approach ensures better performance and correct rendering logic. The content is intended for developers using the Cangjie language within the ArkUI framework.