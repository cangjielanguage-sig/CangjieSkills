<!-- cj-doc kind="api-member" level="7" id="std.core.class.stringbuilder.append.append-4884a955a9" parent="std.core.class.stringbuilder.append" -->
# StringBuilder.func append<T>(Array<T>) where T <: ToString

[← StringBuilder.append](index.md)

## 签名

```cangjie role=signature
public func append<T>(val: Array<T>): Unit where T <: ToString
```

在 StringBuilder 末尾插入参数 `val` 指定的 Array<T> 的字符串表示，类型 `T` 需要实现 ToString 接口。

## 契约

参数：

- val: Array\<T> - 插入的 Array\<T> 类型实例。
