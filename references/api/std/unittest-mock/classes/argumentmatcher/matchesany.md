<!-- cj-doc kind="api-member" level="6" id="std.unittest.mock.class.argumentmatcher.matchesany" parent="std.unittest.mock.class.argumentmatcher" -->
# ArgumentMatcher.matchesAny

[← ArgumentMatcher](index.md)

## 签名

```cangjie role=signature
public func matchesAny(arg: Any)
```

匹配任意类型的任意值。

## 契约

参数：

- arg: Any - 被检查的输入参数。任意类型的任意值。

返回值：

- Bool - 匹配结果 。
