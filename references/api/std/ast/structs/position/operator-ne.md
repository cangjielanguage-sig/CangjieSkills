<!-- cj-doc kind="api-member" level="6" id="std.ast.struct.position.operator-ne" parent="std.ast.struct.position" -->
# Position.!=

[← Position](index.md)

## 签名

```cangjie role=signature
public operator func !=(r: Position): Bool
```

比较两个 Position 实例是否不等。

## 契约

参数：

- r: Position - 与当前位置比较的另一个位置实例。

返回值：

- Bool - 当两个 Position 实例不完全相等时返回 true。
