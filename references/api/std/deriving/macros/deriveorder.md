<!-- cj-doc kind="api-member" level="5" id="std.deriving.macro.deriveorder" parent="std.deriving" -->
# @DeriveOrder

[← std.deriving](../index.md)

## 签名

```cangjie role=signature
@DeriveOrder
```

`DeriveOrder` 可为已被 @Derive 宏修饰的声明指定处理字段和属性的顺序，通常对 `Comparable` 接口有意义。
