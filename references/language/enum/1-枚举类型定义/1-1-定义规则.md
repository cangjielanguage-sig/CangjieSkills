<!-- cj-doc kind="guide-leaf" level="5" id="language.enum.1-枚举类型定义.1-1-定义规则" parent="language.enum.1-枚举类型定义" -->
# 1.1 定义规则

[← 1. 枚举类型定义](index.md)

- 使用 `enum` 关键字 + 名称 + `{}` 体定义
- 体内包含**构造器**，以 `|` 分隔（第一个 `|` 可选）
- 每个枚举须有**至少一个具名构造器**
- 构造器可以是**无参**（`Red`）或**有参**（`Red(UInt8)`）
- **同名构造器**在参数数量不同时允许（包括 0 个参数）
- **构造器名不能与关键字同名**（如 `Bool`、`Int64`、`match`）。`String` 不是关键字。若确实需要使用关键字作为构造器名，须用反引号转义（如 `` `Bool` ``），或改用其他名称（如 `JBool`）：
  ```cangjie cjtest=syntax id=syntax-7d959a8e7c-1 form=unit
  enum Value {
      // 错误：Bool 是关键字
      // | Bool(Bool)  // 编译错误
      // 正确：使用反引号转义或改名
      | `Bool`(Bool)   // 反引号转义
      | JBool(Bool)    // 改名避免冲突
      | Num(Int64)
  }
  ```
- **非穷举枚举**：可有一个特殊的未命名 `...` 构造器作为最后一个。不能直接匹配，须用 `_` 或绑定模式（详见[模式匹配](../../pattern_match/index.md)）
- **递归定义**支持（构造器参数可引用枚举自身）
- 枚举体还可包含**成员函数**、**运算符函数**和**成员属性**，但不能与构造器同名
- **枚举仅能在顶层作用域定义**
