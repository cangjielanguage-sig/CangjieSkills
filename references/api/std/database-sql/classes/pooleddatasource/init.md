<!-- cj-doc kind="api-member" level="6" id="std.database.sql.class.pooleddatasource.init" parent="std.database.sql.class.pooleddatasource" -->
# PooledDatasource.init

[← PooledDatasource](index.md)

## 签名

```cangjie role=signature
public init(datasource: Datasource)
```

通过数据源 datasource 构造一个 PooledDatasource 实例，入参必须为 Datasource 对象。

## 契约

参数：

- datasource: Datasource - 数据源。
