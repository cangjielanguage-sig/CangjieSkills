<!-- cj-doc kind="api-member" level="6" id="std.io.class.bufferedoutputstream.writebyte" parent="std.io.class.bufferedoutputstream" -->
# BufferedOutputStream<T> where T <: OutputStream.writeByte

[← BufferedOutputStream<T> where T <: OutputStream](index.md)

## 签名

```cangjie role=signature
public func writeByte(v: Byte): Unit
```

写入一个字节到绑定的输出流中。

## 契约

参数：

- v: Byte - 待写入的字节。
