# 示例代码

This directory contains a collection of Cangjie code examples focused on mastering the Flex layout system within the ArkUI framework. It covers essential layout mechanics including arrangement direction, wrapping behavior, and alignment along both main and cross axes. Additionally, it demonstrates how to manage spacing for single and multi-line layouts. These examples are designed for developers using the Cangjie language to build user interfaces for HarmonyOS, providing practical references for implementing flexible and responsive UI designs.

## Quick Navigation

**What do you want to learn?**

*   Control the direction of child components (Row/Column) → 示例1子组件排列方向.md
*   Manage single or multi-line wrapping behavior → 示例2子组件单多行排列.md
*   Align items along the main axis (justifyContent) → 示例3子组件在主轴上的对齐格式.md
*   Align items along the cross axis (alignItems) → 示例4子组件在交叉轴上的对齐方式.md
*   Align multiple lines of content (alignContent) → 示例5多行内容的对齐方式.md
*   Configure spacing between lines and items (mainSpace/crossSpace) → 示例6子组件单多行排列时的主交叉轴间距.md

## Detailed Description

### 示例1子组件排列方向.md 示例1子组件排列方向.md
This document provides a Cangjie code example demonstrating how to control the arrangement direction of child components within a Flex container. The main topics cover the implementation of four specific layout directions: Row, RowReverse, Column, and ColumnReverse. Key information includes the syntax for using FlexParams with the direction property to organize child Text components horizontally or vertically, including reversed orders. This tutorial is intended for developers using the Cangjie language and ArkUI framework to build user interfaces, serving as a practical reference for layout management.

### 示例2子组件单多行排列.md 示例2子组件单多行排列.md
This document provides a code example demonstrating how to control the layout of child components into single or multiple lines using the Flex component's wrap property in the Cangjie language. The main topics cover three specific wrapping modes: FlexWrap.Wrap, FlexWrap.NoWrap, and FlexWrap.WrapReverse, implemented within a Column layout. Key information includes the configuration of FlexParams to achieve different visual arrangements, showing how items flow or compress based on the wrap setting. This tutorial is intended for developers using the Cangjie UI framework who need to understand flexible layout behaviors for interface design.

### 示例3子组件在主轴上的对齐格式.md 示例3子组件在主轴上的对齐格式.md
This document serves as a code tutorial demonstrating how to control the alignment of child components along the main axis in a Flex layout using the justifyContent property in the Cangjie programming language. The content covers the implementation of a custom JustifyContentFlex component and an EntryView that showcases six distinct alignment modes: Start, Center, End, SpaceBetween, SpaceAround, and SpaceEvenly. Key takeaways include understanding how FlexParams and FlexAlign enums modify layout distribution, specifically how different values affect spacing between and around elements. Targeted at developers using the Cangjie language for UI development, this example is designed for those learning layout mechanics within the HarmonyOS UIKit framework.

### 示例4子组件在交叉轴上的对齐方式.md 示例4子组件在交叉轴上的对齐方式.md
This tutorial demonstrates how to control the alignment of child components along the cross axis within a Flex container using the Cangjie language. The document presents a code example featuring a reusable AlignItemsFlex component and an EntryView that iterates through different alignment configurations. Key takeaways include the visual and functional differences between ItemAlign values such as Auto, Start, Center, End, Stretch, and Baseline. Intended for Cangjie developers, this example serves as a practical guide for mastering Flex layout properties and UI component positioning.

### 示例5多行内容的对齐方式.md 示例5多行内容的对齐方式.md
This document serves as a practical code example demonstrating how to control the alignment of multi-line content within a Flex layout using the Cangjie UI framework. It covers the alignContent property used in conjunction with FlexWrap.Wrap, illustrating various alignment modes including Start, Center, End, SpaceBetween, SpaceAround, and SpaceEvenly. The code defines a reusable component AlignContentFlex and visualizes how different FlexAlign values distribute rows vertically within a container, with comments explaining specific spacing behaviors. This tutorial is intended for developers using the Cangjie language who need to implement complex flex layouts with wrapped content.

### 示例6子组件单多行排列时的主交叉轴间距.md 示例6子组件单多行排列时的主交叉轴间距.md
This document serves as a code example demonstrating how to configure main and cross-axis spacing for child components within a Flex layout using the Cangjie language. It covers three specific layout scenarios: FlexWrap.Wrap, FlexWrap.NoWrap, and FlexWrap.WrapReverse, each implemented within a Flex container. The code illustrates the use of FlexSpaceOptions to define mainSpace and crossSpace, showing how these properties affect spacing differently based on the wrapping behavior. This tutorial is intended for developers using the Cangjie UIKit who need to understand spacing control in flexible layouts.