<!-- cj-doc kind="api-member" level="5" id="std.math.func.reverse" parent="std.math" -->
# reverse

[← std.math](../index.md)

本页汇总 4 个同名重载；先按签名选择，再读取对应契约。

## reverse(UInt16)

### 签名

```cangjie role=signature
public func reverse(x: UInt16): UInt16
```

求无符号整数按位反转后的数。

### 契约

参数：

- x: UInt16 - 需要进行反转的无符号整数。

返回值：

- UInt16 - 返回反转后的无符号数。

## reverse(UInt32)

### 签名

```cangjie role=signature
public func reverse(x: UInt32): UInt32
```

求无符号整数按位反转后的数。

### 契约

参数：

- x: UInt32 - 需要进行反转的无符号整数。

返回值：

- UInt32 - 返回反转后的无符号数。

## reverse(UInt64)

### 签名

```cangjie role=signature
public func reverse(x: UInt64): UInt64
```

求无符号整数按位反转后的数。

### 契约

参数：

- x: UInt64 - 需要进行反转的无符号整数。

返回值：

- UInt64 - 返回反转后的无符号数。

## reverse(UInt8)

### 签名

```cangjie role=signature
public func reverse(x: UInt8): UInt8
```

求无符号整数按位反转后的数。

### 契约

参数：

- x: UInt8 - 需要进行反转的无符号整数。

返回值：

- UInt8 - 返回反转后的无符号数。
