# geometryTransition结合模态转场使用

This directory provides comprehensive technical guides on implementing seamless shared element animations, specifically "one-shot" transitions, between list elements and modal pages within the HarmonyOS development environment. It explores the advanced integration of `geometryTransition` with modal transitions to achieve smooth visual continuity during view navigation. The content covers essential techniques such as setting up data models, configuring `bindContentCover`, and applying `TransitionEffect` for non-shared elements. It includes practical code examples for both the Cangjie language and ArkTS, detailing how to use matching IDs and `animateTo` for precise state control. This resource is ideal for HarmonyOS developers seeking to master advanced UI transitions and significantly enhance user experience with fluid, professional-grade navigation effects.

## Quick Navigation

**What do you want to learn?**

*   Learn to implement "one-shot" animations in **Cangjie** using `bindContentCover` and `TransitionEffect` → geometryTransition结合模态转场使用_1.md
*   Learn to implement shared element transitions in **ArkTS** using the `.geometryTransition()` modifier → geometryTransition结合模态转场使用_2.md

## Detailed Description

The following files provide specific implementation details for different languages within the HarmonyOS ecosystem:

#### geometryTransition结合模态转场使用_1.md geometryTransition结合模态转场使用_1.md
This tutorial demonstrates how to implement seamless "one-shot" animations between a list element and a modal page using geometryTransition combined with modal transitions in the Cangjie language. It covers the setup of data models, the main entry view with a list, and the construction of a modal personal page using bindContentCover. The core technique involves applying geometryTransition with matching IDs to shared elements like avatars across the source and destination views, while using TransitionEffect for non-shared elements. The code utilizes animateTo to control state changes and opacity for smooth transitions. This guide targets HarmonyOS developers using Cangjie who need to implement advanced UI transitions and shared element animations in their applications.

#### geometryTransition结合模态转场使用_2.md geometryTransition结合模态转场使用_2.md
This tutorial demonstrates how to implement a shared element transition using geometryTransition combined with a modal page transition in HarmonyOS ArkTS. The document presents a code example showing a list component where clicking an avatar triggers a modal. Key sections include the UI build function and the application of the .geometryTransition() modifier with a unique ID and animation parameters. The main takeaway is the specific configuration required to achieve a seamless "one-shot" animation effect between the main page and the modal. This guide targets developers familiar with ArkTS who need to create smooth, continuous navigation animations in their applications. Keywords: geometryTransition, modal transition, ArkTS, shared element, animation, HarmonyOS.