<!-- cj-doc kind="api-type" level="5" id="std.unittest.mock.class.continuation" parent="std.unittest.mock" -->
# Continuation<A>

[← std.unittest.mock](../../index.md)

`Continuation<A> where A <: ActionSelector`

此类提供了可继续定义桩签名的行为的 API 。

## 方法

| 签名 | 功能 |
|---|---|
| [`then(): A`](then.md) | 当链中的先前操作完成时，返回 ActionSelector 的子类对象。 |
