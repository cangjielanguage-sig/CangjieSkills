<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.enum.overflowstrategy.operator-eq" parent="std.math.numeric.enum.overflowstrategy" -->
# OverflowStrategy.==

[← OverflowStrategy](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: OverflowStrategy): Bool
```

判等。

## 契约

参数：

- that: OverflowStrategy - 被比较的溢出策略。

返回值：

- Bool - 溢出策略相同，返回 true；否则，返回 false。
