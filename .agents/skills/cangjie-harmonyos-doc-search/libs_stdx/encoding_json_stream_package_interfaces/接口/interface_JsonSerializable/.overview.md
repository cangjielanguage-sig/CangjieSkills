# interface_JsonSerializable

This directory contains API documentation for the Cangjie language's `JsonSerializable` interface, which enables the serialization of various data types into JSON data streams. It covers the base interface definition and numerous extensions for fundamental types, including integers, floating-point numbers, strings, and generic collections like `Array` and `HashMap`. The documentation details the `toJson` function implementation for each type, specifying constraints for generic parameters. This resource is suitable for Cangjie developers implementing JSON encoding logic who need to understand how specific types are converted and written to output streams using `JsonWriter`.

## Quick Navigation

**What do you want to learn?**

*   Understand the base interface definition and special types like `BigInt`, `Bool`, and `DateTime` → interface_JsonSerializable_5more.md
*   Serialize numeric and string types (`Decimal`, `Float`, `String`, `Int16`) → extend_Decimal_JsonSeriali_6more.md
*   Serialize standard integer types (`Int32`, `Int64`, `Int8`, `IntNative`, `UInt16`, `UInt32`) → extend_Int32_JsonSerializa_6more.md
*   Serialize unsigned integers and collections (`UInt64`, `Array`, `ArrayList`) → extend_UInt64_JsonSerializ_5more.md
*   Serialize optional values and string-keyed maps (`Option`, `HashMap`) → extendT_OptionT_JsonSerial_2more.md

## Detailed Description

### extendT_OptionT_JsonSerial_2more.md extendT_OptionT_JsonSerial_2more.md
This API documentation file details Cangjie language extensions that enable JSON serialization for generic collection types. The document covers two primary extensions: Option and HashMap<String, V>, both implementing the JsonSerializable interface. Key information includes the constraint that generic type parameters must also satisfy JsonSerializable. Each extension provides a toJson function that accepts a JsonWriter instance to output the serialized data. This reference guide is intended for Cangjie developers working with JSON encoding who need to serialize optional values and string-keyed maps.

### extend_Decimal_JsonSeriali_6more.md extend_Decimal_JsonSeriali_6more.md
This API reference document outlines the implementation of the JsonSerializable interface for six fundamental data types in the Cangjie language. It details extensions for Decimal, Float16, Float32, Float64, String, and Int16, enabling their serialization into JSON data streams. The primary takeaway is the definition of the public toJson function for each type, which accepts a JsonWriter instance to handle the output. This guide is intended for developers working with JSON encoding who need to understand how specific numeric and string types are converted and written to a stream.

### extend_Int32_JsonSerializa_6more.md extend_Int32_JsonSerializa_6more.md
This API reference document details the implementation of the JsonSerializable interface for six integer types in the Cangjie language. The main sections cover extensions for Int32, Int64, Int8, IntNative, UInt16, and UInt32, explicitly defining their inheritance from the JsonSerializable parent type. Key information includes the public toJson function for each type, which accepts a JsonWriter parameter to serialize the integer value into a JSON data stream. Intended for developers implementing JSON encoding, this documentation requires knowledge of the Cangjie syntax and stream-based serialization mechanisms.

### extend_UInt64_JsonSerializ_5more.md extend_UInt64_JsonSerializ_5more.md
This API reference document outlines the implementation of the JsonSerializable interface for specific unsigned integer types and generic collections in the Cangjie programming language. It details extensions for UInt64, UInt8, UIntNative, Array, and ArrayList, enabling them to be serialized into JSON data streams. Each section defines the public toJson function, which accepts a JsonWriter instance to write the serialized output. Notably, the collection extensions require that their generic type parameters also implement JsonSerializable. This documentation serves as a technical guide for developers working with JSON encoding and stream processing in Cangjie.

### interface_JsonSerializable_5more.md interface_JsonSerializable_5more.md
This API reference document defines the JsonSerializable interface in the Cangjie language, which enables types to be serialized into JSON data streams. It covers the base interface definition and its specific extensions for the BigInt, Bool, and DateTime types. Key information includes the toJson function, which accepts a JsonWriter instance to handle the output. The documentation highlights that the DateTime implementation supports configurable formatting through the dateTimeFormat property. This resource is intended for developers implementing JSON serialization logic within the Cangjie ecosystem.