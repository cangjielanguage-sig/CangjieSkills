# ListItem

This directory contains comprehensive documentation for the ListItem component within the Cangjie ArkUI framework. It covers the component's structure, initialization, basic type definitions, properties, and event handling. Developers can find references for configuring list items, managing selection states, and implementing interactive features like swipe actions and sticky headers. The content includes practical code examples demonstrating dynamic list rendering using ForEach. Suitable for software developers building HarmonyOS applications who need to implement detailed list interfaces and user interactions using the Cangjie language.

## Quick Navigation

What do you want to learn?

-   Understand the component structure and initialization methods? → ListItem_4more.md
-   Define basic types and styles (e.g., CARD mode)? → 基础类型定义.md
-   View a practical code example for implementation? → 示例代码.md
-   Handle mouse selection events? → 组件事件.md
-   Configure properties like swipe actions or sticky headers? → 组件属性.md

## Detailed Description

### ListItem_4more.md ListItem_4more.md
This document provides API reference documentation for the ListItem component in the Cangjie language, designed for displaying specific items within a List. It outlines the component's structure, specifying that it must be a child of List or ListItemGroup and can contain a single subcomponent. The text details two initialization methods, including a basic constructor and an advanced version accepting ListItemOptions starting from version 19. Important notes clarify child component creation timing differences when using LazyForEach compared to standard control flows. The document confirms full support for universal attributes and events, serving as a technical guide for developers building ArkUI interfaces.

### 基础类型定义.md 基础类型定义.md
This API documentation defines basic types for configuring ListItem components within the ArkUI framework using the Cangjie language. It covers the ListItemOptions class and the ListItemStyle enumeration. Key information includes the ListItemOptions constructor, which accepts a style parameter defaulting to NONE, and the style property that determines the visual appearance. The ListItemStyle enum supports NONE and CARD modes, where CARD provides default dimensions, padding, and interaction states like focus and hover, but requires a vertical layout. This reference guide targets developers building list interfaces in HarmonyOS applications.

### 示例代码.md 示例代码.md
This document provides a practical code example demonstrating the basic implementation of ListItem creation within the Cangjie programming language framework. The content focuses on the structure of an EntryView class, specifically detailing the usage of the List and ListItem components alongside the ForEach control flow to render dynamic content. Key takeaways include how to define a data array, iterate through it to generate list items, and apply styling properties such as width, height, and background color to text elements. This tutorial is intended for developers familiar with Cangjie syntax who are learning to build user interfaces using the UIKit framework.

### 组件事件.md 组件事件.md
This document serves as API reference material for the onSelect event handler within the ArkUI framework. It details the function signature, system capabilities, and specific parameters required to implement mouse selection state tracking for ListItem elements. Key takeaways include the function's availability starting from version 12 and its specific behavior of returning true when an element enters the mouse selection range and false when it exits. The target audience is software developers utilizing the ArkUI framework who need to handle interactive selection states in their applications.

### 组件属性.md 组件属性.md
This document serves as an API reference for configuring properties of the ListItem component within the Cangjie ArkUI framework. It details specific methods including editable, selectable, selected, sticky, and swipeAction. Each function entry provides the Cangjie syntax, functionality description, system capabilities, version requirements, and parameter definitions. Key features include enabling edit modes for deletion or movement, managing mouse selection states, implementing sticky headers, and configuring custom swipe actions with edge effects and offset callbacks. The target audience is software developers using the Cangjie language to build user interfaces, specifically those managing list interactions and behaviors.