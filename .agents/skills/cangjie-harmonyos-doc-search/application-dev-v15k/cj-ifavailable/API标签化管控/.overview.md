# API标签化管控

This directory focuses on API tagging and management within the Cangjie programming language ecosystem. It primarily covers the `@IfAvailable` macro expression, which allows developers to handle API versioning and System Capabilities (syscap) gracefully. Key topics include compile-time scope restrictions, runtime execution logic, and ensuring code compatibility across different SDK versions and device configurations. This documentation is suitable for Cangjie developers using DevEco Studio who need to implement fine-grained control over API availability and prevent errors when deploying to diverse hardware environments. By utilizing these guides, developers can ensure their applications function correctly across varying system capabilities and API levels.

## Quick Navigation

- **What do you want to learn?**
    - How to apply `@IfAvailable` in practical coding scenarios for API levels and syscap? → IfAvailable_使用示例.md IfAvailable_使用示例.md
    - What are the technical syntax, runtime semantics, and validation rules for the macro? → 概述_3more.md 概述_3more.md

## Detailed Description

### IfAvailable_使用示例.md IfAvailable_使用示例.md
This technical guide provides usage examples for the `@IfAvailable` attribute in the Cangjie programming language. It covers two primary scenarios: controlling API levels and managing System Capabilities (syscap). The document explains compile-time scope restrictions and runtime execution logic, demonstrating how to safely invoke APIs based on device support. It details the syntax for `level` and `syscap` parameters, highlighting how DevEco Studio validates these capabilities to prevent errors. Intended for Cangjie developers using DevEco Studio, it ensures code compatibility across varying SDK versions and device configurations.

### 概述_3more.md 概述_3more.md
This document serves as a technical guide for developers on using the `@IfAvailable` macro expression in the Cangjie language to manage API tagging and usage restrictions. It covers the macro's syntax, runtime semantics, and specific validation checks for `level` (API version) and `syscap` (system capabilities). Key information includes how the macro conditionally executes lambda expressions based on global labels, treating symbols in the first lambda as weak symbols. It enforces strict rules preventing calls to higher-level APIs in lower-level scopes and manages device capability intersections and unions. Targeted at Cangjie developers using DevEco Studio, this guide ensures fine-grained control over API compatibility across different system versions and devices.