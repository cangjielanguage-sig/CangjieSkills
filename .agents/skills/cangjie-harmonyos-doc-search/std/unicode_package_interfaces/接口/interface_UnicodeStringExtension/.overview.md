# interface_UnicodeStringExtension

This directory contains API reference documentation for the `UnicodeStringExtension` interface in the Cangjie programming language. It focuses on extending standard string types with robust Unicode-specific manipulation capabilities, including case conversion (toUpper, toLower, toTitle), whitespace detection (isBlank), and trimming operations (trim, trimStart, trimEnd). The documentation covers function signatures, locale-specific handling via `CasingOption`, and error handling for invalid UTF-8 encoding. It is suitable for Cangjie developers and software engineers requiring advanced text processing, internationalization features, and input sanitization in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the main interface definition and its methods → interface_UnicodeStringExt_6more.md
*   Learn about case conversion (`toUpper`) and trimming functions (`trim`) → func_toUpper_6more.md
*   Get specific details on removing leading whitespace (`trimStart`) → func_trimStart.md
*   See how the `String` type implements this extension → `extend_String_UnicodeStringExtension/`

## Detailed Description

### func_toUpper_6more.md func_toUpper_6more.md
This document provides API reference documentation for string manipulation functions in the Cangjie programming language, specifically focusing on case conversion and whitespace trimming. The content details the `toUpper` functions for Unicode character conversion and the `trim` family of functions for removing whitespace characters. Key takeaways include the support for language-specific casing via `CasingOption`, the definition of whitespace via `Rune.isWhiteSpace`, and the deprecation of `trimLeft` and `trimRight` in favor of `trimStart` and `trimEnd`. All functions throw `IllegalArgumentException` for invalid UTF-8 encoding. This reference guide targets Cangjie developers seeking to implement string formatting and validation logic.

### func_trimStart.md func_trimStart.md
This document serves as API reference documentation for the Cangjie programming language, specifically detailing the trimStart function used to remove leading whitespace from strings. The content covers the function signature, its specific functionality regarding whitespace removal based on the isWhiteSpace definition, the return type, and potential exceptions. Key takeaways include that trimStart returns a new String without leading characters and throws an IllegalArgumentException if the string contains invalid UTF-8 encoding. This reference is intended for Cangjie developers who need to manipulate string data, specifically sanitizing inputs or formatting text by stripping leading spaces.

### interface_UnicodeStringExt_6more.md interface_UnicodeStringExt_6more.md
This document serves as API reference documentation for the UnicodeStringExtension interface in the Cangjie programming language, designed to extend standard string types with Unicode-specific manipulation capabilities. The content details function signatures and behaviors for whitespace detection via isBlank, case conversion methods like toLower and toTitle, and various trimming operations. Key information includes the specific Unicode code points defined as whitespace, the support for locale-specific transformations through CasingOption parameters, and exception handling for invalid UTF-8 encoding. This reference is intended for software developers requiring robust text processing and internationalization features in their applications.

### extend_String_UnicodeStringExtension/
This directory contains API reference documentation for extending the Cangjie `String` type to implement the `UnicodeStringExtension` interface. It comprehensively covers advanced character set operations, including Unicode-compliant casing conversions (toLower, toTitle, toUpper), whitespace validation (isBlank), and various string trimming methods (trim, trimStart, trimEnd). The documents detail function signatures, locale-specific handling via `CasingOption`, and error handling for invalid UTF-8 encoding. Additionally, it highlights deprecated functions and their modern replacements. This resource is suitable for Cangjie developers seeking to manipulate text casing, sanitize inputs, and ensure proper Unicode compliance in their applications.