# 使用HiLog打印日志

This directory focuses on the HiLog logging system within the Cangjie development environment, providing essential resources for debugging and monitoring applications. It comprehensively covers the theoretical aspects of the logging API, including the five distinct log levels, parameter constraints like the 4096-byte limit, and best practices for consistency checks. Additionally, it offers practical implementation guidance through hands-on code examples involving UI interactions. The content is designed to help developers analyze application execution and logic effectively. It is suitable for Cangjie and ArkUI developers who need to integrate logging into their projects, understand system constraints, and utilize DevEco Studio for log filtering and analysis to ensure code stability. The directory includes both a technical API guide and a practical development example.

## Quick Navigation

What do you want to learn?

*   If you want to understand the technical specifications of HiLog, including API interfaces, the five log levels (DEBUG, INFO, WARN, ERROR, FATAL), and parameter constraints like the 4096-byte limit, refer to the technical guide → 使用HiLog打印日志_3more.md
*   If you want to learn how to implement logging in a real Cangjie project with UI interaction examples, button click events, and DevEco Studio log filtering, check out the development example → 开发示例.md

## Detailed Description

### 使用HiLog打印日志_3more.md 使用HiLog打印日志_3more.md
This document serves as a technical guide for developers on using the HiLog system to output logs for analyzing application execution and logic. It covers the main API interfaces, detailing five log levels—DEBUG, INFO, WARN, ERROR, and FATAL—and their specific functions. Key sections include parameter analysis, explaining the usage of domain, tag, format, and args, alongside constraints like the 4096-byte log limit. The document emphasizes consistency between isLoggable checks and print methods. It targets application developers seeking to debug and monitor code behavior within the development environment.

### 开发示例.md 开发示例.md
This tutorial demonstrates how to implement a button click event and print logs in a Cangjie project. It guides users through creating a "Cangjie Empty Ability" project, modifying the `index.cj` file to add a button with an `onClick` handler, and utilizing the `Hilog` API for logging. Key steps include running the application on a real device and filtering logs in DevEco Studio using specific tags and levels. The document provides code examples showing `Hilog.info` and `Hilog.error` usage, illustrating how to track application behavior. Intended for Cangjie developers, this example serves as an introduction to basic UI interaction and debugging techniques within the ArkUI framework.