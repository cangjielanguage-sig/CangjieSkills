# 函数

This directory contains API reference documentation for mathematical utility functions within the Cangjie standard library's numeric package. It covers a range of operations for arbitrary-precision integers (`BigInt`) and decimal numbers (`Decimal`), including absolute value calculation, greatest common divisor (GCD), least common multiple (LCM), rounding, square roots, and bit manipulation tasks like counting trailing zeros. These documents provide function signatures, parameter descriptions, return types, and verified code examples. This resource is suitable for Cangjie developers implementing complex mathematical logic or high-precision arithmetic in their applications.

## Quick Navigation

**What do you want to learn?**

*   Perform basic mathematical operations (absolute value, GCD, bit counting) → func_absBigInt_5more.md
*   Calculate advanced metrics (LCM, rounding, square roots) → func_lcmBigInt_BigInt_4more.md
*   Analyze binary data (count trailing zeros) → func_trailingZerosBigInt.md

## Detailed Description

### func_absBigInt_5more.md func_absBigInt_5more.md
This document serves as API reference documentation for mathematical utility functions within the Cangjie standard library's numeric package. It details the `abs` function for both `BigInt` and `Decimal` types, the `countOnes` function for `BigInt` (marking `countOne` as deprecated), and the `gcd` function for calculating the greatest common divisor of two `BigInt` values. Each entry includes function signatures, parameter descriptions, return types, and verified code examples demonstrating usage. The `gcd` function specifically notes it always returns a non-negative result. This reference guide targets Cangjie developers implementing mathematical operations requiring arbitrary-precision integers or decimal arithmetic.

### func_lcmBigInt_BigInt_4more.md func_lcmBigInt_BigInt_4more.md
This document serves as API reference documentation for mathematical functions in the Cangjie standard library's numeric package. It covers four primary functions: `lcm` for calculating the least common multiple of BigInts, `round` for rounding Decimals using specific modes, and overloaded `sqrt` functions for computing square roots of both BigInt and Decimal types. Key information includes detailed parameter descriptions, return value specifications, and exception handling for errors like overflow or illegal arguments. The document provides code examples for verification. It is intended for Cangjie developers seeking precise definitions of numeric operations, requiring familiarity with BigInt and Decimal data types.

### func_trailingZerosBigInt.md func_trailingZerosBigInt.md
This document serves as API reference documentation for the trailingZeros function within the Cangjie standard library's numeric package. It details the function's signature, functionality, parameters, and return values, specifically focusing on calculating the number of consecutive zero bits from the least significant bit of a BigInt. The document includes a practical code example demonstrating the function's usage and output. Key information includes the input type BigInt and the return type Int64, with a specific example showing the result for a hexadecimal input. This reference is intended for software developers working with arbitrary-precision integers who need low-level bit manipulation capabilities. Keywords: Cangjie, BigInt, trailing zeros, bit manipulation, API reference.