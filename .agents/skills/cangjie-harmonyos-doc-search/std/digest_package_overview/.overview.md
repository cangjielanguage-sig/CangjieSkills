# digest_package_overview

This directory acts as the central hub for the API overview of the `std.crypto.digest` package, providing developers with the necessary documentation to utilize common cryptographic hash algorithm interfaces effectively. The content encompasses a thorough feature introduction that explicitly lists supported algorithms such as MD5, SHA, HMAC, and SM3, giving a clear picture of the cryptographic capabilities available. Furthermore, it presents a well-organized API list that categorizes functions and interfaces to streamline the development process. Among the key components discussed are the generic `digest` function, which is designed to support a variety of input types including `Array<Byte>`, `String`, and `InputStream`, and the `Digest` interface, which sets the standard for algorithm implementation. This reference guide is highly suitable for developers focused on implementing data hashing who need to understand the nuances of generic types and input streams.

## Quick Navigation

**What do you want to learn about the cryptographic package?**

*   If you want to see the list of supported algorithms like MD5, SHA, and SM3 → digest_package_overview.md
*   If you need to understand the generic `digest` function and its input types → digest_package_overview.md
*   If you are looking for the `Digest` interface definition and standards → digest_package_overview.md
*   If you want to browse the categorized API list of functions and interfaces → digest_package_overview.md

## Detailed Description

The following section provides a comprehensive breakdown of the documentation file included in this directory. It outlines the scope of the cryptographic package, detailing the interfaces and functions available to developers.

### digest_package_overview.md digest_package_overview.md

This document serves as an API overview for the std.crypto.digest package, detailing common cryptographic hash algorithm interfaces. It covers a feature introduction listing supported algorithms like MD5, SHA, HMAC, and SM3, followed by an API list categorizing functions and interfaces. Key components include the generic `digest` function supporting `Array<Byte>`, `String`, and `InputStream` inputs, alongside the `Digest` interface defining the standard for algorithms. Targeted at developers implementing data hashing, this reference guide requires understanding of generic types and input streams.