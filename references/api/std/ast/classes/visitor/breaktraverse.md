<!-- cj-doc kind="api-member" level="6" id="std.ast.class.visitor.breaktraverse" parent="std.ast.class.visitor" -->
# Visitor.breakTraverse

[← Visitor](index.md)

## 签名

```cangjie role=signature
public func breakTraverse(): Unit
```

用于重写 `visit` 函数中，通过调用该函数来终止继续遍历子节点的行为。
