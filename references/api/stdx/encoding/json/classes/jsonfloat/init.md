<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonfloat.init" parent="stdx.encoding.json.class.jsonfloat" -->
# JsonFloat.init

[← JsonFloat](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init(Float64)

### 签名

```cangjie role=signature
public init(fv: Float64)
```

将指定的 Float64 类型实例封装成 JsonFloat 实例。

### 契约

参数：

- fv: Float64 - Float64 类型。

## init(Int64)

### 签名

```cangjie role=signature
public init(v: Int64)
```

将指定的 Int64 类型实例封装成 JsonFloat 实例。

### 契约

参数：

- v: Int64 - Int64 类型。
