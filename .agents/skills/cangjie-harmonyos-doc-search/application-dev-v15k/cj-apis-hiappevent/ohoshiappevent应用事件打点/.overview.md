# ohoshiappevent应用事件打点

This directory contains API reference documentation for the HiAppEvent module in the Cangjie programming language, designed for HarmonyOS application event tracking and analysis. It covers essential components for logging, observing, and processing application events, including enums, structs for configuration and filtering, and classes for data handling. Key topics include event definitions, subscription management, data processors, and storage configuration. This documentation is suitable for Cangjie developers and system integrators looking to implement robust performance monitoring, user behavior analysis, and fault diagnosis within the SystemCapability.HiviewDFX.HiAppEvent framework.

## Quick Navigation

**What do you want to learn?**

*   Understand event definitions and data types → enum_Domain_4more.md, enum_ParamType_2more.md
*   Configure event logging and storage settings → struct_ConfigOption.md, struct_AppEventReportConfig.md
*   Create and manage event data structures → struct_AppEventInfo.md, struct_Parameters_2more.md
*   Observe and subscribe to event streams → [struct_Watcher/], struct_AppEventFilter_2more.md, class_AppEventPackageHolder.md
*   Process and report event data → [class_Processor/], ohoshiappevent应用事件打点_4more.md
*   Access core system capabilities and management → [class_HiAppEvent/]

## Detailed Description

### class_AppEventPackageHolder.md class_AppEventPackageHolder.md
This document provides API reference documentation for the Cangjie class `AppEventPackageHolder`, designed to manage and process subscription event data within the HiAppEvent system. It details the class constructor which links instances to specific watchers, the `setSize` method for configuring data retrieval thresholds, and the `takeNext` function for fetching event packages. Key takeaways include the default size limit of 512KB and the handling of data exhaustion via Option return types. This guide targets developers implementing application event monitoring and analysis, requiring knowledge of Cangjie syntax and the PerformanceAnalysisKit.

### enum_Domain_4more.md enum_Domain_4more.md
This API reference document defines enumeration constants for the HiAppEvent module in the Cangjie programming language. It details four primary enums: `Domain`, `Event`, `EventType`, and `Param`. `Domain` specifies event areas like OS, while `Event` lists predefined occurrences such as `APP_CRASH`, `USER_LOGIN`, and `SCROLL_JANK`. `EventType` categorizes events into `FAULT`, `STATISTIC`, `SECURITY`, or `BEHAVIOR`, and `Param` provides keys like `USER_ID`. Each enum features a read-only value property for retrieving its string or numeric representation.

### enum_ParamType_2more.md enum_ParamType_2more.md
This API reference document defines the Cangjie enumerations `ParamType` and `ValueType` used within the HiAppEvent system capability. It details the specific variants for each enum, such as `FLOAT`, `STRING`, `BOOL`, and various array types like `ARRSTRING` and `ARRAYI32`, along with their respective start versions. Key takeaways include the distinction between `ParamType` (version 20) for event parameters and `ValueType` (version 12+) for general data fields, plus the read-only value property for string conversion.

### ohoshiappevent应用事件打点_4more.md ohoshiappevent应用事件打点_4more.md
This document serves as API reference documentation for the `ohos.hiappevent` module in the Cangjie programming language, specifically detailing the `AppEventPackage` class used for handling subscribed application events. It covers module import instructions, usage guidelines for Cangjie template projects, and a comprehensive breakdown of the `AppEventPackage` class structure. Key information includes definitions for read-only properties such as `packageId`, `row`, `size`, and `data`, alongside the class constructor parameters and system capability requirements.

### struct_AppEventFilter_2more.md struct_AppEventFilter_2more.md
This document provides API reference documentation for the Cangjie language, defining structures for filtering and grouping application events. It covers the `AppEventFilter` struct, which specifies subscription parameters such as domain, event types, and names, and the `AppEventGroup` struct, which holds collections of returned event objects. Key information includes the read-only properties of these structs and their respective initializers, with `AppEventFilter` supporting optional named parameters. The document specifies the system capability `SystemCapability.HiviewDFX.HiAppEvent` and version 12.

### struct_AppEventInfo.md struct_AppEventInfo.md
This API reference document defines the Cangjie struct `AppEventInfo`, which encapsulates parameters for application event information within the HiAppEvent system. The documentation details the struct's constructor and four read-only properties: `domain`, `name`, `event`, and `params`. Key information includes strict validation rules for naming conventions and data types, such as character length limits for domain and name, and specific constraints on parameter values supporting `String`, `Int32`, `Float64`, `Bool`, and `Array` types.

### struct_AppEventReportConfig.md struct_AppEventReportConfig.md
This API reference document defines the Cangjie struct `AppEventReportConfig`, which serves as a configuration object for data processors to report events. The documentation details the struct's three read-only properties: `domain`, `name`, and `isRealTime`, specifying their data types, validation rules (such as character limits and naming conventions), and functionality. It also explains the constructor method for initializing instances with default parameters. Intended for Cangjie developers working with the `SystemCapability.HiviewDFX.HiAppEvent` capability.

### struct_ConfigOption.md struct_ConfigOption.md
This API documentation defines the Cangjie struct `ConfigOption`, which provides configuration options for application event tracking within the HiAppEvent system. The document details the struct's read-only properties, including the `disable` boolean switch for toggling functionality and the `maxStorage` string for setting directory quotas. Key information includes the default values (`false` for disable, "10M" for maxStorage) and the specific formatting rules for storage units, which trigger automatic cleanup of old data files when limits are exceeded.

### struct_Parameters_2more.md struct_Parameters_2more.md
This API reference documentation details two Cangjie language structures for the HiAppEvent system. The document covers the `Parameters` struct, which defines event key-value pairs with specific validation rules for naming and length, and the `TriggerCondition` struct, which configures thresholds for triggering subscription callbacks. Key information includes strict constraints on parameter names and values, as well as the logic where callbacks trigger upon meeting any specified condition for event count, size, or timeout.

### class_HiAppEvent/
This directory contains API reference documentation for the `HiAppEvent` class in the Cangjie programming language, part of the Performance Analysis Kit for HarmonyOS. It covers application event tracking capabilities, including data processing, configuration, user management, and real-time monitoring. Key functionalities include adding processors, managing event parameters, and configuring storage quotas. This documentation is suitable for Cangjie developers seeking to implement robust analytics, performance monitoring, and event-driven logging in their applications, with detailed guidance on error handling and system capabilities.

### class_Processor/
This directory contains comprehensive documentation for the Cangjie `Processor` class, a fundamental data handler object within the HiAppEvent system capability introduced in API version 12. It provides essential resources for developers working with event reporting, covering the complete lifecycle of the Processor from initialization to configuration. Key topics include defining read-only properties such as `name`, `appId`, and `routeInfo`, as well as setting up complex reporting triggers like `onStartReport`, `onBackgroundReport`, `periodReport`, and `batchReport`.

### struct_Watcher/
This directory provides comprehensive API reference documentation for the `Watcher` struct within the HiAppEvent system, specifically tailored for developers using the Cangjie language in HarmonyOS. It covers the essential components required to implement application event monitoring, including the initialization constructor, parameter definitions, and callback mechanisms. Key topics include setting up unique names, defining trigger conditions, and configuring event filters. The documentation also clarifies the functional differences between batch processing callbacks and real-time callbacks.