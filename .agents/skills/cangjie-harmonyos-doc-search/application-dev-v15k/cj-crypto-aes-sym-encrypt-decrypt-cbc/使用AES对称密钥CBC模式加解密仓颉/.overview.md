# 使用AES对称密钥CBC模式加解密仓颉

This directory serves as a comprehensive technical resource for implementing AES symmetric encryption and decryption using the CBC mode within the Cangjie programming environment. It provides a deep dive into the CryptoArchitectureKit, outlining the necessary steps for secure data processing, such as generating 128-bit SymKeys, creating Cipher instances with AES128/CBC/PKCS7, and managing initialization vectors. The documentation covers both encryption and decryption workflows, highlighting specific API calls and operational nuances like skipping the update step for short data. This collection is ideal for Cangjie developers seeking to integrate advanced cryptographic security and error handling into their applications, ensuring data integrity and confidentiality through standardized protocols. By following these guides, developers can effectively secure sensitive data against unauthorized access while maintaining high performance in their Cangjie applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the theoretical steps and API configuration for AES/CBC → 使用AES对称密钥CBC模式加解密仓颉_3more.md
*   Examine a practical synchronous code example with full implementation → 示例.md

## Detailed Description

### 使用AES对称密钥CBC模式加解密仓颉_3more.md 使用AES对称密钥CBC模式加解密仓颉_3more.md
This technical guide provides step-by-step instructions for implementing AES symmetric encryption and decryption using CBC mode within the Cangjie programming environment. The document is divided into two primary workflows: Encryption and Decryption, detailing the specific API calls required for each process. Key steps include generating a 128-bit SymKey, creating a Cipher instance configured with 'AES128|CBC|PKCS7', initializing it with CryptoMode and IvParamsSpec, and executing the operation via doFinal. It emphasizes that for short data, the update step can be skipped. This guide targets Cangjie developers utilizing the CryptoArchitectureKit who need to integrate secure data processing capabilities into their applications.

### 示例.md 示例.md
This document provides a Cangjie code example demonstrating synchronous AES encryption and decryption operations using the CryptoArchitectureKit. It covers functions for generating random numbers, creating initialization vector parameters, converting raw data to symmetric keys, and performing encryption and decryption with AES128/CBC/PKCS7. The example illustrates the complete workflow of initializing a cipher, processing data via doFinal, and verifying the result by comparing original and decrypted text. It targets developers implementing cryptographic security in Cangjie applications who need a practical reference for symmetric key usage and error handling within a try-catch block.