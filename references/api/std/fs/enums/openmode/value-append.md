<!-- cj-doc kind="api-member" level="6" id="std.fs.enum.openmode.value-append" parent="std.fs.enum.openmode" -->
# OpenMode.Append

[← OpenMode](index.md)

## 签名

```cangjie role=signature
Append
```

构造一个 OpenMode 实例，指定以追加写入的方式打开文件。

## 契约

功能：构造一个 OpenMode 实例，指定以追加写入的方式打开文件。如果文件不存在，则将创建文件。
