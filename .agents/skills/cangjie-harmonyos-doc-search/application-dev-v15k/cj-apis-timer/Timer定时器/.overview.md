# Timer定时器

This directory provides essential API reference documentation for the Timer module in the Cangjie programming language, designed for use with the ArkUI framework. It focuses on the mechanisms required to schedule and manage time-based tasks, including one-time executions and repetitive operations. The documentation covers critical functions such as `once` for delayed execution, `repeatDuration` for periodic intervals, and `cancel` for stopping tasks. It explains parameters like delay, interval, and CatchupStyle, offering developers the tools to implement robust timing logic. This content is specifically suitable for Cangjie developers working on HarmonyOS applications who need to handle asynchronous code execution, background operations, or precise event scheduling within their software architecture.

## Quick Navigation

**What do you want to do?**

*   Schedule a task to run once after a specific delay → Timer定时器_3more.md
*   Schedule a task to run repeatedly at set intervals → static_func_repeatDuration_Duration_-Unit_4929eb57.md
*   Stop or cancel a currently scheduled timer → func_cancel_3more.md

## Detailed Description

### Timer定时器_3more.md Timer定时器_3more.md
This document serves as API reference documentation for the Timer module in the Cangjie programming language, detailing how to schedule one-time task execution. It covers the module import statement and specifically documents the static function once, which accepts a time delay and a task function as parameters. The once function returns a Timer object instance for control. Key parameters include delay, which defines the execution interval, and task, the function to execute. If the delay is zero or less, the task runs immediately. Targeted at developers using the Cangjie language and ArkUI framework who need to implement delayed or scheduled execution of code logic within their applications.

### func_cancel_3more.md func_cancel_3more.md
This API documentation details the cancel() function for the Cangjie Timer class, explaining how to stop scheduled tasks. It covers the function signature, behavioral notes, code examples for one-shot and repeating timers, UI integration, and execution constraints like delay limits and asynchronous behavior. Key information includes that the function is non-blocking and idempotent; it prevents future execution but does not interrupt active tasks. It also defines valid duration ranges between Duration.Min and Duration.Max. Targeted at Cangjie developers using the ArkUI framework for HarmonyOS application development.

### static_func_repeatDuration_Duration_-Unit_4929eb57.md static_func_repeatDuration_Duration_-Unit_4929eb57.md
This document serves as API reference documentation for a static function designed to schedule repetitive timed tasks in the Cangjie language. It details the function signature, system capabilities, and parameter definitions, including delay, interval, task, and catchup style. Key information highlights that the function accepts Duration objects to control timing and a CatchupStyle enum to manage execution delays, returning a Timer object for task control. The delay parameter allows immediate execution if set to zero or less, while the interval must be positive. This reference is intended for Cangjie developers utilizing the ArkUI framework to implement periodic background operations or scheduling logic.