<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjprof.3-堆内存分析.3-5-堆分析选项" parent="tools.cjprof.3-堆内存分析" -->
# 3.5 堆分析选项

[← 3. 堆内存分析](index.md)

速查`-d` / `--dump <pid>`：导出指定进程的堆内存；`-i` / `--input <file>`：分析的堆数据文件（默认 `cjprof.data`）；`-o` / `--output <file>`：导出的堆数据文件（默认 `cjprof.data`）；另含更多表项。

| 选项 | 说明 |
|------|------|
| `-d` / `--dump <pid>` | 导出指定进程的堆内存 |
| `-i` / `--input <file>` | 分析的堆数据文件（默认 `cjprof.data`） |
| `-o` / `--output <file>` | 导出的堆数据文件（默认 `cjprof.data`） |
| `--show-reference[=<names>]` | 展示对象引用关系（`;` 分隔多个对象名） |
| `--incoming-reference` | 展示被引用关系（需配合 `--show-reference`） |
| `-D` / `--depth <n>` | 引用关系最大展示深度（默认 10） |
| `-t` / `--show-thread` | 展示仓颉线程栈及栈中引用的对象 |
| `-V` / `--verbose` | 打印解析日志 |

---
