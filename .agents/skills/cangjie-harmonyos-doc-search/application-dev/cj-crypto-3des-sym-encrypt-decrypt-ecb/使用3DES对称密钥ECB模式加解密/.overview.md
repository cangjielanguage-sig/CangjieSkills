# 使用3DES对称密钥ECB模式加解密

This directory provides comprehensive resources for implementing encryption and decryption using the 3DES symmetric key algorithm in ECB mode. It covers the complete workflow within the Cangjie CryptoArchitectureKit, including key generation, cipher initialization, data processing, and finalization. Key technical aspects include the use of a 192-bit key length, PKCS7 padding, and specific API functions like `createCipher` and `doFinal`. The content is designed for Cangjie developers and security engineers who need to integrate robust cryptographic data handling into their applications, offering both theoretical guidance and practical code references.

## Quick Navigation

- **What do you want to do?**
    - Learn the step-by-step procedure for 3DES encryption and decryption → 使用3DES对称密钥ECB模式加解密_3more.md
    - View a synchronous code example for implementation → 示例.md

## Detailed Description

### 使用3DES对称密钥ECB模式加解密_3more.md 使用3DES对称密钥ECB模式加解密_3more.md
This document serves as a comprehensive step-by-step tutorial for performing encryption and decryption using the 3DES symmetric key algorithm in ECB mode. It meticulously details the specific procedures required for both operations, covering the entire lifecycle from key generation and cipher instance creation to initialization, data updating, and finalization. Key technical information provided includes the mandatory use of a 192-bit key length and PKCS7 padding. The guide highlights specific API functions essential for the process, such as `createCipher`, `initialize`, `update`, and `doFinal`. Furthermore, it notes that ECB mode is unique as it requires no encryption parameters and provides crucial advice on handling data segmentation for larger inputs. This technical guide is specifically targeted at developers working with the CryptoArchitectureKit who need to implement secure data handling using 3DES.

### 示例.md 示例.md
This document offers a synchronous code example that demonstrates symmetric encryption and decryption operations using the Cangjie CryptoArchitectureKit. The main topics covered include the implementation of `encryptMessage` and `decryptMessage` functions, symmetric key generation via `genSymKeyByData`, and a comprehensive `test` function to validate the workflow. Key takeaways for the reader involve understanding how to utilize the 3DES192 algorithm with ECB mode and PKCS7 padding, initializing ciphers correctly, and verifying data integrity through logging. Intended primarily for Cangjie developers, this example serves as a practical reference for implementing cryptographic workflows and handling DataBlob conversions effectively in real-world scenarios.