# class_MatchExpr

This directory provides essential API reference documentation for the `MatchExpr` class and its associated functions within the Cangjie Abstract Syntax Tree (AST) package. It focuses on the structural definition and manipulation of pattern matching expressions, a critical component for language parsing and compilation. The content covers class initialization, mutable properties like keywords and delimiters, and methods for node conversion and tree traversal. Key topics include AST nodes, match cases, selectors, and visitor patterns. Additionally, it details error handling mechanisms such as ASTException for invalid token assignments. This documentation is specifically suitable for developers working on compiler construction, language processing tools, or anyone needing to programmatically analyze, transform, or generate Cangjie syntax trees. It provides the necessary API references to handle both selector-based and selector-less match expressions effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure, initialization, and properties of pattern matching expressions → class_MatchExpr_10more.md
*   Learn about converting nodes to tokens or traversing the AST tree using visitors → func_toTokens_2more.md

## Detailed Description

### class_MatchExpr_10more.md class_MatchExpr_10more.md
This document serves as API reference documentation for the MatchExpr class in the Cangjie AST package, defining the structure for pattern matching expressions. It covers the class initialization methods and various mutable properties such as keyword, lBrace, rBrace, lParen, rParen, matchCases, and selector. Key information includes support for both selector-based and selector-less match expressions, where properties manage specific tokens and a list of MatchCase nodes. The document specifies that invalid token assignments result in an ASTException. This technical reference is designed for developers working on compiler construction or language processing tools who need to manipulate AST nodes.

### func_toTokens_2more.md func_toTokens_2more.md
This document provides API reference documentation for two key functions within an Abstract Syntax Tree (AST) package in the Cangjie language. It covers the `toTokens()` method, which converts a syntax tree node into a `Tokens` type, and the `traverse(Visitor)` method, designed to iterate over the current node and its children. Key information includes the return types, parameters, and the ability to prematurely halt traversal by overriding the `visit` function and calling `breakTraverse`. This guide is intended for developers working with AST manipulation who require node conversion or custom tree traversal capabilities.