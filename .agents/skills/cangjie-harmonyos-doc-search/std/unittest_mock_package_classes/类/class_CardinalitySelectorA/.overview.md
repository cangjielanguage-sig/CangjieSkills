# class_CardinalitySelectorA

This directory provides comprehensive API reference documentation for the `CardinalitySelector` class and related functions within a Cangjie unit testing framework. It is designed to assist developers in defining and validating the execution frequency of stub behaviors, ensuring precise control over mock object interactions. The content covers the class definition and specific methods such as `anyTimes`, `atLeastOnce`, `atLeastTimes`, `once`, and `times`. It details validation logic where exceeding call limits triggers an immediate `ExpectationFailedException`, whereas insufficient calls result in exceptions after test completion. Additionally, it explains how methods like `once` and `times` return a `Continuation` object for further configuration. This documentation is suitable for developers writing unit tests who need to enforce strict verification rules on mock interactions.

## Quick Navigation

What do you want to learn?

*   Understand the CardinalitySelector class and its methods (anyTimes, atLeastOnce, once) → class_CardinalitySelectorA_6more.md
*   Learn about the `times` function for defining execution frequency constraints → func_timesInt64_Int64.md

## Detailed Description

### class_CardinalitySelectorA_6more.md class_CardinalitySelectorA_6more.md

This API reference document details the CardinalitySelector class, which provides methods to define and validate the execution frequency of stub behaviors in a Cangjie unit testing framework. The documentation covers the class definition and specific functions including anyTimes, atLeastOnce, atLeastTimes, once, and times. Key takeaways include the validation logic where exceeding call limits triggers immediate ExpectationFailedException, while insufficient calls throw exceptions after test completion. Methods like once and times return a Continuation object for further configuration. This is intended for developers writing unit tests who need precise control over mock object interactions and verification.

### func_timesInt64_Int64.md func_timesInt64_Int64.md

This document serves as API reference documentation for the `times` function, which defines execution frequency constraints for stub behaviors within a unit testing framework. The content details the function signature, specifying two Int64 parameters, `min!` and `max!`, to establish the expected range of executions. Key takeaways include the function's role in verifying that stub calls occur within the specified limits, throwing an `ExpectationFailedException` upon verification failure or an `IllegalArgumentException` for negative inputs. This technical reference is designed for developers writing unit tests who require precise control over mock object interaction verification.