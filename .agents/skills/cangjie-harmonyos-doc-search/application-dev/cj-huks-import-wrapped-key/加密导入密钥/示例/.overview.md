# 示例

This directory contains a comprehensive set of code examples demonstrating cryptographic operations and key management within the HarmonyOS ecosystem, specifically utilizing the Universal Keystore (HUKS) framework and the Cangjie programming language. It covers essential security workflows including AES encryption, Elliptic Curve Diffie-Hellman (ECDH) key agreement, key wrapping, and secure key import between devices. The examples range from basic parameter configuration and initialization vector (IV) setup to complex session-based encryption and lifecycle management. These resources are designed for developers seeking to implement secure data handling, key storage, and cryptographic protocols in their HarmonyOS applications.

## Quick Navigation

**What do you want to learn or do?**

- **Understand basic setup and utility functions?**
  - Cangjie code setup and helper functions → 示例_1.md
  - Configuring Initialization Vectors (IV) → 示例_4.md
- **Configure cryptographic parameters for HUKS?**
  - ECC Curve25519 and AES-256 GCM setup → 示例_2.md
  - ECDH and wrapped AES-192 key parameters → 示例_3.md
- **Manage key lifecycle and sessions?**
  - Key generation, import, deletion, and session flows → 示例_5.md
  - Key agreement sessions and shared secrets → 示例_6.md
- **Implement secure key exchange between devices?**
  - Building wrapped data for secure import → 示例_7.md
  - Full workflow: wrapping, transmission, and cleanup → 示例_8.md

## Detailed Description

### 示例_1.md 示例_1.md
This Cangjie code example demonstrates the setup and utility functions required for cryptographic key management, specifically focusing on AES encryption and ECDH key agreement workflows. The content covers variable initialization for cryptographic parameters like IV, AAD, and Nonce, alongside key aliases and data buffers. It includes helper functions for logging, array slicing, type conversion, and data packing. Key information involves AES-192 and AES-256 encryption, Key Encryption Keys (KEK), and Elliptic Curve Diffie-Hellman (ECDH) for deriving shared secrets. This document serves as a technical reference for developers implementing secure key storage and exchange mechanisms using the Universal Keystore Kit.

### 示例_2.md 示例_2.md
The document serves as a code snippet demonstrating the configuration of cryptographic parameters for the HarmonyOS Universal KeyStore (HUKS) API. It details the setup of HuksOptions for generating ECC Curve25519 keys for unwrapping and ECDH agreement, as well as importing AES-256 keys for encryption using GCM mode. Key takeaways include the specific tag-value pair structures required for defining algorithm types, key sizes, and padding. This technical reference is intended for HarmonyOS developers implementing secure key management and cryptographic workflows, focusing on key wrapping, agreement, and encryption processes.

### 示例_3.md 示例_3.md
This document provides code examples for configuring cryptographic parameters using the HarmonyOS Universal Keystore Service (HUKS) API. It details the construction of HuksOptions objects for ECDH key agreement, AES-256-GCM encryption, and importing wrapped AES-192 keys. Key information includes the specific assignment of tags for algorithms, key sizes, padding, block modes, and unwrap suites. The content serves as a technical reference for HarmonyOS developers implementing secure key management, encryption workflows, and key agreement protocols in their applications.

### 示例_4.md 示例_4.md
This document serves as a code example demonstrating how to configure cryptographic parameters, specifically setting an Initialization Vector (IV) within the HarmonyOS Universal KeyStore (HUKS) framework. The content focuses on parameter construction, highlighting the usage of HuksParam with HUKS_TAG_IV and HuksParamValue.BytesValue. It also illustrates the initialization of input data. Key takeaways include the syntax for converting an IV array to bytes and the structure of the parameter list required for HUKS operations. The example shows inData being initialized as empty, suggesting a setup phase or specific operation mode. This is intended for developers working on HarmonyOS application security who need to implement encryption or decryption using the HUKS API.

### 示例_5.md 示例_5.md
This document provides code examples for key management and cryptographic operations using the HUKS API. It covers key lifecycle functions such as generation, import, deletion, and wrapped key import, alongside session-based cipher operations. Key functions include publicInitFunc, publicUpdateSessionfunc for chunked data processing, and publicFinishSession, which collectively handle multi-stage encryption or decryption flows with error handling. Intended for developers implementing secure data handling, this reference code illustrates how to orchestrate complex cryptographic sessions and manage key storage effectively.

### 示例_6.md 示例_6.md
This document provides a code example demonstrating cryptographic operations using the HUKS framework, specifically focusing on key agreement and key encryption workflows. The content covers functions for managing key agreement sessions, importing keys and shared secrets, generating and exporting public keys, and encrypting keys using AES. Key takeaways include the implementation of the three-stage session process for key agreement, the handling of authentication tags during encryption, and the integration of shared secrets for subsequent key wrapping. This technical reference is intended for HarmonyOS developers implementing secure key management and encryption protocols, requiring prior knowledge of cryptographic concepts and the HUKS API.

### 示例_7.md 示例_7.md
This document provides a code example demonstrating the secure import of a wrapped key between two devices using cryptographic key agreement and encryption. It details the BuildWrappedDataAndImportWrappedKey function for serializing cryptographic data and the ImportWrappedKey function simulating the protocol. Key steps include generating wrapping and caller key pairs, performing ECDH shared secret agreement, and encrypting the target key and KEK. The example illustrates how Device A prepares the payload for Device B. This tutorial targets security developers implementing key import features, specifically those using HUKS or similar keystore services, requiring knowledge of asymmetric cryptography and data serialization.

### 示例_8.md 示例_8.md
This code example demonstrates the secure process of wrapping, transmitting, and importing an AES-192 key between Device A and Device B, followed by cleanup. The main topics cover key encapsulation, key import functionality, and secure key deletion. Key information includes Device A building wrapped data containing public keys and encrypted materials, Device B importing the wrapped key, and both devices subsequently deleting the wrapping, ECDH, and KEK keys to maintain security hygiene. This document serves as a technical tutorial or code snippet for developers implementing secure key exchange and lifecycle management in cryptographic applications.