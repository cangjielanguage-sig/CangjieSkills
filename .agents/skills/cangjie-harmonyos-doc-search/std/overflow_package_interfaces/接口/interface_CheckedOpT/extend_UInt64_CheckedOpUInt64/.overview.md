# extend_UInt64_CheckedOpUInt64

This directory serves as a comprehensive API documentation resource for the Cangjie programming language, focusing specifically on the implementation of the `CheckedOp` interface for the `UInt64` type. It thoroughly covers safe arithmetic and bit manipulation operations designed to handle potential overflow errors gracefully. The documentation highlights the use of an `Option` return strategy, which ensures that operations return `None` if an error occurs, such as overflow, rather than throwing runtime exceptions. Key topics include checked addition, subtraction, multiplication, division, modulo, increment, decrement, negation, and bitwise shifts. This resource is ideal for Cangjie developers who need to perform robust integer calculations and explicitly manage error states to ensure application stability and prevent undefined behavior during complex computations.

## Quick Navigation

What do you want to learn about regarding safe UInt64 operations?

*   Understand the core CheckedOp interface implementation and standard arithmetic functions (Add, Div, Mul, etc.) → extend_UInt64_CheckedOpUIn_7more.md
*   Learn about negation, subtraction, and bitwise shift operations with error handling → func_checkedNeg_4more.md

## Detailed Description

The following files provide in-depth technical specifications and usage guidelines for the functions included in this extension. They detail the return types and error handling mechanisms for each operation.

### extend_UInt64_CheckedOpUIn_7more.md extend_UInt64_CheckedOpUIn_7more.md
This API documentation details the implementation of the CheckedOp interface for the UInt64 type in the Cangjie programming language. The document outlines the extension declaration and provides specifications for arithmetic functions including checkedAdd, checkedDec, checkedDiv, checkedInc, checkedMod, and checkedMul. Key information highlights that these operations utilize an Option return strategy to safely handle potential overflow errors, returning None if an overflow occurs or the valid result otherwise. This reference guide targets Cangjie developers who require safe arithmetic operations for unsigned 64-bit integers to prevent undefined behavior during calculations.

### func_checkedNeg_4more.md func_checkedNeg_4more.md
This API reference document outlines safe arithmetic and bit manipulation functions in the Cangjie programming language that utilize the Option return strategy. The content details four specific methods: checkedNeg, checkedShl, checkedShr, and checkedSub. Key takeaways include that these functions return ?UInt64, providing None when operations result in overflow or invalid shift amounts, ensuring robust error handling without runtime exceptions. This documentation is intended for Cangjie developers who need to perform safe integer calculations and bitwise operations while explicitly managing potential error states.