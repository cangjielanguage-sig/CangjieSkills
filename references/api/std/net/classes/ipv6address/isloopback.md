<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipv6address.isloopback" parent="std.net.class.ipv6address" -->
# IPv6Address.isLoopback

[← IPv6Address](index.md)

## 签名

```cangjie role=signature
public func isLoopback(): Bool
```

判断此 IPv6Address 对象是不是环回地址。

## 契约

返回值：

- Bool - 返回 true 表示是环回地址，否则返回 false。
