# 自定义组件成员属性访问限定符使用限制

This document collection provides a comprehensive technical guide on the usage restrictions and initialization rules for access qualifiers on custom component member properties within the Cangjie ArkUI framework. It covers the intricate compatibility requirements between access modifiers (such as `private` and `public`) and state management decorators (including `@State`, `@Link`, `@Prop`, `@StorageLink`, and `@Consume`). Key topics include external initialization constraints, validation rules, and compiler error prevention. By distinguishing between decorators that permit external initialization and those that forbid local initialization, this resource is essential for Cangjie developers and component architects aiming to ensure robust state management, adhere to framework standards, and avoid syntax pitfalls during application development.

## Quick Navigation

**What do you want to learn?**

- Understand general usage restrictions and initialization rules for access qualifiers → 自定义组件成员属性访问限定符使用限制_2more.md
- Explore specific usage scenarios and compatibility examples with code comparisons → 使用场景/

## Detailed Description

### 自定义组件成员属性访问限定符使用限制_2more.md 自定义组件成员属性访问限定符使用限制_2more.md
This reference document outlines the usage restrictions and initialization rules for access qualifiers on custom component member properties within the Cangjie state management framework. It details specific constraints for various state management decorators, categorizing them based on whether they permit external initialization. Key takeaways include that variables decorated with `@State`, `@Prop`, and `@Provide` can be initialized externally, whereas `@StorageLink` and `@Consume` cannot. Additionally, `@Link` variables strictly require external initialization and forbid local initialization. Intended for component developers, this guide ensures proper state variable validation and adherence to framework standards.

### 使用场景/
This directory serves as a technical guide for developers using the Cangjie ArkUI framework, specifically focusing on the constraints and validation rules governing state management. It details the compatibility requirements between various access modifiers (such as `private` and `public`) and state management macros (including `@State`, `@Link`, `@StorageLink`, and others). By providing comparative code examples that highlight common compiler errors versus correct syntax, these documents help developers navigate access control restrictions. This resource is essential for Cangjie developers aiming to implement robust state management in their ArkUI applications while avoiding syntax pitfalls.