# 启动命令start

This directory provides comprehensive technical documentation on the `aa start` command within the HarmonyOS development ecosystem. It focuses specifically on the Stage model, detailing the protocols for launching core components such as UIAbility and ServiceExtensionAbility. The content covers essential technical aspects including explicit and implicit startup syntax, parameter passing mechanisms, URI configuration, and error code troubleshooting. Designed specifically for developers and testers utilizing the Cangjie programming language, these resources offer practical guidance for debugging application startup processes, implementing command-line control logic, and managing data transfer during component initialization. Whether you are troubleshooting launch failures or testing parameter retrieval, this directory serves as a critical reference for effective command-line application management and lifecycle control.

## Quick Navigation

**What do you want to learn?**

- Master the basic syntax, understand error codes, and perform implicit launches → 启动命令start_1.md
- Implement parameter passing in Cangjie and launch specific URLs or browsers → 启动命令start_2.md

## Detailed Description

### 启动命令start_1.md 启动命令start_1.md
This reference document details the aa start command for launching Stage model components like UIAbility and ServiceExtensionAbility. It covers syntax for both explicit and implicit starts, explaining parameters such as device ID, bundle name, and URI. The guide includes a comprehensive list of error codes and return values for troubleshooting launch failures. Additionally, it provides a practical example of implicit launching using URI configuration. This resource is intended for developers and testers needing to debug or control application startup processes via the command line.

### 启动命令start_2.md 启动命令start_2.md
This technical guide demonstrates how to launch UIAbility components and pass parameters via the command line in a HarmonyOS development environment. It covers the `aa start` command syntax for passing various data types, retrieving these parameters within a Cangjie-based `UIAbility` class, and launching a browser with a specific URL. Key information includes specific command-line flags for URI and parameters, alongside a code snippet showing how to access `want.parameters` in the `onCreate` lifecycle method. The content targets HarmonyOS developers using the Cangjie programming language who need to test or implement application startup logic and data passing.