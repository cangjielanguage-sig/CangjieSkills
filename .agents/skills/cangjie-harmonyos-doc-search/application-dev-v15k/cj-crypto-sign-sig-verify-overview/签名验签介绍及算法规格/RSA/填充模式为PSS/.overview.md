# 填充模式为PSS

This directory provides technical documentation and reference guides for implementing RSA cryptographic operations using the Probabilistic Signature Scheme (PSS) padding mode. It covers essential aspects such as parameter construction syntax, compatibility matrices for various key sizes (RSA512 to RSA8192), and specific constraints regarding hash algorithms, Mask Generation Functions (MGF), and salt lengths. Additionally, it addresses API compatibility and key type handling for signature verification. This resource is designed for developers, security engineers, and cryptographers who need to configure or validate RSA-PSS parameters within their systems.

## Quick Navigation

**What do you want to learn?**

*   Learn how to construct string parameters for PSS operations → 填充模式为PSS_1.md
*   Check supported combinations for API v12+ (RSA512-4096) → 填充模式为PSS_2.md
*   Review parameter matrix for larger keys (RSA1024-8192) and salt lengths → 填充模式为PSS_3.md
*   Understand key type input compatibility and verification behavior → 填充模式为PSS_4.md

## Detailed Description

### 填充模式为PSS_1.md 填充模式为PSS_1.md
This technical reference explains how to construct string parameters for RSA signature and verification operations using the PSS padding mode. It covers the syntax for concatenating key type, padding mode, digest, and mask digest using the pipe symbol, providing a specific usage example. The document highlights a critical constraint where the sum of the digest and mask digest lengths must be less than the RSA key length, noting that a 512-bit key cannot support SHA256 for both. This guide targets developers and security engineers configuring cryptographic algorithms, requiring prerequisite knowledge of RSA specifications and digest algorithms.

### 填充模式为PSS_2.md 填充模式为PSS_2.md
This document serves as a technical reference table defining the supported combinations of RSA key sizes, hash algorithms, and Mask Generation Functions (MGF) for the PSS padding mode across API version 12 and above. It covers asymmetric key types ranging from RSA512 to RSA4096, detailing valid pairings between message digests (like MD5, SHA1, SHA256) and MGF1 algorithms. The data highlights specific compatibility constraints, particularly for smaller key sizes like RSA512 and RSA768, where certain MGF algorithms are excluded to ensure security and size requirements. All listed configurations require API version 12 or higher. This reference guide is intended for developers and security engineers implementing cryptographic signatures who need to verify valid parameter combinations for RSA-PSS operations.

### 填充模式为PSS_3.md 填充模式为PSS_3.md
This document serves as a technical reference matrix detailing the supported parameter combinations for RSA signatures using the PSS padding mode. It covers compatibility across various RSA key lengths ranging from 1024 to 8192 bits, specific hash algorithms including MD5 and the SHA family, MGF1 mask generation functions, and salt length constraints. The key takeaway is that PSS mode supports a flexible combination of hash and MGF1 algorithms for all listed key sizes, with a mandatory minimum salt length of 12 bytes. This reference is intended for cryptographers, security engineers, or developers configuring cryptographic libraries to ensure valid RSA-PSS parameter selection.

### 填充模式为PSS_4.md 填充模式为PSS_4.md
This technical note clarifies RSA signature and verification parameter handling, specifically regarding key type input compatibility for PSS padding mode. The document addresses the specific behavior of key type inputs when dealing with keys generated from key parameters. Key information includes that the system supports inputting key types without specifying a length to ensure compatibility. Consequently, the signature verification operation dynamically depends on the actual length of the input key rather than the type declaration. This information is intended for developers or system integrators implementing RSA PSS cryptographic functions who require flexibility in key management. Keywords: RSA, PSS padding, signature verification, key type, key parameters, cryptography, API compatibility.