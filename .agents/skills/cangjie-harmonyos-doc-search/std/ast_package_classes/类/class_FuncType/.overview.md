# class_FuncType

This directory provides comprehensive documentation for the `class_FuncType` module within the Cangjie AST package. It focuses on the structural definition of function types in the abstract syntax tree, detailing how nodes represent parameters, return values, and associated syntax elements like arrows and parentheses. Additionally, it covers essential utilities for traversing these syntax trees using the visitor pattern, enabling programmatic analysis and manipulation. Key topics include AST node construction, lexical token management, inheritance hierarchies, and traversal control mechanisms such as early termination. This resource is designed for software developers, language engineers, and compiler designers who are building tools for the Cangjie language or performing static analysis. It serves as a practical guide for those needing to understand the internal representation of function types and implement custom logic for walking through syntax tree structures.

## Quick Navigation

**What do you want to learn?**

*   Understand the API and structure of function type nodes (parameters, return values, tokens) → class_FuncType_11more.md
*   Learn how to traverse syntax tree nodes using the visitor pattern → func_traverseVisitor.md

## Detailed Description

### class_FuncType_11more.md
This document serves as API reference documentation for the FuncType class within the Cangjie AST package, defining a node structure for function types consisting of parameters and return values. It covers the class inheritance from TypeNode, properties for accessing syntax elements like arrows, parentheses, and type lists, and methods for object construction and token conversion. Key information includes the specific handling of lexical tokens for CFunc keywords and exception throwing for invalid token assignments. This guide targets developers and language engineers working with abstract syntax trees who need to parse, construct, or manipulate function type definitions programmatically.

### func_traverseVisitor.md
This document serves as API reference documentation for the `traverse` function in the Cangjie AST package, detailing how to traverse syntax tree nodes using a visitor pattern. It covers the function signature, the specific functionality of walking through nodes and their children, and the parameter requirements for the `Visitor` instance. Key information includes the mechanism for early termination, which allows users to stop the traversal process by overriding the `visit` function and calling `breakTraverse`. This documentation is intended for developers working on compiler tools or static analysis in the Cangjie language who require programmatic access to syntax tree structures.