# class_RemoteProxy

This directory contains API reference documentation for the Cangjie `RemoteProxy` class, a core component for Inter-Process Communication (IPC). It covers the implementation of `IRemoteObject`, detailing transaction codes, interface management, and connection status checks. Additionally, it includes functions for managing remote object lifecycles, such as registering and unregistering death notifications, and handling message requests. This documentation is essential for developers building distributed applications or services that require robust communication between processes, providing detailed specifications on parameters, return types, and exception handling.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `RemoteProxy` class structure, constants, and interface methods → class_RemoteProxy_9more.md
*   Manage remote object lifecycle and death notifications → func_registerDeathRecipien_2more.md, func_unregisterDeathRec_c155b791.md
*   Handle synchronous or asynchronous message transmission → func_registerDeathRecipien_2more.md

## Detailed Description

### class_RemoteProxy_9more.md class_RemoteProxy_9more.md
This document serves as API reference documentation for the Cangjie RemoteProxy class, which implements IRemoteObject for Inter-Process Communication (IPC). It details static constants defining internal transaction codes, such as PING_TRANSACTION and DUMP_TRANSACTION, alongside methods for managing remote object states. Key functions include getDescriptor for retrieving interface strings, getLocalInterface for querying local objects, and isObjectDead to check connection status. The documentation also specifies return types, parameters, and potential BusinessException errors. This reference is intended for developers implementing IPC mechanisms who need to understand proxy behavior and transaction handling.

### func_registerDeathRecipien_2more.md func_registerDeathRecipien_2more.md
This API reference document details two Cangjie functions for Inter-Process Communication (IPC), specifically managing remote object lifecycles and message transmission. It covers `registerDeathRecipient` for handling death notifications and `sendMessageRequest` for synchronous or asynchronous data exchange using MessageSequence objects. Key information includes parameter definitions for callbacks, message codes, and options, alongside system capabilities and exception handling for errors like invalid parameters or proxies. This documentation targets developers implementing IPC mechanisms who need to monitor remote process health and facilitate structured communication between processes.

### func_unregisterDeathRec_c155b791.md func_unregisterDeathRec_c155b791.md
This document provides API reference documentation for the Cangjie function `unregisterDeathRecipient`, which is used to remove callbacks for remote object death notifications within an IPC framework. The main sections include the function signature, system capabilities, parameter definitions for the recipient and flags, and detailed exception handling with specific error codes. Key information highlights that the function requires a valid `DeathRecipient` and flags, throwing `BusinessException` for parameter mismatches or invalid remote objects. Intended for developers managing inter-process communication, this document assumes familiarity with IPC concepts and error code resolution.