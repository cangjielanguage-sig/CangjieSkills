<!-- cj-doc kind="api-member" level="6" id="std.sync.class.atomicint64.init" parent="std.sync.class.atomicint64" -->
# AtomicInt64.init

[← AtomicInt64](index.md)

## 签名

```cangjie role=signature
public init(val: Int64)
```

构造一个封装 Int64 数据类型的原子类型 AtomicInt64 的实例，其内部数据初始值为入参 `val` 的值。

## 契约

参数：

- val: Int64 - 原子类型的初始值。
