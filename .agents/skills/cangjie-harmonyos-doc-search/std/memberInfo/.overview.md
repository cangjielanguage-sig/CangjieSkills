# memberInfo

This directory serves as a comprehensive educational resource for mastering runtime reflection within the Cangjie programming language, with a specific focus on the `memberInfo` module. It provides detailed guidance on how developers can programmatically inspect types, access internal class structures, and manipulate object data dynamically during application execution. The content thoroughly explains the retrieval of `TypeInfo` objects, the navigation of `instanceVariables` for reading and writing values, and the inspection of `instanceFunctions` to verify method signatures. By utilizing a practical example involving a `Rectangular` object, the documentation illustrates the effective use of `getValue`, `setValue`, and `TypeInfo.of` methods. This material is ideally suited for advanced developers, systems programmers, and library authors who are working with the Cangjie standard library and require robust introspection capabilities to build flexible, dynamic frameworks or debugging tools.

## Quick Navigation

**What do you want to learn?**

*   **Master runtime reflection:** Understand the core principles of type inspection and member access in Cangjie → memberInfo.md memberInfo.md
*   **Access and modify data:** Learn how to use `getValue` and `setValue` to dynamically read and change object properties → memberInfo.md memberInfo.md
*   **Explore class structure:** Discover how to retrieve `TypeInfo` and iterate through `instanceVariables` and `instanceFunctions` → memberInfo.md memberInfo.md
*   **Analyze method signatures:** Find out how to inspect return types and other metadata of class functions at runtime → memberInfo.md memberInfo.md
*   **Review practical code:** Study a complete, runnable example that modifies a `Rectangular` object to see these concepts in action → memberInfo.md memberInfo.md

## Detailed Description

The following section provides an in-depth analysis of the specific files contained within this directory. These resources are carefully structured to guide developers from theoretical concepts to practical implementation. By reviewing these documents, you will gain a functional understanding of how to leverage the reflection API to perform complex dynamic programming tasks, such as serialization, debugging, or generic data handling, within the Cangjie environment.

### memberInfo.md memberInfo.md
This document serves as a practical tutorial demonstrating how to utilize reflection to access and manipulate class member information in the Cangjie programming language. It covers retrieving TypeInfo, accessing instanceVariables for reading and writing values, and inspecting instanceFunctions to check return types. The example code dynamically modifies a Rectangular object's properties and verifies function signatures, illustrating the getValue, setValue, and TypeInfo.of methods. It targets developers seeking to understand runtime type inspection and dynamic member manipulation within the Cangjie standard library.