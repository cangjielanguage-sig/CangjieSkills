<!-- cj-doc kind="api-member" level="6" id="std.core.func.println.println-uint64" parent="std.core.func.println" -->
# println(UInt64)

[← println](index.md)

## 签名

```cangjie role=signature
public func println(i: UInt64): Unit
```

向控制台输出 UInt64 类型数据的字符串表达，末尾添加换行。

## 契约

参数：

- i: UInt64 - 待输出的 UInt64 类型数据。
