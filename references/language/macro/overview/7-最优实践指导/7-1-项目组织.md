<!-- cj-doc kind="guide-leaf" level="6" id="language.macro.overview.7-最优实践指导.7-1-项目组织" parent="language.macro.overview.7-最优实践指导" -->
# 7.1 项目组织

[← 7. 最优实践指导](index.md)

- 宏定义必须在 `macro package` 中，与调用代码分离为独立模块
- 使用 cjpm 管理宏模块依赖，避免手动 `cjc --compile-macro` 编译
- 宏包中仅宏定义可为 `public`，辅助函数保持包内可见
