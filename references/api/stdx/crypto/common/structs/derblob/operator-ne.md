<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.common.struct.derblob.operator-ne" parent="stdx.crypto.common.struct.derblob" -->
# DerBlob.!=

[← DerBlob](index.md)

## 签名

```cangjie role=signature
public override operator func !=(other: DerBlob): Bool
```

判不等。

## 参数

- other: DerBlob - 被比较的 DerBlob 对象。

## 返回值

- Bool - 若对象不同，返回 true；否则，返回 false。

