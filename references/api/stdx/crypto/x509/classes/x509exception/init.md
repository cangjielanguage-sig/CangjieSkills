<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509exception.init" parent="stdx.crypto.x509.class.x509exception" -->
# X509Exception.init

[← X509Exception](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造 X509Exception 对象。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

构造 X509Exception 对象。

### 契约

参数：

- message: String - 异常的信息。
