<!-- cj-doc kind="api-member" level="6" id="std.collection.class.hashset.iterator" parent="std.collection.class.hashset" -->
# HashSet<T> where T <: Hashable & Equatable<T>.iterator

[← HashSet<T> where T <: Hashable & Equatable<T>](index.md)

## 签名

```cangjie role=signature
public func iterator(): Iterator<T>
```

返回此 HashSet 的迭代器。

## 契约

返回值：

- Iterator\<T> - 返回此 HashSet 的迭代器。
