<!-- cj-doc kind="api-member" level="6" id="stdx.compress.zlib.enum.wraptype.value-gzipformat" parent="stdx.compress.zlib.enum.wraptype" -->
# WrapType.GzipFormat

[← WrapType](index.md)

## 签名

```cangjie role=signature
GzipFormat
```

构造一个表示 `Gzip` 压缩数据格式的枚举实例。
