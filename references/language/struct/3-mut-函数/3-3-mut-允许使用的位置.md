<!-- cj-doc kind="guide-leaf" level="5" id="language.struct.3-mut-函数.3-3-mut-允许使用的位置" parent="language.struct.3-mut-函数" -->
# 3.3 `mut` 允许使用的位置

[← 3. `mut` 函数](index.md)

- 仅在 `interface`、`struct` 和结构体扩展中
- **不能用于 `class`**（类是引用类型，实例函数本身就能修改成员）
