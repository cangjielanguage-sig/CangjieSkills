# class_MacroExpandDecl

This directory provides comprehensive API documentation for the `MacroExpandDecl` class, a critical component within the Abstract Syntax Tree (AST) of the Cangjie programming language. It details the representation of macro invocation nodes, covering essential aspects such as class inheritance, mutable properties like `fullIdentifier` and `macroInputs`, and initialization constructors. Additionally, it includes references for AST utility functions, specifically focusing on token conversion and tree traversal mechanisms. This documentation is specifically designed for software developers, compiler engineers, and language tool creators who require deep programmatic access to macro declaration structures and syntax tree manipulation for building static analysis tools or compiler components.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition, properties, and constructors for macro invocation nodes → class_MacroExpandDecl_11more.md
*   Learn how to convert AST nodes to tokens or traverse the syntax tree → func_toTokens_2more.md

## Detailed Description

### class_MacroExpandDecl_11more.md class_MacroExpandDecl_11more.md
This API documentation defines the MacroExpandDecl class, which represents macro invocation nodes within the Abstract Syntax Tree (AST) of the Cangjie language. The content covers the class definition, its inheritance from Decl, and various mutable properties such as fullIdentifier, macroInputs, and specific delimiter tokens for parentheses and brackets. It also details two constructors for initialization, including one that accepts a collection of tokens. Key takeaways include the handling of both function-like and attribute-like macro syntax, with specific exceptions thrown for invalid token types. This reference material is intended for developers working on compiler tools or AST manipulation who need to programmatically access and modify macro declaration structures.

### func_toTokens_2more.md func_toTokens_2more.md
This document provides API reference documentation for Abstract Syntax Tree (AST) manipulation functions within the Cangjie language environment. It covers two main functionalities: the `toTokens` function, which converts a current syntax tree node into a `Tokens` type, and the `traverse` function, which iterates through the node and its children using a `Visitor` instance. Key takeaways include the specific return types for `toTokens` and the mechanism for controlling traversal flow, such as overriding the `visit` function to call `breakTraverse` for early termination. This technical reference is intended for developers and language engineers building compiler tools or static analysis applications who require programmatic access to syntax tree structures.