<!-- cj-doc kind="api-member" level="5" id="std.math.func.asinh" parent="std.math" -->
# asinh

[← std.math](../index.md)

本页汇总 3 个同名重载；先按签名选择，再读取对应契约。

## asinh(Float16)

### 签名

```cangjie role=signature
public func asinh(x: Float16): Float16
```

计算半精度浮点数的反双曲正弦函数值。

### 契约

参数：

- x: Float16 - 传入的半精度浮点数。

返回值：

- Float16 - 返回传入参数的反双曲正弦函数值。

## asinh(Float32)

### 签名

```cangjie role=signature
public func asinh(x: Float32): Float32
```

计算单精度浮点数的反双曲正弦函数值。

### 契约

参数：

- x: Float32 - 传入的单精度浮点数。

返回值：

- Float32 - 返回传入参数的反双曲正弦函数值。

## asinh(Float64)

### 签名

```cangjie role=signature
public func asinh(x: Float64): Float64
```

计算双精度浮点数的反双曲正弦函数值。

### 契约

参数：

- x: Float64 - 传入的双精度浮点数。

返回值：

- Float64 - 返回传入参数的反双曲正弦函数值。
