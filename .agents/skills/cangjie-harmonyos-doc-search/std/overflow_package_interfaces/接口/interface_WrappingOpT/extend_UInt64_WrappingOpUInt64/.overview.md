# extend_UInt64_WrappingOpUInt64

This directory contains API documentation for the Cangjie programming language, specifically focusing on the `WrappingOp` interface implementation for the `UInt64` type. It covers a comprehensive list of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, and bit shifting. The documentation highlights the use of a high-order truncation strategy to handle overflow, ensuring results wrap around rather than causing errors. Additionally, it details specific behaviors for shift operations, such as using the low 6 bits of the right operand for large shift amounts. This resource is suitable for Cangjie developers and software engineers requiring precise control over unsigned integer arithmetic behavior, particularly in performance-critical or low-level scenarios where overflow wrapping is desired.

## Quick Navigation

**What do you want to learn?**

*   Understand the complete implementation of the WrappingOp interface for UInt64, including arithmetic and bitwise operations → extend_UInt64_WrappingOpU_10more.md
*   Find specific details on the wrapping subtraction function, including its signature and overflow handling strategy → func_wrappingSubUInt64.md

## Detailed Description

### extend_UInt64_WrappingOpU_10more.md extend_UInt64_WrappingOpU_10more.md
This API documentation details the implementation of the `WrappingOp` interface for the `UInt64` type in the Cangjie programming language. The document covers a comprehensive list of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, and bit shifting. Key information includes that all operations utilize a high-order truncation strategy to handle overflow, ensuring results wrap around rather than causing errors. Shift operations specifically use the low 6 bits of the right operand if the shift amount is too large. This reference is intended for Cangjie developers requiring precise control over unsigned integer arithmetic behavior, particularly in scenarios where overflow wrapping is desired.

### func_wrappingSubUInt64.md func_wrappingSubUInt64.md
This document serves as API reference documentation for the Cangjie programming language, specifically detailing the `wrappingSub` function for unsigned 64-bit integers. The content covers the function signature, its high-order truncation strategy for handling overflow, input parameters, and return values. Key information includes that the function performs subtraction where overflow results in high-bit truncation rather than an error, returning a `UInt64` value. This reference is intended for software developers working with Cangjie who require precise control over arithmetic operations and overflow behavior. Keywords: Cangjie, UInt64, wrapping subtraction, overflow handling, API reference.