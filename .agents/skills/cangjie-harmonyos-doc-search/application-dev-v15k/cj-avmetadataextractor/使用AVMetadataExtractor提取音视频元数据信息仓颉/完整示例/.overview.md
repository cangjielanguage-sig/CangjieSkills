# 完整示例

This directory serves as an essential repository of complete code examples tailored for HarmonyOS developers proficient in the Cangjie programming language. It provides in-depth demonstrations of media processing workflows, specifically focusing on the extraction of audio metadata and album covers via the MediaKit. The content thoroughly explains three distinct methods for configuring data sources: utilizing `fdSrc` with bundled resources, `fdSrc` with sandbox file paths, and `dataSrc` with a custom callback mechanism. Beyond media extraction, it illustrates the implementation of data source callbacks for handling file reading operations and managing the main application lifecycle, including startup events and view loading. These examples are highly suitable for developers aiming to build feature-rich media applications that require seamless integration with the file system and robust UI lifecycle management within the HarmonyOS environment.

## Quick Navigation

**What do you want to learn or implement?**

*   Extract audio metadata and album covers using MediaKit → 完整示例_1.md
*   Implement data source callbacks for custom file reading → 完整示例_2.md
*   Manage the main application lifecycle and UI initialization → 完整示例_2.md

## Detailed Description

### 完整示例_1.md 完整示例_1.md
This document provides a complete Cangjie code example demonstrating how to extract audio metadata and album covers using the MediaKit. It covers three distinct methods for setting data sources: using fdSrc with bundled resources, fdSrc with sandbox file paths, and dataSrc with a custom callback mechanism. The example illustrates the workflow of creating an AVMetadataExtractor, configuring the source, calling fetchMetadata and fetchAlbumCover, and displaying the result via a PixelMap in the UI. It targets HarmonyOS developers using the Cangjie language who need to implement media file analysis in their applications.

### 完整示例_2.md 完整示例_2.md
This document provides a complete code example demonstrating how to implement a data source callback and manage the main application lifecycle in the Cangjie programming language. It covers the DataSrcCallback class for file reading operations and the MainAbility class for handling application initialization and UI loading. Key information includes the use of FileFs.read for data retrieval with specific offsets and lengths, alongside the management of startup events and view loading in MainAbility. This serves as a reference for developers building applications using the Cangjie language, specifically those needing to integrate file system operations with UI lifecycle management.