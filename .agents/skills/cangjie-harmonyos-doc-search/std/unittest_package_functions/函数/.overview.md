# 函数

This directory contains API reference documentation for core functions within the Cangjie unit testing framework. It covers assertion mechanisms, exception handling, equality comparisons, and configuration management. Key topics include standard assertions (`assert`), expectation-based validations (`expect`), custom assertion execution, and internal macro expansion utilities. This documentation is suitable for developers building automated tests, extending the testing framework with custom logic, or working on the internal compiler implementation of the testing tools.

## Quick Navigation

**What do you want to learn?**

*   Implement standard assertions, handle unexpected exceptions, and configure test execution → func_assertCaughtUnexpecte_5more.md
*   Use expectation-based validations that log failures without halting execution → func_expectCaughtUnexpecte_5more.md
*   Execute custom assertions using `@Assert` and `@Expect` macros → func_invokeCustomAssertTAr_2more.md
*   Understand internal functions for approximate equality during macro expansion → func_isNearExpansionCT_DCT_2more.md

## Detailed Description

### func_assertCaughtUnexpecte_5more.md func_assertCaughtUnexpecte_5more.md
This document provides API reference documentation for assertion and configuration functions within a Cangjie unit testing framework. It details specific functions for handling unexpected exceptions, comparing values for equality using generic types, generating default configurations, and defining the main entry point for test execution. Key information includes the parameters for the `assertEqual` function, which supports an optional `isDelta` flag for approximate comparisons, and the `entryMain` function designed for use with the `cjc --test` command. This reference is intended for developers seeking to implement test logic and configure the testing environment, offering precise signatures and parameter descriptions for effective unit testing.

### func_expectCaughtUnexpecte_5more.md func_expectCaughtUnexpecte_5more.md
This document serves as API reference documentation for unit testing assertion functions in the Cangjie programming language. It details the signatures, parameters, and behaviors of five specific functions used for test validation and failure handling. Key sections cover exception expectation verification (expectCaughtUnexpectedE), equality comparison with support for approximate matching (expectEqual), and explicit failure triggers (fail, failExpect). A critical distinction is that "expect" functions log failures without throwing exceptions, whereas fail immediately halts execution. This reference is intended for developers implementing automated tests who need to validate logic, handle exceptions, and manage assertion contexts within the unittest framework.

### func_invokeCustomAssertTAr_2more.md func_invokeCustomAssertTAr_2more.md
This API reference document details the usage and signatures of two functions designed to execute user-defined assertions within a unit testing framework. It covers `invokeCustomAssert<T>` and `invokeCustomExpect`, explaining their roles in running custom assertions triggered by `@Assert` and `@Expect` macros. Key information includes the parameters for passed arguments, caller names, assertion closures, and optional parent contexts for handling nested failures. `invokeCustomAssert` returns a generic type, while `invokeCustomExpect` returns Unit. This documentation is intended for developers extending the testing framework with custom assertion logic, requiring knowledge of Cangjie macros and the `AssertionCtx` class.

### func_isNearExpansionCT_DCT_2more.md func_isNearExpansionCT_DCT_2more.md
This document provides API reference details for two internal Cangjie functions designed to determine approximate equality between two values during macro expansion. It covers two overloaded function signatures, their generic constraints involving NearEquatable and Comparable, parameters including delta and cmpType, and return values. Key information indicates that these functions are strictly for internal use within the PowerAssert macro and should not be invoked by users. The functions differ in constraints, with one requiring Comparable and the other using an overloadHack parameter to resolve function overloading. This reference material targets compiler developers or those working on unit testing framework internals.