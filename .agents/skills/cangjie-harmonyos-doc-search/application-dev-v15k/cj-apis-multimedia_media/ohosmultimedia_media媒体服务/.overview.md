# ohosmultimedia_media媒体服务

This directory provides API reference documentation for the HarmonyOS multimedia media service module, tailored for the Cangjie programming language. It encompasses a broad spectrum of functionalities including audio and video playback, recording, screen capture, transcoding, and metadata management. The documentation details essential classes, enums, structs, and factory functions necessary for developing robust multimedia applications. It is intended for software developers and engineers utilizing the MediaKit framework who require precise control over media processing, state management, and event handling within their Cangjie-based projects.

## Quick Navigation

**What do you want to do?**

*   **Play Media** → type_MediaDescription_6more.md type_MediaDescription_6more, enum_PlaybackSpeed.md enum_PlaybackSpeed, [class_AVPlayer/](#class_avplayer)
*   **Record Media** → func_createAVRecorder_3more.md func_createAVRecorder_3more, [class_AVRecorder/](#class_avrecorder), [class_AVRecorderConfig/](#class_avrecorderconfig)
*   **Capture Screen** → [class_AVScreenCaptureRecorder/](#class_avscreencapturerecorder), enum_AVScreenCaptureRecordPreset.md enum_AVScreenCaptureRecordPreset
*   **Transcode Video** → [class_AVTranscoder/](#class_avtranscoder), enum_AVTranscoderCallbackType.md enum_AVTranscoderCallbackType
*   **Handle Metadata** → [class_AVMetadataExtractor/](#class_avmetadataextractor), [struct_MediaDescriptionKey/](#struct_mediadescriptionkey)
*   **Generate Thumbnails** → [class_AVImageGenerator/](#class_avimagegenerator), struct_PixelMapParams.md struct_PixelMapParams
*   **Manage Audio** → [class_SoundPool/](#class_soundpool), struct_PlayParameters.md struct_PlayParameters
*   **Handle Errors/States** → [enum_AVErrorCode/](#enum_averrorcode), [enum_AVPlayerState/](#enum_avplayerstate)

## Detailed Description

### class_AVDataSrcDescriptor.md class_AVDataSrcDescriptor.md
This API reference details the Cangjie class `AVDataSrcDescriptor`, designed to describe audio and video data sources for early playback scenarios. It covers the class constructor and properties like `fileSize` and `callback`, enabling custom DataSource playback within the `AVPlayer` framework.

### class_AVFileDescriptor.md class_AVFileDescriptor.md
This document defines the Cangjie class `AVFileDescriptor` for managing audio and video resources stored contiguously within a single file. It details properties like `fd`, `offset`, and `length`, essential for playing specific segments of embedded media files.

### class_EncoderInfo.md class_EncoderInfo.md
This reference documentation defines the Cangjie `EncoderInfo` class, which specifies encoder capabilities. It details read-only properties such as MIME type, bit rate, and video/audio attributes to assist in configuring the `AVRecorder` module.

### class_Location_3more.md class_Location_3more.md
This document provides API references for the `Location`, `MediaSource`, and `OnAVRecorderStateChangeHandler` classes. It covers geographic coordinates for recording, media data info, and state machine event callbacks for developers building multimedia applications.

### class_Range.md class_Range.md
This document defines the Cangjie `Range` class, representing a numerical interval with read-only `min` and `max` properties. It is used within the `AVRecorder` framework to handle value ranges.

### class_SubtitleInfo.md class_SubtitleInfo.md
This reference details the Cangjie `SubtitleInfo` class for handling external subtitle information. It covers properties for duration, start time, and text content, assuming familiarity with event subscription mechanisms for subtitle callbacks.

### enum_AVImageQueryOptions.md enum_AVImageQueryOptions.md
This document defines the Cangjie enum `AVImageQueryOptions`, specifying the relationship between a requested timestamp and the actual video frame retrieved during thumbnail generation. It distinguishes between retrieving any frame versus a keyframe.

### enum_AVMimeTypes.md enum_AVMimeTypes.md
This document defines the `AVMimeTypes` enum for specifying media MIME types. It includes values like `ApplicationM3U8` and standard methods, intended for developers managing media type configurations.

### enum_AVRecorderCallbackType.md enum_AVRecorderCallbackType.md
This document defines the `AVRecorderCallbackType` enum, categorizing callback events for the `AVRecorder` component. It includes error events, state changes, and audio updates, aiding in asynchronous event handling.

### enum_AVScreenCaptureRecordPreset.md enum_AVScreenCaptureRecordPreset.md
This document defines the `AVScreenCaptureRecordPreset` enum for screen recording configurations. It specifies presets for video codecs (H.264/H.265), audio codec (AAC), and container format (MP4).

### enum_AVTranscoderCallbackType.md enum_AVTranscoderCallbackType.md
This document defines the `AVTranscoderCallbackType` enum for handling video transcoding events. It details values for progress updates, completion, and errors, assisting in managing asynchronous status updates.

### enum_AudioSourceType.md enum_AudioSourceType.md
This document defines the `AudioSourceType` enum for specifying audio input sources. It includes members like `MIC` and `VOICE_COMMUNICATION` for configuring audio capture in various recording scenarios.

### enum_BufferingInfoType.md enum_BufferingInfoType.md
This document defines the `BufferingInfoType` enum for media buffering events. It details values like `BUFFERING_START` and `BUFFERING_END`, explaining player behavior during these states.

### enum_CodecMimeType.md enum_CodecMimeType.md
This reference defines the `CodecMimeType` enum for supported audio and video codec MIME types. It lists members like `VIDEO_AVC` and `AUDIO_AAC`, assisting in identifying media formats during encoding and decoding.

### enum_ContainerFormatType.md enum_ContainerFormatType.md
This document defines the `ContainerFormatType` enum for identifying multimedia container formats. It enumerates values like `CFT_MPEG_4` and `CFT_MP3` for media processing applications.

### enum_FileGenerationMode.md enum_FileGenerationMode.md
This document defines the `FileGenerationMode` enum for creating media files. It specifies modes for app-managed sandbox files versus system-managed files in camera recording scenarios.

### enum_HdrType.md enum_HdrType.md
This document defines the Cangjie enum `HdrType`, which specifies video High Dynamic Range types. It includes values like `AV_HDR_TYPE_NONE` and `AV_HDR_TYPE_VIVID` for identifying video HDR formats.

### enum_MediaType.md enum_MediaType.md
This reference defines the `MediaType` enum, categorizing multimedia formats into `Audio`, `Video`, and `Subtitle`. It is intended for developers integrating multimedia handling capabilities.

### enum_PlaybackInfoKey.md enum_PlaybackInfoKey.md
This document defines the `PlaybackInfoKey` enum for describing multimedia playback information. It includes members like `SERVER_IP_ADDRESS` and `BUFFER_DURATION` for querying specific playback metrics.

### enum_PlaybackSpeed.md enum_PlaybackSpeed.md
This document defines the `PlaybackbackSpeed` enum, specifying playback rate multipliers for video content. It lists values ranging from slow motion to fast forward for integrating video playback controls.

### enum_ProfileCallbackType.md enum_ProfileCallbackType.md
This document defines the `ProfileCallbackType` enum for connection management in `AVScreenCapture`. It includes values for connection errors and state changes, available starting from system version 19.

### enum_SeekMode.md enum_SeekMode.md
This document provides reference for the `SeekMode` enum, controlling video playback seeking behaviors. It details modes like `SEEK_CLOSEST` and `SEEK_NEXT_SYNC` for timeline navigation.

### enum_SoundPoolEvent.md enum_SoundPoolEvent.md
This document defines the `SoundPoolEvent` enum for SoundPool multimedia events. It covers states like `LoadCompleted` and `PlayFinished` for developers integrating audio playback features.

### enum_StateChangeReason.md enum_StateChangeReason.md
This document defines the `StateChangeReason` enum, categorizing reasons for state transitions in multimedia instances. It distinguishes between user-initiated changes and system-forced actions.

### enum_SwitchMode.md enum_SwitchMode.md
This document defines the `SwitchMode` enum for video track switching during playback, particularly for DASH streams. It covers modes like `SMOOTH` and `SEGMENT` for adaptive streaming features.

### enum_ValueType.md enum_ValueType.md
This reference defines the `ValueType` enum for representing value types in multimedia key-value pairs. It covers variants like `INT` and `STRING` for handling typed data in media descriptions.

### enum_VideoScaleType.md enum_VideoScaleType.md
This document defines the `VideoScaleType` enum, specifying how video content is scaled within a display window. It includes options for fitting the window or cropping to maintain aspect ratio.

### enum_VideoSourceType.md enum_VideoSourceType.md
This document defines the `VideoSourceType` enum for specifying video source formats. It details values for surfaces carrying Elementary Stream data or raw data for video recording.

### func_createAVRecorder_3more.md func_createAVRecorder_3more.md
This document details factory functions for creating multimedia recording and transcoding instances. It covers `createAVRecorder`, `createAVTranscoder`, and `createAVScreenCaptureRecorder`, noting instance limits and exceptions.

### func_createMediaSourceWith_2more.md func_createMediaSourceWith_2more.md
This document details functions for creating multimedia instances, specifically `createMediaSourceWithUrl` and `createSoundPool`. It covers support for streaming formats and audio usage behaviors.

### ohosmultimedia_media媒体服务_7more.md ohosmultimedia_media媒体服务_7more.md
This document serves as an API reference for the `ohos.multimedia_media` module. It covers module imports, permissions, and factory functions for creating instances like `AVPlayer` and `AVRecorder`.

### struct_PixelMapParams.md struct_PixelMapParams.md
This document defines the Cangjie struct `PixelMapParams`, which specifies format parameters for generating video thumbnails. It details properties for width and height to ensure proper scaling.

### struct_PlayParameters.md struct_PlayParameters.md
This document defines the `PlayParameters` struct for configuring audio playback settings in `SoundPool`. It details properties for loop, rate, and volume to control playback behavior.

### type_MediaDescription_6more.md type_MediaDescription_6more.md
This document defines type aliases for media playback functionality. It covers data structures for media descriptions and callback handlers for player state changes and buffering updates.

### class_AVImageGenerator/
This directory provides API reference for the `AVImageGenerator` class, focusing on extracting video thumbnails. It covers setting data sources, fetching frames at precise timestamps, and managing system resources to prevent memory leaks.

### class_AVMetadata/
This directory contains documentation for the `AVMetadata` class, defining and managing audio/video metadata fields. It covers descriptive attributes like title and artist, as well as technical specs like duration and video dimensions.

### class_AVMetadataExtractor/
This directory documents the `AVMetadataExtractor` class for retrieving metadata from media resources. It covers configuring data sources, fetching album covers, and resource management via the `release()` function.

### class_AVPlayer/
This directory contains documentation for the `AVPlayer` class, a core component for managing multimedia playback. It covers playback control, track management, event handling, and adaptive bitrate control.

### class_AVRecorder/
This directory provides comprehensive documentation for the `AVRecorder` class. It covers the complete lifecycle of media recording, including configuration, state management, error handling, and recording operations.

### class_AVRecorderConfig/
This directory documents the `AVRecorderConfig` class for configuring recording settings. It details the structural definition, initialization parameters, and management of output file destinations.

### class_AVRecorderProfile/
This directory contains documentation for the `AVRecorderProfile` class, detailing the configuration of audio and video recording settings. It covers container formats, codecs, bitrates, and frame dimensions.

### class_AVScreenCaptureRecordConfig/
This directory provides API documentation for the `AVScreenCaptureRecordConfig` class. It details the setup for audio and video capture, file output management, and encoding specifications for screen recording.

### class_AVScreenCaptureRecorder/
This directory documents the `AVScreenCaptureRecorder` class. It covers the lifecycle of screen recording operations, including initialization, event subscription, recording control, and privacy settings.

### class_AVTranscoder/
This directory provides documentation for the `AVTranscoder` class, designed for video transcoding. It covers configuration, lifecycle management, resource cleanup, and event handling for transcoding workflows.

### class_AVTranscoderConfig/
This directory documents the `AVTranscoderConfig` class for configuring transcoding tasks. It covers initialization, output specifications, and parameter settings for file formats and codecs.

### class_PlaybackStrategy/
This directory documents the `PlaybackStrategy` class for configuring media playback settings. It covers options like video dimensions, HDR mode, and buffer duration management to optimize the media consumption experience.

### class_SoundPool/
This directory contains documentation for the `SoundPool` class, designed for managing short audio clips. It covers loading files, playing sounds with parameters, and managing playback states and events.

### enum_AVErrorCode/
This directory documents the `AVErrorCode` enum, defining error codes for multimedia operations. It covers specific error members and operator functions for precise error handling in media applications.

### enum_AVPlayerCallbackType/
This directory provides documentation for the `AVPlayerCallbackType` enumeration. It defines callback events for `AVPlayer`, including state changes, errors, and buffering progress, essential for event handling.

### enum_AVPlayerState/
This directory documents the `AVPlayerState` enum, detailing the state machine for the `AVPlayer` lifecycle. It explains entry conditions, valid transitions, and error handling mechanisms.

### enum_AVRecorderState/
This directory documents the `AVRecorderState` enum, focusing on the state machine for recording processes. It covers specific recording states, valid lifecycle transitions, and utility functions.

### enum_AVScreenCaptureStateCode/
This directory documents the `AVScreenCaptureStateCode` enum, defining status codes for screen capture sessions. It covers operational states, microphone-related states, and privacy scene transitions.

### struct_MediaDescriptionKey/
This directory documents the `MediaDescriptionKey` struct and static constants. It provides string keys required to access and manipulate multimedia metadata, covering track details and technical specifications.