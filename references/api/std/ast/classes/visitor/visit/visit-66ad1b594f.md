<!-- cj-doc kind="api-member" level="7" id="std.ast.class.visitor.visit.visit-66ad1b594f" parent="std.ast.class.visitor.visit" -->
# Visitor.func visit(LetPatternExpr)

[← Visitor.visit](index.md)

## 签名

```cangjie role=signature
protected open func visit(_: LetPatternExpr): Unit
```

定义访问节点时的操作，需要重写。

## 契约

参数：

- _: LetPatternExpr - LetPatternExpr 类型的被遍历节点。
