<!-- cj-doc kind="guide-leaf" level="5" id="language.function.8-运算符重载.8-4-函数调用运算符" parent="language.function.8-运算符重载" -->
# 8.4 函数调用运算符 `()`

[← 8. 运算符重载](index.md)

- `operator func ()(params): ReturnType` — 任意参数/返回类型
- 不能通过 `this()` 或 `super()` 调用（它们始终引用构造函数）
