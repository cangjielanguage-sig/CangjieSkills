# struct_Path

This directory contains comprehensive API reference documentation for the `Path` struct within the Cangjie programming language's file system package. It covers the struct's definition, properties, methods, and operator overloads designed for representing and manipulating local file system paths. Key topics include path normalization, component extraction (like file names and extensions), parent directory retrieval, and equality comparisons. The documentation details OS-specific behaviors, constraints, and exception handling. It is suitable for developers working on file I/O operations who need precise, cross-platform path manipulation logic using Cangjie syntax.

## Quick Navigation

What do you want to learn?

- Understand the struct definition, constants, and constraints? → struct_Path_4more.md
- Compare two paths for equality? → operator_func_Path.md
- Extract file names or extensions? → prop_fileName_2more.md, struct_Path_4more.md
- Retrieve the parent directory or check if a path is absolute? → prop_parent_5more.md
- Join paths, normalize syntax, or convert to string? → func_isRelative_5more.md

## Detailed Description

### func_isRelative_5more.md func_isRelative_5more.md
This document serves as API reference documentation for the Path struct in the Cangjie file system package, detailing five specific methods. It covers isRelative for checking path types, overloaded join functions for concatenating paths, normalize for cleaning up path syntax, and toString for string conversion. Key information includes function signatures, parameter descriptions, return types, and specific exceptions like IllegalArgumentException and FSException. The text explains normalization rules such as handling redundant separators and resolving parent directory references. This technical guide is designed for developers implementing file path manipulation, requiring knowledge of the Cangjie language syntax and file system concepts.

### operator_func_Path.md operator_func_Path.md
This document serves as API reference documentation for the equality operator overloading of the Path struct in the Cangjie programming language. It details the function signature, parameters, and return type for comparing two Path instances. The key information highlights that equality is determined by comparing the normalized string representations of the paths, ensuring logical equivalence regardless of formatting differences. This documentation is intended for software developers utilizing the file system package who need to perform accurate path comparisons. Keywords: Cangjie, Path struct, equality operator, normalization, API reference, file system.

### prop_fileName_2more.md prop_fileName_2more.md
This document serves as API reference documentation for the Path struct, specifically detailing properties for extracting file name components. It covers the fileName property, which returns the full file name including extension, and fileNameWithoutExtension, which returns the name portion after splitting at the last dot. Key information includes cross-platform and Windows-specific parsing behaviors, such as excluding volume names, and the String return type. Both properties throw an IllegalArgumentException if the path is empty or contains a string terminator. This guide is intended for developers using the Cangjie language to manipulate file system paths programmatically.

### prop_parent_5more.md prop_parent_5more.md
This document serves as API reference documentation for the Cangjie language's file system module, specifically detailing members of the Path struct. It primarily explains the parent property, which retrieves the parent directory path by splitting at the last separator, with specific examples for Unix and Windows systems. Additionally, it documents the init constructor and utility functions including hashCode, isAbsolute, and isEmpty. Key takeaways include the OS-dependent behavior of path parsing and the fact that these operations do not access the filesystem. This guide is intended for developers requiring precise path manipulation logic, handling exceptions like IllegalArgumentException for invalid inputs.

### struct_Path_4more.md struct_Path_4more.md
This document serves as API reference documentation for the Path struct in the Cangjie language, which is used to represent and manipulate local file system paths. It covers the struct's definition, its inheritance from Equatable, Hashable, and ToString, and details specific members like the OS-dependent Separator and ListSeparator constants. Additionally, it explains the extensionName property for retrieving file extensions and defines constraints such as the 4096-byte size limit and criteria for illegal paths involving invalid characters or accessibility issues. This guide is intended for developers seeking to handle file paths across different operating systems, requiring familiarity with Cangjie syntax and basic file system concepts.