# class_TimeZone

This directory contains comprehensive API reference documentation for the `TimeZone` class within the Cangjie programming language's time package. It covers the representation and management of time zones, including system local time, UTC, and custom zones. Key topics include class definition, static properties, initialization constructors, instance methods like `toString`, comparison operators, and static loading functions for system or custom time zone data. This documentation is suitable for Cangjie developers who need to handle time zone conversions, system time settings, and loading IANA time zone data in their applications.

## Quick Navigation

What do you want to learn?

- Understand class definition, properties, and initialization → class_TimeZone_5more.md
- Convert instances to strings or compare equality → func_toString_3more.md
- Load time zones from system, paths, or raw data → static_func_loadString_3more.md

## Detailed Description

### class_TimeZone_5more.md class_TimeZone_5more.md
This document provides API reference documentation for the TimeZone class in the Cangjie programming language, detailing how to represent and manage time zones. The main sections cover the class definition, static properties Local and UTC, the id property, and the initialization constructor. Key information includes the logic for resolving the local time zone via environment variables or system files, the creation of custom time zones using IDs and UTC offsets, and specific exception handling for invalid inputs. This reference guide targets Cangjie developers who need to handle time zone conversions and system time settings within their applications.

### func_toString_3more.md func_toString_3more.md
This document provides API reference details for specific instance methods and operators within the TimeZone class of the Cangjie time package. The main sections cover the toString() method for retrieving the time zone ID, and the inequality != and equality == operators for comparing instance references. Key takeaways include understanding that toString returns a String representing the ID, while the comparison operators return Boolean values based on reference equality rather than value equality. This reference material is intended for Cangjie developers implementing time zone handling or comparing TimeZone instances.

### static_func_loadString_3more.md static_func_loadString_3more.md
This API reference document outlines three static methods for loading TimeZone instances in the Cangjie programming language. The primary sections cover the load(String) function for system-level retrieval, loadFromPaths for loading from specific directories, and loadFromTZData for constructing instances from raw byte arrays. Key information includes OS-specific environment variable handling for CJ_TZPATH, requirements for IANA time zone formats, and detailed exception handling for invalid IDs or data. This documentation serves developers implementing time zone functionality who need to understand loading mechanisms, parameter constraints, and error management.