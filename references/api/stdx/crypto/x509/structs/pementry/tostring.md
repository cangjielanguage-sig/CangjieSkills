<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.pementry.tostring" parent="stdx.crypto.x509.struct.pementry" -->
# PemEntry.toString

[← PemEntry](index.md)

## 签名

```cangjie role=signature
public override func toString(): String
```

返回 PEM 对象的标签和二进制内容的长度。

## 契约

返回值：

- String - PEM 对象的标签和二进制内容的长度。
