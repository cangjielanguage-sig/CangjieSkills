<!-- cj-doc kind="guide-leaf" level="5" id="language.basic_data_type.8-元组类型.8-1-元素访问" parent="language.basic_data_type.8-元组类型" -->
# 8.1 元素访问

[← 8. 元组类型](index.md)

以从零开始的编译期整数字面量访问元组元素，如 `pair[0]`；不支持 `.0`/`.1` 字段语法，动态索引和越界索引都会编译失败。

- `t[index]`，`index` 为编译时整数字面量，从 0 开始
- 越界索引导致编译错误
