<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.stream.class.jsonwriter.flush" parent="stdx.encoding.json.stream.class.jsonwriter" -->
# JsonWriter.flush

[← JsonWriter](index.md)

## 签名

```cangjie role=signature
public func flush(): Unit
```

将缓存中的数据写入 out，并且调用 out 的 flush 方法。
