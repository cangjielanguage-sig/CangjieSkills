<!-- cj-doc kind="api-member" level="5" id="stdx.encoding.hex.func.tohexstring-array-byte" parent="stdx.encoding.hex" -->
# toHexString(Array<Byte>)

[← stdx.encoding.hex](../index.md)

## 签名

```cangjie role=signature
public func toHexString(data: Array<Byte>): String
```

此函数用于将 Byte 数组转换成 Hex 编码的字符串。

## 契约

参数：

- data: Array\<Byte> - 要编码的 Byte 数组。

返回值：

- String - 返回编码后的字符串。
