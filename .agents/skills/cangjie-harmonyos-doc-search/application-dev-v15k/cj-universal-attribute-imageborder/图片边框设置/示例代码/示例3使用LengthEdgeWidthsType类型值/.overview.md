# 示例3使用LengthEdgeWidthsType类型值

This directory provides a comprehensive collection of code examples focused on utilizing the `LengthEdgeWidthsType` type value within the `borderImage` interface for Cangjie UI and ArkUI development. It covers the practical implementation of the EntryView class, detailed state management for border properties such as slice, width, and outset, and the construction of interactive user interfaces. Key topics include dynamically adjusting border image parameters using state variables, configuring the BorderImageOption with specific pixel values, and implementing toggle controls for the borderImageFill property. These examples are specifically designed for developers working with the Cangjie language and the HarmonyOS UI framework who need to implement, customize, and manage complex border image styling and interactive UI components effectively.

## Quick Navigation

What do you want to learn or implement?

*   Learn how to utilize LengthEdgeWidthsType and implement dynamic border image adjustments with sliders and pixel values → 示例3使用LengthEdgeWidthsType类型值_1.md
*   Discover how to add a toggle control to manage the border image fill property and handle UI state changes → 示例3使用LengthEdgeWidthsType类型值_2.md

## Detailed Description

The following files provide specific code implementations and explanations for the topics mentioned above:

### 示例3使用LengthEdgeWidthsType类型值_1.md
This document provides a practical code example demonstrating how to utilize the LengthEdgeWidthsType within the borderImage interface for Cangjie UI development. The code covers the implementation of the EntryView class, state management for border properties (slice, width, outset), and the construction of a user interface with interactive sliders and a selection menu. It illustrates how to dynamically adjust border image parameters using state variables and the BorderImageOption configuration, specifically highlighting the use of pixel values for edge widths. This tutorial is designed for developers working with the Cangjie language and HarmonyOS UI framework who need to implement and customize border image styling.

### 示例3使用LengthEdgeWidthsType类型值_2.md
This document serves as a code example demonstrating the implementation of a toggle control for the borderImageFill property within an ArkUI application. The content focuses on constructing a user interface using Row and Flex containers to center a Text component and a Toggle switch. Key information includes the configuration of the Toggle component with specific colors and an onChange event handler that updates the FillValue state. This example is intended for developers seeking practical guidance on handling UI state changes and border image styling in HarmonyOS development.