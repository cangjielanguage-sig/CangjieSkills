<!-- cj-doc kind="guide-leaf" level="4" id="tools.cjdb.8-仓颉线程" parent="tools.cjdb" -->
# 8. 仓颉线程

[← cjdb 调试器](index.md)

代码展示 `(cjdb) cjthread list              # 列出所有仓颉线程` 的典型用法。

```text
(cjdb) cjthread list              # 列出所有仓颉线程
(cjdb) cjthread backtrace 1       # 查看指定线程调用栈
```

---
