# 到达末尾时新增数据

This directory provides comprehensive technical guidance on implementing and optimizing dynamic data loading within WaterFlow components, specifically focusing on scenarios where new data must be appended as the user scrolls to the bottom. It addresses the complete implementation of infinite scrolling mechanisms, utilizing key ArkUI concepts such as the `onReachEnd` event, `LazyForEach`, and custom data sources. Furthermore, it emphasizes critical performance optimization strategies to prevent UI lag, contrasting efficient data appending with inefficient full dataset reloads. The content is tailored for ArkUI developers and UI engineers utilizing the Cangjie language who aim to create high-performance, responsive applications with smooth scrolling experiences and efficient rendering logic. By following these guides, developers can ensure their applications handle large datasets gracefully without sacrificing user experience.

## Quick Navigation

**What do you want to learn?**
Use the decision tree below to locate the specific file that addresses your development needs regarding data appending and layout performance.

*   How to implement infinite scrolling and append data dynamically? → 到达末尾时新增数据_1.md
*   How to optimize performance and avoid layout lag when adding data? → 到达末尾时新增数据_2.md

## Detailed Description

The following files provide in-depth technical instructions and code examples for managing data updates in waterfall flow layouts.

### 到达末尾时新增数据_1.md 到达末尾时新增数据_1.md
This tutorial explains how to implement infinite scrolling in a WaterFlow component by dynamically adding new data when the user reaches the end. It covers the onReachEnd event, LazyForEach usage, custom data source implementation, and footer styling for loading states. The provided Cangjie code demonstrates appending data to the source and using DataAddOperation for updates, explicitly warning against full dataset reloads to maintain performance. This guide is intended for ArkUI developers using the Cangjie language to build responsive, data-driven user interfaces.

### 到达末尾时新增数据_2.md 到达末尾时新增数据_2.md
This document serves as a technical guide for optimizing performance when appending data to waterfall flow layouts. It covers the structural dependency of waterfall nodes and the inefficiencies of full data reloading. The key takeaway is that reloading all data causes layout recalculation lag due to inconsistent node heights; therefore, developers should append new data and use the `onDatasetChange()` method to notify the component. This approach prevents redundant processing of existing data. Intended for software developers and UI engineers, this guide provides best practices for maintaining smooth scrolling and rendering efficiency in dynamic list interfaces.