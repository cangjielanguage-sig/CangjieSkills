<!-- cj-doc kind="api-member" level="6" id="std.ast.class.reftype.prop-identifier" parent="std.ast.class.reftype" -->
# RefType.identifier

[← RefType](index.md)

## 签名

```cangjie role=signature
public mut prop identifier: Token
```

获取或设置构造 RefType 类型的关键字，如 `var a : A = A()` 中的 `A`。

## 契约

类型：Token
