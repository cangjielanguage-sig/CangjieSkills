# 启动应用内的UIAbility并获取返回结果

This directory provides comprehensive technical guidance on the HarmonyOS Cangjie framework mechanism for launching an in-app UIAbility and retrieving its execution results. It thoroughly details the complete interaction flow, covering the caller's role in initiating requests via `startAbilityForResult` and the callee's responsibility in returning data using `terminateSelfWithResult`. Key technical concepts include `Want` object construction, JSON parameter passing, asynchronous callback handling, and parsing `AbilityResult` return codes. This resource is specifically designed for developers building Stage model applications who require robust inter-ability communication and data return logic within their HarmonyOS projects. It serves as an essential reference for mastering inter-component data exchange and ensuring seamless user experiences across different application modules.

## Quick Navigation

What do you want to learn?

- Learn the standard workflow: initiating the ability and handling termination → 启动应用内的UIAbility并获取返回结果_1.md
- Focus on specific implementation details like JSON parsing and result codes → 启动应用内的UIAbility并获取返回结果_2.md

## Detailed Description

Below are the specific files that outline the implementation details, code structures, and best practices for achieving these functionalities.

### 启动应用内的UIAbility并获取返回结果_1.md 启动应用内的UIAbility并获取返回结果_1.md
This tutorial guides developers on launching an in-app UIAbility and retrieving execution results using the HarmonyOS Cangjie framework. It covers two primary steps: the caller's implementation using startAbilityForResult to initiate the process and handle asynchronous callbacks, and the callee's implementation using terminateSelfWithResult to return data upon termination. Key information includes constructing Want objects, passing parameters via JSON, and parsing AbilityResult return codes. The document is intended for developers building stage model applications who require inter-ability communication and result handling.

### 启动应用内的UIAbility并获取返回结果_2.md 启动应用内的UIAbility并获取返回结果_2.md
This technical tutorial demonstrates how to start an in-app UIAbility and retrieve return results using the Cangjie language in HarmonyOS development. The content covers initiating an ability with startAbilityForResult, passing parameters via Want, and terminating the ability with terminateSelfWithResult. Key takeaways include the implementation of the result callback mechanism to parse returned JSON data and the necessity of matching RESULT_CODE values between the caller and callee. This guide is intended for HarmonyOS application developers working with the Stage model who need to implement inter-page communication and data return logic.