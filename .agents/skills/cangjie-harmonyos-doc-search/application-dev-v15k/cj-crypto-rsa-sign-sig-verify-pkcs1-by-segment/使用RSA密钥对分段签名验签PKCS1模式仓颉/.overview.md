# 使用RSA密钥对分段签名验签PKCS1模式仓颉

This directory serves as a comprehensive technical resource for developers utilizing the Cangjie framework, specifically focusing on the implementation of segmented RSA digital signatures and verification using the PKCS1 mode. It addresses the critical requirement of processing large data volumes efficiently by employing the SHA256 algorithm to handle data in discrete segments. The content covers the complete cryptographic lifecycle, including the generation of 1024-bit RSA key pairs, the initialization of Sign and Verify instances with corresponding private and public keys, and the iterative application of the update method for data processing. This collection is ideally suited for Cangjie developers, security engineers, and software architects aiming to implement robust data integrity checks and secure digital signature capabilities while optimizing memory management for large-scale data processing.

## Quick Navigation

**What do you want to learn?**

*   Understand the theoretical workflow and step-by-step implementation guide → 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md
*   Review practical code examples and test drivers for segmented operations → 示例.md

## Detailed Description

The following files provide in-depth technical details and code implementations for the specified cryptographic processes.

### 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md
This technical tutorial guides developers on implementing segmented RSA signing and verification using PKCS1 mode and SHA256 within the Cangjie framework. The document covers two primary workflows: the Signing process and the Verification process. Key information includes a step-by-step procedure for generating 1024-bit RSA key pairs, creating Sign and Verify instances, initializing them with respective private and public keys, and processing data in segments using the update method before finalizing operations. Targeted at Cangjie developers, this guide requires an understanding of cryptographic APIs and aims to facilitate secure data integrity through digital signatures.

### 示例.md 示例.md
This document provides a Cangjie code example demonstrating how to perform RSA digital signature and verification operations on large messages using segmented processing. It covers three main functions: segmented signing, segmented verification, and a test driver that generates key pairs and validates the workflow. Key takeaways include the use of the CryptoArchitectureKit with RSA1024/PKCS1/SHA256 and the specific technique of calling the update method iteratively to handle data in chunks before finalizing with sign or verify. This tutorial is intended for Cangjie developers implementing cryptographic security features who need to process large data volumes efficiently.