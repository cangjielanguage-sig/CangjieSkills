# UInt64

This directory serves as a comprehensive API documentation hub for the UInt64 type in the Cangjie programming language, focusing on its capabilities as a 64-bit unsigned integer. It covers the fundamental type definition, mathematical constants (Max, Min), and a wide array of interface implementations such as Comparable, Countable, Hashable, and ToString. The content details essential functions for value comparison, positional operations, hash generation, and string formatting. This resource is specifically tailored for Cangjie developers who need to master type behaviors, method signatures, and extensions to effectively handle unsigned 64-bit integers in their software projects.

## Quick Navigation

**What do you want to learn?**

*   Understand the core type definition, math constants, and interfaces (Comparable, Countable, Hashable) → UInt64_5more.md
*   Learn how to convert UInt64 values to human-readable strings → extend_UInt64_ToString.md

## Detailed Description

The following files provide specific details on the type definition and its extensions.

### UInt64_5more.md UInt64_5more.md
This API documentation file defines the UInt64 type and its extensions in the Cangjie programming language, detailing its 64-bit unsigned integer capabilities. The document covers extensions for mathematical constants (Max, Min), the Comparable interface for value comparison, the Countable interface for positional operations, and the Hashable interface for hash generation. Key functions include compare for ordering, next for moving along the number line with overflow handling, and hashCode for hashing. The type supports a range from 0 to 2^64 - 1. This reference guide targets Cangjie developers seeking to understand type behaviors, interfaces, and method signatures for unsigned 64-bit integers.

### extend_UInt64_ToString.md extend_UInt64_ToString.md
This document serves as API reference material for extending the Cangjie language's UInt64 type to support string conversion via the ToString interface. The content covers the extension declaration, the parent interface ToString, and the specific implementation of the public function toString(). The key takeaway is that this extension enables UInt64 values to be easily converted into human-readable String objects for output, utilizing the toString() function which returns the converted string representation. This reference guide targets Cangjie language developers who need to format or output unsigned 64-bit integer values, assuming familiarity with basic syntax and interfaces.