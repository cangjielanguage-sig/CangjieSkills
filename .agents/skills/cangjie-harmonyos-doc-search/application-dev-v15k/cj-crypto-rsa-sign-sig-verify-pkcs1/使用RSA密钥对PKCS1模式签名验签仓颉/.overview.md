# 使用RSA密钥对PKCS1模式签名验签仓颉

This directory serves as a comprehensive resource for developers working with the Cangjie programming language to implement secure digital signatures. It focuses specifically on using RSA key pairs with PKCS1 padding mode and SHA256 hashing. The content includes both theoretical workflow guides and practical code implementations using the CryptoArchitectureKit. It addresses essential tasks such as key pair generation, data signing, signature verification, and handling large data volumes through segmented processing. This collection is ideal for Cangjie developers seeking to enhance application security through asymmetric cryptography.

## Quick Navigation

What do you want to learn?

*   Understand the step-by-step API workflow and theoretical concepts for RSA signing and verification → 使用RSA密钥对PKCS1模式签名验签仓颉_3more.md
*   Review synchronous code examples and specific function implementations for immediate use → 示例.md

## Detailed Description

The following files provide the necessary documentation and code samples to master RSA operations in Cangjie.

### 使用RSA密钥对PKCS1模式签名验签仓颉_3more.md 使用RSA密钥对PKCS1模式签名验签仓颉_3more.md
This technical guide provides step-by-step instructions for implementing RSA digital signatures and verification using the PKCS1 padding mode within the Cangjie programming environment. The document is divided into two primary workflows: Signing and Verification. Both sections detail the sequence of API calls required to process data securely. Key steps include generating an RSA1024 key pair via createAsyKeyGenerator, creating Sign or Verify instances with the parameter string 'RSA1024|PKCS1|SHA256', and managing data input through init, update, and sign or verify methods. It also notes support for segmented processing for large data volumes. This guide targets Cangjie developers integrating cryptographic security features, requiring familiarity with asymmetric encryption concepts and the CryptoArchitectureKit API.

### 示例.md 示例.md
This document provides a synchronous code example demonstrating how to perform digital signing and verification using the CryptoArchitectureKit in the Cangjie programming language. The content covers three main functions: `signMessage` for creating digital signatures, `verifyMessage` for validating them, and a `test` function that orchestrates key generation and the verification process. Key takeaways include the use of the RSA1024 algorithm with PKCS1 padding and SHA256 hashing. The example specifically demonstrates the multi-part update interface for handling data split across multiple inputs. This tutorial is intended for developers using the Cangjie language who need to implement cryptographic security features.