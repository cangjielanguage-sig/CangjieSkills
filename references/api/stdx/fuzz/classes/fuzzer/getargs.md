<!-- cj-doc kind="api-member" level="6" id="stdx.fuzz.fuzz.class.fuzzer.getargs" parent="stdx.fuzz.fuzz.class.fuzzer" -->
# Fuzzer.getArgs

[← Fuzzer](index.md)

## 签名

```cangjie role=signature
public func getArgs(): Array<String>
```

获取 Fuzz 运行参数。

## 契约

返回值：

- Array\<String> - 当前 Fuzz 运行参数。
