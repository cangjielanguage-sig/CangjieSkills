# interface_Hasher

This directory provides comprehensive API reference documentation for the `Hasher` interface within the Cangjie programming language. It focuses on the mechanisms for computing combined hash values across various data types, specifically detailing the interface structure and overloaded methods for inputting unsigned integers. The content is essential for developers who need to implement hashing logic, update hash states, or manage custom data structures. Key topics include the `Hasher` interface definition, `write` function overloads for UInt types, and state management methods like `finish` and `reset`. This documentation is suitable for Cangjie developers looking to integrate robust hashing capabilities into their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `Hasher` interface definition and its general state management methods (finish, reset) → interface_Hasher_13more.md
*   Learn how to input specific unsigned integer types (UInt8, UInt16, UInt32, UInt64) into the hash process → func_writeUInt16_4more.md

## Detailed Description

### func_writeUInt16_4more.md func_writeUInt16_4more.md
This document serves as API reference documentation for a set of overloaded write functions in the Cangjie programming language, designed to input unsigned integer values into a hash combination process. The content covers four specific function overloads for UInt8, UInt16, UInt32, and UInt64 types. Each section details the function signature, describes its purpose in performing hash combination calculations, and defines the input parameter. Key takeaways include that all functions are mutable, accept a single value argument, and return Unit. This reference is intended for Cangjie developers implementing hashing logic who need to understand how to feed different integer sizes into the hash state. Keywords: Cangjie, API documentation, Hash, write function, Unsigned Integers, UInt8, UInt16, UInt32, UInt64.

### interface_Hasher_13more.md interface_Hasher_13more.md
This API reference document defines the Hasher interface in the Cangjie language, designed for computing combined hash values across various data types. The documentation details the interface structure and specific methods, including finish, reset, and multiple overloaded write functions. Key information includes the ability to sequentially input values such as integers, floats, and strings to update the hash state, with finish returning the final Int64 result and reset clearing the state. This guide targets Cangjie developers who need to implement or utilize hashing mechanisms for custom data structures or algorithms.