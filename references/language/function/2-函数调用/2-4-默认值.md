<!-- cj-doc kind="guide-leaf" level="5" id="language.function.2-函数调用.2-4-默认值" parent="language.function.2-函数调用" -->
# 2.4 默认值

[← 2. 函数调用](index.md)

只有命名参数可以声明默认值；调用时省略该参数使用默认表达式，也可按名称显式覆盖。

- 若调用时未提供带默认值的命名参数，则使用默认值
- 若提供了新值，则覆盖默认值

---

## 已验证示例

默认值只能声明在命名参数上；调用时既可省略，也可按名称覆盖，并且命名参数不受调用顺序限制。

```cangjie cjtest=run id=language.default-parameters.run form=unit timeout=20s
package default_parameters_example

func wrap(value: String, prefix!: String = "[", suffix!: String = "]"): String {
    return prefix + value + suffix
}

main(): Unit {
    println(wrap("Cangjie"))
    println(wrap("Cangjie", suffix: ">", prefix: "<"))
}
```

```text cjtest=expect for=language.default-parameters.run stream=stdout match=exact
[Cangjie]
<Cangjie>
```
