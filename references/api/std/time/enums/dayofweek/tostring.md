<!-- cj-doc kind="api-member" level="6" id="std.time.enum.dayofweek.tostring" parent="std.time.enum.dayofweek" -->
# DayOfWeek.toString

[← DayOfWeek](index.md)

## 签名

```cangjie role=signature
public func toString(): String
```

返回当前 DayOfWeek 实例的字符串表示，如 "Monday" 表示周一。

## 契约

返回值：

- String - 当前 DayOfWeek 实例的字符串表示。
