<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjpm.7-包扫描-循环依赖与命令扩展.7-2-循环依赖" parent="tools.cjpm.7-包扫描-循环依赖与命令扩展" -->
# 7.2 循环依赖

[← 7. 包扫描、循环依赖与命令扩展](index.md)

`build`、`check`、`tree` 等解析包依赖的命令会报告循环路径并中止。先根据路径删除无效 `import`；确有相互依赖时，重构为单向依赖或把相关代码合并/拆分到合适的包。
