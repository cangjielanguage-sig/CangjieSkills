<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-lshift_assign" parent="std.ast.enum.tokenkind" -->
# TokenKind.LSHIFT_ASSIGN

[← TokenKind](index.md)

## 签名

```cangjie role=signature
LSHIFT_ASSIGN
```

构造一个表示 `<<=` 的枚举实例。
