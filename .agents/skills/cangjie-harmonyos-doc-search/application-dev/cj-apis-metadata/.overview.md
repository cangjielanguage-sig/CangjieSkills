# cj-apis-metadata

This directory contains the essential API reference documentation for the `cj-apis-metadata` project, specifically focusing on the `ohos.metadata` module within the Cangjie programming language framework. It is a technical resource dedicated to explaining the mechanisms for handling metadata associated with application modules and abilities in the HarmonyOS ecosystem. The documentation covers the structural definition of the `Metadata` class, the required import statements, and step-by-step usage instructions tailored for Cangjie template projects. Key concepts include the management of metadata properties such as name, value, and resource descriptors. This directory is highly suitable for HarmonyOS developers who are actively using the Cangjie language and need to understand how to retrieve and manipulate metadata via `getBundleInfoForSelf` using specific bundle flags. It assumes the reader has a working knowledge of application context and bundle management.

## Quick Navigation

**What do you want to learn?**

*   Understand the core API reference for the `ohos.metadata` module → cj-apis-metadata.md
*   Learn about the `Metadata` class definition and its properties (name, value, resource) → cj-apis-metadata.md
*   Find specific usage instructions for Cangjie template projects → cj-apis-metadata.md
*   Understand how to retrieve metadata using `getBundleInfoForSelf` → cj-apis-metadata.md

## Detailed Description

### cj-apis-metadata.md cj-apis-metadata.md

This document provides comprehensive API reference documentation for the `ohos.metadata` module, which is a critical component for developers working with the Cangjie programming language. It details the technical procedures for handling metadata related to application modules and abilities. The text covers the complete definition of the `Metadata` class, including the necessary import requirements and practical usage instructions for Cangjie template projects. It offers an in-depth look at the class's properties: `name`, `value`, and `resource`, explaining their roles within the application structure. Key information highlighted in the document includes the fact that this class represents metadata that is retrieved via the `getBundleInfoForSelf` method, which must be invoked with specific bundle flags. The documentation clarifies that the class supports string-based names and values, as well as resource descriptors. This resource is specifically targeted at HarmonyOS developers using Cangjie, and it requires a solid understanding of application context and bundle management capabilities to be effectively utilized.