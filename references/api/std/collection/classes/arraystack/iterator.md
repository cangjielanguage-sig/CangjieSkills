<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arraystack.iterator" parent="std.collection.class.arraystack" -->
# ArrayStack<T>.iterator

[← ArrayStack<T>](index.md)

## 签名

```cangjie role=signature
public func iterator(): Iterator<T>
```

返回此 ArrayStack 中元素的迭代器，其顺序为出栈的顺序。

## 契约

返回值：

- Iterator\<T> - 栈中元素的迭代器。
