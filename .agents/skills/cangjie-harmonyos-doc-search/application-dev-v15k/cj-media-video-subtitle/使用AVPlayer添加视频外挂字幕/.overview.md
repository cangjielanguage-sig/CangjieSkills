# 使用AVPlayer添加视频外挂字幕

This directory provides a comprehensive guide for integrating external subtitles into video playback using the AVPlayer framework, specifically tailored for HarmonyOS developers using the Cangjie language. It covers the end-to-end process of managing subtitle sources, including setup via file descriptors or URLs, and controlling display through event callbacks. The documentation highlights essential constraints, such as the necessity of configuring subtitles prior to playback initiation. Key technical elements include resource setup, callback registration and unregistration, and error handling. This collection is ideal for application engineers seeking to enhance their multimedia applications with dynamic, external subtitle capabilities using methods like `addSubtitleFromFd` and `addSubtitleFromUrl`.

## Quick Navigation

What do you want to learn?

*   Understand the development guide, API usage, and configuration constraints → 使用AVPlayer添加视频外挂字幕_2more.md
*   Review a complete code example with class structures and context initialization → 完整示例.md

## Detailed Description

### 使用AVPlayer添加视频外挂字幕_2more.md 使用AVPlayer添加视频外挂字幕_2more.md
This document provides a development guide for integrating external subtitles into video playback using the AVPlayer instance. It outlines the process of setting subtitle sources via file descriptors or URLs and managing subtitle display through event callbacks. Key sections include resource setup, callback registration using the on method, and optional unregistration. Important constraints note that subtitles must be configured before playback begins. The guide targets application developers using the Cangjie language, offering code examples for methods like addSubtitleFromFd and addSubtitleFromUrl to handle multimedia content effectively.

### 完整示例.md 完整示例.md
This document provides a complete code example demonstrating how to implement subtitle functionality for AVPlayer using the Cangjie language. It covers the AVPlayerSubtitleDemo class, detailing methods to set callbacks, add subtitles via URL or file descriptor, and unregister listeners. The code also includes implementations for SubtitleUpdateCallback and error handling, alongside a MainAbility snippet to initialize the necessary UIAbilityContext. Key takeaways involve managing media resources and handling asynchronous events. This tutorial targets HarmonyOS developers building media applications who need to integrate external or packaged subtitle files.