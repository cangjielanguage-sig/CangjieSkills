# 请求动画绘制帧率

This directory focuses on techniques for requesting and controlling animation drawing frame rates within the ArkUI framework using the Cangjie language. It covers the implementation of the `ExpectedFrameRateRange` parameter within `AnimateParam` to optimize rendering performance and visual effects. Key topics include setting minimum, maximum, and expected frame rates for both property and explicit animations. This resource is suitable for HarmonyOS application developers looking to fine-tune animation performance, manage frame rate constraints effectively, and balance visual smoothness with system resource usage in their UI components. By mastering these APIs, developers can ensure their applications run efficiently across different hardware capabilities while maintaining high-quality user experiences.

## Quick Navigation

- **What do you want to learn?**
    - View a complete code example demonstrating property animations with custom frame rate settings → 完整示例.md
    - Read a technical guide on configuring desired drawing frame rates for property and explicit animations → 请求动画绘制帧率_3more.md

## Detailed Description

#### 完整示例.md 完整示例.md
This document provides a complete code example demonstrating how to implement property animations with custom frame rate settings in the ArkUI framework using the Cangjie language. The code illustrates the definition of `AnimateParam` objects, specifically utilizing `ExpectedFrameRateRange` to control animation performance. Key sections include the setup of UI components within a `Column` layout and the implementation of a button click event that triggers `getUIContext().animateTo` to modify translation states. The example highlights how to configure parameters such as duration, iterations, play mode, and target frame rates (e.g., 40Hz or 60Hz). This serves as a practical reference for developers seeking to optimize UI animations in HarmonyOS applications by managing frame rate constraints.

#### 请求动画绘制帧率_3more.md 请求动画绘制帧率_3more.md
This document serves as a technical guide for configuring desired drawing frame rates for animations in ArkUI applications using the Cangjie language. It details the implementation of the ExpectedFrameRateRange parameter within AnimateParam to control rendering performance. The content is divided into two primary sections: setting frame rates for property animations and for explicit animations. Key takeaways include understanding how to define minimum, maximum, and expected values, such as 60Hz or 30Hz, to optimize visual effects. Intended for application developers, this tutorial assumes prior knowledge of ArkUI components and animation basics, providing practical code examples to demonstrate the usage of the frame rate control API.