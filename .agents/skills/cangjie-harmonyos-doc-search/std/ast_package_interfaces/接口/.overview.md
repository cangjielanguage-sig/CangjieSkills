# 接口

This directory serves as a comprehensive repository for API reference documentation regarding core interfaces within the Cangjie programming language. It primarily focuses on standard protocols designed for type conversion and data transformation. The content covers essential interfaces such as `ToBytes`, which handles object serialization into byte sequences, and `ToTokens`, which facilitates the conversion of data types into token objects for metaprogramming purposes. These interfaces are fundamental for enabling interoperability between custom data types and system-level operations like storage, transmission, and compile-time code generation. This documentation is highly suitable for Cangjie developers, language engineers, and system architects who are building robust applications, compiler tools, or advanced metaprogramming features and require a deep technical understanding of how to implement and utilize these standard interfaces effectively.

## Quick Navigation

**What do you want to learn or implement?**

*   **Serialize data:** Learn how to implement the `ToBytes` interface to convert custom objects into `Array<UInt8>` for storage or network transmission → interface_ToBytes.md interface_ToBytes.md
*   **Metaprogramming & AST:** Explore the `ToTokens` interface to transform primitives, strings, and AST nodes into `Tokens` for quote interpolation and compiler tools → [2] interface_ToTokens/

## Detailed Description

The following sections provide in-depth technical specifications for the interfaces contained in this directory.

### interface_ToBytes.md interface_ToBytes.md

This document provides API reference documentation for the Cangjie programming language, specifically detailing the ToBytes interface designed to facilitate type serialization. The main sections include the interface definition and the specific method toBytes, which outlines the function signature and return values. Key information highlights that the interface requires implementing a function returning an Array<UInt8>, representing the serialized byte sequence of the object. This documentation is intended for Cangjie developers looking to implement serialization capabilities for custom data types, enabling data conversion for storage or network transmission. Keywords include Cangjie, interface, serialization, ToBytes, and Array.

### [2] interface_ToTokens/

This directory contains comprehensive API reference documentation for the `ToTokens` interface within the Cangjie language ecosystem. It details the core interface definition alongside numerous extensions that allow a wide variety of data types—including numeric primitives (signed and unsigned), strings, arrays, and AST nodes—to be converted into `Tokens` objects. This functionality is critical for operations such as quote interpolation, metaprogramming, and Abstract Syntax Tree (AST) manipulation. The documentation is specifically designed for Cangjie developers involved in building compiler tools, parsing systems, or advanced metaprogramming features who require a deep understanding of type-to-token conversion mechanisms.