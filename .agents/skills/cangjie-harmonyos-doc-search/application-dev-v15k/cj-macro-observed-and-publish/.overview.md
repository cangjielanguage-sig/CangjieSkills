# cj-macro-observed-and-publish

This directory serves as a specialized technical repository for Cangjie developers working within the ArkUI framework, focusing specifically on advanced state management techniques. It centers on the implementation of the `@Observed` and `@Publish` macros to handle complex data structures, particularly nested class objects and arrays. By addressing the challenges of deep property observation, this guide helps developers overcome the limitations of simpler state management approaches that often fail to trigger UI updates for nested changes. The content covers macro specifications, behavioral mechanisms, common troubleshooting issues, and strict usage constraints. It is essential reading for those building reactive UI applications that require robust two-way data binding and precise state synchronization across intricate data models. This documentation is designed to ensure developers can implement scalable and maintainable reactive architectures in their Cangjie projects, ensuring that UI components remain perfectly synchronized with underlying data logic.

## Quick Navigation

- What do you want to learn?
    - How to manage state for nested class objects and arrays using Cangjie macros? → [1] Observed宏和Publish宏嵌套类对象属性变化/
    - Understanding macro specifications and behavioral mechanisms for reactive UIs? → [1] Observed宏和Publish宏嵌套类对象属性变化/
    - Troubleshooting common issues with `@Observed` and `@Publish`? → [1] Observed宏和Publish宏嵌套类对象属性变化/
    - What are the strict usage constraints for these macros? → [1] Observed宏和Publish宏嵌套类对象属性变化/

## Detailed Description

The following section details the specific subdirectory contained within this repository. It provides in-depth technical documentation regarding the use of macros for state management in the Cangjie language. This resource is critical for understanding how to achieve granular reactivity in applications where data models are not flat but consist of deeply nested objects and arrays. By mastering these concepts, developers can ensure that their user interfaces update automatically and efficiently whenever data changes occur at any depth within the object graph. The documentation included here is structured to guide you through the theoretical underpinnings as well as practical implementation details.

### [1] Observed宏和Publish宏嵌套类对象属性变化/

This directory provides a comprehensive technical guide on using the `@Observed` and `@Publish` macros within the Cangjie and ArkUI frameworks to manage state for nested class objects and arrays. It addresses the complexities of observing deep property changes, overcoming limitations of simpler state management methods. The content covers macro specifications, behavioral mechanisms, common troubleshooting issues, and strict usage constraints. It is designed for Cangjie developers who are building reactive UI applications and need to implement robust two-way data binding and state synchronization for complex data models.