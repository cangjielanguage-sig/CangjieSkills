<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.isglobalunicast" parent="std.net.class.ipaddress" -->
# IPAddress.isGlobalUnicast

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public open func isGlobalUnicast(): Bool
```

判断此 IPAddress 对象是不是全局单播地址。

## 契约

返回值：

- Bool - 返回 true 表示是全局单播地址，否则返回 false。
