<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-not" parent="std.math.numeric.struct.bigint" -->
# BigInt.!

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func !(): BigInt
```

按位非。

## 契约

功能：按位非。将操作数中的二进制位 0 变 1，1 变 0。

返回值：

- BigInt - 返回此 BigInt 按位非的结果。
