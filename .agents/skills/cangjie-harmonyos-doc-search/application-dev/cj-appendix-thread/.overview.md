# cj-appendix-thread

This directory contains the essential API reference documentation for the `cj-appendix-thread` module, designed specifically for managing thread execution in Cangjie-based OpenHarmony applications. It addresses the critical architectural distinction between UI and non-UI logic, providing developers with the necessary tools to explicitly schedule tasks to the UI thread. This capability is vital for preventing severe performance bottlenecks and potential security vulnerabilities that can arise from Cangjie's unique user-space threading model. The documentation comprehensively covers module import requirements, the `UIThread` instance, the `launch` function for task submission, and the `MainThreadContext` class definition. This resource is best suited for OpenHarmony developers utilizing the Cangjie language who require precise control over thread scheduling to ensure application stability, security, and a responsive user interface. Mastering these APIs is key to building robust applications in this environment.

## Quick Navigation

**What do you want to learn?**
*   Understand the core API reference for thread management in Cangjie → cj-appendix-thread.md
*   Learn how to distinguish between UI and non-UI logic effectively → cj-appendix-thread.md
*   Discover how to explicitly schedule tasks to the UI thread to avoid security issues → cj-appendix-thread.md
*   Find details on the module import requirements and setup → cj-appendix-thread.md
*   Explore the `UIThread` instance and its usage in application architecture → cj-appendix-thread.md
*   Understand the `launch` function for submitting tasks to the main thread → cj-appendix-thread.md
*   Review the technical definition of the `MainThreadContext` class → cj-appendix-thread.md

**What do you want to do?**
*   Ensure UI code runs on the correct OS thread in your OpenHarmony app → cj-appendix-thread.md
*   Implement task scheduling using the `launch` function → cj-appendix-thread.md
*   Reference the `MainThreadContext` for context management → cj-appendix-thread.md

## Detailed Description

### cj-appendix-thread.md cj-appendix-thread.md
This document serves as the definitive API reference for managing thread execution in Cangjie-based OpenHarmony applications. It provides a detailed explanation of the distinction between UI and non-UI logic, emphasizing the importance of explicitly scheduling tasks to the UI thread. This is a critical practice for preventing performance bottlenecks and security issues that are inherent to Cangjie's user-space threading model. The document is structured to guide developers through several essential technical areas. It begins with the module import requirements, ensuring the correct setup. It then introduces the `UIThread` instance, which acts as the central point for UI thread management. Furthermore, it details the `launch` function, which is the primary mechanism for submitting tasks to the main thread, and provides the full class definition for `MainThreadContext`. This documentation is indispensable for OpenHarmony developers using Cangjie, as it provides the specific APIs needed to guarantee that UI code is executed on the correct OS thread, thereby ensuring the application remains stable, secure, and responsive.