<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.stream.struct.writeconfig.prop-datetimeformat" parent="stdx.encoding.json.stream.struct.writeconfig" -->
# WriteConfig.dateTimeFormat

[← WriteConfig](index.md)

## 签名

```cangjie role=signature
public mut prop dateTimeFormat: String
```

用于序列化 DateTime 类型时的格式控制，功能与 `DateTime.toString(String)` 一致。

## 契约

功能：用于序列化 DateTime 类型时的格式控制，功能与 `DateTime.toString(String)` 一致。

类型：String
