<!-- cj-doc kind="api-member" level="7" id="std.ast.interface.totokens.totokens.totokens-59e63c44bd" parent="std.ast.interface.totokens.totokens" -->
# ToTokens.func toTokens()

[← ToTokens.toTokens](index.md)

## 签名

```cangjie role=signature
func toTokens(): Tokens
```

实现对应类型的实例到 Tokens 类型的转换。

## 契约

返回值：

- Tokens - 转换后的 Tokens。
