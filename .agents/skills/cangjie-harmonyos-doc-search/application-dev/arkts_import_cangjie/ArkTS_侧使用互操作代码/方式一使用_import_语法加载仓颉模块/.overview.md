# 方式一使用_import_语法加载仓颉模块

This directory provides comprehensive tutorials on integrating Cangjie modules into ArkTS projects using the `import` syntax. It covers various scenarios including loading third-party libraries, static libraries, and local HAR packages, as well as handling complex dependencies where one library relies on another. Key topics include configuring `oh-package.json5`, defining TypeScript declarations in `.d.ts` files, and implementing interoperability functions using `JSContext` and `JSValue`. These guides are essential for HarmonyOS developers seeking to achieve cross-language interoperability between ArkTS and Cangjie, ensuring type safety and seamless function invocation.

## Quick Navigation

**What do you want to do?**

*   Load a standard Cangjie third-party library → 使用_import_语法加载仓颉三方库并调用接口.md
*   Load a Cangjie module with custom interop functions → 使用_import_语法加载仓颉模块并调用接口.md
*   Integrate a Cangjie static library → 使用_import_语法加载仓颉静态库模块并调用接口.md
*   Load a local HAR package containing Cangjie code → 使用_import_语法加载本地_HAR_包并调用仓颉接口.md
*   Manage dependencies where Library A depends on Library B → [Subdirectory]

## Detailed Description

### 使用_import_语法加载仓颉三方库并调用接口.md 使用_import_语法加载仓颉三方库并调用接口.md
This tutorial provides a step-by-step guide for developers to load Cangjie third-party libraries within ArkTS projects using the import syntax. The document details the process of creating a Cangjie module, placing the .so library file, and defining TypeScript interface declarations in Index.d.ts. It explains how to configure dependencies in oh-package.json5 files to link the library and type definitions. Key takeaways include the specific directory structure required and the syntax for importing and calling Cangjie functions, such as addNumber, directly in ArkTS code. This guide targets developers implementing interoperability between ArkTS and Cangjie in HarmonyOS development.

### 使用_import_语法加载仓颉模块并调用接口.md 使用_import_语法加载仓颉模块并调用接口.md
This tutorial explains how to load Cangjie modules and invoke their interfaces in an OpenHarmony ArkTS project using the import syntax. It details the implementation of interop functions in Cangjie, specifically the required (JSContext, JSCallInfo) -> JSValue signature and registration via JSModule. The guide also covers creating TypeScript declarations in .d.ts files, configuring dependencies in oh-package.json5, and the final import and execution in ArkTS code. It serves developers seeking to integrate Cangjie logic into ArkTS applications through native module loading mechanisms.

### 使用_import_语法加载仓颉静态库模块并调用接口.md 使用_import_语法加载仓颉静态库模块并调用接口.md
This tutorial guides developers on integrating Cangjie static libraries into ArkTS projects using the import syntax for cross-language interoperability. It covers creating the Cangjie module, implementing interoperability functions using JSContext and JSValue, defining TypeScript declarations, configuring dependencies in oh-package.json5, and calling the interface in ArkTS. Key steps include registering the module via JSModule, linking the .so library with type definitions, and importing the library by its package name to execute functions like addNumber. This guide is intended for HarmonyOS developers seeking to leverage Cangjie code within ArkTS applications.

### 使用_import_语法加载本地_HAR_包并调用仓颉接口.md 使用_import_语法加载本地_HAR_包并调用仓颉接口.md
This tutorial guides developers on integrating a local HAR package containing a Cangjie shared object into an ArkTS project to enable cross-language function calls. It covers creating Cangjie modules, placing HAR files in the libs directory, defining TypeScript declarations in Index.d.ts, configuring oh-package.json5 for dependencies, and using import syntax. The critical steps involve linking the .so file with type definitions and declaring local file paths in the project's dependency configuration to allow ArkTS to invoke Cangjie interfaces like addNumber. This is intended for HarmonyOS developers working with ArkTS and Cangjie interoperability who need to load local libraries.

### 使用_import_语法加载仓颉三方库_A且三方库_A_依赖三方库_B/
This directory provides a collection of technical guides focused on integrating Cangjie third-party libraries into ArkTS projects using the `import` syntax, specifically addressing complex scenarios where the primary library (Library A) has a dependency on another Cangjie library (Library B). It details multiple implementation strategies, ranging from nested sub-packages to independent HAR module compilation. Key topics include managing cross-language dependencies, configuring build environments like `cjpm.toml` and `oh-package.json5`, and utilizing `JSModule` for interoperability. These resources are tailored for HarmonyOS developers who need to ensure type safety and seamless function exposure when bridging Cangjie and ArkTS code.