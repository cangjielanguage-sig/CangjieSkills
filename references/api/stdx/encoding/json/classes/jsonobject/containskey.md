<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonobject.containskey" parent="stdx.encoding.json.class.jsonobject" -->
# JsonObject.containsKey

[← JsonObject](index.md)

## 签名

```cangjie role=signature
public func containsKey(key: String): Bool
```

判断 JsonObject 中是否存在 key。

## 契约

参数：

- key: String - 指定的 key。

返回值：

- Bool - 存在返回 true，不存在返回 false。
