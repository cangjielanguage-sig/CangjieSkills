# enum_CameraEvents

本目录包含仓颉语言中 `CameraEvents` 枚举的 API 参考文档。它详细定义了用于监听相机事件的多种类型，涵盖相机错误、状态变化、帧捕获生命周期（如 FrameStart、CaptureEnd）、对焦状态、手电筒状态及折叠屏状态变化等。此外，该模块还提供了用于比较枚举值的运算符重载功能。所有功能均属于 SystemCapability.Multimedia.Camera.Core 系统能力，起始版本为 22。该文档主要面向需要处理相机异步事件和状态管理的多媒体应用开发者。

## 快速导航

- **你想了解什么？**
    - 想要查看枚举定义及具体的事件类型（如错误、状态、帧捕获等） → enum_CameraEvent_17more_aea0b324.md
    - 想要了解如何比较两个枚举值是否相等 → func_CameraEvents_b907270c.md

## 详细说明

### enum_CameraEvent_17more_aea0b324.md enum_CameraEvent_17more_aea0b324.md
本文档是仓颉语言的 API 参考文档，定义了用于监听相机事件的 `CameraEvents` 枚举。主要涵盖了相机错误、状态变化、帧捕获生命周期（如 FrameStart、CaptureEnd）、对焦状态、手电筒状态及折叠屏状态变化等多种事件类型。文档详细说明了每个枚举值的功能，并提供了用于比较枚举值的不等运算符。所有功能均属于 SystemCapability.Multimedia.Camera.Core 系统能力，起始版本为 22。该文档主要面向需要处理相机异步事件和状态管理的多媒体应用开发者。

### func_CameraEvents_b907270c.md func_CameraEvents_b907270c.md
本文档详细说明了 `CameraEvents` 枚举类型的相等运算符重载函数，用于判断两个枚举值是否相等。主要内容包括函数定义、功能描述、系统能力要求、起始版本信息、参数说明以及返回值定义。关键信息在于该函数接收一个 `CameraEvents` 类型的参数，返回布尔值结果，且依赖于多媒体相机核心能力，起始版本为 22。面向使用仓颉语言进行多媒体相机应用开发的开发者，适用于需要比较相机事件状态的场景。