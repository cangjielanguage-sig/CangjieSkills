<!-- cj-doc kind="guide-leaf" level="5" id="language.interface.4-默认实现.4-3-在泛型约束中使用静态成员" parent="language.interface.4-默认实现" -->
# 4.3 在泛型约束中使用静态成员

[← 4. 默认实现](index.md)

泛型约束可通过接口声明的静态成员访问类型级能力，调用时由具体类型提供实现。

```cangjie cjtest=syntax id=syntax-69ac06b7d2-1 form=unit
interface NamedType {
    static func typename(): String
}

class A <: NamedType {
    public static func typename(): String { "A" }
}
class B <: NamedType {
    public static func typename(): String { "B" }
}

func printTypeName<T>() where T <: NamedType {
    println("the type is ${T.typename()}")
}

main() {
    printTypeName<A>()  // 输出：the type is A
    printTypeName<B>()  // 输出：the type is B
}
```
