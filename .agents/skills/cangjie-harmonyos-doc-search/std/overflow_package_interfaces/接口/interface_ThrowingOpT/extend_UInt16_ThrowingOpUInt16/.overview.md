# extend_UInt16_ThrowingOpUInt16

This directory contains comprehensive API documentation for extending the `UInt16` type in the Cangjie programming language with the `ThrowingOp` interface. It focuses on a suite of arithmetic and bitwise operations that utilize strict exception handling to prevent silent data corruption or wrapping. Instead of standard behavior, these functions throw specific exceptions like `OverflowException` or `OvershiftException` when limits are breached. This documentation is essential for developers requiring robust error handling in 16-bit unsigned integer calculations, ensuring numerical safety and explicit error management in high-integrity applications.

## Quick Navigation

What do you want to learn?

*   Learn about basic arithmetic operations (Add, Multiply, Divide, Modulo, Increment, Decrement) and their overflow behavior → extend_UInt16_ThrowingOpUI_7more.md
*   Learn about negation, subtraction, and bitwise shift operations (Left Shift, Right Shift) and their specific exceptions → func_throwingNeg_4more.md

## Detailed Description

### extend_UInt16_ThrowingOpUI_7more.md extend_UInt16_ThrowingOpUI_7more.md
This file serves as API documentation specifying the implementation details of the `ThrowingOp` interface specifically tailored for the `UInt16` type within the Cangjie language. The content comprehensively outlines a suite of public functions designed for arithmetic operations, including `throwingAdd`, `throwingDiv`, `throwingMul`, `throwingMod`, `throwingInc`, and `throwingDec`. A critical aspect of this implementation is the enforcement of strict overflow checking; unlike standard operations that might wrap values, these methods throw an `OverflowException` whenever a calculation exceeds the valid 16-bit range. This reference guide is primarily designed for developers who require explicit error handling mechanisms to manage arithmetic overflows safely in unsigned integer calculations.

### func_throwingNeg_4more.md func_throwingNeg_4more.md
This document provides in-depth API reference documentation for four specific arithmetic and bitwise operations applicable to the `UInt16` type in the Cangjie programming language, emphasizing exception-handling strategies. It details the technical specifications for the functions `throwingNeg`, `throwingShl`, `throwingShr`, and `throwingSub`, covering their syntax, input parameters, return values, and specific exception behaviors. Key information highlighted includes that all functions return a `UInt16` result. However, error handling differs by operation type: arithmetic operations such as negation and subtraction will throw an `OverflowException` upon overflow, whereas shift operations will throw an `OvershiftException` if the requested shift amount exceeds the bit width of the operand. This reference is intended for Cangjie developers implementing safe integer arithmetic where error handling via exceptions is a strict requirement.