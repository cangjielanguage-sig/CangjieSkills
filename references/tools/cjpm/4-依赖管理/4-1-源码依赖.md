<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjpm.4-依赖管理.4-1-源码依赖" parent="tools.cjpm.4-依赖管理" -->
# 4.1 源码依赖

[← 4. 依赖管理](index.md)

配置入口：`[dependencies]`。

```toml
[dependencies]
  # 本地路径依赖
  core = { path = "./libs/core" }
  # Git 依赖（分支）
  logger = { git = "https://example.com/logger.git", branch = "main" }
  # Git 依赖（标签）
  utils = { git = "https://example.com/utils.git", tag = "v1.0.0" }
```
