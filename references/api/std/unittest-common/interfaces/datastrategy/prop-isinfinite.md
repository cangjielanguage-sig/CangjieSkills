<!-- cj-doc kind="api-member" level="6" id="std.unittest.common.interface.datastrategy.prop-isinfinite" parent="std.unittest.common.interface.datastrategy" -->
# DataStrategy.isInfinite

[← DataStrategy](index.md)

本页汇总 3 个同名重载；先按签名选择，再读取对应契约。

## prop isInfinite

### 签名

```cangjie role=signature
prop isInfinite: Bool
```

是否无法穷尽。

### 契约

类型：Bool

## prop isInfinite

适用扩展：[extend<T> Array<T> <: DataStrategy<T>](extensions/extend-t-array-t-datastrategy-t.md)。

### 签名

```cangjie role=signature
public prop isInfinite: Bool
```

是否无法穷尽。

### 契约

类型：Bool

## prop isInfinite

适用扩展：[extend<T> Range<T> <: DataStrategy<T>](extensions/extend-t-range-t-datastrategy-t.md)。

### 签名

```cangjie role=signature
public prop isInfinite: Bool
```

是否无法穷尽。

### 契约

类型：Bool
