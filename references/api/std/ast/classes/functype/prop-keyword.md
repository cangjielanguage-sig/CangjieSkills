<!-- cj-doc kind="api-member" level="6" id="std.ast.class.functype.prop-keyword" parent="std.ast.class.functype" -->
# FuncType.keyword

[← FuncType](index.md)

## 签名

```cangjie role=signature
public mut prop keyword: Token
```

获取或设置 FuncType 节点的中的关键字 `CFunc` 的词法单元，若不是一个 `CFunc` 类型，则获取一个 ILLEGAL 的词法单元。

## 契约

类型：Token
