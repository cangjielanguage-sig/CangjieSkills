<!-- cj-doc kind="api-member" level="6" id="std.fs.class.file.flush" parent="std.fs.class.file" -->
# File.flush

[← File](index.md)

## 签名

```cangjie role=signature
public func flush(): Unit
```

将缓冲区数据写入流。

## 契约

功能：将缓冲区数据写入流。由于 File 不存在缓冲区，所以该函数没有具体作用。
