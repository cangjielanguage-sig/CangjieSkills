<!-- cj-doc kind="api-member" level="7" id="std.ast.interface.totokens.totokens.totokens-395a7ee870" parent="std.ast.interface.totokens.totokens" -->
# ToTokens.func toTokens()

[← ToTokens.toTokens](index.md)

## 签名

```cangjie role=signature
public func toTokens(): Tokens
```

实现 String 类型到 Tokens 类型的转换。

适用扩展：[extend String <: ToTokens](../extensions/extend-string-totokens.md)。

## 契约

返回值：

- Tokens - 转换后的 Tokens。
