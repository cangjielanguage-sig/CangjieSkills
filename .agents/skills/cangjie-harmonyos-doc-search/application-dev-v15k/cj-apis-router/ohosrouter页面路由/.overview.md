# ohosrouter页面路由

This directory provides comprehensive API reference documentation for the `ohos.router` module and associated classes within the Cangjie ArkUI framework. It covers essential page routing functionalities, including URL-based navigation, page stack manipulation, and state management. Key topics include the `RouterState` class, `RouterMode` enumeration, and specific lifecycle constraints for routing operations. The documentation highlights strict usage rules, such as the requirement to invoke routing methods only after page rendering. This resource is specifically designed for HarmonyOS developers working in a pure Cangjie environment to implement robust navigation logic and manage page transitions effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the core module API, URL-based navigation, and usage constraints → ohosrouter页面路由_2more.md
*   Learn about page state properties, parameters, and routing modes → class_RouterState_3more.md
*   Explore detailed Router class methods, stack manipulation, and lifecycle management → class_Router/

## Detailed Description

### class_RouterState_3more.md class_RouterState_3more.md
This API reference documentation details the RouterState class and RouterMode enum for Cangjie ArkUI application development. It covers the properties of RouterState (index, name, path, params), its constructor, and the RouterMode enumeration values (Standard and Single). Key information includes that RouterState represents page status within the stack, while RouterMode defines navigation behavior, distinguishing between multi-instance and single-instance page handling. The document includes a code example demonstrating page navigation and transitions. This is intended for Cangjie developers implementing page routing and navigation logic in ArkUI.

### ohosrouter页面路由_2more.md ohosrouter页面路由_2more.md
This document serves as API reference for the ohos.router module, enabling page navigation within HarmonyOS applications using URL-based routing. It covers core functionality including jumping to specific pages, replacing the current page, and returning to previous pages, alongside the necessary import statement. Key takeaways include strict usage constraints: routing methods must be invoked only after page rendering is complete, prohibiting use in onInit or onReady lifecycles. Furthermore, the module exclusively supports pure Cangjie development environments and is incompatible with mixed ArkTS scenarios. This guide targets HarmonyOS developers working in the Cangjie language who need to implement navigation logic.

### class_Router/
This directory contains API reference documentation for the Cangjie Router class within the ArkUI framework. It focuses on managing page routing, navigation, and state management for HarmonyOS applications. Key topics include page stack manipulation, navigation methods like `pushUrl` and `replaceUrl`, parameter passing via JSON strings, and lifecycle management features such as back alerts. The documentation is intended for application developers using the Cangjie language to implement robust navigation logic and handle page transitions effectively in their ArkUI projects.