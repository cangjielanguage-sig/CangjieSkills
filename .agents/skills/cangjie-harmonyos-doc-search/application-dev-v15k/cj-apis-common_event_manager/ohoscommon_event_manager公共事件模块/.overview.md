# ohoscommon_event_manager公共事件模块

This directory contains the API reference documentation for the `ohoscommon_event_manager` module, designed for the Cangjie programming language within the HarmonyOS ecosystem. It covers the essential components for managing common events, including publishing, subscribing, and handling system-level notifications. Key topics include event data structures, subscriber configuration, and system event constants. This documentation is suitable for Cangjie developers and system engineers building event-driven applications or implementing inter-process communication (IPC) using the BasicServicesKit.

## Quick Navigation

**What do you want to do?**

*   Get started with the module and basic setup → ohoscommon_event_manager公共_3more.md
*   Configure subscription parameters and filters → class_CommonEventSubscribeInfo.md
*   Define data types for event information → enum_ValueType.md
*   Prepare data for publishing events → struct_CommonEventPublishData.md
*   Receive and process incoming event data → struct_CommonEventData.md
*   Manage event lifecycle (create, publish, unsubscribe) → `class_CommonEventManager/`
*   Control subscriber behavior and ordered events → `class_CommonEventSubscriber/`
*   Access system-level event constants (Bluetooth, Wi-Fi, etc.) → `struct_Support/`

## Detailed Description

### class_CommonEventSubscribeInfo.md class_CommonEventSubscribeInfo.md
This API reference document outlines the `CommonEventSubscribeInfo` class, used to define subscription parameters for common events in the Cangjie language. It details the class constructor, which requires an array of event names and accepts optional named parameters for filtering, alongside read-only properties for accessing these values. Key properties include events, priority (ranging from -100 to 1000), publisherBundleName, publisherDeviceId, publisherPermission, and userId. The document specifies system capabilities and version requirements, serving developers implementing event subscription logic within the notification framework.

### enum_ValueType.md enum_ValueType.md
This API reference document defines the Cangjie `ValueType` enum, which specifies the data types for additional information attached to public common events. The documentation enumerates supported types, including basic scalar values like `INT`, `STRING`, `BOOL`, `FLOAT64`, and file descriptors (`FD`), as well as their corresponding array variants such as `ARRAYI32` and `ARRSTRING`. Key information includes the system capability `SystemCapability.Notification.CommonEvent` and the requirement of API version 12. This reference is intended for developers working with the system's notification framework who need to structure event data correctly using Cangjie.

### ohoscommon_event_manager公共_3more.md ohoscommon_event_manager公共_3more.md
This document serves as API documentation for the ohos.common_event_manager module within the Cangjie development environment. It outlines the module's primary purpose: enabling the publication, subscription, and unsubscription of common events. Key sections include the module import statement using kit.BasicServicesKit and specific usage instructions for executing example code in index.cj or main_ability.cj. The guide highlights the necessity of configuring the application Context for certain examples. It targets Cangjie developers building HarmonyOS applications, providing essential setup and functional context for event management.

### struct_CommonEventData.md struct_CommonEventData.md
This API reference document defines the `CommonEventData` struct in the Cangjie language, which encapsulates data for common events within the system notification framework. The documentation details five read-only properties: event, bundleName, code, data, and parameters. Key information includes that these fields store values received by subscribers, mirroring the data originally published via CommonEventPublishData. The struct supports integer codes, string data, and a HashMap for additional parameters. Intended for developers implementing event subscription and handling, this document requires knowledge of the CommonEvent system capability and version 12 APIs.

### struct_CommonEventPublishData.md struct_CommonEventPublishData.md
This document provides API reference documentation for the Cangjie struct `CommonEventPublishData`, which defines the content and attributes for publishing common events. It covers the struct's member properties such as bundle name, result code, custom data, ordering flags, sticky event status, subscriber permissions, and additional parameters. Additionally, it details the constructor function, specifying required and optional named parameters with default values. Key information includes the read-only nature of fields and the restriction that only system applications can send sticky events. This reference is intended for developers working with system notification capabilities who need to configure and dispatch common events within their applications.

### class_CommonEventManager/
This directory provides comprehensive API reference documentation for the `CommonEventManager` class within the Cangjie language ecosystem. It is designed to assist developers in managing common events and inter-process communication (IPC) effectively. The content covers the class structure, static methods for creating subscribers, publishing events, and unsubscribing from notifications. Key topics include parameter definitions, return values, system capabilities, and specific `BusinessException` error codes for troubleshooting. This documentation is intended for Cangjie developers utilizing the BasicServicesKit to implement robust event-driven architectures and system notification features.

### class_CommonEventSubscriber/
This directory contains API reference documentation for the `CommonEventSubscriber` class in the Cangjie programming language, specifically designed for the HarmonyOS BasicServicesKit. It covers the management of ordered common events, including data manipulation, event lifecycle control, and subscriber configuration. Key functionalities include retrieving and setting event codes and data, aborting or finishing events, and checking event properties like stickiness. This documentation is suitable for application developers working with the Common Event Service to implement event-driven communication and control event propagation within their HarmonyOS applications.

### struct_Support/
This directory contains API reference documentation for the `Support` struct and related system event constants within the Cangjie programming language environment. It primarily focuses on defining static constant string identifiers for various system-level common events, including Bluetooth states, Wi-Fi operations, storage volume changes, user lifecycle management, and device power states. The documentation provides specific event values, required subscriber permissions, and API version compatibility. It is suitable for application and system developers who need to subscribe to, handle, or broadcast system-level notifications and hardware state changes within the OpenHarmony ecosystem.