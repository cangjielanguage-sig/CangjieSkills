<!-- cj-doc kind="guide-leaf" level="4" id="language.interface.5-泛型成员" parent="language.interface" -->
# 5. 泛型成员

[← 接口](index.md)

- 接口可定义具有 `open` 语义的泛型实例/静态函数
- 实现时泛型参数名称可不同
```cangjie cjtest=syntax id=syntax-3a492ce7e8-1 form=unit
import std.collection.ArrayList

interface M {
    func foo<T>(a: T): T
    static func toString<T>(b: ArrayList<T>): String where T <: ToString
}

class C <: M {
    public func foo<S>(a: S): S { a }  // 泛型参数名可不同
    public static func toString<T>(b: ArrayList<T>) where T <: ToString {
        var res = ""
        for (s in b) {
            res += s.toString()
        }
        res
    }
}
```

---
