# class_FuncDecl

This directory contains comprehensive API documentation for the `FuncDecl` class and associated Abstract Syntax Tree (AST) manipulation utilities within the Cangjie programming language ecosystem. It covers the structural definition of function declaration nodes, including initialization, property management (such as parameters, return types, and syntax tokens), and validation mechanisms. Furthermore, it provides technical details on converting AST nodes to token sequences and traversing the syntax tree using the Visitor pattern. This documentation is specifically designed for compiler developers, language tool creators, and software engineers involved in building parsers or static analysis tools for Cangjie.

## Quick Navigation

**What do you want to learn?**

*   Understand the core structure, properties, and initialization of the `FuncDecl` class → class_FuncDecl_11more.md
*   Learn how to convert AST nodes to tokens or traverse the tree using the Visitor pattern → func_toTokens_2more.md

## Detailed Description

The following files provide in-depth technical specifications for the components listed above.

### class_FuncDecl_11more.md class_FuncDecl_11more.md
This document provides API reference documentation for the `FuncDecl` class, which represents a function definition node within the Abstract Syntax Tree (AST) of the Cangjie programming language. It details the class inheritance from `Decl`, two initialization methods, and various mutable properties such as `block`, `funcParams`, `declType`, and syntax tokens like `lParen` and `rParen`. Key information includes the ability to manipulate function components including the body, parameters, return type, and overload operators. The class includes validation logic that throws `ASTException` for invalid token assignments and offers a utility method `isConst` to check node type. This technical reference is intended for compiler developers or tool builders working with Cangjie ASTs, requiring knowledge of syntax tree structures and token handling.

### func_toTokens_2more.md func_toTokens_2more.md
This API documentation file outlines methods for manipulating Abstract Syntax Tree (AST) nodes within the Cangjie programming language. It specifically details the `toTokens` function, which converts a syntax tree node into a `Tokens` type, and the `traverse` function, which iterates over the node and its children using a `Visitor` pattern. Key takeaways include the return types for these functions and the ability to prematurely halt traversal by overriding the `visit` method and calling `breakTraverse`. This reference guide is intended for developers working on AST parsing or code analysis tools who require precise control over node conversion and tree traversal logic.