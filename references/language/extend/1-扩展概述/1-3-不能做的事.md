<!-- cj-doc kind="guide-leaf" level="5" id="language.extend.1-扩展概述.1-3-不能做的事" parent="language.extend.1-扩展概述" -->
# 1.3 不能做的事

[← 1. 扩展概述](index.md)

扩展不能增加存储字段，不能扩展函数、元组或接口类型，也不能绕过原类型的可见性规则访问私有实现。

1. 添加成员变量（存储属性）
2. 定义无实现体的函数/属性
3. 对扩展成员使用 `open`、`override` 或 `redef`
4. 访问被扩展类型的 `private` 成员
