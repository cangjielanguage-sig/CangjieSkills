<!-- cj-doc kind="api-member" level="6" id="std.unittest.common.class.prettyprinter.prettyprinter-uint64-uint64" parent="std.unittest.common.class.prettyprinter" -->
# PrettyPrinter.PrettyPrinter(UInt64,UInt64)

[← PrettyPrinter](index.md)

## 签名

```cangjie role=signature
public PrettyPrinter(let indentationSize!: UInt64 = 4, let startingIndent!: UInt64 = 0)
```

PrettyPrinter 构造器。

## 契约

参数：

- indentationSize!: UInt64 - 一个缩进的空格数，默认 4 格。
- startingIndent!: UInt64 - 开头的缩进个数，默认 0 个缩进。
