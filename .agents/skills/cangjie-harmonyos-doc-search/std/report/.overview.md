# report

This directory serves as the central repository for documentation regarding the `report` module, with a specific focus on the `diagReport` interface within the Cangjie AST package. It is designed to assist developers in the implementation of custom error reporting mechanisms directly within macro definitions. The documentation provides an in-depth look at the interface's functionality, supported by a comprehensive usage example that covers macro definition, compilation, and execution. It also clarifies the operational boundaries of the interface, particularly its behavior outside of macro expansion. Key concepts include the reporting of `WARNING` or `ERROR` levels on specific `Tokens` and the utilization of native compiler formatting. This guide is essential for Cangjie macro developers who need to validate input tokens and provide user-friendly diagnostic messages to enhance the development experience.

## Quick Navigation

**What do you want to learn about the Cangjie AST reporting capabilities?**

*   How to implement custom error reporting in Cangjie macros? → report.md
*   How to report WARNING or ERROR levels on specific Tokens? → report.md
*   Understanding the behavior of `diagReport` inside vs. outside macro expansion? → report.md
*   How to validate input tokens using the Cangjie AST package? → report.md

## Detailed Description

### report.md report.md

This technical guide offers a comprehensive explanation of the `diagReport` interface in the Cangjie AST package, focusing on its role in implementing custom error reporting within macros. The document details the interface's functionality and provides a full usage example, including macro definition and compilation steps. It also illustrates the interface's behavior when used outside of macro expansion. Key information includes the ability to report `WARNING` or `ERROR` levels on specific `Tokens` using native compiler formatting. Crucially, the guide notes that `diagReport` is only active during macro expansion and functions as a no-op in standard functions. This document is intended for Cangjie macro developers who need to validate input tokens and provide user-friendly diagnostic messages, serving as a critical resource for effective macro development and error handling.