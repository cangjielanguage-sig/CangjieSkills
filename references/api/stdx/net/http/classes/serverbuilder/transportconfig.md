<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.class.serverbuilder.transportconfig" parent="stdx.net.http.class.serverbuilder" -->
# ServerBuilder.transportConfig

[← ServerBuilder](index.md)

## 签名

```cangjie role=signature
public func transportConfig(config: TransportConfig): ServerBuilder
```

设置传输层配置，默认配置详见 TransportConfig 结构体说明。

## 契约

参数：

- config: TransportConfig - 设定的传输层配置信息。

返回值：

- ServerBuilder - 当前 ServerBuilder 的引用。
