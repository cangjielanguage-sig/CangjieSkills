# class_Generators

This directory serves as a comprehensive technical resource for the `Generators` class within the Cangjie language, specifically designed to facilitate property-based testing. It offers in-depth documentation on static factory methods that enable developers to build custom value generators, covering essential operations such as range-based value creation, random selection from arrays, and the transformation of generator outputs. Key topics include the utilization of `RandomSource`, bounds parameters, and the `Arbitrary` interface for efficient type generation. This reference material is highly suitable for software developers and quality assurance professionals who are implementing property tests and require advanced, programmatic control over data generation strategies to ensure thorough and reliable testing outcomes.

## Quick Navigation

What do you want to learn?

- Understand the core Generators class structure and basic factory methods like `generate` and `iterable` → class_Generators_7more.md
- Explore advanced static utility functions for mapping multiple types and selection strategies like `pick` and `weighted` → static_func_mappedT1_T2_T3_5more.md

## Detailed Description

The following files provide specific API details for the components mentioned above.

### class_Generators_7more.md class_Generators_7more.md
This API reference document details the Generators class, designed to assist developers in creating custom value generators for property-based testing in the Cangjie language. The documentation outlines static factory methods such as generate for range-based or closure-based value creation, iterable for random array selection, lookup for utilizing Arbitrary instances, and mapped for transforming generator outputs. Key functions accept parameters like RandomSource, bounds, or transformation bodies to return specific Generator instances. This technical reference targets developers implementing property tests who require custom data generation strategies, leveraging interfaces like Arbitrary to handle type generation and mapping efficiently.

### static_func_mappedT1_T2_T3_5more.md static_func_mappedT1_T2_T3_5more.md
This API documentation file defines static utility functions for constructing and manipulating value generators within a Cangjie property testing framework. The content details overloaded mapped functions for combining three or four arbitrary types into a new generator, alongside pick, single, and weighted functions for various generator selection strategies. Key information includes the use of RandomSource and Arbitrary interfaces to produce Generator instances, facilitating the creation of constant values, random selections, or weighted combinations. This reference guide is intended for developers implementing property-based tests who require advanced control over data generation logic.