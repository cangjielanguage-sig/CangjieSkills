<!-- cj-doc kind="api-member" level="6" id="std.regex.class.regexexception.init" parent="std.regex.class.regexexception" -->
# RegexException.init

[← RegexException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建 RegexException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据异常信息创建 RegexException 实例。

### 契约

参数：

- message: String - 异常提示信息。
