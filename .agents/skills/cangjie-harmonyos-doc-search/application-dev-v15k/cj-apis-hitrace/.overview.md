# cj-apis-hitrace

This directory provides the comprehensive API reference documentation for the `cj-apis-hitrace` module, which is a fundamental tool for enabling process trajectory tracking and performance measurement within Cangjie applications operating on the HarmonyOS platform. The documentation is centered around the `ohos.hitrace` module, offering developers the necessary interfaces to instrument their code for deep analysis. It specifically details the `Hitrace` class, which is marked as deprecated, and explains the usage of its static methods for tracking execution flow. Key functionalities include the ability to mark the beginning and end of tasks using `startTrace` and `finishTrace`—which rely on matching names and IDs—as well as monitoring the real-time value changes of numerical variables through `traceByValue`. This resource is indispensable for Cangjie developers, system architects, and QA engineers who require robust tools for analyzing application performance, debugging complex logic issues, or tracing process lifecycles. It leverages the SystemCapability.HiviewDFX.HiTrace to deliver these system-level tracing capabilities, ensuring that users can effectively diagnose and optimize their applications.

## Quick Navigation

**What do you want to learn?**

*   Access the full API reference for the `ohos.hitrace` module → cj-apis-hitrace.md
*   Understand the concepts of process trajectory tracking and performance measurement → cj-apis-hitrace.md
*   Learn about the specific methods within the deprecated `Hitrace` class → cj-apis-hitrace.md
*   Find instructions on how to mark the start and end of tasks using IDs → cj-apis-hitrace.md
*   Discover how to implement variable monitoring with `traceByValue` → cj-apis-hitrace.md
*   Verify the system capabilities required for HiTrace integration → cj-apis-hitrace.md

## Detailed Description

### cj-apis-hitrace.md

This document serves as the definitive API reference for the `ohos.hitrace` module, designed to facilitate process trajectory tracking and performance measurement in Cangjie applications. It provides a detailed overview of the deprecated `Hitrace` class, with a specific focus on its static methods.

The documentation highlights the following key areas:
*   **Task Tracking:** It explains how to use `startTrace` and `finishTrace` to mark the boundaries of tasks. This process requires matching names and IDs to ensure accurate correlation of events.
*   **Variable Monitoring:** It covers the `traceByValue` method, which is used to monitor and record changes in numerical variables over time.

The module functionality is dependent on `SystemCapability.HiviewDFX.HiTrace`. This guide is intended for Cangjie developers who need to instrument their code for performance analysis and debugging within the HarmonyOS ecosystem.