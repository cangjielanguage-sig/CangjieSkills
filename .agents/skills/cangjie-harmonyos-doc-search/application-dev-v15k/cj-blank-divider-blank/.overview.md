# cj-blank-divider-blank

This directory hosts the API reference documentation for the Blank component, a fundamental UI element within the ArkUI framework designed for the Cangjie programming language. The documentation focuses on the component's ability to automatically fill remaining space along a container's main axis, a critical feature for creating responsive and adaptive layouts. It provides in-depth details on component creation via the `init` function, including the configuration of the `min` parameter, and explores styling options such as the `color` attribute. Additionally, it defines strict usage constraints, ensuring the component functions correctly only within specific parent containers like Row, Column, or Flex. This resource is indispensable for Cangjie developers who need to implement flexible spacing, push elements to the edges of a container, or manage alignment within linear layouts. By explaining the logic behind automatic stretching and size calculation (max(min, size)), it empowers developers to build robust and dynamic user interfaces.

## Quick Navigation

What do you want to learn?

*   Understand the core functionality and automatic space-filling behavior → cj-blank-divider-blank.md
*   Learn how to initialize the component using the `init` function and `min` parameter → cj-blank-divider-blank.md
*   Configure styling attributes, such as the `color` property → cj-blank-divider-blank.md
*   Review usage constraints within Row, Column, or Flex parent containers → cj-blank-divider-blank.md
*   Explore code examples for space occupation and fixed-width filling → cj-blank-divider-blank.md

## Detailed Description

### cj-blank-divider-blank.md cj-blank-divider-blank.md

This API reference document details the Blank component for the ArkUI framework in Cangjie, designed to automatically fill remaining space along a container's main axis.

**Core Functionality and Initialization**
The document covers component creation via the `init` function, which accepts a `min` parameter. It explains the component's automatic stretching behavior unless a specific size is set, with the final size calculated as `max(min, size)`. The text provides code examples demonstrating space occupation and fixed-width filling to illustrate these concepts.

**Styling and Usage Constraints**
In addition to layout behavior, the document describes the `color` attribute for styling. It also outlines specific usage constraints, noting that the Blank component must be used within Row, Column, or Flex parents. This guide is targeted at Cangjie developers building UI layouts, specifically those needing flexible spacing or alignment within linear containers.