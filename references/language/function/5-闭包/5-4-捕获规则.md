<!-- cj-doc kind="guide-leaf" level="5" id="language.function.5-闭包.5-4-捕获规则" parent="language.function.5-闭包" -->
# 5.4 捕获规则

[← 5. 闭包](index.md)

捕获局部 `var` 的闭包不能逃逸，只能立即调用；捕获 `let` 引用对象时可修改其可变成员并安全返回闭包。

1. 被捕获的变量须在闭包定义处**可见**
2. 被捕获的变量须在闭包定义前**已初始化**
3. 若捕获的变量为**引用类型**，其可变实例成员可被修改
4. **捕获 `var` 的闭包不能逃逸**：不能赋给变量、返回、作为参数传递或作为独立表达式 — **只能直接调用**
5. **传递性捕获**：若函数 `f` 调用捕获了 `var` 变量的函数 `g`（该 `var` 非 `f` 的局部变量），则 `f` 也被视为捕获了 `var`，不能逃逸
6. **静态/全局 `var` 变量**不算捕获 — 访问它们的函数仍为一等值

---

## 已验证示例

捕获 `let` 绑定的引用对象可以逃逸，并可修改对象内部状态；直接捕获局部 `var` 的闭包则只能立即调用，不能返回或保存。

```cangjie cjtest=run id=language.closure-state.run form=unit timeout=20s
package closure_state_example

func makeCounter(start: Int64): () -> Int64 {
    let state = CounterState(start)
    return { =>
        state.current += 1
        state.current
    }
}

class CounterState {
    var current: Int64

    public init(current: Int64) {
        this.current = current
    }
}

main(): Unit {
    let next = makeCounter(40)
    println(next())
    println(next())
}
```

```text cjtest=expect for=language.closure-state.run stream=stdout match=exact
41
42
```

下面的闭包直接捕获局部 `var` 并试图逃逸，编译器会拒绝：

```cangjie cjtest=compile id=language.closure-mutable-escape.error form=unit exit=1 timeout=20s
package closure_mutable_escape_error

func invalidCounter(start: Int64): () -> Int64 {
    var current = start
    return { =>
        current += 1
        current
    }
}

main(): Unit {}
```

```text cjtest=expect for=language.closure-mutable-escape.error stream=stderr match=contains
lambda capturing mutable variables needs to be called directly
```
