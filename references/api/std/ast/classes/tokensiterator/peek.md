<!-- cj-doc kind="api-member" level="6" id="std.ast.class.tokensiterator.peek" parent="std.ast.class.tokensiterator" -->
# TokensIterator.peek

[← TokensIterator](index.md)

## 签名

```cangjie role=signature
public func peek(): Option<Token>
```

获取迭代器中的当前值。

## 契约

返回值：

- Option\<Token> - 返回 Option\<Token> 类型，当遍历结束后，返回 None。
