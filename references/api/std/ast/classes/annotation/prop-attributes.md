<!-- cj-doc kind="api-member" level="6" id="std.ast.class.annotation.prop-attributes" parent="std.ast.class.annotation" -->
# Annotation.attributes

[← Annotation](index.md)

## 签名

```cangjie role=signature
public mut prop attributes: Tokens
```

获取或设置 `Attribute` 中设置的属性值，仅用于 `@Attribute`，如 `@Attribute[xxx]` 中的 `xxx`。

## 契约

类型：Tokens
