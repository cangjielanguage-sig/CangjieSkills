# cj-apis-screenshot

The `cj-apis-screenshot` directory is a comprehensive technical repository dedicated to the `ohos.screenshot` module within the HarmonyOS Cangjie language ecosystem. It serves as an essential guide for developers aiming to implement robust screen capture functionalities in their applications. The directory covers a wide array of scenarios, including the retrieval of full-screen images on tablet devices and the handling of selective capture data on 2-in-1 devices. Beyond basic capture logic, it provides in-depth documentation on API references for core classes such as `PickInfo` and `Rect`, alongside critical information on configuration options, required system permissions, and error handling protocols. This resource is specifically curated for software engineers and developers who are actively utilizing ArkUI and ImageKit to build advanced window management features or integrate seamless screen capture capabilities into their software projects.

## Quick Navigation

**What do you want to learn?**

*   Understand the core screen capture APIs and module structure → `ohosscreenshot屏幕截图/`
*   Learn about specific classes like `PickInfo` and `Rect` for defining capture areas → `ohosscreenshot屏幕截图/`
*   Review system permissions and error handling protocols for secure implementation → `ohosscreenshot屏幕截图/`
*   Explore configuration options for tablets and 2-in-1 devices → `ohosscreenshot屏幕截图/`

## Detailed Description

### ohosscreenshot屏幕截图/
This subdirectory serves as the definitive documentation source for the `ohos.screenshot` module within the HarmonyOS Cangjie language ecosystem. It is structured to provide developers with a deep understanding of screen capture functionalities, specifically addressing the nuances of retrieving full-screen images on tablets and the complexities of selective capture data on 2-in-1 devices. The documentation includes extensive API references for fundamental core classes such as `PickInfo` and `Rect`, offering insights into their properties and methods. Furthermore, it details the necessary configuration options for various capture scenarios, lists the required system permissions to ensure secure operation, and provides robust error handling strategies to manage exceptions effectively. This resource is meticulously designed for developers who are implementing window management features or integrating screen capture capabilities into their applications, leveraging the power of ArkUI and ImageKit to create rich user experiences. By utilizing this documentation, developers can ensure their applications handle image data efficiently while adhering to strict system constraints and security protocols.