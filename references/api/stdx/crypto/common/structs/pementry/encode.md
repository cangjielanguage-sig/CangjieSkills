<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.common.struct.pementry.encode" parent="stdx.crypto.common.struct.pementry" -->
# PemEntry.encode

[← PemEntry](index.md)

## 签名

```cangjie role=signature
public func encode(): String
```

返回 PEM 格式的字符串。行结束符将根据当前操作系统生成。

## 返回值

- String - PEM 格式的字符串。

