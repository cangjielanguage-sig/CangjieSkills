<!-- cj-doc kind="api-member" level="6" id="std.argopt.enum.argumentspec.value-long-string-argumentmode-string-unit" parent="std.argopt.enum.argumentspec" -->
# ArgumentSpec.Long(String, ArgumentMode, (String) -> Unit)

[← ArgumentSpec](index.md)

## 签名

```cangjie role=signature
Long(String, ArgumentMode, (String) -> Unit)
```

表示是一个长选项，同时持有一个 `lambda` 回调函数。
