<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509certificaterequest.tostring" parent="stdx.crypto.x509.class.x509certificaterequest" -->
# X509CertificateRequest.toString

[← X509CertificateRequest](index.md)

## 签名

```cangjie role=signature
public override func toString(): String
```

生成证书签名请求名称字符串，包含证书签名请求的使用者信息。

## 契约

返回值：

- String - 证书签名请求名称字符串。
