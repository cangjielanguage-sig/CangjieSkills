# extendT_BoxT_ComparableBoxT_where_T_ComparableT

This directory serves as a comprehensive technical resource for Cangjie developers, containing API documentation that extends the generic `Box` class with full comparison capabilities. The primary focus is on implementing the `Comparable` interface and overloading relational operators, which are conditional on the encapsulated type `T` satisfying the `Comparable` constraint. The documentation thoroughly explains the `compare` function, the resulting `Ordering` enum, and the syntax for various operators including inequality and less-than comparisons. By providing these extensions, the directory enables developers to perform essential operations such as sorting and logical evaluation on boxed data types. This reference material is crucial for anyone working with generic types in Cangjie, ensuring that boxed objects can be compared seamlessly and efficiently while maintaining strict type safety and delegating logic to the underlying instances.

## Quick Navigation

**What do you want to learn?**

*   Understand the core implementation of the `Comparable` interface for the `Box` class, including the `compare` function and basic inequality operators → extendT_BoxT_ComparableBox_5more.md
*   Reference the specific syntax and behavior for equality, greater-than, and greater-than-or-equal operators → operator_func_BoxT_3more.md

## Detailed Description

The files in this section break down the specific extensions and operator overloads required to enable comparison logic for the Box class.

### extendT_BoxT_ComparableBox_5more.md extendT_BoxT_ComparableBox_5more.md
This API documentation details a Cangjie language extension that implements the Comparable interface for the generic Box class, enabling size comparisons. The document covers the extension definition, the compare function returning an Ordering enum, and overloaded operators for inequality and less-than comparisons. Key information specifies that this extension applies only when the encapsulated type T is also Comparable, delegating the comparison logic to the inner instance. Intended for Cangjie developers, this reference facilitates sorting and logical operations on boxed data types.

### operator_func_BoxT_3more.md operator_func_BoxT_3more.md
This document serves as API reference documentation for comparison operators within the generic Box class in the Cangjie programming language. It details three specific operator overloads: equality (==), greater than (>), and greater than or equal to (>=). Each function accepts a Box parameter named that and returns a Bool indicating the result of the comparison between the current object and the argument. This reference is intended for Cangjie developers who need to perform relational comparisons on Box objects, requiring knowledge of generic types and operator overloading.