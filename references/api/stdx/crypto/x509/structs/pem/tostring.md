<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.pem.tostring" parent="stdx.crypto.x509.struct.pem" -->
# Pem.toString

[← Pem](index.md)

## 签名

```cangjie role=signature
public override func toString(): String
```

返回一个字符串，字符串内容是包含每个条目序列的标签。

## 契约

返回值：

- String - 包含每个条目序列的标签的字符串。
