# digest_package_funcs

This directory provides comprehensive documentation for the generic digest functions within the Cangjie `std.crypto.digest` package. It serves as a critical technical resource for developers aiming to implement cryptographic hash computations across diverse data structures. The content details the API specifications for three overloaded `digest` functions, specifically designed to handle `Array<Byte>`, `InputStream`, and `String` inputs. It thoroughly explains the generic constraints `T <: Digest`, the return type `Array<Byte>`, and highlights the deprecation of the String variant. Additionally, the documentation includes practical examples for custom `Digest` implementations. This directory is suitable for Cangjie developers, security engineers, and system architects who require precise syntax, parameter definitions, and usage scenarios to ensure secure and efficient data handling in their software projects.

## Quick Navigation

*   **What do you want to learn?**
    *   Access the complete API reference for generic digest functions → digest_package_funcs.md
    *   Understand the specific parameters and constraints for `Array<Byte>` inputs → digest_package_funcs.md
    *   Learn how to process `InputStream` data for hashing → digest_package_funcs.md
    *   Review the deprecation status and alternatives for `String` inputs → digest_package_funcs.md
    *   Find code examples for implementing custom `Digest` types → digest_package_funcs.md
*   **What is your development task?**
    *   Implementing a hash function for byte arrays → digest_package_funcs.md
    *   Processing stream data for cryptographic hashing → digest_package_funcs.md
    *   Migrating code from the deprecated String input type → digest_package_funcs.md

## Detailed Description

The following section provides a comprehensive breakdown of the files contained within this directory.

### digest_package_funcs.md
This document serves as API reference documentation for generic digest functions within the Cangjie std.crypto.digest package. It details three overloaded `digest` functions that accept `Array<Byte>`, `InputStream`, or `String` inputs to perform hash computations. All functions are generic, constrained by `T <: Digest`, and return an `Array<Byte>`. Key information includes the deprecation of the String variant and examples demonstrating custom `Digest` implementations. The document targets Cangjie developers needing to implement or utilize cryptographic algorithms, providing clear syntax, parameter descriptions, and usage scenarios for handling data in various formats.