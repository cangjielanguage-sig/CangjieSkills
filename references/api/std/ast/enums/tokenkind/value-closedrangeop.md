<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-closedrangeop" parent="std.ast.enum.tokenkind" -->
# TokenKind.CLOSEDRANGEOP

[← TokenKind](index.md)

## 签名

```cangjie role=signature
CLOSEDRANGEOP
```

构造一个表示 `..=` 的枚举实例。
