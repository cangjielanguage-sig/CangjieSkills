# Observed宏和Publish宏嵌套类对象属性变化

This directory provides a comprehensive technical guide on using the `@Observed` and `@Publish` macros within the Cangjie and ArkUI frameworks to manage state for nested class objects and arrays. It addresses the complexities of observing deep property changes, overcoming limitations of simpler state management methods. The content covers macro specifications, behavioral mechanisms, common troubleshooting issues, and strict usage constraints. It is designed for Cangjie developers who are building reactive UI applications and need to implement robust two-way data binding and state synchronization for complex data models.

## Quick Navigation

**What do you want to learn?**

*   Understand the basic specifications and usage of `@Observed` and `@Publish` for nested objects → Observed宏和Publish宏嵌套类对象属性变_4more.md
*   Learn about the strict constraints and inheritance rules for these decorators → 限制条件_2more.md
*   Understand the underlying mechanisms, rendering lifecycle, and array handling → 观察变化和行为表现.md
*   Troubleshoot common errors regarding constructors and nested types → 常见问题.md

## Detailed Description

### Observed宏和Publish宏嵌套类对象属性变_4more.md Observed宏和Publish宏嵌套类对象属性变_4more.md
This technical reference guide explains the usage of the @Observed and @Publish macros in the Cangjie framework to enable observation of nested class object properties and arrays, addressing limitations of simpler state management macros. The document details the specifications for both macros, including restrictions on class inheritance, constructor definitions, and variable initialization rules. Key takeaways include that @Observed marks classes for internal observation, while @Publish facilitates two-way data binding in child components. It targets developers familiar with basic state management who need to handle complex data models.

### 常见问题.md 常见问题.md
This troubleshooting guide addresses common errors encountered when using state management decorators in the Cangjie programming language. It covers three specific scenarios: constructor restrictions for @Observed classes, conditions for @Publish to trigger UI updates, and handling nested custom types. The document explains that @Observed classes cannot have custom constructors, @Publish requires the parent class to be @Observed to work, and nested classes must also be decorated with @Observed and @Publish to propagate changes. It targets Cangjie developers working on reactive UI development, specifically those managing state and component rendering.

### 观察变化和行为表现.md 观察变化和行为表现.md
This technical guide explains the usage and underlying mechanisms of the @Observed and @Publish decorators for state management in Cangjie UI development. It covers observing changes in complex types like nested classes and arrays, specific behaviors with DateTime objects, and the framework's rendering and update lifecycle. Key takeaways include that nested classes require @Observed to track property changes, arrays should use ObservedArray, and the framework automatically manages getter/setter bindings to trigger UI re-renders when @Publish properties change. This document is intended for Cangjie developers implementing reactive UI components who need to understand deep observation and state synchronization.

### 限制条件_2more.md 限制条件_2more.md
This reference document outlines specific constraints and usage scenarios for the @Observed and @Publish decorators in the Cangjie programming language within the ArkUI framework. It covers a list of five strict limitations regarding class inheritance, constructor definitions, variable types, and initialization, followed by a practical usage scenario for nested custom types. Key takeaways include that @Observed classes cannot inherit or define constructors, and @Publish only applies to var members. Crucially, nested objects requiring UI updates must also be decorated with @Observed. This guide targets Cangjie developers implementing reactive state management in UI applications.