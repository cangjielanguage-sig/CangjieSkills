# class_StringWriterT_where_T_OutputStream

This directory provides comprehensive API reference documentation for the Cangjie `StringWriter` class and its associated I/O functions. It focuses on the mechanisms for converting strings and primitive data types into encoded bytes for writing to output streams. Key topics include the class constructor, internal buffer management (flush), and extensive overloads for `write` and `writeln` methods supporting various numeric, string, and generic types. Additionally, it covers interface implementations for Resource and Seekable capabilities. This documentation is suitable for Cangjie developers working on data serialization, stream management, and output operations who require detailed syntax and parameter information.

## Quick Navigation

**What do you want to learn?**

*   Understand the StringWriter class structure, constructor, and buffer management? → class_StringWriterT_where_10more.md
*   Write specific primitive types (Int8, Rune, String, UInts) or generic ToString types? → func_writeInt8_11more.md
*   Output data followed by a newline character for Floats, Ints, and Strings? → func_writelnFloat32_10more.md
*   Explore remaining writeln overloads (UInt64, UInt8) or manage stream resources and seeking? → func_writelnUInt64_5more.md

## Detailed Description

### class_StringWriterT_where_10more.md
This document provides API reference documentation for the Cangjie StringWriter class, designed to convert strings and ToString types into encoded bytes for writing to an output stream. The content details the class constructor, the flush method for buffer management, and multiple overloaded write functions supporting primitive types such as Bool, Float16, Float32, Float64, Int16, Int32, and Int64. Key information includes the presence of a 4096-byte internal buffer and the current limitation to UTF-8 encoding only. This technical reference is intended for developers working with Cangjie I/O operations who need to serialize data to streams.

### func_writeInt8_11more.md
This document serves as API reference documentation for the Cangjie programming language, detailing overloaded functions for writing data to an output stream. The content covers specific implementations of `write` for types including Int8, Rune, String, and various unsigned integers (UInt8, UInt16, UInt32, UInt64), alongside a generic `write` for types implementing the ToString interface. It also documents `writeln` functions that append a newline character for Bool, Float16, and empty calls. Key takeaways include the distinction between standard writes and line-appending writes, and the support for generic type handling via the ToString constraint. This reference is intended for Cangjie developers seeking syntax and parameter details for output operations.

### func_writelnFloat32_10more.md
This document serves as an API reference for the overloaded writeln functions in the Cangjie programming language, detailing how to output various primitive data types followed by a newline character. The content is organized into specific sections for Float32, Float64, Int8, Int16, Int32, Int64, UInt16, UInt32, Rune, and String types. Each section provides the function signature, a brief description of the output behavior, and parameter details. The key takeaway is that writeln accepts a single argument of the specified type and returns Unit. This reference is intended for Cangjie developers implementing standard output operations and requires familiarity with the language's type system.

### func_writelnUInt64_5more.md
This API reference document details input/output functions and class extensions in the Cangjie programming language, specifically focusing on writing data and managing stream resources. It covers overloaded writeln functions for UInt64, UInt8, and generic ToString types, alongside extensions for StringWriter to implement Resource and Seekable interfaces. Key takeaways include the ability to write specific integer types with newlines, automatic resource cleanup via try-with-resource syntax, and stream seeking capabilities for cursor positioning. Intended for Cangjie developers, this documentation requires knowledge of basic IO operations and interface implementation. Keywords: Cangjie, writeln, StringWriter, Resource, Seekable, API.