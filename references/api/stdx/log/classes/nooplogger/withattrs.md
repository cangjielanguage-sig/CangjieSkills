<!-- cj-doc kind="api-member" level="6" id="stdx.log.class.nooplogger.withattrs" parent="stdx.log.class.nooplogger" -->
# NoopLogger.withAttrs

[← NoopLogger](index.md)

## 签名

```cangjie role=signature
public func withAttrs(attrs: Array<Attr>): Logger
```

NOOP 实现。

## 契约

参数：

- attrs: Array\<Attr> - 日志数据键值对。

返回值：

- Logger - Logger
