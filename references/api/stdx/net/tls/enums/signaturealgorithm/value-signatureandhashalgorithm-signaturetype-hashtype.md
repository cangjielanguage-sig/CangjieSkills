<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.enum.signaturealgorithm.value-signatureandhashalgorithm-signaturetype-hashtype" parent="stdx.net.tls.enum.signaturealgorithm" -->
# SignatureAlgorithm.SignatureAndHashAlgorithm(SignatureType, HashType)

[← SignatureAlgorithm](index.md)

## 签名

```cangjie role=signature
SignatureAndHashAlgorithm(SignatureType, HashType)
```

表明哪个签名和哈希算法对会被用于数字签名，自 TLS 1.2 及以后版本，包含签名和哈希算法类型。
