<!-- cj-doc kind="guide-leaf" level="5" id="language.package.4-包导入.4-6-重新导出" parent="language.package.4-包导入" -->
# 4.6 重新导出

[← 4. 包导入](index.md)

- `public import`、`protected import`、`internal import` 重新导出导入的成员
- `private import`（默认）将导入限制为文件内可见
- 包本身不能被重新导出
```cangjie cjtest=syntax id=syntax-4de6993455-1 form=unit
// package a — 重新导出子包的声明
package a
public import a.b.f   // 将 a.b 中的 f 重新导出

public let x = 0
```
```cangjie cjtest=syntax id=syntax-4de6993455-2 form=unit
// package a.b
internal package a.b
public func f(): Int64 { 0 }
```
```cangjie cjtest=syntax id=syntax-4de6993455-3 form=unit
// 外部包可直接通过 package a 访问 f
import a.f  // OK
let _ = f() // OK
```
```cangjie cjtest=syntax id=syntax-4de6993455-4 form=unit
// ❌ 错误：包不能被重新导出
public import a.b  // Error
```

---
