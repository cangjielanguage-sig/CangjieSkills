<!-- cj-doc kind="api-member" level="7" id="std.math.numeric.struct.decimal.init.init-0e43cd3d7f" parent="std.math.numeric.struct.decimal.init" -->
# Decimal.init(UInt64)

[← Decimal.init](index.md)

## 签名

```cangjie role=signature
public init(val: UInt64)
```

通过 64 位无符号整数构建 Decimal 对象。

## 契约

功能：通过 64 位无符号整数构建 Decimal 对象。默认采用精度值为 0，即无限精度进行构建。

参数：

- val: UInt64 - 64 位无符号整数。
