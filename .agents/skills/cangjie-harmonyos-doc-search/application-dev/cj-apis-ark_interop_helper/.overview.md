# cj-apis-ark_interop_helper

This directory contains the essential API reference documentation for the `ohos.ark_interop_helper` module, a critical toolkit designed for Cangjie development within the HarmonyOS ecosystem. The primary focus of this module is to provide robust utility functions that facilitate seamless data type conversion and interoperability between ArkTS and native environments. By covering critical operations such as bidirectional transformations for HashMap and JSValue, application mode detection, and specific type conversion utilities, it enables developers to bridge different runtime contexts effectively. Currently marked as Beta, this module is indispensable for developers who need to manage complex data mappings between JavaScript and native layers. It is specifically tailored for HarmonyOS developers utilizing the Cangjie language who require efficient mechanisms to bridge system components. However, users should note that while the module offers powerful interoperability features, Kit-based imports are not yet supported in this version, requiring developers to adhere to the current import constraints. This documentation ensures that developers have the necessary guidance to implement these features effectively.

## Quick Navigation

*   What do you want to learn?
    *   Access the core API reference for interoperability utilities → cj-apis-ark_interop_helper.md
    *   Understand the implementation of type conversion functions (ArkTS to Napi) → cj-apis-ark_interop_helper.md
    *   Explore methods for bidirectional mapping between HashMap and JSValue → cj-apis-ark_interop_helper.md
    *   Learn how to detect application modes (Stage vs. FA) using helper functions → cj-apis-ark_interop_helper.md

## Detailed Description

### cj-apis-ark_interop_helper.md cj-apis-ark_interop_helper.md

This document serves as the comprehensive API reference for the `ohos.ark_interop_helper` module, providing the necessary structural descriptions and functional details for Cangjie development. It focuses on utility functions that enable data type conversion and interoperability. The documentation details four main functions essential for developers:
*   **arktsValuetoNapiValue**: This function is critical for converting data types between different environments.
*   **isStageMode**: A utility designed for detecting the current application mode, which is vital for adapting logic to different runtime contexts.
*   **mapFromJSValue** and **mapToJSValue**: These functions facilitate the bidirectional transformation of HashMap and JSValue, allowing for complex data structure mapping.

The document highlights that the module is currently in Beta and plays a significant role in bridging ArkTS and native environments. It explicitly notes that Kit-based imports are not yet supported, guiding developers on current limitations. The content is specifically curated for HarmonyOS developers using Cangjie who need to handle complex data mappings between JavaScript and native layers, ensuring they have the correct reference material to implement these features. The document serves as a structural guide, outlining the inputs and outputs required for these functions.