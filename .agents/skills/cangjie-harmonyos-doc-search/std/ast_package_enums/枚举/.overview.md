# 枚举

This directory contains comprehensive documentation for the enumeration types that form the backbone of the Cangjie programming language's compiler infrastructure. It covers essential data structures used for diagnostic reporting, abstract syntax tree (AST) construction, and lexical analysis. The content details specific enums such as `DiagReportLevel` for error handling, `ImportKind` for module management, and the extensive `TokenKind` list for parsing syntax. These definitions are critical for understanding how the language represents source code internally, categorizes errors, and processes imports. This documentation is primarily intended for compiler developers, language tool implementers, and engineers working on static analysis tools or IDE integrations who require precise structural definitions and behavioral specifications of Cangjie's core language constructs.

## Quick Navigation

**What do you want to learn?**

*   **Explore Diagnostic and Import Enums** → enum_DiagReportLevel_2more.md
    *   If you need to understand how error severity is classified or how different import statements (like Single, Alias, or All) are structured within the AST.
*   **Review Lexical Token Definitions** → enum_TokenKind/
    *   If you are working on a parser, lexer, or syntax highlighter and need the complete list of keywords, operators, and literals recognized by the Cangjie compiler.

## Detailed Description

### enum_DiagReportLevel_2more.md enum_DiagReportLevel_2more.md
This API reference document defines two enumeration types used within the Cangjie programming language's diagnostic and AST systems. The text covers the DiagReportLevel enum, which specifies error severity as ERROR or WARNING, and the ImportKind enum, which categorizes import statements like Single, Alias, All, and Multi. Key takeaways include the level() function for converting severity to Int32 and the toString() implementation for import types. This documentation is intended for compiler developers or tool builders requiring detailed structural definitions of language constructs.

### enum_TokenKind/
This directory contains comprehensive documentation for the `TokenKind` enumeration within the Cangjie programming language compiler. It details the lexical tokens used for parsing, including keywords, operators, punctuation, and various literal forms. The documentation is split across multiple files to cover the extensive list of syntax elements systematically. It serves as a critical reference for understanding the language's internal representation of source code. This resource is suitable for compiler developers, language tool implementers, and engineers working on abstract syntax trees (AST) or lexical analysis who need precise definitions of Cangjie's syntax rules and token types.