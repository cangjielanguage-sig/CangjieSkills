# class_AtomicReferenceT_where_T_Object

This directory provides comprehensive API reference documentation for the Cangjie `AtomicReference` class, specifically designed to handle atomic operations for reference types where the generic type `T` is a subclass of `Object`. It covers essential concurrent programming concepts, including thread-safe logic implementation and management of shared object references. The documentation details key methods such as `compareAndSwap`, `load`, `store`, and `swap`, while also addressing recent API changes regarding memory ordering deprecations. This resource is primarily suitable for Cangjie developers and software engineers looking to build robust multi-threaded applications or update existing concurrent codebases to current language standards.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `AtomicReference` class, its constructor, and standard methods → class_AtomicReferenceT_whe_9more.md
*   Review the deprecated `swap` function with explicit memory ordering parameters → func_swapT_MemoryOrder__14ecd038.md

## Detailed Description

### class_AtomicReferenceT_whe_9more.md class_AtomicReferenceT_whe_9more.md

This document provides API reference documentation for the Cangjie `AtomicReference` class, a crucial component for facilitating atomic operations specifically designed for reference types. The content thoroughly details the class constructor and essential methods such as `compareAndSwap`, `load`, `store`, and `swap`, which are fundamental for managing state in concurrent environments. Key information includes the strict requirement that the generic type `T` must be a subclass of `Object`. Additionally, it notes the deprecation of methods allowing explicit memory ordering, steering developers toward default ordering versions for better safety and simplicity. This technical reference is designed for Cangjie developers seeking to implement thread-safe logic and effectively manage concurrent access to object references.

### func_swapT_MemoryOrder__14ecd038.md func_swapT_MemoryOrder__14ecd038.md

This document serves as API reference material for a deprecated atomic swap function within the Cangjie language. It details the function signature, functionality, parameters, and return types with precision. The key information highlights that the function performs a swap operation on an atomic type using a specified memory order, returning the previous value held by the atomic reference. However, the most critical takeaway is the deprecation notice, which instructs developers to migrate to the newer `swap(T)` function to ensure compatibility with future standards. This documentation is intended for software engineers maintaining legacy concurrent code or actively updating to newer language standards.