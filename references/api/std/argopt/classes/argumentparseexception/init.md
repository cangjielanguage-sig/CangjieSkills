<!-- cj-doc kind="api-member" level="6" id="std.argopt.class.argumentparseexception.init" parent="std.argopt.class.argumentparseexception" -->
# ArgumentParseException.init

[← ArgumentParseException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个不带异常信息的实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据异常信息构造异常实例。

### 契约

参数：

- message: String - 异常信息。
