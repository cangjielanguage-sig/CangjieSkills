<!-- cj-doc kind="api-member" level="6" id="std.unittest.struct.perf.prop-name" parent="std.unittest.struct.perf" -->
# Perf.name

[← Perf](index.md)

## 签名

```cangjie role=signature
prop name: String
```

为当前 CPU 计数器提供唯一的显示名称，例如：`Perf(cycles)`。

## 契约

类型：String。
