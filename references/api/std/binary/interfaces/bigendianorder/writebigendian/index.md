<!-- cj-doc kind="api-member-index" level="6" id="std.binary.interface.bigendianorder.writebigendian" parent="std.binary.interface.bigendianorder" -->
# BigEndianOrder<T>.writeBigEndian

[← BigEndianOrder<T>](../index.md)

本重载族包含 13 个签名；直接按表中签名选择，带示例的签名可继续进入详情页。

| 签名 | 功能 |
|---|---|
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-f011b0bfda.md) | 将 T 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-8a6c76de5c.md) | 将 Bool 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-adc3ef75f7.md) | 将 Float16 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-b05bba6588.md) | 将 Float32 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-6b2b28407e.md) | 将 Float64 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-87d3a2fb89.md) | 将 Int16 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-d631d25641.md) | 将 Int32 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-9e239c1903.md) | 将 Int64 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-407343b1f0.md) | 将 Int8 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-59f3f4a3de.md) | 将 UInt16 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-143039d4a3.md) | 将 UInt32 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-d84cfde958.md) | 将 UInt64 值以大端序的方式写入字节数组中。 |
| [`writeBigEndian(buffer: Array<UInt8>): Int64`](writebigendian-267e2fbdb3.md) | 将 UInt8 值以大端序的方式写入字节数组中。 |
