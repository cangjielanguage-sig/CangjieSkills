# 数据库加密

This directory provides a comprehensive technical overview of database encryption mechanisms within the Cangjie development environment and the ArkData framework. It is designed to assist developers in implementing robust data security measures to protect the integrity of stored content. The documentation covers encryption strategies for both Relational and Key-Value databases, detailing the necessary configuration steps such as retrieving the UIAbilityContext, setting up `StoreConfig`, and utilizing `KVOptions`. Key learning points include the fact that encryption is disabled by default and must be explicitly enabled during initialization, as well as the critical constraint that encryption settings are immutable once the database is created. This resource is suitable for application developers seeking to secure sensitive data through practical code examples and detailed configuration guidelines, ensuring that applications meet high security standards.

## Quick Navigation

**What do you want to learn?**

*   Learn about general encryption principles and how to configure Key-Value database encryption using KVOptions → 场景介绍_2more.md
*   Discover the specific steps to implement encryption for Relational databases using StoreConfig and UIAbilityContext → 关系型数据库加密.md

## Detailed Description

### 关系型数据库加密.md 关系型数据库加密.md
This technical guide demonstrates how to implement encryption for relational databases within the Cangjie development environment. The document outlines the process of retrieving the UIAbilityContext and configuring the StoreConfig object to enable data security. Key takeaways include the fact that database encryption is disabled by default and requires setting the encrypt attribute to true during store initialization. Intended for application developers using the ArkData framework, this tutorial provides the necessary code snippets to secure sensitive data stored in relational databases.

### 场景介绍_2more.md 场景介绍_2more.md
This technical guide explains how to implement database encryption to enhance data security and integrity for stored content. It covers general encryption principles applicable to both Key-Value and Relational databases, specifically detailing Key-Value database configuration. Key information includes that encryption is immutable after creation and requires interface access. The document provides Cangjie code examples demonstrating how to retrieve the application context and enable the encrypt parameter within KVOptions. This guide targets developers using the ArkData framework who need to secure sensitive data through database encryption.