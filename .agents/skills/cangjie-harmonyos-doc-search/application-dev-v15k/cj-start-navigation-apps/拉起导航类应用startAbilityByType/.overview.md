# 拉起导航类应用startAbilityByType

This directory provides comprehensive technical documentation for HarmonyOS developers focusing on the integration and configuration of navigation capabilities using the `startAbilityByType` API. It covers the complete workflow, ranging from the client-side invocation of map features—such as route planning, active navigation, and location search—to the server-side configuration required to act as a target service provider. Key technical topics include detailed parameter construction using GCJ-02 coordinates, POI IDs, vehicle types, and the specific `module.json5` configurations needed to expose these features. The content is specifically tailored for developers proficient in the Cangjie language who need to implement or support complex map service interactions within the UIAbility lifecycle.

## Quick Navigation

- **What is your development goal?**
    - **Invoke Navigation Features:** Learn how to use `startAbilityByType` to call up panels for route planning, navigation, or location search, including JSON parameter construction → 拉起导航类应用startAbilityByType_3more.md
    - **Configure as Service Provider:** Learn how to set up your application to receive, parse, and handle external map requests by configuring `module.json5` and `UIAbility` → 目标方开发步骤/

## Detailed Description

### 拉起导航类应用startAbilityByType_3more.md 拉起导航类应用startAbilityByType_3more.md
This technical guide explains how to invoke navigation application extension panels using the startAbilityByType API. It details parameter specifications for three intent scenarios: route planning, navigation, and location search, followed by implementation steps. Key parameters include coordinates using the GCJ-02 system, POI IDs, and vehicle types. The document provides a Cangjie code example demonstrating how to construct the JSON parameter and call the interface. Targeted at developers integrating navigation capabilities, this guide requires knowledge of the Cangjie language and UIAbility context.

### 目标方开发步骤/
This directory serves as a comprehensive resource for HarmonyOS developers tasked with configuring an application to function as a target service provider for map functionalities. It thoroughly covers the necessary development steps, including the configuration of the `module.json5` file to declare specific features such as Navigation, RoutePlan, and PlaceSearch via the `linkFeature` attribute. Furthermore, it details the technical implementation required to parse incoming parameters within the `UIAbility` lifecycle, offering specific parameter tables for different scenarios and Cangjie code examples. This guide is essential for developers who need to enable their applications to seamlessly receive, process, and respond to external map service requests.