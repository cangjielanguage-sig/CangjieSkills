# cj-apis-app-ability-want

This directory provides a specialized collection of API reference documentation tailored for the HarmonyOS Cangjie (CJ) language, focusing specifically on the `Want` class and its associated enumerations. It acts as a technical guide for developers working on the HarmonyOS platform, detailing the mechanisms used to facilitate information exchange between different application components. The content covers a broad spectrum of functionalities, ranging from basic data type definitions and object construction strategies to complex parameter passing and component startup procedures. By exploring the `WantValueType` enumeration and the various properties and methods of the `Want` class, developers can gain a deep understanding of how to manage both explicit and implicit calling scenarios. This documentation is specifically curated for HarmonyOS application developers who aim to leverage the Cangjie language to build seamless communication channels and data interactions between Ability components.

## Quick Navigation

**What do you want to learn?**

*   Understand the underlying data structures and value types supported by Want → Check the `WantValueType` enumeration and definitions in `ohosappabilitywantWant/`
*   Learn how to instantiate and configure Want objects for specific tasks → Refer to object construction and parameter passing guides in `ohosappabilitywantWant/`
*   Discover how to launch components using different invocation methods → Explore explicit and implicit calling scenarios in `ohosappabilitywantWant/`

## Detailed Description

### ohosappabilitywantWant/

This directory contains the essential API reference documentation for the `Want` class and its related enumerations as implemented in the HarmonyOS Cangjie language. The `Want` object serves as the primary carrier for information passing between application components, playing a pivotal role in the application's navigation and interaction logic. The documentation extensively covers core functions such as the definition of data types, the construction of `Want` objects, the passing of parameters, and the initiation of component startup processes. It provides a detailed breakdown of the `WantValueType` enumeration, alongside a comprehensive list of properties and methods belonging to the `Want` class. Additionally, the text elucidates the practical applications of explicit and implicit calling scenarios, distinguishing between direct component targeting and system-resolved intents. This resource is designed to assist HarmonyOS application developers in effectively utilizing the `Want` class to achieve robust communication and data interaction between Ability components, ensuring smooth interoperability within the HarmonyOS ecosystem.