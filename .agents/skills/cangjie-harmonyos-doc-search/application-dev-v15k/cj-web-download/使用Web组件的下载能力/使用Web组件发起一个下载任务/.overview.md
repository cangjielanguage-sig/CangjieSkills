# 使用Web组件发起一个下载任务

This directory provides comprehensive resources for implementing file download functionality within Web components on the HarmonyOS platform. It covers the complete workflow of initiating downloads programmatically, managing download delegates, and handling lifecycle events such as progress updates and error handling. The tutorials demonstrate how to integrate system tools like `DocumentViewPicker` to manage file storage paths effectively. With examples provided in both Cangjie and ArkTS, this collection is essential for developers seeking to enhance their web-based applications with robust file management capabilities.

## Quick Navigation

**What programming language are you using?**

*   Cangjie → 使用Web组件发起一个下载任务_1.md 使用Web组件发起一个下载任务_1.md
*   ArkTS → 使用Web组件发起一个下载任务_2.md 使用Web组件发起一个下载任务_2.md

## Detailed Description

### 使用Web组件发起一个下载任务_1.md 使用Web组件发起一个下载任务_1.md
This technical tutorial explains how to initiate and manage download tasks using the Web component in the Cangjie programming language. The document details the implementation of the `startDownload()` interface and the configuration of a `WebDownloadDelegate` to handle lifecycle events such as progress updates, failures, and completion. Key sections include setting up event listeners, triggering downloads programmatically, and utilizing `DocumentViewPicker` to define a default storage directory via the `UIAbilityContext`. Intended for HarmonyOS developers, this guide provides essential code snippets for integrating file downloading capabilities into web-based applications.

### 使用Web组件发起一个下载任务_2.md 使用Web组件发起一个下载任务_2.md
This document provides a code example demonstrating how to initiate and manage download tasks within a Web component using ArkTS. It covers setting up a download delegate, handling lifecycle events like `onBeforeDownload`, `onDownloadUpdated`, `onDownloadFailed`, and `onDownloadFinish`, and integrating the `DocumentViewPicker` for file path selection. The code illustrates how to capture the user-selected directory via `DocumentViewPicker` and pass the URI path to `webDownloadItem.start` to begin the download, while logging progress and status updates. This tutorial is intended for HarmonyOS developers with knowledge of ArkTS and Web component integration who need to implement custom download handling.