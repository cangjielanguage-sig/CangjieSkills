# cj-apis-ark_interop_helper

This directory hosts the `cj-apis-ark_interop_helper` module, a critical technical repository for developers working within the HarmonyOS ArkUI framework. It focuses on enabling seamless interoperability between ArkTS and native Cangjie code, addressing the complexities of cross-language communication. The module provides essential public auxiliary functions for high-performance applications, specifically targeting data type conversions—such as mapping native HashMap structures to JavaScript values—and managing application contexts across both FA and Stage models. It is tailored for HarmonyOS developers who require robust tools for native-bridge interactions, system capability integration, and optimizing the interface between the ArkTS runtime and native NAPI layers. This documentation is particularly relevant for projects utilizing API version 12 and the SystemCapability.ArkUI.ArkUI.Napi capability, ensuring type safety and data integrity during complex cross-language calls.

## Quick Navigation

**What do you want to learn?**

*   Understand ArkTS and Cangjie interoperability basics → `ohosark_interop_helperArkTS互操作公共辅助功能函数/`
*   Learn about data type conversions (e.g., HashMap to JSValue) → `ohosark_interop_helperArkTS互操作公共辅助功能函数/`
*   Manage application contexts (FA and Stage models) → `ohosark_interop_helperArkTS互操作公共辅助功能函数/`
*   Explore `JSValue` to `napi_value` conversion utilities → `ohosark_interop_helperArkTS互操作公共辅助功能函数/`

## Detailed Description

### ohosark_interop_helperArkTS互操作公共辅助功能函数/

This directory functions as a comprehensive technical repository for the `ohos.ark_interop_helper` module. Its primary purpose is to provide detailed API references that facilitate seamless interoperability between ArkTS and native Cangjie code within the HarmonyOS ArkUI framework. As applications grow in complexity, the need for efficient communication between these layers becomes paramount, and this directory is designed to assist developers in managing those intricate data transformations and context handling requirements.

The documentation included here covers a wide range of public auxiliary functions essential for high-performance application development. A significant portion of the content is dedicated to data type conversions, offering utilities for mapping native HashMap structures to JavaScript values. This ensures that data flows smoothly between the native Cangjie environment and the ArkTS runtime. Furthermore, it provides robust solutions for managing application contexts across different models, specifically supporting both FA (Feature Ability) and Stage models, which is vital for maintaining application state and lifecycle management.

Key technical elements highlighted within this directory include the `mapToJSValue` function, which is central to data mapping, as well as utilities for converting `JSValue` to `napi_value`. It also features context retrieval utilities that simplify the process of accessing necessary environment information. These resources are indispensable for HarmonyOS developers who are focused on native-bridge interactions and system capability integration. By optimizing the interface between the ArkTS runtime and native NAPI layers, this module helps developers ensure type safety and data integrity during cross-language calls, particularly for systems running API version 12 and utilizing the SystemCapability.ArkUI.ArkUI.Napi capability.