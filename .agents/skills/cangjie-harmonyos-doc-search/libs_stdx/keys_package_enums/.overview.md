# keys_package_enums

This directory provides the API reference documentation for enumeration types utilized within the Cangjie keys package, focusing on the critical configurations needed for cryptographic key generation and padding schemes. It thoroughly documents the `Curve` enum, which is essential for selecting Elliptic Curve Digital Signature Algorithm (ECDSA) keys, and the `PadOption` enum, which manages RSA padding settings. The reference material covers a broad spectrum of NIST curves (P224 through P521) and Brainpool curves (BP256 through BP512), providing developers with necessary insights into their respective security levels and performance metrics. Furthermore, it outlines padding modes such as OAEP, PSS, and PKCS1, offering explicit security advice to avoid PKCS1 due to known vulnerabilities, while advocating for the use of OAEP or PSS. This resource is specifically designed for software developers and security engineers who are implementing asymmetric encryption and digital signatures, requiring a deep understanding of cryptographic principles to build secure systems. By providing these definitions, the package ensures that developers can implement standardized and secure cryptographic practices efficiently.

## Quick Navigation

What do you want to learn?

*   **Select Cryptographic Curves**: Determine the appropriate NIST or Brainpool curve for your ECDSA implementation based on security and performance needs → keys_package_enums.md
*   **Configure RSA Padding**: Choose the correct padding mode (OAEP, PSS, or PKCS1) for your encryption or signature operations → keys_package_enums.md
*   **Address Security Vulnerabilities**: Understand the specific risks associated with PKCS1 padding and why modern alternatives are preferred → keys_package_enums.md
*   **Review Enum Definitions**: Access the complete technical definitions and parameters for the `Curve` and `PadOption` enumerations → keys_package_enums.md

## Detailed Description

### keys_package_enums.md

This document serves as the core API reference for the directory, defining the enumeration types required for cryptographic key generation and padding within the Cangjie keys package. It provides a detailed breakdown of the `Curve` enum, which is used for ECDSA key selection, and the `PadOption` enum, which handles RSA padding configuration.

Specifically, the `Curve` enum supports a variety of standards, including NIST curves (ranging from P224 to P521) and Brainpool curves (ranging from BP256 to BP512). The documentation explains the security levels and performance characteristics of each curve to assist developers in making informed choices. Additionally, the `PadOption` enum offers several modes, including OAEP, PSS, and PKCS1. The guide explicitly advises against the use of PKCS1 due to known security vulnerabilities, recommending OAEP or PSS as secure alternatives. This guide is intended for software developers implementing cryptographic operations, assuming they possess knowledge of asymmetric encryption and digital signatures.