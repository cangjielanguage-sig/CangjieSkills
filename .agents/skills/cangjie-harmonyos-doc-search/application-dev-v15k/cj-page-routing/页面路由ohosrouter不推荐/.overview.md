# 页面路由ohosrouter不推荐

This directory provides comprehensive documentation on the `ohos.router` module for page navigation within HarmonyOS and Cangjie applications. It covers essential routing operations, including page jumping, returning to previous or specific pages, passing data, and managing the page stack lifecycle. Additionally, it addresses user experience enhancements such as adding confirmation dialogs before navigation to prevent data loss. Crucially, the content marks this module as not recommended for new development, advising developers to adopt the Navigation component for better functionality. This resource is suitable for HarmonyOS and Cangjie developers maintaining legacy code or seeking to understand routing mechanics before migrating to the recommended framework.

## Quick Navigation

**What do you want to learn or do?**

-   Understand the general usage of `ohos.router` and why it is not recommended → 页面路由ohosrouter不推荐.md
-   Implement page return logic and data retrieval in Cangjie → 页面返回.md
-   Add a confirmation dialog before returning to a page in Cangjie → 页面返回前增加一个询问框.md
-   Learn about page jumping modes, stack limits, and instance management → [页面跳转/]

## Detailed Description

### 页面路由ohosrouter不推荐.md 页面路由ohosrouter不推荐.md
This guide explains the usage of the `ohos.router` module for implementing page navigation, including jumping between pages and passing data. It details specific operations such as page jumping, page returning, and adding a confirmation dialog before returning. Crucially, the document marks this module as not recommended, advising developers to adopt the Navigation component for enhanced functionality and customization capabilities. It targets HarmonyOS application developers, likely those maintaining legacy code or seeking to understand the differences before migrating to the recommended Navigation framework.

### 页面返回.md 页面返回.md
This guide explains how to implement page navigation and data return functionality using the Router module in Cangjie. It covers importing the module, returning to the previous page, returning to a specific page (normal or named), and returning with custom parameters. The document details the `Router.back()` method syntax and demonstrates how to retrieve passed parameters via `Router.getParams()` within the `onPageShow` lifecycle callback. It clarifies that intermediate pages are destroyed from the stack and the target page is not recreated. This tutorial targets Cangjie developers managing page stacks and inter-page communication in UI applications.

### 页面返回前增加一个询问框.md 页面返回前增加一个询问框.md
This tutorial guides developers on implementing a confirmation dialog before page navigation to prevent accidental data loss. It covers two primary approaches: using the system default dialog via the `Router.showAlertBeforeBackPage` method and creating a custom dialog using `PromptAction.showDialog`. The document provides Cangjie code examples demonstrating how to configure messages, handle user callbacks, and trigger the `Router.back` method upon confirmation. Key concepts include event handling, router module imports, and UI interaction logic. This guide is intended for application developers using the Cangjie language and ArkUI framework who need to enhance user experience and data safety during page transitions.

### 页面跳转/
This directory focuses on page navigation mechanisms within Cangjie application development. It comprehensively covers the Router module, detailing jump modes (`pushUrl`, `replaceUrl`), instance management (Standard, Single), and data passing between pages. Key topics include the 32-page stack limit, lifecycle management, and parameter retrieval using `getParams`. The content includes practical code scenarios for use cases like detail views and login flows. This resource is suitable for developers working with the Cangjie language and UIKit framework who need to implement robust navigation logic, manage page transitions, and handle data transfer in their applications.