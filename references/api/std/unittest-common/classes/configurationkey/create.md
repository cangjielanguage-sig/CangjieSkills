<!-- cj-doc kind="api-member" level="7" id="std.unittest.common.class.configurationkey.create" parent="std.unittest.common.class.configurationkey.extension.extend-configurationkey" -->
# ConfigurationKey.create

[← extend ConfigurationKey](extensions/extend-configurationkey.md)

## 签名

```cangjie role=signature
static func create<T>(String)
```

创建 ConfigurationKey。

## 契约

参数：

- name: String - 配置键值的名称。

返回值：

- ConfigurationKey - 创建的配置键值。
