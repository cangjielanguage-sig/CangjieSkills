# 类

This directory provides comprehensive API reference documentation for classes associated with regular expression processing within the Cangjie programming language. It encompasses both active and deprecated components, offering insights into the `Regex` class for modern pattern matching, as well as legacy classes like `Matcher` and `RegexOption`. Key functionalities covered include compiling expressions, performing matches, replacing text, splitting strings, and managing capture groups. Additionally, it addresses specific behaviors such as lazy evaluation and configuration modes. This documentation is essential for Cangjie developers seeking to implement robust text processing, maintain legacy systems, or transition from deprecated regex implementations to current standards.

## Quick Navigation

**What do you want to do?**

*   Work with modern regular expressions (compiling, matching, lazy evaluation) → `class_Regex/`
*   Maintain or understand legacy pattern matching code (scanning, replacing) → `class_Matcher_supdeprecatedsup/`
*   Reference deprecated regex configuration options (modes, flags) → class_RegexOption_supde_18052437.md

## Detailed Description

### class_RegexOption_supde_18052437.md class_RegexOption_supde_18052437.md
This document provides API reference documentation for the deprecated Cangjie class RegexOption, which is used to specify regular expression matching modes. The content covers the class definition, its inheritance from the ToString interface, and methods for initializing instances and configuring specific behaviors such as ignoreCase and multiLine. Key information includes the deprecation notice urging migration to the RegexFlag enum and details on return values for configuration methods. Intended for Cangjie developers, this reference helps users understand legacy regex configuration options and the available methods for modifying matching patterns programmatically.

### class_Matcher_supdeprecatedsup/
This directory contains API reference documentation for the deprecated Cangjie Matcher class and its associated regular expression functions. It covers legacy pattern matching capabilities, including scanning input sequences, finding matches, replacing text, and splitting strings. Key topics include the Matcher constructor, methods like `findAll`, `replace`, and `split`, as well as state management functions. The documentation highlights that this class is deprecated in favor of the Regex class. It is suitable for Cangjie developers maintaining legacy code or migrating existing applications to modern regex implementations.

### class_Regex/
This directory contains API reference documentation for the Cangjie programming language's Regex class. It covers compiling regular expressions, finding matches, replacing strings, and splitting text. Key topics include constructors, the `find` and `matches` methods, lazy evaluation, and capture group handling. The documentation is intended for Cangjie developers who need to implement pattern matching, string manipulation, and text processing logic within their applications.