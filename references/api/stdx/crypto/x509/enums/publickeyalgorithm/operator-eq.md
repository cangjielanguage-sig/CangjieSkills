<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.enum.publickeyalgorithm.operator-eq" parent="stdx.crypto.x509.enum.publickeyalgorithm" -->
# PublicKeyAlgorithm.==

[← PublicKeyAlgorithm](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: PublicKeyAlgorithm): Bool
```

判等。

## 契约

参数：

- other: PublicKeyAlgorithm - 被比较的公钥算法。

返回值：

- Bool - 若公钥算法相同，返回 true；否则，返回 false。
