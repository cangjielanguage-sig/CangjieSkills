<!-- cj-doc kind="api-member" level="6" id="std.ast.class.synchronizedexpr.prop-structuredmutex" parent="std.ast.class.synchronizedexpr" -->
# SynchronizedExpr.structuredMutex

[← SynchronizedExpr](index.md)

## 签名

```cangjie role=signature
public mut prop structuredMutex: Expr
```

获取或设置 SynchronizedExpr 中的 `StructuredMutex` 的对象。

## 契约

类型：Expr
