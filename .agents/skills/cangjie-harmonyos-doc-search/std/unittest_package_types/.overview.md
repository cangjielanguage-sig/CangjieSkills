# unittest_package_types

This directory serves as a comprehensive resource for API reference definitions regarding type aliases within the unittest package. It specifically focuses on the `MeasurementUnitTable`, a critical component designed to facilitate unit conversion for performance test results. By utilizing an array of Float64 and String pairs, this type alias enables the normalization of data through boundary-value pairs. This mechanism is essential for developers who need to automatically scale measurements—such as converting time from nanoseconds to seconds—based on specific thresholds. The documentation is tailored for developers and engineers involved in implementing or analyzing performance tests in the Cangjie programming language, providing them with the necessary tools for precise control over result formatting and unit scaling. It ensures that users can effectively manage and interpret performance metrics with high accuracy.

## Quick Navigation

**What do you want to learn?**

*   Understand the core type alias definitions used in the unittest package → unittest_package_types.md
*   Learn how to implement unit conversion and scaling for performance tests → unittest_package_types.md
*   Review the specific API reference for the `MeasurementUnitTable` type → unittest_package_types.md
*   Explore how boundary-value pairs normalize data in the `Measurement` interface → unittest_package_types.md

## Detailed Description

### unittest_package_types.md unittest_package_types.md

This document provides API reference definitions for type aliases within the unittest package. It details the `MeasurementUnitTable` type alias, defined as an array of Float64 and String pairs. This type facilitates unit conversion for performance test results within the `Measurement` interface by using boundary-value pairs to normalize data. For example, it allows automatic scaling of time measurements from nanoseconds to seconds based on specific thresholds. The content targets developers implementing or analyzing performance tests who require precise control over result formatting and unit scaling. Keywords: type alias, performance testing, unit conversion, API reference, Cangjie.