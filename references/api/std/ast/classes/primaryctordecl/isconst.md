<!-- cj-doc kind="api-member" level="6" id="std.ast.class.primaryctordecl.isconst" parent="std.ast.class.primaryctordecl" -->
# PrimaryCtorDecl.isConst

[← PrimaryCtorDecl](index.md)

## 签名

```cangjie role=signature
public func isConst(): Bool
```

判断是否是一个 `Const` 类型的节点。

## 契约

返回值：

- Bool - 当前节点为 `Const` 类型的节点时，返回 true；反之，返回 false。
