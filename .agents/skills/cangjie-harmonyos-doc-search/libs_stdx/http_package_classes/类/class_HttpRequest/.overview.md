# class_HttpRequest

This directory contains comprehensive API reference documentation for the `HttpRequest` class within the Cangjie programming language's standard library. It serves as a technical guide for developers looking to implement or understand HTTP communication mechanisms. The documentation covers the essential properties and methods required to construct, send, and parse HTTP requests effectively. Key topics include request configuration (headers, body, method), timeout management, protocol versioning, and serialization. It addresses specific behaviors such as the non-concurrent nature of body streams and form data parsing logic. This resource is suitable for software engineers and backend developers working with HTTP protocols who need to manipulate request objects for robust client-server interactions. By detailing attributes like `isPersistent` and `readTimeout`, it provides the necessary tools for fine-tuning network performance and reliability.

## Quick Navigation

**What do you want to learn?**

*   Understand the core structure and main properties of the HttpRequest class → class_HttpRequest_11more.md
*   Learn about request configuration, body handling, and form data parsing rules → class_HttpRequest_11more.md
*   Inspect HTTP protocol versions or configure write operation timeouts → prop_version_3more.md
*   Serialize request objects into strings for debugging or logging purposes → prop_version_3more.md

## Detailed Description

### class_HttpRequest_11more.md class_HttpRequest_11more.md
This document provides API reference documentation for the HttpRequest class in the Cangjie programming language, detailing properties used to construct and handle HTTP requests. The content lists key properties including body, headers, method, url, isPersistent, form, and readTimeout. It explains that this class is utilized by clients to send encoded requests and by servers to parse incoming requests. Key takeaways include the non-concurrent nature of the body stream, the logic for determining body size, and specific parsing rules for form data based on content type. This reference is intended for developers working with HTTP protocols who need to manipulate request objects and understand their attributes.

### prop_version_3more.md prop_version_3more.md
This document serves as API reference documentation for specific properties and a function within an HTTP request object in the Cangjie programming language. It covers three main components: the version property for retrieving the HTTP protocol version, the writeTimeout property for managing request-level write durations, and the toString function for serializing the request into a string format. Key details include that the version property is read-only and returns a Protocol enum, while writeTimeout accepts an optional Duration. The toString method provides a comprehensive string representation including headers, body size, and trailers. This reference is intended for developers using the Cangjie HTTP library who need to inspect request metadata, configure timeouts, or debug requests via string conversion.