<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.operator-eq" parent="std.net.class.ipaddress" -->
# IPAddress.==

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public operator func ==(rhs: IPAddress): Bool
```

判断两个 IPAddress 对象是否相等。

## 契约

参数：

- rhs: IPAddress - 参与比较的 IPAddress 对象。

返回值：

- Bool - 如果两个 IPAddress 对象相等，则返回 `true`；否则，返回 `false`。
