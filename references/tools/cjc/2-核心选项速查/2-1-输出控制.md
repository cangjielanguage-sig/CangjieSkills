<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjc.2-核心选项速查.2-1-输出控制" parent="tools.cjc.2-核心选项速查" -->
# 2.1 输出控制

[← 2. 核心选项速查](index.md)

命令：`cjc tool.cj --output-type=dylib`。

| 选项 | 说明 |
|------|------|
| `-o <file>` | 指定输出文件路径 |
| `--output-type=[exe\|staticlib\|dylib]` | 输出类型（默认 `exe`） |
| `--output-dir <dir>` | 中间文件与输出保存目录 |

```bash
cjc tool.cj --output-type=dylib           # 生成动态库 libtool.so
cjc tool.cj --output-type=staticlib       # 生成静态库 libtool.a
```
