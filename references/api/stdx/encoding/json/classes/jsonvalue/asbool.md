<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonvalue.asbool" parent="stdx.encoding.json.class.jsonvalue" -->
# JsonValue.asBool

[← JsonValue](index.md)

## 签名

```cangjie role=signature
public func asBool(): JsonBool
```

将 JsonValue 转换为 JsonBool 格式。

## 契约

返回值：

- JsonBool - 转换后的 JsonBool。

异常：

- JsonException - 如果转换失败，抛出异常。
