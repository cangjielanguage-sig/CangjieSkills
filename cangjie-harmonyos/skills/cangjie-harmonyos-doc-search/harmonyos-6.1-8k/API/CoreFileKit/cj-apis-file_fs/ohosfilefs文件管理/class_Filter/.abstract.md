本目录包含仓颉语言中 `Filter` 类的 API 参考文档，主要用于配置 `listFile` 接口的文件过滤规则。内容涵盖了类的构造函数以及 `suffix`、`displayName`、`fileSizeOver` 等关键属性，详细说明了字段类型、读写权限及匹配逻辑（如 OR 关系和通配符支持）。文档特别指出了 `mimeType` 和 `excludeMedia` 等暂不支持的预留字段。该目录适用于具备 FileIO 系统能力的开发者，旨在帮助其在版本 22+ 的系统中实现高效的文件筛选功能。