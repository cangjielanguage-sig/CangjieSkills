# Flex布局

This directory provides comprehensive API reference documentation for implementing Flex layouts within the ArkUI framework using the Cangjie language. It covers essential properties and functions required for controlling component sizing, scaling behavior, and alignment within parent containers such as Flex, Column, Row, and GridRow. Key topics include `flexBasis`, `flexGrow`, `flexShrink`, and `alignSelf`. This documentation is specifically suitable for Cangjie developers and UI engineers aiming to build responsive, adaptive interfaces by managing space allocation and cross-axis alignment programmatically. It serves as a technical guide for understanding the constraints and default values associated with these layout properties.

## Quick Navigation

What do you want to learn?

*   Control component sizing and scaling (flexBasis, flexGrow, flexShrink) → Flex布局_6more.md
*   Override child component alignment on the cross axis → func_alignSelfItemAlign_2more.md

## Detailed Description

### Flex布局_6more.md Flex布局_6more.md
This document provides API reference documentation for Flex layout properties in the ArkUI framework using the Cangjie language. It details functions for controlling component sizing, specifically `flexBasis`, `flexGrow`, and `flexShrink`, including overloads for both Float64 and Int64 types. Key information includes parameter definitions, default values, and specific constraints, such as the requirement for parent components to be Flex, Column, Row, or GridRow. The text highlights that `flexShrink` behavior varies by container type and requires explicit main axis sizing in certain contexts. This guide targets developers implementing responsive UI layouts who need to manage space allocation and component scaling programmatically.

### func_alignSelfItemAlign_2more.md func_alignSelfItemAlign_2more.md
This document provides API reference documentation for the Cangjie function `alignSelf`, which controls child component alignment on the cross axis within parent containers. It details the function signature, system capabilities, and the ItemAlign parameter, which overrides the `alignItems` setting in Flex, Column, Row, and GridRow layouts. The document includes a practical code example demonstrating how `alignSelf` modifies alignment behavior within a Flex container, specifically showing how it overrides the parent's `alignItems` property. Targeted at Cangjie developers, this guide is essential for precise UI layout control in ArkUI applications.