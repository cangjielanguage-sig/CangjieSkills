<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonexception.init" parent="stdx.encoding.json.class.jsonexception" -->
# JsonException.init

[← JsonException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个不包含任何异常提示信息的 JsonException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据指定的异常提示信息构造 JsonException 实例。

### 契约

参数：

- message: String - 指定的异常提示信息。
