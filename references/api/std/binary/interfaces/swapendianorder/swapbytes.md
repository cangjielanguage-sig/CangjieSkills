<!-- cj-doc kind="api-member" level="6" id="std.binary.interface.swapendianorder.swapbytes" parent="std.binary.interface.swapendianorder" -->
# SwapEndianOrder<T>.swapBytes

[← SwapEndianOrder<T>](index.md)

本页汇总 9 个同名重载；先按签名选择，再读取对应契约。

## func swapBytes()

### 签名

```cangjie role=signature
func swapBytes(): T
```

反转 T 值的字节顺序。

### 契约

返回值：

- T - T 值。

## func swapBytes()

适用扩展：[extend Int16 <: SwapEndianOrder<Int16>](extensions/extend-int16-swapendianorder-int16.md)。

### 签名

```cangjie role=signature
public func swapBytes(): Int16
```

反转 Int16 值的字节顺序。

### 契约

返回值：

- Int16 - Int16 值。

## func swapBytes()

适用扩展：[extend Int32 <: SwapEndianOrder<Int32>](extensions/extend-int32-swapendianorder-int32.md)。

### 签名

```cangjie role=signature
public func swapBytes(): Int32
```

反转 Int32 值的字节顺序。

### 契约

返回值：

- Int32 - Int32 值。

## func swapBytes()

适用扩展：[extend Int64 <: SwapEndianOrder<Int64>](extensions/extend-int64-swapendianorder-int64.md)。

### 签名

```cangjie role=signature
public func swapBytes(): Int64
```

反转 Int64 值的字节顺序。

### 契约

返回值：

- Int64 - Int64 值。

## func swapBytes()

适用扩展：[extend Int8 <: SwapEndianOrder<Int8>](extensions/extend-int8-swapendianorder-int8.md)。

### 签名

```cangjie role=signature
public func swapBytes(): Int8
```

反转 Int8 值的字节顺序。

### 契约

返回值：

- Int8 - Int8 值。

## func swapBytes()

适用扩展：[extend UInt16 <: SwapEndianOrder<UInt16>](extensions/extend-uint16-swapendianorder-uint16.md)。

### 签名

```cangjie role=signature
public func swapBytes(): UInt16
```

反转 UInt16 值的字节顺序。

### 契约

返回值：

- UInt16 - UInt16 值。

## func swapBytes()

适用扩展：[extend UInt32 <: SwapEndianOrder<UInt32>](extensions/extend-uint32-swapendianorder-uint32.md)。

### 签名

```cangjie role=signature
public func swapBytes(): UInt32
```

反转 UInt32 值的字节顺序。

### 契约

返回值：

- UInt32 - UInt32 值。

## func swapBytes()

适用扩展：[extend UInt64 <: SwapEndianOrder<UInt64>](extensions/extend-uint64-swapendianorder-uint64.md)。

### 签名

```cangjie role=signature
public func swapBytes(): UInt64
```

反转 UInt64 值的字节顺序。

### 契约

返回值：

- UInt64 - UInt64 值。

## func swapBytes()

适用扩展：[extend UInt8 <: SwapEndianOrder<UInt8>](extensions/extend-uint8-swapendianorder-uint8.md)。

### 签名

```cangjie role=signature
public func swapBytes(): UInt8
```

反转 UInt8 值的字节顺序。

### 契约

返回值：

- UInt8 - UInt8 值。
