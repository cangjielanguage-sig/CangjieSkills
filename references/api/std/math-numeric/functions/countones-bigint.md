<!-- cj-doc kind="api-member" level="5" id="std.math.numeric.func.countones-bigint" parent="std.math.numeric" -->
# countOnes(BigInt)

[← std.math.numeric](../index.md)

## 签名

```cangjie role=signature
public func countOnes(i: BigInt): Int64
```

计算并返回入参 BigInt 的二进制补码中 1 的个数。

## 契约

参数：

- i: BigInt - 需要计算二进制补码中 1 的个数的 BigInt 结构体。

返回值：

- Int64 - 返回入参 BigInt 的二进制补码中 1 的个数。
