# 使用AES对称密钥CCM模式加解密仓颉

This directory provides a comprehensive technical collection focused on implementing AES symmetric encryption and decryption using the CCM (Counter with CBC-MAC) mode within the Cangjie programming language. It covers the entire cryptographic lifecycle, from generating 128-bit symmetric keys to initializing Cipher instances with specific parameters like CcmParamsSpec. The documentation highlights critical operational constraints of CCM mode, such as the lack of support for segmented operations and the mandatory extraction of a 12-byte authentication tag for verification. These resources are specifically designed for Cangjie developers who possess a working knowledge of the CryptoArchitectureKit and need precise, code-level instructions for securing data through authenticated encryption mechanisms. By following these guides, developers can effectively manage cryptographic keys and handle the nuances of authenticated encryption in their applications.

## Quick Navigation

- **What do you want to do?**
    - Encrypt data using AES128 CCM mode → 使用AES对称密钥CCM模式加解密仓颉_2more.md
    - Decrypt data using AES128 CCM mode → 解密_2more.md

## Detailed Description

### 使用AES对称密钥CCM模式加解密仓颉_2more.md 使用AES对称密钥CCM模式加解密仓颉_2more.md
This technical guide provides a step-by-step tutorial for implementing AES symmetric encryption using CCM mode in the Cangjie programming language. The document outlines the encryption workflow, detailing how to generate a 128-bit symmetric key, create a Cipher instance, and initialize it with specific encryption parameters. Key takeaways include the use of `createCipher` with the 'AES128|CCM' parameter, the restriction that CCM mode does not support segmented operations, and the necessity of extracting a 12-byte `authTag` for subsequent decryption authentication. Intended for Cangjie developers, this guide requires prior understanding of the CryptoArchitectureKit and cryptographic specifications.

### 解密_2more.md 解密_2more.md
This document serves as a technical tutorial guiding developers through the process of decrypting data using the AES128 algorithm in CCM mode. It outlines the essential steps: creating a Cipher instance, initializing it with decryption mode and specific parameters like SymKey and CcmParamsSpec, and retrieving the plaintext via doFinal. The content includes a synchronous code example in the Cangjie language, illustrating parameter generation, key conversion, and the complete decryption workflow. Key concepts involve handling authentication tags and initialization vectors. This guide is intended for developers using the CryptoArchitectureKit who require practical implementation details for cryptographic operations.