<!-- cj-doc kind="api-member" level="6" id="std.core.interface.countable.position" parent="std.core.interface.countable" -->
# Countable<T>.position

[← Countable<T>](index.md)

## 签名

```cangjie role=signature
func position(): Int64
```

获取当前可数实例的位置信息，即将当前实例转为 Int64 类型。

## 契约

返回值：

- Int64 - 转换后的 Int64 值。
