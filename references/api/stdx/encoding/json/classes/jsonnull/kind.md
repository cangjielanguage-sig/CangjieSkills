<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonnull.kind" parent="stdx.encoding.json.class.jsonnull" -->
# JsonNull.kind

[← JsonNull](index.md)

## 签名

```cangjie role=signature
public func kind(): JsonKind
```

返回当前 JsonNull 所属的 JsonKind 类型（JsNull）。

## 契约

返回值：

- JsonKind - 当前 JsonNull 所属的 JsonKind 类型（JsNull）。
