<!-- cj-doc kind="api-member" level="6" id="std.ast.class.parseastexception.init" parent="std.ast.class.parseastexception" -->
# ParseASTException.init

[← ParseASTException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 ParseASTException 对象。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据异常信息构造一个 ParseASTException 对象。

### 契约

参数：

- message: String - 异常信息。
