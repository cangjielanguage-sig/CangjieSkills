# 类

This directory contains API reference documentation for the core classes of the Cangjie unit testing framework, focusing on mocking, stubbing, and verification mechanisms. It covers essential components like ActionSelector, MockFramework, and various specific selectors for methods, getters, and setters. The documentation details how to manage mock lifecycles, define argument matchers, and enforce interaction rules. It is suitable for Cangjie developers and QA engineers looking to implement robust unit tests with precise control over object behavior and dependency isolation.

## Quick Navigation

What do you want to do?

*   **Manage test sessions and core framework?**
    *   Handle lifecycle and sessions → class_MockFramework_3more.md MockFramework
*   **Configure mock behaviors?**
    *   Define general actions and chaining → class_ActionSelector_3more.md ActionSelector, class_ContinuationA.md Continuation
    *   Configure specific member types → class_SetterActionSelectorTRet.md SetterActionSelector, [8] GetterActionSelector, [10] MethodActionSelector
    *   General mock configuration → [7] ConfigureMock
    *   Define argument matching logic → [9] Matchers
*   **Verify interactions?**
    *   Check execution counts and frequency → [6] CardinalitySelector, [12] VerifyStatement
    *   Verify behavior and call order → [11] Verify
*   **Handle advanced utilities?**
    *   Manage fields and type matching → class_SyntheticFieldT_3more.md SyntheticField

## Detailed Description

### class_ActionSelector_3more.md class_ActionSelector_3more.md
This API reference documentation focuses on mocking and stubbing mechanisms in the Cangjie unit testing framework. It details the ActionSelector class, which allows developers to specify actions for mock or spy objects and supports method chaining. Additionally, it covers the ArgumentMatcher abstract class and its subclass AnyMatcher for defining argument matching logic in stub signatures. Key functionalities include forcing test failures via fails(), matching any argument type, and configuring parameter descriptions. This guide is intended for developers implementing unit tests who need to control mock behavior and validate function calls.

### class_ContinuationA.md class_ContinuationA.md
This document provides API reference documentation for the Continuation class within the Cangjie mock framework, designed to facilitate chained behavior definitions for stub signatures. The primary focus is on the then() method, which returns an ActionSelector subclass to continue the operation chain. Key information includes the requirement that previous operations must be satisfied; otherwise, a MockFrameworkException is thrown. This documentation targets developers utilizing the unit testing framework to configure complex mock object behaviors and interactions.

### class_MockFramework_3more.md class_MockFramework_3more.md
This API documentation defines core classes for a unit testing mock framework in the Cangjie language, focusing on session management, argument matching, and ordered verification. It details the MockFramework class for managing stack-based test sessions, the NoneMatcher for matching None argument values, and the OrderedVerifier for collecting sequential verification statements. Key functions include openSession and closeSession for lifecycle control, matchesAny for argument validation, and checkThat for building verification chains. The document highlights exception handling, session isolation rules, and mock object accessibility.

### class_SetterActionSelectorTRet.md class_SetterActionSelectorTRet.md
This API reference document details the SetterActionSelector<TRet> class, which enables developers to define specific behaviors for property setters within mock or spy objects during unit testing. The content covers the class definition, its inheritance from ActionSelector, and documentation for five key member functions: doesNothing, setsOriginal, setsField, and two overloads of throws. Key information includes that each method supports method chaining by returning a CardinalitySelector. The class is typically invoked via the @On macro to stub setter logic.

### class_SyntheticFieldT_3more.md class_SyntheticFieldT_3more.md
This document serves as API reference documentation for a testing framework, detailing three core classes used for mocking and verification. It covers SyntheticField for managing mutable properties, TypedMatcher for validating argument types against expectations, and UnorderedVerifier for collecting and executing verification statements dynamically. Key information includes the create method for field instantiation, matches functions for type checking, and checkThat for adding verification steps. This technical guide is intended for developers implementing unit tests who need to handle mock objects, argument matching, and complex verification scenarios.

### [6] class_CardinalitySelectorA/
This directory provides comprehensive API reference documentation for the `CardinalitySelector` class and related functions within a Cangjie unit testing framework. It is designed to assist developers in defining and validating the execution frequency of stub behaviors, ensuring precise control over mock object interactions. The content covers the class definition and specific methods such as `anyTimes`, `atLeastOnce`, `atLeastTimes`, `once`, and `times`. It details validation logic where exceeding call limits triggers an immediate `ExpectationFailedException`, whereas insufficient calls result in exceptions after test completion.

### [7] class_ConfigureMock/
This directory contains comprehensive API reference documentation for the Cangjie `ConfigureMock` class, a fundamental utility designed to facilitate the configuration of mock objects within modern testing frameworks. It provides detailed technical specifications for static methods and functions essential for intercepting and stubbing various types of class members, including property getters, setters, and standard member functions. The documentation covers critical implementation details such as argument matchers, stub call expressions, reference tokens, and the specific selector objects returned for configuration.

### [8] class_GetterActionSelectorTRet/
This directory provides comprehensive API documentation for the `GetterActionSelector` class and associated exception handling functions within a unit testing mock framework. It focuses on enabling developers to configure specific behaviors for property getter functions found within mock or spy objects. The content covers essential methods for stubbing getters, allowing users to return specific values, access synthetic fields, invoke original logic, or throw exceptions. Furthermore, it details how to manage void returns and utilize exception factories, emphasizing the use of return types like `CardinalitySelector` and `Continuation` to facilitate method chaining.

### [9] class_Matchers/
This directory provides comprehensive API reference documentation for the `Matchers` class within the Cangjie mock framework. It serves as a technical guide for developers working on unit testing, specifically focusing on the static functions required to generate argument matchers. These matchers are fundamental tools for defining stub behaviors, filtering arguments, and capturing values for verification. The content covers the integration of matchers within `@On` macro expressions, the distinction between structural and reference equality, and type-based filtering.

### [10] class_MethodActionSelectorTRet/
This directory provides comprehensive API reference documentation for the `MethodActionSelector<TRet>` class, a critical component of the Cangjie unit testing framework. It is designed to assist developers in configuring stub behaviors for member functions within mock and spy objects. The documentation covers essential concepts such as method chaining, the insertion of stub code via the `@On` macro, and the definition of specific return behaviors. It includes detailed explanations of functions for handling direct values, closure factories, and exception scenarios, outlining potential exceptions like `IllegalArgumentException` and return types such as `CardinalitySelector`.

### [11] class_Verify/
This directory provides comprehensive API documentation for the `Verify` class within the Cangjie unit testing framework. It is designed to assist developers in defining and executing behavior verification on mock objects. The content covers essential concepts such as verification statements using the `@Called` macro, execution frequency checks, argument validity, and call order validation. Key static methods detailed include `clearInvocationLog`, `noInteractions`, `ordered`, `that`, and `unordered`. This documentation is suitable for developers utilizing the mock framework to write precise unit tests, assuming familiarity with Cangjie syntax and general mocking principles.

### [12] class_VerifyStatement/
This directory contains API reference documentation for the `VerifyStatement` class within the Cangjie unit testing mocking framework. It serves as a guide for defining verification rules regarding mock object execution counts, ensuring that methods are invoked the expected number of times during testing. The content covers the class definition and key member functions, including `atLeastOnce`, `atLeastTimes`, `once`, `times`, and `never`. It also details exception handling for invalid arguments or duplicate definitions. This documentation is intended for Cangjie developers and software testers who need to implement robust mock verification logic.