# 命令行说明

This directory serves as a comprehensive resource for the `mediatool` command-line interface, specifically focusing on the management of media library resources within a device environment. It provides detailed references for a range of operations, including the importation of local files, the export of media assets back to the local system, and the querying of resource metadata. Additionally, it covers critical maintenance tasks such as permanently deleting resources via URI and locating items by display name. Key functionalities discussed include handling synchronous or asynchronous thumbnail creation, managing source file deletion, and executing bulk operations. This documentation is primarily intended for developers and system administrators who require a robust tool for manipulating media assets programmatically. Users should possess a solid understanding of command-line interfaces, file paths, and URI structures to effectively utilize the tools described.

## Quick Navigation

**What do you want to do?**

- Import local files to the device or export media back to the local system? → 命令行说明_4more.md
- Query resource metadata or list files in CSV format? → 命令行说明_4more.md
- Permanently delete resources or locate items by display name? → mediatool_delete_2more.md

## Detailed Description

### mediatool_delete_2more.md mediatool_delete_2more.md
This document provides a command-line reference for managing media library resources using the mediatool utility. It covers two primary commands: mediatool delete for permanently removing resources by URI, and mediatool query for locating resources by display name. Key information includes the irreversible nature of deletion, the option to reset all data using the "all" argument, and the query command's flexibility to return either file paths or URIs. The documentation highlights that hidden albums are excluded from queries. Intended for developers or system administrators, this guide requires familiarity with command-line interfaces and media resource URIs.

### 命令行说明_4more.md 命令行说明_4more.md
This document serves as a reference guide for the `mediatool` command-line interface, detailing how to manage media files within a device's media library. It covers three primary commands: `send` for importing local files, `list` for querying resource metadata in CSV format, and `recv` for exporting files back to the local system. Key information includes options for synchronous or asynchronous thumbnail creation, control over source file deletion, and the ability to handle specific URIs or bulk operations using the `all` keyword. Intended for developers or system administrators, this API documentation requires understanding of file paths and URI structures to effectively manipulate media assets.