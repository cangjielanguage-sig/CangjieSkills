<!-- cj-doc kind="guide-leaf" level="5" id="language.package.3-程序入口.3-1-main-函数" parent="language.package.3-程序入口" -->
# 3.1 `main` 函数

[← 3. 程序入口](index.md)

- `main` 是入口点，每个根包最多一个
- 不能有访问修饰符
- 被导入包时不会被导入
- 参数：无参或 `Array<String>`
- 返回类型：`Unit` 或整数类型
- 定义时不使用 `func` 关键字
