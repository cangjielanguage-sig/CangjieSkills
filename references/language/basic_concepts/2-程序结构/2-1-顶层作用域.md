<!-- cj-doc kind="guide-leaf" level="5" id="language.basic_concepts.2-程序结构.2-1-顶层作用域" parent="language.basic_concepts.2-程序结构" -->
# 2.1 顶层作用域

[← 2. 程序结构](index.md)

- 允许声明：全局变量、全局函数、自定义类型（`struct`、`class`、`enum`、`interface`）、`main` 入口函数
- 非顶层作用域允许局部变量和局部函数
- `enum`/`interface` 仅支持成员函数（不支持成员变量）
