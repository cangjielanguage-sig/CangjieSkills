<!-- cj-doc kind="api-member" level="6" id="std.argopt.enum.argumentspec.value-short-rune-argumentmode-string-unit" parent="std.argopt.enum.argumentspec" -->
# ArgumentSpec.Short(Rune, ArgumentMode, (String) -> Unit)

[← ArgumentSpec](index.md)

## 签名

```cangjie role=signature
Short(Rune, ArgumentMode, (String) -> Unit)
```

表示是一个短选项，同时持有一个 `lambda` 回调函数。
