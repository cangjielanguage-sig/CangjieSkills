# 请求动画绘制帧率

This directory focuses on the technical implementation of requesting and configuring specific drawing frame rates for animations within the ArkUI framework using the Cangjie language. It provides in-depth coverage of the `ExpectedFrameRateRange` parameter, which is essential for optimizing rendering performance and controlling the smoothness of visual effects. Key topics include property animations, explicit animations, and the precise configuration of minimum, maximum, and expected frame rates (such as 60Hz or 30Hz). This resource is highly suitable for application developers who aim to fine-tune animation performance, reduce power consumption, and ensure efficient resource utilization in their UI applications.

## Quick Navigation

**What do you want to learn?**

*   View a complete, runnable code example demonstrating how to build a UI layout and trigger a frame-rate-controlled animation → 完整示例.md
*   Understand the configuration details for `ExpectedFrameRateRange` in both property and explicit animation scenarios → 请求动画绘制帧率_3more.md

## Detailed Description

### 完整示例.md 完整示例.md
This document provides a comprehensive code example demonstrating property animation implementation within a UI framework using the Cangjie language. It details the definition of `AnimateParam` with specific frame rate ranges, the construction of a UI layout using Column, Row, and Text components, and the logic for triggering animations via a button click event. The example highlights the use of `animateTo` to control state changes, specifically translating a text element, while configuring parameters like duration, iterations, and `expectedFrameRateRange` to optimize performance. It targets developers seeking practical implementation details for creating smooth, frame-rate-controlled animations in ArkUI applications.

### 请求动画绘制帧率_3more.md 请求动画绘制帧率_3more.md
This document guides developers on configuring specific drawing frame rates for animations using the `ExpectedFrameRateRange` parameter in Cangjie application development. It covers two main scenarios: requesting frame rates for property animations and for explicit animations. The text provides code examples demonstrating how to set the minimum, maximum, and expected frame rates (e.g., 60Hz and 30Hz) within the `AnimateParam` configuration. This tutorial targets application developers using the ArkUI framework and Cangjie language who need to optimize animation performance or control rendering frequency.