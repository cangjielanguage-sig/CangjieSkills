# ohosmultimediacamera_picker相机选择器

This directory provides comprehensive API reference documentation for the `ohos.multimediacamera_picker` module within the HarmonyOS Cangjie development ecosystem. It is designed to assist developers in integrating system camera interfaces directly into their applications, facilitating seamless photo and video capture functionalities. The documentation details the necessary APIs for launching the system camera, configuring capture parameters such as camera position and maximum video duration, and handling the resulting media files asynchronously. It covers critical data structures including `PickerProfile` for configuration and `PickerResult` for outcome management. This resource is ideal for HarmonyOS developers using the Cangjie programming language who require a robust understanding of multimedia camera integration, permission handling, and result interpretation to build feature-rich applications.

## Quick Navigation

**What do you want to learn?**

*   Understand how to launch the system camera interface and configure capture settings like camera position and video duration → ohosmultimediacamera_picke_4more.md
*   Learn how to process camera picker results, including status codes, media URIs, and distinguishing between photo and video types → struct_PickerResult_2more.md

## Detailed Description

### ohosmultimediacamera_picke_4more.md
This document serves as API reference documentation for the `ohos.multimedia.camera_picker` module within the HarmonyOS Cangjie development environment, enabling applications to launch a system camera interface for photo and video capture. The content details the primary `pick` function, which requires a `UIAbilityContext`, media types, and a callback to handle results. It also explains the `PickerProfile` struct, which defines configuration settings such as camera position, save URI, and maximum video duration. Key takeaways include the requirement to call these interfaces within a UIAbility and the use of callbacks for asynchronous results. This guide targets HarmonyOS developers using Cangjie to integrate multimedia capabilities.

### struct_PickerResult_2more.md
This API reference documentation defines the data structures for handling camera picker results in the Cangjie programming language. It details the `PickerResult` struct, which encapsulates the operation status, media URI, and type, alongside the `PickerMediaType` enum distinguishing between photo and video modes. Key information includes the logic for URI generation based on save permissions and the specific integer codes for success or failure. The enum also implements equality operators and string conversion methods. This document is intended for developers building multimedia applications who need to interpret camera selection outcomes.