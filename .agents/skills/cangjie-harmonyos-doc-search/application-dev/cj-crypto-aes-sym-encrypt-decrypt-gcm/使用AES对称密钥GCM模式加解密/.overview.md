# 使用AES对称密钥GCM模式加解密

This directory serves as a comprehensive technical resource for implementing AES symmetric encryption and decryption using the GCM (Galois/Counter Mode). It provides a deep dive into the necessary workflows, covering key generation, cipher initialization, data processing, and the management of authentication tags to ensure both confidentiality and integrity. The content is specifically tailored for developers utilizing the Cangjie programming language and the CryptoArchitectureKit. By combining theoretical API steps with practical synchronous code examples, this collection helps developers securely integrate cryptographic features into their applications. It addresses specific requirements such as the AES128|GCM|PKCS7 algorithm string and the handling of 16-byte authentication tags, making it an essential reference for secure data handling in Cangjie projects.

## Quick Navigation

What do you want to learn?
*   Understand the theoretical workflow and API usage for AES-GCM operations → 使用AES对称密钥GCM模式加解密_3more.md
*   Examine a synchronous code example in Cangjie for practical implementation → 示例.md

## Detailed Description

### 使用AES对称密钥GCM模式加解密_3more.md 使用AES对称密钥GCM模式加解密_3more.md
This technical guide provides step-by-step instructions for performing encryption and decryption using AES symmetric keys in GCM mode. The document is divided into two main workflows: Encryption and Decryption. The Encryption section details key generation, cipher creation, data processing, and authentication tag extraction. The Decryption section mirrors this process for reversing the operation. Key steps involve using APIs like createSymKeyGenerator, createCipher, initialize, update, and doFinal. It specifies the algorithm string AES128|GCM|PKCS7 and emphasizes the necessity of a 16-byte authTag for GCM mode authentication during decryption. This guide targets developers implementing cryptographic operations, requiring knowledge of the CryptoArchitectureKit and basic cryptographic concepts.

### 示例.md 示例.md
This document provides a synchronous code example demonstrating AES-128-GCM encryption and decryption using the Cangjie programming language. It covers key functions such as random number generation, GCM parameter specification, and symmetric key conversion. The code illustrates the complete workflow of initializing a cipher, processing data through update and doFinal methods, and handling authentication tags. Key takeaways include the proper handling of DataBlobs and the verification process using Hilog. This tutorial is intended for developers familiar with Cangjie who need to implement cryptographic security features using the CryptoArchitectureKit.