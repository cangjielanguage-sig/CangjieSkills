# enum_JsonToken

This directory contains comprehensive API reference documentation for the `JsonToken` enum within the Cangjie programming language, specifically designed to support the `encoding_json_stream` package. It serves as a critical resource for understanding the structural and value types that constitute JSON encoded strings during stream processing operations. The documentation thoroughly details specific enum variants such as `JsonNull`, `JsonBool`, `BeginArray`, `BeginObject`, and `Name`, alongside essential functional aspects like operator overloads for equality comparisons. This content is primarily suitable for software developers and systems engineers who are actively implementing JSON stream parsing logic and require a granular understanding of token types to correctly identify and handle data flow elements using methods like `JsonReader.peek()`.

## Quick Navigation

**What do you want to learn?**

*   Understand the core enum definition, available token types (e.g., `JsonNull`, `BeginArray`), and inherited methods → enum_JsonToken_12more.md
*   Learn how to compare two `JsonToken` instances for equality using operator overloads → operator_func_JsonToken.md

## Detailed Description

The files in this section offer detailed technical specifications for the `JsonToken` enum and its associated operations. They cover the complete definition of the enum, its variants, and the specific operator functions used for comparing token instances, providing developers with the necessary information to implement robust JSON parsing logic.

### enum_JsonToken_12more.md enum_JsonToken_12more.md
This document provides API reference documentation for the JsonToken enum in the Cangjie language, representing structural and value types within JSON encoded strings. It details specific enum variants such as JsonNull, JsonBool, BeginArray, BeginObject, and Name, alongside inherited methods like hashCode() and inequality operators. Key information explains that JsonToken is typically used with JsonReader.peek() to identify upcoming JSON elements, guiding developers to invoke specific reading methods like startArray() or readValue<T>(). This reference is intended for developers implementing JSON stream parsing who require a detailed understanding of token types to handle data flow correctly.

### operator_func_JsonToken.md operator_func_JsonToken.md
This document serves as API reference documentation for the equality operator overload of the JsonToken type in the Cangjie programming language. It details the function signature, functionality, parameters, and return values. The key information provided is that this operator compares the current JsonToken instance with another specified instance, returning a Boolean value indicating whether they are equal. Intended for developers utilizing the encoding_json_stream package, this reference helps users understand how to perform equality checks between JSON token objects during stream processing.