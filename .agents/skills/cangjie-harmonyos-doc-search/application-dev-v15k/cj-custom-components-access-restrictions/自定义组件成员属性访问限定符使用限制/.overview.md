# 自定义组件成员属性访问限定符使用限制

This documentation provides a comprehensive technical reference regarding the usage restrictions and initialization rules for access qualifiers—specifically private, public, and protected—applied to custom component member properties within the Cangjie framework. It covers the complex interactions between these access modifiers and state management decorators such as @State, @Prop, @Link, @StorageLink, and @Consume. By detailing specific constraints on external versus local initialization, this guide helps developers prevent compiler errors and ensure robust data binding. It is primarily suitable for component developers and system architects working on HarmonyOS UI applications who need to adhere to strict validation standards for stateful components.

## Quick Navigation

**What do you want to learn?**

*   Understand the core reference rules and initialization constraints for access qualifiers on member properties → 自定义组件成员属性访问限定符使用限制_2more.md
*   Explore practical usage scenarios and validation rules to avoid common pitfalls in state management → 使用场景/

## Detailed Description

### 自定义组件成员属性访问限定符使用限制_2more.md 自定义组件成员属性访问限定符使用限制_2more.md
This reference document outlines usage restrictions and initialization rules for access qualifiers on custom component member properties within the Cangjie framework's state management system. It details specific constraints for various state management decorators, categorizing them by whether they allow external initialization. Key takeaways include that @State, @Prop, and @BuilderParam allow external initialization, while @StorageLink and @Consume do not. Crucially, @Link variables must be initialized externally and strictly prohibit local initialization. Intended for component developers familiar with state management concepts, this guide ensures adherence to validation standards for private, public, and protected qualifiers.

### 使用场景/
This directory serves as a technical guide for usage scenarios regarding state management in the Cangjie programming language, primarily targeting HarmonyOS UI component development. It addresses the complex interactions between access modifiers (like `private` and `public`) and various state management macros (including `@State`, `@Link`, `@Prop`, and storage-related decorators). The content explains specific validation rules and restrictions to prevent compiler errors, emphasizing the importance of correct syntax for data binding and state synchronization. By providing clear examples of what works and what doesn't, this documentation helps Cangjie developers build robust applications by avoiding common pitfalls associated with access control in stateful components.