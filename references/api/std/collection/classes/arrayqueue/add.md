<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arrayqueue.add" parent="std.collection.class.arrayqueue" -->
# ArrayQueue<T>.add

[← ArrayQueue<T>](index.md)

## 签名

```cangjie role=signature
public func add(element: T): Unit
```

在此队列尾部插入元素。

## 契约

参数：

- element: T - 被插入到此双端队列的元素。
