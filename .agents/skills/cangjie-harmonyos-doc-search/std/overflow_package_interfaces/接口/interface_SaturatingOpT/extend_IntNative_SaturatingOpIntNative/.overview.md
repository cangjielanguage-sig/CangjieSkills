# extend_IntNative_SaturatingOpIntNative

This directory serves as a technical reference for the implementation of the `SaturatingOp` interface extension for the `IntNative` type within the Cangjie core package. It details a suite of saturating arithmetic functions that ensure numerical safety by clamping results to maximum or minimum limits upon overflow or underflow, rather than allowing values to wrap around. The documentation covers a wide range of operations, including basic arithmetic like addition and multiplication, as well as bit manipulation and negation. This collection of documents is specifically curated for Cangjie developers who require precise, safe, and boundary-checked integer operations to maintain data integrity and prevent runtime errors in high-performance or safety-critical applications.

## Quick Navigation

**What do you want to learn?**

*   Review the core implementation of the SaturatingOp interface for IntNative, including addition, multiplication, and shift logic → extend_IntNative_Saturatin_9more.md
*   Explore specific saturating functions such as saturating shift right for UInt64 and saturating subtraction for IntNative → func_saturatingShrUInt64_2more.md

## Detailed Description

### extend_IntNative_Saturatin_9more.md extend_IntNative_Saturatin_9more.md
This API documentation details the implementation of the SaturatingOp interface for the IntNative type in the Cangjie core package. The document outlines the extension declaration and provides specifications for eight saturating arithmetic functions, including addition, multiplication, division, negation, increment, decrement, modulo, and left shift. Key information includes the saturation strategy where operations clamp results to the type's maximum or minimum values upon overflow or underflow, rather than wrapping. Specifically, saturatingShl clamps the shift amount. This reference guide targets Cangjie developers who require safe, boundary-checked arithmetic operations for native integer types.

### func_saturatingShrUInt64_2more.md func_saturatingShrUInt64_2more.md
This document serves as API reference documentation for saturating arithmetic functions in the Cangjie programming language. It covers two specific functions: `saturatingShr` for `UInt64` and `saturatingSub` for `IntNative`. Key information includes the saturation logic where `saturatingShr` clamps the shift amount to the operand bit width minus one to prevent excessive shifting, and `saturatingSub` returns the type's maximum or minimum value upon overflow or underflow respectively. This guide is intended for developers requiring safe integer operations that prevent undefined behavior or wrapping errors.