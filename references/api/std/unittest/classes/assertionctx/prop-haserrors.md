<!-- cj-doc kind="api-member" level="6" id="std.unittest.class.assertionctx.prop-haserrors" parent="std.unittest.class.assertionctx" -->
# AssertionCtx.hasErrors

[← AssertionCtx](index.md)

## 签名

```cangjie role=signature
public prop hasErrors: Bool
```

如果用户定义内的任何断言失败，则为 `true` 。

## 契约

功能：如果用户定义内的任何断言失败，则为 `true` 。否则为 `false`。

类型：Bool
