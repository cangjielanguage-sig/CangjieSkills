# 类

This directory contains API reference documentation for concurrent classes within the Cangjie standard library. It focuses on thread-safe data structures designed for high-concurrency scenarios, including various implementations of maps and queues. The documentation covers class definitions, initialization, atomic operations, and element manipulation methods such as blocking and non-blocking operations. It is suitable for Cangjie developers who need to implement efficient, thread-safe data storage and retrieval logic, manage shared resources, or handle producer-consumer patterns in multi-threaded applications.

## Quick Navigation

**What do you want to learn?**

*   Iterate over key-value pairs in a ConcurrentHashMap → class_ConcurrentHashMapIteratorK_V_where__17f0212e.md
*   Use a fixed-capacity, array-based blocking queue → [2]
*   Use a thread-safe hash table for concurrent storage → [3]
*   Use a non-blocking, linked-list based queue → [4]
*   Use a configurable-capacity, linked blocking queue → [5]

## Detailed Description

### class_ConcurrentHashMapIteratorK_V_where__17f0212e.md class_ConcurrentHashMapIteratorK_V_where__17f0212e.md
This API documentation defines the ConcurrentHashMapIterator class, which enables iteration over key-value pairs in a ConcurrentHashMap within the Cangjie standard library. The document details the class definition, constructor init, and the next method, along with a usage example. Key information highlights critical concurrency limitations: the iterator does not provide a snapshot view and may not reflect concurrent modifications, suggesting use only when the map is not being modified by other threads. This reference guide targets Cangjie developers implementing concurrent data structures who need to traverse hash maps safely.

### class_ArrayBlockingQueueE/
This directory contains API reference documentation for the `ArrayBlockingQueue` class in the Cangjie programming language. It focuses on a thread-safe, array-based blocking queue with a fixed capacity, essential for concurrent data handling. The documentation covers class initialization, properties, and various element manipulation methods, including blocking, non-blocking, and timed operations. It also highlights deprecated methods and their modern replacements. This resource is suitable for Cangjie developers implementing concurrent data structures who need precise control over bounded queue operations, thread synchronization, and capacity management.

### class_ConcurrentHashMapK_V_where_K_Hashab_a35a4b2e/
This directory provides comprehensive API reference documentation for the `ConcurrentHashMap` class in the Cangjie programming language. It covers a thread-safe hash table implementation designed for concurrent scenarios, requiring keys to be Hashable and Equatable. The documentation details class definitions, initialization constructors, atomic operations, and various manipulation methods such as add, remove, and replace. It also includes information on deprecated functions and recommended alternatives. This resource is intended for Cangjie developers looking to implement efficient, thread-safe data structures and manage concurrent data storage and retrieval logic.

### class_ConcurrentLinkedQueueE/
This directory contains comprehensive API reference documentation for the Cangjie `ConcurrentLinkedQueue` class, a thread-safe, non-blocking collection specifically designed for high-concurrency scenarios. It covers class constructors, the non-atomic size property, and essential methods for element manipulation, with a specific focus on the `add` function for enqueuing. Key topics include the deprecation of legacy methods like `dequeue`, `enqueue`, and `head` in favor of `remove`, `add`, and `peek`, respectively. The documentation also explains the queue's utility in producer-consumer models and task scheduling. This resource is suitable for Cangjie developers who require knowledge of concurrent programming and generic collections to build efficient multi-threaded applications.

### class_LinkedBlockingQueueE/
This directory contains API reference documentation for the `LinkedBlockingQueue` class in the Cangjie programming language. It covers a thread-safe, blocking concurrent queue with configurable capacity, detailing class definitions, initialization methods, and various queue operations. Key topics include blocking and non-blocking element insertion and extraction, deprecation notices for legacy methods, and exception handling for invalid states. This documentation is suitable for Cangjie developers implementing concurrent data structures who need to manage thread synchronization, resource limits, and shared resource access efficiently.