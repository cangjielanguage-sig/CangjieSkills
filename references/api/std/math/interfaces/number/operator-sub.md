<!-- cj-doc kind="api-member" level="6" id="std.math.interface.number.operator-sub" parent="std.math.interface.number" -->
# Number<T>.-

[← Number<T>](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## operator func -()

### 签名

```cangjie role=signature
operator func -(): T
```

算术运算符，计算取负的值。

### 契约

返回值：

- T - 取负的值。

## operator func -(T)

### 签名

```cangjie role=signature
operator func -(rhs: T): T
```

算术运算符，计算减法。

### 契约

参数：

- rhs: T - 运算符右边的数，表示减数。

返回值：

- T - 计算所得差。
