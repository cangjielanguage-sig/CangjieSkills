# 拉起邮件类应用startAbilityByType

This directory provides comprehensive technical documentation for implementing email composition capabilities within the HarmonyOS ecosystem, specifically utilizing the `startAbilityByType` interface. It covers the entire interaction flow, starting from the client application that initiates the email request to the target application that receives and processes the composition intent. Key technical concepts include the AbilityKit framework, `module.json5` configuration, parameter construction (recipients, subject, body, attachments), and the necessity of URL encoding for data transmission. The guides include practical Cangjie code examples for both invoking the API and handling lifecycle events. This resource is essential for application developers and system integrators aiming to enable seamless email sharing or composition features in their HarmonyOS applications, ensuring proper data handling and system integration. By following these guides, developers can ensure their applications correctly interact with the system's email framework.

## Quick Navigation

**What do you want to do?**

*   **Launch a mail app to compose an email:** Learn how to construct the `wantParam` with URL-encoded strings and invoke the interface using Cangjie code. → 拉起邮件类应用startAbilityByType_3more.md 拉起邮件类应用startAbilityByType_3more.md
*   **Configure your app to handle email requests:** Understand how to set up `module.json5`, declare the `ComposeMail` capability, and parse incoming parameters in `UIAbility`. → 目标方开发步骤.md 目标方开发步骤.md

## Detailed Description

### 拉起邮件类应用startAbilityByType_3more.md 拉起邮件类应用startAbilityByType_3more.md
This technical guide explains how to launch a mail application extension panel using the startAbilityByType interface. It details the required parameters for the wantParam argument, including recipient addresses, subject, body, and attachments, while specifying that string parameters must be URL encoded. The document provides a step-by-step tutorial featuring a Cangjie code example to demonstrate how to construct the JSON parameters and invoke the API within a UIAbilityContext. Targeted at application developers, this guide facilitates the integration of email sending capabilities into applications using the AbilityKit framework.

### 目标方开发步骤.md 目标方开发步骤.md
This technical guide outlines the development steps for configuring an application to act as a target for email composition features. It covers module configuration using module.json5 to declare the ComposeMail capability and details parameter parsing within the UIAbility lifecycle methods. Key takeaways include the specific linkFeature attribute setup, the structure of incoming want parameters such as email, subject, and body, and the necessity of URL decoding for received data. A complete Cangjie code example demonstrates handling these parameters and loading the UI. Intended for HarmonyOS developers implementing system-level feature integration.