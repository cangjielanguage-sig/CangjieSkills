# interface_CarryingOpT

This directory contains API reference documentation for the Cangjie language's `CarryingOp` interface and its implementations across various integer types. It focuses on arithmetic operations designed to detect truncation and overflow using a wrapping strategy. Key concepts include explicit overflow detection, tuple return types containing a Boolean flag and the result, and low-level arithmetic safety. This documentation is suitable for Cangjie developers, systems programmers, and engineers working on performance-critical applications requiring precise numeric control and robust error handling.

## Quick Navigation

What do you want to learn?

*   Understand the core interface definition and specifications? → interface_CarryingOpT_9more.md
*   Review specific function examples like `carryingShr` or `carryingSub`? → func_carryingShrUInt64_2more.md
*   Explore implementations for signed integers (Int8, Int16, Int32, Int64, IntNative)? → extend_Int8_CarryingOpInt8, extend_Int16_CarryingOpInt16, extend_Int32_CarryingOpInt32, extend_Int64_CarryingOpInt64_CarryingPow, extend_IntNative_CarryingOpIntNative
*   Explore implementations for unsigned integers (UInt8, UInt16, UInt32, UInt64, UIntNative)? → extend_UInt8_CarryingOpUInt8, extend_UInt16_CarryingOpUInt16, extend_UInt32_CarryingOpUInt32, extend_UInt64_CarryingOpUInt64, extend_UIntNative_CarryingOpUIntNative

## Detailed Description

### func_carryingShrUInt64_2more.md func_carryingShrUInt64_2more.md
This document provides API reference documentation for two arithmetic functions in the Cangjie language designed to detect truncation during operations. The main sections cover the `carryingShr` function for `UInt64` right shifts and the generic `carryingSub` function for subtraction. Key information includes that both functions return a tuple where the first element is a `Bool` indicating if truncation occurred and the second element is the operation result of type `T`. This documentation is intended for Cangjie developers who need precise control over arithmetic operations and explicit overflow detection.

### interface_CarryingOpT_9more.md interface_CarryingOpT_9more.md
This API reference document defines the Cangjie interface CarryingOp, which provides methods for performing integer operations while detecting truncation. The document details the interface definition and specifications for ten functions covering arithmetic operations (add, sub, mul, div, mod), increment/decrement, negation, and bitwise shifts (shl, shr). Key information includes that every function returns a tuple containing a boolean indicating if truncation occurred and the resulting value. This design allows developers to handle overflow explicitly. The content targets Cangjie language developers who need to implement or utilize types requiring precise overflow detection during mathematical computations.

### extend_Int16_CarryingOpInt16/
This directory serves as a comprehensive API reference for the `CarryingOp` interface implementation tailored for the `Int16` type within the Cangjie programming language. It provides detailed documentation for arithmetic operations designed to handle integer overflow through a wrapping strategy. The content covers a wide range of functions, including addition, subtraction, multiplication, division, and bitwise shifts, all of which return a tuple containing a Boolean overflow flag and the result. This resource is essential for developers and systems programmers who require granular control over arithmetic behavior and explicit overflow detection in their software projects.

### extend_Int32_CarryingOpInt32/
This directory serves as a comprehensive API reference for the `CarryingOp` interface implementation tailored for the `Int32` type in the Cangjie programming language. It provides detailed documentation on a wide array of arithmetic functions that employ a wrapping strategy to manage integer overflows effectively. The scope includes fundamental operations such as addition, subtraction, multiplication, division, and modulo, as well as negation and bit shifting. A defining feature of these operations is their return type: a tuple comprising the computed result and a Boolean flag that signals whether truncation took place. This documentation is essential for developers who demand exact precision in integer arithmetic and require robust mechanisms for detecting and handling overflow conditions in their software projects.

### extend_Int64_CarryingOpInt64_CarryingPow/
This directory contains comprehensive API documentation for extending the Int64 type in the Cangjie programming language with specialized carrying arithmetic operations. It focuses on the implementation of the CarryingOp and CarryingPow interfaces, providing a suite of functions—including addition, subtraction, multiplication, division, and power—that handle integer overflow explicitly. Unlike standard arithmetic, these operations utilize a wrapping strategy and return a tuple containing both the calculation result and a boolean flag indicating whether truncation occurred. This resource is essential for Cangjie developers and systems programmers who require precise control over arithmetic behavior and need robust overflow detection mechanisms in low-level mathematical computations.

### extend_Int8_CarryingOpInt8/
This directory provides comprehensive API documentation for the `CarryingOp` interface implementation specifically tailored for the `Int8` type within the Cangjie programming language. It covers a wide array of arithmetic operations, including addition, subtraction, multiplication, division, modulo, increment, decrement, negation, and bitwise shifts. A core feature of these functions is the wrapping strategy, which ensures that operations return a tuple containing both the computed result and a Boolean flag to signal overflow or truncation. This documentation is crucial for developers engaged in low-level integer arithmetic or performance-critical applications where explicit overflow detection and precise control over numerical behavior are mandatory.

### extend_IntNative_CarryingOpIntNative/
This directory provides comprehensive API documentation for the implementation of the `CarryingOp` interface as it applies to the `IntNative` type in the Cangjie programming language. It focuses on arithmetic operations that feature explicit overflow detection, utilizing a wrapping strategy to manage calculations safely. The content covers a broad spectrum of methods, including addition, subtraction, multiplication, division, modulo, and bit shifts. Crucially, these functions return a tuple consisting of a boolean flag indicating whether truncation occurred alongside the computed result. This documentation is specifically designed for Cangjie developers and systems programmers who need precise control over low-level arithmetic behavior and integer overflow states.

### extend_UInt16_CarryingOpUInt16/
This directory contains essential API documentation for extending the `UInt16` type in the Cangjie programming language through the `CarryingOp` interface. It focuses on arithmetic and bitwise operations designed to handle overflow and truncation explicitly using a wrapping strategy. The documentation details a suite of functions that return tuples containing both the computed result and a Boolean flag indicating if an arithmetic boundary was exceeded. This resource is highly suitable for Cangjie developers working on low-level systems programming, cryptographic algorithms, or performance-critical code where detecting overflow is necessary to ensure data integrity and safe numeric computation.

### extend_UInt32_CarryingOpUInt32/
This directory provides comprehensive API reference documentation for the Cangjie programming language, specifically detailing the implementation of the `CarryingOp` interface for the `UInt32` type. It focuses on a suite of arithmetic and bitwise operations designed to handle calculations safely using a wrapping strategy. The core functionality of these APIs involves returning a tuple containing the operation result alongside a Boolean flag that explicitly indicates if truncation or overflow occurred during the process. This documentation is essential for Cangjie developers who need to perform explicit overflow detection and handling, particularly in low-level programming contexts where precise integer control and safety are critical.

### extend_UInt64_CarryingOpUInt64/
This directory provides comprehensive API documentation for the `CarryingOp` interface implementation specifically tailored for the `UInt64` type within the Cangjie programming language. It focuses on low-level arithmetic and bitwise operations that utilize a wrapping strategy, enabling developers to detect overflow or truncation explicitly during execution. The documentation covers a wide range of functions, including addition, subtraction, multiplication, division, negation, and bit shifting. Each operation is designed to return a tuple containing a Boolean flag indicating overflow status alongside the resulting `UInt64` value. This resource is essential for Cangjie developers engaged in systems programming, cryptographic implementations, or performance-critical applications where precise control over numeric computations and error handling is paramount.

### extend_UInt8_CarryingOpUInt8/
This directory contains comprehensive API reference documentation for the `CarryingOp` interface implementation specifically tailored for the `UInt8` type within the Cangjie programming language. It covers a range of arithmetic and bitwise operations that are essential for low-level numeric manipulation. A key feature of these operations is the use of a wrapping strategy to handle overflow, ensuring that operations do not cause runtime errors but instead wrap around. Crucially, these functions return a tuple containing both the resulting `UInt8` value and a Boolean flag indicating whether an overflow or truncation occurred. This documentation is highly suitable for Cangjie developers, systems programmers, and engineers working on performance-critical applications where precise control over arithmetic behavior and explicit overflow detection is required.

### extend_UIntNative_CarryingOpUIntNative/
This directory contains comprehensive API documentation for the `CarryingOp` interface implementation regarding the `UIntNative` type within the Cangjie programming language. It is designed to assist developers in performing low-level unsigned integer arithmetic with a focus on safety and precision. The documentation details a variety of functions that utilize wrapping strategies for calculations, ensuring that operations behave predictably even at boundary limits. A key feature of these functions is their return type: a tuple containing both the operation result and a Boolean flag indicating if truncation or overflow occurred. This resource is particularly suitable for systems programmers and developers working on cryptographic algorithms or embedded systems where explicit overflow detection is critical.