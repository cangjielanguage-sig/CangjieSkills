<!-- cj-doc kind="api-member" level="6" id="std.net.class.tcpsocket.operator-eq" parent="std.net.class.tcpsocket" -->
# TcpSocket.==

[← TcpSocket](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: TcpSocket): Bool
```

判断两个 TcpSocket 实例是否相等。

## 契约

参数：

- other: TcpSocket - 参与比较的 TcpSocket 实例。

返回值：

- Bool - 如果两个 TcpSocket 实例相等，则返回 `true`；否则，返回 `false`。
