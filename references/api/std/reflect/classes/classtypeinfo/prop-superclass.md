<!-- cj-doc kind="api-member" level="6" id="std.reflect.class.classtypeinfo.prop-superclass" parent="std.reflect.class.classtypeinfo" -->
# ClassTypeInfo.superClass

[← ClassTypeInfo](index.md)

## 签名

```cangjie role=signature
public prop superClass: Option<ClassTypeInfo>
```

获取该 `class` 类型信息所对应的 `class` 类型的直接父类。

## 契约

> **注意：**
>
> 理论上只有 class Object 没有直接父类。

类型：Option\<ClassTypeInfo>
