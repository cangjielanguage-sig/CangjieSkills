本目录包含仓颉语言中 `ohos.application.test_runner` 模块的 API 参考文档，专注于指导 HarmonyOS 应用开发者实现自定义单元测试框架。文档详细介绍了模块的导入方式、所需权限声明以及核心 `TestRunner` 类的定义与使用方法。通过继承 `TestRunner` 类并覆盖 `onPrepare` 和 `onRun` 方法，开发者可以灵活地管理测试环境准备和测试用例执行流程。该资源特别适合具备仓颉语言开发能力的工程师，用于构建健壮的应用层单元测试逻辑，确保应...