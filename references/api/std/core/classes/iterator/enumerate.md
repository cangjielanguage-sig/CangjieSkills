<!-- cj-doc kind="api-member" level="7" id="std.core.class.iterator.enumerate" parent="std.core.class.iterator.extension.extend-t-iterator-t" -->
# Iterator<T>.enumerate

[← extend<T> Iterator<T>](extensions/extend-t-iterator-t.md)

## 签名

```cangjie role=signature
public func enumerate(): Iterator<(Int64, T)>
```

用于获取带索引的迭代器。

## 契约

返回值：

- Iterator\<(Int64, T)> - 返回一个带索引的迭代器。
