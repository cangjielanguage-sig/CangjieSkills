# 使用AVPlayer播放视频

This directory focuses on video playback implementation using the AVPlayer API in HarmonyOS. It covers the complete development lifecycle, from player initialization and resource configuration to playback control and resource release. Key topics include state management, error handling, audio interruptions, and integration with XComponent. It also provides practical code examples in the Cangjie language for advanced scenarios like live streaming and multi-track selection. This resource is suitable for application developers seeking to build robust multimedia features with fine-grained control over video playback.

## Quick Navigation

**What do you want to learn?**

*   Understand the end-to-end implementation and lifecycle → 使用AVPlayer播放视频_2more.md
*   Follow a step-by-step development guide with code structure → 开发步骤及注意事项.md
*   Explore comprehensive code examples in Cangjie (Stage model) → 完整示例/

## Detailed Description

### 使用AVPlayer播放视频_2more.md 使用AVPlayer播放视频_2more.md
This development guide provides instructions on implementing end-to-end video playback using the AVPlayer API, contrasting it with the basic Video component. The document details the complete playback lifecycle, including player creation, resource setup, parameter configuration, playback controls, and resource release. It emphasizes the importance of monitoring state changes to handle errors and manage memory efficiently. Additionally, it offers development advice regarding audio interruption handling, output device monitoring, and internet permissions for online resources. Targeted at application developers, this guide highlights that background playback is currently unsupported and recommends proactive management of audio focus events.

### 开发步骤及注意事项.md 开发步骤及注意事项.md
This document provides a step-by-step guide for developers on implementing video and audio playback using the AVPlayer API. It details the complete lifecycle management, including creating instances, configuring essential event listeners like stateChange and error, setting resources via URLs or file descriptors, and configuring display windows using XComponent. The guide explains playback control operations such as play, pause, seek, and stop, alongside resource switching and instance release. Key takeaways include the importance of specific listeners for UI synchronization, requirements for application sandbox paths or internet permissions, and the correct sequence of state transitions. This tutorial targets application developers building multimedia features who need a practical workflow for integrating media controls.

### 完整示例/
This directory contains comprehensive code examples for implementing media playback in HarmonyOS applications using the Cangjie language. It focuses on the AVPlayer class, covering initialization, state management, and lifecycle integration within the Stage model. Key topics include setting various media sources (sandbox URLs, file descriptors, custom data sources), handling playback states, and advanced features like live streaming, custom HTTP headers, and multi-track audio selection. These examples are suitable for HarmonyOS developers looking to integrate robust audio and video functionality into their applications.