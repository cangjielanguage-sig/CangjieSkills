<!-- cj-doc kind="api-member" level="6" id="std.core.struct.array.intersperse" parent="std.core.struct.array" -->
# Array<T>.intersperse

[← Array<T>](index.md)

## 签名

```cangjie role=signature
public func intersperse(separator: T): Array<T>
```

返回每两个元素之间插入一个给定的新元素后的新数组

## 注意
>
不支持平台：OpenHarmony。

## 参数

- separator: T - 给定的元素。

## 返回值

- Array<T> - 返回一个新数组。

