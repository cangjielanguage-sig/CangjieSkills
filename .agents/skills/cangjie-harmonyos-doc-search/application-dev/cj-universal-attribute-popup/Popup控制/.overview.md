# Popup控制

This directory focuses on the implementation and configuration of popup windows within the ArkUI framework using the Cangjie language. It comprehensively covers the `bindPopup` API, detailing how to manage display states, content configuration, and interaction logic for both standard and custom popups. Key topics include state management, event handling, and styling customization such as arrow placement and background blur. It also highlights critical constraints, such as ensuring the `show` parameter is false during page construction to ensure correct positioning. This resource is suitable for HarmonyOS developers and ArkUI engineers who possess a familiarity with component lifecycles and Cangjie syntax and are looking to integrate interactive popup menus and dialogs into their applications.

## Quick Navigation

What do you want to learn?

*   **Understand the core API**: Learn about the `bindPopup` function, `PopupOptions`, and critical constraints like the `show` parameter → Popup控制_4more.md
*   **See implementation in action**: Review practical Cangjie code examples covering state management, event handling, and styling customization → 示例代码.md

## Detailed Description

### Popup控制_4more.md Popup控制_4more.md
This document provides API reference documentation for binding popup windows to UI components in the ArkUI framework using the Cangjie language. It details the `bindPopup` function, explaining how to configure display states, content, and interaction logic using either standard `PopupOptions` or `CustomPopupOptions`. Key information includes the critical constraint that the `show` parameter must be false during page construction to ensure correct positioning. The document targets ArkUI developers who need to implement interactive popups, requiring familiarity with component lifecycles and the Cangjie syntax.

### 示例代码.md 示例代码.md
This document provides a practical Cangjie code example demonstrating how to implement and configure popup components within the ArkUI framework. It covers the `bindPopup` method, utilizing `PopupOptions` for standard dialogs with buttons and `CustomPopupOptions` for custom content builders. The code illustrates state management for visibility control, event handling for dismissals, and extensive styling customization including arrow placement, background blur, and transitions. It targets HarmonyOS developers using the Cangjie language who need to integrate interactive popup menus into their applications.