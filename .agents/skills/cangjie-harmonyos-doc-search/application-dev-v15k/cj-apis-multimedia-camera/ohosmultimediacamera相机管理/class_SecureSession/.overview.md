# class_SecureSession

This directory contains API documentation for the `SecureSession` class and related callback management functions within the Cangjie CameraKit framework. It covers high-security camera functionalities, such as secure preview streams for face recognition and banking, alongside event handling mechanisms for focus states and error monitoring. Key topics include the `SecureSession` class structure, `addSecureOutput` method, and the `on` and `off` functions for registering and unregistering camera event listeners. This documentation is intended for Cangjie developers building multimedia camera applications that require robust security features and real-time state management.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class for high-security camera sessions → class_SecureSession_2more.md
*   Manage camera event listeners (register/unregister) and handle errors → func_offCameraCallbackType_2more.md
*   Monitor real-time focus state changes in auto-focus mode → func_onCameraCallbackTy_d1472a61.md

## Detailed Description

### class_SecureSession_2more.md class_SecureSession_2more.md
This API documentation defines the SecureSession class, a specialized camera session designed for high-security scenarios like face recognition and banking. The document details the class structure, which inherits standard camera controls like Flash and Focus, and highlights the addSecureOutput method for marking preview streams as secure. Key information includes the requirement for a Secure TA for data verification and encryption, support for simultaneous secure and normal streams, and specific system capabilities. Intended for Cangjie developers, this reference provides the necessary interface definitions and code examples to implement secure camera functionalities within the multimedia camera core framework.

### func_offCameraCallbackType_2more.md func_offCameraCallbackType_2more.md
This API reference document outlines Cangjie language functions for managing camera event listeners within the CameraKit framework. It details the `off` function to unregister listeners for error events or focus state changes, and the `on` function to register callbacks for handling error exceptions. Key information includes specific parameter requirements for `CameraCallbackType` and the restriction that the `off` method cannot be invoked inside the `on` callback. The document provides code examples demonstrating session creation and callback implementation. It is intended for developers working on multimedia camera applications who need to handle runtime errors and state changes programmatically.

### func_onCameraCallbackTy_d1472a61.md func_onCameraCallbackTy_d1472a61.md
This API documentation describes the `on` function for registering a callback to monitor camera focus state changes in the Cangjie language. The document details the function signature, system capabilities, and required parameters, specifically `CameraCallbackType.focusStateChange` and a `Callback1Argument<FocusState>`. Key information includes the restriction that the `off` method cannot be called within the callback and that the event triggers only during auto-focus mode. A code example demonstrates implementing a custom callback class and registering it with a `SecureSession`. This reference is intended for developers building camera applications who need to handle real-time focus state updates.