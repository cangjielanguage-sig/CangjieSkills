# cj-create-toast

This directory provides a specialized technical overview for Cangjie developers aiming to implement Toast notifications within the ArkUI development environment. It serves as a definitive guide on using the `PromptAction` class to generate system-standard, non-intrusive user feedback messages. The documentation thoroughly explores the definition of a Toast, emphasizing the importance of adhering to system defaults regarding text density and notification frequency to prevent user disruption. A significant focus is placed on the technical distinction between the `DEFAULT` and `TOP_MOST` display modes, explaining how `TOP_MOST` utilizes a sub-window for higher z-index layering compared to the main window rendering of `DEFAULT`. This resource is particularly suitable for software engineers and UI developers who require a deep understanding of ArkUI's prompt capabilities. It ensures that implemented notifications are lightweight, devoid of interactive elements, and consistent with the platform's design language, thereby enhancing the overall application quality and user experience. By following the guidelines in this directory, developers can effectively communicate status updates or alerts without interrupting the user's primary workflow.

## Quick Navigation

**What do you want to learn?**

*   **Understand the Basics:** Learn the definition of Toast and the role of the `PromptAction` class → cj-create-toast.md
*   **Best Practices:** Discover recommendations for text density, frequency, and avoiding interactive elements → cj-create-toast.md
*   **Display Modes:** Explore the technical differences between `DEFAULT` (main window) and `TOP_MOST` (sub-window) → cj-create-toast.md
*   **Implementation:** View Cangjie code examples demonstrating how to create and configure Toasts → cj-create-toast.md

## Detailed Description

### cj-create-toast.md cj-create-toast.md

This comprehensive guide details the process of implementing and utilizing Toast notifications specifically for the Cangjie language, leveraging the functionality of the `PromptAction` class. It begins by establishing a clear definition of what a Toast is, followed by essential usage recommendations that cover optimal frequency and text density to ensure a good user experience. The document further provides a technical comparison between the `DEFAULT` and `TOP_MOST` display modes, clarifying that `TOP_MOST` creates a sub-window to achieve higher layering, while `DEFAULT` remains within the main window. Key takeaways for developers include the importance of avoiding long text blocks and interactive elements, as well as strictly adhering to system defaults. To facilitate practical application, the document includes Cangjie code examples that demonstrate both display modes and the basic creation process. This technical tutorial is primarily targeted at Cangjie developers using ArkUI who need to provide non-intrusive user feedback.