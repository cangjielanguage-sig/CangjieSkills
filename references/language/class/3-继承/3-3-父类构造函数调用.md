<!-- cj-doc kind="guide-leaf" level="5" id="language.class.3-继承.3-3-父类构造函数调用" parent="language.class.3-继承" -->
# 3.3 父类构造函数调用

[← 3. 继承](index.md)

- 在子类 `init` 中：使用 `super(args)` 或 `this(args)` 作为函数体**第一个表达式**（互斥）
- 主构造函数可用 `super(args)` 但不可用 `this(args)`
- 若两者都未调用，编译器插入 `super()` — 父类须有无参构造函数
