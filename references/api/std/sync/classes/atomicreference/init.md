<!-- cj-doc kind="api-member" level="6" id="std.sync.class.atomicreference.init" parent="std.sync.class.atomicreference" -->
# AtomicReference<T> where T <: Object.init

[← AtomicReference<T> where T <: Object](index.md)

## 签名

```cangjie role=signature
public init(val: T)
```

构造一个封装 `T` 数据类型的原子类型 AtomicReference 的实例，其内部数据初始值为入参 `val` 的值。

## 契约

参数：

- val: T - 原子类型的初始值。
