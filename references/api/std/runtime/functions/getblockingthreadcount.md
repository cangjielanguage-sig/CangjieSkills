<!-- cj-doc kind="api-member" level="5" id="std.runtime.func.getblockingthreadcount" parent="std.runtime" -->
# getBlockingThreadCount()

[← std.runtime](../index.md)

## 签名

```cangjie role=signature
public func getBlockingThreadCount(): Int64
```

获取阻塞的仓颉线程数。

## 契约

返回值：

- Int64 - 阻塞的仓颉线程数。
