<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509certificate.operator-eq" parent="stdx.crypto.x509.class.x509certificate" -->
# X509Certificate.==

[← X509Certificate](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: X509Certificate): Bool
```

判等。

## 契约

参数：

- other: X509Certificate - 被比较的证书对象。

返回值：

- Bool - 若证书相同，返回 true；否则，返回 false。
