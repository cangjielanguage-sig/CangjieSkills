<!-- cj-doc kind="guide-index" level="5" id="language.collections.array.7-拷贝" parent="language.collections.array" -->
# 7. 拷贝

[← Array 类型](../index.md)

| 细粒度主题 | 摘要 |
|---|---|
| [7.1 `clone` — 深拷贝](7-1-clone-深拷贝.md) | `func clone(): Array<T>`：深拷贝。 |
| [7.2 `copyTo` — 拷贝到目标数组](7-2-copyto-拷贝到目标数组.md) | `copyTo(dst, srcStart, dstStart, copyLen)` 把当前数组从 `srcStart` 起的 `copyLen` 个元素复制到目标数组的 `dstStart` 位置；第三个参数是目标起点，不是复制长度。 |
