# core_package_intrinsics

The `core_package_intrinsics` directory serves as the central repository for documentation regarding the intrinsic components and foundational elements of the Cangjie programming language. This section is dedicated to the core package, which defines the essential data types and low-level functionalities that power the entire language ecosystem. It covers a broad spectrum of topics, from basic logic primitives to complex data representations required for modern computing. The documentation is crucial for understanding how the language handles memory, type safety, and system interactions at the most fundamental level. It is specifically designed for Cangjie developers who need a deep understanding of the language's type system, systems programmers building high-performance applications, and engineers involved in platform interoperability. By mastering these intrinsics, developers can optimize their code for efficiency, ensure robust behavior across different computing environments, and leverage the full power of the Cangjie runtime.

## Quick Navigation

**What do you want to learn?**

- Understand the basics of logic and empty types (Bool, Unit) → [1]
- Explore the full range of signed and unsigned integer types (Int8 to UInt64) → [1]
- Learn about floating-point precision and types (Float16, Float32, Float64) → [1]
- Implement system-level interoperability using pointers and C-style strings → [1]
- Handle Unicode text processing and character representation (Rune) → [1]

## Detailed Description

This section provides a granular look at the contents of the `core_package_intrinsics` directory. Given that the root directory does not contain standalone files, the focus is entirely on the subdirectory which houses the type definitions. This structure is intentional, designed to group all intrinsic type behaviors and properties into a single, navigable namespace. Developers should consult this section to understand the specific memory layouts, bitwise operations, and conversion capabilities of the language's primitive types. The documentation provided here is technical in nature and assumes familiarity with basic programming concepts, serving as a reference manual rather than a tutorial.

### 内置类型/
This directory provides comprehensive documentation for the built-in types in the Cangjie programming language. It covers fundamental primitive types such as Bool and Unit, a wide range of numeric types including signed and unsigned integers (Int8 to Int64, UInt8 to UInt64) and floating-point numbers (Float16, Float32, Float64), as well as specialized types for system interoperability (CPointer, CString) and Unicode character representation (Rune). This resource is essential for Cangjie developers, systems programmers, and engineers working on low-level data structures, FFI tasks, or precise numerical computations.