# extend_Int32_WrappingOpInt32

This directory provides comprehensive API documentation for the `WrappingOp` interface implementation specifically tailored for the `Int32` type within the Cangjie standard library. It details a robust set of arithmetic and bitwise operations—such as addition, multiplication, division, modulo, negation, increment, decrement, and shifting—that utilize a high-bit truncation strategy. This approach ensures that results wrap around upon overflow rather than causing runtime errors. Additionally, it specifies that shift operations rely on the low 5 bits of the shift count when values exceed bit width. This resource is essential for Cangjie developers who need deterministic integer overflow behavior for low-level programming, performance optimization, or algorithmic implementations where wrapping semantics are required.

## Quick Navigation

**What do you want to learn?**

*   Understand the complete implementation of the `WrappingOp` interface for `Int32`, including all supported arithmetic and bitwise operations → extend_Int32_WrappingOpIn_10more.md
*   Find specific API reference details for the subtraction operation (`wrappingSub`) and its overflow handling mechanism → func_wrappingSubInt32.md

## Detailed Description

### extend_Int32_WrappingOpIn_10more.md extend_Int32_WrappingOpIn_10more.md
This API documentation details the implementation of the `WrappingOp` interface for the `Int32` type in the Cangjie standard library. It covers a comprehensive set of arithmetic and bitwise operations, including addition, multiplication, division, modulo, negation, increment, decrement, and shifting. Key information highlights that all functions employ a high-bit truncation strategy to handle overflow, ensuring results wrap around without errors. Specifically, shift operations utilize the low 5 bits of the shift count when the value exceeds the bit width. This reference guide is intended for developers needing precise control over integer overflow behavior in Cangjie applications.

### func_wrappingSubInt32.md func_wrappingSubInt32.md
This document serves as API reference documentation for the Cangjie language function `wrappingSub`, detailing a subtraction operation that handles overflow via high-bit truncation. The content covers the function signature, functionality description, input parameters, and return values. Key information includes that the function accepts an `Int32` subtrahend and returns an `Int32` result, specifically truncating high bits when an overflow occurs rather than throwing an error. This reference is intended for Cangjie developers requiring precise control over integer arithmetic behavior, particularly in scenarios involving low-level programming or performance optimization where wrapping semantics are necessary.

**Keywords:** Cangjie, Int32, wrappingSub, subtraction, overflow, high-bit truncation.