<!-- cj-doc kind="api-member" level="7" id="std.unittest.mock.class.nonematcher.value" parent="std.unittest.mock.class.nonematcher.extension.extend-nonematcher" -->
# NoneMatcher.value

[← extend NoneMatcher](extensions/extend-nonematcher.md)

## 签名

```cangjie role=signature
public func value<T>(): Option<T>
```

框架需要调用的参数匹配器的返回值。

## 契约

返回值：

- Option\<T> - 与实际入参值类型匹配的值。
