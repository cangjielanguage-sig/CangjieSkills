# 不新建组件并直接变化原组件

This directory focuses on advanced UI animation techniques within the ArkUI framework, specifically targeting scenarios where developers need to modify existing components dynamically without the performance overhead of creating new instances. It covers the implementation of seamless "one-shot" animations, expandable lists, and complex layout transitions using state variables, `animateTo`, and `TransitionEffect`. The content includes practical code examples in both Cangjie and ArkTS, demonstrating how to achieve smooth property transitions like opacity, translation, and height changes directly on the original component. This resource is suitable for HarmonyOS and ArkUI developers looking to optimize rendering performance and create highly fluid, responsive user interfaces through direct component manipulation.

## Quick Navigation

- What do you want to learn?
    - Implement expandable animations in Cangjie using TransitionEffect and animateTo → 不新建组件并直接变化原组件_1.md
    - Create expandable UI layouts in ArkTS without new component instances → 不新建组件并直接变化原组件_2.md

## Detailed Description

### 不新建组件并直接变化原组件_1.md 不新建组件并直接变化原组件_1.md
This tutorial guides developers on implementing seamless "one-shot" animations by modifying existing components rather than creating new containers. It covers the core concept of combining transitions with attribute animations, outlines a two-step implementation process, and provides a Cangjie code example demonstrating an expandable card list. The document emphasizes using state variables to control component visibility and applying TransitionEffect, such as opacity and translation, alongside animateTo to achieve smooth layout changes. This guide targets ArkUI and Cangjie developers seeking to create complex, fluid UI transitions without the overhead of component recreation.

### 不新建组件并直接变化原组件_2.md 不新建组件并直接变化原组件_2.md
This document serves as a technical tutorial demonstrating how to implement an expandable UI animation in ArkTS without creating new component instances. The code example illustrates a list item that expands upon clicking, animating its height, avatar size, and image grid layout directly. Key sections include the UI structure using Row and Column, the conditional rendering of a comment section, and the onClick event handler utilizing animateTo for smooth property transitions. It highlights the use of TransitionEffect for opacity animations and dynamic width calculations for images. The target audience is HarmonyOS developers seeking to optimize UI performance and create fluid layout transitions using state-driven animations.