This directory contains technical documentation for a specific static function within the Cangjie programming language, identified as `runOutput`. The primary focus is on the function's capability to execute external subprocesses and capture their output streams programmatically. The documentation covers the complete API reference, including function signatures, parameter configurations for working directories and environments, and the structure of return values, which include exit status and byte arrays for standard output and error. Additionally, it provides a comprehensive guide to error handling, detailing specific exceptions like `IllegalArgumentException` and `ProcessException`. This resource is essential for Cangjie developers involved in system-level programming, process spawning, and robust error management, though it notes the function is deprecated in favor of `executeWithOutput`.