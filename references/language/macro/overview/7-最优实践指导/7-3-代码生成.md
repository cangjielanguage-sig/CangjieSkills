<!-- cj-doc kind="guide-leaf" level="6" id="language.macro.overview.7-最优实践指导.7-3-代码生成" parent="language.macro.overview.7-最优实践指导" -->
# 7.3 代码生成

[← 7. 最优实践指导](index.md)

- 优先使用 `quote(...)` + `$(...)` 插值生成代码，保持模板可读性
- 避免手动拼接 `Token`，除非需要动态构造标识符
- 插值不会自动添加括号，必要时手动包裹 `ParenExpr`
