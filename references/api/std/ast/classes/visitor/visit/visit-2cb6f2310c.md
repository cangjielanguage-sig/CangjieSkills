<!-- cj-doc kind="api-member" level="7" id="std.ast.class.visitor.visit.visit-2cb6f2310c" parent="std.ast.class.visitor.visit" -->
# Visitor.func visit(ParenType)

[← Visitor.visit](index.md)

## 签名

```cangjie role=signature
protected open func visit(_: ParenType): Unit
```

定义访问节点时的操作，需要重写。

## 契约

参数：

- _: ParenType - ParenType 类型的被遍历节点。
