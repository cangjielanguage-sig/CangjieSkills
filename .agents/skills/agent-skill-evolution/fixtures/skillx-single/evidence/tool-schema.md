# report_tool.py Schema

- Required option: `--input <csv-path>`.
- Required option: `--output <json-path>`.
- Required option: `--format json`.
- The unsupported option `--target` causes a command failure.
- Validate the schema with `python tools/report_tool.py --help` before writing the command into the Skill.
