# class_MemberAccess

This directory provides comprehensive API documentation for the `MemberAccess` class and related utility functions within the Cangjie language's Abstract Syntax Tree (AST) implementation. It details the structural definition of member access expressions, such as `T.a`, covering properties like `baseExpr`, `field`, `dot`, and generic components like `typeArguments`. Additionally, it explains critical AST manipulation mechanisms, including converting nodes back into token streams and traversing the syntax tree using the Visitor pattern. This documentation is specifically curated for compiler developers, language tooling engineers, and programmers who require a deep understanding of AST node construction, serialization, and traversal to build robust parsing or code generation tools for the Cangjie language.

## Quick Navigation

**What do you want to learn?**

*   **Class Structure**: Understand the `MemberAccess` class definition, inheritance, and properties like `baseExpr` and `typeArguments` → class_MemberAccess_10more.md
*   **Node Operations**: Learn how to convert AST nodes to tokens or traverse the tree using the Visitor pattern → func_toTokens_2more.md

## Detailed Description

### class_MemberAccess_10more.md class_MemberAccess_10more.md
This document provides API reference documentation for the MemberAccess class, which represents member access expressions in the Cangjie language's Abstract Syntax Tree (AST). It details the class structure, inheritance from Expr, and various mutable properties such as baseExpr, field, dot, and generic-related components like typeArguments. Key information includes the class's ability to represent expressions like T.a, constructors for initialization from Tokens, and specific exceptions (ASTException) for invalid token types. This technical reference is intended for compiler developers or programmers working with AST manipulation who need to programmatically access or construct member access nodes.

### func_toTokens_2more.md func_toTokens_2more.md
This document provides API reference documentation for two functions within an Abstract Syntax Tree (AST) package in the Cangjie language. It covers the toTokens function, which converts a syntax tree node into a Tokens type, and the traverse function, which iterates over the current node and its children using a Visitor pattern. Key takeaways include the specific return types for toTokens and the ability to prematurely halt traversal by overriding the visit function and calling breakTraverse. This guide is intended for developers working with AST manipulation who require precise definitions of node conversion and tree traversal mechanisms.