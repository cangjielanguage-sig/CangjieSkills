# convert_使用示例

This directory serves as a vital educational resource for developers utilizing the Cangjie programming language, offering a deep dive into string manipulation and data type conversion mechanisms. It comprehensively covers the `format` method for presenting integers, floating-point numbers, and characters, alongside detailed guides for parsing strings into fundamental types such as Booleans, Runes, and various numeric precisions. The examples provided illustrate advanced concepts like output alignment, width control, and robust error handling using `parse` and `tryParse`. This collection is specifically designed for Cangjie developers who need to master data input validation, transformation, and presentation, ensuring they can handle complex data structures effectively in their software projects.

## Quick Navigation

To help you find the specific information you need, use the decision tree below:

- **What do you want to learn?**
    - Do you need to format strings for output, controlling width and alignment for integers, floats, and characters? → format_使用示例.md
    - Are you looking to convert string inputs into native data types like Int, Float, or Bool, including error handling? → [2]

## Detailed Description

The following sections provide a comprehensive breakdown of the specific files and subdirectories available in this directory. Each component is designed to address specific aspects of data handling, from formatting output to parsing input, complete with practical examples and structural definitions.

### format_使用示例.md format_使用示例.md

This document serves as a practical tutorial demonstrating the usage of the `format` method in the Cangjie programming language for string manipulation. It is divided into three main sections covering the formatting of integers, floating-point numbers, and characters (Runes). The examples illustrate how to control output width and alignment using specific string patterns, such as prefixes for left alignment versus right alignment. It targets Cangjie developers seeking to understand data presentation and string formatting syntax.

### [2] convert_使用示例/

This directory serves as a comprehensive practical guide for developers working with the Cangjie programming language, specifically focusing on the standard library's string-to-type conversion capabilities. It provides detailed examples of parsing strings into a wide array of fundamental data types, including Boolean, Rune, signed and unsigned integers (spanning Int8 to Int64 and UInt8 to UInt64), and floating-point numbers (Float16 to Float64). By exploring both `parse` and `tryParse` methods, the content demonstrates how to effectively transform string representations into native values while handling errors. This resource is ideal for Cangjie developers who need a clear reference for type conversion syntax, error handling strategies, and understanding the nuances of parsing different data structures. It is essential for mastering data input and validation in Cangjie applications.