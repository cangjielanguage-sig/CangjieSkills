<!-- cj-doc kind="guide-leaf" level="5" id="language.for.1-for-in-基本语法.1-2-执行流程" parent="language.for.1-for-in-基本语法" -->
# 1.2 执行流程

[← 1. for-in 基本语法](index.md)

1. 求值 `sequence`，调用 `iterator()` 获取迭代器
2. 调用迭代器 `next()`：返回 `None` 则退出循环，返回 `Some(value)` 则继续
3. 将 `value` 绑定到 `item`，执行循环体
4. 回到步骤 2
