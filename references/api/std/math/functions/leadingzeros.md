<!-- cj-doc kind="api-member" level="5" id="std.math.func.leadingzeros" parent="std.math" -->
# leadingZeros

[← std.math](../index.md)

本页汇总 8 个同名重载；先按签名选择，再读取对应契约。

## leadingZeros(Int16)

### 签名

```cangjie role=signature
public func leadingZeros(x: Int16): Int64
```

求 16 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 16 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: Int16 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(Int32)

### 签名

```cangjie role=signature
public func leadingZeros(x: Int32): Int64
```

求 32 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 32 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: Int32 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(Int64)

### 签名

```cangjie role=signature
public func leadingZeros(x: Int64): Int64
```

求 64 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 64 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: Int64 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(Int8)

### 签名

```cangjie role=signature
public func leadingZeros(x: Int8): Int64
```

求 8 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 8 位有符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: Int8 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(UInt16)

### 签名

```cangjie role=signature
public func leadingZeros(x: UInt16): Int64
```

求 16 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 16 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: UInt16 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(UInt32)

### 签名

```cangjie role=signature
public func leadingZeros(x: UInt32): Int64
```

求 32 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 32 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: UInt32 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(UInt64)

### 签名

```cangjie role=signature
public func leadingZeros(x: UInt64): Int64
```

求 64 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 64 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: UInt64 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。

## leadingZeros(UInt8)

### 签名

```cangjie role=signature
public func leadingZeros(x: UInt8): Int64
```

求 8 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。

### 契约

功能：求 8 位无符号整数的二进制表达中的从最高位算起，连续位为 0 的个数。如果最高位不是 0，则返回 0。

参数：

- x: UInt8 - 需要求前导 0 的整数。

返回值：

- Int64 - 返回前导 0 的位数。
