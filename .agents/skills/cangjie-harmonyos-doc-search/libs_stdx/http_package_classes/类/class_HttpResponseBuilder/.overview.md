# class_HttpResponseBuilder

This directory provides comprehensive API reference documentation for the `HttpResponseBuilder` class within the Cangjie programming language ecosystem. It is designed to assist developers in understanding how to programmatically construct and configure HTTP response instances efficiently. The documentation covers the builder pattern implementation, allowing for method chaining to streamline the creation of complex responses. Key areas of focus include setting response bodies via various data types, managing headers and trailers, and defining status codes and protocol versions. This resource is essential for software developers and engineers working with the Cangjie HTTP package who need to generate robust and standards-compliant HTTP responses in their applications. It assumes familiarity with Cangjie syntax and general HTTP protocol standards.

## Quick Navigation

**What do you want to learn or configure?**

*   Learn about class initialization, body methods (byte arrays, streams, strings), and the build process → class_HttpResponseBuilder_8more.md
*   Configure specific response attributes like headers, trailers, status codes, and HTTP versions, including exception handling → func_headerString_String_7more.md

## Detailed Description

### class_HttpResponseBuilder_8more.md class_HttpResponseBuilder_8more.md
This document serves as API reference documentation for the Cangjie class `HttpResponseBuilder`, which is used to construct `HttpResponse` instances. The text outlines the class initialization and key methods such as `addHeaders`, `addTrailers`, and multiple overloaded `body` functions that accept byte arrays, input streams, or strings. It highlights the `build` method for finalizing the object. Key information includes the use of the builder pattern, where methods return the instance reference to facilitate method chaining. This guide is intended for developers using the HTTP package to programmatically generate HTTP responses.

### func_headerString_String_7more.md func_headerString_String_7more.md
This document serves as API reference documentation for the `HttpResponseBuilder` class in the Cangjie programming language, detailing methods used to construct and configure HTTP responses. It covers seven key functions: `header`, `request`, `setHeaders`, `setTrailers`, `status`, `trailer`, and `version`. Key information includes parameter definitions, return values (all returning the builder instance for method chaining), and specific exceptions like `HttpException` for invalid inputs or status codes outside the 100-599 range. The target audience is software developers integrating HTTP functionality, requiring knowledge of Cangjie syntax and HTTP protocol concepts.