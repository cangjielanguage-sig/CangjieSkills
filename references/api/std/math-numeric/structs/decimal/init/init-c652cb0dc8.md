<!-- cj-doc kind="api-member" level="7" id="std.math.numeric.struct.decimal.init.init-c652cb0dc8" parent="std.math.numeric.struct.decimal.init" -->
# Decimal.init(Int16)

[← Decimal.init](index.md)

## 签名

```cangjie role=signature
public init(val: Int16)
```

通过 16 位有符号整数构建 Decimal 结构体。

## 契约

功能：通过 16 位有符号整数构建 Decimal 结构体。默认采用精度值为 0，即无限精度进行构建。

参数：

- val: Int16 - 16 位有符号整数。
