<!-- cj-doc kind="api-member" level="6" id="std.ast.class.decl.prop-identifier" parent="std.ast.class.decl" -->
# Decl.identifier

[← Decl](index.md)

## 签名

```cangjie role=signature
public mut open prop identifier: Token
```

获取或设置声明节点的标识符，如 `class foo {}` 中的 `foo`。

## 契约

类型：Token
