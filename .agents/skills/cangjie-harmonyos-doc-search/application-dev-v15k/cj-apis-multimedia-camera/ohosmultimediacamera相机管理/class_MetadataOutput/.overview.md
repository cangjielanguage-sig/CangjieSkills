# class_MetadataOutput

This directory contains API reference documentation for the `MetadataOutput` class within the CameraKit framework, specifically designed for the Cangjie programming language. It covers the management of metadata streams, including event listener registration, unsubscription, and lifecycle control. Key topics include handling detected metadata objects, error callbacks, and resource management methods like start, stop, and release. This documentation is suitable for developers building camera applications who need to integrate metadata detection features and manage camera output streams efficiently.

## Quick Navigation

**What do you want to learn?**
*   Understand the class definition and system capabilities → class_MetadataOutput_3more.md
*   Manage event listeners and callbacks → func_offCameraCallbackType_2more.md, func_onCameraCallbackType_Callback1Argume_60b79106.md
*   Control the lifecycle of the metadata output → func_release_3more.md

**What do you want to do?**
*   Unregister event listeners using `off` methods → class_MetadataOutput_3more.md, func_offCameraCallbackType_2more.md
*   Register listeners for metadata objects or errors → func_offCameraCallbackType_2more.md, func_onCameraCallbackType_Callback1Argume_60b79106.md
*   Start, stop, or release metadata output resources → func_release_3more.md

## Detailed Description

### class_MetadataOutput_3more.md class_MetadataOutput_3more.md
This API reference document details the MetadataOutput class, which manages metadata streams within the CameraKit framework for the Cangjie language. The document covers the class definition, system capabilities, and two specific off methods used to unregister event listeners. Key information includes how to unsubscribe from callbacks for detected metadata objects or stream errors using specific CameraCallbackType arguments. Practical Cangjie code examples demonstrate the implementation. Targeted at developers building camera applications, this documentation requires prerequisite knowledge of Cangjie syntax and the CameraKit module.

### func_offCameraCallbackType_2more.md func_offCameraCallbackType_2more.md
This API reference document details methods for managing event callbacks within the Cangjie CameraKit, specifically for metadata output handling. It covers the `off` function to unsubscribe from specific event types and the `on` function to register listeners for detected metadata objects. Key information includes the requirement that the `on` method must use the `metadataObjectsAvailable` event type and a custom callback implementation. A critical constraint is that `off` cannot be invoked inside the `on` callback. This documentation targets developers using the Cangjie language to implement camera metadata detection features in their applications.

### func_onCameraCallbackType_Callback1Argume_60b79106.md func_onCameraCallbackType_Callback1Argume_60b79106.md
This API reference document details the `on` function for registering error callbacks in camera metadata streams using the Cangjie language. It outlines the function signature, system capabilities, parameter requirements, and usage constraints. The function requires the event type to be 'error' and passes a `BusinessException` to the callback. Crucially, it warns against calling the `off` method inside the callback. Intended for developers working with CameraKit, the document provides a code example to implement the `Callback1Argument` interface for handling metadata output errors.

### func_release_3more.md func_release_3more.md
This document provides API reference documentation for managing camera metadata output lifecycle in the Cangjie programming language. It details three specific functions: release() for freeing resources, start() for initiating metadata output, and stop() for halting the process. Key information includes system capabilities, starting version 19, specific error codes like 7400201, and practical code examples demonstrating usage within a camera context. The target audience is software developers integrating camera features into applications, requiring knowledge of the CameraKit and context management.