<!-- cj-doc kind="guide-leaf" level="5" id="language.collections.arraylist.1-概述" parent="language.collections.arraylist" -->
# 1. 概述

[← ArrayList 类型](index.md)

`ArrayList<T>` 是 `std.collection` 包中的 **class** 类型，使用前需导入：

```cangjie cjtest=syntax id=syntax-0e37dfc352-1 form=unit
import std.collection.*
```

- **动态数组** — 可自动扩容，无需预先指定大小
- **引用类型** — `let list2 = list1` 后两者共享数据，修改互相可见
- 实现接口：`List<T>`（继承 `Collection<T>`、`Iterable<T>`）
- `T` 可以是任意类型

---
