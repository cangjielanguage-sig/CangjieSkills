<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjlint.4-告警屏蔽.4-2-源代码注释屏蔽" parent="tools.cjlint.4-告警屏蔽" -->
# 4.2 源代码注释屏蔽

[← 4. 告警屏蔽](index.md)

**单行屏蔽**：
```cangjie cjtest=syntax id=syntax-af08059966-1 form=unit
func foo(a: Int64, b: Int64, c: Int64, d: Int64) { // cjlint-ignore !G.FUN.02 描述
    return a + b + c
}
```

**多行屏蔽**：
```cangjie cjtest=syntax id=syntax-af08059966-2 form=unit
// cjlint-ignore -start !G.FUN.02 描述
func foo(a: Int64, b: Int64, c: Int64, d: Int64) {
    return a + b + c
}
// cjlint-ignore -end 描述
```

> `cjlint-ignore`、选项（`-start`/`-end`）和规则名须在同一行。
