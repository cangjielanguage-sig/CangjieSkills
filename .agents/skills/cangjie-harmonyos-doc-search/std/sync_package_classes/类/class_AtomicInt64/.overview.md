# class_AtomicInt64

This directory contains API reference documentation for the `AtomicInt64` class in the Cangjie programming language. It focuses on providing low-level atomic primitives for 64-bit integers, essential for concurrent programming and thread-safe logic. The documentation covers core operations such as construction, value swapping, arithmetic addition and subtraction, bitwise manipulations, and compare-and-swap mechanisms. It distinguishes between current implementations using default memory ordering and deprecated versions requiring explicit `MemoryOrder` parameters. This resource is suitable for systems programmers and developers implementing high-performance, thread-safe code who need precise control over atomic memory operations.

## Quick Navigation

**What do you want to learn?**

- Understand the class constructor, basic structure, and compare-and-swap logic → class_AtomicInt64_5more.md
- Perform atomic arithmetic (add, subtract) or bitwise operations (and, or) → func_fetchAddInt64_MemoryO_6more.md
- Perform atomic subtraction, bitwise XOR, load, or store operations → func_fetchSubInt64_MemoryO_7more.md
- Exchange values atomically using swap operations → func_swapInt64_2more.md

## Detailed Description

### class_AtomicInt64_5more.md
This document serves as API reference documentation for the AtomicInt64 class in the Cangjie programming language, detailing atomic operations for 64-bit integers. The content covers the class constructor, the compareAndSwap function for atomic updates, a deprecated version with custom memory ordering, and the fetchAdd function for atomic addition. Key information includes the use of default memory ordering for standard operations and the return values for each function, such as the previous value in fetchAdd. This guide is intended for developers implementing concurrent programming or thread-safe logic who require low-level atomic primitives. Keywords: AtomicInt64, Cangjie, atomic operations, compareAndSwap, fetchAdd, thread safety.

### func_fetchAddInt64_MemoryO_6more.md
This document serves as API reference documentation for atomic arithmetic and bitwise operations on Int64 types in the Cangjie programming language. It details specific functions including fetchAdd, fetchAnd, fetchOr, and fetchSub, distinguishing between current implementations using default memory ordering and deprecated versions that accept explicit MemoryOrder parameters. Key information indicates that all operations atomically modify the instance and return the pre-modification value, while explicitly warning developers to migrate away from deprecated overloads. The target audience is systems programmers and developers requiring thread-safe integer manipulation in concurrent environments.

### func_fetchSubInt64_MemoryO_7more.md
This document serves as API reference documentation for atomic operations on Int64 types in the Cangjie programming language. It details functions for atomic subtraction, bitwise XOR, loading, and storing values, distinguishing between current implementations using default memory ordering and deprecated versions requiring explicit MemoryOrder parameters. Key information includes the deprecation of functions accepting MemoryOrder arguments, directing users to replace them with simplified overloads like fetchSub(Int64) and load(). The target audience is Cangjie developers implementing thread-safe code who need to understand atomic arithmetic and bitwise manipulation.

### func_swapInt64_2more.md
This API reference document details the `swap` function for `AtomicInt64` in the Cangjie programming language, explaining atomic value exchange operations. The content covers two function signatures: the standard `swap(Int64)` using default memory ordering and a deprecated `swap(Int64, MemoryOrder)` overload. Key information includes parameter definitions, return values of the previous state, and a practical code example demonstrating usage alongside other atomic methods like `compareAndSwap` and `fetchAdd`. The document highlights that the custom memory ordering version is deprecated. This guide is intended for Cangjie developers implementing thread-safe concurrency mechanisms who require precise control over atomic memory operations.