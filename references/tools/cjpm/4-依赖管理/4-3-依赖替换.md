<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjpm.4-依赖管理.4-3-依赖替换" parent="tools.cjpm.4-依赖管理" -->
# 4.3 依赖替换

[← 4. 依赖管理](index.md)

配置入口：`[replace]`。

```toml
[replace]
  # 将远程依赖替换为本地路径（调试时常用）
  logger = { path = "./local_logger" }
```
