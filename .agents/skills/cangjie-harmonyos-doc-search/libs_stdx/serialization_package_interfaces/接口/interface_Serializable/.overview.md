# interface_Serializable

This directory provides API documentation for the Cangjie `Serializable` interface. It covers implementations for primitives (Int, Float, Bool), collections (ArrayList, HashMap, HashSet), and utilities (Option, Array). The content explains converting native types to and from `DataModel`, handling constraints, and managing `DataModelException` errors. It is designed for Cangjie developers implementing data persistence or transmission logic.

## Quick Navigation

What do you want to learn?

*   **Core Interface & Arrays** → interface_Serializable_4more.md
*   **Collections**
    *   ArrayList & Bool → extendT_ArrayListT_Seriali_2more.md
    *   HashSet & Int16 → extendT_HashSetT_Serializa_2more.md
    *   HashMap & Float64 → extend_Float64_Serializabl_2more.md
*   **Numeric Types**
    *   Signed Integers (Int8, Int32, Int64) → extend_Int32_Serializable_3more.md
    *   Unsigned Integers (UInt8, UInt16, UInt32, UInt64) → extend_UInt16_Serializable_3more.md, extend_UInt8_Serializable.md
    *   Floats (Float16, Float32, Float64) → extend_Float16_Serializabl_2more.md, extend_Float64_Serializabl_2more.md
*   **Utilities & Text** (Option, Rune, String) → extendT_OptionT_Serializab_3more.md

## Detailed Description

### extendT_ArrayListT_Seriali_2more.md extendT_ArrayListT_Seriali_2more.md
This API documentation file details the implementation of the Serializable interface for ArrayList and Bool types in the Cangjie programming language. It covers two main extensions: a generic ArrayList implementation requiring elements to be Serializable, and a specific implementation for the Bool type. Key sections include static deserialize methods for converting DataModel objects back to native types and instance serialize methods for converting types into DataModel formats like DataModelSeq and DataModelBool. The document specifies parameters, return values, and potential DataModelException errors. This reference guide targets developers needing to handle data persistence or transmission, providing the necessary function signatures and constraints for serialization operations.

### extendT_HashSetT_Serializa_2more.md extendT_HashSetT_Serializa_2more.md
This API reference document details the implementation of the Serializable interface for HashSet and Int16 types within the Cangjie serialization framework. It covers two primary extensions: a generic HashSet requiring elements to be Serializable, Hashable, and Equatable, and a specific implementation for the Int16 primitive type. Key information includes the static deserialize method for reconstructing objects from DataModel and the serialize method for converting objects into DataModelSeq or DataModelInt formats. The documentation specifies exceptions like DataModelException for type mismatches. This guide targets developers implementing data persistence, necessitating an understanding of generic constraints and the DataModel class hierarchy.

### extendT_OptionT_Serializab_3more.md extendT_OptionT_Serializab_3more.md
This API reference document details the implementation of the Serializable interface for Option, Rune, and String types in the Cangjie language. It covers three specific extensions: generic Option handling, Rune character serialization, and String serialization. Each section defines static `deserialize` methods to convert DataModel objects back to types and instance `serialize` methods to convert types to DataModel. Key takeaways include the constraint that Option requires T to be Serializable, and specific exceptions like DataModelException are thrown for type mismatches. The target audience is developers integrating serialization capabilities, requiring knowledge of the DataModel class and generic constraints.

### extend_Float16_Serializabl_2more.md extend_Float16_Serializabl_2more.md
This API reference documentation details the implementation of the Serializable interface for the Float16 and Float32 data types in the Cangjie programming language. The document is structured into two primary sections, one for each floating-point type, outlining their serialization capabilities. Key information includes the static deserialize function, which converts DataModel objects specifically DataModelFloat or DataModelInt into the target float type, and the serialize function, which converts the float back into a DataModelFloat. The documentation notes that deserialize throws a DataModelException if the input type is incompatible. This reference is intended for developers implementing data serialization logic who require precise handling of floating-point values. Keywords: Cangjie, Serializable, Float16, Float32, DataModel, serialization, API reference.

### extend_Float64_Serializabl_2more.md extend_Float64_Serializabl_2more.md
This document provides API reference documentation for implementing the Serializable interface on Float64 and HashMap types within the Cangjie programming language. It details two main extensions: one for the Float64 primitive type and a generic extension for HashMap collections requiring specific constraints. Key information includes the specifications for static deserialize methods, which convert DataModel objects back to native types, and instance serialize methods for converting native types to DataModel representations. The text explicitly lists required input types, such as DataModelFloat or DataModelStruct, and potential DataModelExceptions for invalid type conversions. Intended for Cangjie developers, this reference ensures correct data handling during serialization and deserialization processes.

### extend_Int32_Serializable_3more.md extend_Int32_Serializable_3more.md
This API reference document details the implementation of the Serializable interface for the Int8, Int32, and Int64 types in the Cangjie language. It covers three main sections, each dedicated to a specific integer type extension. Key information includes the `deserialize` static function, which converts a DataModel to an integer, and the `serialize` function, which converts an integer to a DataModelInt. Both methods handle type checking and throw DataModelException on mismatch. This documentation targets developers implementing data serialization, providing specific function signatures, parameters, and return values for integer data handling.

### extend_UInt16_Serializable_3more.md extend_UInt16_Serializable_3more.md
This API reference document details the implementation of the Serializable interface for the unsigned integer types UInt16, UInt32, and UInt64 in the Cangjie language. The content is structured into three identical sections, one for each type, explaining how they extend the Serializable interface. Key information includes the static deserialize method, which converts a DataModel to the specific integer type, and the serialize method, which converts the integer back to a DataModelInt. Both methods handle type checking, with deserialize throwing a DataModelException if the input is not a DataModelInt. This documentation targets developers implementing data serialization and deserialization logic for unsigned integers.

### extend_UInt8_Serializable.md extend_UInt8_Serializable.md
This API documentation details the implementation of the Serializable interface for the UInt8 type in the Cangjie programming language. The document outlines the extension declaration, a static deserialize method, and an instance serialize method. Key takeaways include that deserialize converts a DataModel to UInt8, throwing a DataModelException if the input is not a DataModelInt, while serialize transforms a UInt8 instance into a DataModelInt. This reference guide targets Cangjie developers working with data serialization who need to convert between UInt8 values and the DataModel format.

### interface_Serializable_4more.md interface_Serializable_4more.md
This document provides API reference documentation for the Cangjie Serializable interface, which standardizes object serialization and deserialization processes. It covers the interface definition, including the static deserialize function for converting DataModel to objects and the serialize function for converting objects to DataModel. The text details supported types like basic data types and collections, and explains the specific implementation for the Array type, which handles DataModelSeq. Key information includes exception handling for DataModelException and type constraints. Intended for Cangjie developers, this technical reference guides the implementation of serialization logic for custom and standard types.