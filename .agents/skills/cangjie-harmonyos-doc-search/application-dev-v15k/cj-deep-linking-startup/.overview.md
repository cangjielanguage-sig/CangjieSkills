# cj-deep-linking-startup

This directory serves as a specialized startup guide for implementing Deep Linking functionality within the HarmonyOS ecosystem, specifically designed for developers working with the Cangjie programming language. It provides a structured approach to enabling seamless inter-application navigation and robust web-to-native transitions, which are critical for modern app ecosystems. The guide delves into the technical architecture of URI-based app launching, the logic behind implicit Want matching, and the specific configuration steps required for both calling and target applications. This resource is highly suitable for HarmonyOS developers utilizing the ArkUI framework who need to practically implement the `openLink` API, correctly configure `module.json5` files, and handle lifecycle callbacks such as `onNewWant` to ensure smooth user experiences across different application boundaries.

## Quick Navigation

What do you want to learn?

- Understand the principles of URI-based app launching and implicit Want matching → [使用Deep_Linking实现应用间跳转/]
- Configure `module.json5` for both calling and target applications → [使用Deep_Linking实现应用间跳转/]
- Implement the `openLink` API and handle `onNewWant` lifecycle callbacks → [使用Deep_Linking实现应用间跳转/]
- Enable seamless cross-app jumps and web-to-native transitions in HarmonyOS → [使用Deep_Linking实现应用间跳转/]

## Detailed Description

### 使用Deep_Linking实现应用间跳转/
This directory provides a comprehensive guide on implementing inter-application navigation using Deep Linking technology within the HarmonyOS ecosystem. It covers the underlying principles of URI-based app launching, implicit Want matching, and the specific configuration required for both calling and target applications. Key topics include the `openLink` API, `module.json5` configuration, and lifecycle callbacks like `onNewWant`. This resource is suitable for HarmonyOS developers using the Cangjie programming language and ArkUI framework who need to enable seamless cross-app jumps and web-to-native transitions.