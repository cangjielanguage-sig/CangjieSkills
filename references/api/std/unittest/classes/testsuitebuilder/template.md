<!-- cj-doc kind="api-member" level="6" id="std.unittest.class.testsuitebuilder.template" parent="std.unittest.class.testsuitebuilder" -->
# TestSuiteBuilder.template

[← TestSuiteBuilder](index.md)

## 签名

```cangjie role=signature
public func template(template: TestSuite): TestSuiteBuilder
```

执行此方法可为测试套件设置模板。

## 契约

参数

- template: TestSuite - 将作为模板的测试套件。

返回值：

- TestSuiteBuilder - 测试组合构造器。
