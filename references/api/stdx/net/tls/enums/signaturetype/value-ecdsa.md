<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.enum.signaturetype.value-ecdsa" parent="stdx.net.tls.enum.signaturetype" -->
# SignatureType.ECDSA

[← SignatureType](index.md)

## 签名

```cangjie role=signature
ECDSA
```

创建一个 `ECDSA` 类型的枚举实例，表示采用椭圆曲线数字签名算法。
