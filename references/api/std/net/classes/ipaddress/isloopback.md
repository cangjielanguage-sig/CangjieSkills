<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipaddress.isloopback" parent="std.net.class.ipaddress" -->
# IPAddress.isLoopback

[← IPAddress](index.md)

## 签名

```cangjie role=signature
public open func isLoopback(): Bool
```

判断此 IPAddress 对象是不是环回地址。

## 契约

返回值：

- Bool - 返回 true 表示是环回地址，否则返回 false。
