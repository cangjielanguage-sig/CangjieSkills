<!-- cj-doc kind="api-member" level="6" id="std.ast.class.performexpr.prop-keyword" parent="std.ast.class.performexpr" -->
# PerformExpr.keyword

[← PerformExpr](index.md)

## 签名

```cangjie role=signature
public mut prop keyword: Token
```

获取或设置 PerformExpr 节点中的 `perform` 关键字。

类型：Token

## 异常

- ASTException — 当给定的 Token 不是 `perform` 关键字时抛出。

