# distributionFilter标签

This directory provides comprehensive technical documentation for the `distributionFilter` tag within the HarmonyOS development ecosystem. It focuses on enabling developers to implement precise HAP (HarmonyOS Ability Package) distribution strategies by filtering based on specific device hardware capabilities and regional settings. Key topics include screen shape, window dimensions, pixel density, and country codes. These resources are specifically designed for HarmonyOS developers building multi-module applications who require granular control over cloud distribution. By utilizing these configuration guides, developers can ensure that the correct application variants are delivered to the appropriate target devices, optimizing user experience and resource management across diverse hardware profiles like smart wearables and tablets. The documentation also addresses complex scenarios such as overlapping device types and attribute consistency requirements.

## Quick Navigation

- **What do you want to learn?**
    - Understand the fundamental definition, usage scenarios for overlapping device types, and general configuration rules for the `distributionFilter` tag → distributionFilter标签.md
    - Explore specific attribute definitions (screenShape, screenWindow, screenDensity, countryCode), policy parameters, and view practical JSON implementation examples → screenShape标签_4more.md

## Detailed Description

The following documents offer detailed technical specifications and implementation guides for configuring distribution filters in HarmonyOS applications.

### distributionFilter标签.md distributionFilter标签.md
This technical reference document defines the distributionFilter tag used to specify precise HAP distribution strategies based on device specifications. It covers usage scenarios for overlapping device types, configuration rules, and attribute definitions including screenShape, screenWindow, screenDensity, and countryCode. Key takeaways include the requirement for attribute consistency across all Entry modules and the specific placement within the /resources/base/profile directory. This guide targets HarmonyOS developers managing multi-module applications requiring granular cloud distribution control to ensure the correct application package is delivered to the target device.

### screenShape标签_4more.md screenShape标签_4more.md
This reference document defines configuration tags for filtering application distribution based on device capabilities and regions. It details the attributes for screenShape, screenWindow, screenDensity, and countryCode tags, specifying their policy (include or exclude) and value parameters. Key information includes the specific pixel density ranges and screen dimension formats supported. The document provides a practical JSON example showing how to define these filters in a resource file and link them via module.json5 metadata. This guide is intended for developers needing to target specific hardware profiles, such as smart wearables, or restrict app distribution to particular countries.