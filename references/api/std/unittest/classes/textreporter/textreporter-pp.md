<!-- cj-doc kind="api-member" level="6" id="std.unittest.class.textreporter.textreporter-pp" parent="std.unittest.class.textreporter" -->
# TextReporter<PP>.TextReporter(PP)

[← TextReporter<PP>](index.md)

## 签名

```cangjie role=signature
public TextReporter(let into!: PP)
```

TextReporter 构造函数。

## 契约

参数：

- into!: PP - PrettyPrinter 的子类。打印报告。
