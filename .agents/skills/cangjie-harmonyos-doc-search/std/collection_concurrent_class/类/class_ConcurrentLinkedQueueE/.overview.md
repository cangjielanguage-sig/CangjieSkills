# class_ConcurrentLinkedQueueE

This directory contains comprehensive API reference documentation for the Cangjie `ConcurrentLinkedQueue` class, a thread-safe, non-blocking collection specifically designed for high-concurrency scenarios. It covers class constructors, the non-atomic size property, and essential methods for element manipulation, with a specific focus on the `add` function for enqueuing. Key topics include the deprecation of legacy methods like `dequeue`, `enqueue`, and `head` in favor of `remove`, `add`, and `peek`, respectively. The documentation also explains the queue's utility in producer-consumer models and task scheduling. This resource is suitable for Cangjie developers who require knowledge of concurrent programming and generic collections to build efficient multi-threaded applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the class overview, constructors, and the `add` method (including deprecated methods) → class_ConcurrentLinkedQueu_8more.md
*   Explore specific queue manipulation functions like `isEmpty`, `peek`, `remove`, and array conversion → func_isEmpty_5more.md

## Detailed Description

### class_ConcurrentLinkedQueu_8more.md
This document serves as API reference documentation for the Cangjie ConcurrentLinkedQueue class, a thread-safe, non-blocking collection designed for high-concurrency scenarios. It covers class constructors, the non-atomic size property, and essential methods for element manipulation, specifically highlighting the add function for enqueuing. Key information includes the deprecation of methods like dequeue, enqueue, and head in favor of remove, add, and peek respectively. The document explains the queue's utility in producer-consumer models and task scheduling. Targeted at Cangjie developers, it requires knowledge of concurrent programming and generic collections.

### func_isEmpty_5more.md
This document provides API reference documentation for five essential queue manipulation functions in the Cangjie programming language. It details the isEmpty, iterator, peek, remove, and toArray methods, explaining their functionality and return types. Key takeaways include the use of Option for safe element access and warnings that iterator and toArray are non-atomic operations that do not guarantee thread-safe static snapshots during concurrent modifications. This technical reference is intended for Cangjie developers working with collections, specifically those needing to manage queue data structures and understand concurrency implications.