# 枚举

This directory serves as a specialized repository for enumeration definitions within the Cangjie programming language, with a primary focus on data structures utilized in high-performance stream processing. It comprehensively covers the `JsonToken` enum, a fundamental component for parsing, validating, and handling JSON data streams efficiently. The content includes detailed API references for various token types, ranging from structural markers like arrays and objects to value representations such as booleans and nulls, alongside operator overloads for equality comparisons. This resource is meticulously designed for software developers and systems engineers who require a granular understanding of token types to implement robust JSON parsing logic, manage data flow elements, and ensure type safety within their applications using the `encoding_json_stream` package. By mastering these enumerations, users can effectively utilize methods like `JsonReader.peek()` to anticipate and handle data structures accurately, thereby optimizing the performance and reliability of their data processing workflows.

## Quick Navigation

**What do you want to learn?**
- Understand the core structure of JSON tokens in the Cangjie language → [1] `enum_JsonToken/`
- Identify specific enum variants like `JsonNull`, `BeginArray`, or `BeginObject` → [1] `enum_JsonToken/`
- Access API references for implementing JSON stream processing logic → [1] `enum_JsonToken/`
- Learn about operator overloads and equality comparisons for tokens → [1] `enum_JsonToken/`
- Explore the relationship between tokens and stream processing methods → [1] `enum_JsonToken/`

## Detailed Description

The following section provides an in-depth look at the specific enumeration types available in this directory. It offers the necessary technical details to integrate these enums into your Cangjie projects effectively.

### enum_JsonToken/
This directory contains comprehensive API reference documentation for the `JsonToken` enum within the Cangjie programming language, specifically designed to support the `encoding_json_stream` package. It serves as a critical resource for understanding the structural and value types that constitute JSON encoded strings during stream processing operations. The documentation thoroughly details specific enum variants such as `JsonNull`, `JsonBool`, `BeginArray`, `BeginObject`, and `Name`, alongside essential functional aspects like operator overloads for equality comparisons. This content is primarily suitable for software developers and systems engineers who are actively implementing JSON stream parsing logic and require a granular understanding of token types to correctly identify and handle data flow elements using methods like `JsonReader.peek()`. By consulting this documentation, developers can ensure they are correctly interpreting the stream of tokens to reconstruct valid JSON objects or handle errors gracefully.