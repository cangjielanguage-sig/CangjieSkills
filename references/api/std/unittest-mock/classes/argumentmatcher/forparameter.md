<!-- cj-doc kind="api-member" level="6" id="std.unittest.mock.class.argumentmatcher.forparameter" parent="std.unittest.mock.class.argumentmatcher" -->
# ArgumentMatcher.forParameter

[← ArgumentMatcher](index.md)

## 签名

```cangjie role=signature
public func forParameter(name: String): ArgumentMatcher
```

配置所匹配的参数名称。

## 契约

参数：

- name: String - 所匹配的参数名称。

返回值：

- ArgumentMatcher - 被配置的参数匹配器。
