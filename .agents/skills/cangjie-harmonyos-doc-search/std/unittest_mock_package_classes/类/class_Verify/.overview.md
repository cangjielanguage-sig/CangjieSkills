# class_Verify

This directory provides comprehensive API documentation for the `Verify` class within the Cangjie unit testing framework. It is designed to assist developers in defining and executing behavior verification on mock objects. The content covers essential concepts such as verification statements using the `@Called` macro, execution frequency checks, argument validity, and call order validation. Key static methods detailed include `clearInvocationLog`, `noInteractions`, `ordered`, `that`, and `unordered`. This documentation is suitable for developers utilizing the mock framework to write precise unit tests, assuming familiarity with Cangjie syntax and general mocking principles.

## Quick Navigation

What do you want to learn?

*   Get an overview of the Verify class, the `@Called` macro, and utility methods like `clearInvocationLog` → class_Verify_4more.md
*   Understand sequential verification (`ordered`) and single statement validation (`that`) → static_func_orderedArrayVe_3more.md
*   Explore non-sequential verification (`unordered`) with arrays or closures → static_func_unorderedArray_3more.md

## Detailed Description

### class_Verify_4more.md class_Verify_4more.md
This API documentation details the Verify class, which provides static methods for defining and executing behavior verification on mock objects within the Cangjie unit testing framework. It covers the class overview, the concept of verification statements using the @Called macro, and specific static methods like clearInvocationLog, noInteractions, and ordered. The document explains how to verify execution frequency, argument validity, and call order. It highlights that ordered requires exhaustive matching and disjoint statements, while clearInvocationLog helps narrow the verification scope. This guide targets developers using the mock framework to write unit tests, requiring knowledge of Cangjie syntax and mocking concepts.

### static_func_orderedArrayVe_3more.md static_func_orderedArrayVe_3more.md
This API documentation file details static verification functions used to validate mock object behavior in unit tests. It covers three main functions: ordered for sequential verification, that for single statement validation, and unordered for non-sequential checks. Key information includes that ordered ensures execution order and exact counts, while unordered ignores order but requires exhaustive matching via a closure. All functions require disjoint verification statements and throw VerificationFailedException upon failure. This reference guide targets developers using the unittest mock package to implement precise behavioral testing.

### static_func_unorderedArray_3more.md static_func_unorderedArray_3more.md
This API reference document details the overloaded `Verify.unordered` functions in a Cangjie testing framework, designed to validate mock method executions without enforcing order. It covers three function signatures that accept arrays of `VerifyStatement` or a closure for dynamic collection, alongside an `Exhaustiveness` parameter. Key takeaways include the requirement that verification statements must be disjoint to prevent ambiguity and that the default mode is exhaustive matching. The document provides code examples illustrating correct usage and exception handling. This resource is intended for developers implementing unit tests who need to verify mock interactions flexibly.