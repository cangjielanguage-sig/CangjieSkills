# 无限滚动

This directory serves as a comprehensive resource for implementing and optimizing infinite scrolling mechanisms within the ArkUI framework, specifically utilizing the WaterFlow component and the Cangjie language. It covers essential techniques for dynamic data loading, performance optimization, and handling large datasets efficiently to ensure a smooth user experience. Key topics include pre-loading strategies to prevent stuttering, utilizing `onReachEnd` events, implementing `LazyForEach`, and managing custom data sources. This resource is ideal for ArkUI developers and UI engineers seeking to create fluid, responsive user interfaces with high-performance rendering logic and seamless scrolling interactions.

## Quick Navigation

**What do you want to learn?**

*   Optimize performance by pre-loading data before the user reaches the end? → 提前新增数据.md 提前新增数据.md
*   Implement standard infinite scrolling triggered when reaching the bottom? → 到达末尾时新增数据/

## Detailed Description

### 提前新增数据.md 提前新增数据.md
This tutorial explains how to optimize infinite scrolling performance in a WaterFlow component by pre-loading data before reaching the list's end. It addresses the stuttering caused by loading data solely in onReachEnd and introduces a method using the onScrollIndex callback to trigger data addition earlier. The document provides a complete Cangjie code example featuring a custom MyDataSource and logic to append new items when the last visible item is within a specific threshold of the dataset's end. This guide is designed for developers using the ArkUI framework and Cangjie language to create fluid user interfaces. Keywords: Infinite scrolling, WaterFlow, onScrollIndex, LazyForEach, Cangjie, pre-loading, performance optimization.

### 到达末尾时新增数据/
This directory provides comprehensive technical guidance on implementing and optimizing dynamic data loading within WaterFlow components, specifically focusing on scenarios where new data must be appended as the user scrolls to the bottom. It addresses the complete implementation of infinite scrolling mechanisms, utilizing key ArkUI concepts such as the `onReachEnd` event, `LazyForEach`, and custom data sources. Furthermore, it emphasizes critical performance optimization strategies to prevent UI lag, contrasting efficient data appending with inefficient full dataset reloads. The content is tailored for ArkUI developers and UI engineers utilizing the Cangjie language who aim to create high-performance, responsive applications with smooth scrolling experiences and efficient rendering logic. By following these guides, developers can ensure their applications handle large datasets gracefully without sacrificing user experience.