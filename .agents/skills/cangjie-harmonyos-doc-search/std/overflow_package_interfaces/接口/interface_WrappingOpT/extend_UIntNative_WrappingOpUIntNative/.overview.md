# extend_UIntNative_WrappingOpUIntNative

This directory provides comprehensive API documentation for the `WrappingOp` interface implementation specifically tailored for the `UIntNative` type within the Cangjie programming language. It details a suite of arithmetic and bitwise functions designed to manage integer overflow through a high-bit truncation strategy, ensuring that results wrap around predictably instead of triggering runtime errors. The documentation covers essential operations such as addition, multiplication, division, modulo, and bit shifting. This resource is ideal for Cangjie developers who require precise, low-level control over integer behavior and need to ensure consistent overflow handling in performance-critical or systems-level applications.

## Quick Navigation

**What do you want to learn?**

*   Explore the main WrappingOp interface implementation (Add, Mul, Div, Mod, Inc, Dec, Neg, Shl) → extend_UIntNative_Wrapping_9more.md
*   Reference specific functions for right shift and subtraction operations → func_wrappingShrUInt64_2more.md

## Detailed Description

### extend_UIntNative_Wrapping_9more.md extend_UIntNative_Wrapping_9more.md

This API documentation details the implementation of the `WrappingOp` interface for the `UIntNative` type in the Cangjie language. The document outlines a series of arithmetic and bitwise functions, including `wrappingAdd`, `wrappingMul`, `wrappingDiv`, `wrappingMod`, `wrappingInc`, `wrappingDec`, `wrappingNeg`, and `wrappingShl`. Key information highlights that all operations employ a high-bit truncation strategy to handle overflow, ensuring results wrap around instead of raising errors. Specifically, the left shift operation handles cases where the shift amount exceeds the bit width. This reference guide is intended for Cangjie developers who need precise control over integer overflow behavior in their applications.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md

This document serves as API reference documentation for specific wrapping arithmetic and bitwise operations within the Cangjie programming language. It details two primary functions: `wrappingShr`, which performs a right shift using a high-order truncation strategy where the shift amount is determined by low-order bits if it exceeds the bit width, and `wrappingSub`, which executes subtraction that truncates high-order bits upon overflow. Key information includes the parameter types, such as `UInt64` and `UIntNative`, and the return type `UIntNative`. This guide is intended for developers working with low-level bit manipulation who need predictable overflow behavior in their Cangjie applications.