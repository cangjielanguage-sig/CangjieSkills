<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arraydeque.toarray" parent="std.collection.class.arraydeque" -->
# ArrayDeque<T>.toArray

[← ArrayDeque<T>](index.md)

## 签名

```cangjie role=signature
public func toArray(): Array<T>
```

返回一个数组，其包含此双端队列中的所有元素，且顺序为从前到后的顺序。

## 契约

返回值：

- Array\<T> - T 类型数组。
