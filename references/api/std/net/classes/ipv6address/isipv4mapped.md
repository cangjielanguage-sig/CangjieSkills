<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipv6address.isipv4mapped" parent="std.net.class.ipv6address" -->
# IPv6Address.isIPv4Mapped

[← IPv6Address](index.md)

## 签名

```cangjie role=signature
public func isIPv4Mapped(): Bool
```

判断此 IPv6Address 对象是不是 IPv4 映射地址。

## 契约

返回值：

- Bool - 返回 true 表示是 IPv4 映射地址，否则返回 false。
