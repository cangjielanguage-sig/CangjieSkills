<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.crypto.struct.paddingmode.field-pkcs7padding" parent="stdx.crypto.crypto.struct.paddingmode" -->
# PaddingMode.PKCS7Padding

[← PaddingMode](index.md)

## 签名

```cangjie role=signature
public static let PKCS7Padding: PaddingMode
```

采用 PKCS7 协议填充，PKCS7Padding 初始值是 PaddingMode(1)。

## 契约

类型：PaddingMode
