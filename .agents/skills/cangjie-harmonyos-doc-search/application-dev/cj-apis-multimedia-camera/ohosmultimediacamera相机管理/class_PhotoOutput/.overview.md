# class_PhotoOutput

This directory contains API reference documentation for the `PhotoOutput` class within the Cangjie programming language's CameraKit framework. It covers the core functionality for managing photo capture sessions, including configuration, event handling, and resource management. Key topics include capturing photos with custom settings, enabling features like mirroring and moving photos, querying system capabilities, and managing camera event listeners for capture lifecycle events. This documentation is suitable for developers integrating advanced multimedia camera features into Cangjie applications, providing detailed syntax, error codes, and code examples for effective implementation.

## Quick Navigation

What do you want to do?

*   Initialize the class and capture photos? → class_PhotoOutput_3more.md
*   Configure output settings (mirror, moving photo, codec)? → func_enableMirrorBool_2more.md, func_release_2more.md
*   Retrieve profiles or rotation info? → func_getActiveProfile_2more.md
*   Check supported capabilities? → func_getSupportedMovingPho_3more.md
*   Register event listeners (capture ready, error, end, shutter)? → func_offCameraEvents_2more.md, func_onCameraEvents_Callback0Argument.md, func_onCameraEvents_Callback1ArgumentCapt_9d6d8cf8.md, func_onCameraEvents_Callback1ArgumentFloat64.md, func_onCameraEvents_Callback1ArgumentFram_2017c58d.md, func_onCameraEvents_Callback1ArgumentFram_947cb8e7.md
*   Unregister event listeners? → func_offCameraEvents_Callb_2more.md

## Detailed Description

### class_PhotoOutput_3more.md class_PhotoOutput_3more.md
API reference documentation for the PhotoOutput class in the Cangjie programming language, detailing output information for photo capture sessions. It covers the class inheritance from CameraOutput and defines two primary methods: capture() for default settings and capture(PhotoCaptureSetting) for custom parameters. Key sections include system capabilities, version requirements, detailed error codes for exceptions such as invalid parameters or service failures, and executable code examples. This guide is intended for developers integrating camera functionality, requiring familiarity with Cangjie syntax and multimedia camera core concepts.

### func_enableMirrorBool_2more.md func_enableMirrorBool_2more.md
API reference documentation for Cangjie language functions within the CameraKit, specifically detailing how to configure photo output behaviors. It covers two primary functions: enableMirror, which toggles image mirroring, and enableMovingPhoto, which activates dynamic photo capture. Key information includes parameter definitions, required system capabilities starting from version 22, and specific error codes such as parameter errors or permission denials. The documentation highlights prerequisites like checking feature support and obtaining microphone permissions.

### func_getActiveProfile_2more.md func_getActiveProfile_2more.md
API reference documentation for the Cangjie CameraKit, detailing functions for managing camera profiles and image rotation. It covers the getActiveProfile method, which retrieves the current active configuration, and getPhotoRotation, which calculates the necessary rotation angle based on device orientation. Key information includes function signatures, parameter constraints, return types, and specific error codes like 7400201. The text also defines critical concepts such as device natural direction and camera lens angle.

### func_getSupportedMovingPho_3more.md func_getSupportedMovingPho_3more.md
API reference documentation for camera capability query functions in the Cangjie language. It covers three specific methods: retrieving supported video codec types for moving photos, checking mirror photography support, and verifying moving photo capture availability. Each function entry includes detailed descriptions, system capabilities, return value types, and potential BusinessException error codes. The document provides practical Cangjie code examples demonstrating how to initialize camera managers and invoke these methods.

### func_offCameraEvents_2more.md func_offCameraEvents_2more.md
API reference document detailing Cangjie functions for managing camera event listeners, specifically for photo output operations. It covers the `off` function for unsubscribing callbacks and the `on` function for registering capture start listeners, including syntax, parameters, exceptions, and usage examples. Key takeaways include the restriction that `on` callbacks cannot call `off` internally, specific error codes like 7400201 for service failures, and the use of `CaptureStartInfo` data.

### func_offCameraEvents_Callb_2more.md func_offCameraEvents_Callb_2more.md
API reference document detailing the overloaded off functions used to unregister specific camera event listeners in the Cangjie CameraKit. It covers two primary function signatures: one accepting Callback0Argument for events like CaptureReady and CameraError, and another accepting Callback1Argument<Float64> for EstimatedCaptureDuration. Key information includes the requirement for specific event types and corresponding callback instances, as well as the handling of BusinessException for camera service fatal errors.

### func_onCameraEvents_Callback0Argument.md func_onCameraEvents_Callback0Argument.md
API reference document detailing the `on` function in Cangjie for registering callbacks to monitor camera events like `CaptureReady` or `CameraError`. Key sections include the function signature, parameter definitions for event types and callbacks, system capabilities, and exception handling for camera service errors. The documentation highlights a critical constraint: the `off` method cannot be called within the registered callback. It provides a practical code example demonstrating error handling and context setup.

### func_onCameraEvents_Callback1ArgumentCapt_9d6d8cf8.md func_onCameraEvents_Callback1ArgumentCapt_9d6d8cf8.md
API documentation detailing a Cangjie function for registering a callback to monitor the completion of photo capture events. The document covers the function signature, required system capabilities, and parameters, specifically mandating the CaptureEnd event type and a CaptureEndInfo callback handler. Key takeaways include the restriction against calling the off method inside the callback to unregister it and the handling of specific business exceptions like camera service errors.

### func_onCameraEvents_Callback1ArgumentFloat64.md func_onCameraEvents_Callback1ArgumentFloat64.md
API reference document describing the `on` function for registering a callback to monitor estimated photo capture duration in Cangjie applications. The document details the function signature, system capabilities, and parameters, specifically requiring `CameraEvents.EstimatedCaptureDuration` and a `Callback1Argument<Float64>`. Key information includes that the callback returns the sensor frame time in milliseconds and that unregistering the callback within the callback method is unsupported. It also lists potential business exceptions and provides a usage example.

### func_onCameraEvents_Callback1ArgumentFram_2017c58d.md func_onCameraEvents_Callback1ArgumentFram_2017c58d.md
API documentation describing the `on` function for registering a callback to monitor photo exposure end events within the CameraKit framework. The document outlines the function signature, system capabilities, and parameters, specifically requiring `FrameShutterEnd` as the event type and a callback handling `FrameShutterEndInfo`. Key information includes a restriction against calling the `off` method inside the callback and potential `BusinessException` error codes. A Cangjie code example demonstrates initializing the camera manager and setting up the listener.

### func_onCameraEvents_Callback1ArgumentFram_947cb8e7.md func_onCameraEvents_Callback1ArgumentFram_947cb8e7.md
API reference documentation for a Cangjie function that registers a callback to monitor photo frame shutter events. It details the function signature, system capabilities, and parameters, specifically requiring CameraEvents.FrameShutter and a Callback1Argument<FrameShutterInfo>. The document outlines potential business exceptions, such as camera service fatal errors, and provides a code example demonstrating implementation within the CameraKit context. Key takeaways include the requirement for successful photoOutput creation before listening and the specific error handling involved.

### func_release_2more.md func_release_2more.md
API reference documentation for camera output functions in the Cangjie programming language. It covers two primary methods: release(), which is used to free output resources, and setMovingPhotoVideoCodecType(), which configures the video encoding type for moving photos. Both functions belong to the SystemCapability.Multimedia.Camera.Core, require version 22, and handle potential BusinessException errors. The content includes detailed syntax definitions, parameter descriptions, and executable code examples demonstrating usage within a camera application context.