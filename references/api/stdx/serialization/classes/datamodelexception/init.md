<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.datamodelexception.init" parent="stdx.serialization.serialization.class.datamodelexception" -->
# DataModelException.init

[← DataModelException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建 DataModelException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据异常信息创建 DataModelException 实例。

### 契约

参数：

- message: String - 异常信息提示字符串。
