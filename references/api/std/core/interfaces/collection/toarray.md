<!-- cj-doc kind="api-member" level="6" id="std.core.interface.collection.toarray" parent="std.core.interface.collection" -->
# Collection<T>.toArray

[← Collection<T>](index.md)

## 签名

```cangjie role=signature
func toArray(): Array<T>
```

将当前集合转为数组类型。

## 契约

返回值：

- Array\<T> - 转换得到的数组。
