# 结构体

This directory serves as a specialized repository for technical documentation regarding core data structures within the Cangjie programming language ecosystem. It focuses specifically on the definition, properties, initialization, and operational logic of various structs that underpin critical system functionalities, such as logging frameworks and configuration management. The content provided here is indispensable for software developers, system architects, and engineers who are actively building or maintaining applications in Cangjie and require a granular understanding of how data is modeled, compared, and manipulated. By accessing these resources, users can master struct hierarchies, explore property access patterns, understand operator overloading, and utilize type conversion mechanisms effectively. This documentation is particularly suitable for those aiming to implement robust logging systems, manage application verbosity dynamically, or handle complex data comparisons and state management within their software projects. This ensures that developers can write cleaner, more efficient code while leveraging the full power of the language's type system.

## Quick Navigation

- **What do you want to learn?**
    - How to define and utilize logging severity levels in Cangjie? → Go to `struct_LogLevel/`
    - What are the specific integer values assigned to log levels like OFF, ERROR, or ALL? → Go to `struct_LogLevel/`
    - How to perform string conversions and comparisons on logging data? → Go to `struct_LogLevel/`
- **What do you want to do?**
    - Configure log output thresholds to control application verbosity? → Go to `struct_LogLevel/`
    - Filter logs based on severity using comparison operator overloads? → Go to `struct_LogLevel/`
    - Implement custom logic involving log level comparisons for error handling? → Go to `struct_LogLevel/`

## Detailed Description

The following section provides in-depth technical information regarding the specific subdirectories contained within this folder. Each subdirectory is dedicated to a distinct struct, offering detailed insights into its internal definition, available methods, properties, and practical applications within the Cangjie programming language. Readers are encouraged to explore these subsections to gain a complete understanding of the data structures available for use. Understanding these structures is key to mastering the language's standard libraries and runtime environment.

### struct_LogLevel/
This directory contains comprehensive documentation for the `LogLevel` struct, a fundamental component of the Cangjie programming language's logging package. It defines the hierarchy and properties of logging severity levels, ranging from OFF to ALL, including specific integer values and properties like name and value. The documentation covers the struct definition, initialization methods, string conversion, and comparison operator overloads. This resource is essential for developers who need to configure log output thresholds, filter logs based on severity, or implement logic involving log level comparisons in their Cangjie applications. By understanding these levels, developers can effectively manage application verbosity, control debugging output, and ensure proper error handling.