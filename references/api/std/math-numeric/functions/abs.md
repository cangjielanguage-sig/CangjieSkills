<!-- cj-doc kind="api-member" level="5" id="std.math.numeric.func.abs" parent="std.math.numeric" -->
# abs

[← std.math.numeric](../index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## abs(BigInt)

### 签名

```cangjie role=signature
public func abs(i: BigInt): BigInt
```

求一个 BigInt 的绝对值。

### 契约

参数：

- i: BigInt - 需要计算绝对值的 BigInt。

返回值：

- BigInt - 返回入参 BigInt 的绝对值。

## abs(Decimal)

### 签名

```cangjie role=signature
public func abs(d: Decimal): Decimal
```

求一个 Decimal 的绝对值。

### 契约

参数：

- d: Decimal - 需要计算绝对值的 Decimal。

返回值：

- Decimal - 返回入参 Decimal 的绝对值。
