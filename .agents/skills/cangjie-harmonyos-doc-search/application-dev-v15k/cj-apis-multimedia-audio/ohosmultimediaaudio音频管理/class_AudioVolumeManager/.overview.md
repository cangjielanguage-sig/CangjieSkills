# class_AudioVolumeManager

This directory contains API reference documentation for the `AudioVolumeManager` class within the Cangjie programming language. It focuses on functionalities for managing audio volume groups and handling system volume change events. Key topics include retrieving volume group instances synchronously, registering and unregistering event listeners, and handling specific audio-related callbacks. The documentation is tailored for developers building multimedia audio applications on HarmonyOS, requiring familiarity with Cangjie syntax and audio system capabilities. It provides essential details on method signatures, parameters like `groupId`, return types such as `AudioVolumeGroupManager`, and error codes like 401 and 6800101 to facilitate robust audio control implementation.

## Quick Navigation

**What do you want to do?**

*   Retrieve volume group managers or remove event listeners → class_AudioVolumeManager_3more.md
*   Listen to and handle system volume change events → func_onAudioVolumeManag_ac333586.md

## Detailed Description

### class_AudioVolumeManager_3more.md class_AudioVolumeManager_3more.md
This document serves as API reference documentation for the AudioVolumeManager class in the Cangjie programming language, detailing methods for managing audio volume groups and event listeners. The content covers the class definition, the getVolumeGroupManager function for retrieving specific volume group instances synchronously, and the off function for unlistening to audio events. Key information includes input parameters like groupId, return types such as AudioVolumeGroupManager, and specific error codes like 401 and 6800101. Code examples demonstrate synchronous retrieval and callback management. Targeted at developers building multimedia audio applications, this guide requires knowledge of Cangjie syntax and audio system capabilities.

### func_onAudioVolumeManag_ac333586.md func_onAudioVolumeManag_ac333586.md
This document serves as API reference documentation for the `on` function within the AudioVolumeManager class, designed to monitor system volume change events via callbacks. It details the function signature, system capabilities, and required parameters, specifically the `AudioVolumeManagerCallbackType` and `Callback1Argument` interface. Key information includes the mandatory 'VOLUME_CHANGE' event type, the `VolumeEvent` return object, and exception handling for system errors like code 6800301. A Cangjie code example demonstrates how to implement the callback class, register the listener, and handle potential business exceptions. This guide is intended for developers building audio applications who need to react to volume adjustments, requiring familiarity with the Cangjie language and HarmonyOS audio services.