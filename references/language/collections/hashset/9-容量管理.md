<!-- cj-doc kind="guide-leaf" level="5" id="language.collections.hashset.9-容量管理" parent="language.collections.hashset" -->
# 9. 容量管理

[← HashSet](index.md)

```cangjie cjtest=syntax id=syntax-da5848b706-1 form=unit
func reserve(additional: Int64): Unit
```

```cangjie cjtest=syntax id=syntax-da5848b706-2 form=stmt
set.reserve(100)  // 扩容以容纳更多元素
```

- `additional <= 0` 或剩余容量足够时不执行扩容
- 扩容后容量约为原来的 1.5 倍（不保证精确值）
- 当溢出 `Int64.Max` 时抛 `OverflowException`

---
