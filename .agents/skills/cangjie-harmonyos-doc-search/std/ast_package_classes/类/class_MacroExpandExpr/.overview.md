# class_MacroExpandExpr

This directory contains technical documentation for the `MacroExpandExpr` class and associated utility functions within the Cangjie language's Abstract Syntax Tree (AST). It focuses on the structural representation and programmatic manipulation of macro invocation nodes. The documentation covers class definitions, mutable properties for accessing specific tokens (like the `@` operator), initialization methods, and validation logic. Additionally, it includes references for functions that convert nodes to tokens and traverse the AST using the Visitor pattern. This content is intended for compiler developers and programmers working on syntax analysis or AST construction in the Cangjie programming environment.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition, mutable properties, and initialization logic for macro expressions → class_MacroExpandExpr_11more.md
*   Learn how to convert AST nodes to tokens or traverse the tree structure using the Visitor pattern → func_toTokens_2more.md

## Detailed Description

### class_MacroExpandExpr_11more.md
This document serves as a comprehensive API reference for the `MacroExpandExpr` class, a critical component representing macro invocation nodes within the Abstract Syntax Tree (AST) of the Cangjie language. It provides in-depth details regarding the class definition, focusing on mutable properties designed to access specific tokens such as the `@` operator, identifiers, and delimiters. Furthermore, it outlines initialization methods and explains how the class handles both standard and attribute macros through properties like `macroInputs` and `macroAttrs`. The documentation also covers validation logic that throws `ASTException` for invalid token assignments. This technical reference is specifically intended for compiler developers or programmers who are manipulating the Cangjie AST and need to programmatically inspect or construct macro expressions.

### func_toTokens_2more.md
This document offers API reference documentation for two essential functions within a Cangjie Abstract Syntax Tree (AST) package. It details the `toTokens()` function, which is responsible for converting the current syntax tree node into a `Tokens` type, and the `traverse(Visitor)` function, which iterates over the node and its children. Key takeaways include the specific return types associated with `toTokens` and the advanced capability to prematurely halt the traversal process. This is achieved by overriding the visit function and calling `breakTraverse`. This technical reference is designed for developers utilizing the AST library for syntax analysis or manipulation, requiring a solid understanding of the Visitor pattern and Cangjie programming concepts.