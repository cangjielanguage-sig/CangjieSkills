<!-- cj-doc kind="api-member" level="5" id="std.collection.func.collectarraylist-t-iterable-t" parent="std.collection" -->
# collectArrayList<T>(Iterable<T>)

[← std.collection](../index.md)

## 签名

```cangjie role=signature
public func collectArrayList<T>(it: Iterable<T>): ArrayList<T>
```

将一个迭代器转换成 ArrayList 类型。

## 契约

参数：

- it: Iterable\<T> - 给定的迭代器。

返回值：

- ArrayList\<T> - 返回一个 ArrayList。
