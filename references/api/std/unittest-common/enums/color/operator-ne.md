<!-- cj-doc kind="api-member" level="6" id="std.unittest.common.enum.color.operator-ne" parent="std.unittest.common.enum.color" -->
# Color.!=

[← Color](index.md)

## 签名

```cangjie role=signature
public operator func !=(that: Color): Bool
```

判断颜色是否不相等。

## 契约

参数：

- that: Color - 被对比的颜色。

返回值：

- Bool - 不相等时返回 `true` ，否则返回 `false` 。
