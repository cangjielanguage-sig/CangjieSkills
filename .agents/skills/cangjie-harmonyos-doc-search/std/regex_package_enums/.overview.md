# regex_package_enums

This directory serves as the central repository for API reference documentation regarding the `regex_package_enums` module, specifically focusing on the `RegexFlag` enumeration within the Cangjie regex package. It provides a comprehensive overview of the flags used to control and fine-tune regular expression matching behavior. The content details the `RegexFlag` enum and its three distinct members: `IgnoreCase`, `MultiLine`, and `Unicode`. Each member is explained with functional descriptions and code snippets, illustrating how developers can modify regex execution to enable case insensitivity, manage multi-line string inputs, or ensure robust Unicode character support. This documentation is intended for Cangjie developers who are actively implementing regular expressions and need to customize matching modes for specific application requirements. Prerequisites for this material include a solid familiarity with Cangjie syntax and a basic understanding of regular expression concepts.

## Quick Navigation

What do you want to learn?

*   If you need to access the complete API reference documentation for the RegexFlag enumeration → regex_package_enums.md
*   If you want to learn how to implement case-insensitive matching using the IgnoreCase flag → regex_package_enums.md
*   If you are looking for instructions on handling multi-line strings with the MultiLine flag → regex_package_enums.md
*   If you need to ensure your regex patterns support Unicode characters via the Unicode flag → regex_package_enums.md
*   If you wish to review code snippets and detailed functional descriptions for all enum members → regex_package_enums.md

## Detailed Description

The following section provides a detailed breakdown of the files contained within this directory. Each file is described based on its role in defining the enumeration types and their associated functionalities for the Cangjie regex package.

### regex_package_enums.md regex_package_enums.md

This document serves as API reference documentation for the RegexFlag enumeration within the Cangjie regex package, defining flags to control regular expression matching behavior. It details the RegexFlag enum and its three specific members: IgnoreCase, MultiLine, and Unicode. Each member is accompanied by a code snippet and a functional description explaining its specific effect on pattern matching. The key takeaway is that developers can use these flags to modify regex execution, specifically to enable case insensitivity, handle multi-line strings, or ensure Unicode character support. This reference is intended for Cangjie developers implementing regular expressions who need to customize matching modes. Prerequisites include familiarity with the Cangjie syntax and basic regex concepts.