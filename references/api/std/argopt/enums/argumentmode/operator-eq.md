<!-- cj-doc kind="api-member" level="6" id="std.argopt.enum.argumentmode.operator-eq" parent="std.argopt.enum.argumentmode" -->
# ArgumentMode.==

[← ArgumentMode](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: ArgumentMode): Bool
```

比较参数模式是否相同。

## 契约

参数：

- that: ArgumentMode - 参数模式。

返回值：

- Bool - 相同时返回 `true`，否则返回 `false`。
