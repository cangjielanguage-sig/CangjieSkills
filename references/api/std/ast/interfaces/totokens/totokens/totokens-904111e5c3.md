<!-- cj-doc kind="api-member" level="7" id="std.ast.interface.totokens.totokens.totokens-904111e5c3" parent="std.ast.interface.totokens.totokens" -->
# ToTokens.func toTokens()

[← ToTokens.toTokens](index.md)

## 签名

```cangjie role=signature
public func toTokens(): Tokens
```

实现 Float32 类型到 Tokens 类型的转换。

适用扩展：[extend Float32 <: ToTokens](../extensions/extend-float32-totokens.md)。

## 契约

返回值：

- Tokens - 转换后的 Tokens。
