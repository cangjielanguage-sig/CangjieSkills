# CString

This directory provides comprehensive documentation for the `CString` type within the Cangjie programming language, specifically focusing on C language interoperability and Foreign Function Interface (FFI) tasks. It covers the fundamental definition of C-style strings, their creation, and the critical memory management procedures required when bridging Cangjie with C libraries. Key topics include the `CString` constructor, the `LibC` struct's `mallocCString` function, and the necessity of manual memory release using the `LibC free` method. Additionally, it details extensions for the `ToString` interface, enabling safer string manipulation and lexicographical comparisons. This resource is essential for Cangjie developers who need to handle low-level string operations, manage unsafe memory manually, and ensure null-safety when interacting with native C code. The documentation ensures developers can effectively utilize FFI capabilities while maintaining system stability.

## Quick Navigation

**What do you want to learn?**

*   Understand the core API reference for C-style strings and C language interoperability → CString.md
*   Learn how to create strings using the constructor or `mallocCString` → CString.md
*   Master memory management procedures, including manual memory release via `LibC free` → CString.md
*   Explore how to extend the `CString` type to implement the `ToString` interface → extend_CString_ToString/
*   Perform advanced operations like lexicographical comparison, suffix/prefix validation, and `subCString` → extend_CString_ToString/

## Detailed Description

### CString.md CString.md
This document serves as API reference for the CString type in the Cangjie language, specifically designed for C language interoperability. It covers the definition of C-style strings and their usage in cross-language scenarios. Key topics include string creation methods, such as using the CString constructor or the LibC struct's mallocCString function, and memory management procedures. The document highlights that developers must manually release memory on the Cangjie side by calling the LibC free method. This information is crucial for developers working on Foreign Function Interface (FFI) tasks involving C libraries.

### extend_CString_ToString/
This directory contains API documentation for extending the Cangjie `CString` type to implement the `ToString` interface and adding essential string manipulation methods. It covers functions for resource management, lexicographical comparison, suffix/prefix validation, and memory-safe operations like `subCString` and `toString`. Key topics include C interoperability, unsafe memory operations, and null-safety checks. This documentation is suitable for Cangjie developers working with low-level string handling and interfacing with C libraries.