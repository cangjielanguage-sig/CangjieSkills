# fs_package_enums

This directory serves as the definitive API reference for enumerations found within the Cangjie file system package. It is centered around the `OpenMode` enumeration, which is critical for defining the precise mechanics of file access and manipulation during input and output operations. The documentation thoroughly explains four primary variants: Read, Write, Append, and ReadWrite. It provides essential details on their behaviors, such as policies for file creation, content truncation, and exception handling when files are missing. Additionally, the text covers standard interface implementations, including ToString and Equatable, as well as equality operators. This documentation is specifically tailored for Cangjie developers who require granular control over file access modes to ensure robust and error-free file I/O implementation in their software projects. Understanding these modes is vital for preventing data loss and managing system resources effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the core definition and purpose of the OpenMode enumeration → fs_package_enums.md
*   Learn about the Read variant and its exception handling for missing files → fs_package_enums.md
*   Discover how the Write variant handles file truncation and creation → fs_package_enums.md
*   Explore the Append and ReadWrite modes and their data preservation rules → fs_package_enums.md
*   Review standard interface implementations like ToString and Equatable → fs_package_enums.md
*   Find details on equality operators and string conversion methods → fs_package_enums.md

## Detailed Description

The following file provides the complete technical specification for the enumerations used in this package.

### fs_package_enums.md fs_package_enums.md
This API reference document defines the OpenMode enumeration within the Cangjie file system package, specifying how files are accessed and manipulated. It details four variants (Read, Write, Append, ReadWrite) and their specific behaviors regarding file creation and truncation, alongside standard interface implementations like ToString and Equatable. Crucial distinctions include Write truncating files, Read throwing exceptions for missing files, and ReadWrite preserving existing content. It also documents equality operators and string conversion methods. Targeted at Cangjie developers implementing file I/O operations who need precise control over file access modes.