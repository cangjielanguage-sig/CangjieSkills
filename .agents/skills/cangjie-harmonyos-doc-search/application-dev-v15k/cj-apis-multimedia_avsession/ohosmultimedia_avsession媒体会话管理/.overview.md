# ohosmultimedia_avsession媒体会话管理

This directory contains comprehensive API reference documentation for the HarmonyOS AVSession (Audio-Video Session) framework, specifically designed for the Cangjie programming language. It covers the management of media sessions, including playback control, metadata handling, audio-video casting, and device management. Key components include classes for session controllers, playback states, media descriptions, and enumerations for command types and connection states. This resource is suitable for software developers and engineers building multimedia applications who need to integrate system-level media controls, handle casting features, and manage audio-video interactions within the HarmonyOS ecosystem.

## Quick Navigation

- **What do you want to do?**
    - Manage media sessions and controllers → ohosmultimedia_avsession媒体_5more.md, class_AVSession/, class_AVSessionController/
    - Handle audio-video casting → class_AVCastController/, class_AVCastControlCommand.md, class_CastDisplayInfo.md, class_DeviceInfo.md
    - Define media metadata and descriptions → class_AVMetadata/, class_AVMediaDescription/, enum_KeyOfMetaData/
    - Control playback states and loops → class_AVPlaybackState/, enum_PlaybackState/, enum_LoopMode/
    - Manage devices and connections → class_DeviceInfo/, enum_ConnectionState/, enum_DeviceType/
    - Handle call sessions → class_CallMetadata/, enum_CallState/

## Detailed Description

### class_AVCastControlCommand.md class_AVCastControlCommand.md
This document defines the `AVCastControlCommand` class, which describes commands accepted by an audio-video cast controller. It details the constructor and properties for command type (`AVCastControlCommandType`) and optional parameters (`AVCastControlCommandParameterType`), both of which are read-write.

### class_AVControlCommand.md class_AVControlCommand.md
This reference defines the `AVControlCommand` class for objects representing commands accepted by multimedia sessions. It specifies the constructor, the command type property, and an optional parameter property, all readable and writable, targeting the `SystemCapability.Multimedia.AVSession.Core` module.

### class_AVQueueItem.md class_AVQueueItem.md
This document outlines the `AVQueueItem` class, representing individual items in a multimedia playlist. It covers properties for `itemId` and `description` (media metadata), along with two constructor methods for initialization within the AVSession framework.

### class_CallMetadata.md class_CallMetadata.md
This API reference defines the `CallMetadata` class for call session metadata, including properties for name, phone number, and avatar. It details constructors and system capability requirements for managing caller identification in multimedia applications.

### class_CastDisplayInfo.md class_CastDisplayInfo.md
This document defines the `CastDisplayInfo` class, encapsulating attributes for extended display casting devices. It covers properties like device ID, name, state, and screen dimensions, requiring the `SystemCapability.Multimedia.AVSession.ExtendedDisplayCast` capability.

### class_DeviceInfo.md class_DeviceInfo.md
This reference details the `DeviceInfo` class, encapsulating playback device data. It outlines properties such as device ID, name, type, and supported protocols, providing constructors for instantiation in audio-video casting scenarios.

### class_OutputDeviceInfo_2more.md class_OutputDeviceInfo_2more.md
This document covers the `OutputDeviceInfo` class for managing collections of playback devices and the `PlaybackPosition` class for tracking media timing. It details properties for device arrays, elapsed time, and update timestamps.

### enum_AVCastCategory_2more.md enum_AVCastCategory_2more.md
This reference defines enumerations for casting functionality, including `AVCastCategory` for device types and `AVCastControlCommandParameterType` for command parameters. It details inherited protocols and specific parameter types like speed and loop modes.

### enum_AVControlCommandPa_6ff4fb3b.md enum_AVControlCommandPa_6ff4fb3b.md
This document defines the `AVControlCommandParameterType` enum, specifying permissible data types for command parameters. It lists variants such as STRING, INT32, and LOOP_MODE, essential for interacting with the AVSession framework.

### enum_AVSessionType.md enum_AVSessionType.md
This reference defines the `AVSessionType` enum for supported audio-video session types, including audio, video, and voice calls. It details member values and implemented operators for comparison and string representation.

### enum_CallState.md enum_CallState.md
This document provides the `CallState` enum for representing phone call states within the Multimedia AVSession Core. It enumerates states like idle, incoming, and active, including methods for string representation.

### enum_CastDisplayState.md enum_CastDisplayState.md
This documentation defines the `CastDisplayState` enum for managing the display status of extended casting devices. It details states like `STATE_ON` and `STATE_OFF` and includes methods for equality checks and string conversion.

### enum_ConnectionState.md enum_ConnectionState.md
This reference defines the `ConnectionState` enum for device connection statuses. It details states such as connecting, connected, and disconnected, alongside methods for comparison and logging.

### enum_DeviceType.md enum_DeviceType.md
This document defines the `DeviceType` enumeration, categorizing playback devices like Bluetooth and TV. It covers enum values and inherited methods for identifying and managing output devices.

### enum_DisplayTag.md enum_DisplayTag.md
This reference defines the `DisplayTag` enum for identifying special media audio source types, specifically "Golden Tags." It details the `TAG_AUDIO_VIVID` member and standard operators for comparison.

### enum_KeyOfCallMetadata.md enum_KeyOfCallMetadata.md
This document defines the `KeyOfCallMetadata` enum, serving as key identifiers for call session attributes like name and phone number. It includes operators for equality checks and string representation.

### enum_KeyOfCallState.md enum_KeyOfCallState.md
This reference defines the `KeyOfCallState` enum for accessing call state attributes. It outlines values for status and microphone muting, along with utility functions for comparison.

### enum_KeyOfPlaybackState.md enum_KeyOfPlaybackState.md
This document defines the `KeyOfPlaybackState` enum, providing keys for accessing media playback attributes such as speed, position, and volume. It is essential for querying or modifying playback states.

### enum_LoopMode.md enum_LoopMode.md
This reference defines the `LoopMode` enumeration, specifying media playback loop modes like sequence, single, and shuffle. It documents operators for comparison and string representation.

### enum_PlaybackState.md enum_PlaybackState.md
This document defines the `PlaybackState` enum, representing media playback statuses such as play, pause, and stop. It covers the full playback lifecycle and includes methods for state manipulation.

### enum_ProtocolType.md enum_ProtocolType.md
This reference defines the `ProtocolType` enum for categorizing remote device protocols. It details members for local and remote display types (e.g., DLNA) and includes string conversion methods.

### enum_SkipIntervals.md enum_SkipIntervals.md
This document defines the `SkipIntervals` enum, specifying time intervals for fast-forwarding and rewinding. It lists specific values in seconds and includes comparison operators.

### enum_ValueType.md enum_ValueType.md
This reference defines the `ValueType` enum, specifying data types for additional information in public events. It covers primitive types and complex structures like arrays and hash maps.

### ohosmultimedia_avsession媒体_5more.md ohosmultimedia_avsession媒体_5more.md
This module overview enables integration with the system playback control center. It covers session creation, usage notes, and the `AVCallState` class for managing call properties in Cangjie projects.

### class_AVCastController/
This directory contains documentation for the `AVCastController` class, focusing on managing established casting sessions. It covers playback state retrieval, event subscriptions, DRM license management, and sending control commands.

### class_AVMediaDescription/
This directory documents the `AVMediaDescription` class for defining metadata attributes of playlist items. It covers constructors for media objects and properties for managing metadata like titles and artists.

### class_AVMetadata/
This directory provides reference for the `AVMetadata` class, managing media asset metadata. It details properties for asset IDs, titles, lyrics, and DRM schemes within the AVSession framework.

### class_AVPlaybackState/
This directory documents the `AVPlaybackState` class for representing playback status. It covers initialization, playback attributes (speed, position), volume control, and loop modes.

### class_AVSession/
This directory provides documentation for the `AVSession` class, essential for managing session lifecycles. It covers activation, deactivation, event dispatching, and state management for HarmonyOS applications.

### class_AVSessionController/
This directory details the `AVSessionController` class for session management. It covers retrieving metadata, monitoring playback states, sending commands, and handling event listeners.

### enum_AVCastControlCommandType/
This directory defines the `AVCastControlCommandType` enum for casting commands. It details values for playback controls, navigation, and settings like volume and speed.

### enum_AVCastControllerEventType/
This directory documents the `AVCastControllerEventType` enum for subscription events in casting applications. It covers playback state changes, media item updates, and error handling.

### enum_AVControlCommandType/
This directory defines the `AVControlCommandType` enum for session commands. It details operations for media playback and call handling, including play, pause, and mute.

### enum_AVPlaybackStateFilter/
This directory defines the `AVPlaybackStateFilter` enum for specifying which playback state fields to monitor. It allows granular control over updates for status, speed, and volume.

### enum_AVSessionControllerEventType/
This directory documents the `AVSessionControllerEventType` enum for callback events. It details events for session destruction, playback state changes, and metadata updates.

### enum_AVSessionEventType/
This directory defines the `AVSessionEventType` enum for session handling. It covers playback controls, call management, device changes, and custom commands.

### enum_KeyOfMetaData/
This directory defines the `KeyOfMetaData` enum for accessing media metadata attributes. It details keys for media identifiers, artist info, and album details.