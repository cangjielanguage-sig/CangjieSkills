<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-return" parent="std.ast.enum.tokenkind" -->
# TokenKind.RETURN

[← TokenKind](index.md)

## 签名

```cangjie role=signature
RETURN
```

构造一个表示 `return` 的枚举实例。
