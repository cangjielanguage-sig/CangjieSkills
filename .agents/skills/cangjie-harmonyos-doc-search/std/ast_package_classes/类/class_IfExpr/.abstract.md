This directory provides comprehensive API reference documentation for the `IfExpr` class and related utility functions within the Cangjie programming language's Abstract Syntax Tree (AST) framework. It focuses extensively on the structural definition and manipulation of conditional expressions, specifically if-else statements. Core topics covered include the class hierarchy, properties for managing conditions and execution blocks, and the handling of lexical tokens like keywords and parentheses. Additionally, the documentation addresses specific exception handling for invalid tokens and missing else branches. Beyond the class definition, it includes specifications for AST node conversion to tokens and tree traversal mechanisms using the Visitor pattern. This documentation is essential for developers, language engineers, and compiler designers who need to programmatically parse, modify, or traverse conditional logic structures within the Cangjie language environment. It serves as a critical resource for understanding how conditional logic is represented and processed internally by the language compiler or interpreter.