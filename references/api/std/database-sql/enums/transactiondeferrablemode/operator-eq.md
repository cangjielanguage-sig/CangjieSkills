<!-- cj-doc kind="api-member" level="6" id="std.database.sql.enum.transactiondeferrablemode.operator-eq" parent="std.database.sql.enum.transactiondeferrablemode" -->
# TransactionDeferrableMode.==

[← TransactionDeferrableMode](index.md)

## 签名

```cangjie role=signature
public operator func == (rhs: TransactionDeferrableMode): Bool
```

判断两个 TransactionDeferrableMode 是否相等。

## 契约

参数：

- rhs: TransactionDeferrableMode - 传入 TransactionDeferrableMode 的枚举值。

返回值：

- Bool - 如果相等，则返回 `true`，否则返回 `false`。
