# 示例2Grid嵌套滚动

This directory provides a comprehensive collection of code examples focused on solving the complex challenges of nested scrolling within the HarmonyOS development environment. Specifically, it demonstrates how to effectively coordinate scroll events between a parent List component and a child Grid component using the Cangjie programming language and ArkUI framework. The examples cover critical technical aspects such as scroll event distribution, offset transfer logic using `onScrollFrameBegin`, and UI state management features like a "Back to Top" button. These resources are highly suitable for developers building complex user interfaces that require smooth, intuitive scrolling behaviors across multiple layers of content, ensuring a seamless and professional user experience.

## Quick Navigation

**What do you want to learn or implement?**

*   **Implement core nested scrolling logic:** Learn how to manage scroll event distribution between a parent List and a child Grid component using Cangjie → 示例2Grid嵌套滚动_1.md
*   **Add navigation controls:** Discover how to implement a "Back to Top" button that resets scroll positions for both parent and child scrollers in ArkUI → 示例2Grid嵌套滚动_2.md

## Detailed Description

The following files provide the implementation details for the nested scrolling scenarios, ranging from core logic to UI enhancements.

### 示例2Grid嵌套滚动_1.md 示例2Grid嵌套滚动_1.md
This document provides a code example demonstrating how to implement nested scrolling between a parent List and a child Grid component in the Cangjie programming language. It covers the usage of nestedScroll options and the onScrollFrameBegin callback to manage scroll event distribution. The code illustrates a scenario where a List contains a Grid; scrolling logic is implemented to transfer scroll offset from the parent List to the child Grid once the parent reaches a specific threshold, ensuring smooth interaction. This tutorial is intended for developers using Cangjie for HarmonyOS application development who need to handle complex nested scroll layouts.

### 示例2Grid嵌套滚动_2.md 示例2Grid嵌套滚动_2.md
This document serves as a code example demonstrating the implementation of a "Back to Top" button within a nested Grid scrolling scenario using HarmonyOS ArkUI. The content focuses on UI layout construction using Row and Text components, applying styling attributes such as shadows and border radius, and handling user interaction events. Key information includes the logic within the onClick event handler, which simultaneously resets the scroll positions of two distinct scroller objects (`scroller` and `gridScroller`) to their origin coordinates. This snippet is intended for developers seeking practical solutions for managing scroll states in complex, nested list interfaces. Keywords: ArkUI, Grid, nested scrolling, scrollTo, event handling, HarmonyOS.

By combining these examples, developers can achieve a fully functional nested scrolling interface with robust state management.