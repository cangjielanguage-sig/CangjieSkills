<!-- cj-doc kind="api-member" level="6" id="std.time.class.timeparseexception.init" parent="std.time.class.timeparseexception" -->
# TimeParseException.init

[← TimeParseException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个 TimeParseException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

根据参数 `message` 指定的异常信息，构造一个 TimeParseException 实例。

### 契约

参数：

- message: String - 预定义消息。
