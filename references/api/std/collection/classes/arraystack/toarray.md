<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arraystack.toarray" parent="std.collection.class.arraystack" -->
# ArrayStack<T>.toArray

[← ArrayStack<T>](index.md)

## 签名

```cangjie role=signature
public func toArray(): Array<T>
```

返回一个数组，其中元素为栈中的元素，顺序为栈的出栈顺序。

## 契约

返回值：

- Array\<T> - T 类型数组。
