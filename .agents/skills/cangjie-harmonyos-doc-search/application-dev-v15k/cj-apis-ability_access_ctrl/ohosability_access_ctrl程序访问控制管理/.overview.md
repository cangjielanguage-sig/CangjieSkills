# ohosability_access_ctrl程序访问控制管理

This directory provides comprehensive API reference documentation for the `ohos.ability_access_ctrl` module within the Cangjie language framework, focusing on program access control and permission management. It covers essential components such as the `AtManager` class for handling runtime permissions, enumerations for authorization states (`GrantStatus`), and result objects for permission requests. Key topics include verifying application permissions, requesting user authorization via system dialogs, and managing global hardware switches like cameras and microphones. This documentation is intended for HarmonyOS developers implementing security capabilities and user consent logic in their applications.

## Quick Navigation

**What do you want to learn?**

*   **Initialize the access control manager** → ohosability_access_ctrl程序访_4more.md
*   **Perform access control operations** (verify permissions, request authorization, manage switches) → class_AtManager/
*   **Handle permission request results** → class_AccessCtrlPermissionRequestResult.md
*   **Understand authorization states and switch types** → enum_GrantStatus_3more.md

## Detailed Description

### class_AccessCtrlPermissionRequestResult.md class_AccessCtrlPermissionRequestResult.md
This API reference document defines the Cangjie class `AccessCtrlPermissionRequestResult`, which encapsulates the outcome of user permission requests. It details the class constructor and its three public properties: `permissions`, `authResults`, and `dialogShownResults`. Key takeaways include specific integer codes for authorization status, such as 0 for granted, -1 for denied, and 2 for invalid requests, alongside flags indicating if a dialog was displayed. Targeted at developers working with system security capabilities, this document aids in handling permission logic within applications starting from API version 12.

### enum_GrantStatus_3more.md enum_GrantStatus_3more.md
This API reference document defines Cangjie language enumerations and types for managing security authorization states and global hardware switches. It details the `GrantStatus` enum for permission results, the `SwitchType` enum for device controls like camera and microphone, and the `Permissions` string type alias. Key elements include enum values such as `PERMISSION_GRANTED` and `CAMERA`, alongside standard operators for equality comparison and string conversion, all linked to the `SystemCapability.Security.AccessToken` capability. This documentation is intended for developers implementing access control and permission management in Cangjie-based applications.

### ohosability_access_ctrl程序访_4more.md ohosability_access_ctrl程序访_4more.md
This document is an API reference for the `ohos.ability_access_ctrl` module, designed to manage program permissions including authentication and authorization in the Cangjie language. It details the `AbilityAccessCtrl` class and its static method `createAtManager()`, which returns an `AtManager` instance for handling access control. The text includes usage instructions for Cangjie template projects, specifically regarding `index.cj` and `main_ability.cj` configurations. Intended for HarmonyOS developers, this guide requires knowledge of the Cangjie development environment and system capabilities like `SystemCapability.Security.AccessToken`.

### class_AtManager/
This directory contains API reference documentation for the `AtManager` class and related functions within the Cangjie language framework, focusing on access control and permission management. It covers essential operations such as verifying application permissions via access tokens, requesting user authorization through system dialogs, and managing global privacy switches like microphone access. The documentation details function signatures, parameters, return values, and error handling for security features. It is suitable for application developers implementing runtime permission requests, validating security capabilities, and managing user consent in HarmonyOS applications.