# cj-apis-bluetooth-access

This directory serves as the definitive API reference documentation for the HarmonyOS Bluetooth Access module, specifically architected for the Cangjie programming language. It provides a comprehensive overview of the essential functionalities required to effectively manage Bluetooth radio states within the HarmonyOS ecosystem. The content covers critical operations such as enabling and disabling the Bluetooth hardware, querying the current operational status, and implementing robust mechanisms for monitoring state changes through callback subscriptions. Key technical topics include the `BluetoothState` enumeration, the `BluetoothAccessCallbackType` interface, and core functions like `enableBluetooth` and `getState`. This documentation is primarily intended for HarmonyOS developers utilizing the ConnectivityKit to implement basic Bluetooth control and state monitoring features in their applications, ensuring they are aware of the specific permissions and system capabilities required for successful execution.

## Quick Navigation

- What do you want to learn?
    - Get an overview of the HarmonyOS Bluetooth Access module designed for the Cangjie language → See `ohosbluetoothaccess蓝牙access模块/`
    - Understand the definitions and usage of `BluetoothState` and `BluetoothAccessCallbackType` → See `ohosbluetoothaccess蓝牙access模块/`
    - Identify the specific system permissions and capabilities needed to access Bluetooth APIs → See `ohosbluetoothaccess蓝牙access模块/`
- What do you want to do?
    - Programmatically enable or disable the Bluetooth radio on the device → See `ohosbluetoothaccess蓝牙access模块/`
    - Retrieve the current status of the Bluetooth adapter using `getState` → See `ohosbluetoothaccess蓝牙access模块/`
    - Implement callback subscriptions to listen for real-time Bluetooth state changes → See `ohosbluetoothaccess蓝牙access模块/`
    - Integrate basic Bluetooth control features into a HarmonyOS application using ConnectivityKit → See `ohosbluetoothaccess蓝牙access模块/`

## Detailed Description

The following section provides a comprehensive breakdown of the documentation available in this directory. It details the specific API components, data structures, and functionalities that developers can leverage to manage Bluetooth operations effectively within the HarmonyOS environment.

### ohosbluetoothaccess蓝牙access模块/
This directory contains the API reference documentation for the HarmonyOS Bluetooth Access module, designed for the Cangjie programming language. It covers the essential functionalities required to manage Bluetooth radio states, including enabling, disabling, and querying the current status. The module also provides mechanisms for monitoring state changes through callback subscriptions. Key topics include `BluetoothState`, `BluetoothAccessCallbackType`, and functions like `enableBluetooth` and `getState`. This documentation is intended for HarmonyOS developers using the ConnectivityKit to implement basic Bluetooth control and state monitoring features in their applications, requiring specific permissions and system capabilities.