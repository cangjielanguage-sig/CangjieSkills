# 使用geometryTransition共享元素转场

This directory serves as a comprehensive technical resource for HarmonyOS developers aiming to master shared element transitions using the `geometryTransition` API within the ArkUI framework. It thoroughly covers the fundamental principles of binding components via identical IDs to generate smooth "one-shot" animations during view state changes. The content ranges from basic implementation, such as swapping images and containers, to advanced scenarios involving modal transitions and list interactions. With detailed code examples provided in both Cangjie and ArkTS, this collection is essential for developers seeking to create fluid, professional-grade user interfaces and seamless navigation experiences. It specifically addresses the nuances of animating between different objects and the necessary configuration of transition effects to ensure visual continuity across different view hierarchies.

## Quick Navigation

**What do you want to learn or implement?**

*   Learn the core concepts of `geometryTransition` and implement a simple component swap animation using Cangjie → 使用geometryTransition共享元素转场_2more.md
*   Master advanced integration of shared elements with modal transitions (`bindContentCover`) for list-to-page navigation using ArkTS or Cangjie → geometryTransition结合模态转场使用/

## Detailed Description

### 使用geometryTransition共享元素转场_2more.md 使用geometryTransition共享元素转场_2more.md
This tutorial guides developers on implementing seamless shared element transitions in ArkUI using the geometryTransition API. It explains the core concept of binding two components with the same ID to create a "one-shot" animation effect during view state changes. The document features a section on simple usage, providing a Cangjie code example that demonstrates swapping an image and a column container. Key takeaways include the distinction of geometryTransition for animating between different objects and the necessity of combining it with transition effects. This resource is intended for ArkUI developers seeking to enhance UI fluidity with advanced animation techniques.

### geometryTransition结合模态转场使用/
This directory provides comprehensive technical guides on implementing seamless shared element animations, specifically "one-shot" transitions, between list elements and modal pages within the HarmonyOS development environment. It explores the advanced integration of `geometryTransition` with modal transitions to achieve smooth visual continuity during view navigation. The content covers essential techniques such as setting up data models, configuring `bindContentCover`, and applying `TransitionEffect` for non-shared elements. It includes practical code examples for both the Cangjie language and ArkTS, detailing how to use matching IDs and `animateTo` for precise state control. This resource is ideal for HarmonyOS developers seeking to master advanced UI transitions and significantly enhance user experience with fluid, professional-grade navigation effects.