# 函数

This directory contains API reference documentation for utility functions within the Cangjie programming language, specifically focusing on metaprogramming, macro expansion, and Abstract Syntax Tree (AST) manipulation. It covers a wide range of functionalities including lexical analysis, token comparison, parsing tokens into AST nodes (declarations, expressions, patterns, types), diagnostic reporting, and inter-macro communication mechanisms. These tools are essential for developers working on compiler tools, custom macros, or source code analysis, providing the necessary interfaces to validate contexts, handle errors, and programmatically construct or inspect code structures.

## Quick Navigation

**What do you want to do?**

*   **Parse tokens into AST nodes**
    *   Parse declarations, expressions, and patterns → func_parseDeclFragmentToke_4more.md
    *   Parse specific declarations like constructors or properties → func_parseDeclTokens_String.md
    *   Parse patterns, types, and full programs → func_parsePatternFragmentT_4more.md
*   **Manage macro context and communication**
    *   Validate macro hierarchies and perform lexical analysis → func_assertParentContextSt_4more.md
    *   Retrieve messages from inner macros or verify nesting → func_getChildMessagesStrin_3more.md
    *   Pass data types from inner to outer macros → func_setItemString_Bool_4more.md
*   **Handle diagnostics and errors**
    *   Output diagnostic messages during macro expansion → func_diagReportDiagReportLevel_Tokens_Str_b3415654.md

## Detailed Description

### func_assertParentContextSt_4more.md
This document provides API reference documentation for four utility functions in the Cangjie programming language related to macro context checking and lexical analysis. The main sections cover `assertParentContext` for validating macro call hierarchies, two overloads of `cangjieLex` for converting strings into token objects, and `compareTokens` for comparing token sequences. Key information includes the specific parameters for each function, such as the `truncated` boolean in `cangjieLex`, and the exceptions thrown like `IllegalMemoryException`. The `assertParentContext` function has strict usage constraints requiring direct calls. This reference guide targets developers working on metaprogramming or compiler tools who need to manipulate source code tokens and validate macro execution contexts.

### func_diagReportDiagReportLevel_Tokens_Str_b3415654.md
This document provides API reference documentation for the `diagReport` function in the Cangjie programming language, designed to output diagnostic messages during the macro expansion phase. The content details the function signature, functional behavior, and specific usage notes regarding compilation termination and source code highlighting. Key information includes that `ERROR` levels halt compilation but not macro expansion, requiring manual intervention, and that the function highlights code based on token positions. It also lists parameters and exceptions like `ASTException` for invalid inputs. This technical reference targets compiler developers or macro writers implementing custom error reporting.

### func_getChildMessagesStrin_3more.md
This document serves as API reference documentation for three specific functions within the Cangjie macro system. The main sections cover getChildMessages, which retrieves messages from inner macros, getTokenKind, which converts numeric indices to token types, and insideParentContext, which verifies if a macro is nested within another. Key information includes strict usage constraints requiring direct function calls for getChildMessages and insideParentContext, as well as a specific note regarding the shared index 147 for string literals. The document explains the communication mechanism between nested macros using key-value pairs. This reference is intended for developers working on macro expansion and AST manipulation in the Cangjie language.

### func_parseDeclFragmentToke_4more.md
This document serves as API reference documentation for parsing functions within a Cangjie language module, detailing how to convert lexical tokens into Abstract Syntax Tree (AST) nodes. It covers four primary functions: parseDeclFragment, parseExpr, parseExprFragment, and parsePattern. Key information includes function signatures, input parameters (Tokens and optional start indices), return types (Decl, Expr, Pattern), and specific exceptions (ParseASTException) thrown during parsing failures. Notably, parseDeclFragment excludes specific declaration types like FuncParam. The document targets developers or compiler engineers who need to programmatically analyze or construct source code structures, requiring knowledge of the AST class hierarchy and token handling.

### func_parseDeclTokens_String.md
This API reference document details the parseDecl function used to parse lexical tokens into an Abstract Syntax Tree (AST) declaration node within the Cangjie standard library. It covers the function signature, parameters, return values, exceptions, and practical code examples. Key information includes the requirement for the astKind parameter to parse specific node types like PrimaryCtorDecl or PropMemberDecl (for property getters/setters) that lack standard keywords. The function returns a Decl node or throws a ParseASTException upon failure. This documentation targets Cangjie developers engaged in metaprogramming or AST manipulation who need to dynamically parse specific code structures.

### func_parsePatternFragmentT_4more.md
This document serves as API reference documentation for parsing functions within the Cangjie AST package. It details four primary functions: parsePatternFragment, parseProgram, parseType, and parseTypeFragment. These functions are designed to convert lexical tokens into Abstract Syntax Tree (AST) nodes, specifically Pattern, Program, and TypeNode types. Key information includes function signatures, parameters like Tokens and startFrom, return values, and potential ParseASTException errors. A critical note highlights that parseProgram retains package headers, which is incompatible with macro expansion requirements. This documentation targets developers working on compiler tools or macros who need to programmatically analyze or construct Cangjie source code structures.

### func_setItemString_Bool_4more.md
This API reference document details overloaded setItem functions in the Cangjie language, enabling inner macros to transmit data to outer macros. The content covers four specific function overloads accepting Bool, Int64, String, and Tokens value types, all sharing a String key parameter for retrieval. Key takeaways include the strict usage constraint that these functions must be called directly and cannot be assigned to variables, used as arguments, or returned. This documentation targets macro developers requiring a mechanism to pass specific data types between macro layers during compilation.