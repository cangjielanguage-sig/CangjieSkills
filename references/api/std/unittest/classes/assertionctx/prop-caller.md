<!-- cj-doc kind="api-member" level="6" id="std.unittest.class.assertionctx.prop-caller" parent="std.unittest.class.assertionctx" -->
# AssertionCtx.caller

[← AssertionCtx](index.md)

## 签名

```cangjie role=signature
public prop caller: String
```

获取用户定义的断言函数的标识符。

## 契约

类型：String。
