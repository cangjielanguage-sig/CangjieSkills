# 类

This directory contains API reference documentation for the core classes within the Cangjie serialization package. It covers the data model hierarchy, including abstract base types and concrete implementations for primitive data types like integers, floats, and booleans. Additionally, it details complex structures such as sequences, strings, and field-based objects used for converting class instances into serializable formats. This documentation is suitable for developers implementing serialization logic in the Cangjie programming language who need to understand how to structure, encapsulate, and manipulate data programmatically.

## Quick Navigation

**What do you want to learn?**

*   Understand the base abstract class and primitive wrappers (Bool, Int, Float, Null) → class_DataModel_5more.md
*   Handle structured data and class object conversion → class_DataModelStruct.md
*   Manage sequences (ArrayList) and string data → class_DataModelSeq_2more.md
*   Learn about the Field class used within structures → class_Field.md

## Detailed Description

### class_DataModelSeq_2more.md class_DataModelSeq_2more.md
This API reference document details specific subclasses within a Cangjie serialization package, focusing on data encapsulation for sequences and strings. It covers the DataModelSeq class for managing ArrayList<DataModel> collections and the DataModelString class for wrapping String values. Key components include constructors for initialization and methods like add, getItems, and getValue for data manipulation and retrieval. Both classes inherit from the base DataModel type. This technical documentation targets developers implementing serialization logic who need to handle structured data types in the Cangjie programming language.

### class_DataModelStruct.md class_DataModelStruct.md
This API documentation defines the DataModelStruct class, a subclass of DataModel within the Cangjie serialization package, designed to facilitate the conversion of class objects into data models. The document outlines the class structure, inheritance hierarchy, and functional methods. Key sections cover two constructors for initialization—either empty or with a pre-existing list of fields—and instance methods for data manipulation. Important functions include add for appending fields, get for retrieving values by key, and getFields for accessing the internal collection. This reference material is intended for developers working with data serialization who require a structured way to manage and query field data programmatically.

### class_DataModel_5more.md class_DataModel_5more.md
This document provides API reference documentation for the Cangjie serialization package, specifically defining the abstract DataModel class and its type-specific subclasses. The content details DataModelBool, DataModelFloat, DataModelInt, and DataModelNull, which serve as wrappers for primitive data types including Bool, Float64, Int64, and Null. Key takeaways include the constructors for initializing these models with specific values and the getValue methods for retrieving the encapsulated data. This guide is intended for developers working with data serialization who require a structured way to handle and convert basic data types within the Cangjie framework.

### class_Field.md class_Field.md
This document serves as API reference documentation for the Cangjie Field class, which is designed to store elements within a DataModelStruct. The content details the class definition, its initialization constructor, and accessor methods. Key information includes the constructor parameters for name and data, noting that an empty name string behaves identically to other strings, as well as the getData and getName functions for retrieving these values. This reference is intended for developers utilizing the serialization package who need to programmatically manage data model components.