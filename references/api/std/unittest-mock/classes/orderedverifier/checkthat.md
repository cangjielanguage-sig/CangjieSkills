<!-- cj-doc kind="api-member" level="6" id="std.unittest.mock.class.orderedverifier.checkthat" parent="std.unittest.mock.class.orderedverifier" -->
# OrderedVerifier.checkThat

[← OrderedVerifier](index.md)

## 签名

```cangjie role=signature
public func checkThat(statement: VerifyStatement): OrderedVerifier
```

添加一条 “验证语句”。

## 契约

参数：

- statement: VerifyStatement - 待被添加的“验证语句”。

返回值：

- OrderedVerifier - 返回对象自身。
