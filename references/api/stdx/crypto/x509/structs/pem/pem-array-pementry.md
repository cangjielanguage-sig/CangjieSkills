<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.pem.pem-array-pementry" parent="stdx.crypto.x509.struct.pem" -->
# Pem.Pem(Array<PemEntry>)

[← Pem](index.md)

## 签名

```cangjie role=signature
public Pem(private let items: Array<PemEntry>)
```

构造 Pem 对象。

## 契约

参数：

- items: Array\<PemEntry> - 多个 PemEntry 对象。
