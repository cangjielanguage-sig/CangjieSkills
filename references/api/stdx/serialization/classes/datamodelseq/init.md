<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.datamodelseq.init" parent="stdx.serialization.serialization.class.datamodelseq" -->
# DataModelSeq.init

[← DataModelSeq](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个参数为空的 DataModelSeq 实例。

### 契约

功能：构造一个参数为空的 DataModelSeq 实例。其中的数据默认为空的 ArrayList\<DataModel>。

## init(ArrayList<DataModel>)

### 签名

```cangjie role=signature
public init(list: ArrayList<DataModel>)
```

构造一个具有初始数据的 DataModelSeq 实例。

### 契约

参数：

- list: ArrayList\<DataModel> - 传入的 ArrayList\<DataModel> 类型的数据。
