# RSA

This directory contains technical documentation for implementing RSA cryptographic operations, specifically focusing on signing, verification, and parameter configuration within an algorithm library framework. It covers various padding schemes like PKCS1 and PSS, signature recovery modes, and specific parameter settings such as salt lengths and digest algorithms. The documentation addresses compatibility across different RSA key sizes (512 to 8192 bits) and API versions. It is intended for developers, security engineers, and cryptographers integrating asymmetric encryption functions who require detailed specifications for configuration and implementation.

## Quick Navigation

- **What do you want to learn?**
    - General RSA signing and verification modes (PKCS1, PSS) → RSA_2more.md
    - Configuration of PSS padding parameters and "OnlySign" mode → 获取设置PSS填充模式的参数_2more.md
    - Implementation of RSA signature recovery ("Recover" mode) → 验签模式为Recover.md
    - Specifics of PSS padding mode implementation → [填充模式为PSS/](填充模式为PSS/)

## Detailed Description

### RSA_2more.md
This technical reference document outlines the implementation of RSA signing and verification modes within an algorithm library framework. It details two padding schemes: PKCS1 and PSS, explaining their specific parameter requirements such as digest algorithms and salt lengths. The text provides a comprehensive table mapping supported RSA key sizes (512 to 8192 bits) to compatible digest algorithms for the PKCS1 mode, along with instructions for constructing string parameters. Key takeaways include the constraint that digest output length must be less than the key length and a performance warning regarding synchronous key generation for large keys. This guide is intended for developers integrating cryptographic functions, requiring knowledge of asymmetric encryption and OpenSSL standards.

### 获取设置PSS填充模式的参数_2more.md
This technical reference document details the configuration of RSA PSS padding parameters and the "OnlySign" signature mode within a cryptographic library. The first section lists PSS parameters like salt length and MGF, specifying which are readable or writable. The second section explains the "OnlySign" functionality, which performs RSA signatures without automatic hashing. It provides the specific string syntax for instantiating signature objects, combining key type, padding, digest, and mode. Key information includes supported RSA key sizes (512 to 8192 bits), API version requirements (12+), and strict data length constraints for PKCS1 and NoPadding modes. This guide targets developers implementing cryptographic operations who need precise parameter specifications and compatibility details.

### 验签模式为Recover.md
This document serves as a technical reference guide for implementing RSA signature recovery within an algorithm library framework. It covers the string parameter construction method, combining asymmetric key types, padding modes, digest algorithms, and the "Recover" verification mode. Users must concatenate parameters using the "|" symbol, such as "RSA2048|PKCS1|SHA256|Recover". The document lists supported RSA key sizes (512 to 8192 bits), PKCS1 and NoPadding options, and various hash algorithms. It notes that API version 12+ is required and that generic "RSA" types are supported for backward compatibility. Targeted at developers integrating cryptographic functions, this reference requires knowledge of asymmetric encryption concepts and API versioning.

### 填充模式为PSS/
This directory provides technical documentation and reference guides for implementing RSA cryptographic operations using the Probabilistic Signature Scheme (PSS) padding mode. It covers essential aspects such as parameter construction syntax, compatibility matrices for various key sizes (RSA512 to RSA8192), and specific constraints regarding hash algorithms, Mask Generation Functions (MGF), and salt lengths. Additionally, it addresses API compatibility and key type handling for signature verification. This resource is designed for developers, security engineers, and cryptographers who need to configure or validate RSA-PSS parameters within their systems.