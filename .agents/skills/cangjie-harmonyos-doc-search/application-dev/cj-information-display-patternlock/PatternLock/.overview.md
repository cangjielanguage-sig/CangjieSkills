# PatternLock

This directory contains comprehensive API reference documentation for the PatternLock component within the ArkUI framework, specifically designed for developers utilizing the Cangjie programming language. It covers the implementation of a 3x3 grid-based password verification system that tracks touch input to form secure patterns for user authentication. The documentation details component creation, state management via `PatternLockController`, visual customization properties, and event handling for password completion. It is suitable for UI developers and engineers building gesture-based security features for HarmonyOS applications who require precise control over pattern lock interfaces and logic.

## Quick Navigation

**What do you want to learn?**

- Learn about component initialization and basic functionality? → PatternLock_5more.md
- Customize visual appearance (colors, radius, side length)? → 组件属性.md
- Implement event handling and state reset logic? → 组件事件_3more.md

## Detailed Description

### PatternLock_5more.md PatternLock_5more.md
This document provides API reference documentation for the PatternLock component in the ArkUI framework, designed for implementing 3x3 grid-based password verification. It details the component's functionality, which tracks touch input to form patterns, and covers the necessary module imports. Key sections include the component creation process using the init method, which accepts an optional PatternLockController for state resetting. The document confirms support for all universal attributes and events. Intended for developers using the Cangjie language, this guide facilitates the integration of secure gesture-based authentication into application interfaces.

### 组件事件_3more.md 组件事件_3more.md
This document serves as API reference documentation for the PatternLock component within the ArkUI framework, specifically using the Cangjie language. It details the `onPatternComplete` event, which triggers upon password input completion and returns an array of selected dot indices. Additionally, it defines the `PatternLockController` class, highlighting its `reset` method for clearing the component state. The document includes a practical code example demonstrating password validation logic, state management, and UI integration. This guide is intended for developers implementing gesture-based security features in HarmonyOS applications, requiring knowledge of Cangjie syntax and ArkUI component lifecycle.

### 组件属性.md 组件属性.md
This document serves as an API reference for configuring the properties of a grid-based pattern lock component within the ArkUI framework using the Cangjie language. It details eight distinct configuration functions covering visual styling and interaction behavior. Key properties include `activeColor`, `selectedColor`, and `pathColor` for visual customization, `circleRadius` and `sideLength` for geometry, and `autoReset` for state management. All functions return `This` to support method chaining. This guide is intended for UI developers using ArkUI who need to customize the appearance and logic of pattern input interfaces.