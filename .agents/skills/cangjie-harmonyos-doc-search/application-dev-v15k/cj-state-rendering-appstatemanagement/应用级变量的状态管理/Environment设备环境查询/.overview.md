# Environment设备环境查询

This directory contains technical documentation and practical examples for the ArkUI framework's Environment class, specifically implemented in the Cangjie language. It is designed to assist developers in querying and utilizing device environment parameters to create adaptive and responsive user interfaces. The documentation covers the singleton class structure, static methods for binding system variables to AppStorage, and the retrieval of available properties. Key topics include the immutability of environment properties, the necessity of initializing `envProp` during application startup, and the handling of enums like `ColorMode` and `LayoutDirection`. This resource is highly suitable for developers working on features such as multi-language support, dark mode adaptation, and dynamic layout adjustments based on system settings.

## Quick Navigation

- **What do you want to learn?**
    - Review the API reference, class structure, and available enums → Environment设备环境查询_4more.md
    - Examine code examples for accessing and linking environment parameters → 示例代码.md

## Detailed Description

### Environment设备环境查询_4more.md Environment设备环境查询_4more.md

This document serves as the core API reference for the ArkUI framework's Environment class in the Cangjie language. It provides a comprehensive guide for developers on how to query device environment parameters effectively. The documentation details the singleton class structure, with a specific focus on static methods such as `envProp`, which is used to bind system variables to AppStorage, and `keys`, which retrieves available properties. Additionally, it defines essential enums for `ColorMode` and `LayoutDirection`. Key takeaways from this file include the immutability of environment properties and the critical necessity to call `envProp` during application startup. This resource is intended for developers implementing adaptive UI features such as multi-language support or dark mode based on system settings.

### 示例代码.md 示例代码.md

This document provides a practical code example demonstrating how to access and utilize device environment parameters within a UI application. It details the technical process of storing environment variables into AppStorage using `Environment.envProp` and linking them to UI components via `@StorageProp`. The content explains the unidirectional data flow from Environment to AppStorage to Component, emphasizing that these parameters are read-only. The provided Cangjie code snippet illustrates practical usage of parameters like language code, color mode, and layout direction. This guide is intended for developers working with the ArkUI framework to create responsive interfaces that adapt to system settings.