# 函数

This directory contains API reference documentation for file system manipulation functions in the Cangjie programming language. It covers essential operations such as path normalization, file and directory copying, conditional removal, and renaming. The documentation details function signatures, parameters, return values, and exception handling for types like `Path` and `String`. It is suitable for Cangjie developers implementing file I/O logic who need precise syntax details and error management specifications for handling file system operations efficiently.

## Quick Navigation

- **What do you want to do?**
    - Normalize paths or copy files/directories → func_canonicalizePath_3more.md
    - Check file existence or perform removal operations → func_copyString_String_Boo_5more.md
    - Conditionally remove files or rename/move items → func_removeIfExistsPath_Bo_3more.md
    - Understand specific rename behavior and overwrite logic → func_renameString_String_Bool.md

## Detailed Description

### func_canonicalizePath_3more.md func_canonicalizePath_3more.md
This document provides API reference details for file system operations in the Cangjie language, specifically path normalization and file copying. It covers three main functions: canonicalize(Path), canonicalize(String), and copy(Path, Path, Bool). Key information includes that canonicalize resolves relative paths and symlinks to absolute paths, while copy handles file and directory replication with overwrite controls and type matching requirements. Both functions define specific parameters, return values, and exceptions such as FSException and IllegalArgumentException. This technical reference is intended for Cangjie developers implementing file system logic who require precise syntax details and error handling specifications.

### func_copyString_String_Boo_5more.md func_copyString_String_Boo_5more.md
This document provides API reference documentation for file system manipulation functions in the Cangjie programming language. It details the specifications for `copy`, `exists`, and `remove` functions, including overloaded versions that accept either `String` or `Path` types. Key information includes the `copy` function's support for overwriting and type consistency checks, and the `remove` function's capability for recursive deletion. The documentation highlights error handling, noting that these functions throw `FSException` or `IllegalArgumentException` for invalid paths or failed operations. This reference is intended for developers implementing file I/O operations who require precise parameter definitions and exception behavior details.

### func_removeIfExistsPath_Bo_3more.md func_removeIfExistsPath_Bo_3more.md
This document provides API reference documentation for file manipulation functions in the Cangjie programming language, specifically focusing on conditional removal and renaming operations. It details three function overloads: removeIfExists accepting either a Path or String object, and rename for moving or renaming files and directories. Key information includes the recursive parameter for deleting directories and the overwrite flag in rename, which implicitly deletes existing targets. The documentation specifies return types, parameters, and potential exceptions such as FSException and IllegalArgumentException. This reference guide targets developers implementing file system operations who need precise syntax and behavior details for handling paths and error management.

### func_renameString_String_Bool.md func_renameString_String_Bool.md
This document serves as API reference documentation for the rename function in the Cangjie language, detailing how to rename or move files and directories. The content includes the function signature, parameter descriptions for source path, destination path, and overwrite flag, as well as potential exceptions. Key takeaways include the overwrite behavior: if set to true, existing targets are deleted recursively, which requires caution. The document also lists exceptions like FSException and IllegalArgumentException. This technical reference is intended for developers implementing file system operations in Cangjie applications.