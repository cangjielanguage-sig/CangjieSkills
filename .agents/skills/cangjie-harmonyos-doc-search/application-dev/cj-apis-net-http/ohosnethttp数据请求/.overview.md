# ohosnethttp数据请求

This directory provides comprehensive API reference documentation for the `ohos.net.http` module in the Cangjie programming language, designed to facilitate HTTP data requests in HarmonyOS applications. It covers the SystemCapability.Communication.NetStack, detailing essential classes for requests, responses, caching, and SSL management, alongside enumerations for protocols and methods. Developers will find resources for configuring network behaviors, handling multipart data, and monitoring performance. This documentation is essential for Cangjie developers implementing secure and efficient network communication, offering guidance on everything from basic request setup to advanced metrics tracking and resource management.

## Quick Navigation

**What do you want to do?**

*   **Get started with the module** (Imports, permissions, initialization) → ohosnethttp数据请求_6more.md
*   **Configure and send requests** (Options, lifecycle, streaming) → class_HttpRequestOptions/ → class_HttpRequest/
*   **Handle server responses** (Parsing, caching, status codes) → class_HttpResponse.md → class_HttpResponseCache.md
*   **Define data types and protocols** (Methods, events, certificates) → enum_RequestMethod_2more.md → enum_HttpRequestEvent.md → enum_CertType_4more.md
*   **Implement advanced features** (Uploads, security, metrics) → class_MultiFormData.md → class_ClientCert_3more.md → class_PerformanceTiming/

## Detailed Description

### class_ClientCert_3more.md class_ClientCert_3more.md
This document serves as an API reference for classes used in network communication within the Cangjie language. It defines the `ClientCert` class for managing client-side certificates, including properties for paths, types, and passwords. Additionally, it covers `DataReceiveProgressInfo` and `DataSendProgressInfo` classes, which utilize Int64 variables to track data transfer progress against total sizes. This documentation is vital for developers implementing secure connections and monitoring transmission efficiency using the NetStack module.

### class_HttpResponse.md class_HttpResponse.md
This document details the `HttpResponse` class in Cangjie, which represents the return value for HTTP request callbacks. It outlines six public properties: `result`, `resultType`, `responseCode`, `header`, `cookies`, and `performanceTiming`. Key aspects include the logic for result formatting based on content-type and the requirement for manual parsing of header JSON strings. All fields are read/write, making this reference essential for developers handling response data and status codes.

### class_HttpResponseCache.md class_HttpResponseCache.md
This API reference covers the `HttpResponseCache` class, which manages HTTP response storage to improve application performance. It explains HTTP caching concepts, including headers like Cache-Control and ETag, and directives such as max-age. The documentation outlines methods for disabling the cache (`delete`) and writing data to the file system (`flush`). Developers must initialize the cache using `createHttpResponseCache` before use, requiring familiarity with HTTP protocols.

### class_MultiFormData.md class_MultiFormData.md
This document details the `MultiFormData` class for constructing multipart form data within the NetStack capability. It focuses on properties like `name`, `contentType`, `data`, and `filePath`, along with the class constructor. Key information includes the requirement to specify data via `HttpData` or file paths, and the necessity of the `remoteFileName` field for binary uploads. This resource is intended for developers implementing file uploads or complex form submissions.

### enum_CertType_4more.md enum_CertType_4more.md
This document provides an API reference for network-related enumerations in Cangjie. It details `CertType` (Pem, Der), `HttpData` and `HttpDataType` for payloads, and `HttpProtocol` for versions like HTTP/1.1 and HTTP/3. Notably, HTTP/3 is restricted to HTTPS URLs. Targeted at developers working on network communication, this reference provides definitions and system capabilities starting from version 22.

### enum_HttpRequestEvent.md enum_HttpRequestEvent.md
This reference defines the `HttpRequestEvent` enum for managing HTTP request lifecycles. It lists members such as `HeadersReceive`, `DataReceive`, `DataEnd`, and progress events. The document also covers inherited operators for equality and hashing. This enum is part of the NetStack capability and is available from version 22, assisting developers in handling asynchronous streaming events and tracking transmission progress.

### enum_RequestMethod_2more.md enum_RequestMethod_2more.md
This document details enums for network communication, specifically `RequestMethod` (defining GET, POST, PUT, Delete) and `UsingProxy` (specifying proxy modes like NotUse or UseSpecified). It provides functional descriptions for each member and outlines the associated system capabilities. This reference is intended for developers integrating HTTP client functionality into applications using the Cangjie framework.

### ohosnethttp数据请求_6more.md ohosnethttp数据请求_6more.md
This document serves as the main API reference for the `ohos.net.http` module, enabling HTTP data requests in Cangjie applications. It covers module imports, internet permissions, and context configuration. Key sections include the `createHttp` function for initiating requests and `createHttpResponseCache` for storage management. It emphasizes the necessity of calling the `destroy` method on `HttpRequest` objects to prevent resource leaks.

### class_HttpRequest/
This directory contains API reference documentation for the `HttpRequest` class in Cangjie, designed for HarmonyOS development. It covers essential operations such as initiating asynchronous requests, managing streaming responses, and handling request lifecycles. Key topics include methods like `request`, `requestInStream`, `on`, and `destroy`, along with error handling for `BusinessException`. It is suitable for developers implementing robust network communication and asynchronous callbacks.

### class_HttpRequestOptions/
This directory provides documentation for the `HttpRequestOptions` class, covering configuration parameters for HTTP requests. Key topics include SSL security settings, DNS configurations, request headers, data types, timeouts, caching strategies, and proxy management. It is designed for developers needing precise control over HTTP/HTTPS client behaviors, from basic setup to advanced features like resumable downloads.

### class_PerformanceTiming/
This directory offers documentation for the `PerformanceTiming` class, used to capture network request metrics with high precision. It covers granular timing data for DNS resolution, TCP connection, TLS negotiation, and redirects, measured in milliseconds. The properties are Float64 types supporting read/write access. This resource is ideal for developers analyzing and optimizing network performance within the NetStack capability.