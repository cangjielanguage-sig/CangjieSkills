<!-- cj-doc kind="api-member" level="6" id="std.fs.enum.openmode.operator-eq" parent="std.fs.enum.openmode" -->
# OpenMode.==

[← OpenMode](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: OpenMode): Bool
```

比较 OpenMode 实例是否相等。

## 契约

参数：

- that: OpenMode - 待比较的 OpenMode 实例。

返回值：

- Bool - 如果相等，则返回 true，否则返回 false。
