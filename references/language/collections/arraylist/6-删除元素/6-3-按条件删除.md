<!-- cj-doc kind="guide-leaf" level="6" id="language.collections.arraylist.6-删除元素.6-3-按条件删除" parent="language.collections.arraylist.6-删除元素" -->
# 6.3 按条件删除

[← 6. 删除元素](index.md)

```cangjie cjtest=syntax id=syntax-8f895a81b9-1 form=unit
func removeIf(predicate: (T) -> Bool): Unit
```

```cangjie cjtest=syntax id=syntax-8f895a81b9-2 form=stmt
let list = ArrayList<Int64>([1, 2, 3, 4, 5, 6])
list.removeIf { v => v % 2 == 0 }  // [1, 3, 5]
```

- 在 `predicate` 中修改 ArrayList 会抛 `ConcurrentModificationException`
