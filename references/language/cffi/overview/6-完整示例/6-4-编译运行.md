<!-- cj-doc kind="guide-leaf" level="6" id="language.cffi.overview.6-完整示例.6-4-编译运行" parent="language.cffi.overview.6-完整示例" -->
# 6.4 编译运行

[← 6. 完整示例](index.md)

> 完整的编译构建命令请参考 [CFFI 构建](../../build/index.md)。以下为上述示例的快速编译参考：

**Linux**
```shell
clang -shared -fPIC -fstack-protector-all native.c -o libnative.so
cjc -L . -l native main.cj -o main
./main
```

**Windows**
```shell
clang -shared -fstack-protector-all native.c -o libnative.dll
cjc -L . -l native main.cj -o main.exe
./main.exe
```

---
