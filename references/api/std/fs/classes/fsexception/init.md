<!-- cj-doc kind="api-member" level="6" id="std.fs.class.fsexception.init" parent="std.fs.class.fsexception" -->
# FSException.init

[← FSException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个文件异常实例，无异常提示信息。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

构造一个文件异常实例，有异常提示信息。

### 契约

参数：

- message: String - 错误信息。
