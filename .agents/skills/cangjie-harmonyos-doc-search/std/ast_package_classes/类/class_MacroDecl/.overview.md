# class_MacroDecl

This directory contains technical documentation for the `MacroDecl` class and associated traversal utilities within the Cangjie language's Abstract Syntax Tree (AST). It focuses on the structural definition of macro declaration nodes, detailing their inheritance hierarchy, mutable properties like tokens and block structures, and methods for serialization back into tokens. Furthermore, it covers the visitor pattern implementation for traversing syntax trees, allowing for programmatic inspection and manipulation of code structures. Key concepts include AST node construction, token handling, and tree traversal algorithms. This resource is specifically designed for compiler developers, language engineers, and software developers involved in building or maintaining the Cangjie compiler infrastructure.

## Quick Navigation

**What do you want to learn?**

*   Understand the structure, properties, and constructors of the MacroDecl class → class_MacroDecl_10more.md
*   Learn how to iterate over AST nodes using the visitor pattern → func_traverseVisitor.md

## Detailed Description

### class_MacroDecl_10more.md class_MacroDecl_10more.md
This document provides API reference documentation for the MacroDecl class, which represents a macro definition node within the Abstract Syntax Tree (AST) of the Cangjie language. It covers the class structure, including its inheritance from the Decl type, and details various mutable properties such as block, colon, declType, funcParams, and parentheses tokens. Key information includes two constructors for default and token-based initialization, a method to convert nodes back to Tokens, and exception handling for invalid token types. Intended for compiler developers and language engineers, this guide assists in programmatically constructing, manipulating, and serializing macro definitions within the AST framework.

### func_traverseVisitor.md func_traverseVisitor.md
This document provides API reference documentation for the `traverse` function, designed to iterate over syntax tree nodes and their children using a visitor pattern. The content covers the function signature, a description of its traversal capabilities, parameter details for the `Visitor` instance, and a reference to an external example for customizing traversal behavior. Key takeaways include that the function accepts a `Visitor` object to walk the Abstract Syntax Tree (AST). Developers can control the traversal flow by overriding the `visit` function and calling `breakTraverse` to stop early. This reference material targets developers working with AST manipulation in the Cangjie language who need to inspect or process code structures programmatically.