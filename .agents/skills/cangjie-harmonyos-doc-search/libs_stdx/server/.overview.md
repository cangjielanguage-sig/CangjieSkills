# server

This directory is a comprehensive educational resource designed for developers utilizing the Cangjie programming language to build secure network applications. It focuses specifically on the implementation of a Transport Layer Security (TLS) server, providing a step-by-step code tutorial that addresses the complexities of encrypted communication. The content covers the entire lifecycle of setting up a secure server, starting with the necessary imports of standard libraries and progressing to the technicalities of parsing X509 certificates and private keys from PEM files. It offers in-depth explanations on configuring the TLS server context, creating a `TlsServerConfig`, and binding a `TcpServerSocket` to port 8443. Additionally, the tutorial explores advanced concurrency patterns, demonstrating how to manage multiple client connections simultaneously through a custom `acceptLoop` extension that leverages the `spawn` functionality. By detailing the handshake process and data reading mechanisms via `TlsSocket`, this guide ensures developers understand both the security and networking aspects. It is intended for Cangjie developers who possess a foundational understanding of network programming and have their own certificate files ready for integration. By following this guide, developers will gain the practical skills necessary to deploy secure, high-performance servers capable of handling encrypted traffic efficiently.

## Quick Navigation

What do you want to learn?

- Implement a secure TLS server from scratch in Cangjie → server.md
- Import and configure standard libraries for network security → server.md
- Parse X509 certificates and private keys from PEM files → server.md
- Set up TlsServerConfig and bind TcpServerSocket to port 8443 → server.md
- Handle concurrent client connections using a custom acceptLoop → server.md
- Utilize the spawn function for effective concurrency management → server.md
- Understand the TLS handshake and data reading via TlsSocket → server.md

## Detailed Description

The following file provides the complete implementation guide and code examples for the server module.

### server.md
This document serves as a detailed code tutorial demonstrating how to implement a secure TLS server using the Cangjie language. It covers essential steps including importing standard libraries, parsing X509 certificates and private keys from PEM files, and configuring the TLS server context. Key takeaways include the creation of a TlsServerConfig, binding a TcpServerSocket to port 8443, and handling concurrent client connections via a custom acceptLoop extension that utilizes spawn for concurrency. The example highlights the handshake process and data reading through TlsSocket. Intended for Cangjie developers, this guide requires users to provide their own certificate files and assumes familiarity with network programming concepts. This file is essential for understanding the practical integration of security protocols in Cangjie server applications.