<!-- cj-doc kind="api-member" level="6" id="std.net.enum.socketnet.operator-ne" parent="std.net.enum.socketnet" -->
# SocketNet.!=

[← SocketNet](index.md)

## 签名

```cangjie role=signature
public operator func !=(that: SocketNet): Bool
```

判断两个 SocketNet 是否不相等。

## 契约

参数：

- that: SocketNet - 传入的 SocketNet。

返回值：

- Bool - 如果不相等，则返回 `true`；否则，返回 `false`。
