<!-- cj-doc kind="api-member" level="7" id="std.math.numeric.struct.decimal.init.init-7d4d394237" parent="std.math.numeric.struct.decimal.init" -->
# Decimal.init(Int64)

[← Decimal.init](index.md)

## 签名

```cangjie role=signature
public init(val: Int64)
```

通过 64 位有符号整数构建 Decimal 对象。

## 契约

功能：通过 64 位有符号整数构建 Decimal 对象。默认采用精度值为 0，即无限精度进行构建。

参数：

- val: Int64 - 64 位有符号整数。
