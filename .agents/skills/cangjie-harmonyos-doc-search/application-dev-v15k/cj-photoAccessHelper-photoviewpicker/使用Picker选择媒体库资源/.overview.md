# 使用Picker选择媒体库资源

This directory provides a comprehensive guide for HarmonyOS developers on how to access and manage user media resources efficiently. It focuses on utilizing the system photo picker to select files without needing complex permission declarations, followed by the necessary steps to process the selected URIs. The content covers the entire workflow from invoking the picker interface to reading raw file data and retrieving high-quality media assets using APIs like MediaLibraryKit and CoreFileKit. It is specifically designed for developers using the Cangjie programming language and UIAbility components who need to implement features such as media sharing, file selection, or asset management within their applications. By following these tutorials, developers can learn to handle read-only permissions correctly and avoid common pitfalls like performing file operations directly within picker callbacks.

## Quick Navigation

**What do you want to do?**

*   Select media files using the system picker without requesting specific permissions → 使用Picker选择媒体库资源.md
*   Read raw file data or retrieve high-quality media assets using the returned URIs → 指定URI读取文件数据_2more.md

## Detailed Description

### 使用Picker选择媒体库资源.md 使用Picker选择媒体库资源.md
This tutorial instructs developers on using the system photo picker to access user media files without requiring specific permissions. It details the implementation process, including importing modules like MediaLibraryKit, configuring selection options such as MIME type and maximum number, and invoking the select interface. Key takeaways include understanding that returned URIs possess read-only permissions and the critical constraint of avoiding direct file operations within the picker callback. The document also references subsequent steps for reading file data and retrieving metadata. This guide targets developers working with UIAbility components who need to integrate media sharing or selection capabilities into their applications.

### 指定URI读取文件数据_2more.md 指定URI读取文件数据_2more.md
This technical guide explains how to read file data and retrieve media resources using specific URIs in the Cangjie programming language. It covers two primary workflows: reading raw file data using the CoreFileKit API and fetching image or video assets via the MediaLibraryKit. Key steps include opening files with OpenMode.READ_ONLY to obtain a file descriptor for reading bytes, and using PhotoAccessHelper with predicates to query and request high-quality media data. Intended for HarmonyOS developers, this tutorial requires knowledge of file I/O operations and media asset management.