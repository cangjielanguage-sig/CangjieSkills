<!-- cj-doc kind="api-member" level="7" id="std.core.interface.hasher.write.write-f8080545e0" parent="std.core.interface.hasher.write" -->
# Hasher.func write(UInt8)

[← Hasher.write](index.md)

## 签名

```cangjie role=signature
mut func write(value: UInt8): Unit
```

通过该函数把想要哈希运算的 UInt8 值传入，然后进行哈希组合运算。

## 契约

参数：

- value: UInt8 - 待运算的值。
