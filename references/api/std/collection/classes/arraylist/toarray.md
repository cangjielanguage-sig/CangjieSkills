<!-- cj-doc kind="api-member" level="6" id="std.collection.class.arraylist.toarray" parent="std.collection.class.arraylist" -->
# ArrayList<T>.toArray

[← ArrayList<T>](index.md)

## 签名

```cangjie role=signature
public func toArray(): Array<T>
```

返回一个数组，其中包含此列表中按正确顺序排列的所有元素。

## 契约

返回值：

- Array\<T> - T 类型数组。
