# ifelse条件渲染

This directory provides a comprehensive guide to conditional rendering using `if`, `else`, and `else if` statements within the Cangjie framework for ArkUI development. It covers the fundamental implementation rules, including variable type constraints, parent-child component relationships, and specific container restrictions. The content explains the critical update mechanisms where state variables trigger real-time UI updates, contrasting with regular variables. It also details the lifecycle behavior where components are destroyed and reconstructed upon condition changes. This resource is essential for Cangjie developers and UI engineers aiming to build responsive, dynamic interfaces with robust state management and efficient rendering logic.

## Quick Navigation

- **What do you want to learn?**
    - Understand the core rules, syntax, and update mechanisms of `if/else` rendering → ifelse条件渲染_3more.md
    - Explore practical usage scenarios, lifecycle management, and state preservation strategies → [使用场景/]

## Detailed Description

The following sections provide in-depth technical details and practical examples to help you implement conditional logic effectively.

### ifelse条件渲染_3more.md ifelse条件渲染_3more.md
This document outlines the implementation and rules of conditional rendering using if, else, and else if statements within the Cangjie framework for dynamic UI development. It covers usage rules, including variable types, parent-child relationships, and container restrictions, alongside the update mechanism for state changes. Key takeaways include that state variables trigger real-time UI updates, while regular variables do not. When conditions change, the system deletes old components and reconstructs the new branch. Developers must ensure non-empty build functions and respect specific container component limitations like Grid. This technical guide targets developers using the Cangjie framework who need to manage dynamic UI content based on application state.

### 使用场景/
This directory focuses on practical usage scenarios for conditional rendering within the ArkUI framework using the Cangjie language. It covers the implementation of `if` statements, component lifecycle management during state changes, and state preservation strategies across conditional branches. Key topics include `if...else` logic, nested conditional statements, and the impact of `@State` and `@Link` decorators on child component state. Understanding these scenarios is crucial for building responsive interfaces that maintain data integrity during UI updates. This resource is suitable for Cangjie developers and UI engineers looking to master dynamic UI updates and state management in ArkUI applications.