# 导入所需API模块

This directory provides technical tutorials and code examples for Cangjie developers implementing Bluetooth Low Energy (BLE) GATT client functionality. It covers the complete workflow of BLE operations, including module imports, connection management, service discovery, and data exchange via reading and writing characteristics and descriptors. Key topics include handling asynchronous callbacks, managing connection states, and enabling notifications or indications for real-time updates. The content is suitable for developers looking to integrate robust BLE communication into their applications using the ConnectivityKit, with a focus on error handling and specific API implementations.

## Quick Navigation

- **What do you want to learn?**
    - Understand the basic BLE GATT client workflow (connection, discovery, read/write) → 导入所需API模块_1.md
    - Learn specific BLE operations (read/write characteristics/descriptors, notifications) → 导入所需API模块_2.md
    - Handle server characteristic changes (Notifications vs. Indications) → 导入所需API模块_3.md
    - See a specific code implementation for enabling characteristic change indication → 导入所需API模块_4.md

## Detailed Description

### 导入所需API模块_1.md 导入所需API模块_1.md
This document serves as a technical tutorial for Cangjie developers implementing Bluetooth Low Energy (BLE) GATT client functionality. It outlines a five-step workflow: creating a client instance, subscribing to connection state changes, initiating a connection, performing service discovery, and transmitting data through reading or writing characteristic values. Key takeaways include the necessity of service discovery before any read/write operations and the use of specific callbacks to monitor connection states. The guide provides code snippets for importing required modules like ConnectivityKit and handling business exceptions.

### 导入所需API模块_2.md 导入所需API模块_2.md
This technical guide provides Cangjie code examples for performing Bluetooth Low Energy (BLE) operations using the ConnectivityKit. It details module imports and demonstrates managing GATT client connections, specifically covering reading and writing characteristic values, reading and writing descriptor values, and receiving server notifications. The content emphasizes asynchronous callbacks, UUID configuration for services and characteristics, and robust error handling using business exceptions. It serves as a practical tutorial for developers implementing BLE data exchange in applications.

### 导入所需API模块_3.md 导入所需API模块_3.md
This document serves as a tutorial for implementing Bluetooth Low Energy (BLE) client functionality to handle server characteristic changes using the Cangjie language. It covers the prerequisites for notifications and indications, specifically the requirement for the Client Characteristic Configuration descriptor. The content details the step-by-step process of importing necessary modules, defining callback classes, and creating GATT client devices. Key takeaways include the distinction between notifications (no confirmation) and indications (automatic confirmation), and the specific API calls required to subscribe to events and enable these features. The guide provides practical Cangjie code examples for developers to integrate real-time data updates from BLE peripherals.

### 导入所需API模块_4.md 导入所需API模块_4.md
This document serves as a technical code snippet demonstrating how to enable characteristic change indication for a Bluetooth GATT client. The primary focus is on the implementation of the `setCharacteristicChangeIndication` method within a BLE application context. Key sections include the function invocation with parameters for the target characteristic and a boolean enable flag, an asynchronous callback handling success or failure via `BusinessException`, and a try-catch block for runtime error management. Important takeaways involve understanding the callback structure for asynchronous operations and proper logging using `Hilog`. This reference is intended for developers implementing Bluetooth Low Energy features in ArkTS, requiring prior knowledge of GATT client roles and exception handling.