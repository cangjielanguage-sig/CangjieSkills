# cj-declare-permissions-in-acl

This directory provides a comprehensive technical guide for HarmonyOS developers who need to navigate the complexities of application privilege levels and security models. It specifically addresses the common development hurdle where an application requires access to restricted system capabilities—such as global floating windows or screen capture—that exceed the app's standard Ability Privilege Level (APL). The content focuses on the Access Control List (ACL) mechanism, which allows for the temporary elevation of permissions strictly during the debugging phase. It details two primary methods for resolving level mismatches: utilizing the integrated features of DevEco Studio or manually editing the `HarmonyAppProvision` JSON configuration file. Additionally, the guide covers critical post-configuration steps, including editing specific fields like "allowed-acls" and "app-services-capabilities," and the necessity of re-signing the application. This resource is essential for developers ensuring their apps function correctly with high-level resources while adhering to the constraint that these methods are not suitable for commercial release.

## Quick Navigation

**What do you want to do?**

*   **Resolve APL level mismatch issues** during the development lifecycle → cj-declare-permissions-in-acl.md
*   **Apply for restricted permissions** using the automated DevEco Studio approach → cj-declare-permissions-in-acl.md
*   **Manually configure permissions** by editing the `HarmonyAppProvision` JSON file → cj-declare-permissions-in-acl.md
*   **Understand specific configuration fields** such as "allowed-acls" and "app-services-capabilities" → cj-declare-permissions-in-acl.md
*   **Learn the re-signing process** required after modifying permission configurations → cj-declare-permissions-in-acl.md
*   **Clarify usage boundaries** regarding debugging versus commercial release environments → cj-declare-permissions-in-acl.md

## Detailed Description

This section outlines the specific file available in the directory, providing the necessary steps to configure permissions for HarmonyOS applications.

### cj-declare-permissions-in-acl.md cj-declare-permissions-in-acl.md

This guide explains how HarmonyOS applications can apply for restricted permissions that exceed their APL level using ACL methods during the debugging phase. It covers the problem of level mismatch and provides two specific methods: using DevEco Studio or manually modifying the HarmonyAppProvision JSON configuration file. Key information includes editing "allowed-acls" and "app-services-capabilities" fields and re-signing the app. Crucially, the document states these methods are strictly for debugging and not for commercial release. This is intended for HarmonyOS developers needing to access high-level system resources like global floating windows or screen capture during development. By following these instructions, developers can ensure their applications have the necessary temporary access to validate functionality involving sensitive system operations.