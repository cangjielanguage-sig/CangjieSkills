<!-- cj-doc kind="guide-leaf" level="5" id="language.class.3-继承.3-1-基本规则" parent="language.class.3-继承" -->
# 3.1 基本规则

[← 3. 继承](index.md)

- 单继承：`class B <: A { }`。`A` 须为 `open` 或 `abstract`
- 子类继承所有非 `private`、非构造函数成员
- 类不能继承自身。无显式父类的类继承自 `Object`
- `open` 修饰的成员被继承时，`open` 修饰符也会被继承
