# extend_UInt64_SaturatingOpUInt64

This directory provides comprehensive API reference documentation for extending the `UInt64` type with saturating operations within the Cangjie programming language. It focuses on the implementation of the `SaturatingOp` interface and specific utility functions designed to handle arithmetic and bitwise manipulations safely. The documentation covers a wide range of operations, including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and bit shifting. A core concept explored here is saturation behavior, where results are clamped to the type's maximum or minimum limits upon overflow or underflow, thereby preventing undefined behavior. This resource is essential for Cangjie developers who require precise control over numeric boundaries and robust error handling in low-level or performance-critical programming tasks.

## Quick Navigation

**What do you want to learn?**

*   Review the complete implementation of the `SaturatingOp` interface for `UInt64`, including arithmetic and bitwise operations → extend_UInt64_SaturatingOp_9more.md
*   Explore specific saturating functions such as `saturatingShr` (right shift) and `saturatingSub` (subtraction) → func_saturatingShrUInt64_2more.md

## Detailed Description

The following files provide specific implementation details and API references for the saturating operations available for the `UInt64` type.

### extend_UInt64_SaturatingOp_9more.md extend_UInt64_SaturatingOp_9more.md

This API reference document details the implementation of the SaturatingOp interface for the UInt64 type in the Cangjie programming language. The content covers arithmetic and bitwise operations including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and left shift. Key takeaways include the specific saturation behaviors where operations clamp results to the type's maximum or minimum values upon overflow or underflow, ensuring safe computation. This documentation targets Cangjie developers who require precise control over numeric boundaries and error handling in low-level programming.

### func_saturatingShrUInt64_2more.md func_saturatingShrUInt64_2more.md

This document serves as API reference documentation for two specific arithmetic functions in the Cangjie programming language, specifically designed for the UInt64 type. It details the saturatingShr function for right-shifting and the saturatingSub function for subtraction, both utilizing saturation strategies to handle edge cases. Key information includes that saturatingShr clamps the shift amount to the bit width minus one to prevent excessive shifting, while saturatingSub returns the type's maximum or minimum value upon overflow or underflow respectively. Both functions accept a UInt64 parameter and return a UInt64 result. This reference is intended for Cangjie developers needing safe arithmetic operations that prevent undefined behavior by saturating results rather than wrapping or throwing errors.