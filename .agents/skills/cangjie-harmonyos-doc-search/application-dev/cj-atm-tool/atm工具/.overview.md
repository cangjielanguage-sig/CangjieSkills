# atm工具

This directory contains comprehensive documentation for the Access Token Manager (atm) tool, a specialized utility designed for querying application process permissions and managing access control information within the system. It covers essential topics such as environment setup using the hdc shell, command lists for `help` and `dump` functions, and methods to inspect access control data based on specific identifiers like token IDs, package names, and process names. Additionally, it includes detailed guides on retrieving system application processes, checking current permission states, and auditing historical permission usage records. The content is tailored for developers and system administrators who need to debug applications, enforce security compliance, and maintain oversight of the operating system's permission management framework.

## Quick Navigation

**What do you want to learn?**

*   Understand the basic tool reference and environment setup requirements → atm工具_7more.md
*   Explore specific query commands, syntax flags, and JSON output interpretation → 查询命令/

## Detailed Description

### atm工具_7more.md atm工具_7more.md

This document serves as a reference guide for the Access Token Manager (atm) tool, a utility used to query application process permissions and access control information. Main sections include environment setup requirements using the hdc shell and a command list detailing the `help` and `dump` functions. Key information covers the tool's ability to inspect access control data based on token IDs, package names, and process names. The target audience is developers who need to debug or inspect application permissions, requiring familiarity with the hdc tool.

### 查询命令/

This directory serves as a specialized repository for technical documentation regarding the `atm dump` command-line tool, which is essential for querying and managing system-level application data. It encompasses detailed guides on retrieving system application processes, checking current permission states, and auditing historical permission usage records. The content explains command syntax, various parameter flags for filtering specific data points like token IDs and bundle names, and how to interpret the resulting JSON output structures. Key functionalities include monitoring process attributes, defining permission usage types, and tracking access to sensitive system resources such as the camera. This documentation is primarily intended for system developers and administrators who need to debug applications, enforce security compliance, and maintain oversight of the operating system's permission management framework.