# extend_IntNative_WrappingOpIntNative

This directory provides comprehensive API documentation for the `WrappingOp` interface implementation specifically tailored for the `IntNative` type within the Cangjie programming language. It focuses on arithmetic and bitwise operations designed to handle integer overflow through a high-bit truncation strategy, ensuring results wrap around predictably rather than causing runtime errors. Key topics include standard arithmetic functions like addition, subtraction, multiplication, and division, alongside bitwise manipulations such as shifting. This resource is essential for Cangjie developers engaged in low-level systems programming or performance-critical tasks where deterministic overflow behavior is necessary.

## Quick Navigation

**What do you want to learn?**

*   Understand the core implementation of the WrappingOp interface for IntNative → extend_IntNative_WrappingO_9more.md
*   Explore specific functions like wrapping right shift and subtraction → func_wrappingShrUInt64_2more.md

## Detailed Description

### extend_IntNative_WrappingO_9more.md extend_IntNative_WrappingO_9more.md

This API documentation details the implementation of the `WrappingOp` interface for the `IntNative` type in the Cangjie programming language. The document covers specific arithmetic and bitwise functions including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and left shift operations. Key information is that all operations utilize a high-bit truncation strategy to handle overflow, ensuring results wrap around rather than causing errors. This reference is intended for Cangjie developers requiring precise control over integer overflow behavior in low-level or performance-critical code.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md

This document provides API reference documentation for two wrapping operations in the Cangjie language: `wrappingShr` and `wrappingSub`. The text details the `wrappingShr` function, which performs a right shift using low bits of the shift amount if it exceeds the bit width, and `wrappingSub`, which performs subtraction with high-bit truncation upon overflow. Key takeaways include the specific handling of overflow and shift boundaries, with both functions returning an `IntNative` type. This reference is intended for Cangjie developers requiring precise control over low-level integer arithmetic and bitwise manipulation behaviors.