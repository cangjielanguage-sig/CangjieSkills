<!-- cj-doc kind="example-category" level="3" id="examples.project-build" parent="examples" -->
# 项目构建与测试

[← 应用示例](../index.md)

建立可运行的 cjpm 工程，并用仓颉单元测试组织实现验证。

| 示例 | 教学目标 |
|---|---|
| [创建最小可执行工程](cjpm-minimal.md) | 用 1.0.5 清单、包声明和 main 建立可直接执行的 cjpm 工程骨架。 |
| [组织并运行单元测试](unit-test.md) | `std.unittest` 的 `@Test` / `@TestCase` 使用 `@Assert(actual, expected)` 或 `@Expect(actual, expected)` 做相等性断言；没有 `assertEquals` 函数。 |
