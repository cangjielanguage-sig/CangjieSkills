<!-- cj-doc kind="api-member" level="6" id="std.math.interface.integer.issigned" parent="std.math.interface.integer" -->
# Integer<T>.isSigned

[← Integer<T>](index.md)

本页汇总 11 个同名重载；先按签名选择，再读取对应契约。

## static func isSigned()

### 签名

```cangjie role=signature
static func isSigned(): Bool
```

判断类型是否是有符号的。

### 契约

返回值：

- Bool - 如果类型是有符号的，返回 `true`；否则返回 `false`。

## static func isSigned()

适用扩展：[extend Int16 <: Integer<Int16>](extensions/extend-int16-integer-int16.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 Int16 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `true`。

## static func isSigned()

适用扩展：[extend Int32 <: Integer<Int32>](extensions/extend-int32-integer-int32.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 Int32 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `true`。

## static func isSigned()

适用扩展：[extend Int64 <: Integer<Int64>](extensions/extend-int64-integer-int64.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 Int64 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `true`。

## static func isSigned()

适用扩展：[extend Int8 <: Integer<Int8>](extensions/extend-int8-integer-int8.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 Int8 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `true`。

## static func isSigned()

适用扩展：[extend IntNative <: Integer<IntNative>](extensions/extend-intnative-integer-intnative.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 IntNative 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `true`。

## static func isSigned()

适用扩展：[extend UInt16 <: Integer<UInt16>](extensions/extend-uint16-integer-uint16.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 UInt16 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `false`。

## static func isSigned()

适用扩展：[extend UInt32 <: Integer<UInt32>](extensions/extend-uint32-integer-uint32.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 UInt32 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `false`。

## static func isSigned()

适用扩展：[extend UInt64 <: Integer<UInt64>](extensions/extend-uint64-integer-uint64.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 UInt64 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `false`。

## static func isSigned()

适用扩展：[extend UInt8 <: Integer<UInt8>](extensions/extend-uint8-integer-uint8.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 UInt8 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `false`。

## static func isSigned()

适用扩展：[extend UIntNative <: Integer<UIntNative>](extensions/extend-uintnative-integer-uintnative.md)。

### 签名

```cangjie role=signature
public static func isSigned(): Bool
```

判断 UIntNative 类型是否是有符号类型。

### 契约

返回值：

- Bool - 总是返回 `false`。
