# interface_ThrowingOpT

This directory provides comprehensive API documentation for the `ThrowingOp` interface in the Cangjie programming language. It focuses on arithmetic and bitwise operations that utilize exception-throwing strategies to handle errors like overflow and invalid shifts. The content covers the core interface definition and its implementations across various integer types, including `Int8`, `Int64`, `UInt32`, and `UIntNative`. By throwing specific exceptions such as `OverflowException` and `OvershiftException`, these tools ensure data integrity and prevent silent data corruption. This resource is essential for Cangjie developers building high-reliability systems requiring strict error handling and explicit validation in numerical computations.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `ThrowingOp` interface definition and its methods → interface_ThrowingOpT_9more.md
*   Review specific exception-handling functions (e.g., `throwingShr`, `throwingSub`) → func_throwingShrUInt64_2more.md
*   Explore implementations for specific integer types?
    *   Signed Integers (Int8, Int16, Int32, Int64, IntNative) → [3], [4], [5], [6], [7]
    *   Unsigned Integers (UInt8, UInt16, UInt32, UInt64, UIntNative) → [8], [9], [10], [11], [12]

## Detailed Description

### func_throwingShrUInt64_2more.md func_throwingShrUInt64_2more.md
This API reference details two exception-handling arithmetic functions in the Cangjie language: `throwingShr` for right-shifting `UInt64` values and `throwingSub` for generic subtraction. Both functions utilize a throwing strategy to handle errors, throwing `OvershiftException` if the shift amount exceeds the bit width, or `OverflowException` upon arithmetic overflow. The documentation specifies parameters, return types, and specific exception classes, targeting developers who need to implement safe arithmetic operations with explicit error handling.

### interface_ThrowingOpT_9more.md interface_ThrowingOpT_9more.md
This document defines the `ThrowingOp` interface in the Cangjie language, providing arithmetic and bitwise operations that throw exceptions upon overflow. It details methods such as `throwingAdd`, `throwingSub`, `throwingMul`, and shift operations. Key information includes that arithmetic functions throw `OverflowException` when results exceed the representable range, while shift operations throw `OvershiftException` for invalid shift amounts. This guide targets developers who prefer error detection via exceptions over silent wrapping or saturation.

### [3] extend_Int16_ThrowingOpInt16/
This directory details the `ThrowingOp` interface implementation for the `Int16` type. It focuses on arithmetic and bitwise functions that employ an exception-handling strategy to manage errors robustly. Unlike standard operations that may wrap results, functions documented here throw `OverflowException` or `OvershiftException` when operational limits are exceeded. This is crucial for developers requiring strict validation in numerical computations to prevent silent data corruption in critical application paths.

### [4] extend_Int32_ThrowingOpInt32/
This directory focuses on the implementation of the `ThrowingOp<Int32>` interface for the `Int32` type. It provides API documentation on arithmetic and bitwise operations that prioritize safety through rigorous exception-throwing strategies. Covered operations include addition, multiplication, division, modulo, negation, and bit shifting. Each function throws specific exceptions like `OverflowException` or `OvershiftException` when boundaries are exceeded, ideal for systems where strict error handling is paramount.

### [5] extend_Int64_ThrowingOpInt64_ThrowingPow/
This directory provides API reference documentation for extending the `Int64` type, focusing on the `ThrowingOp` and `ThrowingPow` interfaces. These interfaces handle arithmetic operations through a rigorous exception-throwing mechanism, throwing `OverflowException` or `OvershiftException` to ensure data integrity. The documentation covers basic arithmetic, negation, power calculations, and bitwise shifts, essential for developers prioritizing robust error handling in high-stakes numerical computations.

### [6] extend_Int8_ThrowingOpInt8/
This directory details the implementation of the `ThrowingOp` interface for the `Int8` type. It focuses on extension functions designed for safe arithmetic and bit manipulation operations. The core functionality revolves around an exception-throwing strategy, ensuring that errors such as arithmetic overflows or invalid shift amounts are explicitly signaled via exceptions. This resource is essential for developers requiring robust error handling to prevent silent data corruption in integer calculations.

### [7] extend_IntNative_ThrowingOpIntNative/
This directory provides API documentation for the `ThrowingOp` interface implementation for the `IntNative` type. It covers arithmetic and bitwise operations designed to prioritize safety by throwing exceptions—such as `OverflowException` and `OvershiftException`—upon encountering errors. Key topics include addition, multiplication, division, negation, and bit shifting. It is a critical reference for building secure, reliable systems where data integrity is paramount, preventing common bugs associated with integer overflows.

### [8] extend_UInt16_ThrowingOpUInt16/
This directory contains API documentation for extending the `UInt16` type with the `ThrowingOp` interface. It focuses on arithmetic and bitwise operations that utilize strict exception handling to prevent silent data corruption. Instead of standard behavior, these functions throw specific exceptions like `OverflowException` or `OvershiftException` when limits are breached. This is essential for developers requiring robust error handling in 16-bit unsigned integer calculations and high-integrity applications.

### [9] extend_UInt32_ThrowingOpUInt32/
This directory focuses on the `ThrowingOp` interface implementation for the `UInt32` type. It provides details on arithmetic and bitwise operations that utilize an exception-throwing strategy to handle errors. Instead of wrapping around on overflow or invalid shifts, these functions throw specific exceptions like `OverflowException` and `OvershiftException`. This approach ensures robust error handling and prevents silent data corruption, suitable for developers implementing strict error management in safe integer operations.

### [10] extend_UInt64_ThrowingOpUInt64/
This directory details the implementation of the `ThrowingOp` interface for the `UInt64` data type. It focuses on arithmetic and bitwise operations designed to enhance computational safety by throwing exceptions rather than defaulting to wrapping behavior. The documentation covers functions like addition, multiplication, bitwise shifts, and negation, utilizing `OverflowException` and `OvershiftException` for explicit error handling. It is a critical resource for developers requiring precise control over numerical edge cases in high-reliability systems.

### [11] extend_UInt8_ThrowingOpUInt8/
This directory contains API documentation for the `ThrowingOp` interface implementation for the `UInt8` type. It covers arithmetic and bitwise operations that utilize exception-handling strategies to manage errors such as overflow and overshift. Key topics include functions like `throwingAdd`, `throwingNeg`, `throwingShl`, and `throwingSub`, which are designed to throw specific exceptions rather than wrapping around. This documentation is suitable for developers requiring safe, checked numeric operations in low-level programming tasks.

### [12] extend_UIntNative_ThrowingOpUIntNative/
This directory details the extension of the `UIntNative` type through the `ThrowingOp` interface. It covers arithmetic and bitwise operations—including `throwingAdd`, `throwingMul`, `throwingShl`, and `throwingSub`—that prioritize safety by utilizing an exception-throwing strategy. Instead of wrapping around on errors, these functions throw specific exceptions like `OverflowException` and `OvershiftException`. This resource is essential for developers who need to implement robust error handling and explicit arithmetic checks to prevent silent data corruption.