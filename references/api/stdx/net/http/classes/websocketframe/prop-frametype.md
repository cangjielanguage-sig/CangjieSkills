<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.class.websocketframe.prop-frametype" parent="stdx.net.http.class.websocketframe" -->
# WebSocketFrame.frameType

[← WebSocketFrame](index.md)

## 签名

```cangjie role=signature
public prop frameType: WebSocketFrameType
```

获取 WebSocketFrame 的帧类型，fin 与 frameType 共同说明了帧是否分段和帧的类型。

## 契约

类型：WebSocketFrameType
