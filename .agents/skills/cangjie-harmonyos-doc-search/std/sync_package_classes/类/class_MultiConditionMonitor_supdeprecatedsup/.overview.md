# class_MultiConditionMonitor_supdeprecatedsup

This directory provides comprehensive API documentation for the deprecated `MultiConditionMonitor` class and its related synchronization functions in the Cangjie programming language. It details advanced concurrency control patterns, specifically focusing on the architecture that allows multiple condition variables to be bound to a single reentrant mutex. The content encompasses class definitions, inheritance hierarchies, and essential methods for thread management, such as waiting, notifying, and handling timeouts. Critical topics include the use of ConditionID instances for identifying wait queues and managing IllegalSynchronizationStateException errors. This documentation is tailored for experienced developers who are maintaining complex legacy synchronization code or seeking to understand the underlying mechanics before migrating to the recommended `Mutex` standard.

## Quick Navigation

**What do you want to learn?**

*   Review the deprecated class definition, inheritance, and core methods like `newCondition` and `notify` → class_MultiConditionMonito_5more.md
*   Investigate the `wait` function signature, parameters, and exception handling for thread suspension → func_waitConditionID_Duration.md

## Detailed Description

### class_MultiConditionMonito_5more.md class_MultiConditionMonito_5more.md
This API reference documents the deprecated MultiConditionMonitor class, which enables binding multiple condition variables to a single reentrant mutex for advanced concurrency control. The content covers the class definition, its inheritance from ReentrantMutex, and core methods including the constructor, newCondition, notify, and notifyAll. Key takeaways include the deprecation warning advising migration to Mutex and the requirement that threads must hold the mutex to perform operations, otherwise risking an IllegalSynchronizationStateException. The document explains how ConditionID instances uniquely identify specific wait queues. This technical reference targets experienced developers implementing complex synchronization algorithms where standard monitors are insufficient.

### func_waitConditionID_Duration.md func_waitConditionID_Duration.md
This document provides API reference details for the wait function in the Cangjie sync package, designed to suspend a thread until a notification occurs or a timeout elapses. The content covers the function signature, functionality, parameters, return values, and potential exceptions. Key information includes the automatic release and re-acquisition of the mutex during the wait process, the default infinite timeout setting, and specific error conditions such as illegal synchronization states or invalid arguments. This reference guide targets Cangjie developers implementing thread synchronization and concurrency control, assuming prerequisite knowledge of mutexes and condition variables.