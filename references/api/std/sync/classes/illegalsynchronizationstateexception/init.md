<!-- cj-doc kind="api-member" level="6" id="std.sync.class.illegalsynchronizationstateexception.init" parent="std.sync.class.illegalsynchronizationstateexception" -->
# IllegalSynchronizationStateException.init

[← IllegalSynchronizationStateException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建一个 IllegalSynchronizationStateException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

创建一个 IllegalSynchronizationStateException 实例，其信息由参数 `message` 指定。

### 契约

参数：

- message: String - 预定义消息。
