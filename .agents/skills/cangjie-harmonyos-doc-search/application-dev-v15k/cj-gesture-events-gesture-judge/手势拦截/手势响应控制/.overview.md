# 手势响应控制

This directory focuses on managing and resolving gesture conflicts in complex UI scenarios, particularly nested scrolling. It covers technical implementations in Cangjie and ArkTS (HarmonyOS), detailing how to use APIs like `shouldBuiltInRecognizerParallelWith`, `onGestureRecognizerJudgeBegin`, and `parallelGesture` to control gesture responses dynamically. The content includes code examples for coordinating parent and child scroll components, adjusting recognizer states based on scroll position, and setting up parallel gestures. It is suitable for developers working on advanced user interfaces requiring precise interaction logic and smooth scrolling transitions.

## Quick Navigation

**What do you want to learn?**

*   Understand core concepts and Cangjie APIs for gesture response → 手势响应控制_1.md
*   Resolve nested scrolling conflicts dynamically in Cangjie → 手势响应控制_2.md
*   Implement parallel gesture execution in ArkTS → 手势响应控制_3.md
*   Use dynamic controllers for gesture conflicts in ArkTS → 手势响应控制_4.md
*   Learn logic for toggling recognizer enabled states → 手势响应控制_5.md
*   Configure layout and styling for gesture targets → 手势响应控制_6.md

## Detailed Description

### 手势响应控制_1.md 手势响应控制_1.md
This technical guide explains how to manage gesture response callbacks dynamically after successful recognition using specific APIs. It covers core concepts including business and monitoring workflows, parallel gesture setup, and dynamic enable/disable mechanisms. The document details key interfaces like shouldBuiltInRecognizerParallelWith and onGestureRecognizerJudgeBegin, providing a Cangjie code example for nested scrolling scenarios to coordinate outer and inner Scroll components. It targets developers working on complex UI interactions, specifically those needing to handle nested scrolling or dynamic gesture states.

### 手势响应控制_2.md 手势响应控制_2.md
This technical guide demonstrates how to resolve gesture conflicts in nested scrolling components using Cangjie's gesture recognition system. It covers implementing onGestureRecognizerJudgeBegin for initial conflict resolution and using parallelGesture with onActionUpdate for dynamic control during interaction. The code dynamically enables or disables childRecognizer and currentRecognizer based on the scroll position (isBegin, isEnd) and pan direction (offsetY), ensuring smooth scrolling transitions between parent and child components. This is intended for Cangjie developers needing to implement complex gesture handling logic in UI layouts.

### 手势响应控制_3.md 手势响应控制_3.md
This document provides a code example demonstrating advanced gesture response control for nested scrollable components in ArkTS. It covers the implementation of the shouldBuiltInRecognizerParallelWith method to handle gesture conflicts between an outer and inner scroll container. Key information includes the logic to identify built-in Pan gestures targeting the inner scroll component, allowing them to execute in parallel with the parent container's gestures. This snippet is intended for developers resolving complex scrolling interaction issues in HarmonyOS applications, specifically focusing on gesture recognizer arbitration and parallel execution logic.

### 手势响应控制_4.md 手势响应控制_4.md
This document provides a code example for resolving gesture conflicts in nested scrolling scenarios using ArkTS. It covers the implementation of a dynamic controller that adjusts the enabled state of gesture recognizers based on scroll position and movement direction. Key logic involves checking if the target is "outer" and the inner target is "inner," then toggling recognizers depending on whether the scroll is at the beginning or end and the vertical offset. This ensures smooth interaction between parent and child scrollable components. It targets developers building complex user interfaces requiring precise gesture control.

### 手势响应控制_5.md 手势响应控制_5.md
This document provides a code snippet illustrating how to control gesture response conflicts between parent and child scrollable components. It focuses on logic for checking recognizer states and scroll boundaries. Key takeaways include the dynamic toggling of recognizer enabled states using setEnabled based on scroll position (isEnd, isBegin) and direction (offsetY). The code ensures smooth interaction by transferring control between components when scrolling reaches limits. This technical reference is intended for developers implementing nested scrolling or parallel gestures in UI frameworks like ArkUI.

### 手势响应控制_6.md 手势响应控制_6.md
This document serves as a technical guide or tutorial segment focusing on gesture response control within a UI development framework, likely HarmonyOS ArkUI. It covers layout configuration and component styling, specifically setting dimensions and background colors, presumably within the context of implementing or testing gesture interactions. The provided snippet demonstrates the use of percentage-based height and hexadecimal color definitions to style a container component that likely serves as a gesture target. The target audience includes mobile application developers with prior knowledge of declarative UI syntax and event handling mechanisms.