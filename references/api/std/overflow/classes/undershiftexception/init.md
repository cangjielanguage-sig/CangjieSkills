<!-- cj-doc kind="api-member" level="6" id="std.overflow.class.undershiftexception.init" parent="std.overflow.class.undershiftexception" -->
# UndershiftException.init

[← UndershiftException](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建 UndershiftException 实例。

## init(String)

### 签名

```cangjie role=signature
public init(message: String)
```

创建带有异常信息 message 的 UndershiftException 实例。

### 契约

参数：

- message: String - 异常信息。
