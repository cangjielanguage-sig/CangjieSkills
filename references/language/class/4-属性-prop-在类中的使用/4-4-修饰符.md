<!-- cj-doc kind="guide-leaf" level="5" id="language.class.4-属性-prop-在类中的使用.4-4-修饰符" parent="language.class.4-属性-prop-在类中的使用" -->
# 4.4 修饰符

[← 4. 属性（prop）在类中的使用](index.md)

- 与成员函数相同的访问修饰符（`public`、`private`、`protected`、`internal`）
- 支持 `open`、`override`、`redef` — 允许在子类中重写/重定义
- 重写时若父类属性有 `mut`，子类也须有 `mut`，类型相同
