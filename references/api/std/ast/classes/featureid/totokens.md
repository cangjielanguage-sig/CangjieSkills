<!-- cj-doc kind="api-member" level="6" id="std.ast.class.featureid.totokens" parent="std.ast.class.featureid" -->
# FeatureId.toTokens

[← FeatureId](index.md)

## 签名

```cangjie role=signature
public func toTokens(): Tokens
```

转换当前的抽象语法树节点为 Tokens 类型。

## 返回值

- Tokens - 转换出来的 Tokens 对象。

