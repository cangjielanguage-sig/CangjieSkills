<!-- cj-doc kind="api-member" level="6" id="std.sync.class.atomicuint64.init" parent="std.sync.class.atomicuint64" -->
# AtomicUInt64.init

[← AtomicUInt64](index.md)

## 签名

```cangjie role=signature
public init(val: UInt64)
```

构造一个封装 UInt64 数据类型的原子类型 AtomicUInt64 的实例，其内部数据初始值为入参 `val` 的值。

## 契约

参数：

- val: UInt64 - 原子类型的初始值。
