# cj-universal-event-touch

This directory serves as a comprehensive resource for handling universal touch events within the HarmonyOS ArkUI framework, specifically implemented using the Cangjie programming language. It provides in-depth coverage of touch interaction implementation, ranging from basic event callbacks to complex state management using the `@State` decorator. Developers will find detailed guidance on retrieving essential touch data, including precise coordinates and timestamps, to enhance user experience. The repository includes practical code examples that demonstrate UI layout construction using fundamental components like Column and Button. Furthermore, it offers API references for the `onTouch` function introduced in system version 12 and defines core data structures such as `TouchEvent` and `TouchObject`. This material is highly suitable for HarmonyOS developers aiming to build interactive, responsive user interfaces and gain a thorough understanding of the underlying event propagation logic. By leveraging these resources, developers can ensure their applications respond intuitively to user gestures.

## Quick Navigation

**What do you want to learn?**

- Implement touch event handling in HarmonyOS ArkUI with Cangjie → **触摸事件/**
- Learn how to use the `onTouch` API (API 12) and callbacks → **触摸事件/**
- Manage UI state with `@State` during touch interactions → **触摸事件/**
- Retrieve and utilize touch data (coordinates, timestamps) → **触摸事件/**
- Study code examples using Column and Button components → **触摸事件/**
- Understand `TouchEvent` and `TouchObject` data structures → **触摸事件/**
- Explore event propagation logic for responsive UIs → **触摸事件/**

## Detailed Description

### 触摸事件/
This directory focuses on touch event handling within the HarmonyOS ArkUI framework using the Cangjie programming language. It covers the implementation of touch interactions, including event callbacks, state management with `@State`, and data retrieval such as coordinates and timestamps. The content includes practical code examples demonstrating UI layout construction with Column and Button components, as well as API references for the `onTouch` function supported from system version 12. Additionally, it provides definitions for core data structures like `TouchEvent` and `TouchObject`. It is suitable for HarmonyOS developers looking to build interactive and responsive user interfaces and understand the underlying event propagation logic.