# cj-state-management-overview

This directory serves as a comprehensive resource for understanding state management within the Cangjie declarative UI framework. It details the fundamental mechanisms where state changes drive UI rendering, clearly distinguishing between state variables and regular variables. The content covers essential component-level decorators such as @State, @Prop, and @Link, alongside application-level solutions like AppStorage and LocalStorage. Furthermore, it addresses data flow patterns, including unidirectional and bidirectional flows, and nested object observation. This guide is ideal for developers building dynamic applications who require a deep understanding of data synchronization between components, pages, and global states to ensure efficient and responsive UI updates.

## Quick Navigation

**What do you want to learn?**

*   Understand the core principles of state management and UI rendering → 状态管理概述/
*   Learn about component-level decorators (@State, @Prop, @Link) → 状态管理概述/
*   Explore application-level storage solutions (AppStorage, LocalStorage) → 状态管理概述/
*   Understand data flow patterns and nested object observation → 状态管理概述/

## Detailed Description

### 状态管理概述/
This directory provides a comprehensive overview of state management mechanisms within the Cangjie declarative UI framework. It covers the fundamental principles of how state changes drive UI rendering, distinguishing between state variables and regular variables. Key topics include component-level decorators like @State, @Prop, and @Link, as well as application-level solutions such as AppStorage and LocalStorage. It also addresses data flow patterns (unidirectional vs. bidirectional) and nested object observation. This resource is suitable for developers building dynamic applications who need to understand data synchronization between components, pages, and global states to ensure efficient UI updates.