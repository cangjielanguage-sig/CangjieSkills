<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-else" parent="std.ast.enum.tokenkind" -->
# TokenKind.ELSE

[← TokenKind](index.md)

## 签名

```cangjie role=signature
ELSE
```

构造一个表示 `else` 的枚举实例。
