# hot_update_cert

This directory contains a technical demonstration focused on the critical infrastructure task of performing hot updates for TLS certificates using the Cangjie programming language. In modern network security, the ability to rotate certificates without service interruption is essential for maintaining compliance and operational security. This directory provides a practical guide on how to achieve this by managing a `TlsServerConfig` object within a custom server implementation. It specifically addresses the challenge of applying new cryptographic credentials to incoming traffic while preserving the integrity and stability of ongoing sessions. The content is designed for developers and system engineers who are building secure, high-availability network servers in Cangjie. It covers essential topics such as socket programming, the lifecycle of TLS connections, and the specific syntax required for mutable configuration properties. By studying this example, users will learn how to implement a robust `MyServer` class that handles certificate updates seamlessly, ensuring that their applications remain secure and operational during maintenance windows. Keywords include Cangjie, TLS, hot update, certificate, TlsServerConfig, and socket programming.

## Quick Navigation

What do you want to learn or do?

*   **Implement dynamic certificate rotation** → hot_update_cert.md
    *   Learn how to modify server configurations on the fly to update security credentials.
*   **Understand connection handling logic** → hot_update_cert.md
    *   See how new connections receive updated certificates while old ones persist without interruption.
*   **Study Cangjie server code structure** → hot_update_cert.md
    *   Review the `MyServer` class, mutable properties, and the `onAcceptedConnection` method implementation.

## Detailed Description

### hot_update_cert.md hot_update_cert.md

This document serves as a comprehensive code example demonstrating how to perform hot updates for TLS certificates in the Cangjie programming language. It focuses on a `MyServer` class implementation that manages a `TlsServerConfig` object. Key sections include the definition of a mutable `certificate` property and the `onAcceptedConnection` method which applies the configuration to new `TlsSocket` instances. The primary takeaway is that modifying the certificate configuration only affects new connections, ensuring existing sessions remain stable. This technical example targets developers building secure network servers who require seamless certificate rotation. Keywords: Cangjie, TLS, hot update, certificate, TlsServerConfig, socket programming.

The example effectively demonstrates the separation of concerns between the server's global state and individual connection states. By utilizing a mutable property for the certificate, the system allows for runtime updates. The `onAcceptedConnection` method acts as the critical integration point, ensuring that every new socket handshake utilizes the latest credentials. This pattern is vital for zero-downtime deployments.