# cj-crypto-aes-sym-encrypt-decrypt-ccm

This directory serves as a specialized technical repository for implementing AES symmetric encryption and decryption using the CCM (Counter with CBC-MAC) mode within the Cangjie programming language ecosystem. It provides a deep dive into the cryptographic lifecycle, covering essential processes such as generating 128-bit symmetric keys, configuring Cipher instances, and managing CcmParamsSpec. The content emphasizes the nuances of authenticated encryption, including the critical requirement for extracting a 12-byte authentication tag and the mode's restriction against segmented operations. This collection is specifically tailored for Cangjie developers who utilize the CryptoArchitectureKit and require precise, code-level guidance to secure data integrity and confidentiality in their applications. It is particularly suitable for those building secure communication systems or data storage solutions where both privacy and authenticity are paramount, offering a clear path through the complexities of modern cryptographic standards and the specific requirements of the Cangjie runtime environment.

## Quick Navigation

- **What do you want to learn?**
    - Implement AES CCM encryption/decryption in Cangjie → `使用AES对称密钥CCM模式加解密仓颉/`

## Detailed Description

### 使用AES对称密钥CCM模式加解密仓颉/
The following subdirectory contains the core implementation details and examples necessary for mastering these cryptographic techniques.

This directory provides a comprehensive technical collection focused on implementing AES symmetric encryption and decryption using the CCM (Counter with CBC-MAC) mode within the Cangjie programming language. It covers the entire cryptographic lifecycle, starting from the fundamental step of generating 128-bit symmetric keys to the advanced configuration of Cipher instances using specific parameters like CcmParamsSpec. The documentation is designed to be a definitive resource for understanding how these components interact within the Cangjie framework.

The documentation highlights critical operational constraints of CCM mode that developers must navigate carefully. For instance, it explains the lack of support for segmented operations, which requires data to be processed in distinct blocks rather than continuous streams, and the mandatory extraction of a 12-byte authentication tag for verification. These details are crucial for preventing implementation errors that could compromise security, ensuring that developers are aware of the strict boundaries of the CCM mode.

These resources are specifically designed for Cangjie developers who possess a working knowledge of the CryptoArchitectureKit and need precise, code-level instructions for securing data through authenticated encryption mechanisms. By following these guides, developers can effectively manage cryptographic keys and handle the nuances of authenticated encryption in their applications. This ensures that their software implementations are not only functional but also adhere to best practices for data security and integrity in the Cangjie ecosystem.