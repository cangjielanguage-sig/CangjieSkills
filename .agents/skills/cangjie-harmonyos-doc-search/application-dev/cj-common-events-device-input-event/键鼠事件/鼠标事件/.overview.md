# 鼠标事件

This directory provides comprehensive documentation on handling mouse events within the Cangjie ArkUI framework. It is designed to help developers understand and implement interactive features by detailing the specific APIs used to capture mouse inputs. The content covers the `onMouse` and `onHover` event interfaces, explaining how to track coordinates, manage button states, and handle event bubbling. Additionally, it addresses the logic for converting left-clicks to touch events and distinguishing between different input sources. This resource is essential for ArkUI developers using the Cangjie language who aim to build responsive UI components that support both mouse and touchpad interactions effectively.

## Quick Navigation

What do you want to learn?

- Understand the basic `onMouse` API reference and coordinate tracking → onMouse.md
- Learn about `onHover` events and touch event conversion logic → 鼠标事件_2more.md

## Detailed Description

### onMouse.md onMouse.md
This document provides API reference documentation for the onMouse event callback function in the Cangjie ArkUI framework. It covers the function signature, the properties of the MouseEvent object (such as coordinates, buttons, and actions), and event bubbling behavior. Key information includes how to capture mouse interactions like clicks and movements, with specific details on button states and timestamps. The document includes a runnable code example demonstrating coordinate tracking on different components. Intended for ArkUI developers using Cangjie, this guide assists in implementing custom mouse interaction logic.

### 鼠标事件_2more.md 鼠标事件_2more.md
This document provides technical specifications and usage guidelines for handling mouse events within the ArkUI framework. It details the onHover and onMouse event interfaces, explains the event processing logic where left-clicks are converted to touch events, and includes a code example demonstrating the onHover callback. Key information includes that onHover detects mouse entry and exit with a boolean parameter, while onMouse captures clicks and movements. It clarifies that mouse left-clicks can trigger touch event callbacks, and developers can distinguish input sources using the source field. This guide targets application developers using the Cangjie language and ArkUI framework to implement interactive UI components supporting mouse and touchpad inputs.