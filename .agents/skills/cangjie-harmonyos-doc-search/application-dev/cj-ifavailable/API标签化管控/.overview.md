# API标签化管控

This directory focuses on API tagging control mechanisms within the Cangjie programming language, specifically utilizing the `@IfAvailable` macro to manage conditional compilation. It covers how to handle API availability based on global configurations, including version restrictions (`level`) and device capability intersections (`syscap`). The content details syntax, semantics, and enforcement rules for conditional execution, including the execution of conditional lambda expressions and marking symbols as weak. It is suitable for Cangjie developers using DevEco Studio who need to ensure strict code compatibility across different project versions and diverse hardware capabilities.

## Quick Navigation

**What do you want to learn?**
*   **Understand the core theory:** Learn the technical principles, syntax, and semantics of the `@IfAvailable` macro, including how it handles level checks and device capability intersections → API标签化管控_4more.md
*   **Apply the knowledge:** Explore practical code examples demonstrating how to control `APILevel` and `syscap` for conditional compilation and execution → IfAvailable_使用示例.md

## Detailed Description

### API标签化管控_4more.md API标签化管控_4more.md
This technical guide explains API tagging control in the Cangjie language using the `@IfAvailable` macro to manage API availability based on global configurations. It covers the syntax and semantics of `@IfAvailable`, specifically detailing `level` checks for API version restrictions and `syscap` checks for device capability intersections and unions. Key takeaways include the macro's ability to execute conditional lambda expressions, marking symbols as weak, and enforcing strict rules against calling higher-level APIs in lower-level scopes. The document outlines specific error and warning behaviors for device capability compatibility. Intended for Cangjie developers, this guide ensures API compatibility across different project versions and device hardware capabilities.

### IfAvailable_使用示例.md IfAvailable_使用示例.md
This document provides usage examples for the `@IfAvailable` annotation in the Cangjie programming language to handle conditional compilation and execution based on system features. It covers two primary use cases: controlling `APILevel` to manage version-specific APIs and controlling `syscap` to handle system capabilities. The examples demonstrate how `@IfAvailable` enforces compile-time scope restrictions and runtime branch selection. It details syntax like `level: xx` and `syscap: "SystemCapability.xx"`, explaining how the compiler validates API availability within specific blocks. This guide targets Cangjie developers using DevEco Studio who need to ensure compatibility across different API levels and device capabilities.