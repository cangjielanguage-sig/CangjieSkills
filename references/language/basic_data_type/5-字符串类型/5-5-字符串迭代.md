<!-- cj-doc kind="guide-leaf" level="5" id="language.basic_data_type.5-字符串类型.5-5-字符串迭代" parent="language.basic_data_type.5-字符串类型" -->
# 5.5 字符串迭代

[← 5. 字符串类型](index.md)

- **`for (c in s)` 迭代的是字节（`UInt8`/`Byte`）**，因为 `String` 实现了 `Collection<Byte>`（UTF-8 编码字节序列）
- **`for (c in s.runes())` 迭代的是字符（`Rune`）**，返回 `Iterator<Rune>`
- 这与大多数编程语言的行为不同，需要特别注意：
  ```cangjie cjtest=syntax id=syntax-76825d04fb-1 form=stmt
  // c 是 UInt8 类型（字节），"你好" 的 UTF-8 编码为 6 个字节
  for (c in "你好") { println(c) }    // 输出 6 个字节值

  // c 是 Rune 类型（Unicode 字符）
  for (c in "你好".runes()) { println(c) }  // 输出: 你  好
  ```
- `toRuneArray()` 方法可将字符串转换为 `Array<Rune>`

---
