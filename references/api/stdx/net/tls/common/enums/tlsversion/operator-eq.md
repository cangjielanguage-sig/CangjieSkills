<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.common.enum.tlsversion.operator-eq" parent="stdx.net.tls.common.enum.tlsversion" -->
# TlsVersion.==

[← TlsVersion](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: TlsVersion): Bool
```

比较两个 TlsVersion 是否相同。

## 参数

- that: TlsVersion - 待比较的 TLS 版本。

## 返回值

- Bool - 相同时返回 `true`，否则返回 `false`。

