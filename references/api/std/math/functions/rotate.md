<!-- cj-doc kind="api-member" level="5" id="std.math.func.rotate" parent="std.math" -->
# rotate

[← std.math](../index.md)

本页汇总 8 个同名重载；先按签名选择，再读取对应契约。

## rotate(Int16, Int8)

### 签名

```cangjie role=signature
public func rotate(num: Int16, d: Int8): Int16
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: Int16 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- Int16 - 返回旋转后的整数。

## rotate(Int32, Int8)

### 签名

```cangjie role=signature
public func rotate(num: Int32, d: Int8): Int32
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: Int32 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- Int32 - 返回旋转后的整数。

## rotate(Int64, Int8)

### 签名

```cangjie role=signature
public func rotate(num: Int64, d: Int8): Int64
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: Int64 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- Int64 - 返回旋转后的整数。

## rotate(Int8, Int8)

### 签名

```cangjie role=signature
public func rotate(num: Int8, d: Int8): Int8
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: Int8 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- Int8 - 返回旋转后的整数。

## rotate(UInt16, Int8)

### 签名

```cangjie role=signature
public func rotate(num: UInt16, d: Int8): UInt16
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: UInt16 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- UInt16 - 返回旋转后的整数。

## rotate(UInt32, Int8)

### 签名

```cangjie role=signature
public func rotate(num: UInt32, d: Int8): UInt32
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: UInt32 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- UInt32 - 返回旋转后的整数。

## rotate(UInt64, Int8)

### 签名

```cangjie role=signature
public func rotate(num: UInt64, d: Int8): UInt64
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: UInt64 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- UInt64 - 返回旋转后的整数。

## rotate(UInt8, Int8)

### 签名

```cangjie role=signature
public func rotate(num: UInt8, d: Int8): UInt8
```

求整数的按位旋转后的结果。

### 契约

参数：

- num: UInt8 - 传入一个整数。
- d: Int8 - 旋转位数，负数右移，正数左移。

返回值：

- UInt8 - 返回旋转后的整数。
