# 接口

This directory contains API reference documentation for core interfaces in the Cangjie programming language, primarily focusing on property-based testing and random data generation. It covers essential interfaces such as `Generator`, `RandomSource`, `Arbitrary`, `ArbitraryRange`, and `Shrink`. These components enable developers to create random values, access data structures, and reduce test cases to minimal failures. The documentation is suitable for Cangjie developers, QA engineers, and software testers working with the `unittest_prop_test` package who need to implement robust testing logic, fuzzing strategies, or custom randomization algorithms.

## Quick Navigation

**What do you want to learn?**

*   Understand core generation and access utilities → interface_GeneratorT_2more.md
*   Generate random values within specific numerical ranges → [2]
*   Implement property-based testing (Arbitrary interface) → [3]
*   Generate pseudo-random numbers (integers, floats, Gaussian) → [4]
*   Reduce test cases to minimal failures (Shrinking) → [5]

## Detailed Description

### interface_GeneratorT_2more.md interface_GeneratorT_2more.md
This API reference document defines two utility interfaces in the Cangjie programming language: Generator and IndexAccess. The text covers the Generator interface, designed for producing values of type T, and the IndexAccess interface, which facilitates accessing tuple elements by index. Key information includes the next() function, which returns the next generated value, and getElementAsAny(Int64), which retrieves an element as an optional Any type, returning None if the index is invalid. This documentation is intended for Cangjie developers implementing generation logic or requiring indexed access to tuple data structures.

### [2] interface_ArbitraryRangeT/
This directory contains API documentation for the `ArbitraryRange` interface in the Cangjie programming language, specifically designed for property-based testing frameworks. It covers the generation of random values within specified numerical ranges. The content details the core interface definition and its implementations across various numeric types, including signed and unsigned integers (Int8, Int16, Int32, Int64, IntNative, UInt8, UInt16, UInt32, UInt64, UIntNative) and floating-point numbers (Float16, Float32, Float64). This documentation is suitable for developers implementing testing logic who need to create constrained random data generators using `RandomSource` and `Generator` types.

### [3] interface_ArbitraryT/
This directory contains API documentation for the `Arbitrary` interface within the Cangjie property-based testing framework. It details how various data types implement this interface to enable automatic random value generation for testing. The content covers fundamental numeric types, strings, collections, and core data structures. Key concepts include the `RandomSource` parameter, `Generator` return types, and interface extensions. This documentation is suitable for software developers and QA engineers working with the `unittest_prop_test` package who need to generate random test data or implement custom property-based tests in Cangjie.

### [4] interface_RandomSource/
This directory contains API reference documentation for the `RandomSource` interface in the Cangjie programming language. It covers the generation of pseudo-random numbers across various data types, including signed and unsigned integers, floating-point numbers, booleans, and runes. The documentation details methods for generating values within default ranges, specific bounds, and Gaussian distributions. It is suitable for Cangjie developers implementing random number generators, property-based testing frameworks, or applications requiring robust randomization logic. Key topics include `next` functions, `suggest` functions, and interface implementation details.

### [5] interface_ShrinkT/
This directory contains API documentation for the `Shrink` interface in the Cangjie programming language. It focuses on implementations that allow data types to generate smaller versions of themselves, a crucial capability for property-based testing and fuzzing. The documentation covers the core generic interface definition and its extensions across various data types, including primitive integers, floating-point numbers, strings, and complex collections like HashMaps and Arrays. It is suitable for Cangjie developers looking to implement data reduction strategies to isolate minimal failure cases in their test suites.