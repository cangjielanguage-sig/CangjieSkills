# BuilderParam宏引用Builder函数

This directory provides comprehensive documentation on the `@BuilderParam` macro within the ArkUI framework and the Cangjie language environment. It details the mechanism for dynamically injecting specific UI descriptions or behaviors into custom component instances, effectively functioning as slot placeholders. The content covers essential concepts such as initialization via global or local `@Builder` functions, strict type consistency requirements, and specific usage limitations. Additionally, it addresses common compilation errors related to state management. This resource is designed for developers familiar with `@Builder` decorators who aim to achieve flexible component customization and dynamic UI composition in their ArkUI applications.

## Quick Navigation

What do you want to learn?

*   Understand the core definition, function, and strict limitations of the @BuilderParam macro → BuilderParam宏引用Builder函数_3more.md
*   Learn how to initialize components and apply @BuilderParam in practical usage scenarios → 使用场景.md
*   Troubleshoot common compilation errors involving @State variables and initialization rules → 常见问题.md

## Detailed Description

### BuilderParam宏引用Builder函数_3more.md BuilderParam宏引用Builder函数_3more.md
This technical guide explains the @BuilderParam macro in ArkUI, designed to allow developers to dynamically inject specific UI descriptions or behaviors into custom component instances without affecting all instances. The document details the macro's function as a slot placeholder, demonstrates initialization using global or local @Builder functions, and outlines strict usage limitations. Key takeaways include requirements that @BuilderParam variables must be explicitly typed functions returning Unit, initialized solely by @Builder methods, and restricted to class member variables with private visibility. Intended for developers familiar with @Builder decorators, this reference facilitates flexible component customization in the Cangjie language environment.

### 使用场景.md 使用场景.md
This technical guide illustrates the usage scenarios of the @BuilderParam decorator within the ArkUI Cangjie framework. It details two primary methods: initializing components with parameters, where types must match, and initializing using global or parent component @Builder methods. Key takeaways include the requirement for type consistency between @BuilderParam and the referenced @Builder, and the mechanism for passing UI content from parent to child components. The document serves as a practical tutorial for developers aiming to achieve dynamic UI composition and component reusability in ArkUI applications.

### 常见问题.md 常见问题.md
This document addresses a common compilation error encountered when using the @BuilderParam macro in Cangjie ArkUI development. It covers the initialization rules for @BuilderParam variables within child components, specifically contrasting invalid usage with @State variables against valid usage with global @Builder functions. The key takeaway is that passing @State decorated variables to initialize @BuilderParam triggers errors, whereas passing global @Builder functions is the correct approach. The text includes code examples illustrating both the anti-pattern and the recommended solution. This guide is intended for Cangjie developers working with ArkUI component state management and custom builders.