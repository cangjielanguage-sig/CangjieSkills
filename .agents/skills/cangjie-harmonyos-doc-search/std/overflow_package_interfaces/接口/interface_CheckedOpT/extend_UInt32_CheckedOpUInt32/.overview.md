# extend_UInt32_CheckedOpUInt32

This directory serves as a comprehensive API documentation resource for the Cangjie language, focusing specifically on the implementation of the `CheckedOp` interface for the `UInt32` data type. It provides detailed specifications for a wide array of safe arithmetic and bit manipulation functions, including addition, multiplication, division, negation, and bit shifting. The primary focus of this documentation is to demonstrate how these operations utilize the `Option` return type (`?UInt32`) to manage potential overflows and invalid operations gracefully. Instead of wrapping values or throwing exceptions, these functions return `None` when an error occurs, such as an overflow or an invalid shift amount. This approach is critical for developers who need to implement robust, error-safe numerical logic in their software, ensuring predictable behavior and preventing undefined states in unsigned 32-bit integer calculations. It is particularly suitable for systems programming, financial applications, and any context where data integrity is paramount.

## Quick Navigation

**What do you want to learn?**

*   Learn about safe arithmetic operations (addition, multiplication, division, modulo, increment, decrement) → extend_UInt32_CheckedOpUIn_7more.md
*   Learn about safe negation, subtraction, and bit shifting operations → func_checkedNeg_4more.md

## Detailed Description

The documentation is split into specific files to categorize the functionality effectively.

### extend_UInt32_CheckedOpUIn_7more.md extend_UInt32_CheckedOpUIn_7more.md

This API documentation specifies the implementation of the CheckedOp interface for the UInt32 type in the Cangjie language. It details a suite of safe arithmetic functions, including checkedAdd, checkedMul, checkedDiv, checkedMod, checkedInc, and checkedDec. The key takeaway is that these operations return an Option type (?UInt32), providing a mechanism to handle potential overflows gracefully by returning None instead of wrapping values. This reference guide is designed for developers who need to perform error-safe mathematical operations on unsigned 32-bit integers without risking undefined behavior.

### func_checkedNeg_4more.md func_checkedNeg_4more.md

This API reference document details safe arithmetic and bit manipulation functions for the Cangjie language that utilize the Option return strategy. It covers four specific functions: checkedNeg, checkedShl, checkedShr, and checkedSub. Key information includes that all functions return ?UInt32, returning None upon overflow or invalid shift operations (e.g., shifting by more than the bit width), ensuring error handling without exceptions. This is intended for Cangjie developers implementing robust numerical logic where overflow safety is critical.