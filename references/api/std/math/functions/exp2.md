<!-- cj-doc kind="api-member" level="5" id="std.math.func.exp2" parent="std.math" -->
# exp2

[← std.math](../index.md)

本页汇总 3 个同名重载；先按签名选择，再读取对应契约。

## exp2(Float16)

### 签名

```cangjie role=signature
public func exp2(x: Float16): Float16
```

求 2 的 `x` 次幂。

### 契约

参数：

- x: Float16 - 传入的半精度浮点数指数。

返回值：

- Float16 - 返回 2 的 `x` 次幂。

## exp2(Float32)

### 签名

```cangjie role=signature
public func exp2(x: Float32): Float32
```

求 2 的 `x` 次幂。

### 契约

参数：

- x: Float32 - 传入的单精度浮点数指数。

返回值：

- Float32 - 返回 2 的 `x` 次幂。

## exp2(Float64)

### 签名

```cangjie role=signature
public func exp2(x: Float64): Float64
```

求 2 的 `x` 次幂。

### 契约

参数：

- x: Float64 - 传入的双精度浮点数指数。

返回值：

- Float64 - 返回 2 的 `x` 次幂。
