<!-- cj-doc kind="guide-leaf" level="5" id="language.collections.hashmap.8-容量管理" parent="language.collections.hashmap" -->
# 8. 容量管理

[← HashMap 类型](index.md)

```cangjie cjtest=syntax id=syntax-2c8f1fbf1d-1 form=unit
func reserve(additional: Int64): Unit
```

```cangjie cjtest=syntax id=syntax-2c8f1fbf1d-2 form=stmt
map.reserve(100)  // 扩容以容纳更多元素
```

- `additional <= 0` 或剩余容量足够时不执行扩容
- 扩容后容量约为原来的 1.5 倍（不保证精确值）
- 当溢出 `Int64.Max` 时抛 `OverflowException`

---
