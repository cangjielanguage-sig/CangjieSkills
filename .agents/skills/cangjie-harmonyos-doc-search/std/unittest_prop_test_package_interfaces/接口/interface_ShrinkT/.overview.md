# interface_ShrinkT

This directory contains API documentation for the `Shrink` interface in the Cangjie programming language. It focuses on implementations that allow data types to generate smaller versions of themselves, a crucial capability for property-based testing and fuzzing. The documentation covers the core generic interface definition and its extensions across various data types, including primitive integers, floating-point numbers, strings, and complex collections like HashMaps and Arrays. It is suitable for Cangjie developers looking to implement data reduction strategies to isolate minimal failure cases in their test suites.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and basic integer types → interface_ShrinkT_7more.md
*   Implement shrinking for collection types (Array, HashMap, HashSet) → extendK_V_HashMapK_V_Sh_55543cdd.md, extendT_ArrayT_ShrinkArray_4more.md
*   Handle specific numeric and primitive types (Floats, Strings, Runes) → extend_IntNative_ShrinkInt_6more.md, extend_UInt8_ShrinkUInt8_6more.md

## Detailed Description

### extendK_V_HashMapK_V_Sh_55543cdd.md extendK_V_HashMapK_V_Sh_55543cdd.md
This API documentation details the implementation of the Shrink interface for the HashMap class within the Cangjie programming language. The document covers the extension declaration `extend<K, V> HashMap<K, V>` and specifically defines the `shrink()` function. Key information includes the function signature `func shrink(): Iterable<HashMap<K, V>>`, which generates a collection of smaller HashMap instances from the original value, facilitating value reduction strategies. Targeted at Cangjie developers, this reference is essential for those implementing property-based testing or fuzzing where shrinking inputs is required to find minimal failure cases.

### extendT_ArrayT_ShrinkArray_4more.md extendT_ArrayT_ShrinkArray_4more.md
This API reference document outlines the implementation of the Shrink interface for four generic data types within the Cangjie language. The content is divided into four main sections, each detailing an extension for Array, Option, ArrayList, and HashSet. Key information includes the specific syntax for implementing the Shrink interface and the shrink() method, which generates an Iterable of smaller values for each type. This functionality is essential for property-based testing, allowing developers to reduce failing test cases to their simplest form. The document targets developers seeking to understand or utilize shrinking strategies for standard collection types.

### extend_IntNative_ShrinkInt_6more.md extend_IntNative_ShrinkInt_6more.md
This API reference document details the implementation of the Shrink interface for six core data types in the Cangjie language. It covers extensions for IntNative, Rune, String, UInt16, UInt32, and UInt64, explaining how each type conforms to the Shrink parent type. Key information includes the definition of the shrink() function for each type, which returns an Iterable collection of "smaller" values to facilitate value reduction. This documentation is intended for Cangjie developers who need to utilize shrinking capabilities, likely for property-based testing or data simplification tasks.

### extend_UInt8_ShrinkUInt8_6more.md extend_UInt8_ShrinkUInt8_6more.md
This API reference document details the implementation of the Shrink interface for six specific data types in the Cangjie programming language. The content covers extensions for UInt8, UIntNative, Unit, Float16, Float32, and Float64, detailing how each implements the Shrink parent type. Key information includes the definition of the shrink() function for each type, which returns an Iterable of potential smaller values, enabling value reduction strategies. Targeted at developers working with property-based testing or data reduction, this documentation requires familiarity with Cangjie's type system and interfaces.

### interface_ShrinkT_7more.md interface_ShrinkT_7more.md
This document provides API reference documentation for the Cangjie language's generic Shrink interface, designed to reduce values of type T into multiple smaller values. It covers the core interface definition and its extensions for primitive types, specifically Bool and the integer family Int8, Int16, Int32, and Int64. The primary takeaway is the shrink() method, which returns an Iterable of potential smaller values, essential for simplifying data states. Intended for Cangjie developers, likely implementing property-based testing or data reduction algorithms, this document assumes familiarity with generic interfaces and iterables.