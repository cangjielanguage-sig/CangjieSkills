# cj-arkts_interoperability_lib

This directory contains a guide for HarmonyOS developers implementing interoperability between ArkTS and Cangjie using a manual library approach, serving as an alternative to declarative macros. The documentation covers the end-to-end process, starting with creating a Cangjie module and defining functions that utilize JSContext and JSCallInfo parameters. Developers will learn to register modules programmatically using JSModule.registerModule and handle type conversions between JSValue and Cangjie types. It also provides instructions for generating TypeScript declarations and importing dynamic libraries into ArkTS projects. Crucially, the guide emphasizes strict naming conventions to prevent symbol overwriting between manually registered functions and those generated by the @Interop decorator. This resource is intended for developers with a strong command of both ArkTS and Cangjie syntax seeking advanced integration techniques. By following this guide, developers can ensure seamless communication between the two environments while maintaining code integrity.

## Quick Navigation

**What do you want to learn?**

*   **Understand the manual interoperability workflow**: Learn how to implement ArkTS-to-Cangjie integration without relying on declarative macros → cj-arkts_interoperability_lib.md
*   **Define and register functions**: Master the creation of functions using JSContext and JSCallInfo parameters and register them via JSModule.registerModule → cj-arkts_interoperability_lib.md
*   **Handle data types**: Explore the technical mechanisms for converting types between JSValue and Cangjie types → cj-arkts_interoperability_lib.md
*   **Manage declarations and imports**: Find out how to create TypeScript declarations and import dynamic libraries in ArkTS → cj-arkts_interoperability_lib.md
*   **Adopt best practices**: Understand the strict naming conventions required to avoid symbol overwriting with @Interop functions → cj-arkts_interoperability_lib.md

## Detailed Description

This section provides a comprehensive breakdown of the documentation file included in the directory, detailing its purpose and content.

### cj-arkts_interoperability_lib.md cj-arkts_interoperability_lib.md
This guide instructs developers on implementing ArkTS-to-Cangjie interoperability using the interoperability library, serving as an alternative to declarative macros. It details the workflow of creating a Cangjie module, defining functions with JSContext and JSCallInfo parameters, and registering them via JSModule.registerModule. Key steps include type conversion between JSValue and Cangjie types, creating TypeScript declarations, and importing the dynamic library in ArkTS. The document emphasizes strict naming conventions to avoid symbol overwriting between manually registered functions and those decorated with @Interop. Targeted at HarmonyOS developers, this tutorial requires knowledge of both ArkTS and Cangjie syntax.