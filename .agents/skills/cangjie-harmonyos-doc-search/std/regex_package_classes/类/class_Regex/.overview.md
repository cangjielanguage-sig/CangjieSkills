# class_Regex

This directory contains API reference documentation for the Cangjie programming language's Regex class. It covers compiling regular expressions, finding matches, replacing strings, and splitting text. Key topics include constructors, the `find` and `matches` methods, lazy evaluation, and capture group handling. The documentation is intended for Cangjie developers who need to implement pattern matching, string manipulation, and text processing logic within their applications.

## Quick Navigation

**What do you want to do?**

*   Compile regular expressions or find the first match? → class_Regex_4more.md
*   Retrieve all matches, use lazy evaluation, or map capture group names? → func_findAllString_Bool_4more.md
*   Check for pattern matches or perform basic string replacements? → func_matchesString_4more.md
*   Replace patterns with limits, split strings, or retrieve the input sequence? → func_replaceAllString_Stri_4more.md

## Detailed Description

### class_Regex_4more.md class_Regex_4more.md
This document serves as API reference for the Cangjie Regex class, detailing how to compile and use regular expressions. It covers two constructors and the find function. One constructor is deprecated in favor of using an array of RegexFlag. The find method searches for the first match and returns an Option<MatchData>, handling cases where no match is found. The document includes syntax details, parameters, exceptions like RegexException, and a usage example demonstrating case sensitivity. This is intended for Cangjie developers implementing pattern matching.

### func_findAllString_Bool_4more.md func_findAllString_Bool_4more.md
This document serves as API reference documentation for regular expression matching functions within the Cangjie programming language's standard library. It details the `findAll` method for retrieving all matches as an array, the `lazyFindAll` method for iterator-based lazy evaluation, and the `getNamedGroups` utility for mapping capture group names to indices. It also documents the deprecated `matcher` function. Key functions accept an input string and a boolean flag for group extraction, returning `MatchData` objects or iterators. The documentation highlights exception handling for `RegexException` and provides verified code examples demonstrating date parsing and basic pattern matching. This reference is intended for Cangjie developers who need to perform string pattern matching, extract capture groups, or manage regex operations efficiently.

### func_matchesString_4more.md func_matchesString_4more.md
This document serves as API reference documentation for string manipulation functions within the Cangjie Regex class. It details four specific methods: matches, replace, replace with an index, and replaceAll. The text explains how to check for pattern matches and perform string replacements, either for the first occurrence, from a specific index, or globally. It includes parameter definitions, return types, exception handling for index errors, and verified code examples. This technical guide targets Cangjie developers implementing regular expression matching and substitution logic.

### func_replaceAllString_Stri_4more.md func_replaceAllString_Stri_4more.md
This document serves as API reference documentation for string manipulation functions within the Cangjie standard library's regex module. It details four specific functions: `replaceAll` for substituting matched patterns with a limit, two overloads of `split` for dividing strings into arrays, and `string` for retrieving the input sequence. Key information includes parameter definitions for `input`, `replacement`, and `limit`, explaining how limit values affect operation counts. The content provides syntax, return types, and verified code examples demonstrating usage. Targeted at Cangjie developers, this reference requires knowledge of regular expressions to effectively implement text processing and pattern matching tasks.