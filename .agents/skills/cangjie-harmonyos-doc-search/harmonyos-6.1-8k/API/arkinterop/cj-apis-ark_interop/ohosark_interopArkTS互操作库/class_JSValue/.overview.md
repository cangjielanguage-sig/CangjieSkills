# class_JSValue

本目录包含仓颉语言中 `JSValue` 类的 API 参考文档，专为 ArkTS 运行时互操作设计。内容涵盖 `JSValue` 的定义、生命周期管理、类型转换及对象绑定。核心主题包括 ArkTS 与仓颉类型间的转换、数据类型检查（如 BigInt、Array、Function）以及引用越界或线程不匹配等异常处理。该文档适合从事 ArkTS 与仓颉跨语言交互的开发者，帮助他们安全地操作 JavaScript 值。

## Quick Navigation

- 想了解 `JSValue` 类的基本概念与生命周期限制 → class_JSValue_7more_7aa79b39.md
- 想将 `JSValue` 转换为 ArkTS 或仓颉特定类型（如 Function, Number, String, BigInt） → func_asFunction_6more_034c8b58.md, func_asSymbol_5more_d75ab06e.md, func_setPropertyJ_4more_49d0da54.md, func_toNumber_4more_ef1ff2d2.md
- 想检查 `JSValue` 的数据类型（如 Array, BigInt, Boolean, Promise） → func_getElementIn_4more_9a809300.md, func_isBigInt_4more_3d23291c.md, func_isFunction_5more_0cba6289.md, func_isString_4more_252958c6.md
- 想操作对象属性或数组元素（读取、写入、绑定） → func_asSymbol_5more_d75ab06e.md, func_getElementIn_4more_9a809300.md, func_isString_4more_252958c6.md, func_setPropertyJ_4more_49d0da54.md

## Detailed Description

### class_JSValue_7more_7aa79b39.md class_JSValue_7more_7aa79b39.md
本文档介绍了仓颉语言中 `JSValue` 类的 API 参考，用于说明 ArkTS 运行时统一类型的定义与使用。涵盖了该类的生命周期限制（弱类型、短生命周期）及类型转换方法（如 `asArray`、`asBoolean`）。关键信息指出 `JSValue` 仅能由互操作接口创建，不可拷贝或直接返回，必须转换为仓颉类型或安全引用传递。文档还列出了类型不匹配或线程错误时的异常代码，面向需要进行 ArkTS 与仓颉互操作的开发者。

### func_asFunction_6more_034c8b58.md func_asFunction_6more_034c8b58.md
本文档介绍了将 JSValue 转换为特定 ArkTS 数据类型的六个函数：`asFunction`、`asNull`、`asNumber`、`asObject`、`asPromise` 和 `asString`。详细列出了每个函数的返回类型（如 `JSFunction`、`JSObject` 等）以及可能抛出的 `BusinessException` 异常，包括引用越界、线程不匹配和类型不匹配等错误码。这些功能自版本 22 起可用，旨在帮助开发者安全地进行类型转换和错误处理。

### func_asSymbol_5more_d75ab06e.md func_asSymbol_5more_d75ab06e.md
本文档涵盖 JSValue 类的类型转换与对象绑定功能。详细说明了 `asSymbol` 和 `asUndefined` 方法用于将 JSValue 转换为特定的 ArkTS 类型，以及 `bindObject` 和 `getBindingObject` 方法用于在 ArkTS 对象与仓颉对象之间建立和获取绑定关系。每个方法均包含参数说明、返回值类型、可能抛出的异常及对应错误码，并提供了代码示例。

### func_getElementIn_4more_9a809300.md func_getElementIn_4more_9a809300.md
本文档旨在指导开发者进行 ArkTS 互操作时的数据读取与类型判断。详细介绍了四个核心函数：`getElement` 用于通过索引读取 ArkTS 数组元素，`getProperty` 用于通过键读取对象属性，`isArray` 和 `isArrayBuffer` 分别用于判断 JSValue 是否为数组或 ArrayBuffer 类型。内容涵盖了各函数的参数定义、返回值类型以及可能抛出的错误码，并提供了具体的代码示例。

### func_isBigInt_4more_3d23291c.md func_isBigInt_4more_3d23291c.md
本文档提供了 JSValue 类型检查功能的接口说明。涵盖了四个核心函数：`isBigInt`、`isBoolean`、`isClass` 和 `isExternal`，分别用于判断 JSValue 是否为 BigInt、布尔值、ArkTS 类或外部对象。每个函数均包含功能描述、返回值说明、可能抛出的异常（如引用越界或线程不匹配）及代码示例，适用于从事 ArkTS 与仓颉互操作开发的程序员。

### func_isFunction_5more_0cba6289.md func_isFunction_5more_0cba6289.md
本文档详细介绍了用于类型检查的五个成员函数。主要内容包括 `isFunction`、`isNull`、`isNumber`、`isObject` 和 `isPromise` 的功能描述、返回值说明、异常处理及代码示例。这些函数均返回布尔值，用于判断 JSValue 实例的具体类型，并可能抛出引用越界或线程不匹配的异常，提供了在 JSContext 中安全处理 JS 数据类型的实用方法。

### func_isString_4more_252958c6.md func_isString_4more_252958c6.md
本文档用于 JavaScript 互操作中的类型检查与数组操作。详细介绍了 `isString`、`isSymbol`、`isUndefined` 三个类型判断函数及 `setElement` 数组写入函数。核心内容包括函数签名、返回值、参数说明、`BusinessException` 异常处理（如错误码 34300003）及代码示例，帮助开发者准确判断 JSValue 类型并操作数组数据。

### func_setPropertyJ_4more_49d0da54.md func_setPropertyJ_4more_49d0da54.md
本文档主要介绍 JSValue 对象的属性操作与类型转换功能。详细说明了 `setProperty`（写入属性）、`strictEqual`（严格判等）、`toBigInt` 及 `toBoolean`（类型转换）四个函数的用法。关键内容包括函数参数、返回值、异常处理（如线程不匹配、类型不匹配）及代码示例。面向进行仓颉与 ArkTS 数据交互的开发者，需了解 JSContext 及互操作基础。

### func_toNumber_4more_ef1ff2d2.md func_toNumber_4more_ef1ff2d2.md
本文档详细说明了 JSValue 类的四个类型转换与检测方法：`toNumber`、`toString`、`toUtf16String` 和 `typeof`。涵盖了各方法的语法、功能描述、返回值类型以及可能抛出的异常，并提供了代码示例。主要目的是指导开发者如何在 ArkTS 与仓颉互操作场景下，将 JavaScript 值转换为仓颉的 `Float64`、`String` 或 `Utf16String` 类型，或获取其 `JSType`。