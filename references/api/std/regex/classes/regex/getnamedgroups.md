<!-- cj-doc kind="api-member" level="6" id="std.regex.class.regex.getnamedgroups" parent="std.regex.class.regex" -->
# Regex.getNamedGroups

[← Regex](index.md)

## 签名

```cangjie role=signature
public func getNamedGroups(): Map<String, Int64>
```

获取命名捕获组的名称与索引映射。

## 契约

返回值：

- Map\<String, Int64> - 命名捕获组的名称与索引映射。
