<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.signature.hashcode" parent="stdx.crypto.x509.struct.signature" -->
# Signature.hashCode

[← Signature](index.md)

## 签名

```cangjie role=signature
public override func hashCode(): Int64
```

返回证书签名哈希值。

## 契约

返回值：

- Int64 - 对证书签名对象进行哈希计算后得到的结果。
