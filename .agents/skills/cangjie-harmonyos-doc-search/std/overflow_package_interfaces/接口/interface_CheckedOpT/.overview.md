# interface_CheckedOpT

This directory contains comprehensive documentation for the Cangjie programming language's `CheckedOp` interface, which defines safe arithmetic and bitwise operations designed to prevent integer overflow errors. It covers the generic interface definition alongside specific implementations for various integer types, including signed and unsigned variants (Int8, Int16, Int32, Int64, IntNative, and their unsigned counterparts). The core mechanism involves returning an `Option` type (`?T`) to gracefully handle errors by returning `None` instead of wrapping values or throwing exceptions. This resource is essential for Cangjie developers who require robust error handling and explicit management of numerical boundaries to ensure application stability and data integrity.

## Quick Navigation

**What do you want to learn?**

*   Understand the generic interface definition and core concepts → interface_CheckedOpT_11more.md
*   Implement safe operations for specific integer types:
    *   **Signed Integers** (Int8, Int16, Int32, Int64, IntNative) → extend_Int8_CheckedOpInt8/, extend_Int16_CheckedOpInt16/, extend_Int32_CheckedOpInt32/, extend_Int64_CheckedOpInt64_CheckedPow/, extend_IntNative_CheckedOpIntNative/
    *   **Unsigned Integers** (UInt8, UInt16, UInt32, UInt64, UIntNative) → extend_UInt8_CheckedOpUInt8/, extend_UInt16_CheckedOpUInt16/, extend_UInt32_CheckedOpUInt32/, extend_UInt64_CheckedOpUInt64/, extend_UIntNative_CheckedOpUIntNative/

## Detailed Description

### interface_CheckedOpT_11more.md interface_CheckedOpT_11more.md
This document provides API reference documentation for the Cangjie programming language's `CheckedOp` interface, which defines safe arithmetic operations that prevent overflow errors. It details the interface definition and specifications for ten methods covering basic arithmetic such as addition, subtraction, multiplication, division, and modulo, alongside increment, decrement, negation, and bitwise shifting. Key information includes that all functions return an Option type `?T`, returning `None` if an overflow occurs or if shift amounts are invalid, rather than wrapping values. This design allows developers to explicitly handle calculation failures. The intended audience is Cangjie developers who need robust integer arithmetic logic and understand generic types and Option handling.

### extend_Int16_CheckedOpInt16/
This directory provides comprehensive API reference documentation for the Cangjie programming language, specifically targeting safe arithmetic and bitwise operations for the `Int16` type. It encompasses the implementation details of the `CheckedOp` interface alongside specific utility functions designed to manage potential overflow and errors gracefully. Key topics include checked addition, subtraction, multiplication, division, modulo, bitwise shifts, and negation. A central feature of these methods is their use of an `Option` return strategy, which returns `None` when operations exceed the `Int16` range or encounter invalid parameters, rather than wrapping values or throwing exceptions.

### extend_Int32_CheckedOpInt32/
This directory serves as a comprehensive technical resource for the `CheckedOp` interface implementation tailored specifically for the `Int32` data type within the Cangjie programming language. It focuses on providing safe mechanisms for arithmetic and bitwise computations, specifically addressing the critical risks associated with integer overflow. By utilizing an `Option` return strategy, these APIs allow developers to handle errors gracefully—returning `None` instead of wrapping values or throwing exceptions—thereby significantly enhancing application robustness. This documentation is essential for Cangjie developers who prioritize numerical stability and require explicit error handling strategies in their software projects to prevent unexpected runtime failures.

### extend_Int64_CheckedOpInt64_CheckedPow/
This directory provides comprehensive API documentation for the Cangjie programming language, specifically focusing on the implementation of safe arithmetic and bitwise operations for the `Int64` type. It details the `CheckedOp` and `CheckedPow` interfaces, which are essential for preventing runtime errors caused by integer overflows. The documentation covers a wide range of functions, including basic arithmetic like addition, division, and multiplication, as well as advanced operations such as negation, power calculation, and bitwise shifting. A core feature of these APIs is the use of the `Option` return strategy, which returns `None` upon overflow or invalid input, ensuring robust error handling.

### extend_Int8_CheckedOpInt8/
This directory contains essential API reference documentation for the Cangjie programming language, specifically focusing on the implementation of safe arithmetic operations for the `Int8` type. It provides a detailed breakdown of the `CheckedOp` interface, which is crucial for performing secure mathematical calculations. The content encompasses a broad spectrum of functions, including basic arithmetic such as addition, division, modulo, multiplication, negation, increment, and decrement, alongside bit manipulation tasks like shifting and subtraction. A defining characteristic of these operations is the utilization of the `Option` return strategy, which ensures that overflow scenarios and invalid shift amounts are handled gracefully by returning `None` rather than throwing runtime exceptions.

### extend_IntNative_CheckedOpIntNative/
This directory serves as a comprehensive API reference for the `CheckedOp` interface implementation specifically tailored for the `IntNative` type within the Cangjie programming language ecosystem. It focuses extensively on safe integer arithmetic and bitwise manipulation, providing developers with the necessary tools to handle potential errors gracefully. The core functionality revolves around an `Option` return strategy, which returns `None` in cases of overflow or invalid operations, thereby preventing unexpected runtime crashes or silent data wrapping. This documentation is essential for Cangjie developers who prioritize system stability and need to implement explicit error handling for mathematical operations in their applications.

### extend_UInt16_CheckedOpUInt16/
This directory contains comprehensive API documentation for the implementation of the `CheckedOp` interface specifically designed for the `UInt16` type within the Cangjie programming language. It focuses on providing safe arithmetic and bitwise operations that prevent runtime exceptions by utilizing `Option` return types to handle overflow and error conditions gracefully. The documentation covers a wide range of essential functions, including addition, subtraction, multiplication, division, negation, and bit shifting. This resource is highly suitable for Cangjie developers who need to implement robust, error-checked mathematical logic on unsigned 16-bit integers, ensuring data integrity and application stability in systems-level programming or performance-critical code segments.

### extend_UInt32_CheckedOpUInt32/
This directory serves as a comprehensive API documentation resource for the Cangjie language, focusing specifically on the implementation of the `CheckedOp` interface for the `UInt32` data type. It provides detailed specifications for a wide array of safe arithmetic and bit manipulation functions, including addition, multiplication, division, negation, and bit shifting. The primary focus of this documentation is to demonstrate how these operations utilize the `Option` return type (`?UInt32`) to manage potential overflows and invalid operations gracefully. Instead of wrapping values or throwing exceptions, these functions return `None` when an error occurs, such as an overflow or an invalid shift amount.

### extend_UInt64_CheckedOpUInt64/
This directory serves as a comprehensive API documentation resource for the Cangjie programming language, focusing specifically on the implementation of the `CheckedOp` interface for the `UInt64` type. It thoroughly covers safe arithmetic and bit manipulation operations designed to handle potential overflow errors gracefully. The documentation highlights the use of an `Option` return strategy, which ensures that operations return `None` if an error occurs, such as overflow, rather than throwing runtime exceptions. Key topics include checked addition, subtraction, multiplication, division, modulo, increment, decrement, negation, and bitwise shifts. This resource is ideal for Cangjie developers who need to perform robust integer calculations and explicitly manage error states.

### extend_UInt8_CheckedOpUInt8/
This directory contains essential API documentation for the Cangjie programming language, focusing specifically on the implementation of safe arithmetic operations for the `UInt8` type. It covers the extension of the `CheckedOp` interface and details various functions designed to handle arithmetic and bitwise manipulations securely. Key concepts include overflow detection, the utilization of the `Option` return strategy to prevent runtime errors, and the explicit handling of edge cases such as invalid shift amounts. This documentation is highly suitable for Cangjie developers who need to write robust, error-safe code when performing calculations on unsigned 8-bit integers. It ensures that operations like addition, subtraction, and bit shifting manage potential overflows gracefully, returning `None` instead of causing application crashes.

### extend_UIntNative_CheckedOpUIntNative/
This directory provides comprehensive API documentation for the Cangjie programming language, specifically focusing on the `CheckedOp` interface implementation for the `UIntNative` type. It serves as a critical resource for developers looking to implement safe arithmetic and bitwise operations that actively prevent runtime exceptions. The documentation covers a wide range of functionalities, including addition, subtraction, multiplication, division, negation, and bit shifting. A core feature of these operations is the use of an `Option` return strategy, which ensures that potential overflows or invalid operations return `None` rather than causing system crashes or wrapping values. This directory is highly suitable for Cangjie developers who prioritize robust error handling and numerical stability in their software projects.