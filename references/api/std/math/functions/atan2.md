<!-- cj-doc kind="api-member" level="5" id="std.math.func.atan2" parent="std.math" -->
# atan2

[← std.math](../index.md)

本页汇总 3 个同名重载；先按签名选择，再读取对应契约。

## atan2(Float16, Float16)

### 签名

```cangjie role=signature
public func atan2(y: Float16, x: Float16): Float16
```

计算两个半精度浮点数 y/x 的反正切函数值，单位为弧度。

### 契约

参数：

- y: Float16 - 传入的半精度浮点数。
- x: Float16 - 传入的半精度浮点数。

返回值：

- Float16 - 返回 y/x 的反正切函数值，单位为弧度。

## atan2(Float32, Float32)

### 签名

```cangjie role=signature
public func atan2(y: Float32, x: Float32): Float32
```

计算两个单精度浮点数 y/x 的反正切函数值，单位为弧度。

### 契约

参数：

- y: Float32 - 传入的单精度浮点数。
- x: Float32 - 传入的单精度浮点数。

返回值：

- Float32 - 返回 y/x 的反正切函数值，单位为弧度。

## atan2(Float64, Float64)

### 签名

```cangjie role=signature
public func atan2(y: Float64, x: Float64): Float64
```

计算两个双精度浮点数 y/x 的反正切函数值，单位为弧度。

### 契约

参数：

- y: Float64 - 传入的双精度浮点数。
- x: Float64 - 传入的双精度浮点数。

返回值：

- Float64 - 返回 y/x 的反正切函数值，单位为弧度。
