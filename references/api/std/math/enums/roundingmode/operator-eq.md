<!-- cj-doc kind="api-member" level="6" id="std.math.enum.roundingmode.operator-eq" parent="std.math.enum.roundingmode" -->
# RoundingMode.==

[← RoundingMode](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: RoundingMode): Bool
```

判等。

## 契约

参数：

- that: RoundingMode - 被比较的舍入规则。

返回值：

- Bool - 若舍入规则相同，返回 true；否则，返回 false。
