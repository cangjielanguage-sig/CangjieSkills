# 开启关闭扫描

This directory focuses on the implementation of Bluetooth Low Energy (BLE) scanning functionalities, specifically initiating and terminating scan processes, and parsing the resulting advertising data. It covers technical guides using the Cangjie programming language and ArkTS, detailing how to configure scan filters, manage scan options, and handle callbacks. Additionally, it provides code examples for decoding raw byte arrays into structured information like service UUIDs and device names. This content is suitable for software developers building connectivity features for HarmonyOS/OpenHarmony systems who need to implement device discovery and data interpretation logic.

## Quick Navigation

**What do you want to learn?**

*   How to start and stop BLE scanning using Cangjie? → 开启关闭扫描_1.md
*   How to parse raw advertising data byte arrays? → 开启关闭扫描_2.md
*   How to handle scan results and callbacks in ArkTS? → 开启关闭扫描_3.md

## Detailed Description

### 开启关闭扫描_1.md 开启关闭扫描_1.md
This document provides a technical guide and code example for initiating and terminating Bluetooth Low Energy (BLE) scanning using the Cangjie programming language. It covers prerequisites such as system capabilities and device states, followed by a detailed code demonstration including imports, constants, and a BleScanManager class implementation. Key information includes configuring scan filters based on manufacturer data, setting scan options like duty mode and PHY type, handling callbacks for scan results, and managing exceptions. The document also details parsing raw advertising data into structured information like service UUIDs and local names. This tutorial targets software developers building connectivity features for HarmonyOS/OpenHarmony systems who need to implement device discovery logic.

### 开启关闭扫描_2.md 开启关闭扫描_2.md
This document provides a code implementation for parsing Bluetooth Low Energy (BLE) advertising data, likely part of a scanning feature. The main section details a loop that iterates through raw byte arrays to identify and extract specific data types such as device flags, local names, TX power levels, and various service UUIDs (16, 32, and 128-bit). It also includes logic for handling manufacturer-specific data and service solicitation UUIDs, utilizing helper functions to convert byte slices into UUID strings. This technical reference is intended for developers working on BLE connectivity who need to decode scan records, requiring an understanding of data structures and Bluetooth protocols.

### 开启关闭扫描_3.md 开启关闭扫描_3.md
This technical documentation provides code examples for parsing and handling Bluetooth Low Energy (BLE) scan results. It details helper functions for converting byte arrays to formatted UUID strings, extracting service data, and parsing manufacturer-specific data. The document includes a callback class implementation (ScanResultCb) to process incoming scan events and log device information. Key takeaways involve data manipulation techniques for raw BLE advertising packets. Intended for developers working with BLE connectivity, this reference assumes familiarity with ArkTS syntax and Bluetooth protocols, concluding with a reference to relevant error codes.