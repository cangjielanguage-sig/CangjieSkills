# interface_ConcurrentMapK_V

This directory contains API reference documentation for the `ConcurrentMap` interface in the Cangjie programming language. It covers thread-safe key-value storage, atomic operations, and specific manipulation functions like add, put, remove, and replace. The documentation details method signatures, return types, and deprecation notices for legacy functions. It is suitable for Cangjie developers implementing concurrent collections or managing shared data structures requiring guaranteed atomicity and thread safety.

## Quick Navigation

**What do you want to do?**

*   Learn about the core interface definition and thread-safety contract → interface_ConcurrentMapK_V.md
*   Perform basic operations like add, get, contains, or entryView → func_addK_V_5more.md
*   Handle insertion, deletion, and check deprecated put/remove methods → func_putK_V_supdeprecateds_4more.md
*   Use replace functions or subscript operators for direct access → func_replaceK_V_-_Bool_V_-_5more.md

## Detailed Description

### func_addK_V_5more.md
This document provides API reference documentation for essential manipulation functions of the Map interface in the Cangjie programming language. It covers five primary methods: add, addIfAbsent, contains, entryView, and get. Key information includes detailed parameter definitions, return types such as Option types and Booleans, and specific behaviors for inserting, retrieving, or conditionally updating key-value pairs. The entryView function is highlighted for enabling complex atomic operations through a view callback. This technical guide targets Cangjie developers who need to implement or interact with Map collections, requiring an understanding of generic types and functional programming concepts.

### func_putK_V_supdeprecateds_4more.md
This document provides API reference documentation for Map manipulation functions within the Cangjie collection package. It details four specific methods: put, putIfAbsent, remove, and a conditional remove that accepts a predicate. The text explains the parameters, return values, and operational logic for each function, such as replacing existing values or deleting entries based on conditions. A critical takeaway is that three of these functions are marked as deprecated, with specific guidance provided for their replacements like add and entryView. This guide is intended for Cangjie developers seeking to manage key-value associations and update legacy code to current standards.

### func_replaceK_V_-_Bool_V_-_5more.md
This API reference document details functions and operators for modifying and accessing values within a Map collection in the Cangjie programming language. It covers three overloaded replace functions, two of which are deprecated, alongside two subscript operator overloads. Key information includes that replace functions conditionally update values based on key existence and optional predicates, returning the previous value or None. The deprecated versions specifically recommend using entryView instead. Additionally, the subscript operators provide direct access: one retrieves values (throwing exceptions if missing) and the other sets or inserts key-value pairs. This guide targets Cangjie developers needing to manipulate Map data structures, requiring knowledge of generic types and lambda expressions.

### interface_ConcurrentMapK_V.md
This document serves as API reference documentation for the ConcurrentMap interface in the Cangjie language, defining a standard for thread-safe and atomic map operations. The content details the interface structure, listing method signatures for key-value manipulations such as add, put, remove, replace, and subscript operators. Key information highlights that the interface enforces atomicity for all declared methods in concurrent scenarios, acting as a contract for implementations like ConcurrentHashMap. It defines a mapping from keys of type K to values of type V. This documentation is intended for developers implementing or utilizing concurrent collections who require guaranteed thread safety and atomic behavior in their applications.