<!-- cj-doc kind="api-member" level="6" id="std.ast.class.genericparam.prop-parameters" parent="std.ast.class.genericparam" -->
# GenericParam.parameters

[← GenericParam](index.md)

## 签名

```cangjie role=signature
public mut prop parameters: Tokens
```

获取或设置 GenericParam 节点中的类型形参的 Tokens 类型，可能为空，如 `<T1, T2, T3>` 中的 `T1` `T2` 和 `T3`。

## 契约

类型：Tokens
