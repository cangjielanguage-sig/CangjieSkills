# runtime_package_structs

This directory serves as a comprehensive reference for the deprecated data structures found within the Cangjie runtime package, specifically focusing on the retrieval of system and runtime statistics. It provides detailed documentation for three key structs: `MemoryInfo`, which was used for heap memory metrics; `ProcessorInfo`, designed for accessing processor counts; and `ThreadInfo`, utilized for gathering thread statistics. A central theme of this documentation is the deprecation of these structs in favor of a more modern, function-based API. Developers will find guidance on how static properties, such as `allocatedHeapSize` and `threadCount`, have been replaced by specific global functions like `getAllocatedHeapSize` and `getThreadCount`. This resource is essential for Cangjie developers who are maintaining legacy codebases or actively migrating applications to ensure compatibility with the latest runtime environment standards.

## Quick Navigation

**What do you want to learn?**

*   **Review API Reference:** Access the complete documentation for deprecated runtime structs and their properties → runtime_package_structs.md
*   **Understand Metrics:** Investigate the specific data points previously available in MemoryInfo, ProcessorInfo, and ThreadInfo → runtime_package_structs.md
*   **Migration Guide:** Determine the correct global function replacements for deprecated static properties like `allocatedHeapSize` → runtime_package_structs.md
*   **Developer Targeting:** Understand why these changes matter for Cangjie developers accessing runtime environment data → runtime_package_structs.md

## Detailed Description

The content within this directory focuses on the technical specifications of legacy runtime components. As the Cangjie language evolves, certain structural patterns are being replaced to improve efficiency and clarity. The following file provides the necessary details for developers to understand these changes and implement the required updates in their projects.

*   **Focus:** Deprecated structs and their replacements.
*   **Impact:** Requires migration from static properties to global functions.
*   **Audience:** Developers maintaining or updating runtime-dependent code.
*   **Goal:** Facilitate a smooth transition to the new API.

### runtime_package_structs.md runtime_package_structs.md

This API reference document details three deprecated structs within the Cangjie runtime package used for retrieving system and runtime statistics. It covers MemoryInfo for heap memory metrics, ProcessorInfo for processor counts, and ThreadInfo for thread statistics. The key information is that all structs are marked as deprecated, with static properties like allocatedHeapSize and threadCount replaced by specific global functions such as getAllocatedHeapSize and getThreadCount. This guide targets Cangjie developers needing to access runtime environment data, warning them to migrate to the newer function-based API.