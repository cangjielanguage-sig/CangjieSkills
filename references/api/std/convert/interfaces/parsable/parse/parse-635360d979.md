<!-- cj-doc kind="api-member" level="7" id="std.convert.interface.parsable.parse.parse-635360d979" parent="std.convert.interface.parsable.parse" -->
# Parsable<T>.static func parse(String)

[← Parsable<T>.parse](index.md)

## 签名

```cangjie role=signature
static func parse(value: String): T
```

从字符串中解析特定类型。

## 契约

参数：

- value: String - 待解析的字符串。

返回值：

- T - 转换后的值。
