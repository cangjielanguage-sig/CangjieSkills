<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonarray.add" parent="stdx.encoding.json.class.jsonarray" -->
# JsonArray.add

[← JsonArray](index.md)

## 签名

```cangjie role=signature
public func add(jv: JsonValue): JsonArray
```

向 JsonArray 中加入 JsonValue 数据。

## 契约

参数：

- jv: JsonValue - 需要加入的 JsonValue。

返回值：

- JsonArray - 加入数据后的 JsonArray。
