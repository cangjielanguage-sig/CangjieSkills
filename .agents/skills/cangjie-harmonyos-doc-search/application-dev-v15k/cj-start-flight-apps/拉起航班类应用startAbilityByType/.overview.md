# 拉起航班类应用startAbilityByType

This directory provides a complete technical guide for developers implementing the `startAbilityByType` functionality to invoke flight-related applications within the HarmonyOS ecosystem. It addresses the full integration workflow, covering both the caller side (itinerary apps initiating requests) and the target side (flight apps processing queries). The documentation details specific implementation scenarios, such as querying flight status by flight number or by origin and destination locations. It includes essential technical specifications for parameter definitions, `module.json5` configuration, URI handling, and data parsing using Cangjie and ArkUI. This resource is ideal for developers seeking to enable seamless cross-application connectivity, enhance user experience in travel apps, or build the underlying service logic for flight information retrieval.

## Quick Navigation

**What do you want to learn?**

*   **Caller Side Implementation:** Learn how to construct parameters and invoke flight app extension panels using the `startAbilityByType` API from an itinerary application → 拉起航班类应用startAbilityByType_3more.md
*   **Target Side Development:** Understand how to configure the target application, declare features in `module.json5`, and parse incoming query parameters to handle flight status requests → 目标方开发步骤/

## Detailed Description

### 拉起航班类应用startAbilityByType_3more.md 拉起航班类应用startAbilityByType_3more.md
This technical guide explains how to invoke flight application extension panels using the startAbilityByType API within itinerary apps. It details parameter specifications for two query scenarios—by flight number or by origin/destination—and provides step-by-step development instructions. Key parameters include sceneType, flightNo, and originLocation. The document includes a Cangjie code example demonstrating parameter construction and API invocation. Intended for developers integrating flight status query capabilities into travel applications, it serves as a practical tutorial for enhancing app connectivity.

### 目标方开发步骤/
This directory serves as a comprehensive technical resource for developers tasked with building the target side of a flight query application within the HarmonyOS ecosystem. It meticulously covers the necessary steps to configure `module.json5` for feature declaration using `linkFeature` and URI attributes, as well as parsing incoming parameters inside `UIAbility` lifecycle methods. The documentation provides detailed parameter definitions for two primary scenarios: querying by flight number and by location, specifying required fields such as `flightNo` and `originLocation`. Furthermore, it includes code examples for page routing logic utilizing `localStorage` and `windowStage`. These guides are specifically designed for developers utilizing the Cangjie language and ArkUI frameworks to implement feature-based application invocation and efficient data handling.