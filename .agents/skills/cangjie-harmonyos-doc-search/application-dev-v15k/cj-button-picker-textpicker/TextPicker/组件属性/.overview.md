# 组件属性

This directory serves as a specialized repository for API reference documentation concerning component properties within the ArkUI framework, explicitly designed for use with the Cangjie language. It provides a deep dive into the Picker component, offering developers the tools necessary to configure complex interaction behaviors and visual styles. The documentation encompasses a broad spectrum of configuration options, such as enabling cyclic scrolling, defining item heights through multiple data types, customizing divider lines, and implementing gradient fade effects for visual depth. It also explains how to set default selection indices for various data picker configurations. Beyond structural properties, the directory covers aesthetic customization, detailing methods to adjust text styles for both active and inactive items. This resource is indispensable for HarmonyOS UI developers and engineers aiming to create polished, user-friendly interfaces. By outlining specific parameter overloads, system capabilities, and version starting points, it ensures precise control over component functionality and design consistency. Mastering these properties allows for the creation of highly responsive and intuitive selection interfaces.

## Quick Navigation

What do you want to learn or configure?

*   **Functional properties**: cyclic scrolling, item height, dividers, gradients, and selection indices → func_canLoopBool_8more.md
*   **Visual styling**: text color, font size, and weight for selected and non-selected items → func_selectedTextStylePick_2more.md

## Detailed Description

Below are the detailed specifications for each file contained in this directory.

### func_canLoopBool_8more.md
This document provides API reference documentation for configuring a Picker component in the ArkUI framework using the Cangjie language. It covers methods for controlling cyclic scrolling behavior, setting item heights via multiple data types, customizing divider styles, and defining gradient fade effects. Additionally, it details functions for setting default selection indices for both single and multi-column data pickers. Key takeaways include the specific parameter overloads for height and index settings, as well as the system capabilities and starting versions for each function. Intended for UI developers, this guide helps in fine-tuning the visual presentation and interaction logic of picker components.

### func_selectedTextStylePick_2more.md
This API reference document outlines methods for customizing text appearance within a Picker component using the Cangjie language. It details two primary functions: selectedTextStyle, which modifies the currently selected item's text, and textStyle, which adjusts the appearance of non-selected items. Both functions accept a PickerTextStyle parameter to control color, font size, and weight, with specific default values defined for system version 19. The documentation is intended for HarmonyOS developers seeking to fine-tune UI component aesthetics through ArkUI system capabilities.