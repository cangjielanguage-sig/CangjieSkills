<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.enum.signaturetype.value-rsa" parent="stdx.net.tls.enum.signaturetype" -->
# SignatureType.RSA

[← SignatureType](index.md)

## 签名

```cangjie role=signature
RSA
```

创建一个 `RSA` 类型的枚举实例，表示采用 RSA 加密算法。
