# 完整示例

This directory contains a collection of complete, practical code examples written in the Cangjie language, designed to assist developers in mastering the MediaKit framework for media application development. It specifically targets the implementation of the AVPlayer class, providing deep dives into streaming media playback for various protocols including HLS Video on Demand (VOD), HLS Live, and DASH. Beyond basic playback, the content covers advanced features such as track selection, surface ID management, and the configuration of custom HTTP headers. Furthermore, it addresses the critical aspects of player lifecycle management, state machine transitions, and robust error handling mechanisms. These resources are ideal for developers seeking to build professional-grade video players or integrate complex media functionalities into their Cangjie-based projects, offering clear guidance on handling asynchronous callbacks and maintaining playback stability.

## Quick Navigation

**What do you want to learn?**

*   Implement streaming media playback (HLS VOD, Live, DASH) and manage track selection? → 完整示例_1.md
*   Manage the complete player lifecycle, state transitions, and error handling logic? → 完整示例_2.md

## Detailed Description

### 完整示例_1.md 完整示例_1.md
This document provides a complete Cangjie code example demonstrating how to implement streaming media playback using the AVPlayer class. It covers callback registration for state changes and errors, specific implementations for HLS Video on Demand (VOD), HLS Live streams, and DASH video playback, including track selection logic. The code illustrates initializing the player, setting URLs, handling surface IDs for display, and managing audio/video tracks via selectTrack and deselectTrack. This tutorial is intended for developers using the Cangjie language and MediaKit to build video applications.

### 完整示例_2.md 完整示例_2.md
This document provides a complete code example demonstrating the implementation and lifecycle management of an AVPlayer instance. It covers configuring the media source with custom HTTP headers, setting playback strategies, and registering various callback handlers. Key sections include the StateChangeCallback, which manages the player's state machine transitions from initialization to release, and error handling logic to reset the player upon failure. The example also illustrates handling seek completion, buffering updates, and frame rendering events. This tutorial-style snippet is intended for developers integrating media playback, requiring an understanding of asynchronous callbacks and the player's operational states.