# 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行

本目录详细阐述了仓颉与ArkTS互操作时的线程执行要求，重点在于确保逻辑必须在ArkTS运行时绑定的系统线程上执行，以避免`JSThreadMisMatch`异常。内容涵盖线程绑定规则、检测方法、任务调度策略以及模块注册实现。核心关键词包括线程绑定、JSContext、互操作、JSThreadMisMatch、JSModule。适合具备多线程及互操作基础的仓颉开发者和HarmonyOS应用开发者阅读，旨在帮助开发者安全高效地进行跨语言调用。

## 快速导航

您想了解什么？

- 了解线程绑定规则、异常预防及线程切换策略 → 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_1.md
- 学习模块注册、异步函数导出及runtime.function封装 → 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_2.md

## 详细描述

### 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_1.md 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_1.md
这份文档旨在规范仓颉与ArkTS互操作时的线程执行要求，防止`JSThreadMisMatch`异常。主要内容包括线程绑定规则、检测方法及线程切换策略。核心要点是仓颉调用ArkTS接口必须在ArkTS运行时绑定的系统线程上执行。文档详细介绍了使用`JSContext.isInBindThread()`判断当前线程状态，以及利用`JSContext.postJSTask`或`spawn(UIThread)`将任务调度至正确线程的解决方案，并辅以正反代码示例。这是一份面向仓颉开发者的技术指南，要求读者具备多线程及互操作基础知识。

### 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_2.md 仓颉与_ArkTS_互操作逻辑要求在与_ArkTS_运行时绑定的系统线程上执行_2.md
该文档概述了仓颉与ArkTS编程语言之间互操作的线程要求，特别规定逻辑必须在绑定到ArkTS运行时的系统线程上执行。它涵盖了使用`JSModule`进行模块注册的技术实现，详细说明了如何导出异步函数（如`addNumberAsync`）。关键信息包括演示使用`runtime.function`包装原生函数的代码示例，以确保它们遵守必要的线程约束，从而实现安全的跨语言调用。这份技术参考资料适合从事HarmonyOS应用开发的开发者，需要具备仓颉和ArkTS运行时环境的先验知识，以有效管理系统线程绑定和模块导出。