# interface_LogValue

This directory contains comprehensive API documentation for the `LogValue` interface within the Cangjie programming language, designed to facilitate data serialization for logging purposes. It covers the fundamental interface definition alongside its extensions for a wide range of standard types, including primitive values like `Bool` and `Int64`, as well as complex structures such as `Array`, `HashMap`, and `Duration`. The documents detail the `writeTo` method, which is essential for writing data to a `LogWriter` instance. This resource is primarily suitable for Cangjie developers who are implementing or extending logging capabilities for their applications and need to understand how to serialize both built-in and custom data types effectively. By leveraging these interfaces, developers can ensure their data structures are compatible with logging streams.

## Quick Navigation

What do you want to learn?

*   Understand the core `LogValue` interface definition and how basic types (Bool, String, Int64, Exception, etc.) implement serialization? → interface_LogValue_8more.md
*   Learn about specific implementations for complex data structures, collections, and time measurements (Duration, Array, HashMap, TreeMap, Option)? → extend_Duration_LogValue_5more.md

## Detailed Description

### extend_Duration_LogValue_5more.md extend_Duration_LogValue_5more.md
This API reference document details the implementation of the LogValue interface for various Cangjie standard types to enable data serialization. It covers extensions for Duration, Array, HashMap, TreeMap, and Option, specifying the syntax and inheritance hierarchy for each. Key information includes the public writeTo function, which accepts a LogWriter instance to serialize the specific type to a stream. Generic types like Array and Map require their type parameters to also implement LogValue. This documentation is intended for Cangjie developers seeking to integrate logging capabilities for complex data structures and time measurements.

### interface_LogValue_8more.md interface_LogValue_8more.md
This API documentation defines the LogValue interface, which enables type serialization to log output targets in the Cangjie language. The document details the core interface definition, specifically the writeTo method, and lists extensions for standard types including Bool, Exception, Int64, Float64, String, and DateTime. Key information highlights that implementing this interface allows types to be written to a LogWriter instance. This reference guide targets developers integrating logging capabilities who need to understand how built-in types support serialization. Keywords: Cangjie, LogValue, LogWriter, serialization, interface, API.