<!-- cj-doc kind="api-member" level="6" id="std.unittest.prop_test.interface.randomsource.suggestint8" parent="std.unittest.prop_test.interface.randomsource" -->
# RandomSource.suggestInt8

[← RandomSource](index.md)

本页汇总 4 个同名重载；先按签名选择，再读取对应契约。

## func suggestInt8()

### 签名

```cangjie role=signature
public open func suggestInt8(): Int8
```

获取一个 Int8 类型的伪随机数。

### 契约

返回值：

- Int8 - 一个 Int8 类型的伪随机数。

## func suggestInt8(Int8, Int8)

### 签名

```cangjie role=signature
func suggestInt8(l: Int8, r: Int8): Int8
```

获取一个 Int8 类型的伪随机数。

### 契约

参数：

- l: Int8 - 可生成范围的最小值。
- r: Int8 - 可生成范围的最大值。

返回值：

- Int8 - 一个 Int8 类型的伪随机数。

## func suggestInt8()

适用扩展：[extend Random](extensions/extend-random.md)。

### 签名

```cangjie role=signature
public open func suggestInt8(): Int8
```

获取一个 Int8 类型的伪随机数。

### 契约

返回值：

- Int8 - 一个 Int8 类型的伪随机数。

## func suggestInt8(Int8, Int8)

适用扩展：[extend Random](extensions/extend-random.md)。

### 签名

```cangjie role=signature
func suggestInt8(l: Int8, r: Int8): Int8
```

获取一个 Int8 类型的伪随机数。

### 契约

参数：

- l: Int8 - 可生成范围的最小值。
- r: Int8 - 可生成范围的最大值。

返回值：

- Int8 - 一个 Int8 类型的伪随机数。
