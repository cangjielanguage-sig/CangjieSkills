<!-- cj-doc kind="api-member" level="6" id="stdx.compress.tar.enum.tarentryformat.operator-ne" parent="stdx.compress.tar.enum.tarentryformat" -->
# TarEntryFormat.!=

[← TarEntryFormat](index.md)

## 签名

```cangjie role=signature
operator func !=(rhs: TarEntryFormat): Bool
```

判断当前 tar 文件头部格式枚举实例是否与传入的 tar 文件头部格式枚举实例不相等。

## 参数

- rhs: TarEntryFormat - 要比较的 tar 文件头部格式枚举实例。

## 返回值

- Bool - 如果两个 tar 文件头部格式枚举实例不相等，则返回 true；否则返回 false。

