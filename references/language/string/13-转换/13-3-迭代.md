<!-- cj-doc kind="guide-leaf" level="5" id="language.string.13-转换.13-3-迭代" parent="language.string.13-转换" -->
# 13.3 迭代

[← 13. 转换](index.md)

`func iterator(): Iterator<Byte>   // 按字节迭代`：迭代。

```cangjie cjtest=syntax id=syntax-273ac11136-1 form=unit
func iterator(): Iterator<Byte>   // 按字节迭代
func runes(): Iterator<Rune>      // 按字符（Rune）迭代
```

```cangjie cjtest=syntax id=syntax-273ac11136-2 form=stmt
// 按字符迭代
for (r in "Hello 仓颉".runes()) {
    print(r)
}

// 按字节迭代
for (b in "Hello") {
    print(b)  // 输出 UTF-8 字节值
}
```
