# argopt_package_function

This directory contains the essential API reference documentation for the `argopt_package_function`, specifically focusing on the `parseArguments` function. It is a critical resource for developers and software engineers tasked with building Command Line Interface (CLI) tools. The documentation provides a deep dive into parsing command-line arguments based on defined specifications, covering the function signature and supported parsing modes for both short and long options. It details specific behaviors for argument values and option grouping, including the handling of RequiredValue, OptionalValue, and NoValue modes. Additionally, it addresses the return type ParsedArguments and exceptions like ArgumentParseException. This guide is suitable for those needing to understand edge cases such as the `--` terminator, short option grouping precedence, and duplicate value overwriting, ensuring robust and effective CLI implementation.

## Quick Navigation

**What do you want to learn?**

*   Understand the core API reference for the `parseArguments` function → argopt_package_function.md
*   Learn about parsing modes (short/long options) and value handling → argopt_package_function.md
*   Handle edge cases like the `--` terminator or option grouping → argopt_package_function.md
*   Manage exceptions and callbacks in CLI tools → argopt_package_function.md

## Detailed Description

### argopt_package_function.md argopt_package_function.md
This document provides API reference documentation for the parseArguments function, which parses command-line arguments based on defined specifications. It covers the function signature, supported parsing modes for short and long options, and specific behaviors for argument values and option grouping. Key information includes the handling of RequiredValue, OptionalValue, and NoValue modes, the return type ParsedArguments, and exceptions like ArgumentParseException. It also explains edge cases such as the -- terminator, short option grouping precedence, and duplicate value overwriting. Intended for developers building CLI tools, this guide explains how to handle callbacks and illegal options effectively.