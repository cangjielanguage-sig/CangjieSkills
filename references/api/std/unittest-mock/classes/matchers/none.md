<!-- cj-doc kind="api-member" level="7" id="std.unittest.mock.class.matchers.none" parent="std.unittest.mock.class.matchers.extension.extend-matchers" -->
# Matchers.none

[← extend Matchers](extensions/extend-matchers.md)

## 签名

```cangjie role=signature
public static func none(): NoneMatcher
```

过滤值为 `None` 的入参值。

## 契约

返回值：

- NoneMatcher - `None` 值匹配器。
