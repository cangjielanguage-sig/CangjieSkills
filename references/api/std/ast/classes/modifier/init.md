<!-- cj-doc kind="api-member" level="6" id="std.ast.class.modifier.init" parent="std.ast.class.modifier" -->
# Modifier.init

[← Modifier](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 Modifier 对象。

## init(Token)

### 签名

```cangjie role=signature
public init(keyword: Token)
```

构造一个 Modifier 对象。

### 契约

参数：

- keyword: Token - 将要构造 Modifier 类型的词法单元。
