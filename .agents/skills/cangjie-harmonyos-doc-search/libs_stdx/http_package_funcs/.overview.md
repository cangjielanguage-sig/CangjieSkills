# http_package_funcs

This directory contains API reference documentation for utility functions within an HTTP package specifically designed for the Cangjie programming language. It focuses on essential server-side operations, including handling request responses, managing specific error codes, and facilitating protocol upgrades such as CONNECT requests. The documentation covers functions for sending specific error responses, managing 404 "Not Found" states, and establishing low-level socket connections for raw data transmission. It is primarily suitable for backend developers implementing HTTP servers who require convenient error handling mechanisms or need direct socket access for advanced protocol implementations. By providing these utilities, the package aims to streamline the development of robust network applications, allowing developers to focus on business logic rather than low-level protocol details. The reference guide ensures that users understand the behavior of functions like `upgrade`, including how it handles headers and potential exceptions, while also offering structural descriptions of the code organization.

## Quick Navigation

What do you want to do?

*   **Handle HTTP errors and status codes**: If you need to send specific error codes to clients or manage standard 404 Not Found responses efficiently → http_package_funcs.md
*   **Manage protocol transitions**: If you are implementing advanced features like CONNECT requests and need to return a StreamingSocket for raw data transmission → http_package_funcs.md
*   **Understand low-level socket behavior**: If you want to learn about header management, request body clearing, and exception handling during protocol upgrades → http_package_funcs.md

## Detailed Description

### http_package_funcs.md http_package_funcs.md

This document serves as API reference documentation for utility functions within an HTTP package, specifically designed for handling request responses and protocol upgrades in the Cangjie language. It details three primary functions: `handleError` for sending specific error codes, `notFound` for 404 responses, and `upgrade` for managing protocol transitions like CONNECT requests. Key information includes the upgrade function's ability to return a StreamingSocket for raw data transmission, its behavior of sending headers first and clearing the request body, and its potential to throw HttpException. This reference guide targets backend developers implementing HTTP servers who need convenient error handling or low-level socket access for advanced protocols.