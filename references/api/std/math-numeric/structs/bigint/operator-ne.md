<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-ne" parent="std.math.numeric.struct.bigint" -->
# BigInt.!=

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func !=(that: BigInt): Bool
```

判不等运算。

## 契约

参数：

- that: BigInt - 判不等运算的另一个 BigInt。

返回值：

- Bool - 判不等的结果。不等返回 true，相等返回 false。
