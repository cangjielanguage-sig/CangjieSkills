# interface_LengthProp

This directory contains essential API documentation for the `LengthProp` interface and its implementations in the Cangjie language, designed specifically for the ArkUI framework. It comprehensively covers extensions for both `Int64` and `Float64` types, enabling developers to seamlessly interpret numeric values as structured length units such as px, vp, fp, lpx, and percent. The documentation details read-only properties for accessing raw values and unit types, highlighting dependencies on SystemCapability.ArkUI.ArkUI.Full starting from version 22. This resource is highly suitable for ArkUI developers and Cangjie programmers who need to handle unit conversions, manage layout dimensions, and utilize length properties programmatically within their user interface designs. By defining clear interfaces for these types, the documentation ensures type safety and consistency in UI development workflows.

## Quick Navigation

**What do you want to learn?**

*   Explore the interface definition and see how Float64 types implement length properties for ArkUI → interface_LengthProp_7more.md
*   Discover how Int64 types are extended to support the LengthProp and Length interfaces → extend_Int64_LengthProp_Length.md

## Detailed Description

The following files provide specific implementation details and API references for the LengthProp interface and its type extensions, ensuring developers have the necessary information to implement robust UI measurement logic.

### extend_Int64_LengthProp_Length.md extend_Int64_LengthProp_Length.md
This API documentation details the extension of the Int64 type to implement LengthProp and Length interfaces in the Cangjie language. The document defines read-only properties that allow integer values to be interpreted as specific length units, including px, vp, fp, lpx, and percent. It also outlines properties for accessing the raw numeric value and the unit type. Key takeaways include the read-only nature of these properties and their dependency on SystemCapability.ArkUI.ArkUI.Full starting from version 22. This reference is intended for ArkUI developers working with Cangjie who need to convert or utilize integer values as structured length measurements in user interface design.

### interface_LengthProp_7more.md interface_LengthProp_7more.md
This document serves as API reference documentation for the Cangjie language, defining the LengthProp interface and its implementation via Float64 extension within the ArkUI framework. The content details the interface definition with read-only properties for various length units (px, vp, fp, percent, lpx) and describes the extension of the Float64 type to support these properties alongside value and unitType. Key information includes that all properties are read-only, return Length types, and are available starting from system version 22. This targets Cangjie developers working on ArkUI applications who need to handle unit conversions or length properties programmatically.