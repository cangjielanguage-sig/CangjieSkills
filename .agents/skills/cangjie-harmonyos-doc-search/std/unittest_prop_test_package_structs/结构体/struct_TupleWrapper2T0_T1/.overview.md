# struct_TupleWrapper2T0_T1

This directory provides comprehensive documentation for the Cangjie `TupleWrapper2<T0, T1>` struct, a generic utility designed to encapsulate two-element tuples. It covers the fundamental struct definition, including its constructor and the `apply` method for executing closures on the tuple. The documentation also details extensions for standard interfaces like `ToString`, `Equatable`, and `IndexAccess`, enabling string representation, equality checks, and indexed element retrieval. Furthermore, it includes specialized extensions for property-based testing, specifically the `Arbitrary` and `Shrink` interfaces for random generation and value reduction. This content is intended for Cangjie developers and QA engineers working with data structures or implementing automated testing frameworks.

## Quick Navigation

What do you want to learn?

*   Understand the core struct, constructor, and standard interfaces (ToString, Equatable, IndexAccess) → struct_TupleWrapper2T0_T1_7more.md
*   Implement property-based testing logic (Arbitrary, Shrink) for random generation and shrinking → extendT0_T1_TupleWrapper2T_2more.md

## Detailed Description

### extendT0_T1_TupleWrapper2T_2more.md extendT0_T1_TupleWrapper2T_2more.md
This API documentation details the implementation of the Arbitrary and Shrink interfaces for the TupleWrapper2 struct in Cangjie. It covers two primary extensions: one for random value generation and another for value reduction. The Arbitrary extension provides a static arbitrary function returning a generator for random tuples, while the Shrink extension overrides the shrink function to return an iterable of reduced tuples. Both implementations require generic types T0 and T1 to satisfy their respective interface constraints. This reference material targets developers implementing property-based testing who need to understand how two-element tuples are generated and shrunk.

### struct_TupleWrapper2T0_T1_7more.md struct_TupleWrapper2T0_T1_7more.md
This document provides API reference documentation for the Cangjie struct TupleWrapper2<T0, T1>, designed to encapsulate a two-element generic tuple. It covers the struct definition, constructor, and the apply method for executing closures on the tuple. Additionally, it details extensions implementing standard interfaces: ToString for string representation, Equatable for equality comparisons, and IndexAccess for retrieving elements by index. Key takeaways include the struct's generic nature and its support for functional operations and dynamic access. This guide is intended for Cangjie developers seeking to manipulate or wrap tuple data structures within their applications.