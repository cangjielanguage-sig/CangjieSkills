# cj-environment

This directory contains a specialized technical guide focused on the ArkUI Environment singleton within the Cangjie development ecosystem. It comprehensively covers the methodologies for querying and utilizing device parameters—such as system language, color mode, font scale, and layout direction—to facilitate adaptive application behavior. The guide details the architectural flow where Environment writes read-only system variables into AppStorage, making them accessible to developers through `Environment.envProp`, `@StorageProp`, or `AppStorage.prop`. It explains the critical update chain from Environment to AppStorage and finally to the Component. This resource is specifically tailored for Cangjie developers aiming to synchronize their UI components and application logic with dynamic system states, though it requires a solid foundational knowledge of AppStorage.

## Quick Navigation

*   **What do you want to learn?**
    *   How to utilize the ArkUI Environment singleton for adaptive behavior → cj-environment.md
    *   The specific mechanisms for querying device parameters like language and color mode → cj-environment.md
    *   How to handle built-in parameters such as fontScale and layoutDirection → cj-environment.md
*   **What do you want to do?**
    *   Implement read-only system variable access in your application → cj-environment.md
    *   Trace the update chain from Environment to AppStorage to Component → cj-environment.md
    *   Apply environment parameters to both UI components and logic → cj-environment.md

## Detailed Description

### cj-environment.md cj-environment.md
This technical guide explains how to use the ArkUI Environment singleton to query device parameters like language and color mode for adaptive application behavior. It details built-in parameters such as fontScale and layoutDirection, and demonstrates usage scenarios for both UI components and application logic. Key takeaways include that Environment writes read-only system variables to AppStorage, accessed via Environment.envProp and @StorageProp or AppStorage.prop, following the update chain Environment to AppStorage to Component. Targeted at Cangjie developers, this document requires prior knowledge of AppStorage.