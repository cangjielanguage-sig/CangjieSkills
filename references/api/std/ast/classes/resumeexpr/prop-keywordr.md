<!-- cj-doc kind="api-member" level="6" id="std.ast.class.resumeexpr.prop-keywordr" parent="std.ast.class.resumeexpr" -->
# ResumeExpr.keywordR

[← ResumeExpr](index.md)

## 签名

```cangjie role=signature
public mut prop keywordR: Token
```

获取或设置 `resume` 关键字的词法单元。

类型：Token

## 异常

- ASTException — 当提供的 Token 不是 `resume` 关键字时抛出。

