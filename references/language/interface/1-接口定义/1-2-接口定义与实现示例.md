<!-- cj-doc kind="guide-leaf" level="5" id="language.interface.1-接口定义.1-2-接口定义与实现示例" parent="language.interface.1-接口定义" -->
# 1.2 接口定义与实现示例

[← 1. 接口定义](index.md)

类型通过 `<: Interface` 声明实现接口，并提供接口要求的公开成员；接口引用可统一调用不同实现。

```cangjie cjtest=syntax id=syntax-971e96ef68-1 form=unit
interface Flyable {
    func fly(): Unit
}

class Bird <: Flyable {
    public func fly(): Unit {
        println("Bird flying")
    }
}

class Airplane <: Flyable {
    public func fly(): Unit {
        println("Airplane flying")
    }
}

// 接口作为参数类型，实现多态
func fly(item: Flyable): Unit {
    item.fly()
}

main() {
    fly(Bird())      // 输出：Bird flying
    fly(Airplane())  // 输出：Airplane flying
}
```

## 已验证示例

调用方只依赖接口类型；新增实现不需要修改 `describe`，即可复用同一调用路径。

```cangjie cjtest=run id=language.interface-polymorphism.run form=unit timeout=20s
package interface_polymorphism_example

interface Named {
    func name(): String
}

class User <: Named {
    let value: String

    public init(value: String) {
        this.value = value
    }

    public func name(): String {
        return value
    }
}

func describe(item: Named): Unit {
    println("user=${item.name()}")
}

main(): Unit {
    describe(User("Ada"))
}
```

```text cjtest=expect for=language.interface-polymorphism.run stream=stdout match=exact
user=Ada
```
