# 不建议借助StorageLink的双向同步机制实现事件通知

This directory provides comprehensive technical guidance and best practices for optimizing event notification mechanisms within HarmonyOS applications. It specifically addresses the anti-pattern of using `@StorageLink` and AppStorage's bidirectional synchronization for event handling, highlighting the significant performance overhead and unnecessary UI refreshes that result from state-based notifications. The content covers the theoretical drawbacks of this approach and offers a practical alternative using the `emit` interface for event subscription and callbacks. By decoupling components through the Emitter pattern, developers can achieve better performance and code readability. This resource is essential for HarmonyOS developers and architects aiming to refine state management strategies and build efficient, responsive applications using ArkTS.

## Quick Navigation

**What do you want to learn?**
*   Understand the performance drawbacks and anti-patterns of using `@StorageLink` for event notifications → 不建议借助StorageLink的双向同步机制实现事件通知_1.md
*   Learn the recommended alternative approach using the Emitter mechanism for decoupled communication → 不建议借助StorageLink的双向同步机制实现事件通知_2.md

**What do you want to do?**
*   Optimize state management to avoid unnecessary UI refreshes and improve application performance → 不建议借助StorageLink的双向同步机制实现事件通知_1.md
*   Implement efficient event communication between parent and child components in ArkTS → 不建议借助StorageLink的双向同步机制实现事件通知_2.md

## Detailed Description

### 不建议借助StorageLink的双向同步机制实现事件通知_1.md 不建议借助StorageLink的双向同步机制实现事件通知_1.md
This guide advises developers against using `@StorageLink` and AppStorage's two-way synchronization for event notifications due to performance overhead. Main topics include the drawbacks of state-based notifications, specifically how variable changes trigger unnecessary UI refreshes across multiple components. Key information suggests using the emit interface for event subscription and callbacks instead, which reduces overhead and improves readability, though it is unsupported in the Previewer. This document targets HarmonyOS developers looking to optimize application performance and state management efficiency.

### 不建议借助StorageLink的双向同步机制实现事件通知_2.md 不建议借助StorageLink的双向同步机制实现事件通知_2.md
This document provides an ArkTS code example demonstrating the recommended approach for implementing event notifications between components using the Emitter mechanism, serving as an alternative to the discouraged StorageLink bidirectional synchronization. It covers component interaction patterns, event emission and subscription logic, and state management within a Grid layout. The code illustrates how a parent EntryView emits specific events upon user interaction, while child TapImage components listen for these events to dynamically update their UI state, such as changing border colors. Intended for HarmonyOS developers seeking efficient decoupled communication methods in ArkTS applications. Keywords: ArkTS, Event Emitter, Component Communication, State Management, HarmonyOS Development.