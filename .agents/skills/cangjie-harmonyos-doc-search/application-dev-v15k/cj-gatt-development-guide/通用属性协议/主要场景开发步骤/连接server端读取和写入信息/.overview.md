# 连接server端读取和写入信息

This directory provides a comprehensive technical guide for implementing a Bluetooth Low Energy (BLE) GATT client using the Cangjie language within the HarmonyOS ecosystem. It covers the complete lifecycle of client-server communication, including establishing connections, discovering services, and performing read and write operations on characteristics and descriptors. The documentation emphasizes connection state management, UUID validation, and proper resource cleanup. It is suitable for HarmonyOS developers who possess a foundational understanding of BLE protocols and Cangjie syntax and are looking to build robust Bluetooth connectivity features in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the complete BLE GATT client workflow and initialization → 连接server端读取和写入信息_1.md
*   Learn how to initiate connections and discover services → 连接server端读取和写入信息_2.md
*   Implement descriptor writing and manage disconnection/cleanup → 连接server端读取和写入信息_3.md

## Detailed Description

### 连接server端读取和写入信息_1.md 连接server端读取和写入信息_1.md
This document serves as a technical tutorial for implementing a Bluetooth Low Energy (BLE) GATT client using the Cangjie language. It outlines the complete workflow for connecting to a GATT server, reading and writing characteristic values and descriptors, and managing the connection lifecycle. The content includes a detailed code example featuring the GattClientManager class, which demonstrates initializing BLE objects, validating services via UUIDs, and handling connection state change callbacks. Targeted at HarmonyOS developers, this guide requires prerequisite knowledge of BLE protocols and Cangjie syntax to effectively manage client-server communication.

### 连接server端读取和写入信息_2.md 连接server端读取和写入信息_2.md
This document serves as a technical guide for implementing a BLE GATT client to connect to a server and perform data operations. It details the workflow from initiating a connection using startConnect to discovering services via discoverServices. Key sections include reading and writing characteristic values and reading descriptor values. The code highlights the importance of verifying connection states and service availability before executing operations. It is intended for developers working with Bluetooth connectivity, providing specific function implementations and error handling patterns for GATT interactions.

### 连接server端读取和写入信息_3.md 连接server端读取和写入信息_3.md
This document provides a code example for writing to a BLE GATT server descriptor and managing client disconnection. It details the writeDescriptorValue function, which validates connection status and writes data to a specific descriptor, and the stopConnect function, which handles disconnecting, unregistering callbacks, and closing the client. Additionally, it includes a ChangeStateCb class to log connection state changes. Key takeaways involve the importance of checking connection states before operations and proper resource cleanup. The content targets developers working on Bluetooth connectivity, specifically those implementing GATT client profiles, and assumes familiarity with BLE concepts and error handling.