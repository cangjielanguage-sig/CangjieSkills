# 开发步骤

本目录提供了使用 CryptoArchitectureKit 进行 HMAC 消息认证码计算的详细开发指南。内容涵盖了基于仓颉语言和 SHA256 算法的实现方法，重点介绍了对称密钥生成、Mac 实例创建、初始化、数据更新及结果获取等核心步骤。文档特别区分了“一次性传入数据”和“分段传入数据”两种处理模式，以满足不同数据量级的需求。适合仓颉语言开发者及加密应用开发人员参考，旨在帮助其掌握流式数据处理及消息认证码生成的关键技术。

## 快速导航

你想了解哪种数据处理方式？

*   想学习如何处理大数据量或流式数据（分段传入） → 以分段传入数据获取消息认证码计算结果为例_655cf798.md
*   想查看完整的开发指南，对比一次性传入与分段传入的区别 → 开发步骤_4more_75ff5a00.md

## 详细描述

### 以分段传入数据获取消息认证码计算结果为例_655cf798.md 以分段传入数据获取消息认证码计算结果为例
本文档演示了在仓颉语言中，通过分段传入数据使用HMAC算法和SHA256摘要计算消息认证码的方法。主要包含对称密钥生成和同步HMAC计算两个函数，展示了初始化、循环分段更新数据及获取最终结果的完整流程。关键信息包括利用`createMac`、`initialize`、`update`及`doFinal`接口处理数据，重点说明了数据可分段传入且无长度限制。面向使用CryptoArchitectureKit进行加密开发的仓颉程序员，适用于需处理大数据量或流式数据的消息认证场景。

### 开发步骤_4more_75ff5a00.md 开发步骤
本文档是一份使用CryptoArchitectureKit实现HMAC操作的开发指南，旨在指导开发者如何计算消息认证码。文档涵盖了两种主要的数据输入方式：一次性传入所有数据和分段传入数据，并明确指出两者计算结果无差异。内容详细介绍了创建Mac实例、生成对称密钥、初始化、更新数据及获取结果的完整步骤，并提供了基于SHA256算法的Cangjie语言代码示例。该指南面向开发者，帮助其根据实际数据量需求选择合适的数据处理策略，掌握关键API的使用方法。