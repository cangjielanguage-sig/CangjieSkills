<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.enum.websocketframetype.operator-eq" parent="stdx.net.http.enum.websocketframetype" -->
# WebSocketFrameType.==

[← WebSocketFrameType](index.md)

## 签名

```cangjie role=signature
public override operator func ==(that: WebSocketFrameType): Bool
```

判断枚举值是否相等。

## 契约

参数：

- that: WebSocketFrameType - 被比较的枚举值。

返回值：

- Bool - 当前实例与 `that` 相等返回 `true`，否则返回 `false`。
