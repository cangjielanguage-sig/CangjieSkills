<!-- cj-doc kind="api-member" level="6" id="std.core.struct.defaulthasher.init" parent="std.core.struct.defaulthasher" -->
# DefaultHasher.init

[← DefaultHasher](index.md)

## 签名

```cangjie role=signature
public init(res!: Int64 = 0)
```

构造函数，创建一个 DefaultHasher。

## 契约

参数：

- res!: Int64 - 初始哈希值，默认为 0。
