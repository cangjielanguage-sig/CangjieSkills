<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.struct.derblob.operator-eq" parent="stdx.crypto.x509.struct.derblob" -->
# DerBlob.==

[← DerBlob](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: DerBlob): Bool
```

判等。

## 契约

参数：

- other: DerBlob - 被比较的 DerBlob 对象。

返回值：

- Bool - 若对象相同，返回 true；否则，返回 false。
