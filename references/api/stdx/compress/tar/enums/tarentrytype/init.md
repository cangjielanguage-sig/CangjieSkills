<!-- cj-doc kind="api-member" level="6" id="stdx.compress.tar.enum.tarentrytype.init" parent="stdx.compress.tar.enum.tarentrytype" -->
# TarEntryType.init

[← TarEntryType](index.md)

## 签名

```cangjie role=signature
Unknown(UInt8)
```

构造一个未知类型条目枚举实例，用于保留无法识别或自定义的 `typeflag` 字节值。

