# cj-start-intent-panel

This directory serves as a specialized technical resource for developers and system architects focusing on intent-based application launching mechanisms. It details the implementation of the `startAbilityByType` interface, a sophisticated feature that decouples application invocation from specific app IDs. Instead, it leverages business categories—such as navigation, finance, and mail—to present users with a vertical domain panel for app selection.

Key topics covered include:
*   **Vertical Domain Panels:** UI components for category-based app selection.
*   **Parameter Mapping:** Technical tables linking input types to system features.
*   **Protocol Support:** Implementation of `mailto` and file handling protocols.

The documentation includes critical technical assets, such as comprehensive mapping tables that link input parameters (type, sceneType) to specific target application configurations (linkFeature). This guide is essential for those building cross-app capabilities, ensuring seamless integration and a flexible user experience within the operating system's intent framework.

## Quick Navigation

**What do you want to learn?**

*   **Understand the core concept:** Learn how to launch applications based on business type rather than specific IDs using the vertical domain panel → cj-start-intent-panel.md
*   **Configure parameters:** Access the detailed mapping table linking input parameters (type, sceneType) to target application configurations (linkFeature) → cj-start-intent-panel.md
*   **Implement specific protocols:** Find instructions for launching email apps using the `mailto` protocol or opening files via `startAbility` → cj-start-intent-panel.md
*   **Developer integration:** Review the technical guide intended for implementing cross-app capabilities and intent-based system integration → cj-start-intent-panel.md

## Detailed Description

### cj-start-intent-panel.md
This document provides a technical guide for developers on launching applications by business type rather than specific app IDs. It focuses on the `startAbilityByType` interface, which displays a vertical domain panel for categories like navigation, finance, and mail, allowing users to select an appropriate app. Key information includes a detailed mapping table linking input parameters (type, sceneType) to target application configurations (linkFeature). The guide also briefly covers launching email apps via the mailto protocol and opening files using startAbility. This resource is intended for developers implementing cross-app capabilities and intent-based system integration.