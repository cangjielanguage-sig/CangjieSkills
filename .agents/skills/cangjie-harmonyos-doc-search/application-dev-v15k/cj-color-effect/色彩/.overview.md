# 色彩

This directory contains technical documentation and code examples focused on implementing color gradient effects within the HarmonyOS ArkUI framework using the Cangjie programming language. It covers essential gradient types including linear, radial, and angular (sweep) gradients, providing practical guidance on configuring parameters like angles, color stops, and repeating behaviors. These resources are designed for developers looking to enhance the visual aesthetics of their UI components through advanced styling techniques. By utilizing specific APIs such as `.radialGradient()` and `sweepGradient`, users can create dynamic and visually appealing interfaces.

## Quick Navigation

**What do you want to learn?**

*   Apply radial gradient effects to components → 为组件添加径向渐变效果.md
*   Apply angular (sweep) gradient effects to components → 为组件添加角度渐变效果.md
*   Understand gradient concepts and apply linear gradients → 色彩_2more.md

## Detailed Description

### 为组件添加径向渐变效果.md 为组件添加径向渐变效果.md
This document provides a practical code example demonstrating how to apply radial gradient effects to UI components using the Cangjie language. The main topic is the implementation of the `.radialGradient()` modifier within a Grid layout. Key information includes configuring the gradient center point, radius, and color stops, as well as controlling the repeating behavior. The code illustrates four distinct scenarios, comparing different center coordinates and the visual difference between repeating and non-repeating gradients. Detailed comments explain how color ratios map to specific radius ranges. This tutorial is intended for HarmonyOS developers using the Cangjie UI framework who wish to enhance component visuals with advanced styling techniques.

### 为组件添加角度渐变效果.md 为组件添加角度渐变效果.md
This document provides a code example demonstrating how to implement angular (sweep) gradient effects on UI components using the Cangjie language. It covers the `sweepGradient` method, focusing on configuring center coordinates, start and end angles, color stops, and the repeating behavior. The code illustrates four distinct scenarios within a Grid layout, comparing center positions and the visual impact of the `repeating` boolean flag. Detailed comments explain the mathematical mapping of color stops to angle ranges (0-360 degrees). This tutorial targets HarmonyOS developers using the Cangjie language and UIKit to enhance UI aesthetics.

### 色彩_2more.md 色彩_2more.md
This document explains how to apply color gradient effects to UI components using specific APIs in the ArkUI framework with Cangjie code. It covers an introduction to gradient concepts, a table of available gradient APIs including linear, sweep, and radial gradients, and a practical code example demonstrating linear gradients. The key takeaway is the usage of the linearGradient interface to create smooth color transitions by configuring parameters like angle, color stops, and repeating behavior. Targeted at developers using the Cangjie language and ArkUI framework, this guide serves as a tutorial for enhancing visual aesthetics through background styling.