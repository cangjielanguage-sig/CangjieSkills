<!-- cj-doc kind="api-member" level="6" id="std.net.struct.socketdomain.operator-eq" parent="std.net.struct.socketdomain" -->
# SocketDomain.==

[← SocketDomain](index.md)

## 签名

```cangjie role=signature
public operator func ==(r: SocketDomain): Bool
```

比较两个 SocketDomain 实例是否相等。

## 契约

参数：

- r: SocketDomain - 参与比较的 SocketDomain 实例。

返回值：

- Bool - 当二者代表的 Int32 值相等时，返回 `true`；否则，返回 `false`。
