<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.prop-sign" parent="std.math.numeric.struct.bigint" -->
# BigInt.sign

[← BigInt](index.md)

## 签名

```cangjie role=signature
public prop sign: Int64
```

获取此 BigInt 的符号。

## 契约

功能：获取此 BigInt 的符号。正数返回 1；0 返回 0；负数返回 -1。

类型：Int64
