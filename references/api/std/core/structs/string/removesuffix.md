<!-- cj-doc kind="api-member" level="6" id="std.core.struct.string.removesuffix" parent="std.core.struct.string" -->
# String.removeSuffix

[← String](index.md)

## 签名

```cangjie role=signature
public func removeSuffix(suffix: String): String
```

去除字符串的 suffix 后缀。

## 契约

参数：

- suffix: String - 待去除的后缀。

返回值：

- String - 去除后缀后得到的新字符串。
