<!-- cj-doc kind="guide-leaf" level="5" id="language.extend.4-访问规则.4-10-导出规则" parent="language.extend.4-访问规则" -->
# 4.10 导出规则

[← 4. 访问规则](index.md)

### 直接扩展
- 与被扩展类型**同包**时，当类型和所有泛型约束均为导出类型时导出
- **不同包**时，永不导出

### 接口扩展（与类型同包）
- 随类型一起导出，**无论**接口的访问级别如何

### 接口扩展（与类型不同包）
- 导出由所实现接口和泛型约束中**最低访问级别**决定
- 仅接口中声明的成员被导出
