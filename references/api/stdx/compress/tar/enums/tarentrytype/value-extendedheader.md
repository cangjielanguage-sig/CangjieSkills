<!-- cj-doc kind="api-member" level="6" id="stdx.compress.tar.enum.tarentrytype.value-extendedheader" parent="stdx.compress.tar.enum.tarentrytype" -->
# TarEntryType.ExtendedHeader

[← TarEntryType](index.md)

## 签名

```cangjie role=signature
ExtendedHeader
```

构造一个 PAX 扩展头类型枚举实例，对应 typeflag `'x'`，用于存储附加元数据。

