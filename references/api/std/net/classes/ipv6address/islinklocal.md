<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipv6address.islinklocal" parent="std.net.class.ipv6address" -->
# IPv6Address.isLinkLocal

[← IPv6Address](index.md)

## 签名

```cangjie role=signature
public func isLinkLocal(): Bool
```

判断此 IPv6Address 对象是不是链路本地地址。

## 契约

返回值：

- Bool - 返回 true 表示是链路本地地址，否则返回 false。
