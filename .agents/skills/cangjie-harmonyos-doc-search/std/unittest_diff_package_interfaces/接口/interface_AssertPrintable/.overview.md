# interface_AssertPrintable

This directory contains API reference documentation for the `AssertPrintable` interface within the Cangjie programming language's unit testing framework. It covers the core interface definition required for custom assertion printing and details specific implementations for various data types, including `Option<T>`, `String`, `Float16`, `Float32`, and `Float64`. These documents explain how to format test failure outputs using `PrettyPrinter`, handling nested differences, and managing prefixes for expected versus actual values. This resource is suitable for developers working on or extending the Cangjie testing framework who need to understand or customize assertion display logic.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and Float16 implementation → interface_AssertPrintable_4more.md
*   Learn about assertion printing for Option and String types → extendT_OptionT_AssertPrin_2more.md
*   Learn about assertion printing for Float32 and Float64 types → extend_Float32_AssertPrint_2more.md

## Detailed Description

### extendT_OptionT_AssertPrin_2more.md extendT_OptionT_AssertPrin_2more.md
This API reference document details extensions for the Option<T> and String types to implement the AssertPrintable interface in the Cangjie language. It covers two main sections: a generic extension for Option<T> (constrained by Equatable) and a specific extension for String. Both sections define a hasNestedDiff property and a pprintForAssertion function, which formats assertion results using a PrettyPrinter with specific prefixes and nesting levels. Targeted at developers working with unit testing frameworks, this documentation ensures proper display of test failures involving these types.

### extend_Float32_AssertPrint_2more.md extend_Float32_AssertPrint_2more.md
This API documentation file details the implementation of the AssertPrintable interface for Float32 and Float64 types within the Cangjie unit testing framework. The document is structured into two main sections, each extending a floating-point type to support assertion printing. It defines the hasNestedDiff property for checking nested diff levels and the pprintForAssertion function, which formats and prints results for @Assert and @Expect macros. Key takeaways include the specific parameters required for the pretty printer, such as prefixes for expected versus actual values and indentation levels. This reference guide targets developers who need to understand or customize how floating-point values are displayed during test failures.

### interface_AssertPrintable_4more.md interface_AssertPrintable_4more.md
This API reference document defines the AssertPrintable interface in the Cangjie language, which enables custom printing of assertion results for unit testing macros like @Assert and @Expect. The documentation covers the interface definition, the hasNestedDiff property for identifying nested differences, and the pprintForAssertion method used to format output. It also details a specific implementation extension for the Float16 type. Key information includes the method's parameters, such as the PrettyPrinter, prefixes for expected and actual values, and the nesting level. This guide is intended for developers working with the Cangjie unit testing framework who need to implement or understand assertion display logic.