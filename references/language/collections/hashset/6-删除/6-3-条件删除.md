<!-- cj-doc kind="guide-leaf" level="6" id="language.collections.hashset.6-删除.6-3-条件删除" parent="language.collections.hashset.6-删除" -->
# 6.3 条件删除

[← 6. 删除](index.md)

```cangjie cjtest=syntax id=syntax-a4ca5945cc-1 form=unit
func removeIf(predicate: (T) -> Bool): Unit
```

```cangjie cjtest=syntax id=syntax-a4ca5945cc-2 form=stmt
let set = HashSet<Int64>([1, 2, 3, 4, 5, 6])
set.removeIf { v => v % 2 == 0 }  // 删除偶数 → {1, 3, 5}
```

- 在 `predicate` 中修改 HashSet 会抛 `ConcurrentModificationException`
