# extend_UInt64_ThrowingOpUInt64

This directory provides comprehensive API reference documentation for the Cangjie language, specifically detailing the implementation of the `ThrowingOp` interface for the `UInt64` data type. It focuses on arithmetic and bitwise operations designed to enhance computational safety by throwing exceptions rather than defaulting to wrapping behavior when errors occur. The documentation covers a wide range of functions, including basic arithmetic like addition and multiplication, as well as bitwise shifts and negation. Key concepts include `OverflowException` and `OvershiftException` for explicit error handling. This resource is essential for Cangjie developers who require precise control over numerical edge cases and wish to implement robust error handling mechanisms in their applications to ensure data integrity. By utilizing these throwing operations, developers can prevent silent data corruption and manage errors explicitly, making it a critical resource for high-reliability systems.

## Quick Navigation

**What do you want to learn?**

*   **Arithmetic Operations:** Learn about functions like `throwingAdd`, `throwingMul`, and `throwingDiv` that throw `OverflowException` → extend_UInt64_ThrowingOpUI_7more.md
*   **Bitwise and Negation:** Learn about `throwingNeg`, `throwingShl`, and `throwingShr` with specific exception behaviors → func_throwingNeg_4more.md

## Detailed Description

The documentation in this directory is organized into two primary files, covering the core arithmetic interface and specific bitwise or negation operations.

### extend_UInt64_ThrowingOpUI_7more.md extend_UInt64_ThrowingOpUI_7more.md
This API reference document details the implementation of the `ThrowingOp` interface for the `UInt64` type in the Cangjie language, enabling arithmetic operations that throw exceptions on overflow. The content covers specific functions including `throwingAdd`, `throwingDec`, `throwingDiv`, `throwingInc`, `throwingMod`, and `throwingMul`. Key takeaways are that all operations return a `UInt64` result but throw an `OverflowException` if an overflow occurs, ensuring safer computation compared to standard wrapping behavior. This documentation targets Cangjie developers who require explicit error handling for arithmetic edge cases in their applications.

### func_throwingNeg_4more.md func_throwingNeg_4more.md
This document serves as API reference documentation for four Cangjie functions that utilize exception-throwing strategies for arithmetic and bitwise operations. It details the `throwingNeg`, `throwingShl`, `throwingShr`, and `throwingSub` functions, outlining their syntax, parameters, return values, and specific exception behaviors. Key information includes that all functions return `UInt64`, with negation and subtraction throwing `OverflowException` upon overflow, while shift operations throw `OvershiftException` if the shift count exceeds the operand's bit width. This reference is intended for Cangjie developers implementing safe integer operations requiring explicit error handling via exceptions.