<!-- cj-doc kind="api-member" level="7" id="std.core.interface.any.isascii" parent="std.core.interface.any.extension.extend-byte" -->
# Any.isAscii

[← extend Byte](extensions/extend-byte.md)

## 签名

```cangjie role=signature
public func isAscii(): Bool
```

判断 Byte 是否是在 Ascii 范围内。

## 契约

返回值：

- Bool - 如果 Byte 在 Ascii 范围内返回 true，否则返回 false。
