<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.class.jsonobject.init" parent="stdx.encoding.json.class.jsonobject" -->
# JsonObject.init

[← JsonObject](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

创建空 JsonObject。

## init(HashMap<String, JsonValue>)

### 签名

```cangjie role=signature
public init(map: HashMap<String, JsonValue>)
```

将指定的 HashMap 类型实例封装成 JsonObject 实例。

### 契约

参数：

- map: HashMap\<String, JsonValue> - data 数据。
