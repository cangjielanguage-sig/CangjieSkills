<!-- cj-doc kind="guide-leaf" level="5" id="language.class.1-类定义.1-7-成员函数" parent="language.class.1-类定义" -->
# 1.7 成员函数

[← 1. 类定义](index.md)

- **实例函数**：通过对象访问，可使用 `this`
- **静态函数**：`static func`，通过类型名访问，不能访问实例成员
- **抽象函数**：无函数体，仅在抽象类或接口中。隐式 `open`，须为 `public` 或 `protected`
- **限制**：同一个类中，静态方法和实例方法**不能同名**（即使参数不同）
