# cj-scroll-swipe-listitem

This directory hosts the API reference documentation for the `ListItem` component, a crucial element of the ArkUI framework tailored for the Cangjie programming language. It serves as a technical guide for developers aiming to construct scrollable lists, detailing the strict requirement that `ListItem` must be nested within a `List` component to function. The content covers the full lifecycle of component usage, from initialization and child component constraints to advanced attribute configuration. Specific attention is given to interactive features such as the `selectable` attribute and the `swipeAction` attribute, which enables the creation of responsive interfaces through start and end swipe gesture builders. Furthermore, the document explains the `onSelect` event for managing selection state changes. This resource is specifically suitable for Cangjie developers who need precise syntax definitions and practical implementation examples, including how to integrate `ListItem` effectively within `ForEach` loops to render dynamic data sets.

## Quick Navigation

What do you want to learn?

- Master the API reference and syntax definitions for the ListItem component in Cangjie → cj-scroll-swipe-listitem.md
- Understand the structural requirements, such as nesting within a List component → cj-scroll-swipe-listitem.md
- Configure interactive swipe gestures using the swipeAction attribute with custom builders → cj-scroll-swipe-listitem.md
- Handle user interactions through the onSelect event and selectable attributes → cj-scroll-swipe-listitem.md
- Examine a code example showing ListItem implementation inside a ForEach loop → cj-scroll-swipe-listitem.md

## Detailed Description

### cj-scroll-swipe-listitem.md cj-scroll-swipe-listitem.md

This document serves as API reference documentation for the ListItem component in the ArkUI framework, specifically for the Cangjie programming language. It details the usage of ListItem, which must be nested within a List component to display specific items. Key sections include component initialization, child component constraints, and specific attributes such as selectable and swipeAction, which configures swipe gestures with start and end builders. The document also outlines the onSelect event for handling selection state changes. Intended for Cangjie developers, this guide provides syntax definitions and a code example demonstrating basic implementation within a ForEach loop.