<!-- cj-doc kind="api-member" level="6" id="std.unittest.prop_test.interface.randomsource.nextfloat16" parent="std.unittest.prop_test.interface.randomsource" -->
# RandomSource.nextFloat16

[← RandomSource](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## func nextFloat16()

### 签名

```cangjie role=signature
public open func nextFloat16(): Float16
```

获取一个 Float16 类型的伪随机数，其范围为 [0.0, 1.0)。

### 契约

返回值：

- Float16 - 一个 Float16 类型的伪随机数。

## func nextFloat16()

适用扩展：[extend Random](extensions/extend-random.md)。

### 签名

```cangjie role=signature
public open func nextFloat16(): Float16
```

获取一个 Float16 类型的伪随机数，其范围为 [0.0, 1.0)。

### 契约

返回值：

- Float16 - 一个 Float16 类型的伪随机数。
