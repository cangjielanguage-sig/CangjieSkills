<!-- cj-doc kind="api-member" level="6" id="stdx.net.http.class.funchandler.funchandler-httpcontext-unit" parent="stdx.net.http.class.funchandler" -->
# FuncHandler.FuncHandler((HttpContext) -> Unit)

[← FuncHandler](index.md)

## 签名

```cangjie role=signature
public FuncHandler(let handler: (HttpContext) -> Unit)
```

FuncHandler 的构造函数。

## 契约

参数：

- handler: (HttpContext) -> Unit - 是调用 handle 的处理函数。
