# 使用AES对称密钥GCM模式分段加解密仓颉

This directory focuses on implementing AES symmetric encryption in GCM mode with segmented processing using the Cangjie programming language. It covers the technical workflow for encryption and decryption, handling initialization vectors (IV), authentication tags, and chunked data processing via the CryptoArchitectureKit. Key topics include AES-128, PKCS7 padding, and secure data handling for large files. This resource is suitable for Cangjie developers and security engineers looking to integrate robust cryptographic features into their applications for secure data transmission and storage.

## Quick Navigation

**What do you want to do?**

*   Understand the segmented encryption workflow → 使用AES对称密钥GCM模式分段加解密仓颉_2more.md
*   View a complete synchronous code example → 示例.md
*   Learn the specific steps for decryption → 解密.md

## Detailed Description

### 使用AES对称密钥GCM模式分段加解密仓颉_2more.md 使用AES对称密钥GCM模式分段加解密仓颉_2more.md
This document provides a technical guide for performing segmented encryption using the AES symmetric key algorithm in GCM mode within the Cangjie programming environment. It details the step-by-step encryption process, including generating a 128-bit SymKey, creating a Cipher instance with 'AES128|GCM|PKCS7' parameters, and initializing it with GcmParamsSpec. Key information explains how to handle data segmentation using the update method, distinguishing between block and stream cipher behaviors, and finalizing the process with doFinal. It highlights the necessity of extracting a 16-byte authTag for subsequent decryption verification. This guide is intended for Cangjie developers implementing secure data handling, particularly for scenarios requiring large data processing in chunks.

### 示例.md 示例.md
This document provides a practical code example in the Cangjie language demonstrating synchronous AES-128-GCM encryption and decryption using the CryptoArchitectureKit. It covers random number generation, GCM parameter specification, chunked message encryption and decryption, symmetric key conversion from raw data, and a test function validating the process. The code illustrates handling initialization vectors (IV), additional authenticated data (AAD), authentication tags, and chunked data processing via the update and doFinal methods. This tutorial targets Cangjie developers implementing cryptographic security, specifically those needing to secure data transmission or storage using AES-GCM.

### 解密.md 解密.md
This document provides a step-by-step tutorial for performing AES-128 GCM decryption using the CryptoArchitectureKit API. The guide outlines the process of creating a Cipher instance with specific algorithm parameters, initializing it in decryption mode using a symmetric key and GCM parameters, and processing data in chunks. Key steps include calling createCipher, init, update, and doFinal methods. It highlights the importance of setting the correct CryptoMode and handling data segmentation. This technical guide is intended for developers implementing cryptographic security features who require a practical workflow for decrypting data within their applications.