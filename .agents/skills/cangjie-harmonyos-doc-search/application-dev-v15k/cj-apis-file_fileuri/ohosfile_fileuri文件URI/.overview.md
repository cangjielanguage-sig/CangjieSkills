# ohosfile_fileuri文件URI

This directory provides comprehensive API documentation for the `ohos.file_fileuri` module, a critical component for HarmonyOS developers using the Cangjie language. It centers on the `FileUri` class, which enables secure file sharing by managing the conversion between file URIs and application sandbox paths. The documentation covers essential functionalities such as URI generation, path encoding, and string representation, supporting various URI types including sandbox and public directories. It includes details on error handling, static methods like `getUriFromPath`, and integration with the CoreFileKit. This resource is ideal for developers needing to implement file open, read, and write operations within the HarmonyOS environment.

## Quick Navigation

What do you want to learn?

*   Get an overview of the module, import instructions, and usage guidelines → ohosfile_fileuri文件URI_3more.md
*   Review the specific class definition, constructor, properties, and static methods → class_FileUri.md

## Detailed Description

### class_FileUri.md
This API documentation defines the FileUri class in Cangjie, designed to facilitate file sharing by managing conversions between file URIs and application sandbox paths. The document details the class constructor, read-only properties for file name and path, and static methods for URI generation and string conversion. Key functionality includes getUriFromPath for encoding paths into URIs and toString for string representation. It supports various URI types like sandbox and public directories, throwing specific error codes for invalid arguments. Targeted at HarmonyOS developers using the CoreFileKit, this reference requires knowledge of file system capabilities and Cangjie syntax.

### ohosfile_fileuri文件URI_3more.md
This API documentation describes the ohos.file_fileuri module, which enables developers to convert file paths into Uniform Resource Identifiers (URIs) for file sharing and manipulation. The document covers module import instructions using kit.CoreFileKit, usage guidelines for example code execution, and references to the file management module for subsequent operations. It highlights that URIs obtained via this module facilitate open, read, and write operations. It also specifies prerequisites like configuring the application Context in main_ability.cj. Targeted at HarmonyOS developers using the Cangjie language who need to implement file sharing features.