# interface_BigEndianOrderT

This directory contains API documentation for the Cangjie language's `BigEndianOrder` interface and its type extensions. It covers the implementation of big-endian byte serialization and deserialization for various numeric and boolean types, including integers (Int8 to Int64), unsigned integers (UInt8 to UInt64), floating-point numbers (Float16, Float32, Float64), and Bool. The core functionality involves `readBigEndian` and `writeBigEndian` methods to convert between values and byte arrays. This resource is suitable for Cangjie developers working on low-level binary data manipulation, network protocols, or file I/O requiring specific endianness handling.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition → interface_BigEndianOrderT_3more.md
*   Handle Boolean values → extend_Bool_BigEndianOrderBool.md
*   Work with Signed Integers (Int8, Int16, Int32, Int64) → extend_Int8_BigEndianOrderInt8.md, extend_Int16_BigEndianOrderInt16.md, extend_Int32_BigEndianOrderInt32.md, extend_Int64_BigEndianOrderInt64.md
*   Work with Unsigned Integers (UInt8, UInt16, UInt32, UInt64) → extend_UInt8_BigEndianOrderUInt8.md, extend_UInt16_BigEndianOrderUInt16.md, extend_UInt32_BigEndianOrderUInt32.md, extend_UInt64_BigEndianOrderUInt64.md
*   Work with Floating-point numbers (Float16, Float32, Float64) → extend_Float16_BigEndianOrderFloat16.md, extend_Float32_BigEndianOrderFloat32.md, extend_Float64_BigEndianOrderFloat64.md

## Detailed Description

### extend_Bool_BigEndianOrderBool.md extend_Bool_BigEndianOrderBool.md
This API documentation details a Cangjie language extension that implements the BigEndianOrder interface for the Bool type, facilitating conversion between boolean values and big-endian byte sequences. It covers the static `readBigEndian` function for parsing bytes into a Bool and the instance `writeBigEndian` function for serializing a Bool into a byte array. Key information includes parameter requirements for the `Array<UInt8>` buffer, return types, and the `IllegalArgumentException` thrown for insufficient buffer sizes. This reference guide targets Cangjie developers performing low-level binary data manipulation.

### extend_Float16_BigEndianOrderFloat16.md extend_Float16_BigEndianOrderFloat16.md
This API documentation details the extension of the Float16 type to implement the BigEndianOrder interface, facilitating big-endian byte serialization. It covers the static `readBigEndian` method, which converts a byte array into a Float16 value, and the instance `writeBigEndian` method, which writes a Float16 value into a byte array. Key information includes parameter requirements for the byte buffer, return types, and the throwing of `IllegalArgumentException` for insufficient buffer sizes. This reference is intended for Cangjie developers performing low-level binary data manipulation and network protocol implementation.

### extend_Float32_BigEndianOrderFloat32.md extend_Float32_BigEndianOrderFloat32.md
This API documentation details the extension of the Float32 type in the Cangjie language to implement the BigEndianOrder interface, facilitating conversion between floating-point values and big-endian byte sequences. It covers the extension declaration, a static method for reading Float32 values from byte arrays, and an instance method for writing Float32 values to byte arrays. Key information includes the specific parameters and return types for these functions, which handle IEEE 754 representations and throw `IllegalArgumentException` if the buffer size is insufficient. Code examples demonstrate practical usage.

### extend_Float64_BigEndianOrderFloat64.md extend_Float64_BigEndianOrderFloat64.md
This API documentation details the extension of the Float64 type to implement the BigEndianOrder interface, enabling conversion between Float64 values and big-endian byte sequences. It covers two primary functions: the static method `readBigEndian` for parsing a Float64 from a byte array, and the instance method `writeBigEndian` for serializing a Float64 into a byte array. Key information includes parameter definitions, return values, and exception handling for insufficient buffer sizes. This reference guide targets Cangjie developers working with low-level binary data, requiring an understanding of endianness and array manipulation.

### extend_Int16_BigEndianOrderInt16.md extend_Int16_BigEndianOrderInt16.md
This API documentation details the extension of the Int16 type to implement the BigEndianOrder interface, enabling conversion between Int16 values and big-endian byte sequences. It covers the static function `readBigEndian` for reading values from byte arrays and the instance function `writeBigEndian` for writing values to byte arrays. Key information includes parameter requirements for the UInt8 buffer, return types, and the `IllegalArgumentException` thrown for insufficient buffer sizes. Practical code examples demonstrate both reading and writing operations. This reference guide targets Cangjie developers handling low-level binary data serialization and deserialization.

### extend_Int32_BigEndianOrderInt32.md extend_Int32_BigEndianOrderInt32.md
This API reference document details the extension of the Int32 type in the Cangjie language to implement the BigEndianOrder interface for big-endian byte sequence conversion. It covers the extension declaration, a static `readBigEndian` function for parsing byte arrays into Int32, and an instance `writeBigEndian` function for serializing Int32 values into byte arrays. Key information includes that both functions accept an `Array<UInt8>` buffer and throw `IllegalArgumentException` if the buffer size is insufficient, while the write function returns the byte count as Int64. This documentation targets Cangjie developers performing low-level binary data manipulation and serialization.

### extend_Int64_BigEndianOrderInt64.md extend_Int64_BigEndianOrderInt64.md
This API documentation details the extension of the Int64 type to implement the BigEndianOrder interface, enabling conversion between 64-bit integers and big-endian byte sequences. It covers two primary functions: the static `readBigEndian` method for parsing byte arrays into Int64 values, and the instance `writeBigEndian` method for serializing integers into byte arrays. Key information includes parameter requirements for the byte buffer, return types, and the throwing of `IllegalArgumentException` when buffer sizes are insufficient. This reference guide targets Cangjie developers who need to handle low-level binary data serialization and deserialization.

### extend_Int8_BigEndianOrderInt8.md extend_Int8_BigEndianOrderInt8.md
This API documentation details the extension of the Int8 type to implement the BigEndianOrder interface in the Cangjie language, facilitating conversion between Int8 values and big-endian byte sequences. It covers the static function `readBigEndian` for reading values from byte arrays and the instance function `writeBigEndian` for writing values to byte arrays. Key information includes function signatures, parameters, return values, and exceptions such as `IllegalArgumentException` for insufficient buffer sizes. This reference guide targets Cangjie developers working with low-level binary data serialization and deserialization, providing code examples to demonstrate usage.

### extend_UInt16_BigEndianOrderUInt16.md extend_UInt16_BigEndianOrderUInt16.md
This API documentation details the extension of the UInt16 type in the Cangjie language to implement the BigEndianOrder interface, enabling conversion between numeric values and big-endian byte sequences. It covers the extension declaration, a static `readBigEndian` function for parsing byte arrays, and an instance `writeBigEndian` function for serializing data. Key information includes that both functions require an `Array<UInt8>` buffer and throw `IllegalArgumentException` if the buffer size is insufficient. The read function returns a UInt16, while the write function returns the number of bytes written as Int64. This reference guide targets Cangjie developers handling low-level binary data serialization and deserialization.

### extend_UInt32_BigEndianOrderUInt32.md extend_UInt32_BigEndianOrderUInt32.md
This API documentation details the extension of the UInt32 type to implement the BigEndianOrder interface in the Cangjie language, enabling conversion between UInt32 values and big-endian byte sequences. It covers the extension declaration, a static method `readBigEndian` for parsing byte arrays, and an instance method `writeBigEndian` for serializing values. Key information includes that both methods require an `Array<UInt8>` buffer and throw an `IllegalArgumentException` if the buffer size is insufficient. The read method returns a UInt32, while the write method returns the number of bytes written as an Int64. This reference guide targets Cangjie developers who need to handle binary data serialization and deserialization.

### extend_UInt64_BigEndianOrderUInt64.md extend_UInt64_BigEndianOrderUInt64.md
This API documentation details the extension of the UInt64 type to implement the BigEndianOrder interface, enabling conversion between 64-bit unsigned integers and big-endian byte sequences. It covers the static `readBigEndian` method for parsing byte arrays into UInt64 values and the instance `writeBigEndian` method for serializing values into byte arrays. Key information includes that both functions require an `Array<UInt8>` buffer and throw an `IllegalArgumentException` if the buffer size is insufficient. The write method returns the number of bytes written, while the read method returns the parsed integer. This reference guide targets Cangjie developers working with low-level binary data processing or network protocols requiring specific byte order handling.

### extend_UInt8_BigEndianOrderUInt8.md extend_UInt8_BigEndianOrderUInt8.md
This API documentation details the extension of the UInt8 type in the Cangjie language to implement the BigEndianOrder interface, facilitating conversion between UInt8 values and big-endian byte sequences. It covers two primary functions: the static `readBigEndian` method for retrieving a UInt8 from a byte array, and the instance `writeBigEndian` method for writing a value to an array. Key information includes parameter definitions for the input buffer, return types, and the `IllegalArgumentException` thrown for insufficient buffer space. Code examples illustrate practical usage for both reading and writing operations. This reference guide is intended for developers performing low-level binary data manipulation and serialization.

### interface_BigEndianOrderT_3more.md interface_BigEndianOrderT_3more.md
This document provides API reference documentation for the Cangjie programming language, specifically defining the BigEndianOrder interface for big-endian byte sequence conversion. The content outlines the interface structure and details two core methods: the static `readBigEndian` function for parsing a type T from a byte array, and the instance `writeBigEndian` function for serializing a type T into a byte array. Key information includes parameter definitions, return values, and exception handling for buffer size constraints. This documentation is intended for developers working with low-level data serialization and binary protocols in Cangjie, requiring knowledge of generic types and array manipulation.