# BuilderParam宏引用Builder函数

This directory provides comprehensive technical documentation on the `@BuilderParam` macro within the ArkUI and Cangjie UI frameworks. It focuses on how developers can dynamically reference `@Builder` functions to inject specific UI content into custom component instances, effectively acting as slot placeholders. The content covers initialization methods, usage scenarios, type matching, and common troubleshooting for compilation errors. It is suitable for HarmonyOS developers and UI engineers looking to implement dynamic, declarative UI components with flexible content injection capabilities while managing component scope and state effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the core definition, syntax rules, and limitations of `@BuilderParam` → BuilderParam宏引用Builder函数_3more.md
*   Explore usage scenarios, including parameterized builders and parent component references → 使用场景.md
*   Troubleshoot compilation errors and understand initialization constraints → 常见问题.md

## Detailed Description

### BuilderParam宏引用Builder函数_3more.md BuilderParam宏引用Builder函数_3more.md
This technical guide explains the @BuilderParam macro in ArkUI, which allows developers to dynamically reference @Builder functions to add specific functionality to individual custom component instances. The document covers the macro's purpose as a slot placeholder, initialization methods using global @Builder functions, and specific usage limitations. Key takeaways include that @BuilderParam variables must be initialized by @Builder functions, must have explicit function types returning Unit, and are restricted to class members with private visibility. Intended for ArkUI developers familiar with @Builder syntax, this reference helps solve the problem of applying specific features to specific component instances without global impact.

### 使用场景.md 使用场景.md
This technical guide explains the usage scenarios of the @BuilderParam decorator in Cangjie UI development. It covers two primary methods: initializing components with parameterized or parameterless builders, and initializing @BuilderParam variables using global or parent component @Builder functions. Key concepts include type matching between the parameter and the target builder, and scope management when passing parent component methods. The document provides code examples demonstrating how to dynamically inject UI content into child components. It is intended for developers building declarative UIs in HarmonyOS using the Cangjie language.

### 常见问题.md 常见问题.md
This document serves as a troubleshooting guide addressing a specific compilation error related to the @BuilderParam macro in Cangjie development. It covers the initialization rules for @BuilderParam variables, contrasting incorrect usage with @State variables against the correct approach using global @Builder functions. The key takeaway is that @BuilderParam properties must be initialized with @Builder functions, not state variables, to avoid compilation errors. Targeted at Cangjie developers building HarmonyOS applications, this FAQ assumes familiarity with component decorators and state management. Keywords: @BuilderParam, @State, @Builder, Cangjie, HarmonyOS, compilation error, component initialization.