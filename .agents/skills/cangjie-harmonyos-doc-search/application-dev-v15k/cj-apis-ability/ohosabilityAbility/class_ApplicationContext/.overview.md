# class_ApplicationContext

This directory contains API reference documentation for the Cangjie language's `ApplicationContext` class, a core component for managing application-level context within the HarmonyOS ecosystem. It covers essential functionalities including application lifecycle management, event listener registration for system and ability state changes, and runtime configuration settings like language and font scaling. The documentation is intended for software developers building applications using the Cangjie framework who need to handle application data, process termination, reactive behaviors, and system-level UI adjustments programmatically.

## Quick Navigation

**What do you want to learn or do?**

*   Explore the core class definition, inheritance, and lifecycle management → class_ApplicationContext_7more.md
*   Manage event listeners for system environment and ability lifecycles → func_offApplicationContext_4more.md
*   Implement callbacks for state changes, app restarts, and visual modes → func_onApplicationContextT_4more.md
*   Configure runtime behaviors such as font scaling, language, and caching → func_setFontSizeScaleFloat_3more.md

## Detailed Description

### class_ApplicationContext_7more.md
This document provides API reference documentation for the Cangjie language's ApplicationContext class, which offers application-level context capabilities. It details the class definition, its inheritance from the Context parent type, and associated system capabilities. The main sections cover six key member functions focused on application lifecycle and management, including data cleanup, retrieving application information, managing app clones, and process control. Key information includes method signatures, return types, starting versions, and specific BusinessException error codes. This technical reference is intended for software developers using the Cangjie framework to build applications, specifically those needing to handle application data, process termination, and runtime information retrieval.

### func_offApplicationContext_4more.md
This document serves as API reference material for the Cangjie programming language, specifically detailing methods to manage application context event listeners. It covers four primary functions: registering listeners for system environment changes and ability lifecycles using the on method, and deregistering these listeners using the off method. Key information includes the requirement for main thread execution, the use of Int32 IDs for listener management, and specific error handling for parameter validation. This guide is intended for developers implementing reactive behaviors based on application state and system environment shifts.

### func_onApplicationContextT_4more.md
This document serves as API reference documentation for Cangjie language functions managing application context and configuration. It details four primary functions: registering callbacks for application state changes, restarting the application with a specific UIAbility, setting the color mode, and configuring the font type. Key takeaways include that all operations are restricted to the main thread and throw specific BusinessExceptions for parameter or context errors. The content is intended for application developers needing to control app lifecycle, visual appearance, and restart behavior within the HarmonyOS system environment.

### func_setFontSizeScaleFloat_3more.md
This document serves as API reference documentation for Cangjie language functions designed to configure application runtime behaviors. It covers three primary functions: setFontSizeScale for adjusting font scaling ratios, setLanguage for setting the application language, and setSupportedProcessCache for enabling cached fast launches. Key information includes parameter requirements, system capabilities, and specific exception handling for errors like invalid arguments or missing contexts. Notably, all functions require main thread execution, and process caching is restricted to the Stage model. This guide is intended for developers seeking to programmatically manage application context settings and localization.