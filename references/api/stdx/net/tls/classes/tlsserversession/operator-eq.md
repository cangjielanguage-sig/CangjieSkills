<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.class.tlsserversession.operator-eq" parent="stdx.net.tls.class.tlsserversession" -->
# TlsServerSession.==

[← TlsServerSession](index.md)

## 签名

```cangjie role=signature
public override operator func ==(other: TlsServerSession): Bool
```

判断两 TlsServerSession 实例名称是否相同。

## 参数

- other: TlsServerSession - 被比较的会话上下文对象。

## 返回值

- Bool - 若 TlsServerSession 对象相同，返回 `true`；否则，返回 `false`。

