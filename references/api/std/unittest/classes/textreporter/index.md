<!-- cj-doc kind="api-type" level="5" id="std.unittest.class.textreporter" parent="std.unittest" -->
# TextReporter<PP>

[← std.unittest](../../index.md)

`TextReporter<PP> <: Reporter<TestReport, PP> & Reporter<BenchReport, PP> where PP <: PrettyPrinter`

将单元测试用例结果或性能测试结果打印到 PrettyPrinter 的子类。

## 方法

| 签名 | 功能 |
|---|---|
| [`TextReporter(let into!: PP)`](textreporter-pp.md) | TextReporter 构造函数。 |
