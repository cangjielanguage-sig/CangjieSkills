<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-add" parent="std.math.numeric.struct.bigint" -->
# BigInt.+

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func +(that: BigInt): BigInt
```

BigInt 加法。

## 契约

参数：

- that: BigInt - 加数。

返回值：

- BigInt - 一个新 BigInt，它是此 BigInt 与另外一个 BigInt 相加后的结果。
