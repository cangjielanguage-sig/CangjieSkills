<!-- cj-doc kind="api-member" level="6" id="std.unittest.struct.timenow.init" parent="std.unittest.struct.timenow" -->
# TimeNow.init

[← TimeNow](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

自动选择输出格式的默认构造函数。

## init(?TimeUnit)

### 签名

```cangjie role=signature
public init(unit: ?TimeUnit)
```

`unit` 参数用于指定打印结果时将使用的时间单位。

### 契约

参数：

- unit: ?TimeUnit - 指定的时间单位。
