<!-- cj-doc kind="guide-leaf" level="5" id="language.concurrency.2-创建线程.2-2-示例" parent="language.concurrency.2-创建线程" -->
# 2.2 示例

[← 2. 创建线程](index.md)

使用 `spawn` 启动并发任务，并通过返回的 `Future<T>` 等待任务完成和取得结果。

```cangjie cjtest=syntax id=syntax-36e74e882b-1 form=unit
main(): Int64 {
    spawn { =>
        println("New thread before sleeping")
        sleep(100 * Duration.millisecond)
        println("New thread after sleeping")
    }
    println("Main thread")
    return 0
}
```

---

## 已验证示例

`spawn` 立即返回 `Future<T>`；`get()` 等待任务结束，并取得 Lambda 的返回值。

```cangjie cjtest=run id=language.spawn-future.run form=unit timeout=20s
package spawn_future_example

main(): Unit {
    let answer = spawn { => 6 * 7 }
    println(answer.get())
}
```

```text cjtest=expect for=language.spawn-future.run stream=stdout match=exact
42
```
