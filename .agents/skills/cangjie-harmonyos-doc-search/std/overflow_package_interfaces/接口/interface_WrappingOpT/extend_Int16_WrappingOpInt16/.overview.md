# extend_Int16_WrappingOpInt16

This directory provides comprehensive API reference documentation for the `WrappingOp` interface implementation specifically tailored for the `Int16` type within the Cangjie programming language. It covers a wide array of arithmetic and bitwise operations, including addition, multiplication, division, negation, increment, decrement, and bit shifting. A defining characteristic of these operations is the use of high-order truncation to manage overflow, ensuring that results wrap around safely instead of triggering runtime errors. This resource is crucial for Cangjie developers who need deterministic integer behavior, particularly in systems programming, low-level algorithm design, or performance-critical code paths where exception handling is undesirable.

## Quick Navigation

What do you want to learn?

*   Review the full `WrappingOp` interface for `Int16`, including arithmetic, bitwise, and shift operations with overflow handling → extend_Int16_WrappingOpIn_10more.md
*   Access specific API details for the `wrappingSub` function, including parameters and return values for safe subtraction → func_wrappingSubInt16.md

## Detailed Description

### extend_Int16_WrappingOpIn_10more.md extend_Int16_WrappingOpIn_10more.md
This API reference document details the implementation of the `WrappingOp` interface for the `Int16` type in the Cangjie programming language. The content covers a comprehensive list of arithmetic and bitwise operations, including addition, multiplication, division, negation, increment, decrement, and bit shifting. Key information is that all operations utilize a high-order truncation strategy to handle overflow, meaning results wrap around rather than causing errors. Shift operations specifically mask the shift count to the low 4 bits if the count exceeds the bit width. This documentation targets Cangjie developers requiring precise control over integer overflow behavior, likely for systems programming or algorithm implementation.

### func_wrappingSubInt16.md func_wrappingSubInt16.md
This document provides API reference details for the `wrappingSub` function in the Cangjie programming language, specifically designed for the `Int16` type. The content covers the function signature, functionality description, input parameters, and return values. Key information highlights that this subtraction operation employs a high-bit truncation strategy to handle overflow, meaning the result wraps around instead of causing an error. The function accepts a single `Int16` parameter `y` as the subtrahend and returns the resulting `Int16` value. This reference is intended for Cangjie developers who require precise control over arithmetic overflow behavior in their applications.