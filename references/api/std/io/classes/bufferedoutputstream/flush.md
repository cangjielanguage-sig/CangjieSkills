<!-- cj-doc kind="api-member" level="6" id="std.io.class.bufferedoutputstream.flush" parent="std.io.class.bufferedoutputstream" -->
# BufferedOutputStream<T> where T <: OutputStream.flush

[← BufferedOutputStream<T> where T <: OutputStream](index.md)

## 签名

```cangjie role=signature
public func flush(): Unit
```

刷新 BufferedOutputStream：将内部缓冲区的剩余数据写入绑定的输出流，并刷新 BufferedOutputStream。
