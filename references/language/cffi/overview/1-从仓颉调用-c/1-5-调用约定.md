<!-- cj-doc kind="guide-leaf" level="6" id="language.cffi.overview.1-从仓颉调用-c.1-5-调用约定" parent="language.cffi.overview.1-从仓颉调用-c" -->
# 1.5 调用约定

[← 1. 从仓颉调用 C](index.md)

- `@CallingConv[CDECL]`：默认约定，可省略
- `@CallingConv[STDCALL]`：Win32 API 调用约定

适用于 `foreign` 块、单个 `foreign` 函数和顶层 `CFunc` 函数：

```cangjie cjtest=syntax id=syntax-55febc86c5-1 form=stmt
@CallingConv[STDCALL]
foreign func MessageBoxA(hwnd: CPointer<Unit>, text: CString, caption: CString, uType: UInt32): Int32
```

---
