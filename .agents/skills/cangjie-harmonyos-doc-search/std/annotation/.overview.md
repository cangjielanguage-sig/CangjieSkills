# annotation

This directory contains technical documentation and tutorials focused on the advanced usage of annotations within the Cangjie programming language. It specifically covers the mechanisms for defining custom annotation classes, applying them to target classes, and retrieving their values dynamically using reflection. The content provides a deep dive into the TypeInfo API, illustrating how to use methods like `TypeInfo.of()` and `findAnnotation()` to access and manipulate metadata at runtime. This resource is designed for Cangjie developers and software engineers who need to implement runtime introspection, dynamic code analysis, or sophisticated metadata handling in their applications.

## Quick Navigation

**What do you want to learn?**

*   How to retrieve annotation values using reflection? → annotation.md annotation.md
*   How to define and apply custom annotations? → annotation.md annotation.md
*   How to use the TypeInfo API for runtime metadata? → annotation.md annotation.md

## Detailed Description

### annotation.md annotation.md
This document serves as a code tutorial demonstrating how to retrieve annotation values from class instances using reflection in the Cangjie programming language. It covers the definition of a custom annotation class, its application to a target class, and the usage of the TypeInfo API to access metadata at runtime. Key information includes the specific methods TypeInfo.of() and findAnnotation() to extract properties from the annotation instance. This guide is intended for Cangjie developers looking to implement runtime introspection and metadata handling. Keywords: Cangjie, reflection, annotations, TypeInfo, runtime metadata, code example.