<!-- cj-doc kind="api-member" level="6" id="std.ast.class.vardecl.isconst" parent="std.ast.class.vardecl" -->
# VarDecl.isConst

[← VarDecl](index.md)

## 签名

```cangjie role=signature
public func isConst(): Bool
```

判断是否是一个 `Const` 类型的节点。

## 契约

返回值：

- Bool - 是一个 `Const` 类型的节点返回 true；反之，返回 false。
