<!-- cj-doc kind="api-member" level="6" id="stdx.serialization.serialization.class.field.init" parent="stdx.serialization.serialization.class.field" -->
# Field.init

[← Field](index.md)

## 签名

```cangjie role=signature
public init(name: String, data: DataModel)
```

Field 的构造函数。

## 契约

参数：

- name: String - `name` 字段值，`name` 字段为 `""` 时行为与为其它字符串时一致。
- data: DataModel - `data` 字段值。
