<!-- cj-doc kind="api-member" level="6" id="std.ast.class.funcdecl.isconst" parent="std.ast.class.funcdecl" -->
# FuncDecl.isConst

[← FuncDecl](index.md)

## 签名

```cangjie role=signature
public func isConst(): Bool
```

判断是否是一个 `Const` 类型的节点。

## 契约

返回值：

- Bool - 是一个 `Const` 类型的节点返回 true；反之，返回 false。
