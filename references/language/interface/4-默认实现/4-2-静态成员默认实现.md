<!-- cj-doc kind="guide-leaf" level="5" id="language.interface.4-默认实现.4-2-静态成员默认实现" parent="language.interface.4-默认实现" -->
# 4.2 静态成员默认实现

[← 4. 默认实现](index.md)

- 有默认实现的静态成员可通过接口名和实现者名访问
- 无默认实现的静态成员不能通过接口名直接访问
- 通常在泛型约束中使用静态成员：`func f<T>() where T <: I { T.staticFunc() }`
```cangjie cjtest=syntax id=syntax-e473da5726-1 form=unit
interface NamedType {
    static func typename(): String {
        "interface NamedType"
    }
}

class A <: NamedType {}

main() {
    println(NamedType.typename())  // 输出：interface NamedType
    println(A.typename())          // 输出：interface NamedType
}
```
