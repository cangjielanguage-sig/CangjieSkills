# unittest_diff_package_interfaces

This directory serves as a critical foundational component within the Cangjie programming language's development ecosystem, specifically dedicated to the structural blueprints of its unit testing framework. It acts as a central repository for interface definitions and API references that are essential for managing assertion logic and generating precise test diagnostics. The content focuses heavily on the `AssertPrintable` contract, which governs how custom data types are formatted and displayed when test failures occur. Additionally, it covers utilities like `PrettyPrinter` for comparing expected versus actual values. This resource is indispensable for software engineers, framework contributors, and QA specialists who aim to extend testing capabilities or implement custom display logic for complex types. By mastering these interfaces, users ensure robust debugging and a significantly enhanced developer experience when writing automated tests, covering everything from generic options to floating-point precisions.

## Quick Navigation

**What do you want to learn?**

*   **Understand core interface definitions** for the Cangjie unit testing framework to build robust, maintainable tests → 接口/
*   **Implement custom formatting** for test failures using the `AssertPrintable` contract to handle complex data types → 接口/
*   **Explore API references** regarding assertion logic and data type handling for generic options, strings, and floats → 接口/
*   **Learn about diagnostic tools** like `PrettyPrinter` for comparing expected and actual values effectively during debugging → 接口/

## Detailed Description

The directory contains a single subdirectory that houses all necessary technical documentation and interface definitions required for the testing framework.

### 接口/
This directory functions as a central hub for interface definitions and technical documentation within the Cangjie programming language's development environment. It is specifically designed to house the structural blueprints and API references that are critical for the language's unit testing framework. The primary focus of this directory is to provide comprehensive details on assertion interfaces, particularly the `AssertPrintable` contract, which dictates how custom data types are formatted and displayed when test failures occur. By covering a wide range of data types—including generic options, strings, and various floating-point precisions—it ensures that developers can produce clear, human-readable diagnostic outputs. This directory is an essential resource for software engineers, framework contributors, and quality assurance specialists who are tasked with extending the testing capabilities of Cangjie. It is particularly valuable for those who need to implement custom display logic for complex types or wish to gain a deeper understanding of how the testing framework handles differences between expected and actual values through tools like `PrettyPrinter`. Mastering these interfaces allows for more robust debugging and a better developer experience when writing automated tests.