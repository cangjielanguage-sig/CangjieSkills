# encoding_json_stream_package_structs

This directory serves as the definitive guide for the structural components of the `encoding_json_stream` package, specifically focusing on the configuration data structures required for JSON serialization in the Cangjie programming language. It provides an in-depth examination of the `WriteConfig` struct, which is the central tool for developers needing to control how data is serialized into JSON format. The documentation covers a broad spectrum of formatting capabilities, ranging from simple static presets like "compact" and "pretty" to complex, mutable properties that manage indentation, newline behavior, and separator spacing. Additionally, it addresses essential functional requirements such as HTML escaping for security and custom date formatting. By outlining specific default values and validation rules for string properties, this directory acts as a crucial reference for backend developers and systems engineers who need to ensure their JSON output meets specific formatting standards and data integrity requirements, whether for APIs, logging, or data exchange. This ensures that the generated JSON is both human-readable and machine-parsable according to specific needs.

## Quick Navigation

**What do you want to learn?**
*   Understand the architecture of the `WriteConfig` struct? → encoding_json_stream_package_structs.md
*   How to implement "compact" vs "pretty" formatting presets? → encoding_json_stream_package_structs.md
*   How to define custom indentation and newline characters? → encoding_json_stream_package_structs.md
*   How to manage separator spacing in the output? → encoding_json_stream_package_structs.md
*   How to enable or disable HTML escaping features? → encoding_json_stream_package_structs.md
*   How to apply specific date formatting rules? → encoding_json_stream_package_structs.md
*   What are the validation constraints for configuration strings? → encoding_json_stream_package_structs.md
*   How to integrate these structs with the `JsonWriter` class? → encoding_json_stream_package_structs.md

## Detailed Description

### encoding_json_stream_package_structs.md encoding_json_stream_package_structs.md
This API reference document offers a detailed technical analysis of the `WriteConfig` struct, which is utilized to configure serialization formats for the `JsonWriter` class found in the Cangjie `encoding_json_stream` package. The content is extensive, covering the implementation of static presets such as "compact" and "pretty," while also diving into the various mutable properties available for developers. These properties provide control over critical formatting elements, including indentation levels, newline insertion, HTML escaping mechanisms, date formatting styles, and the spacing between separators. Key takeaways from this document include the specific default values assigned to different presets and the validation rules that govern string properties like indent and newline. This documentation is primarily targeted at developers who are seeking to customize JSON output formatting, and it assumes the reader possesses knowledge of Cangjie syntax and a general understanding of JSON structures to effectively utilize these configuration options.