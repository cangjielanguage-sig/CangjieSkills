# argopt_package_enums

This directory provides comprehensive API reference documentation for the enumerations included in the `argopt` package, a specialized library developed for command-line argument parsing within the Cangjie programming language. The documentation is centered around the core enumerations that dictate how command-line inputs are validated and structured. It thoroughly details the `ArgumentMode` enum, which defines the strict value requirements for arguments—categorizing them as `NoValue`, `RequiredValue`, or `OptionalValue`—and the `ArgumentSpec` enum, which specifies the structural syntax of arguments, including `Short`, `Long`, `Full`, and `NonOptions` types. Beyond basic definitions, the content covers the technical implementation of standard interfaces such as `ToString` and `Equatable` for the `ArgumentMode` enum. It also highlights the advanced capability of `ArgumentSpec` variants to hold lambda callback functions for dynamic value handling. This resource is specifically tailored for Cangjie developers and software engineers who are in the process of building or maintaining command-line interfaces and require a reliable guide for defining and parsing command options.

## Quick Navigation

What do you want to learn?

*   Understand the specific value requirements for command-line arguments (NoValue, RequiredValue, OptionalValue) → argopt_package_enums.md
*   Explore the structural specifications available for arguments (Short, Long, Full, NonOptions) → argopt_package_enums.md
*   Learn how lambda callback functions are integrated into `ArgumentSpec` variants for value handling → argopt_package_enums.md
*   Review the implementation details of standard interfaces like `ToString` and `Equatable` → argopt_package_enums.md

## Detailed Description

### argopt_package_enums.md argopt_package_enums.md

This document provides API reference documentation for enumerations within the argopt package, designed for command-line argument parsing in the Cangjie programming language. It details the ArgumentMode enum, which defines value requirements like NoValue, RequiredValue, and OptionalValue, and the ArgumentSpec enum, which specifies argument structures such as Short, Long, Full, and NonOptions. Key information includes the implementation of ToString and Equatable interfaces for ArgumentMode, and the ability of ArgumentSpec variants to hold lambda callback functions for handling values. This reference is intended for developers implementing command-line interfaces who need to define and parse command options.