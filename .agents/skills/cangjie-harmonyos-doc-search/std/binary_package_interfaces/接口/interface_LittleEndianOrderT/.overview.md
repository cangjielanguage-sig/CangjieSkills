# interface_LittleEndianOrderT

This directory contains API documentation for the Cangjie language's `LittleEndianOrder` interface and its implementations across various primitive data types. It covers extensions for signed and unsigned integers (Int8 to Int64, UInt8 to UInt64), floating-point numbers (Float16, Float32, Float64), and Boolean values. The documentation details methods for serializing data to and deserializing data from little-endian byte sequences using `Array<UInt8>` buffers. Key topics include binary data manipulation, low-level serialization, and endianness handling. This resource is suitable for Cangjie developers working on system programming, file I/O, or network protocols requiring precise byte order control.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition → interface_LittleEndianOrde_3more.md
*   Handle Boolean values → extend_Bool_LittleEndianOrderBool.md
*   Handle Signed Integers (Int8, Int16, Int32, Int64) → extend_Int8_LittleEndianOrderInt8.md, extend_Int16_LittleEndianOrderInt16.md, extend_Int32_LittleEndianOrderInt32.md, extend_Int64_LittleEndianOrderInt64.md
*   Handle Unsigned Integers (UInt8, UInt16, UInt32, UInt64) → extend_UInt8_LittleEndianOrderUInt8.md, extend_UInt16_LittleEndianOrderUInt16.md, extend_UInt32_LittleEndianOrderUInt32.md, extend_UInt64_LittleEndianOrderUInt64.md
*   Handle Floating-point numbers (Float16, Float32, Float64) → extend_Float16_LittleEndianOrderFloat16.md, extend_Float32_LittleEndianOrderFloat32.md, extend_Float64_LittleEndianOrderFloat64.md

## Detailed Description

### extend_Bool_LittleEndianOrderBool.md extend_Bool_LittleEndianOrderBool.md
This API documentation details the extension of the Bool type to implement the LittleEndianOrder<Bool> interface, facilitating conversion between boolean values and little-endian byte sequences. The document covers two primary functions: the static readLittleEndian method for parsing bytes into a Bool, and the instance writeLittleEndian method for serializing a Bool into a byte array. Key information includes parameter definitions, return types, and exception handling for insufficient buffer sizes, specifically IllegalArgumentException. Practical code examples demonstrate reading and writing operations. This reference is intended for Cangjie developers working with binary data who need to serialize or deserialize boolean values in a little-endian format.

### extend_Float16_LittleEndianOrderFloat16.md extend_Float16_LittleEndianOrderFloat16.md
This API documentation details the extension of the Float16 type to implement the LittleEndianOrder interface, enabling byte serialization for half-precision floating-point numbers. The document covers two primary functions: the static readLittleEndian method, which parses a Float16 value from a byte array, and the instance writeLittleEndian method, which serializes a Float16 value into a byte array. Key information includes parameter requirements for the buffer, return types, and the IllegalArgumentException thrown for insufficient buffer sizes. This reference is intended for Cangjie developers performing low-level binary data manipulation who require precise control over endianness.

### extend_Float32_LittleEndianOrderFloat32.md extend_Float32_LittleEndianOrderFloat32.md
This document provides API reference documentation for extending the Float32 type in the Cangjie language to implement the LittleEndianOrder interface, facilitating conversion between Float32 values and little-endian byte sequences. The main sections cover the static readLittleEndian function, which parses a byte array into a Float32, and the writeLittleEndian function, which serializes a Float32 into a byte array. Key information includes parameter definitions for the byte buffer, return values indicating byte count, and exceptions thrown for insufficient buffer sizes. Intended for developers handling binary data or low-level serialization, this guide requires knowledge of Cangjie syntax and array operations.

### extend_Float64_LittleEndianOrderFloat64.md extend_Float64_LittleEndianOrderFloat64.md
This API documentation details the extension of the Float64 type in Cangjie to implement the LittleEndianOrder interface, enabling conversion between floating-point values and little-endian byte sequences. The document covers two primary functions: the static method readLittleEndian for parsing bytes into a Float64, and the instance method writeLittleEndian for serializing a Float64 into a byte array. Key information includes parameter requirements for the byte buffer, return types, and the throwing of IllegalArgumentException for insufficient buffer sizes. This reference is intended for Cangjie developers performing low-level binary data processing and serialization tasks.

### extend_Int16_LittleEndianOrderInt16.md extend_Int16_LittleEndianOrderInt16.md
This API documentation details the extension of the Int16 type to implement the LittleEndianOrder interface in the Cangjie standard library, facilitating conversion between Int16 values and little-endian byte sequences. The document covers two primary functions: the static readLittleEndian method for parsing byte arrays into Int16 values, and the instance writeLittleEndian method for serializing Int16 values into byte arrays. Key information includes parameter requirements for Array<UInt8> buffers, return types, and the throwing of IllegalArgumentException for insufficient buffer sizes. This reference is intended for Cangjie developers performing low-level binary data processing, serialization, or network communication tasks.

### extend_Int32_LittleEndianOrderInt32.md extend_Int32_LittleEndianOrderInt32.md
This API reference document details the extension of the Int32 type in the Cangjie language to implement the LittleEndianOrder interface, enabling conversion between Int32 values and little-endian byte sequences. The documentation covers the extension declaration, a static method readLittleEndian for parsing byte arrays, and an instance method writeLittleEndian for serializing data. Key information includes that both methods require an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient. The read method returns an Int32, while the write method returns the number of bytes written as Int64. This guide targets Cangjie developers needing low-level binary data manipulation, specifically for handling little-endian encoding.

### extend_Int64_LittleEndianOrderInt64.md extend_Int64_LittleEndianOrderInt64.md
This API documentation details the extension of the Int64 type in the Cangjie language to implement the LittleEndianOrder interface for byte serialization. It covers the static function readLittleEndian for converting byte arrays to Int64, and the instance function writeLittleEndian for converting Int64 values to byte arrays. Both functions require an Array<UInt8> buffer and throw IllegalArgumentException if the buffer size is insufficient, with the write function returning the number of bytes written. This is intended for Cangjie developers working with low-level binary data processing and serialization.

### extend_Int8_LittleEndianOrderInt8.md extend_Int8_LittleEndianOrderInt8.md
This API documentation details the extension of the Int8 type to implement the LittleEndianOrder interface, facilitating conversion between Int8 values and little-endian byte sequences in the Cangjie language. The document covers two primary functions: the static readLittleEndian method, which parses a byte array to retrieve an Int8 value, and the instance writeLittleEndian method, which serializes an Int8 value into a byte array. Key information includes parameter requirements for the byte array buffer, return types, and the throwing of IllegalArgumentException for insufficient buffer sizes. This reference is intended for developers performing low-level binary data manipulation and serialization.

### extend_UInt16_LittleEndianOrderUInt16.md extend_UInt16_LittleEndianOrderUInt16.md
This API documentation details the extension of the UInt16 type in the Cangjie language to implement the LittleEndianOrder interface for binary data conversion. It covers the static method readLittleEndian for parsing byte arrays into UInt16 values and the instance method writeLittleEndian for serializing UInt16 values into byte arrays. Key information includes that both functions operate on little-endian byte order, require an Array<UInt8> buffer, and throw an IllegalArgumentException if the buffer size is insufficient. The write method returns the number of bytes written. This reference guide targets developers needing to handle low-level binary data serialization and deserialization within the Cangjie standard library.

### extend_UInt32_LittleEndianOrderUInt32.md extend_UInt32_LittleEndianOrderUInt32.md
This API documentation details the extension of the UInt32 type to implement the LittleEndianOrder interface, enabling conversion between 32-bit unsigned integers and little-endian byte sequences. The document covers the extension declaration, a static readLittleEndian function for parsing byte arrays, and an instance writeLittleEndian function for serializing data. Key information includes that both functions accept an Array<UInt8> buffer, throw IllegalArgumentException for insufficient buffer sizes, and handle byte order reversal. The read function returns a UInt32, while the write function returns the byte count as Int64. This reference guide targets Cangjie developers requiring low-level binary data manipulation and serialization.

### extend_UInt64_LittleEndianOrderUInt64.md extend_UInt64_LittleEndianOrderUInt64.md
This API reference document details the extension of the UInt64 type in the Cangjie language to implement the LittleEndianOrder interface, facilitating conversions between 64-bit unsigned integers and Little-Endian byte sequences. The documentation covers the interface implementation definition, a static readLittleEndian function for parsing byte arrays, and an instance writeLittleEndian function for serializing data. Key information includes that both functions operate on an Array<UInt8> buffer, throw IllegalArgumentException for insufficient buffer sizes, and handle the specific byte ordering required for binary interoperability. This resource is intended for Cangjie developers working on low-level data processing, file I/O, or network protocols who need precise control over binary data representation.

### extend_UInt8_LittleEndianOrderUInt8.md extend_UInt8_LittleEndianOrderUInt8.md
This API documentation details the extension of the UInt8 type in Cangjie to implement the LittleEndianOrder interface for binary data conversion. The document covers two primary functions: the static readLittleEndian method for extracting values from byte arrays, and the instance writeLittleEndian method for inserting values into buffers. Key information includes parameter requirements for the buffer array, return types such as the byte count, and the IllegalArgumentException thrown when buffer sizes are insufficient. This reference guide targets Cangjie developers who need to perform low-level binary data manipulation and serialization using little-endian encoding.

### interface_LittleEndianOrde_3more.md interface_LittleEndianOrde_3more.md
This document provides API reference documentation for the Cangjie language interface LittleEndianOrder, designed for handling little-endian byte sequence conversions. It details the interface definition and two core member functions: the static readLittleEndian for deserializing data from a byte array, and the instance writeLittleEndian for serializing data into a byte array. Key takeaways include the specific parameter types using Array<UInt8>, return values of type T or Int64, and the throwing of IllegalArgumentException for insufficient buffer sizes. This guide is intended for developers working on low-level data processing, serialization, or system programming tasks requiring precise byte order control.