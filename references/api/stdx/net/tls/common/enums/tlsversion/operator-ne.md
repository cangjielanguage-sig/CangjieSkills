<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.common.enum.tlsversion.operator-ne" parent="stdx.net.tls.common.enum.tlsversion" -->
# TlsVersion.!=

[← TlsVersion](index.md)

## 签名

```cangjie role=signature
public operator func !=(that: TlsVersion): Bool
```

比较两个 TlsVersion 是否不等。

## 参数

- that: TlsVersion - 待比较的 TLS 版本。

## 返回值

- Bool - 若不等返回 `true`，否则返回 `false`。

