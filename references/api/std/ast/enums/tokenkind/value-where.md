<!-- cj-doc kind="api-member" level="6" id="std.ast.enum.tokenkind.value-where" parent="std.ast.enum.tokenkind" -->
# TokenKind.WHERE

[← TokenKind](index.md)

## 签名

```cangjie role=signature
WHERE
```

构造一个表示 `where` 的枚举实例。
