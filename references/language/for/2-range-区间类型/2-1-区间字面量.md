<!-- cj-doc kind="guide-leaf" level="5" id="language.for.2-range-区间类型.2-1-区间字面量" parent="language.for.2-range-区间类型" -->
# 2.1 区间字面量

[← 2. Range 区间类型](index.md)

- **半开区间**（左闭右开）：`start..end` — 不包含 `end`
- **闭区间**（左闭右闭）：`start..=end` — 包含 `end`
- 可选步长 `: step`，省略时默认为 1；**step 不能为 0**
```cangjie cjtest=syntax id=syntax-aac3063ac4-1 form=stmt
for (i in 0..5) { print(i) }      // 输出：01234
for (i in 0..=5) { print(i) }     // 输出：012345
for (i in 0..10 : 2) { print(i) } // 输出：02468
```
