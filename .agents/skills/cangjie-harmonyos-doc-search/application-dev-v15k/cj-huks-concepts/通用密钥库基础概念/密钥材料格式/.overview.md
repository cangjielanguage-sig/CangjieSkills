# 密钥材料格式

This directory provides comprehensive technical specifications for the encoding and structural formats of cryptographic key materials within the HUKS framework. It covers the standardized binary layouts for public keys, private keys, and key pairs across various algorithms such as RSA, ECC, and DSA. The documentation details X.509 DER encoding, memory offsets, and byte-level structures essential for key import and export operations. It is designed for software developers, cryptographers, and security engineers implementing low-level key management, serialization, and storage mechanisms in systems like Cangjie.

## Quick Navigation

**What do you want to learn?**

*   Understand the general HUKS key material standards → 密钥材料格式.md
*   Learn about specific key types?
    *   Public Key encoding (X.509/DER) → 公钥材料格式.md
    *   Private Key structure and encapsulation → 私钥材料格式.md
    *   Key Pair binary layouts (RSA, ECC, etc.) → 密钥对材料格式/

## Detailed Description

### 公钥材料格式.md 公钥材料格式.md
This document specifies the standard encoding format used for public key material during export and import operations. It covers the X.509 specification and DER encoding, providing a concrete example of an ECC P-256 public key represented as a byte array in the Cangjie programming language. The primary takeaway is that key materials adhere to the X.509 DER format, ensuring interoperability and standard compliance. The provided code snippet illustrates the raw byte structure of an ECC public key. This reference material targets software developers and cryptographers implementing key management in Cangjie, requiring an understanding of cryptographic standards and binary data encoding. Keywords: X.509, DER, ECC, Public Key, Cangjie, Cryptography.

### 密钥材料格式.md 密钥材料格式.md
This document serves as a technical reference defining the standardized formats for key materials within the HUKS framework. It details the specific structures required for key pairs, public keys, and private keys across various cryptographic algorithms. The primary takeaway is that these defined formats are mandatory for ensuring successful key import and export operations. This specification is intended for developers and security engineers implementing or interacting with the HUKS system, providing the necessary structural definitions to manage cryptographic keys effectively. Keywords include HUKS, key material format, cryptographic algorithms, key import, and key export.

### 私钥材料格式.md 私钥材料格式.md
This technical reference document defines the structural format for encapsulating private key material within a cryptographic system. It explains that private key material reuses the key pair material format, specifically by setting the public key length field in the header to zero and appending the private key body. The document provides a detailed Cangjie code example demonstrating the byte-level structure of an RSA 2048 private key, including fields for algorithm type, key size, modulus, and private exponent. This information is intended for developers and engineers implementing low-level key serialization or storage mechanisms.

### 密钥对材料格式/
This directory contains technical reference documents defining the binary memory layout and byte structure for cryptographic key pair materials. It covers specifications for various algorithms including RSA, ECC, DSA, DH, and Curve25519, detailing how to map variables to memory offsets and construct key byte arrays. The content explains the composition of key materials, including headers, original data, and length fields for variable components. It is suitable for software developers and system architects implementing low-level cryptographic storage or communication protocols who need to manually format raw key data.