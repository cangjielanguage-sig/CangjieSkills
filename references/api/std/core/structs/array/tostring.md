<!-- cj-doc kind="api-member" level="7" id="std.core.struct.array.tostring" parent="std.core.struct.array.extension.extend-t-array-t-where-t-tostring" -->
# Array<T>.toString

[← extend<T> Array<T> where T <: ToString](extensions/extend-t-array-t-where-t-tostring.md)

## 签名

```cangjie role=signature
public func toString(): String
```

将数组转换为可输出的字符串。

## 契约

字符串形如 "[1, 2, 3, 4, 5]"

返回值：

- String - 转化后的字符串。
