<!-- cj-doc kind="api-member" level="6" id="std.fs.enum.openmode.value-write" parent="std.fs.enum.openmode" -->
# OpenMode.Write

[← OpenMode](index.md)

## 签名

```cangjie role=signature
Write
```

构造一个 OpenMode 实例，指定以只写的方式打开文件，即文件存在时会将该文件截断为零字节大小，文件不存在则将创建文件。
