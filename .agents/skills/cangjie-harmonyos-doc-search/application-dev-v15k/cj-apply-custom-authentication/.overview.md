# cj-apply-custom-authentication

This directory provides a specialized guide for Cangjie language developers working within the Unified User Authentication Framework. It focuses on the technical implementation of switching from standard system authentication to custom business authentication, a process essential for applications requiring specific fallback methods such as payment passwords. The content covers the complete switching logic, details supported authentication types, and outlines configuration constraints. Developers will find instructions on configuring the `navigationButtonText` field within `WidgetParam` to generate a navigation button. The guide further explains the system response mechanism, specifically the return of result code `12500011` upon user interaction, which triggers the application to launch a custom interface. Additionally, it addresses compatibility issues, explicitly noting that lock screen password authentication cannot be used with this feature. This documentation is vital for ensuring secure, flexible, and user-compliant authentication flows in Cangjie-based applications.

## Quick Navigation

**What do you want to learn?**

*   **Implement the authentication switching logic** → cj-apply-custom-authentication.md
    *   Understand the mechanism to transition from system to custom authentication.
*   **Configure UI elements and parameters** → cj-apply-custom-authentication.md
    *   Learn how to set `navigationButtonText` in `WidgetParam` to display the navigation button.
*   **Handle system result codes** → cj-apply-custom-authentication.md
    *   Find out how to interpret and act upon result code `12500011` to launch custom interfaces.
*   **Identify technical constraints** → cj-apply-custom-authentication.md
    *   Review limitations, including the incompatibility with lock screen password authentication.
*   **Target audience and use cases** → cj-apply-custom-authentication.md
    *   Determine if this guide fits your needs as a Cangjie developer integrating payment passwords.

## Detailed Description

### cj-apply-custom-authentication.md

This file serves as the primary instructional document for the directory. It explains how to implement a mechanism for switching from system authentication to custom business authentication within the Unified User Authentication Framework. It covers the switching logic, supported authentication types, and configuration constraints. Key information includes configuring the `navigationButtonText` field in `WidgetParam` to display a navigation button. When users click this button, the system returns result code `12500011`, signaling the application to launch its custom interface. The document notes that lock screen password authentication is incompatible with this feature. This tutorial targets application developers using the Cangjie language who need to integrate fallback authentication methods like payment passwords.