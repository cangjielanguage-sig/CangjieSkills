# interface_ArbitraryT

This directory contains API documentation for the `Arbitrary` interface within the Cangjie property-based testing framework. It details how various data types implement this interface to enable automatic random value generation for testing. The content covers fundamental numeric types, strings, collections, and core data structures. Key concepts include the `RandomSource` parameter, `Generator` return types, and interface extensions. This documentation is suitable for software developers and QA engineers working with the `unittest_prop_test` package who need to generate random test data or implement custom property-based tests in Cangjie.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `Arbitrary` interface definition and basic implementations? → interface_ArbitraryT_5more.md
*   Generate random collection instances (Array, ArrayList, HashMap, etc.)? → extendT_ArrayT_ArbitraryAr_3more.md, extendT_HashSetT_Arbitrary_2more.md
*   Generate random numeric values (Integers, Floats, Unsigned)? → extend_Float64_ArbitraryFl_4more.md, extend_Int8_ArbitraryInt8_4more.md, extend_String_ArbitraryStr_4more.md, extend_UInt8_ArbitraryUInt_3more.md

## Detailed Description

### extendT_ArrayT_ArbitraryAr_3more.md extendT_ArrayT_ArbitraryAr_3more.md
This API documentation details the implementation of the Arbitrary interface for generic collection types Array, Option, and ArrayList within the Cangjie property testing framework. The document covers three specific extensions where each type implements Arbitrary provided the element type T also satisfies the Arbitrary constraint. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator capable of producing random instances of the collection. This reference material is intended for developers implementing property-based tests who need to generate random data structures for standard collections.

### extendT_HashSetT_Arbitrary_2more.md extendT_HashSetT_Arbitrary_2more.md
This API documentation file details the implementation of the Arbitrary interface for HashSet and HashMap collection types in the Cangjie language. It covers two main extensions: one for HashSet and another for HashMap, specifying the constraints where element types must also implement Arbitrary. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator to produce random instances of these collections. This enables property-based testing by allowing developers to generate random hash sets and maps. The target audience consists of software developers utilizing the unittest_prop_test framework who need to generate random test data for complex collection types.

### extend_Float64_ArbitraryFl_4more.md extend_Float64_ArbitraryFl_4more.md
This API documentation file details the implementation of the Arbitrary interface for fundamental numeric types in the Cangjie language. The document covers four specific extensions: Float64, Int16, Int32, and Int64, each implementing the Arbitrary interface to support property-based testing. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator capable of producing random values of that specific numeric type. Intended for developers utilizing property-based testing frameworks, this reference enables the generation of random test data for standard numeric types.

### extend_Int8_ArbitraryInt8_4more.md extend_Int8_ArbitraryInt8_4more.md
This API reference document details the implementation of the Arbitrary interface for four specific data types in the Cangjie programming language. The main sections cover extensions for Int8, IntNative, Ordering, and Rune, enabling them to support random value generation. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator to produce random instances. This documentation is intended for developers using the unittest_prop_test package to facilitate property-based testing by providing the necessary mechanisms to generate random test data for these core types.

### extend_String_ArbitraryStr_4more.md extend_String_ArbitraryStr_4more.md
This API documentation file details the implementation of the Arbitrary interface for the String, UInt16, UInt32, and UInt64 data types within the Cangjie programming language. The document is organized into four distinct sections, each extending a specific type to conform to the Arbitrary interface. Key information includes the static arbitrary function defined for each type, which accepts a RandomSource parameter and returns a Generator capable of producing random values for that specific type. This reference material is intended for developers implementing property-based testing who require the ability to generate random test data for these fundamental types.

### extend_UInt8_ArbitraryUInt_3more.md extend_UInt8_ArbitraryUInt_3more.md
This API documentation file details the implementation of the Arbitrary interface for the UInt8, UIntNative, and Unit types in the Cangjie programming language. The document is divided into three main sections, each describing an extension for a specific type: UInt8, UIntNative, and Unit. All sections follow a consistent structure, defining the interface implementation and a static function. Key information includes the static func arbitrary method, which accepts a RandomSource parameter and returns a Generator for the respective type. This enables the generation of random values for property-based testing. The target audience is developers working with property testing frameworks in Cangjie who need to understand how to generate random instances of these fundamental types.

### interface_ArbitraryT_5more.md interface_ArbitraryT_5more.md
This API reference document defines the Arbitrary interface for generating random values of type T within the Cangjie property-based testing framework. It details the interface's static arbitrary function and documents specific implementations for the Bool, Float16, and Float32 data types. The core functionality involves taking a RandomSource input and returning a Generator to produce random instances. Each type extension implements this interface to support automated test case generation. This technical documentation targets developers implementing or extending property-based tests who need to understand how random value generation works for primitive types.