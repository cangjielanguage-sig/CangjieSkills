<!-- cj-doc kind="api-member" level="6" id="stdx.net.tls.enum.signatureschemetype.tostring" parent="stdx.net.tls.enum.signatureschemetype" -->
# SignatureSchemeType.toString

[← SignatureSchemeType](index.md)

## 签名

```cangjie role=signature
public func toString(): String
```

加密算法类型的字符串表示。

## 契约

如 `RSA_PKCS1_SHA256` 的字符串表示为 "rsa_pkcs1_sha256"。

返回值：

- String - 加密算法类型的字符串表示。
