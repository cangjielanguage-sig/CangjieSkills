<!-- cj-doc kind="api-member" level="6" id="std.unittest.prop_test.interface.randomsource.suggestrune" parent="std.unittest.prop_test.interface.randomsource" -->
# RandomSource.suggestRune

[← RandomSource](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## func suggestRune()

### 签名

```cangjie role=signature
public open func suggestRune(): Rune
```

获取一个 Rune 类型的伪随机值。

### 契约

返回值：

- Rune - 一个 Rune 类型的伪随机数。

## func suggestRune()

适用扩展：[extend Random](extensions/extend-random.md)。

### 签名

```cangjie role=signature
public open func suggestRune(): Rune
```

获取一个 Rune 类型的伪随机值。

### 契约

返回值：

- Rune - 一个 Rune 类型的伪随机数。
