# extendT_CPointerT

This directory contains API documentation for the `extend<T> CPointer<T>` extension in the Cangjie language, specifically designed to facilitate robust C interoperability and low-level memory management. It provides a comprehensive guide to essential interfaces for pointer manipulation, including resource management mechanisms like `asResource`, safety checks for null validation, and methods for data reading and writing. The documentation also delves into pointer arithmetic and address conversion, highlighting the necessity of unsafe contexts to prevent undefined behavior. This resource is suitable for systems programmers and developers working on Foreign Function Interface (FFI) tasks who require precise control over memory allocation, automatic resource cleanup, and direct pointer operations similar to those in the C language. By mastering these APIs, developers can safely integrate C libraries and manage memory lifecycles effectively within the Cangjie environment.

## Quick Navigation

**What do you want to learn?**

*   Understand core resource management, null validation, and data retrieval interfaces → extendT_CPointerT_6more.md
*   Learn about address conversion, data writing, and pointer arithmetic operators → func_toUIntNative_5more.md

## Detailed Description

### extendT_CPointerT_6more.md extendT_CPointerT_6more.md
This API documentation defines the extension `extend<T> CPointer<T>` for the Cangjie language, providing essential interfaces for C pointer manipulation. It covers resource management via `asResource`, null validation using `isNotNull` and `isNull`, and data retrieval through read methods. Key takeaways include the requirement for the generic type `T` to satisfy the `CType` constraint and the necessity of an unsafe context for read operations to prevent undefined behavior. This reference is intended for developers performing C interoperability who require low-level memory control and automatic resource cleanup.

### func_toUIntNative_5more.md func_toUIntNative_5more.md
This API documentation file details specific methods and operators for the `CPointer` type in the Cangjie language, focusing on memory manipulation and pointer arithmetic. It covers the `toUIntNative` function for address conversion, overloaded write functions for data insertion at specific indices or the start, and arithmetic operators `+` and `-` for pointer offsetting. Key information includes that write operations and pointer arithmetic are marked as unsafe, requiring users to ensure pointer validity to avoid undefined behavior. Examples demonstrate memory allocation, array manipulation, and pointer movement similar to C language behavior. This reference is intended for developers performing low-level memory management or FFI interactions who need to understand `CPointer` behavior.