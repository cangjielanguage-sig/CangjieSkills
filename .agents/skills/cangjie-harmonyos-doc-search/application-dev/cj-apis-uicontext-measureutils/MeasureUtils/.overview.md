# MeasureUtils

This directory provides API reference documentation for the MeasureUtils module within the ArkUI framework, specifically tailored for developers using the Cangjie language. It covers essential classes and methods for calculating text dimensions, configuring measurement attributes, and defining size options for component layouts. Key functionalities include single-line text width calculation, full layout dimension measurement, and the configuration of font styles and overflow handling. This documentation is suitable for HarmonyOS developers working with ArkUI system version 22 or higher who require precise control over text layout and component sizing.

## Quick Navigation

**What do you want to learn?**

*   Calculate text width and full layout dimensions using the main utility class? → MeasureUtils_3more.md MeasureUtils API
*   Configure text measurement attributes such as font style, layout constraints, and overflow? → class_MeasureOptions.md MeasureOptions Class
*   Define width and height dimensions for component layouts? → class_SizeOptions.md SizeOptions Class

## Detailed Description

### MeasureUtils_3more.md MeasureUtils_3more.md
This document provides API reference documentation for the MeasureUtils class in the ArkUI framework, designed to calculate text width and height. It details the class definition, import requirements, and system capabilities, focusing on two primary methods: `measureText` for single-line width and `measureTextSize` for full layout dimensions. Key takeaways include the prerequisite of obtaining a MeasureUtils instance via `UIContext.getMeasureUtils()` and the use of `MeasureOptions` for input parameters, with results returned in pixels. Intended for developers using the Cangjie language, this guide supports ArkUI system version 22 and is essential for precise text layout calculations.

### class_MeasureOptions.md class_MeasureOptions.md
This API reference document defines the MeasureOptions class in the Cangjie language, designed to configure attributes for text measurement within the ArkUI framework. The documentation focuses on the class constructor, detailing a comprehensive list of parameters that control text content, font styles, layout constraints, and overflow handling. Key information includes a parameter table specifying data types, default values like `FontWeight.Normal` and `16.fp`, and descriptions for properties such as `maxLines`, `textAlign`, and `wordBreak`. Intended for HarmonyOS developers using Cangjie, this reference assists in setting up text measurement logic and requires familiarity with ArkUI components and layout systems.

### class_SizeOptions.md class_SizeOptions.md
This document provides API reference documentation for the Cangjie SizeOptions class, which defines width and height dimensions for component layouts. The main sections cover the class definition, public properties for width and height, and the class constructor. Key information indicates that both properties are of the Length type, are readable and writable, and default to 0. The constructor accepts named parameters for these dimensions. This class belongs to the SystemCapability.ArkUI.ArkUI Full capability and is available starting from version 22. The intended audience is ArkUI developers using Cangjie who need to programmatically specify component sizes.