<!-- cj-doc kind="guide-leaf" level="5" id="tools.cjpm.2-项目结构与-cjpm-toml.2-4-示例主程序" parent="tools.cjpm.2-项目结构与-cjpm-toml" -->
# 2.4 示例主程序

[← 2. 项目结构与 cjpm.toml](index.md)

提供可独立构建的示例，演示示例主程序。

```cangjie cjtest=syntax id=syntax-243cd2d5de-1 form=unit
// main.cj —— 可执行项目入口
package myapp

import std.collection.*

main(): Int64 {
    let list = ArrayList<Int64>([1, 2, 3])
    for (v in list) {
        println(v)
    }
    return 0
}
```

---
