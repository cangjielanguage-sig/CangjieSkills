# class_File

This directory provides comprehensive API reference documentation for the `File` class and related concurrency functions within the Cangjie programming language. It is centered on the `SystemCapability.FileManagement.File.FileIO` framework, offering detailed guidance on file object management, property access, and synchronization mechanisms. The content covers essential read-only attributes such as file descriptors (`fd`), names, and paths, as well as instance methods for directory retrieval and non-blocking file locking. Additionally, it includes specifications for releasing locks via the `unlock` function. With a strong emphasis on error handling through `BusinessException` and defined error codes, this documentation is tailored for developers implementing secure and efficient file I/O operations, managing shared or exclusive access, and handling system-level exceptions in Cangjie applications.

## Quick Navigation

**What do you want to learn?**
*   Understand the core structure of the File class, including its read-only properties (fd, name, path) and instance methods for directory management → class_File_6more.md
*   Learn about the unlock() function, its system capabilities, version requirements, and specific error codes for releasing locks → func_unlock.md

**What do you want to do?**
*   Implement file object management, retrieve parent directories, or apply non-blocking file locking (shared or exclusive) in your application → class_File_6more.md
*   Manage file concurrency and access control by properly releasing file locks and handling potential exceptions → func_unlock.md

## Detailed Description

### class_File_6more.md class_File_6more.md
This document serves as API reference documentation for the File class in the Cangjie programming language, detailing file object management within the SystemCapability.FileManagement.File.FileIO framework. It covers read-only properties including fd, name, and path, alongside instance methods such as getParent for retrieving parent directories and tryLock for non-blocking file locking. Key information includes the read-only nature of attributes, support for shared or exclusive locks, and specific error handling via BusinessException with defined error codes. Intended for developers implementing file I/O operations, this guide assumes familiarity with Cangjie syntax and exception handling.

### func_unlock.md func_unlock.md
This document provides API reference documentation for the unlock() function in the Cangjie programming language, designed to release file locks. The content covers the function signature, system capabilities, version requirements, and a detailed table of potential BusinessException error codes. It also includes a practical code example demonstrating file opening, locking, unlocking, and error handling. Key takeaways include specific error codes related to file operations and the necessity of exception handling. The target audience is software developers working with file I/O in Cangjie who need to manage file concurrency and access control.