<!-- cj-doc kind="api-member" level="6" id="std.ast.class.typenode.prop-typeparametername" parent="std.ast.class.typenode" -->
# TypeNode.typeParameterName

[← TypeNode](index.md)

## 签名

```cangjie role=signature
public mut prop typeParameterName: Token
```

获取或设置类型节点的参数，如：`(p1:Int64, p2:Int64)` 中的 `p1` 和 `p2`，可能为 ILLEGAL 的词法单元。

## 契约

类型：Token
