# interface_Formattable

This directory contains API reference documentation for the `Formattable` interface in the Cangjie programming language. It covers the core interface definition and its extensions across various numeric data types, including signed and unsigned integers (Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64), floating-point numbers (Float16, Float32, Float64), and the Rune type. The documents detail the `format` function, which converts instances to formatted strings, and handle exceptions like `IllegalArgumentException`. This resource is suitable for Cangjie developers looking to implement or utilize string formatting capabilities for primitive types within their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and Float16/Float32 support → interface_Formattable_4more.md
*   Format Float64, Int16, or Int32 values → extend_Float64_Formattable_3more.md
*   Format Int64, Int8, or Rune values → extend_Int64_Formattable_3more.md
*   Format unsigned integers (UInt16, UInt32, UInt64) → extend_UInt16_Formattable_3more.md
*   Format unsigned 8-bit integers (UInt8) → extend_UInt8_Formattable.md

## Detailed Description

### extend_Float64_Formattable_3more.md
This API reference document details the implementation of the Formattable interface for Float64, Int16, and Int32 data types in the Cangjie programming language. The content is organized into three distinct sections, each describing an extension for a specific numeric type and its associated format function. Key information includes the function signature public func format(fmt: String): String, which converts numeric instances into formatted strings based on a provided format specifier, and the handling of IllegalArgumentException for invalid format strings. This documentation targets Cangjie developers who need to perform string formatting on numeric values, assuming familiarity with the language's type system and exception handling.

### extend_Int64_Formattable_3more.md
This API reference document details the implementation of the Formattable interface for Int64, Int8, and Rune types within the Cangjie standard library. The content is organized into three distinct sections, each describing the extension of a specific primitive type to support string formatting. Key information includes the public format function, which accepts a format string parameter and returns a formatted string representation of the instance. Developers should note that this function throws an IllegalArgumentException if the provided format string is invalid. This documentation serves as a technical guide for developers requiring precise string conversion capabilities for numeric and character types in Cangjie applications.

### extend_UInt16_Formattable_3more.md
This API documentation describes the extension of the Formattable interface for the UInt16, UInt32, and UInt64 data types in the Cangjie programming language. The document details the public format function for each type, which accepts a format string parameter and returns a formatted string representation of the integer instance. Key information includes the function's signature, its return type, and the specific exception, IllegalArgumentException, thrown when the format string is invalid. This reference material is intended for Cangjie developers who need to convert unsigned integer values into specific string formats for output or data processing.

### extend_UInt8_Formattable.md
This API documentation describes the extension of the UInt8 type to implement the Formattable interface within the Cangjie programming language. The content focuses on the extension definition and the specific format function required for string conversion. Key details include the public format method, which takes a string parameter to define the output style and returns the formatted string representation of the UInt8 instance. It also notes that an IllegalArgumentException is raised if the format string is invalid. This reference guide is intended for Cangjie developers seeking to customize the string output of unsigned 8-bit integers.

### interface_Formattable_4more.md
This API documentation defines the Formattable interface in the Cangjie language, which enables type instances to be converted into formatted strings. The document covers the interface definition, specifically the format(fmt: String): String function, and details its extensions for the Float16 and Float32 numeric types. Key information includes the function parameters, return values, and the IllegalArgumentException thrown for invalid format strings. The text explains that other types can implement this interface to provide custom formatting capabilities. This reference material is intended for Cangjie developers working with string conversion and the convert package, requiring knowledge of basic type systems and exception handling.