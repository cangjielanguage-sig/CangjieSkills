<!-- cj-doc kind="api-member" level="6" id="std.net.class.socketaddress.getaddressbytes" parent="std.net.class.socketaddress" -->
# SocketAddress.getAddressBytes

[← SocketAddress](index.md)

## 签名

```cangjie role=signature
public func getAddressBytes(): Array<Byte>
```

返回此 SocketAddress 对象的原始 IP 地址。

## 契约

返回值：

- Array\<Byte> - 原始 IP 地址的 Array\<Byte> 表示。
