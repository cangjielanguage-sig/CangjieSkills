# class_VideoSession

This directory contains API reference documentation for the `VideoSession` class within the Cangjie language CameraKit. It covers the implementation of standard video recording modes, including capabilities like flash, exposure, focus, zoom, and stabilization. The documentation details methods for querying hardware support (`canPreconfig`), preconfiguring sessions (`preconfig`), and managing event listeners for errors, focus states, and smooth zoom operations (`on`/`off`). It is intended for developers building HarmonyOS camera applications who need to handle video session configuration and event monitoring.

## Quick Navigation

**What do you want to do?**

- Understand the VideoSession class structure and capabilities → class_VideoSession_2more.md
- Check if specific configurations are supported by hardware → class_VideoSession_2more.md
- Preconfigure the session with specific resolution and aspect ratio → func_preconfigPreconfig_9c44a0ad.md
- Register event listeners for monitoring → func_onCameraEvents_Callback0Argument.md (Errors), func_onCameraEvents_Callback1ArgumentFocusState.md (Focus), func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md (Smooth Zoom)
- Unregister event listeners (cancel callbacks) → func_offCameraEvents_Callb_2more.md

## Detailed Description

### class_VideoSession_2more.md class_VideoSession_2more.md
This document serves as API reference documentation for the VideoSession class in the Cangjie language, designed for standard video recording modes. It details the class structure, which inherits capabilities for flash, exposure, focus, zoom, and stabilization. The primary focus is the canPreconfig method, which queries support for specific resolution types and aspect ratios. Key information includes parameter definitions, return values, potential business exceptions, and a usage example. This guide is intended for developers working with the CameraKit who need to implement video recording features and verify hardware configuration support.

### func_offCameraEvents_Callb_2more.md func_offCameraEvents_Callb_2more.md
This API reference document details the overloaded `off` functions used to unregister event listeners for camera sessions in the Cangjie programming language. It covers two specific function signatures: one for canceling smooth zoom status callbacks and another for removing error event listeners. Key parameters include `eventType` (specifically `SmoothZoomInfoAvailable` for the first overload) and an optional `callback` argument. Both functions require SystemCapability.Multimedia.Camera.Core, start from version 22, and may throw a BusinessException with error code 7400201. This guide targets developers working on HarmonyOS camera applications who need to manage event listeners within video sessions.

### func_onCameraEvents_Callback0Argument.md func_onCameraEvents_Callback0Argument.md
This API reference document details the Cangjie function `on` used to monitor error events during normal video recording sessions. The documentation outlines the function signature, system capabilities, and parameters, specifically requiring `CameraError` as the event type and `Callback0Argument` for handling results. Key information includes the restriction against calling `off` within the callback and the specific error code 7400201 for camera service failures. The text explains that errors are triggered by session operations like configuration or input addition. Intended for developers using the CameraKit, this guide provides the necessary syntax and exception handling details to implement robust error monitoring in camera applications.

### func_onCameraEvents_Callback1ArgumentFocusState.md func_onCameraEvents_Callback1ArgumentFocusState.md
This API documentation details the `on` function used to monitor camera focus state changes in the Cangjie language. The document outlines the function signature, system capabilities, parameters, and potential exceptions. Key information includes the requirement for the event type to be `FocusStateChange` and the restriction against calling the `off` method within the callback. It provides a code example demonstrating how to implement the callback class and register it with a video session. Intended for developers using the CameraKit, this reference requires an understanding of session creation and auto-focus modes.

### func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md func_onCameraEvents_Callback1ArgumentSmoo_c500d985.md
This API reference document details the Cangjie function for monitoring camera smooth zoom status changes. It covers the function signature, system capabilities, required parameters, and potential exceptions. Key information includes the requirement to use SmoothZoomInfoAvailable as the event type and the restriction against unregistering callbacks within the listener. The document provides a code example demonstrating how to implement the Callback1Argument interface and register the listener on a VideoSession. Intended for developers using the CameraKit, this guide requires knowledge of Cangjie syntax and camera session management.

### func_preconfigPreconfig_9c44a0ad.md func_preconfigPreconfig_9c44a0ad.md
This document serves as API reference documentation for the preconfig function in the Cangjie language, designed to preconfigure camera sessions. The content details the function signature, system capabilities, and parameters, specifically the required PreconfigType for resolution and the optional PreconfigRatio for aspect ratio, which defaults to 16:9. It also outlines potential exceptions, such as camera service fatal errors, and provides a practical code example demonstrating usage within a VideoSession context. Targeted at developers working with multimedia camera features, this guide requires knowledge of the CameraKit and exception handling to effectively implement session preconfiguration.