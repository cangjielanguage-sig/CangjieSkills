<!-- cj-doc kind="api-member" level="6" id="std.net.class.ipv4address.field-localhost" parent="std.net.class.ipv4address" -->
# IPv4Address.localhost

[← IPv4Address](index.md)

## 签名

```cangjie role=signature
public static let localhost: IPv4Address = IPv4Address(0x7F, 0, 0, 0x01)
```

返回 IPv4Address 的 `localhost` 地址：`127.0.0.1`。

## 契约

类型：IPv4Address
