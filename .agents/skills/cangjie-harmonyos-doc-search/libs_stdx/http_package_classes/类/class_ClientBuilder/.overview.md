# class_ClientBuilder

This directory contains API documentation for the `ClientBuilder` class in the Cangjie language, which acts as the exclusive constructor for HTTP Client instances. It covers the builder pattern implementation, including initialization, configuration of redirects, cookies, server push, and custom connectors. The documentation also details HTTP/2 protocol settings, network proxy configuration, and connection management features like timeouts and TLS. It is suitable for software developers and system integrators who need to instantiate and fine-tune HTTP clients with specific behaviors, security settings, and performance constraints in Cangjie applications.

## Quick Navigation

What do you want to learn?

- Understand the core class structure, initialization, and basic configuration? → class_ClientBuilder_7more.md
- Configure HTTP/2 parameters, network proxies, or logging? → func_headerTableSizeUInt32_8more.md
- Adjust connection pooling, timeouts, or TLS security settings? → func_noProxy_5more.md

## Detailed Description

### class_ClientBuilder_7more.md
This API documentation defines the ClientBuilder class, which serves as the exclusive constructor for HTTP Client instances in the Cangjie language. The document details the initialization method, configuration functions for redirects, cookies, server push, and custom connectors, and the final build method. Key information includes that the class employs a builder pattern where parameter validation occurs strictly during the build phase, potentially throwing exceptions for illegal values. It supports both HTTP/1.1 and HTTP/2 protocols. This reference guide targets developers needing to configure and instantiate HTTP clients with specific behaviors like connection handling and cookie management.

### func_headerTableSizeUInt32_8more.md
This document serves as API reference documentation for configuring the ClientBuilder class within the Cangjie HTTP package. It details methods for setting HTTP/2 parameters such as header table size, initial window size, max concurrent streams, max frame size, and max header list size. Additionally, it covers network proxy configuration for HTTP and HTTPS, along with logger setup. Key takeaways include specific default values for HTTP/2 settings and the requirement for thread-safe loggers. All methods return the ClientBuilder instance to support a fluent interface design. The target audience is software developers integrating or customizing HTTP client behavior, specifically those needing fine-grained control over HTTP/2 protocol settings and network routing.

### func_noProxy_5more.md
This document provides API reference documentation for configuring the HTTP ClientBuilder in the Cangjie programming language. It covers five specific methods: noProxy, poolSize, readTimeout, tlsConfig, and writeTimeout. Key takeaways include that all functions return the ClientBuilder instance to support method chaining, poolSize defaults to 10 and throws HttpException for invalid inputs, and read/write timeouts default to 15 seconds. This reference is intended for developers building HTTP clients who need to customize network behavior, including connection limits, security via TLS, and request duration constraints.