# ohoscomponent_snapshot组件截图

This directory contains API reference documentation for the `ohos.component_snapshot` module and related classes within the Cangjie UIKit framework. It focuses on enabling developers to capture screenshots of UI components, covering both loaded and unloaded elements. Key topics include configuration options for scaling and rendering synchronization, as well as methods for asynchronous, synchronous, and off-screen rendering. This documentation is suitable for HarmonyOS and ArkUI developers using the Cangjie language who need to implement precise image capture features, handle visual effects, or manage component rendering boundaries in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the module overview and general usage for capturing UI components → ohoscomponent_snapshot组件截图_3more.md
*   Configure snapshot parameters like scaling and rendering synchronization → class_CJSnapshotOptions_2more.md
*   Explore specific class methods for async, sync, and off-screen rendering → class_componentSnapshot/

## Detailed Description

### class_CJSnapshotOptions_2more.md class_CJSnapshotOptions_2more.md
This document serves as API reference documentation for the CJSnapshotOptions class in the Cangjie language, designed to configure snapshot parameters within the ArkUI framework. It covers the class constructor, two read-only properties (scale and waitUntilRenderFinished), and the SnapshotRetCallBack type alias. Key information includes the scale property for defining pixelmap resolution, with warnings about performance impacts for large sizes, and the waitUntilRenderFinished flag to ensure the latest visual state by forcing render completion. This guide is intended for developers implementing screenshot features who need precise control over image scaling and rendering synchronization.

### ohoscomponent_snapshot组件截图_3more.md ohoscomponent_snapshot组件截图_3more.md
This API reference document outlines the functionality of the ohos.component_snapshot module, which enables capturing screenshots of both loaded and unloaded UI components within the Cangjie UIKit framework. The content covers module import instructions and detailed usage explanations regarding rendering boundaries and visual effects. Crucially, snapshots are limited to the component's defined size; overflow content and sibling components are excluded. Empty areas render as transparent pixels, and image effects may alter results. Developers are advised to consider window snapshots for full-screen needs. This guide targets HarmonyOS developers using the Cangjie language who need to implement component-level image capture features.

### class_componentSnapshot/
This directory contains API reference documentation for the `componentSnapshot` class within the Cangjie programming environment and ArkUI framework. It covers the functionality required to capture screenshots of UI components, including both asynchronous and synchronous methods, as well as off-screen rendering capabilities. Key topics include the class declaration, system capability requirements, and specific static functions like `get`, `getSync`, and `createFromBuilder`. This documentation is suitable for HarmonyOS and ArkUI developers using Cangjie who need to implement visual capture features, handle component snapshots, or render custom components programmatically in their applications.