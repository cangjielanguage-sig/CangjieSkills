# 类

This directory provides API documentation for essential I/O classes in the Cangjie standard library, focusing on stream management, data buffering, and text processing. It includes utilities for chaining streams, optimizing read/write operations via buffering, and handling in-memory byte buffers. Additionally, it covers classes for converting between streams and strings, supporting various encodings and data types. These resources are suitable for Cangjie developers seeking to build efficient, robust applications with advanced input/output capabilities and resource management.

## Quick Navigation

**What do you want to do?**

*   Aggregate multiple streams for sequential reading or simultaneous writing → class_ChainedInputStreamT__2more.md
*   Enhance I/O performance with buffering capabilities → [2] (Input), [3] (Output)
*   Manage in-memory byte data and cursor positioning → [4]
*   Process text, read characters, or write encoded strings → [5] (Reader), [6] (Writer)

## Detailed Description

### class_ChainedInputStreamT__2more.md class_ChainedInputStreamT__2more.md
This API documentation defines two utility classes, ChainedInputStream and MultiOutputStream, for managing multiple streams within the IO package. The text details class definitions, constructors, and core methods for both. ChainedInputStream enables sequential reading from an array of InputStreams, while MultiOutputStream allows simultaneous writing to an array of OutputStreams. Key information includes parameter requirements, return types like Int64, and exceptions such as IllegalArgumentException for empty arrays. The document also provides a usage example for ChainedInputStream. This reference guide targets developers using the Cangjie language who need to aggregate input streams or duplicate output streams efficiently.

### [2] class_BufferedInputStreamT_where_T_InputStream/
This directory contains API documentation for the `BufferedInputStream<T>` class in the Cangjie standard library, designed to optimize data reading by wrapping input streams with buffering capabilities. It covers the class definition, initialization methods, and extensions for resource management and seekable operations. Key topics include buffer configuration, automatic cleanup via the `Resource` interface, random access via the `Seekable` interface, and core reading functions. This documentation is suitable for Cangjie developers looking to implement efficient I/O operations, manage stream lifecycles, and handle buffered data streams with generic types.

### [3] class_BufferedOutputStreamT_where_T_OutputStream/
This directory contains API documentation for the `BufferedOutputStream` class in the Cangjie programming language. It focuses on a generic output stream wrapper that enhances I/O performance through buffering. The documentation covers class initialization, data writing methods, and extensions for automatic resource management and random access operations. Key topics include buffer capacity configuration, the `Resource` interface for safe disposal, and the `Seekable` interface for cursor manipulation. This resource is suitable for Cangjie developers implementing efficient, safe, and advanced I/O operations in their applications.

### [4] class_ByteBuffer/
This directory contains comprehensive API reference documentation for the `ByteBuffer` class within the Cangjie standard library. It covers the implementation of IOStream and Seekable interfaces for efficient in-memory byte stream manipulation. Key topics include buffer initialization, capacity management, data reading and writing operations, and cursor positioning. The documentation details methods for handling byte arrays, managing buffer growth, and exception handling. It is suitable for Cangjie developers looking to perform low-level I/O operations, manage memory buffers, and manipulate byte data streams effectively in their applications.

### [5] class_StringReaderT_where_T_InputStream/
This directory contains API documentation for the `StringReader` class in the Cangjie programming language, designed to read and convert data from input streams into characters or strings. It covers core functionality including stream reading, resource management, and cursor manipulation. Key topics include UTF-8 encoding support, buffer management, and specific parsing methods like `readUntil` and `runes`. This documentation is suitable for developers working with IO streams who need efficient text processing, resource handling, and precise stream navigation capabilities in Cangjie applications.

### [6] class_StringWriterT_where_T_OutputStream/
This directory provides comprehensive API reference documentation for the Cangjie `StringWriter` class and its associated I/O functions. It focuses on the mechanisms for converting strings and primitive data types into encoded bytes for writing to output streams. Key topics include the class constructor, internal buffer management (flush), and extensive overloads for `write` and `writeln` methods supporting various numeric, string, and generic types. Additionally, it covers interface implementations for Resource and Seekable capabilities. This documentation is suitable for Cangjie developers working on data serialization, stream management, and output operations who require detailed syntax and parameter information.