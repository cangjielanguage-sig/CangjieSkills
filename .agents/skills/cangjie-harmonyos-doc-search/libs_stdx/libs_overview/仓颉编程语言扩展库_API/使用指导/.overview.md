# 使用指导

This directory serves as a comprehensive resource for developers working with the Cangjie programming language, specifically focusing on the integration and usage of standard extension libraries (stdx). It provides essential technical guidance on compiling programs that rely on complex dependencies such as crypto, encoding, and networking modules. The content covers critical aspects of the development workflow, including environment variable configuration, the setup of the `cjc` compiler and `cjpm` build tool, and the management of static and dynamic binary artifacts. Furthermore, it addresses OS-specific linking requirements for Linux, macOS, and Windows. This documentation is primarily suitable for Cangjie developers seeking to streamline their build processes and resolve dependency linking issues in projects utilizing the stdx ecosystem.

## Quick Navigation

**What do you want to learn?**

*   How to execute compilation commands for projects using multiple stdx libraries? → cjc_使用命令示例_2more.md
*   How to configure environment variables like `CANGJIE_STDX_PATH` and runtime paths? → cjc_使用命令示例_2more.md
*   How to set up `cjpm.toml` for binary dependencies based on system architecture? → cjc_使用命令示例_2more.md
*   How to understand the binary structure and dependency mapping of the stdx library? → 使用指导_3more.md
*   What is the strict linking order required by the `cjc` compiler versus `cjpm`? → 使用指导_3more.md
*   What are the specific linking flags for crypto and network libraries on different OSs? → 使用指导_3more.md

## Detailed Description

### cjc_使用命令示例_2more.md cjc_使用命令示例_2more.md
This technical guide provides compilation examples for Cangjie programs utilizing multiple standard extension libraries (stdx). It covers cjc compiler commands for Linux/macOS and Windows, environment variable configuration for library paths, and cjpm build tool setup. The document details specific linking flags for modules like crypto, encoding, and networking, emphasizing the setup of CANGJIE_STDX_PATH and runtime library paths. It also explains configuring cjpm.toml for binary dependencies based on system architecture. This guide targets Cangjie developers who need to integrate and compile projects with complex stdx dependencies.

### 使用指导_3more.md 使用指导_3more.md
This document serves as a usage guide for the Cangjie programming language's stdx extension library, detailing its binary structure and dependency management. It covers the organization of static and dynamic binary artifacts and provides a comprehensive table mapping import statements to their required packages. Key takeaways include the strict linking order required when using the cjc compiler, whereas cjpm handles this automatically. Additionally, it specifies OS-specific linking flags for crypto and network libraries on Windows and Linux. This guide targets Cangjie developers integrating the stdx library into their projects.