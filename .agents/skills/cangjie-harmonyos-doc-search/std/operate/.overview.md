# operate

This directory serves as a comprehensive technical resource for developers engaged in advanced metaprogramming within the Cangjie language ecosystem. It focuses specifically on the manipulation of Abstract Syntax Trees (ASTs), providing the necessary tools and knowledge to programmatically analyze, parse, and modify source code structures. The core content revolves around handling `ClassDecl` node types, demonstrating how to perform type casting, inject new member functions into existing classes, and utilize code generation functions like `quote` and `parseDecl`. By accessing the `body.decls` collection, developers can dynamically alter code behavior at the syntax level. This directory is ideal for compiler developers, language tool creators, and experienced Cangjie programmers who need to automate code refactoring, implement custom compiler passes, or build sophisticated development tools that require deep integration with the language's syntax tree. Through these examples, users will gain the confidence to implement complex syntax transformations and extend the capabilities of the Cangjie compiler toolchain.

## Quick Navigation

**What do you want to learn?**

*   **Fundamentals of AST Manipulation:** Do you want to understand how to parse source code into AST nodes and perform type casting in Cangjie? → operate.md
*   **Modifying Class Structures:** Are you looking for methods to dynamically add new member functions to existing class declarations programmatically? → operate.md
*   **Code Generation & Parsing:** Do you need practical examples of using the `quote` function for code generation and `parseDecl` for syntax analysis? → operate.md
*   **Verification Techniques:** Are you interested in learning how to verify AST manipulation operations by checking class identifiers and updated member counts? → operate.md

## Detailed Description

The following section provides a granular breakdown of the files available in this directory. Each file is summarized to highlight its specific contribution to understanding AST manipulation in the Cangjie language.

### operate.md operate.md

This document provides a practical code example demonstrating how to manipulate Abstract Syntax Tree (AST) objects, specifically focusing on the ClassDecl node type in the Cangjie language. The tutorial covers parsing source code into AST nodes, performing type casting, and modifying class structures by adding new member functions. Key takeaways include using the quote function for code generation, parseDecl for parsing, and accessing the body.decls collection to insert a FuncDecl. The example verifies the operation by printing the class identifier and the updated member count. This guide targets developers familiar with Cangjie syntax and AST concepts who need to programmatically modify code structures.