# 项目技术细节与优化记录

## 一、Token 消耗统计

### 1.1 背景

当前 LLM 离线增强流程（card 侧 rule+LLM 模式、graph 侧 enhance-graph）存在大规模 API 调用（graph 侧 9,336 个英文孤岛节点增强为 417 个），但全程缺少 Token 消耗的定量记录。

### 1.2 当前基础设施

| 组件 | 位置 | 状态 |
|------|------|------|
| graph.json 数据字段 | 顶层 `"input_tokens": 0, "output_tokens": 0` | 已存在，值为 0 |
| 序列化写入 | `graph/builder/builders/build.py:133-134` — `save_graph()` 将 `G.graph.input_tokens` / `G.graph.output_tokens` 写入 JSON | 管道就绪，数据源为空 |
| 多图合并聚合 | `graph/builder/builders/build.py:176-222` — `merge_graphs()` 累加子图 input/output tokens | 管道就绪，数据源为空 |
| 设计规格 | `graph/evals/datasets/DESIGN_SPEC.md:221-222` — `expected_token_input` / `expected_token_output` 已规划 | 未落地 |

### 1.3 缺失项

| 缺失项 | 位置 | 说明 |
|--------|------|------|
| API usage 解析 | `graph/builder/llm/enhancer.py` — `call_llm()` | 仅解析 `choices[0].message.content`，未提取返回 JSON 中的 `usage.prompt_tokens` / `usage.completion_tokens` |
| 批次累积传递 | `graph/builder/llm/pipeline.py` — `enhance_graph_from_files()` | 每批次调用后未累加 usage |
| 评测报告输出 | `graph/evals/run_eval.py` | 报告不包含 Token 统计 |
| Card 侧统计 | `card/builder/build_index_v3.py` (rule+LLM 模式) | 未统计 |

### 1.4 实施计划

<details>
<summary><b>第一步：enhancer.py 解析 usage</b></summary>

**文件**：`graph/builder/llm/enhancer.py`

当前 `call_llm()` 流程：
```
urllib.request → response body → json.loads → choices[0].message.content → 返回字符串
```

OpenAI 兼容 API 标准返回：
```json
{
  "choices": [{"message": {"content": "..."}}],
  "usage": {
    "prompt_tokens": 1234,
    "completion_tokens": 567,
    "total_tokens": 1801
  }
}
```

方案（推荐全局计数器）：
- 模块级维护 `_total_input_tokens` / `_total_output_tokens`
- `call_llm()` 内部解析 usage 后就地累加
- 暴露 `get_token_stats()` 供 pipeline.py 读取
</details>

<details>
<summary><b>第二步：pipeline.py 批次累积落盘</b></summary>

**文件**：`graph/builder/llm/pipeline.py`

在 `enhance_graph_from_files()` 批次循环结束后，读取 enhancer 的全局 Token 计数，写入 `G.graph["input_tokens"]` / `G.graph["output_tokens"]`。build.py 的 `save_graph()` 已自动序列化这两个字段。
</details>

<details>
<summary><b>第三步：card 侧</b></summary>

**文件**：`card/builder/build_index_v3.py`

rule+LLM 模式同样调用 LLM API，需在 LLM 调用处解析 usage，可复用 enhancer.py 的全局计数器模式。
</details>

<details>
<summary><b>第四步：评测报告端</b></summary>

在评测报告中增加 Token 统计行：
```
| Token 消耗 | 输入 | 输出 | 总计 |
|-----------|------|------|------|
| graph 增强 | xxx  | xxx  | xxx  |
| card 增强  | xxx  | xxx  | xxx  |
```
</details>

<details>
<summary><b>第五步：发布门禁接入</b></summary>

Token 异常检测规则：
- 消耗相比基线暴增 > 50% → gray_release
- 消耗相比基线暴增 > 100% → blocked
</details>

### 1.5 预估量级（未经实测）

| 项目 | 估算 |
|------|------|
| 增强节点数 | ~11,107（llm_enhanced 标记） |
| 单节点 input（文档 500-1500 字符） | ~300-800 tokens |
| 单节点 output（keywords + label_zh + description） | ~50-150 tokens |
| 预估总 input | ~3M-9M tokens |
| 预估总 output | ~500K-1.5M tokens |

### 1.6 回溯性估算（不重跑 LLM）

1. 读取 `graph.json` 中 `llm_enhanced` 节点的 `source_file`，定位原始文档
2. 统计文档字符数
3. 按比例估算：中文 ~1.5 token/char，英文 ~0.3 token/word
4. 加上 batch prompt 固定开销 + 输出字段长度

精度不够但可快速产出参考值。

---

## 二、Graph LLM 增强引擎

> 涉及文件：`graph/builder/llm/enhancer.py`、`graph/builder/llm/pipeline.py`、`graph/builder/build_cli.py`

### 2.1 threading.Thread 包装 urllib

**`enhancer.py:196-210`**

LLM API 调用使用原生 `urllib.request`（同步阻塞），通过 `threading.Thread` 包装为子线程执行，主线程以 `worker.join(timeout=LLM_TIMEOUT)` 限时等待。

```python
def call_llm(prompt: str) -> Optional[list[dict]]:
    result_holder: dict = {"result": None, "error": None}
    worker = threading.Thread(target=_call_llm_sync, args=(prompt, result_holder))
    worker.start()
    worker.join(timeout=LLM_TIMEOUT)
    if result_holder.get("error"):
        return None
    return result_holder.get("result")
```

`urllib.request.urlopen` 的 `timeout` 参数在 DNS 解析阶段不生效，Thread + join 是更保守的超时兜底。

### 2.2 单次请求超时

**`enhancer.py:31`**

`LLM_TIMEOUT = 600`（秒，可通过环境变量覆盖），同时作用于：
- `_call_llm_sync` 内 `urllib.request.urlopen(..., timeout=LLM_TIMEOUT)`
- `call_llm` 内 `worker.join(timeout=LLM_TIMEOUT)`

### 2.3 固定重试与退避

**`pipeline.py:38-44`**

```python
def _process_single_batch(batch_idx, batch, ...):
    for retry in range(3):
        result = call_llm(prompt)
        if result:
            break
        time.sleep(2)
```

固定 3 次 + 2s 间隔。与 card 侧的指数退避不同：graph 批次粒度粗（40000 字符），重试成本高，不宜激进。

### 2.4 ThreadPoolExecutor 并发处理

**`pipeline.py:65, 126-133`**

```python
def enhance_graph_from_files(..., max_workers: int = 5, ...):
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        for batch_idx, batch in enumerate(batches, 1):
            future = executor.submit(_process_single_batch, ...)
```

- 默认并发数 5，CLI `--max-workers` 可调
- IO 密集型：等待 LLM 返回的时间远大于 CPU 时间
- 每个 worker 独立调用 `call_llm()`，内部已有自己的 threading 包装

### 2.5 连续失败熔断（FAIL_ABORT）

**定义**：`enhancer.py:32` — `FAIL_ABORT = 50`
**执行**：`pipeline.py:148-155`

```python
if FAIL_ABORT > 0 and consecutive_fails >= FAIL_ABORT:
    print(f"\n  [熔断] 连续失败 {consecutive_fails} 批 (>= {FAIL_ABORT})，自动停止。")
    executor.shutdown(wait=False, cancel_futures=True)
    break
```

- 阈值 50 批 ≈ 万级节点总批次数（~200 批）的 25%
- 区分"偶发网络故障"和"API 配置错误"
- `consecutive_fails` 在任意批次成功后归零——仅统计连续失败
- 熔断后 `executor.shutdown(wait=False, cancel_futures=True)` 立即取消待执行任务

### 2.6 全流程超时（total_timeout）

**`pipeline.py:67, 137-140`**

```python
for future in as_completed(futures):
    if total_timeout > 0 and (time.time() - start_time) > total_timeout:
        executor.shutdown(wait=False, cancel_futures=True)
        break
```

- 默认 0（禁用）
- 与 FAIL_ABORT 互补：熔断针对连续错误，超时针对一切正常但太慢

### 2.7 断点续传（Resume）

**`pipeline.py:88-101`**

```python
if resume:
    enhanced_ids = {n["id"] for n in nodes_dict.values() if n.get("llm_enhanced")}
    source_files = [sf for sf in source_files if f"{root}_{sf}" not in enhanced_ids]
```

- 每节点有 `llm_enhanced` 布尔标记
- 默认开启，跳过已增强节点
- 任务中断后可增量继续，避免重复处理

### 2.8 每批即时落盘（Checkpoint）

**`pipeline.py:157-160`**

```python
# 每批次完成后立即保存，防止中断丢失数据
if isinstance(graph_data.get("nodes"), list):
    graph_data["nodes"] = list(nodes_dict.values())
_save_graph(graph_data, output_path)
```

每 1 批（非每 N 批）落盘一次。~200 批 → 写盘 200 次，JSON 序列化 < 100ms，IO 开销可忽略，换取"进程崩溃最多丢失 1 批"的鲁棒性。

### 2.9 按字符数智能分批

**`enhancer.py:293-329`**

```python
def create_batches(source_files, base_dir, max_chars=MAX_BATCH_CHARS):
    # MAX_BATCH_CHARS 默认 40000（环境变量可调）
    # - 超大文件（> max_chars）单独成批，不截断
    # - 常规文件按累加字符数分批，不超 max_chars
    # - 空文件直接跳过
```

40000 字符 ≈ 15000-20000 tokens，留有足够余量给 prompt 固定开销和 LLM 输出空间。

### 2.10 CLI 参数

**`build_cli.py:229-243`**

```
enhance-graph:
  --max-workers   (default 5)
  --batch-limit   (default 0 = 不限制)

build:
  --max-workers   (default 5)
  --batch-limit   (default 0 = 不限制)
```

---

## 三、Card LLM 增强引擎

> 涉及文件：`card/builder/build_index_v3.py`

### 3.1 与 Graph 侧的架构差异

| 维度 | Graph 侧 | Card 侧 |
|------|---------|---------|
| LLM 调用 | 原生 urllib + threading.Thread | `OpenAICompatClient` 封装 |
| 并发模型 | ThreadPoolExecutor(max_workers=5) | ThreadPoolExecutor(max_workers=llm_concurrency, 默认 1) |
| 重试策略 | 固定 3 次 + 2s 间隔 | 指数退避（1s, 2s...）+ 最多 max_retries 次 |
| 错误分类 | 无（仅 None 判定） | HTTP 状态码分级：402/401 → stop；429/5xx → retry |
| 批次策略 | 按字符数分批 | 按文档条数分批（DEFAULT_LLM_BATCH_SIZE=6） |

### 3.2 HTTP 错误分级分类

**`build_index_v3.py:1059-1082`** — `classify_llm_exception()`

| HTTP 状态码 | 分类 | retryable | stop_provider | 语义 |
|------------|------|-----------|---------------|------|
| 402 | quota_or_billing | ❌ | ✅ | 配额耗尽/欠费 |
| 401/403 | auth_or_permission | ❌ | ✅ | API Key 无效 |
| 429 | rate_limit | ✅ | ❌ | 限流，等待后重试 |
| 5xx | server_error | ✅ | ❌ | 服务端瞬时故障 |
| 其他 4xx | http_error | ❌ | ❌ | 请求无效 |

### 3.3 Provider 健康状态机

**`build_index_v3.py:2828-2838`**

```
healthy ──(stop_provider error)──▶ degraded ──(持续失败)──▶ stopped
                                       │
                                  (后续成功恢复)
                                       │
                                       ▼
                                    healthy
```

关键设计：
- **并发模式**（`llm_concurrency > 1`）：某 worker 遇 402 标记 `degraded` 而非 `stopped`，避免误判全局停机
- **串行模式**（`llm_concurrency = 1`）：遇 402 直接 `stopped` 并终止

### 3.4 指数退避重试

**`build_index_v3.py:1130-1146`**

```python
for attempt in range(self.config.max_retries + 1):
    try:
        with urllib.request.urlopen(req, timeout=self.config.timeout) as response:
            ...
    except (...) as exc:
        error_info = classify_llm_exception(exc)
        if not error_info.retryable or attempt >= self.config.max_retries:
            raise
    time.sleep(1.0 * (attempt + 1))   # 1s, 2s, 3s...
```

重试次数通过 `OPENAI_MAX_RETRIES` 环境变量配置（默认 2）。

### 3.5 并发控制与线程安全

**`build_index_v3.py:2956-2997`**

```python
from threading import Lock

client_lock = Lock()

if llm_concurrency > 1:
    with concurrent.futures.ThreadPoolExecutor(max_workers=llm_concurrency) as executor:
        ...
```

- `run_maintenance.py` 中默认 `llm_concurrency = 24`
- 高并发下每个 worker 独立创建 `OpenAICompatClient` 实例
- `Lock` 仅保护 stats 聚合写入（非热路径）

### 3.6 证据文档截断

**`build_index_v3.py:37-38, 2334-2389`**

```python
MAX_EVIDENCE_DOCS = 4       # 每次 LLM 调用最多引用 4 份原文
MAX_EVIDENCE_CHARS = 1800   # 每份原文最多截取 1800 字符
```

LLM prompt 包含原文档上下文，此限制确保 prompt 不超 token 上限。

### 3.7 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `LLM_CONCURRENCY` | 1 | 并发 worker 数 |
| `OPENAI_TIMEOUT` | 60 | 单次请求超时 (s) |
| `OPENAI_MAX_RETRIES` | 2 | 最大重试次数 |
| `OPENAI_TEMPERATURE` | 0.2 | LLM 温度 |
| `LLM_MAX_EVIDENCE_DOCS` | 4 | 最大引用文档数 |
| `LLM_MAX_EVIDENCE_CHARS` | 1800 | 最大引用字符数 |

---

## 四、双引擎共性设计原则

| 原则 | Graph 实现 | Card 实现 |
|------|-----------|-----------|
| 避免 SDK 依赖 | urllib + threading 原生实现 | 自建 `OpenAICompatClient` |
| 失败可恢复 | 断点续传 + 每批即时落盘 | llm-cache-dir 缓存 |
| 资源可预测 | 字符数分批 + total_timeout | 条数分批 + 证据截断 |
| 异常可诊断 | FAIL_ABORT 打印停止原因 | 三级健康状态 + 错误分类日志 |
| 环境变量全配置 | MAX_BATCH_CHARS / LLM_TIMEOUT / FAIL_ABORT | LLM_CONCURRENCY / OPENAI_TIMEOUT / OPENAI_MAX_RETRIES |
