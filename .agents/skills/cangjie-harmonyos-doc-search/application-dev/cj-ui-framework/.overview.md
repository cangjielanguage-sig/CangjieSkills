# cj-ui-framework

This directory serves as a specialized repository for the Cangjie UI framework (cj-ui-framework), specifically focusing on the API reference documentation for ArkUI's internal interfaces and binding mechanisms. It provides detailed technical insights into foundational UI components such as `CustomView` and `RemoteView`, alongside the critical `bind` function used for linking `@Builder` decorated functions to custom components. The content emphasizes advanced concepts including declarative UI definitions, observed properties, and dynamic UI construction for navigation elements. This documentation is strictly intended for system framework developers or advanced ArkUI developers operating within a Beta environment, as many classes are designated for internal use only and require deep technical understanding of the framework architecture to be utilized effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the core architecture of ArkUI internal interfaces and binding mechanisms within the Cangjie language environment → [框架接口/]
*   Review the API reference for `CustomView` and `RemoteView` base classes which serve as foundations for UI components → [框架接口/]
*   Learn about the `bind` function used for linking `@Builder` decorated functions to custom components → [框架接口/]
*   Study declarative UI definitions, observed properties, and dynamic UI construction for navigation elements → [框架接口/]

## Detailed Description

### 框架接口/

This directory contains API reference documentation for the ArkUI framework's internal interfaces and binding mechanisms within the Cangjie language environment. It primarily focuses on the `CustomView` and `RemoteView` base classes, which serve as foundations for UI components, and the `bind` function used for linking `@Builder` decorated functions to custom components.

Key concepts include declarative UI definitions, observed properties, and dynamic UI construction for navigation elements. These documents are strictly intended for system framework developers or advanced ArkUI developers working in a Beta environment, as many classes are marked for internal use only.