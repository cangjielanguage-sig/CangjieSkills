# 基础类型定义

This directory defines fundamental data types and classes for gesture recognition within the Cangjie ArkUI framework. It covers core components such as `EventTargetInfo` for accessing component metadata, the base `GestureRecognizer` class for managing gesture states, properties, and validity, and specialized recognizers like `PanGestureRecognizer` for handling drag interactions. Additionally, it includes enumerations for operational states (e.g., Ready, Detecting) and information classes for scrollable targets. These definitions are essential for developers building interactive user interfaces, providing the necessary building blocks to handle, inspect, and control gesture events effectively in applications targeting version 20 or later.

## Quick Navigation

**What do you want to learn?**

*   Access component metadata (like ID) during gesture handling → class_EventTargetInfo.md EventTargetInfo
*   Manage base gesture properties, states, tags, and validity → class_GestureRecognizer.md GestureRecognizer
*   Handle specific drag gestures, scrollable container info, or state enums → class_PanGestureRecognizer_3more.md PanGestureRecognizer & Related Types

## Detailed Description

### class_EventTargetInfo.md class_EventTargetInfo.md
This document provides API reference documentation for the Cangjie `EventTargetInfo` class, which represents information about components associated with gesture recognizers. The main sections cover the class definition and the `getId()` method, detailing its functionality to return the current component's identifier as a String. Key takeaways include the system capability requirement (`SystemCapability.ArkUI.ArkUI.Full`) and the class's availability starting from version 20. Intended for developers working with the ArkUI framework, this reference assists in accessing component metadata during gesture event handling.

### class_GestureRecognizer.md class_GestureRecognizer.md
This document provides API reference documentation for the Cangjie `GestureRecognizer` class, used for handling gesture recognition within the ArkUI framework. The content outlines the class initialization and a suite of methods designed to inspect and manage gesture properties. Key sections include functions to retrieve component information, gesture state, type, and tag, as well as methods to check validity and built-in status. Additionally, it details how to control the recognizer's active state using `isEnabled` and `setEnabled`. This technical reference targets Cangjie developers who need to implement or manipulate gesture interactions in their applications, assuming familiarity with ArkUI system capabilities.

### class_PanGestureRecognizer_3more.md class_PanGestureRecognizer_3more.md
This document provides API reference documentation for gesture recognition components in the Cangjie ArkUI framework. It details the `PanGestureRecognizer` class for handling drag gestures, the `ScrollableTargetInfo` class for managing scrollable container information, and the `GestureRecognizerState` enum defining various operational states. Key information includes methods to retrieve gesture options and determine if a scrollable component is at the top or bottom, alongside state definitions such as Ready, Detecting, and Blocked. Intended for developers building interactive user interfaces, this reference requires familiarity with the Cangjie language and ArkUI system capabilities.