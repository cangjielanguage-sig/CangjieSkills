<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-pow" parent="std.math.numeric.struct.bigint" -->
# BigInt.**

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func **(n: UInt64): BigInt
```

求 BigInt 的 n 次幂。

## 契约

参数：

- n: UInt64 - 指数。

返回值：

- BigInt - 幂运算结果。
