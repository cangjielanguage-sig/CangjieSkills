# class_ExtensionAbilityInfo

本目录包含关于 `ExtensionAbilityInfo` 类的仓颉（Cangjie）语言 API 参考文档。该类用于封装 ExtensionAbility 组件的配置信息，涵盖了类定义、系统能力以及多个只读属性，如应用包名、模块名、权限和元数据等。文档详细说明了如何通过 `getBundleInfoForSelf` 接口获取这些信息，并涉及 HAP 模块和资源 ID 等核心概念。主要面向使用仓颉语言进行 HarmonyOS 应用开发，需要查询和管理 ExtensionAbility 组件元数据及权限控制的开发者。

## 快速导航

- **您想了解类的整体定义与基础属性？**
  → 查看 class_ExtensionA_10more_db2dc8ca.md 获取类定义、系统能力及基础配置信息（如包名、类型、导出状态）。
- **您想查询具体的属性字段（如权限、模块名、元数据）？**
  → 查看 let_labelId_8more_423a3ca0.md 获取详细的只读属性列表（如 labelId, permissions, skills 等）及其使用规范。

## 详细说明

### class_ExtensionA_10more_db2dc8ca.md class_ExtensionA_10more_db2dc8ca.md
本文档是 Cangjie 语言的 API 参考文档，详细定义了 `ExtensionAbilityInfo` 类，用于封装 ExtensionAbility 的配置信息。文档涵盖了类定义、系统能力及多个只读属性，包括应用包名、应用信息、导出状态、类型及分身索引等。关键点在于该类通过 `getBundleInfoForSelf` 接口获取，包含资源 ID、权限和可见性等元数据。主要面向使用 Cangjie 语言进行应用开发，需要查询和管理 ExtensionAbility 组件信息的开发者。

### let_labelId_8more_423a3ca0.md let_labelId_8more_423a3ca0.md
本文档是 Cangjie 语言中 ExtensionAbility 相关属性的 API 参考文档，旨在定义 ExtensionAbility 的配置信息。主要涵盖八个只读属性：`labelId`、`metadata`、`moduleName`、`name`、`permissions`、`readPermission`、`skills` 及 `writePermission`。关键信息包括这些属性均属于 `BundleManager.BundleFramework.Core` 系统能力，起始版本为 22，涉及 HAP 模块、元数据及权限控制等概念。文档面向 HarmonyOS 应用开发者，用于查询 ExtensionAbility 的属性定义及使用规范。