<!-- cj-doc kind="guide-leaf" level="5" id="language.package.4-包导入.4-1-语法" parent="language.package.4-包导入" -->
# 4.1 语法

[← 4. 包导入](index.md)

`import` 可导入单个声明、同包内多个声明或通配导入；组合导入可在一条语句中引入多个包。

```cangjie cjtest=syntax id=syntax-f6fd04cdae-1 form=unit
import fullPkg.item           // 导入单个声明
import fullPkg.{a, b}         // 导入多个声明
import fullPkg.*              // 导入所有可见声明
import {pkg1.*, pkg2.*}       // 同时导入多个包
```
