<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.json.stream.enum.jsontoken.operator-ne" parent="stdx.encoding.json.stream.enum.jsontoken" -->
# JsonToken.!=

[← JsonToken](index.md)

## 签名

```cangjie role=signature
public operator func !=(that: JsonToken): Bool
```

判不等。

## 契约

参数：

- that: JsonToken - 被比较的 JsonToken 对象

返回值：

- Bool - 当前实例与 that 不相等返回 true，否则返回 false
