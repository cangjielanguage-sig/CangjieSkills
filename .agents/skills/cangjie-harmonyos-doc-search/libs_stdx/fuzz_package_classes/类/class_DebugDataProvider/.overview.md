# class_DebugDataProvider

This directory contains comprehensive API reference documentation for the Cangjie `DebugDataProvider` class, a specialized component within a fuzzing framework designed to extend standard `FuzzDataProvider` functionality with advanced debugging capabilities. The documentation thoroughly covers methods for consuming, retrieving, and converting raw test data into a wide array of primitive types, including integers, floating-point numbers, booleans, characters, and strings, as well as their array counterparts. It details critical implementation aspects such as method signatures, parameter constraints, return types, and specific exception handling protocols like `IllegalArgumentException`. This resource is essential for developers and QA engineers working with the Cangjie fuzzing framework who require precise control over data generation and transformation during the testing and debugging process.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class structure and basic data consumption methods (e.g., `consumeBool`, `consumeInt16`, `consumeFloat32`) → class_DebugDataProvider_16more.md
*   Access specific functions for integer types (Int8-64), unsigned integers, characters, strings, and array variants, or learn about the static wrap utility → func_consumeInt64sInt64_14more.md

## Detailed Description

### class_DebugDataProvider_16more.md class_DebugDataProvider_16more.md
This document provides API reference documentation for the Cangjie class `DebugDataProvider`, which extends `FuzzDataProvider` to incorporate debugging capabilities. The content details a comprehensive set of overridden methods for consuming data, including `consumeAll`, `consumeBool`, `consumeInt16`, `consumeFloat32`, and their array variants. Key information includes method signatures, parameter descriptions, return types, and specific exceptions like `IllegalArgumentException` for invalid inputs. This reference guide targets developers utilizing the fuzzing framework who need to understand how to retrieve and convert test data into various primitive types and strings. It serves as a foundational resource for mastering the class's extended capabilities over the base data provider.

### func_consumeInt64sInt64_14more.md func_consumeInt64sInt64_14more.md
This API reference documentation details a set of data consumption functions in the Cangjie programming language, specifically for a `DebugDataProvider` class used in fuzzing contexts. The file covers methods for consuming primitive data types (Int8, Int16, Int32, Int64, UInt8, UInt16, UInt32, UInt64), characters (Rune), and strings, both as single instances and arrays. It also includes a static wrap utility function. Key information includes that array-consuming functions accept a count parameter and throw `IllegalArgumentException` for negative inputs, while the wrap function converts a `FuzzDataProvider` into a `DebugDataProvider`. This document is intended for developers implementing or debugging fuzzing tests who need to generate or transform specific data types from a data provider. It is particularly useful for handling complex data type requirements and ensuring proper data provider initialization.