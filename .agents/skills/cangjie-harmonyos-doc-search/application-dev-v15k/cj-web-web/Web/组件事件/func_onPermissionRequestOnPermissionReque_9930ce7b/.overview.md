# func_onPermissionRequestOnPermissionReque_9930ce7b

This directory provides a specialized technical overview focused on managing media permissions within the HarmonyOS development environment. It centers on the `onPermissionRequest` function, a critical component for handling security requests when web content hosted in a `WebviewController` attempts to access hardware features such as the camera and microphone. The documentation is tailored for developers using the Cangjie language, offering insights into system capabilities, permission definitions, and the implementation of user consent dialogs via `AbilityAccessCtrl`. Complementing the native implementation, the directory also includes a web-centric guide demonstrating the HTML5 MediaDevices API for capturing user media streams. This dual perspective makes it a valuable reference for building secure, interactive applications that require seamless integration between native OS controls and web-based multimedia functionalities.

## Quick Navigation

**What do you want to learn?**

- Handle permission requests in a HarmonyOS WebviewController using Cangjie → func_onPermissionRequestOnPermissionReque_9930ce7b_1.md
- Implement camera and microphone access using HTML5 and JavaScript → func_onPermissionRequestOnPermissionReque_9930ce7b_2.md

## Detailed Description

### func_onPermissionRequestOnPermissionReque_9930ce7b_1.md
This API reference document details the `onPermissionRequest` function used to handle permission requests within a WebviewController component in Cangjie applications. It covers the function signature, required system permissions (Camera and Microphone), system capabilities, and the callback parameter definition. Key information includes a practical code example demonstrating how to register the callback, request necessary system permissions using `AbilityAccessCtrl`, and display an `AlertDialog` to allow users to grant or deny the web resource's access. This guide targets HarmonyOS developers using the Cangjie language who need to manage webview interactions and security permissions.

### func_onPermissionRequestOnPermissionReque_9930ce7b_2.md
This document provides a practical code example demonstrating how to access and stream user media, specifically video and audio, using the HTML5 MediaDevices API. It covers the HTML setup for video and canvas elements and the JavaScript implementation of the getMedia function. The core logic involves defining media constraints, calling navigator.mediaDevices.getUserMedia, and handling the resulting Promise to attach the MediaStream to a video element. This tutorial is intended for web developers seeking to implement camera and microphone functionality in browser-based applications.