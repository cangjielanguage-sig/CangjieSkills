<!-- cj-doc kind="api-member" level="6" id="std.unittest.common.class.prettytext.init" parent="std.unittest.common.class.prettytext" -->
# PrettyText.init

[← PrettyText](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

默认构造器，生成一个空的对象。

## init(String)

### 签名

```cangjie role=signature
public init(string: String)
```

构造器，生成一个以入参开头的文本构造器。

### 契约

参数：

- string: String - 希望放入打印文本开头的字符串。
