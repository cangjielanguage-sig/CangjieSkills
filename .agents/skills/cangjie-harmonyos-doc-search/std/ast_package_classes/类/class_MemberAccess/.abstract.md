This directory provides comprehensive API documentation for the `MemberAccess` class and related utility functions within the Cangjie language's Abstract Syntax Tree (AST) implementation. It details the structural definition of member access expressions, such as `T.a`, covering properties like `baseExpr`, `field`, `dot`, and generic components like `typeArguments`. Additionally, it explains critical AST manipulation mechanisms, including converting nodes back into token streams and traversing the syntax tree using the Visitor pattern. This documentation is specifically curated for compiler developers, language tooling engineers, and programmers who require a deep understanding of AST node construction, serialization, and traversal to build robust parsing or code generation tools for the Cangjie language.