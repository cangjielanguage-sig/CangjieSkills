# class_BundleManager

This directory contains essential API reference documentation for the `BundleManager` class as implemented in the Cangjie language for HarmonyOS development. It serves as a comprehensive technical guide for developers looking to interact with the system's bundle management layer, covering tasks such as querying bundle information, checking URI scheme capabilities, and extracting JSON configuration profiles. The documentation details specific static methods including `canOpenLink`, `getBundleInfoForSelf`, and `getProfileByAbility`, providing complete function signatures, parameter definitions, return value specifications, and error handling via `BusinessException`. Furthermore, it includes practical code examples to assist in implementation. This content is specifically tailored for HarmonyOS application developers who require programmatic access to application metadata and bundle resources to build robust, system-integrated applications.

## Quick Navigation

**What do you want to do?**

*   **Verify link schemes and get current app info** → class_BundleManager_3more.md (Covers `canOpenLink` and `getBundleInfoForSelf` methods)
*   **Access JSON configuration for abilities** → static_func_getProfileByAbilityString_Str_46b45474.md (Covers `getProfileByAbility` and resource reference formats)

## Detailed Description

The following files provide in-depth specifications and usage examples for the `BundleManager` class and its associated functions in the Cangjie language.

### class_BundleManager_3more.md class_BundleManager_3more.md
This document serves as API reference documentation for the Cangjie language's BundleManager class, focusing on bundle information queries. It details two primary static methods: canOpenLink, which determines if a specific link scheme is accessible, and getBundleInfoForSelf, which retrieves the current application's BundleInfo based on specified flags. Key sections include function signatures, parameter descriptions, return values, potential BusinessException errors, and practical code examples. This guide targets HarmonyOS application developers using the Cangjie language who need to manage bundle capabilities and access application metadata programmatically.

### static_func_getProfileByAbilityString_Str_46b45474.md static_func_getProfileByAbilityString_Str_46b45474.md
This document serves as API reference documentation for the Cangjie static function getProfileByAbility, which retrieves JSON configuration strings based on module and ability names. Key sections include the function signature, system capabilities, parameter descriptions, return values, and specific BusinessException error codes. Important takeaways note that the function returns an array of strings and preserves resource reference formats like $string:res_id. Intended for HarmonyOS developers using the BundleManager framework, this guide helps access application metadata and handle potential errors such as missing modules or disabled abilities.