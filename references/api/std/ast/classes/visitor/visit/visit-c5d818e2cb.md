<!-- cj-doc kind="api-member" level="7" id="std.ast.class.visitor.visit.visit-c5d818e2cb" parent="std.ast.class.visitor.visit" -->
# Visitor.func visit(WildcardPattern)

[← Visitor.visit](index.md)

## 签名

```cangjie role=signature
protected open func visit(_: WildcardPattern): Unit
```

定义访问节点时的操作，需要重写。

## 契约

参数：

- _: WildcardPattern - WildcardPattern 类型的被遍历节点。
