# class_GZip

This directory contains API reference documentation for the GZip class within the Cangjie programming language, specifically designed for the BasicServicesKit. It provides comprehensive details on interfaces for gzip file compression and decompression, leveraging the underlying Zlib subsystem. The documentation covers essential operations such as opening, reading, writing, and closing compressed files, with a focus on handling byte arrays, file descriptors, and specific error codes. This resource is suitable for developers who need to implement efficient data compression workflows or manage compressed file streams in Cangjie applications.

## Quick Navigation

**What do you want to learn?**

-   Manage file lifecycle (close files, associate descriptors, check end-of-file) → class_GZip_4more.md
-   Read lines or write strings to gzip files → func_gzgetsArrayByte_3more.md
-   Read or write raw byte arrays (bulk data processing) → func_gzreadArrayByte_2more.md

## Detailed Description

### class_GZip_4more.md class_GZip_4more.md
This document serves as API reference documentation for the GZip class in the Cangjie programming language, outlining interfaces for gzip file compression and decompression. The content details three primary functions: `gzclose` for closing files and releasing resources, `gzdopen` for associating file descriptors with gzip operations, and `gzeof` for checking end-of-file status. Key information includes method signatures, parameter descriptions, return values, and specific error codes associated with the Zlib subsystem. This guide is intended for developers working with the BasicServicesKit who need to handle compressed file streams, requiring an understanding of file descriptors and exception handling.

### func_gzgetsArrayByte_3more.md func_gzgetsArrayByte_3more.md
This document provides API reference documentation for three specific GZip file manipulation functions in the Cangjie programming language. It details `gzgets` for reading decompressed lines into a byte array, `gzopen` for opening gzip files with specific modes, and `gzputs` for writing compressed strings. Key information includes function syntax, required parameters, return types, and specific `BusinessException` error codes like internal structure errors. The content is designed for developers utilizing the Zlib subsystem to handle file compression and decompression tasks, featuring practical code examples using the BasicServicesKit.

### func_gzreadArrayByte_2more.md func_gzreadArrayByte_2more.md
This document serves as API reference documentation for two specific Zlib functions in the Cangjie programming language: `gzread` and `gzwrite`. The content is divided into two main sections, each detailing a function's signature, functionality, parameters, return values, potential exceptions, and usage examples. Key takeaways include that `gzread` decompresses file data into a byte array while `gzwrite` compresses byte array data to a file, both returning the count of processed bytes and handling specific error codes like internal structure errors. This guide targets developers working with file compression and decompression using the BasicServicesKit, requiring knowledge of Cangjie syntax and file I/O operations.