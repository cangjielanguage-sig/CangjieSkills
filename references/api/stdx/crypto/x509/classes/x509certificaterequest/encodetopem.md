<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.x509.class.x509certificaterequest.encodetopem" parent="stdx.crypto.x509.class.x509certificaterequest" -->
# X509CertificateRequest.encodeToPem

[← X509CertificateRequest](index.md)

## 签名

```cangjie role=signature
public func encodeToPem(): PemEntry
```

将数字证书签名请求编码成 PEM 格式。

## 契约

返回值：

- PemEntry - 编码后的 PEM 格式的数字证书签名请求。
