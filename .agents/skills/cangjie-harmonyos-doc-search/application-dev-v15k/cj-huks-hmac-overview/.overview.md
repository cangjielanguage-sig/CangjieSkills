# cj-huks-hmac-overview

This directory provides a comprehensive technical reference for implementing HMAC (Hash-based Message Authentication Code) within the HUKS (HarmonyOS Universal Keystore Store) framework. It covers the fundamental definitions of MAC and HMAC, emphasizing their critical role in verifying information integrity across unreliable media. The documentation details the specific cryptographic algorithms supported by HUKS, including a detailed table of supported digest algorithms such as SHA256, SHA384, SHA512, and SM3. Furthermore, it specifies corresponding key length ranges and the minimum API level requirements (API 15+). This resource is essential for developers and security engineers who require a deep understanding of HMAC capabilities and constraints to secure their applications effectively and ensure compliance with system specifications.

## Quick Navigation

**What do you want to learn?**
*   Understand the fundamental definitions of MAC and HMAC → cj-huks-hmac-overview.md
*   Learn how HMAC is used to verify information integrity on unreliable media → cj-huks-hmac-overview.md
*   Review the specific cryptographic algorithms supported by the HUKS framework → cj-huks-hmac-overview.md

**What do you want to verify?**
*   Check the table of supported digest algorithms (SHA256, SHA384, SHA512, SM3) → cj-huks-hmac-overview.md
*   Identify the corresponding key length ranges for each supported algorithm → cj-huks-hmac-overview.md
*   Confirm the minimum API level requirement (API 15+) for implementation → cj-huks-hmac-overview.md

## Detailed Description

This section offers an in-depth analysis of the files provided in the directory. The primary focus is on the technical reference material that outlines the integration of HMAC (Hash-based Message Authentication Code) into the HUKS framework. By examining the content below, users can gain a thorough understanding of the theoretical underpinnings of message authentication codes, the practical constraints of key lengths, and the specific algorithmic support provided by the system. This ensures that developers are well-equipped to implement robust security features that maintain data integrity across various transmission media.

### cj-huks-hmac-overview.md cj-huks-hmac-overview.md

This document serves as a technical reference overview introducing HMAC (Hash-based Message Authentication Code) and its specifications within the HUKS framework. It covers the definition of MAC and HMAC, explaining their role in verifying information integrity on unreliable media. The content details the specific cryptographic algorithms supported by HUKS, providing a table listing supported digest algorithms (SHA256, SHA384, SHA512, SM3), their corresponding key length ranges, and the minimum API level requirement (API 15+). This guide is intended for developers or security engineers implementing cryptographic operations who need to understand HMAC capabilities and constraints.