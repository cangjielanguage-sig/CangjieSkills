# class_TcpServerSocket

This directory contains API reference documentation for the `TcpServerSocket` class within the Cangjie programming language's network package. It covers the implementation of TCP servers, including initialization, connection listening, and socket configuration. Key topics include socket lifecycle management (bind, accept, close), low-level option manipulation (SO_KEEPALIVE, reuseAddress), and exception handling. This documentation is suitable for network application developers and systems programmers who need to build robust server-side applications or manage low-level TCP socket behaviors and system-specific options in Cangjie.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition, properties, and initialization constraints → class_TcpServerSocket_9more.md
*   Manage socket options (get/set boolean or integer values) → func_getSocketOptionBoolIn_5more.md, func_setSocketOptionIntNat_2more.md
*   Handle server lifecycle: initialization, binding, accepting connections, and cleanup → initUInt16_6more.md

## Detailed Description

### class_TcpServerSocket_9more.md class_TcpServerSocket_9more.md
This document serves as API reference documentation for the TcpServerSocket class in the Cangjie programming language, detailing the implementation of a TCP server for listening to connections. The content covers the class definition, initialization methods accepting a SocketAddress or port, and various mutable properties for socket configuration such as backlogSize, receiveBufferSize, reuseAddress, and reusePort. Key information includes the requirement to call bind() to start listening, the need to explicitly close the socket, and specific constraints where properties like backlogSize must be set before binding. This documentation targets network application developers who need to manage low-level TCP socket behaviors and system-specific options.

### func_getSocketOptionBoolIn_5more.md func_getSocketOptionBoolIn_5more.md
This document serves as API reference documentation for socket management functions in the Cangjie programming language. It details five specific functions: getSocketOptionBool, getSocketOptionIntNative, isClosed, setSocketOption, and setSocketOptionBool. Key takeaways include the ability to retrieve and modify socket parameters using levels like SOL_SOCKET and options like SO_KEEPALIVE. Boolean conversions map zero to false and non-zero to true. All functions may throw SocketException upon failure. The target audience is network developers using Cangjie who need low-level socket control.

### func_setSocketOptionIntNat_2more.md func_setSocketOptionIntNat_2more.md
This API reference document details specific functions for managing socket configurations and status representation within the Cangjie network package. The content primarily covers the setSocketOptionIntNative function, which allows developers to set socket parameters such as SO_KEEPALIVE by specifying the level, option name, and integer value. It also documents the toString method, which returns a string representation of the current TcpServerSocket status. Key information includes parameter definitions for Int32 and IntNative types, potential SocketException errors, and return values. This documentation is intended for Cangjie developers working on network programming who require precise control over socket behavior and debugging capabilities.

### initUInt16_6more.md initUInt16_6more.md
This document provides API reference details for the TcpServerSocket class in the Cangjie net package, focusing on server-side socket lifecycle management. It covers the constructor init(UInt16), connection handling via accept (blocking and timeout versions), port binding, resource cleanup with close, and low-level configuration via getSocketOption. Key information includes that initialization allows specifying a port or using a random one, while accept methods handle client connections with specific exceptions for timeouts and system failures. The bind function is critical for activation but does not support retries. This reference guide targets Cangjie developers implementing network servers who need to manage TCP connections, handle exceptions, and configure socket options.