<!-- cj-doc kind="api-member" level="6" id="std.math.numeric.struct.bigint.operator-eq" parent="std.math.numeric.struct.bigint" -->
# BigInt.==

[← BigInt](index.md)

## 签名

```cangjie role=signature
public operator func ==(that: BigInt): Bool
```

判等运算。

## 契约

参数：

- that: BigInt - 判等运算的另一个 BigInt。

返回值：

- Bool - 判等的结果。相等返回 true，不等返回 false。
