# 共享元素转场一镜到底

This directory serves as a comprehensive technical guide to implementing shared element transitions, often referred to as "one-shot" effects, within the ArkUI framework. It covers essential techniques for creating smooth, continuous animations where UI elements seamlessly morph between different states or views without visual interruption. Key topics include direct component property manipulation for performance optimization and the use of the advanced `geometryTransition` API for complex view hierarchies. This resource is ideal for HarmonyOS developers and UI engineers seeking to enhance user experience with fluid visual continuity, master state-driven layout changes, and implement professional-grade navigation logic using ArkTS and Cangjie.

## Quick Navigation

- **What do you want to do?**
    - Understand the core concepts and comparison of implementation methods → 共享元素转场一镜到底.md
    - Implement transitions by modifying existing component properties directly (no new instances) → `不新建组件并直接变化原组件/`
    - Implement transitions using the system `geometryTransition` API for complex scenarios → `使用geometryTransition共享元素转场/`

## Detailed Description

### 共享元素转场一镜到底.md 共享元素转场一镜到底.md
This technical guide defines shared element transitions, also known as one-shot effects, and compares implementation methods for creating smooth UI animations. The document covers the concept of matching element positions and sizes during interface switches, providing visual examples and a detailed comparison of two approaches. Key information includes the distinction between directly modifying the original container for simple, no-route-jump scenarios and using the geometryTransition API. The latter utilizes system capabilities to automatically handle size, position, and opacity changes between components bound by the same ID. This guide targets UI developers looking to implement natural, fluid transitions in their applications, offering advice on selecting the appropriate method based on performance and complexity requirements.

### 不新建组件并直接变化原组件/
This directory focuses on advanced UI animation techniques within the ArkUI and ArkTS frameworks, specifically emphasizing the strategy of modifying existing component properties directly rather than creating new component instances to achieve smooth transitions. It covers the implementation of "one-shot" transition effects, state-driven layout changes, and asymmetric transitions using core functions like `animateTo` and `TransitionEffect`. The content is ideal for HarmonyOS developers and UI engineers looking to optimize performance and enhance user experience in expandable lists, cards, and interactive elements without restructuring the component tree.

### 使用geometryTransition共享元素转场/
This directory serves as a comprehensive technical resource for HarmonyOS developers aiming to master shared element transitions using the `geometryTransition` API within the ArkUI framework. It thoroughly covers the fundamental principles of binding components via identical IDs to generate smooth "one-shot" animations during view state changes. The content ranges from basic implementation, such as swapping images and containers, to advanced scenarios involving modal transitions and list interactions. With detailed code examples provided in both Cangjie and ArkTS, this collection is essential for developers seeking to create fluid, professional-grade user interfaces and seamless navigation experiences. It specifically addresses the nuances of animating between different objects and the necessary configuration of transition effects to ensure visual continuity across different view hierarchies.