<!-- cj-doc kind="api-member" level="6" id="std.core.struct.string.removeprefix" parent="std.core.struct.string" -->
# String.removePrefix

[← String](index.md)

## 签名

```cangjie role=signature
public func removePrefix(prefix: String): String
```

去除字符串的 prefix 前缀。

## 契约

参数：

- prefix: String - 待去除的前缀。

返回值：

- String - 去除前缀后得到的新字符串。
