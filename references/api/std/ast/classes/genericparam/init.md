<!-- cj-doc kind="api-member" level="6" id="std.ast.class.genericparam.init" parent="std.ast.class.genericparam" -->
# GenericParam.init

[← GenericParam](index.md)

本页汇总 2 个同名重载；先按签名选择，再读取对应契约。

## init()

### 签名

```cangjie role=signature
public init()
```

构造一个默认的 GenericParam 对象。

## init(Tokens)

### 签名

```cangjie role=signature
public init(parameters: Tokens)
```

构造一个 GenericParam 对象。

### 契约

参数：

- parameters: Tokens - 将要构造 GenericParam 的类型形参的词法单元集合 (Tokens)。
