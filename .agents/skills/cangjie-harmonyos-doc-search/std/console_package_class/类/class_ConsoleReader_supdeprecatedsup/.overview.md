# class_ConsoleReader_supdeprecatedsup

This directory provides comprehensive API reference documentation for the deprecated `ConsoleReader` class and associated standard input utilities within the Cangjie programming language. It focuses on the mechanisms for reading, converting, and processing console input, including detailed explanations of EOF behavior, synchronous buffered operations, and specific methods such as `read()`, `readln()`, `readToEnd`, and `readUntil`. The documentation highlights critical considerations like UTF-8 truncation risks and exception handling strategies. It is specifically designed for Cangjie developers who are maintaining legacy systems or require a deep understanding of console input streams, assuming a solid foundation in basic I/O operations and data types like `Rune` and `String`. The content serves as a vital resource for understanding the transition to the `env` package and handling legacy input stream logic.

## Quick Navigation

**What do you want to learn?**

*   Understand the deprecated `ConsoleReader` class, its singleton structure, and methods like `read()` and `readln()` → class_ConsoleReader_supdep_4more.md
*   Learn about standard input functions such as `readToEnd` and `readUntil` for reading until specific conditions → func_readToEnd_3more.md

## Detailed Description

### class_ConsoleReader_supdep_4more.md
This document serves as API reference documentation for the deprecated ConsoleReader class in the Cangjie programming language, detailing how to read and convert console input. It covers the class definition, instantiation via Console.stdIn, EOF behavior, and specific methods including read(), read(Array<Byte>), and readln(). Key information includes the class being a singleton extending InputStream with synchronous, buffered operations. It highlights deprecation in favor of the env package version and warns about UTF-8 truncation risks in byte reading and exception handling in character reading. This reference is intended for Cangjie developers maintaining legacy code or understanding console input streams, requiring knowledge of basic I/O operations and UTF-8 encoding.

### func_readToEnd_3more.md
This document serves as API reference documentation for standard input reading functions in the Cangjie programming language. It details three specific functions: readToEnd, readUntil accepting a predicate, and readUntil accepting a specific character. Key information includes that these functions read from standard input until conditions like EOF or a matching character are met, returning optional ?String types. Notably, readToEnd handles non-UTF-8 input without throwing exceptions. This guide targets Cangjie developers requiring console input handling, assuming familiarity with types like Rune and String.