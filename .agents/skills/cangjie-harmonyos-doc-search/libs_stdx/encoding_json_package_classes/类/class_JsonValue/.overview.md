# class_JsonValue

This directory contains comprehensive API reference documentation for the `JsonValue` class within the Cangjie language's `encoding_json` package. It serves as the fundamental data layer for handling JSON data, covering essential operations such as parsing strings into JSON objects, converting between data types, and serializing objects back into strings. Key topics include the `fromStr` method for parsing strings with various number formats (binary, octal, hexadecimal), instance methods for type casting (e.g., `asArray`, `asBool`, `asInt`), and serialization functions like `toJsonString`. The documentation is specifically suitable for Cangjie developers who need to parse, validate, transform, or serialize JSON data structures in their applications, with a strong emphasis on exception handling (`JsonException`) and type inspection mechanisms. This resource is essential for ensuring robust data interchange in Cangjie-based software projects.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition and parsing logic → class_JsonValue_5more.md
*   Learn about type conversion methods (Int, String, Object, Null) → func_asInt_7more.md
*   Explore serialization and formatted string generation → func_asInt_7more.md
*   Inspect JSON types and handle exceptions → class_JsonValue_5more.md, func_asInt_7more.md
*   Parse strings with specific number formats (binary, octal, hex) → class_JsonValue_5more.md
*   Understand escape sequences and error handling → class_JsonValue_5more.md

## Detailed Description

### class_JsonValue_5more.md class_JsonValue_5more.md

This API reference document details the `JsonValue` class in the Cangjie language, serving as the core data layer for converting between JSON objects and strings. It covers the class definition, the static `fromStr` method for parsing strings with specific number formats and escape sequences, and instance methods like `asArray`, `asBool`, and `asFloat` for type conversion. Key information includes support for binary, octal, and hexadecimal number prefixes during parsing and the throwing of `JsonException` on errors. This documentation targets Cangjie developers who need to parse, validate, or transform JSON data structures within their applications.

### func_asInt_7more.md func_asInt_7more.md

This document serves as API reference documentation for the Cangjie language's encoding_json package, detailing methods within the JsonValue class. It covers type conversion functions including asInt, asNull, asObject, and asString, alongside type inspection via kind and serialization methods like toJsonString and toString. Key information includes that conversion methods return specific JSON types but throw JsonException if the conversion fails. The toJsonString method specifically generates formatted JSON strings. This reference is intended for developers utilizing the Cangjie standard library to parse, manipulate, and serialize JSON data, requiring knowledge of the JsonValue class structure and exception handling.