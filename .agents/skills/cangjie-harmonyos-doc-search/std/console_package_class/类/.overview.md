# 类

This directory contains API reference documentation for deprecated classes within the Cangjie programming language, specifically focusing on console input and output (I/O) operations. It covers the `Console`, `ConsoleReader`, and `ConsoleWriter` classes, detailing their interfaces for standard streams, synchronous buffered reading, and thread-safe writing. The content emphasizes deprecation notices and migration paths to the `env` package. This documentation is suitable for Cangjie developers maintaining legacy systems or requiring a deep understanding of historical console stream handling, data types like `Rune` and `String`, and exception strategies.

## Quick Navigation

- **What do you want to learn?**
    - About the main deprecated Console class and its static properties → class_Console_supdeprecatedsup.md
    - How to handle standard input streams and reading operations → [2]
    - How to handle standard output/error streams and writing operations → [3]

## Detailed Description

### class_Console_supdeprecatedsup.md class_Console_supdeprecatedsup.md
This API reference document outlines the deprecated Console class in the Cangjie programming language, designed to provide interfaces for standard input, output, and error streams. The content details three static properties: stdIn for retrieving standard input via ConsoleReader, and stdOut and stdErr for output operations via ConsoleWriter. A critical takeaway is the deprecation notice, which instructs developers to replace this class with functions from the env package in future versions. This technical documentation targets developers managing console I/O, requiring familiarity with Cangjie syntax and stream handling.

### [2] class_ConsoleReader_supdeprecatedsup/
This directory provides comprehensive API reference documentation for the deprecated `ConsoleReader` class and associated standard input utilities within the Cangjie programming language. It focuses on the mechanisms for reading, converting, and processing console input, including detailed explanations of EOF behavior, synchronous buffered operations, and specific methods such as `read()`, `readln()`, `readToEnd`, and `readUntil`. The documentation highlights critical considerations like UTF-8 truncation risks and exception handling strategies. It is specifically designed for Cangjie developers who are maintaining legacy systems or require a deep understanding of console input streams, assuming a solid foundation in basic I/O operations and data types like `Rune` and `String`. The content serves as a vital resource for understanding the transition to the `env` package and handling legacy input stream logic.

### [3] class_ConsoleWriter_supdeprecatedsup/
This directory provides comprehensive API reference documentation for the deprecated `ConsoleWriter` class and related console output functions within the Cangjie programming language. It focuses on thread-safe mechanisms for writing to standard output and error streams, covering a wide array of overloaded methods for data types such as Byte arrays, Booleans, Floats, Integers, and Runes. Additionally, it distinguishes between standard write operations and those that append newlines. This documentation is essential for Cangjie developers maintaining legacy codebases or seeking to understand the migration path to the `env` package for console operations.