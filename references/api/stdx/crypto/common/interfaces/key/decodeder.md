<!-- cj-doc kind="api-member" level="6" id="stdx.crypto.common.interface.key.decodeder" parent="stdx.crypto.common.interface.key" -->
# Key.decodeDer

[← Key](index.md)

## 签名

```cangjie role=signature
static func decodeDer(encoded: DerBlob): Key
```

将密钥从 DER 格式解码。

## 参数

- encoded: DerBlob - DER 格式的对象。

## 返回值

- Key - 由 DER 格式解码出的密钥。

