# cj-apis-sercurity-permission_request_result

This directory contains API reference documentation for the Cangjie `PermissionRequestResult` class, a critical element for developers building applications on the HarmonyOS platform using the AbilityKit. The documentation focuses on the data structure used to encapsulate the results of system permission requests, providing a clear blueprint of the class's internal architecture. It thoroughly explains the three public properties: `permissions` (the list of requested permissions), `authResults` (the status codes indicating access status), and `dialogShownResults` (reflecting whether the system dialog was displayed). Additionally, the document provides a detailed breakdown of the integer values used within `authResults`, allowing developers to distinguish between successful grants, denials, and invalid request scenarios. This resource is tailored for HarmonyOS developers who need to manage application context and enforce secure user authorization protocols. Prerequisites include familiarity with the HarmonyOS application context and the AbilityKit framework.

## Quick Navigation

**What do you want to learn?**

*   **Explore the Class Definition:** Understand the core structure and purpose of the `PermissionRequestResult` class within the Cangjie environment → cj-apis-sercurity-permission_request_result.md
*   **Analyze Public Properties:** Review detailed explanations of the `permissions`, `authResults`, and `dialogShownResults` properties to understand what data they return → cj-apis-sercurity-permission_request_result.md
*   **Decode Authorization Status:** Look up the specific integer values used in `authResults` to identify if a permission was granted, denied, or if the request was invalid → cj-apis-sercurity-permission_request_result.md
*   **Implement Security Logic:** Learn how to apply this API reference to handle user authorization decisions and manage UI dialog states effectively → cj-apis-sercurity-permission_request_result.md

## Detailed Description

### cj-apis-sercurity-permission_request_result.md
This document serves as API reference documentation for the Cangjie class `PermissionRequestResult`, which encapsulates the outcome of system permission requests. It details the class structure and explains its three public properties: `permissions` (requested list), `authResults` (authorization status codes), and `dialogShownResults` (UI visibility). Key information includes the specific integer values for `authResults`, distinguishing between granted, denied, and invalid request states. Intended for HarmonyOS developers using the AbilityKit, this guide requires knowledge of application context and is essential for handling user authorization logic securely.