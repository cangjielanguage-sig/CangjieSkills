# 使用AES对称密钥GCM模式分段加解密

This directory provides a comprehensive guide and code examples for implementing segmented encryption and decryption using AES symmetric keys in GCM mode. It covers essential cryptographic operations such as key generation, Cipher instance creation, GCM parameter specification, and chunked data processing using the CryptoArchitectureKit. Key topics include handling initialization vectors (IV), authentication tags (authTag), and multi-part updates. Designed for developers working with the Cangjie programming language, these resources address the need to securely process large data volumes while maintaining high security standards.

## Quick Navigation

**What do you want to learn?**

*   Understand the complete workflow for segmented encryption and decryption → **使用AES对称密钥GCM模式分段加解密_2more.md**
*   Review a complete synchronous code example in Cangjie → **示例.md**
*   Learn specific steps for implementing AES128-GCM decryption → **解密.md**

## Detailed Description

### 使用AES对称密钥GCM模式分段加解密_2more.md 使用AES对称密钥GCM模式分段加解密_2more.md
This technical tutorial guides developers through performing segmented encryption and decryption using AES symmetric keys in GCM mode. The document details the encryption workflow, covering key generation, Cipher instance creation, initialization with GCM parameters, segmented data updates via the update method, and finalization using doFinal. Key takeaways include specific API calls like createSymKeyGenerator and createCipher, explanations of segmented processing behavior for different modes, and the necessity of extracting the 16-byte authTag for decryption verification. Intended for developers using the CryptoArchitectureKit, this guide addresses the need to process large data in chunks while maintaining security standards.

### 示例.md 示例.md
This document provides a synchronous code example demonstrating AES-128 GCM encryption and decryption using the Cangjie programming language. It covers key functions such as random number generation, GCM parameter specification, symmetric key conversion, and chunked data processing. The code illustrates the complete workflow from key generation through encryptMessage and decryptMessage functions to verification, utilizing the CryptoArchitectureKit. It targets developers seeking practical implementation guidance for cryptographic operations, specifically focusing on handling initialization vectors, authentication tags, and segmented data updates.

### 解密.md 解密.md
This document provides a step-by-step tutorial for performing AES128-GCM decryption using the CryptoArchitectureKit API. It details the process of creating a Cipher instance with specific algorithm parameters, initializing it in decryption mode using a symmetric key and GCM parameters, and processing data in chunks. Key steps include calling createCipher, initialize, update, and doFinal methods. The guide highlights handling multi-part data updates and finalizing the operation to retrieve the plaintext. Intended for developers implementing secure data decryption, this guide requires familiarity with cryptographic concepts like symmetric keys and cipher modes. Keywords: AES128, GCM, PKCS7, decryption, CryptoArchitectureKit, Cipher, SymKey, GcmParamsSpec.