# cj-start-navigation-apps

This directory serves as a comprehensive technical resource for HarmonyOS developers focusing on the integration and configuration of navigation capabilities using the `startAbilityByType` API. It covers the complete workflow, ranging from the client-side invocation of map features—such as route planning, active navigation, and location search—to the server-side configuration required to act as a target service provider. Key technical topics include detailed parameter construction using GCJ-02 coordinates, POI IDs, vehicle types, and the specific `module.json5` configurations needed to expose these features. The content is specifically tailored for developers proficient in the Cangjie language who need to implement or support complex map service interactions within the UIAbility lifecycle.

## Quick Navigation

**What do you want to learn?**

*   How to invoke navigation features like route planning and active navigation? → Go to `拉起导航类应用startAbilityByType/`
*   How to configure server-side settings to act as a map service provider? → Go to `拉起导航类应用startAbilityByType/`
*   How to construct parameters using GCJ-02 coordinates and POI IDs? → Go to `拉起导航类应用startAbilityByType/`
*   How to configure `module.json5` for navigation capabilities? → Go to `拉起导航类应用startAbilityByType/`

## Detailed Description

### 拉起导航类应用startAbilityByType/

This directory provides comprehensive technical documentation for HarmonyOS developers focusing on the integration and configuration of navigation capabilities using the `startAbilityByType` API. It covers the complete workflow, ranging from the client-side invocation of map features—such as route planning, active navigation, and location search—to the server-side configuration required to act as a target service provider.

Key technical topics include detailed parameter construction using GCJ-02 coordinates, POI IDs, vehicle types, and the specific `module.json5` configurations needed to expose these features. The content is specifically tailored for developers proficient in the Cangjie language who need to implement or support complex map service interactions within the UIAbility lifecycle. By following these guides, developers can ensure their applications correctly handle location data and user intents for navigation scenarios.