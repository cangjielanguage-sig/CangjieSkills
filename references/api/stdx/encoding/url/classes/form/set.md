<!-- cj-doc kind="api-member" level="6" id="stdx.encoding.url.class.form.set" parent="stdx.encoding.url.class.form" -->
# Form.set

[← Form](index.md)

## 签名

```cangjie role=signature
public func set(key: String, value: String): Unit
```

重置指定 key 对应的 value。

## 契约

参数：

- key: String - 指定键。
- value: String - 将指定键的值设置为 value。
